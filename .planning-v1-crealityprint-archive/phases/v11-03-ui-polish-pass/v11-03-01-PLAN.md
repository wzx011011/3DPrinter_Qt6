---
phase: v11-03-ui-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/Theme.qml
  - src/qml_gui/panels/SliceProgress.qml
  - src/qml_gui/pages/PreviewPage.qml
  - src/qml_gui/components/LayerSlider.qml
  - src/qml_gui/components/StatsPanel.qml
  - src/qml_gui/components/MoveSlider.qml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "SliceProgress panel uses Theme tokens for all non-semantic colors"
    - "PreviewPage header and panels use Theme tokens for all non-semantic colors"
    - "LayerSlider, StatsPanel, MoveSlider use Theme tokens for all non-semantic colors"
    - "Theme.qml has new bgTooltip, textMuted, borderInput, bgErrorSubtle, bgWarningSubtle, radiusXXL tokens"
    - "Build passes with zero QML warnings"
  artifacts:
    - path: "src/qml_gui/Theme.qml"
      provides: "6 new design tokens for recurring color patterns"
      contains: "bgTooltip"
    - path: "src/qml_gui/panels/SliceProgress.qml"
      provides: "Tokenized slice progress panel"
      min_lines: 500
    - path: "src/qml_gui/pages/PreviewPage.qml"
      provides: "Tokenized preview page"
      min_lines: 250
    - path: "src/qml_gui/components/LayerSlider.qml"
      provides: "Tokenized layer slider"
      min_lines: 350
  key_links:
    - from: "src/qml_gui/panels/SliceProgress.qml"
      to: "Theme.qml"
      via: "Theme.bgPanel, Theme.accent, Theme.textTertiary, etc."
      pattern: "Theme\\."
    - from: "src/qml_gui/components/LayerSlider.qml"
      to: "Theme.qml"
      via: "Theme.bgPanel, Theme.textMuted, etc."
      pattern: "Theme\\."
---

<objective>
Add 6 new Theme tokens and tokenize SliceProgress, PreviewPage, LayerSlider, StatsPanel, and MoveSlider.

Purpose: These are the highest visual-impact, lowest-risk files. SliceProgress is the most visible panel during the slicing workflow. PreviewPage and its child components (LayerSlider, StatsPanel, MoveSlider) are the second-most-visited page. Tokenizing these first establishes the replacement patterns for the larger files in Plan 02/03.

Output: 6 files with all hardcoded hex colors replaced by Theme tokens (except semantic/brand colors that are intentionally kept).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v11-03-ui-polish-pass/03-RESEARCH.md
@src/qml_gui/Theme.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 new tokens to Theme.qml and tokenize SliceProgress.qml</name>
  <files>src/qml_gui/Theme.qml, src/qml_gui/panels/SliceProgress.qml</files>
  <action>
Step A -- Extend Theme.qml. After the existing `radiusXL: 12` line (line 72), add a `radiusXXL` token. After the existing `statusInfo` color (line 50), add the following tokens inside their respective sections:
- In the Border section, after `borderFocus`: `readonly property color borderInput: "#2e3848"`
- In the Status section, after `statusInfo`: `readonly property color bgErrorSubtle: "#4a1c1c"` and `readonly property color bgWarningSubtle: "#3a3420"`
- In the Text section, after `textDisabled`: `readonly property color textMuted: "#8b949e"`
- In the Background palette section, after `bgPressed`: `readonly property color bgTooltip: "#1a2332"`
- In the Radii section, after `radiusXL`: `readonly property int radiusXXL: 16`

Step B -- Tokenize SliceProgress.qml (25 hardcoded colors). Replace each hardcoded hex with the mapped Theme token per this exact mapping:

Line 39: `color: "#1e2229"` -> `color: Theme.bgPanel`
Line 40: `border.color: root.slicingNow ? "#22c564" : "#2e3540"` -> `border.color: root.slicingNow ? Theme.accent : Theme.borderSubtle`
Line 50-52: icon color `root.slicingNow ? "#22c564" : root.pct >= 100 ? "#1baa52" : "#566070"` -> `root.slicingNow ? Theme.accent : root.pct >= 100 ? Theme.accentDark : Theme.textDisabled`
Line 58: `color: root.slicingNow ? "#dde4ef" : "#7a8fa3"` -> `color: root.slicingNow ? Theme.textPrimary : Theme.textTertiary`
Line 73: `color: "#9daaba"` -> `color: Theme.textSecondary`
Line 79: `color: "#e2e8f1"` -> `color: Theme.textPrimary`
Line 94: `color: "#2a3040"` (progress bar background) -> `color: Theme.borderSubtle`
Line 101: `color: root.slicingNow ? "#22c564" : (root.pct >= 100 ? "#1baa52" : "#405060")` -> `root.slicingNow ? Theme.accent : (root.pct >= 100 ? Theme.accentDark : Theme.textDisabled)`
Line 113: `color: "#1a332a"` -> `color: Theme.accentSubtle`
Line 115: `border.color: "#22c564"` -> `border.color: Theme.accent`
Line 125: `color: "#22c564"` -> `color: Theme.accent`
Line 132: `color: "#22c564"` -> `color: Theme.accent`
Line 147: `color: "#9daaba"` -> `color: Theme.textSecondary`
Line 160: `color: root.editorVm.isPlateSliced(index) ? "#1a332a" : "#2a2a2a"` -> `root.editorVm.isPlateSliced(index) ? Theme.accentSubtle : Theme.bgElevated`
Line 177: `color: "#22c564"` -> `color: Theme.accent`
Line 343: `color: "#18c75e"` (filament color chip) -> `color: Theme.accent`
Line 420: extruder color array `index === 0 ? "#18c75e" : ...` -- KEEP AS-IS (intentional per-extruder semantic colors)
Line 483: `color: root.canRequestSlice ? Theme.bgPanel : "#241d1d"` -> `Theme.bgPanel : Theme.bgErrorSubtle` (but keep the hex `#241d1d` mapped to a new token; since `#241d1d` is close to `bgErrorSubtle` (#4a1c1c) but not identical, use `Theme.bgErrorSubtle` for the error-state background)
Line 484: `border.color: root.canRequestSlice ? Theme.borderSubtle : "#5d2f2f"` -> `Theme.borderSubtle : Theme.statusError` (close enough, or add inline; `#5d2f2f` is error-tinted border -- use `Qt.darker(Theme.statusError, 1.5)` to keep it derived)
Line 504-505: button hover/pressed colors `#7d2020`/`#5e1818` (cancel) and `#19a84e`/`#157a39` (slice) -- KEEP AS-IS (these are interactive state colors with no Theme token; they are derived from accent/error and adding tokens for hover/pressed states is over-engineering)
Line 543: `color: previewMA.containsMouse ? "#7c3aed" : "#6d28d9"` -- KEEP AS-IS (intentional brand purple)
Line 568: `color: exportMA.containsMouse ? "#2563eb" : "#1d4ed8"` -- KEEP AS-IS (intentional brand blue)

Also replace font.pixelSize literals: line 59 `font.pixelSize: 12` -> `Theme.fontSizeMD`, line 74 `font.pixelSize: 11` -> `Theme.fontSizeSM`, line 80 `font.pixelSize: 11` -> `Theme.fontSizeSM`.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/panels/SliceProgress.qml | awk '{print "Remaining hardcoded colors in SliceProgress.qml: " $1}' && grep -c 'Theme\.' src/qml_gui/panels/SliceProgress.qml | awk '{print "Theme references in SliceProgress.qml: " $1}' && grep 'bgTooltip\|textMuted\|borderInput\|bgErrorSubtle\|bgWarningSubtle\|radiusXXL' src/qml_gui/Theme.qml | wc -l | awk '{print "New tokens in Theme.qml: " $1}'</automated>
  </verify>
  <done>SliceProgress.qml has at most 12 remaining hardcoded hex colors (all intentional semantic/brand colors). Theme.qml has 6 new tokens. All font.pixelSize in SliceProgress that match a Theme token size use the token.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize PreviewPage, LayerSlider, StatsPanel, and MoveSlider</name>
  <files>src/qml_gui/pages/PreviewPage.qml, src/qml_gui/components/LayerSlider.qml, src/qml_gui/components/StatsPanel.qml, src/qml_gui/components/MoveSlider.qml</files>
  <action>
Tokenize PreviewPage.qml (6 hardcoded colors):
- Line 117: `color: viewMA.containsMouse ? "#2a3545" : "#1e2229"` -> `viewMA.containsMouse ? Theme.bgHover : Theme.bgPanel`
- Line 118: `border.color: viewMA.containsMouse ? "#3e5060" : "#2e3540"` -> `viewMA.containsMouse ? Theme.borderDefault : Theme.borderSubtle`
- Line 123: `color: "#9daaba"` -> `color: Theme.textSecondary`
- Line 160: `color: "#1e2229"` (layer/move summary badge) -> `color: Theme.bgPanel`
- Line 162: `border.color: "#2e3540"` -> `border.color: Theme.borderSubtle`
- Line 168: `color: "#7a8a9a"` -> `color: Theme.textTertiary`
Also normalize: line 75 `radius: 14` -> `Theme.radiusXXL` (16, close enough and consistent). Line 185 `radius: 16` -> `Theme.radiusXXL`. Line 202 `radius: 18` -> `Theme.radiusXXL` (close enough; 18 vs 16 is minor). Line 238 `radius: 16` -> `Theme.radiusXXL`. Line 293 `radius: 16` -> `Theme.radiusXXL`.

Tokenize LayerSlider.qml (19 hardcoded colors):
- Line 56: `color: "#252b38"` -> `color: Theme.bgElevated`
- Line 87: `color: "#1a2332"` -> `color: Theme.bgTooltip`
- Line 134: `color: "#1a2332"` -> `color: Theme.bgTooltip`
- Line 173: `color: "#1a2030"` -> `color: Theme.bgPanel`
- Line 186: `color: "#1a2030"` -> `color: Theme.bgPanel`
- Line 200: `color: ... ? Theme.accent : "#c0d0e0"` -> `Theme.accent : Theme.textSecondary` (light gray)
- Line 202: `border.color: ... ? Theme.accent : "#8090a0"` -> `Theme.accent : Theme.textTertiary`
- Line 209: `color: ... ? "#fff" : "#506070"` -> `"#fff" : Theme.textDisabled`
- Line 251: same pattern as 200 for second thumb
- Line 253: same pattern as 202 for second thumb
- Line 260: same pattern as 209 for second thumb
- Line 309-310: input fields `color: "#1e2229"`, `border.color: ... ? Theme.accent : "#2e3540"` -> `Theme.bgPanel`, `Theme.accent : Theme.borderSubtle`
- Line 336-337: same as 309-310 for max input
- Line 362-363: jump button `color: ... ? "#2a3545" : "#1e2229"`, `border.color: ... ? Theme.accentDark : "#2e3540"` -> `Theme.bgHover : Theme.bgPanel`, `Theme.accentDark : Theme.borderSubtle`
- Line 449-450: jump input same pattern as 309-310

Tokenize StatsPanel.qml (2 hardcoded colors):
- Line 273: bar chart colors `#22c55e`, `#eab308`, `#ef4444` -- KEEP AS-IS (semantic green/yellow/red)
- Line 287: `color: "#1e293b"` -> `color: Theme.bgElevated`

Tokenize MoveSlider.qml (5 hardcoded colors):
- Line 44: `color: "#2a3040"` -> `color: Theme.borderSubtle`
- Line 65: `color: "#009688"` -- KEEP AS-IS (teal semantic color for elapsed time)
- Line 96: `color: "#1a2332"` -> `color: Theme.bgTooltip`
- Line 123: `color: "#1a2332"` -> `color: Theme.bgTooltip`
- Line 144: `color: "#80cbc4"` -- KEEP AS-IS (teal for current time)
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && echo "=== PreviewPage ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/pages/PreviewPage.qml && echo "=== LayerSlider ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/components/LayerSlider.qml && echo "=== StatsPanel ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/components/StatsPanel.qml && echo "=== MoveSlider ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/components/MoveSlider.qml</automated>
  </verify>
  <done>PreviewPage.qml has 0 remaining non-semantic hardcoded hex colors. LayerSlider.qml has 0 remaining non-semantic hardcoded hex colors. StatsPanel.qml has 1 remaining (semantic green/yellow/red). MoveSlider.qml has 2 remaining (semantic teal colors). All radius values of 14-18 replaced with Theme.radiusXXL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only QML presentation layer. No trust boundaries exist -- no user input processing, no network calls, no data persistence. Pure visual property replacement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v11-03-01 | N/A | N/A | accept | No security-relevant changes in visual polish pass |
</threat_model>

<verification>
1. Build passes: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
2. Hardcoded color count reduced: `grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/panels/SliceProgress.qml` should be <= 12 (down from 25)
3. `grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/pages/PreviewPage.qml` should be 0 (down from 6)
4. `grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/components/LayerSlider.qml` should be 0 (down from 19)
5. New Theme tokens present: `grep -c 'bgTooltip\|textMuted\|borderInput\|bgErrorSubtle\|bgWarningSubtle\|radiusXXL' src/qml_gui/Theme.qml` should be 6
</verification>

<success_criteria>
1. Theme.qml has 6 new tokens (bgTooltip, textMuted, borderInput, bgErrorSubtle, bgWarningSubtle, radiusXXL) in addition to the existing 33 color + 4 radii tokens
2. SliceProgress.qml: hardcoded hex count reduced from 25 to <= 12 (remaining are intentional semantic/brand colors)
3. PreviewPage.qml: 0 non-semantic hardcoded hex colors remaining
4. LayerSlider.qml: 0 non-semantic hardcoded hex colors remaining
5. StatsPanel.qml: 1 remaining (semantic green/yellow/red triple)
6. MoveSlider.qml: 2 remaining (semantic teal colors)
7. Build passes with zero errors and zero QML warnings
</success_criteria>

<output>
After completion, create `.planning/phases/v11-03-ui-polish-pass/v11-03-01-SUMMARY.md`
</output>
