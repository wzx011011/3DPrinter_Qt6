# PLAN: v12-01-01 — Fix Multi-Plate Loading, Reset Metadata, Add Auto-Arrange

## Context

Multi-plate 3MF files (e.g., 城楼.3mf) load as single-plate because `loadProject()` releases plate data before extracting it. Plate metadata arrays aren't reset between loads, causing stale state. No auto-arrange runs after model load.

## Tasks

### Wave 1: Bug Fixes

**T1. Fix loadProject() plate data extraction order**
- File: `src/core/services/ProjectServiceMock.cpp` ~lines 4318-4370
- Change: Move plate name/indices extraction (currently at line 4350) to BEFORE `release_PlateData_list()` call (line 4318)
- Reference: `loadFile()` lines 507-537 (correct pattern) — copy this structure
- Verify: Load 城楼.3mf via open project → plateCount > 1

**T2. Reset plate metadata arrays on new load**
- File: `src/core/services/ProjectServiceMock.cpp` ~lines 700-723 (loadFile success path)
- Change: Add clearing of 8 per-plate arrays when setting new plateNames_/plateObjectIndices_:
  - plateBedTypes_, plateLockedStates_, platePrintSequences_, plateSpiralModes_
  - plateFirstLayerSeqChoices_, plateFirstLayerSeqOrders_, plateOtherLayersSeqChoices_, plateOtherLayersSeqEntries_
- Same fix in loadProject() success path (~lines 4436-4456)
- Verify: Load 5-plate project, then load 3-plate project → no stale plates 4-5

### Wave 2: Auto-Arrange on Load

**T3. Call arrangeObjects() after loadFile() and loadProject() succeed**
- File: `src/core/services/ProjectServiceMock.cpp`
- In loadFile() success callback (~line 700): add `arrangeObjects()` call after plate data is set
- In loadProject() success callback (~line 4436): same
- Verify: Load STL → object appears centered on bed, not off-screen

## Verification

1. `scripts/auto_verify_with_vcvars.ps1` — build passes
2. `ctest --output-on-failure` — E2EWorkflowTests + ViewModelSmokeTests pass
3. Manual: load 城楼.3mf → plateCount matches upstream
4. Manual: load project A, then project B → no stale state
