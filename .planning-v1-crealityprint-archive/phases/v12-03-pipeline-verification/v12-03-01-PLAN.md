# PLAN: v12-03-01 — E2E Pipeline Verification with Real Files

## Context

Phases 1 and 2 fix multi-plate loading, metadata reset, auto-arrange, config propagation, and real bed shape. This phase verifies the full load → arrange → config → slice → export → preview pipeline end-to-end with real files, and fixes any remaining gaps discovered during verification.

## Tasks

### Wave 1: Automated Test Coverage

**T1. Add multi-plate load verification to E2EWorkflowTests**
- File: `tests/E2EWorkflowTests.cpp`
- Add test case `loadMultiPlateProject()`:
  1. Load a multi-plate 3MF file (use test fixture or generate minimal 2-plate 3MF)
  2. Verify `plateCount() > 1`
  3. Verify `plateObjectIndices()` for each plate is non-overlapping
  4. Verify total objects across plates equals `modelCount()`
  5. Verify no stale metadata (plateBedTypes all default)

**T2. Add config propagation verification test**
- File: `tests/E2EWorkflowTests.cpp`
- Add test case `projectConfigPropagation()`:
  1. Create a DynamicPrintConfig with known overrides (e.g., layer_height=0.3, infill_density=20)
  2. Verify ConfigViewModel::applyProjectConfig() updates the values
  3. Verify mergedConfigValues() reflects the loaded config
  4. Verify subsequent slice uses loaded config values

**T3. Add arrange verification test**
- File: `tests/E2EWorkflowTests.cpp`
- Add test case `arrangeWithRealBedShape()`:
  1. Load a model with 2+ objects
  2. Call arrangeObjects() with a printable_area string
  3. Verify all object positions are within bed bounds
  4. Verify objects don't overlap (basic bounds check)

### Wave 2: Manual E2E Pipeline Test

**T4. Verify multi-plate 3MF load pipeline**
- File: `城楼.3mf` (user-provided multi-plate model)
- Steps:
  1. Launch app with `CREALITY_OPENGL=1`
  2. File → Open → select 城楼.3mf
  3. Verify plate count matches upstream (should be multi-plate, not collapsed to 1)
  4. Verify each plate shows correct objects
  5. Switch between plates → verify object list updates
  6. Verify no stale metadata from previous loads

**T5. Verify STL → arrange → slice → export → preview pipeline**
- Steps:
  1. Load a single STL file
  2. Verify auto-arrange centers object on bed
  3. Adjust settings in config panel
  4. Click Slice → verify progress, completion
  5. Click Export → verify gcode file created
  6. Switch to Preview → verify gcode visualization loads
  7. Verify layer navigation works

**T6. Verify project A → project B load transition**
- Steps:
  1. Load project A (multi-plate, custom settings)
  2. Verify state is correct for A
  3. Load project B (different plate count, different settings)
  4. Verify ALL state from A is gone (no stale plates, no stale settings)
  5. Verify B's settings are correctly loaded from embedded config

### Wave 3: Gap Fixes (if discovered during verification)

**T7. Fix any gaps found during Wave 2 testing**
- Common potential gaps based on research:
  - fit-to-view after load (camera should frame the loaded model)
  - plate selection signal not updating GLViewport when switching plates
  - gcode preview data not clearing between slices
- Each fix: locate upstream reference, implement, verify

## Verification

1. `scripts/auto_verify_with_vcvars.ps1` — build passes
2. `ctest --output_on-failure` — all test suites pass (including new test cases)
3. Manual E2E checklist:
   - [ ] Multi-plate 3MF loads with correct plate count
   - [ ] Objects auto-arrange within bed boundaries
   - [ ] Project settings restore on load
   - [ ] Load project A then B — no stale state
   - [ ] Full slice pipeline produces valid gcode
   - [ ] Preview loads and navigates correctly
4. No QML warnings in `startup_diagnostics.log`
