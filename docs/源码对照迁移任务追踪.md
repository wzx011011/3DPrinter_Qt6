# OWzx Slicer (OrcaSlicer → Qt6) — 源码对照迁移任务追踪

> 更新日期：2026-09-06（任务账本统一：当前任务状态的唯一权威清单是 `.planning/WORK_ITEMS.yaml`）
> 2026-09-04 全工程评审（11 分区只读评审 + ctest 实测）：新增 6 项 P0（集中在未提交 WIP）与一批 P1，详见 [docs/代码评审报告_2026-09-04.md](代码评审报告_2026-09-04.md)，已在第 15 节登记。
> 功能真值：`third_party/OrcaSlicer`（官方 upstream/main，2026-09-12 基线推进至 `0a3724ed2f106dd40b8a6b6f89c6a83a546cc81c`；历史锁定 `8b93cc5df`，2026-07-21；OWzx 兼容改动以 4 补丁系列在 configure 时应用，见 AGENTS.md Upstream Lock）。历史文档中的 `third_party/CrealityPrint` 路径已废弃（P17.11），仅作历史证据。
> 本文用于追踪“功能承接度”，不是追踪“文件是否创建过”。当前未完成/进行中/阻塞任务的唯一权威清单是 [.planning/WORK_ITEMS.yaml](../.planning/WORK_ITEMS.yaml)（含 R-P0.*/R-P1.*/G-*/P* 历史编号别名映射）；本文条目状态若与之冲突，以 WORK_ITEMS.yaml 为准。

---

## 0. 当前结论

- 当前工程已经具备可运行的 Qt6/QML 主壳、基础组件、3D 视口桥接、3MF 导入、切片链路、G-code 预览链路的基础承接能力。
- Monitor / Device / Camera / Cloud / Model Mall / MultiMachine 模块已从骨架升级为具备 Mock 交互能力的功能页面。
- **2026-09-06 口径更正**（本节早期文本已被后续提交推翻，仅留历史对照）：libslic3r 真实调用链路已打通——对象变换（P0.5.1）、网格变换（P0.5.2）、对象 CRUD（P0.5.3）、Volume 管理（P0.5.4）、Plate 核心域（P0.5.5a）均已承接真实 `Model`/`ModelInstance`/`ModelVolume`/`PartPlate` API 并有测试；后台任务（P1.5 Jobs 框架）与 Undo/Redo（P1.4 QUndoStack）已迁移。仍为 Mock/外部阻塞的是设备、云、摄像头、商城、多机等外部能力（bambu_networking、FFmpeg、MQTT 真机等，见 WORK_ITEMS.yaml `EXTERNAL-BLOCKERS`）。
- 距离“基于 OrcaSlicer 上游完整还原功能”仍有差距，当前主要未完成项（权威清单见 WORK_ITEMS.yaml）：P0.5.5b 深层 Plate 语义（持久 Print/GCodeResult 生命周期等）、P11.B2.3 支撑绘制轴锁、Preview G-code source 语义化、G-12 剩余 QML 绑定环，以及 stash@{0}/stash@{1} 处置（需用户决策）。
- 因此，旧文档中的”70/70 全部完成”不再作为真实进度口径。

---

## 1. 进度记账规则

- `[x]`：已与上游功能真值完成对照，具备可用能力，并通过回归或端到端验证。
- `[-]`：已完成基础承接或局部闭环，但尚未完成上游对照和完整验收。
- `[ ]`：尚未开始，或仅有占位 UI / Mock 能力。
- **2026-09-06 统一规则**：
  - 当前状态判定顺序：代码/测试证据 > WORK_ITEMS.yaml（唯一权威清单）> 本文条目 > phase SUMMARY > 其他历史文档。
  - 每个问题只有一个 canonical ID；`R-P0.*`/`R-P1.*`/`G-*`/`P*.*` 等历史编号是别名（映射见 WORK_ITEMS.yaml `alias_map`），不得作为新任务编号复用。
  - “实现程度 / 上游对齐 / 验证状态 / 可用性”四个维度分开记录（WORK_ITEMS.yaml 四字段），不得合并为单一勾选框表达；验证结论必须绑定 commit。

---

## 2. 已完成的基础承接

以下事项已经完成，且可作为后续全功能迁移的基础：

- [x] CMake + Qt6 + QML 主入口与构建链路稳定可用
- [x] `BackendContext`、基础 ViewModel 注入、页面导航框架已建立
- [x] 共享设计令牌与基础控件体系已建立：`Theme`、`CxButton`、`CxComboBox`、`CxSlider`、`CxIconButton`、`CxPillAction`、`CxPanel` 等
- [x] `GLViewport`、`GLViewportRenderer`、`CameraController` 3D 视口桥接骨架可运行
- [x] `ProjectService` 已打通 3MF / BBS 3MF 导入主链路
- [x] `SliceService` 已打通真实切片与 G-code 导出主链路
- [x] `GCodeRenderer` 已具备真实 G-code 预览渲染主链路
- [x] `PreviewViewModel` 已可消费切片结果并驱动预览页面基础数据
- [x] 程序当前可构建并成功启动，`scripts/auto_verify_with_vcvars.ps1` 仍是权威完整验证路径

---

## 3. 真实功能迁移状态

### P0 — 真值基线与迁移框架

- [x] P0.1 锁定上游功能真值版本（tag / commit）2026-03-10
  - 基线文档：[docs/源码真值基线.md](源码真值基线.md)
  - 锁定结果：`third_party/OrcaSlicer` 基于官方 `upstream/main` 提交 `8b93cc5df3347c657ce6ac9a58f6923a21c2959b`，OWzx 兼容子提交为 `4cb3b9ce792f15c38fabfb4bb9700895d32b1166`
  - 2026-09-12 基线推进（Phase B / BASELINE-ADVANCE-0A3724ED2F）：gitlink 前移至官方 `upstream/main` 提交 `0a3724ed2f106dd40b8a6b6f89c6a83a546cc81c`（2026-09-11 快照，740 个提交漂移）；4 补丁系列在新基线上仍干净适用（upstream 的 keys_map 采纳只覆盖赋值路径，serialize/deserialize 守卫仍必要）；新增上游源（TexturePainting/TextureToColor → CGAL 目标，AssimpImport → delay-load）已接入 from-source 构建，详见 WORK_ITEMS.yaml
- [x] P0.2 建立“上游模块 -> Qt6 模块 -> 当前状态 -> 验收方式”功能矩阵 2026-03-10
  - 功能矩阵文档：[docs/源码真值功能矩阵.md](源码真值功能矩阵.md)
  - 说明：已建立统一执行入口，后续按矩阵继续细化子任务和验收项
- [x] P0.3 梳理闭源依赖、动态库、云服务与协议边界 2026-03-10
  - 审计文档：[docs/依赖与协议边界审计.md](依赖与协议边界审计.md)
  - 说明：已识别 `bambu_networking`、WebView2、MQTT/SSDP/HTTP、登录/商城、媒体播放等关键边界
- [x] P0.4 将任务追踪切换为”功能承接度”口径

### P0.5 — libslic3r 真实调用链路桥接

- [x] P0.5.1 对象变换桥接 — objectPosition/Rotation/Scale() 从 ModelInstance::get_offset()/get_rotation()/get_scaling_factor() 读取真实变换（含坐标系转换 GL(X,Z,Y) ↔ slic3r(X,Y,Z)，角度↔弧度转换），setObjectPosition/Rotation/Scale() 写入真实 ModelInstance 并同步 Mock 平行数组；loadFile/addObject/duplicateObject/deleteObject 成功后自动从真实模型同步变换到 Mock 数组；loadProject(JSON) 时重置 model_ 防止读取过期数据；objectInstanceCount() 已实现（对齐上游 ModelObject::instances.size()）；单实例 per-object 模型与上游常规工作流一致（duplicate 创建独立 ModelObject 而非同对象多 instance）；orientObject() 调用 Slic3r::orientation::orient(ModelObject*) 真实自动朝向；syncTransformsFromModel() 从真实模型同步变换到 Mock 数组；autoOrientSelected() 已从 Mock 切换为真实 orient API；构建验证 273/273 通过 2026-03-19
- [x] P0.5.2 网格实例变换 — meshData() 已实现完整变换管线：遍历 objects→instances→volumes，组合 instance×volume Transform3d 矩阵应用到顶点，slic3r(X,Y,Z)→GL(X,Z,Y) 坐标转换已实现 2026-03-17
- [x] P0.5.3 对象 CRUD 真实实现 — addObject/duplicateObject/deleteObject 已调用 Model::add_object/Model::delete_object 真实 API 并同步元数据和变换数组；moveObject 已对齐上游 GUI_ObjectList::OnDrop 迭代 std::swap 重排序 model_->objects[]；renameObject 已同步真实模型对象名称；splitObject 已调用真实 ModelObject::split() API（克隆模型→split→移除原对象→添加拆分结果→重建元数据→同步变换）；autoOrientSelected 已调用 Slic3r::orientation::orient(ModelObject*) 真实 API；构建验证 285/285 通过 2026-03-19
- [x] P0.5.4 Volume 管理真实实现 — addVolume/deleteVolume/objectVolumeCount/Name/TypeLabel 已调用真实 API（addVolume 对齐上游 GUI_ObjectList::load_generic_subobject，从第一个 model_part volume 复制网格并调用 ModelObject::add_volume；addVolumeFromFile 对齐上游文件加载，通过 Model::read_from_file 加载外部网格后添加为真实 ModelVolume）；volumeExtruderId() 已从 ModelVolume::extruder_id() 读取；setVolumeExtruderId() 已通过 volume->config.set("extruder") 写入真实配置；changeVolumeType() 已调用 ModelVolume::set_type()；addTextVolume 已调用真实 Emboss API（Emboss::create_font_file + text2shapes + polygons2model + ProjectZ，对齐上游 GLGizmoEmboss，系统字体 arial.ttf）；addSvgVolume 已调用真实 Model::read_from_file 加载 SVG 为 3D 网格（对齐上游 GLGizmoSVG）；addPrimitive 已调用真实 its_make_cube/sphere/cylinder 生成原始体网格并添加为真实 ModelVolume（对齐上游 create_mesh，Torus 回退到 sphere）；objectInstanceCount() 已实现；构建验证 269/269 通过 2026-03-19
- [x] P0.5.5a Plate 核心域与工作流真实实现 — 已落地 Qt6 原生 `PartPlate`/`PartPlateList`，由其承接实例级对象归属、平板增删/重命名/锁定/克隆/重排/可打印状态、网格排列与锁盘、per-plate `DynamicPrintConfig`、3MF `PlateData` 保存加载、逐平板切片队列与配置合并、缩略图和耗材映射；`ProjectServiceMock` 的 plate API 已不再依赖平行 Mock 数组；`PartPlateTests`、`ViewModelSmokeTests`、QML audit、真实切片 E2E 与应用启动均经唯一构建命令验证通过 2026-08-02
- [-] P0.5.5b Plate 深层上游语义补齐 — `PartPlateList` 现已为每个新平板分配不可变、单调递增且不复用的 `printIndex`，平板重排/删除不会改变幸存平板身份，新建平板也不会复用已删除身份；`PartPlateTests::platePrintIdentityLifecycle` 已覆盖首建、重排、删除后新建并独立运行通过（2026-09-05）。`SliceService::plateResults_` 已迁移为以稳定 `printIndex` 为结果键并收口（2026-09-06，commit cc0df24，canonical ID `PLATE-RESULT-STABLE-KEY`；此前批量导出单文件现象根因为测试时序竞态并已修复；canonical 门禁 EXIT=0：12 套件 560 测试 0 失败，E2E 29/29 含重排结果跟随断言）。完整的持久 `Print`/G-code result 生命周期、unprintable/shared plate、`find_instance`/`contains` 完整几何语义、plate shape/exclude/extruder areas、per-plate wipe-tower/Print 上下文及完整 slice-state/result API 仍待迁移（canonical ID `PLATE-PRINT-LIFECYCLE`）；本轮未将这些未完成项误标为已完成。首层/其他层耗材顺序已核实并非缺口：每次写入均同步进 plate `DynamicPrintConfig`（`syncPlateOtherLayersSeqConfig` 等，ProjectServiceMock.cpp:8505-8626），该 config 即 3MF `PlateData` 序列化源，加载路径读回三个键（:1040-1047），且 `plateConfigValue` 断言覆盖 Auto 清除分支（ViewModelSmokeTests.cpp:3671-3687）。

### P1 — 主框架与全局工作流

- [x] P1.1 顶栏导航、页面切换、文件入口、最近文件基础闭环已存在；已新增项目保存功能：ProjectServiceMock 新增 `saveProject(filePath)` 方法（Mock 模式保存为 JSON，包含项目名/源路径/平板配置/对象元数据/per-object 参数覆盖），BackendContext::topbarSaveProject()/topbarSaveProjectAs() 现在调用 ProjectServiceMock::saveProject() 实际写入磁盘并显示保存成功/失败通知（ErrorToast）；已新增项目加载功能：ProjectServiceMock 新增 `loadProject(filePath)` 从 JSON 恢复全部项目状态（项目名/源路径/平板列表/对象元数据/per-object 参数覆盖），BackendContext::topbarOpenProject() 在 3MF 加载失败时自动尝试 JSON 加载，EditorViewModel 新增 `refreshAfterLoad()`；已新增新建项目确认对话框（对齐上游清空确认），防止误操作丢失未保存更改；文件对话框已支持 `.json` 格式；已新增全局通知系统增强：BackendContext 新增 `postNotification(message, title, severity)` 方法支持带标题的分级通知（severity 0=info/1=warning/2=error），新增 `lastErrorTitle` Q_PROPERTY；已新增通知队列系统（FIFO）：BackendContext 新增 `QQueue<NotificationEntry>` 和 `dismissNotification()`/`pendingNotificationCount` 方法，`postNotification()` 在当前通知正在显示时自动入队，`dismissNotification()` 关闭当前通知并自动显示队列中下一条；ErrorToast 更新为使用 `dismissNotification()` 替代 `clearError()`，新增待处理通知计数徽章（9+ 封顶）；ErrorBanner 同步更新为显示待处理计数和使用 `dismissNotification()`；已新增持久通知系统（对齐上游 `notification_manager`）：NotificationEntry 扩展为包含 persistent/progressValue/progressMin/progressMax/hasProgress/requiresConfirm/confirmAction 字段，BackendContext 新增 `m_currentNotification` 追踪当前通知状态、`currentNotificationProgress`/`currentNotificationHasProgress`/`currentNotificationPersistent` Q_PROPERTY、`updateNotificationProgress(int)`/`confirmCurrentNotification()`/`cancelCurrentNotification()` Q_INVOKABLE；ErrorToast 新增进度条可视化（动画宽度跟随进度值，200ms 过渡）、确认/取消按钮行（仅持久且非进度通知时显示，调用 confirm/cancel）；持久通知不自动关闭；但仍缺上游完整通知管理器 UI（通知历史列表、通知偏好设置等）2026-03-14
- [x] P1.2 已新增 Edit 菜单（全选/取消选择/删除选中/清空全部）；已新增 View 菜单（适应视图、相机预设子菜单：俯视/前视/右视/等轴视、线框模式切换）；已新增 Help 菜单（快捷键一览对话框显示 16 个快捷键映射、关于 Creality Print 对话框显示版本和上游基线信息）；原有 File 菜单包含新建/打开/导入/保存/另存为/最近文件/退出，工具栏含保存/撤销/重做按钮和页面 Tab 切换；线框模式已完整实现；已补全 Edit 菜单上游完整项：撤销/重做/剪切(Ctrl+X)/复制(Ctrl+C)/粘贴(Ctrl+V)/删除/清空/克隆(Ctrl+D)/全选/取消选择，EditorViewModel 新增 `copySelectedObjects()`/`pasteObjects()`/`cutSelectedObjects()` 剪贴板操作和 `hasClipboardContent` 属性，ProjectServiceMock 新增 `addObject(name)` 方法；已新增 Settings 菜单（偏好设置 Ctrl+P/参数设置）并在标题栏添加设置菜单按钮；View 菜单已添加快捷键（Ctrl+0/1/3/6）；Help 菜单已添加快捷键提示（?）；快捷键对话框已扩展到 24 个映射（新增 PageUp/Down 层范围±1层、Shift+PageUp/Down 层范围±10层、Ctrl+0/1/3/6 预设视角）；已新增 View 菜单"显示标签"子菜单（对齐上游 MainFrame View → Show），支持逐个切换工作流标签页的显示/隐藏（在线模型/准备/预览/设备），通过 `hiddenTabs` 字典和 `toggleTabVisibility()` 函数控制，`buildWorkflowTabs()` 过滤隐藏标签；已新增全局通知管理器（对齐上游 `notification_manager`）：顶栏新增 Bell 图标（带未读红点徽章），点击弹出通知中心面板（NotificationCenter.qml）显示通知历史列表（最近 100 条，含时间戳/标题/消息/严重级别颜色标记），支持标记全部已读和清空历史；通知级别从 3 级扩展到 9 级（对齐上游 NotificationLevel 枚举：Info/Success/Warning/Error/SeriousWarning/Hint/PrintInfo/PrintInfoShort/Progress），ErrorToast 每个级别有独立颜色方案和图标；已新增通知历史追踪：`postNotification()` 和 `postError()` 自动为每个通知记录时间戳，`dismissNotification()` 将通知移入历史队列，新增 `historyCount`/`unreadHistoryCount`/`historyMessage(int)`/`historyTitle(int)`/`historySeverity(int)`/`historyTime(int)` Q_INVOKABLE 和 `clearHistory()`/`markHistoryRead()` 方法；已新增悬浮暂停功能（对齐上游 hover 行为）：ErrorToast 鼠标悬浮时暂停自动消失计时器，离开后 2 秒延迟关闭；已新增通知偏好设置（对齐上游 notification_manager preferences）：新增"启用通知"开关、 "显示提示"开关、 "自动消失时间"选择（3s/5s/8s/10s/15s）在 PreferencesPage 通用分类；通知偏好存储在 BackendContext（`notificationsEnabled`/`hintsEnabled`/`autoDismissSec`），禁用通知时 `postError()`/`postNotification()` 静默跳过；已新增专用通知类型（对齐上游 NotificationType 枚举和 NotificationManager 专用通知）：新增 NotificationType 枚举（16 种类型，对齐上游 CustomNotification/ExportFinished/SlicingProgress/SlicingError/PlaterWarning/ValidateError/DidYouKnowHint 等），新增 NotificationLevel.NotiSlicingProgress（第 10 级）；BackendContext 新增 `postSlicingProgress()`（支持就地更新进度条，不排队替换）、`postSlicingComplete()`（切片完成通知含导出/预览按钮）、`postExportFinished()`、`postExportOngoing()`、`postPlaterWarning/Error()`、`postValidateWarning/Error()`、`postArrangeOngoing()` 等 10 个专用通知方法；SliceService 信号已连接到专用通知（progressUpdated → postSlicingProgress，sliceFinished → postSlicingComplete，sliceFailed → postError）；NotificationEntry 扩展 type/showExportButton/showPreviewButton/hintHasNext/hintHasPrev 字段；已新增 HintDatabase（对齐上游 HintDatabase/HintNotification）：内置 12 条 "Did you know" 提示（层高/填充/支撑/速度/裙边/温度/回退/壁/自适应层高/多平板/预览/可变层高），每条提示含 id/text/weight/hypertext/documentationLink/callbackType/callbackTarget（对齐上游 HintData 结构）；加权随机选择算法（优先显示未读提示），已显示提示集合追踪防止重复，60 秒自动周期显示（对齐上游 push_delayed_notification_data 间隔）；BackendContext 新增 `postHint()`/`nextHint()`/`prevHint()`/`hintCount()`/`currentHintIndex()`/`currentHintText()`/`currentHintHypertext()`/`currentHintFollowText()` 9 个 Q_INVOKABLE；ErrorToast 重构为支持 4 种通知模式：普通通知、进度通知、提示通知（含 < > 导航箭头 + 计数器 + "不再提示"按钮，对齐上游 HintNotification 渲染）、切片完成通知（含预览/导出/关闭按钮，对齐上游 SlicingProgressNotification 导出/预览链接）；已新增 HintData JSON 文件加载（对齐上游 HintDatabase::load_hints_from_file）：从 Qt 资源 `:/qml/data/hints.json` 加载 38 条提示数据（中文本地化，对齐上游 hints.ini 全部条目），含 id/text/weight/documentation_link 字段；JSON 加载失败时自动回退到内置 5 条最小提示集；已新增文档链接点击功能（对齐上游 HintNotification hypertext_type=documentation）：BackendContext 新增 `openHintDocumentation()` Q_INVOKABLE（调用 QDesktopServices::openUrl 打开当前提示的 documentation_link）和 `currentHintHasDocumentationLink()` Q_INVOKABLE（检查当前提示是否有文档链接）；ErrorToast 提示导航栏新增绿色"文档"按钮（仅当当前提示有文档链接时显示），点击即打开对应 wiki 页面；但仍缺部分 BBL 扩展通知类型（BBLPlateInfo/BBL3MFInfo/BBLObjectInfo 等） 2026-03-14
- [x] P1.3 需要建立统一的上游 GUI 迁移适配层，禁止页面逻辑直接散落在 QML 内联脚本中；已将 SettingsPage 的 `rebuildFilter()` 内联 JavaScript 过滤逻辑迁移到 `ConfigViewModel::filterOptionIndices()` C++ 方法，QML 端简化为 1 行调用；已完成 PrintSettings.qml 全部 5 个内联函数迁移到 C++：`rebuildFilter()` → `filterOptionIndices("", searchText)`、`materialPresetAt()` → `materialPresetName()`（同时修复"耗材丝"→"耗材"分类名 bug）、`groupedIndices()` → `filterIndicesByCategory()`、`enumOptionLabels()` → `ConfigOptionModel::optEnumLabelsList()`、`oUnit` regex → `ConfigOptionModel::optUnit()`（基于 key 映射替代 label regex）；QML 中仅保留 `scopeTitle()` 和 `scopeSubtitle()` 两个纯展示文本函数；已完成 main.qml `currentProjectTitle()` 迁移到 `BackendContext::displayProjectTitle`（C++ Q_PROPERTY，路径分割逻辑从 JS `split(/[\\/]/)` 改为 `QFileInfo::fileName()`）；已审计全部 QML 页面内联函数：PreparePage 全部为 UI 适配逻辑（委托给 ViewModel/Viewport 方法），PrintSettings/SettingsPage 的 `rebuildFilter()` 已是 1 行 C++ 代理，main.qml `buildWorkflowTabs()` 是静态数据；但仍需持续监控新增 QML 页面是否引入业务逻辑 2026-03-14
- [x] P1.4 Undo/Redo 真实系统（对齐上游 UndoRedo 框架）— UndoRedoManager（QUndoStack 包装器，支持 canUndo/canRedo/undoText/redoText/undoLimit/push/undo/redo/clear/beginMacro/endMacro）已实现并注入到 BackendContext→EditorViewModel；8 个 QUndoCommand 子类已实现：TransformCommand（支持 merge 连续变换）、MultiTransformCommand、DeleteObjectsCommand（含对象快照恢复）、AddObjectCommand、SelectionCommand、RenameCommand、MoveObjectCommand、CloneCommand、VolumeDeleteCommand；EditorViewModel 全部变更操作已接入对应 Command（setObjectPosition/Rotation/Scale→TransformCommand、deleteSelected→DeleteObjectsCommand、duplicateSelected→CloneCommand、pasteObjects→AddObjectCommand、renameObject→RenameCommand、moveObject→MoveObjectCommand）；undoLimit 已连接 SettingsViewModel；clearWorkspace 时自动清除撤销栈；构建验证 271/271 通过、61/61 smoke test、0 QML warnings 2026-03-19
- [x] P1.5 后台任务系统（对齐上游 Jobs/ 框架）— JobBase 抽象基类（QObject+QRunnable，支持 progress/cancel/cross-thread finalize，对齐上游 GUI::Job）；JobManager 单例（QThreadPool 包装器，支持 enqueue/cancelAll/maxThreads，对齐上游 GUI::Worker）；线程安全进度上报（std::atomic + QueuedConnection 信号）；SliceService 保持已有 QtConcurrent::run（已正确异步），Jobs 框架供 ArrangeJob/OrientJob/ExportJob 等后续具体任务使用；构建验证 271/271 通过、61/61 smoke test、0 QML warnings 2026-03-19

### P2 — Prepare 工作区

- [-] P2.1 已具备模型导入、视口显示、基础对象列表、平板切换、切片入口；已新增平板管理操作（添加/删除平板）：对齐上游 `PartPlateList`，ProjectServiceMock 新增 `addPlate()`/`deletePlate(int)`，EditorViewModel 新增对应桥接方法，PreparePage 左侧面板新增"添加平板"和"删除平板"按钮（至少保留 1 个平板），删除当前平板时自动切换到有效平板索引；已新增平板右键上下文菜单对齐上游 `GUI_Factories::create_plate_menu()`：选择全部对象、清空平板、重命名（Dialog 弹窗）、锁定/解锁、删除平板；ProjectServiceMock 新增 `renamePlate()`、`removeAllOnPlate()`、`isPlateLocked()`/`setPlateLocked()`、`plateObjectIndices(int)`；EditorViewModel 新增对应桥接方法；平板卡片显示锁定图标；已新增平板缩略图预览（对齐上游 `PartPlate::thumbnail_data`）：平板卡片左侧新增 32x32 彩色缩略图区域，每个平板使用独特颜色区分，中心显示对象数量；EditorViewModel 新增 `plateThumbnailColor(int)` Q*INVOKABLE；已新增对象跨平板拖拽（对齐上游 `Plater::priv::on_arrange` 跨平板拖拽）：ObjectList 对象项支持 Drag.source（key="object-drag"），平板卡片 DropArea 接收拖拽并调用 `moveSelectedObjectToPlate()`；EditorViewModel 新增 `moveSelectedObjectToPlate(int targetPlateIndex)` Q_INVOKABLE；ProjectServiceMock 新增 `setObjectPlateForIndex(int, int)` 方法从源平板移除并添加到目标平板；已新增平板设置对话框（对齐上游 `PlateSettingsDialog`）：平板右键菜单新增"平板设置"项，Dialog 包含热床类型选择（PEI/EP/PC/纹理PEI/自定义，对齐上游 BedType 枚举）、打印顺序选择（按层/按对象，对齐上游 PrintSequence）、螺旋花瓶模式（跟随全局/开启/关闭，对齐上游 spiral_vase），ProjectServiceMock 新增 `plateBedTypes*`/`platePrintSequences*`/`plateSpiralModes*`存储和对应 getter/setter，EditorViewModel 新增 6 个桥接 Q_INVOKABLE；已扩展 BedType 枚举完整对齐上游（btDefault/btPEI(smooth)/btPEI(hightemp)/btPTE/btPC/btEP/btER/btCustom，共 8 种），打印顺序新增"跟随全局"选项（对齐上游 ByDefault）；已新增首层耗材顺序配置（对齐上游`first*layer_print_sequence`）：ProjectServiceMock 新增 `plateFirstLayerSeqChoices*`/`plateFirstLayerSeqOrders*`存储（Auto/Custom 模式 + 挤出机顺序数组），EditorViewModel 新增 4 个桥接 Q_INVOKABLE，PreparePage 平板设置对话框新增首层耗材顺序面板（Auto/Custom 模式切换 + 自定义时显示挤出机列表，对齐上游 DragCanvas）；已新增其他层耗材顺序配置（对齐上游`other_layers_print_sequence`+`OtherLayersSeqPanel`）：ProjectServiceMock 新增 `MockLayerSeqEntry`结构体（beginLayer/endLayer/extruderOrder）和`plateOtherLayersSeqChoices*`/`plateOtherLayersSeqEntries\_`存储（Auto/Custom 模式 + 动态层范围条目列表），支持添加/删除/修改层范围条目、begin_layer 必须 >= 2（对齐上游校验规则）、自动排序（对齐上游 auto-sort），EditorViewModel 新增 10 个桥接 Q_INVOKABLE，PreparePage 平板设置对话框新增其他层耗材顺序面板（层范围条目列表 + 起始/结束层输入框 + 添加/删除按钮 + 挤出机颜色条，对齐上游 OtherLayersSeqPanel）；新增`plateExtruderCount()` 获取平板使用耗材数（Mock 模式基于对象数推断）；项目保存/加载已完整序列化所有新增平板设置数据；已新增 GL FBO 缩略图渲染（对齐上游 `GLCanvas3D::render_thumbnail_framebuffer`）：GLViewport 新增 `CaptureThumbnail` InputEvent 和 `requestThumbnailCapture()` Q_INVOKABLE，GLViewportRenderer 新增 `captureThumbnailToFBO(int size)` 使用 QOpenGLFramebufferObject 创建临时 FBO、正交相机俯视图、仅渲染 mesh（不渲染 gizmo/grid）、glReadPixels 读取像素、Y 轴翻转后编码为 base64 PNG，通过 `thumbnailCaptured()` 信号跨线程传递结果；PreparePage 平板卡片优先使用 GL FBO 缩略图（当前平板），回退到 QPainter 合成缩略图；已新增 DragCanvas 拖拽排序交互（对齐上游 `DragCanvas` 拖拽排序）：首层耗材顺序面板从静态颜色条升级为 Flow 布局的拖拽排序挤出机 pill（Drag.active + DropArea swap-on-drop），每个 pill 显示挤出机编号和颜色圆点，拖拽时半透明高亮 + 目标位置边框提示，顺序变更自动持久化到 ViewModel（setPlateFirstLayerSeqOrder/setPlateOtherLayersSeqOrder）；其他层耗材顺序每个层范围条目的挤出机顺序同样支持拖拽排序（独立 Drag.keys 避免跨条目拖拽冲突）；`setPlateFilamentMap()` 现已同步写入 live `PartPlate::config()` 的 `filament_map_mode` 与 `filament_map`，确保当前会话的 `SliceService::plateDynamicConfig()` 消费与保存数据一致，空映射会清除旧配置；`PartPlateTests` 直接验证 live config，唯一构建命令验证通过（E2E 29 passed, 0 failed）2026-09-01；仍缺真实切片引擎耗材分配结果读回和手动分配编辑 UI。2026-09-02 P2.1 收口：读回半项已由 Phase 108 完成（hasAutoFilamentMap/autoFilamentMapMode/autoFilamentMaps 三属性 + FilamentGroupPopup 自动推荐预览）；本轮补齐手动分配编辑 UI——EditorViewModel 新增 setPlateFilamentMap(plate, mode, maps) 全量写桥接（委托 ProjectServiceMock，同步 live PartPlate::config 并失效该盘切片）与 plateFilamentMaps 读桥接，FilamentGroupPopup 自定义模式新增每挤出机分组步进编辑器（1..N 组、恒等映射回退种子、存储映射回填），修复弹窗对 editorVm.projectService 的越界访问（改为 ViewModel 桥接，符合 QML 不触服务的边界规则）；上游 GUI 侧 filament_map 编辑仅存在于设备流程（SelectMachine/DeviceManager，外部阻塞），盘内真值为 PlateData::filament_maps + Print::get_filament_maps 配置语义，已完整承接。新测试 manualFilamentMapBridgeRoundTripsToService（往返 + 非法盘拒绝）+ 2 项审计断言；canonical 全绿（exit 0）：E2E 30/30、ViewModel 185/185、QML audit 172/172。
- [-] P2.2 已完成准备页顶部工具条和左右侧栏的第一轮视觉重构；已新增底部状态指示器显示选中对象数量和当前状态文本；已新增 ProcessBar 分类标签栏（对齐上游 `ProcessBar::GLToolbar`）；已新增 CxIconButton 工具栏动画反馈（对齐上游 GLToolbar 按钮状态机）；已新增对象列表拖拽重排序（对齐上游 `GUI_ObjectList` OnBeginDrag/OnDrop）：ObjectList ListView 新增 drag reorder 支持，对象行通过 Drag.keys="object-reorder" 启动拖拽，目标行 DropArea 接收并调用 `EditorViewModel::moveObject()`，ProjectServiceMock::moveObject() 交换所有平行数组（names/moduleNames/printableStates/visibleStates/transforms/overrides）并更新 plateObjectIndices\_ 引用；已新增对象行内重命名（对齐上游 `GUI_ObjectList::rename_item`）：对象名称 Text 支持双击切换为 TextField 行内编辑（Enter/失焦确认），右键菜单新增"重命名"项；已增强拖拽排序视觉反馈（对齐上游 GUI_ObjectList 拖拽放置位置指示）：拖拽源行自动降低不透明度至 40%（opacity 状态机）；DropArea 新增 `onEntered`/`onExited` 追踪悬浮状态，目标行高亮背景色（8% accent 透明度）+ 顶部 2px accent 插入指示线；拖拽结束时自动清除所有视觉状态；已增强跨平板拖拽放置视觉反馈（对齐上游 GUI_ObjectList 拖拽放置指示）：平板卡片 delegate 新增 `dragHover` 状态属性，拖拽悬浮时显示 2px accent 边框高亮 + 半透明叠加层 + 居中 "+" 图标（150ms 淡入动画），DropArea 改用 `parent.dragHover` 属性驱动视觉状态（替代原先直接修改 `parent.color` 的脆弱方式），onDropped 正确清除 dragHover 状态；已新增合成缩略图渲染（对齐上游 `PartPlate::thumbnail_data`）：ProjectServiceMock 新增 `generatePlateThumbnail(int plateIndex, int size)` 使用 QPainter 合成平板缩略图（深色背景 + 热床网格 + 3D 风格对象方块 + 对象编号 + 已切片绿色角标），返回 base64 PNG 供 QML Image 组件显示；EditorViewModel 新增桥接 Q_INVOKABLE；PreparePage 平板卡片缩略图从 32x32 彩色矩形升级为 48x48 QImage 合成缩略图（包含网格线/对象轮廓/编号）；已新增工具栏编辑操作按钮组（对齐上游 `GLCanvas3D` main toolbar delete/copy/paste/clone/delete_all）：PreparePage 工具栏新增"添加平板"按钮（layout-grid-plus.svg，对齐上游 add_plate）、"删除选中"按钮（trash.svg，对齐上游 delete）、"清空全部"按钮（x.svg，对齐上游 delete_all）、"复制"按钮（copy.svg，对齐上游 copy）、"粘贴"按钮（clipboard.svg，对齐上游 paste）、"克隆"按钮（clone.svg，对齐上游 clone），所有按钮带 enabled 状态绑定（hasSelection/hasClipboardContent/objectCount>0/plateCount<10）和工具提示（含快捷键）；已补入可见的"清空全部"工具栏动作：GLToolbars 以 `modelCount > 0` 对齐上游 `can_delete_all()`，仅发出 `deleteAllRequested()`，PreparePage 转发到 main.qml 共享确认框，确认后调用 `EditorViewModel::clearWorkspace()`，未复用仅清空当前平板的 `removeAllOnPlate()`；已新增剪贴板快捷键（对齐上游 Ctrl+C/V/X）：PreparePage Keys.onPressed 新增 Qt.Key_C 调用 copySelectedObjects()、Qt.Key_V 调用 pasteObjects()、Qt.Key_X（无Shift）调用 cutSelectedObjects()、Qt.Key_X（Shift）切换 Cut Gizmo 模式；新增 copy.svg/clipboard.svg/clone.svg/trash.svg/plus.svg/layout-grid-plus.svg 图标资源并注册到 qml.qrc；已实现 GL FBO 缩略图渲染管线（对齐上游 `render_thumbnail_framebuffer`），平板卡片优先使用 GL 捕获缩略图 2026-03-14
- [-] P2.3 左侧对象工作区已补入 plate 卡片切换、对象所属 plate 信息、基础右键动作，以及基于 ViewModel 的多选与批量操作；对象列表中的状态切换已从本地假显隐改为承接上游 `printable` 语义，并已支持按平板 / 按模块两种组织方式、分组头展开/收起，以及对象节点下的 volume 子层级展示；当前还新增了 volume 子项的同对象内多选、高亮、批量删除，并让删除快捷键与顶部工具栏删除动作跟随当前选中节点，同时加入了基于上游规则的基础删除约束；此外已补入与上游 `Edit in Parameter Table` 对应的单对象 / 单部件参数表入口，并让右侧打印参数面板开始对部分上游真实 key 承接对象 / 部件 scope 的读取与写回；本轮进一步把 `initial_layer_print_height`、`infill_wall_overlap`、`chamber_temperature` 等真实 key 纳入 Qt6 参数表承接；已新增 ObjectManipulation 面板（对齐上游 `ObjectManipulation`）；已补全对象右键上下文菜单（对齐上游 `create_object_menu`/`create_extra_object_menu`）；已新增 Add Volume 子菜单（对齐上游 `GUI_Factories::append_menu_items_add_volume`）；已新增 volume 级 extruder 分配基础设施（对齐上游 `ModelVolume::extruder_id`）：`MockVolumeEntry` 新增 `extruderId` 字段（-1=继承父对象，0+=指定挤出机），ProjectServiceMock 新增 `volumeExtruderId()`/`setVolumeExtruderId()` 方法，EditorViewModel 新增对应桥接方法；网格面拾取基础设施已就位（ray-triangle intersection + CPU 侧顶点副本）；已新增 Seam Painting Gizmo 基础实现（对齐上游 `GLGizmoSeam`）：`SupportPaintTypes.h` 新增 `SeamPaintTool`/`SeamPaintSettings` 枚举和结构体，GLViewport 新增 `GizmoSeamPaint=7` 模式，EditorViewModel 新增 6 个缝线绘制 Q_PROPERTY（seamPaintTool/seamPaintCursorRadius/seamPaintOnOverhangsOnly）和 `clearSeamPaintOnSelection()` Q_INVOKABLE，PreparePage 工具栏新增缝线绘制按钮和视口上方缝线绘制控制面板（强制缝线/阻止缝线工具选择、光标半径滑块、清除全部按钮）；绘制数据管理层已落地（PaintEngine 包装上游 TriangleSelector + FacetsAnnotation 写回 + RHI/Software overlay 渲染），并补齐绘制类型隔离：PaintEngine 缓存键升级为 (object, volume, kind)，`OWzx::PaintKind`（Support/Seam/Mmu）成为渲染层规范枚举（ProjectServiceMock 以别名复用），EditorViewModel 的 paint/smartFill/commit/resync/overlay 全部按活动 kind 路由，PreparePage 切换绘制 Gizmo 时显式赋值 `activePaintKind`（0/1/2），修复 Seam/Mmu 笔画误写入 supported_facets 的串扰；ViewModelSmokeTests 新增 Support/Seam 同体隔离回归，QML 审计锁定 kind-aware API 与 QML 路由 2026-09-01；仍登记显式差异清单：①clipping-plane 画笔（上游 ClippingPlane 传入 select_patch，Qt 侧固定默认裁剪面）②seed-fill 区域预览可视化（上游 seed_fill_select_triangles 预览高亮）③支撑体积自动生成（上游从 enforcer 面生成支撑 mesh）④Support/Seam/MMU 面板 Details 折叠区与上游逐项对齐

2026-09-02 P2.3 差异清单四项全部实现：①clipping-plane 画笔——新增 ViewModel paintClippingPosition（0..1，0=关）+ resetPaintClippingPlane()，RhiViewport 两个绘制信号追加 cameraForward（forwardVector()，上游 get_dir_forward），EditorViewModel::buildVolumeClippingPlane 复刻上游两段链（ObjectClipper::set_position_by_ratio 的法线=-相机前向/offset=center+n·c+r-ratio·2r（r 取上游同源 instance_bounding_box 半对角线，selectionWorldBoundingBox），get_clipping_plane_in_volume_coordinates 的全逆变换+转置线性转体坐标），被裁剪侧拾取命中直接拒绝（is_mesh_point_clipped，GLGizmoPainterBase.cpp:383-393），平面线程穿透 PaintEngine::paintAt/smartFillAt→cursor_factory/seed_fill_select_triangles；支撑/缝线/MMU 三面板均挂截面滑条+重置。②seed-fill 预览可视化——PaintEngine 新增 PaintSelector 只读子类暴露受保护 m_triangles，getSeedSelection/hasSeedSelection 读暂存区（上游 TriangleSelectorGUI::render 同理由子类访问），paintOverlayData 以负 state 标记（-待应用 EnforcerBlockerType）追加暂存面片，渲染器 stateColor 对负 state 按 get_seed_fill_color 基色×1.25 提亮；新测试锁定首击暂存可读、二击提交时序。③支撑体积预览——上游 gizmo 内切片后渲染 SupportLayer::support_fills 挤出路径（GLGizmoFdmSupports.cpp:826-928），Qt 侧在切片 worker process() 后同源捕获 support_fills 多段线（端点对展开为毫米坐标线段，plate origin 同擦拭塔处理），经 supportPreviewReady 信号（Q_DECLARE_METATYPE）→ ViewModel 字节管道（measure 格式）→ RhiViewport Q_PROPERTY → 渲染器灰色（0.5,0.5,0.5 同上游 GLVolume）线管线，仅支撑绘制 gizmo 激活时显示；绘制笔画写回时清空管道（对齐上游 supported_facets.timestamp 失效判定），下次切片重建。④面板逐项对齐——支撑面板补上游 remove 工具（擦除=state 0），缝线面板补光标类型（上游 cursor_type 圆形/球体）与截面行，MMU 面板补截面行；残余差异登记：上游 MMU 的 edge_detection/gap_area/height_range 滑条与 FDM GapFill 工具（需 TriangleSelectorPatch 级实现）；2026-09-02 残余差异实现收口：①MMU 高度范围工具——PaintEngine 新增 PaintCursorType::HeightRange 与 paintHeightRangeAt（上游 HEIGHT_RANGE 画笔 GLGizmoPainterBase.cpp:726-733：cursor_factory 世界 Z 锚 + select_patch 的 hr_cursor 全网格扫描），RhiViewport 新增 heightRangePickRequested 信号与 brushHeightRange 属性（0.2mm 默认、0.1..8 同上游），EditorViewModel::paintHeightRangeAt 两段拾取 + 裁剪面拒绝 + 命中点世界 Z 锚定；MMU 面板新增高度范围工具 chip 与跨度滑条。②边检测——paintEdgeDetection 布尔（上游 m_detect_geometry_edge 默认 true），关闭时 smart fill 角度强制 -1°（GLGizmoMmuSegmentation.cpp:640-643，区域生长止于近共面邻域），MMU 面板新增边检测复选框。③缝隙面积——supportPaintGapArea（0..5mm² step 0.2 同上游 TriangleSelectorPatch::gap_area）暴露到支撑面板缝隙面积滑条，缝隙填充工具 chip（toolType 3）激活时 PaintEngine::getGapFragments（TrianglePatch::is_fragment 面积阈值语义，懒建选择器）以 -3 琥珀标记进 overlay（渲染器专用色），仅支撑 gizmo 生效（MMU 的 toolType 3 是高度范围，按 gizmoMode 区分分派）。新测试 paintHeightRangeGapFragmentsAndEdgeDetectionMatchUpstream（带选择性/碎片阈值/-1°角停止生长三段）+ 6 项审计断言；canonical 全绿（exit 0）：E2E 30/30、ViewModel 184/184、QML audit 172/172。新测试：ViewModelSmokeTests 裁剪面拒绝/seed 暂存时序 2 例，E2E 支撑管道打包/失效清除 1 例，QML 审计 4 项接线断言。canonical 验证全绿（exit 0）：E2E 30/30（新增支撑管道测试）、ViewModel 182/182（新增裁剪面拒绝+seed 暂存时序 2 例）、QML audit 172/172（新增 8 项 P2.3 接线断言）
- [-] P2.4 右侧工作区已开始承接真实切片结果摘要；已新增 per-plate 切片状态追踪（`m_slicedPlateIndices`），切片完成后自动标记对应 plate，对象变更时清除当前 plate 标记，清空工作区时全部重置；`EditorViewModel::isPlateSliced(int)` 提供给 QML 查询；平板卡片显示绿色圆点标记已切片的 plate；已新增 SliceInfoPanel 对齐的切片结果摘要面板：SliceProgress.qml 重构为使用 Theme 设计令牌，新增切片结果分区标题、分隔线、耗材用量（长度）、预估成本（$xx.xx）等字段显示；SliceService 新增 `resultFilamentLabel()`/`resultCostLabel()` 存储切片结果；EditorViewModel 新增 `sliceResultFilament`/`sliceResultCost` 桥接属性；Mock 模式新增模拟切片流程（5 步进度 + 400ms 延迟 + 基于对象数量生成模拟结果），替代原先直接失败的错误提示；已新增切片后操作栏（对齐上游 SliceInfoPanel 后续操作）：切片完成后显示"预览"、"导出 G-code"和"全部切片"三个操作按钮，预览按钮调用 `EditorViewModel::switchToPreview()` 发射 `previewRequested()` 信号，PreparePage 通过 Connections 转发到 `backend.setCurrentPage(2)` 切换到 Preview 页面（对齐上游 `Plater::priv::on_preview`）；耗材重量显示旁已新增耗材颜色标记小方块（对齐上游 SliceInfoPopup filament color badge）；已新增切片层数显示（对齐上游 SliceInfoPanel）：EditorViewModel 新增 `sliceResultLayerCount` Q_PROPERTY（从 SliceService::resultLayerCount() 桥接），SliceProgress.qml 切片结果摘要新增"切片层数"行（显示 N 层，仅切片完成后可见）；已新增模型尺寸信息显示（对齐上游 SliceInfoPanel model dimensions）：EditorViewModel 新增 `modelSizeText` Q_PROPERTY（从 measureDimensions 桥接，格式 "W × D × H mm"），SliceProgress.qml 切片结果摘要新增"模型尺寸"行（等宽字体显示，有选中对象时可见）；已新增右侧面板三段式可折叠结构（对齐上游 `Plater::Sidebar` Printer/Material/ParamsPanel 三段式布局）：新建 `CollapsibleSection.qml` 可复用组件（标题栏点击切换展开/折叠，高度动画200ms OutCubic + 透明度渐变 + 箭头旋转指示器），`PrintSettings.qml` 从扁平堆叠重构为 3 个 `CollapsibleSection`：Section 1 打印机（打印机预设选择 + CRUD + 热床类型选择器，对齐上游 Sidebar Printer Section），Section 2 耗材（耗材槽位网格 + 兼容性警告指示器 + 自动匹配按钮，对齐上游 Sidebar FilamentSection），Section 3 打印设置（作用域选择器 + 打印/耗材预设行 + 分类选项列表，对齐上游 ParamsPanel）；已新增平均打印速度显示（对齐上游 PrintEstimatedStatistics）；已新增多平板切片状态摘要；2026-09-02 P2.4 对照收尾：预估时间格式对齐上游 get_bbl_time_dhms 紧凑 `NdNhNmNs`（SliceInfoPanel.cpp/StatusPanel.cpp 同源，替换原 HH:MM:SS），重量格式对齐 `%.2fg` 无空格（真实与 mock 路径一致），板标签行已在 SliceProgress 面板显示；上游 `thumbnail_url` 云端缩略图拉取为 BBL 云特性，登记为外部阻塞（需云协议）；canonical 验证通过（12 套件全绿 exit 0，E2E 29/29 含新紧凑 DHMS 断言）。已新增排列设置对话框（对齐上游 `ArrangeSettings` popup）：EditorViewModel 新增 5 个排列设置 Q_PROPERTY（arrangeDistance/arrangeRotation/arrangeAlignY/arrangeMultiMaterial/arrangeAvoidCalibration）和 `resetArrangeSettings()` 方法；GLViewport InputEvent 新增 arrange payload（spacing/rotation/alignY），`arrangeSelected()` 改为带参数版本；GLViewportRenderer 排列算法升级支持上游参数（可配置间距、自动旋转 0°/90°/180°/270°、对齐 Y 轴单列排列）；PreparePage 新增排列设置 Popup 对话框（间距输入 + 自动旋转/对齐Y轴/多耗材/避免校准区域 4 个开关 + 重置/排列按钮），工具栏排列按钮改为打开设置对话框，平板右键菜单"排列对象"使用当前设置参数 2026-03-14
- [-] P2.5 已实现 Move/Rotate/Scale 三模式 Gizmo 切换系统和线框模式：GLViewport 新增 gizmoMode 属性支持 Move/Rotate/Scale 三种模式切换，GLViewportRenderer 新增旋转环面几何体（torus rings）和缩放立方体手柄几何体，Mesh shader 升级为 mat4 模型矩阵支持完整的平移+旋转+缩放变换，对象拾取更新为变换后 AABB 检测，undo/redo 支持完整 TransformCmd 记录，PreparePage 工具栏新增移动/旋转/缩放模式按钮；线框模式通过"更多"菜单切换，使用 glPolygonMode + DARK_GRAY 颜色匹配上游 Plater::toggle_show_wireframe；已新增 Measure Gizmo 基础实现（对齐上游 `GLGizmoMeasure`）：GLViewport 新增 `GizmoMeasure=3` 模式，EditorViewModel 新增 `measureDimensions` Q_PROPERTY（dx/dy/dz/体积），`refreshMeshCacheAndFitHint()` 中从 TLV 边界框数据计算尺寸，PreparePage 工具栏新增测量按钮、Ctrl+U 快捷键、视口上方尺寸信息面板（X/Y/Z 维度 + 体积，等宽字体，RGB 分色标注）；已新增测量拾取模式（对齐上游 `GLGizmoMeasure` feature/point selection）：EditorViewModel 新增 `measureSelectionMode` Q_PROPERTY（0=点测量/1=特征测量），Measure 面板新增模式切换按钮（点测量/特征测量），特征测量模式显示提示文本"点击网格面拾取特征 (点/边/圆/平面)"；已新增自动朝向（对齐上游 `Plater::orient()` / `AutoOrienter`）：EditorViewModel 新增 `autoOrientSelected()` 方法，Mock 模式使用异步模拟，PreparePage 工具栏和对象右键菜单均添加"自动朝向"入口；已新增 Flatten Gizmo（对齐上游 `GLGizmoFlatten`）：GLViewport 新增 `GizmoFlatten=4` 模式，EditorViewModel 新增 `flattenSelected()` 方法（Mock 模式模拟凸包面检测，6 个候选面，将选中对象最大面朝下平放）、`flattenFaceCount` Q_PROPERTY，PreparePage 工具栏新增平放按钮（G 快捷键）、视口上方 Flatten 设置面板（候选面计数 + 平放按钮 + 提示文本）；已新增 Cut Gizmo（对齐上游 `GLGizmoCut`）：GLViewport 新增 `GizmoCut=5` 模式，EditorViewModel 新增 `cutSelected()` 方法（Mock 模式将对象切割为 2 个部分）、`cutAxis`/`cutPosition`/`cutKeepMode` Q_PROPERTY + `flipCutPlane()`/`centerCutPlane()` Q_INVOKABLE，PreparePage 工具栏新增切割按钮（Ctrl+Shift+X 快捷键）、视口上方 Cut 控制面板（X/Y/Z 轴选择 pill、位置滑块 -50~50mm、保留模式全部/上半/下半 pill、翻转/居中/执行切割按钮）；已新增切割连接器设置（对齐上游 `GLGizmoCut` connector type/shape/size）：EditorViewModel 新增 6 个连接器 Q_PROPERTY（cutMode/connectorType/connectorStyle/connectorShape/connectorSize/connectorDepth），支持切割模式切换（平面切割/舌槽模式 Dovetail，对齐上游 `CutMode`）、连接器类型选择（Plug/Dowel/Snap，对齐上游 `ConnectorType`）、连接器形状选择（△□⬡○ 四种，对齐上游 `ConnectorShape`）、连接器尺寸和深度滑块，舌槽模式下显示连接器参数面板；新增 scissors.svg 和 layers-subtract.svg 图标资源；已新增视口告警显示系统（对齐上游 EWarning / Plater::\_set_warning_notification）：EditorViewModel 新增 viewportWarning 属性和 viewportWarningMessage 属性，checkViewportWarnings() 方法基于对象位置检查热床范围和打印高度，PreparePage 视口底部新增告警覆盖层；已新增真实网格面拾取（对齐上游 mesh face picking / Möller–Trumbore）：MeshBatch 新增 `cpuVerts` CPU 侧顶点副本存储，`pickObject()` 从 AABB 近似升级为 ray-triangle intersection（先 AABB 早退，再逐三角形 Möller–Trumbore 检测，ray 变换到模型空间进行精确交叉，hit 距离变换回世界空间排序）；已新增 Support Painting Gizmo 基础实现（对齐上游 `GLGizmoFdmSupports`）：GLViewport 新增 `GizmoSupportPaint=6` 模式，`src/core/rendering/SupportPaintTypes.h` 新增支撑绘制类型定义（SupportPaintState/SupportPaintCursorType/SupportPaintToolType 枚举对齐上游 EnforcerBlockerType/CursorType/ToolType），EditorViewModel 新增 11 个支撑绘制 Q_PROPERTY（supportPaintTool/supportPaintCursorType/supportPaintToolType/supportPaintCursorRadius/supportPaintAngleThreshold/supportPaintSmartFillAngle/supportPaintGapArea/supportPaintOnOverhangsOnly/supportEnable/supportType）和 2 个 Q_INVOKABLE（setSupportPaintToolFromQml/clearSupportPaintOnSelection），PreparePage 工具栏新增支撑绘制按钮（P 快捷键）；已新增 Hollow Gizmo 基础实现（对齐上游 `GLGizmoHollow`）：`SupportPaintTypes.h` 新增 `HollowGizmoSettings` 结构体（enableHollowing/newHoleRadius/newHoleHeight/hollowingOffset/hollowingQuality/closingDistance/selectedHoleCount），GLViewport 新增 `GizmoHollow=8` 模式，EditorViewModel 新增 8 个 SLA 空洞标记 Q_PROPERTY（hollowEnabled/hollowHoleRadius/hollowHoleHeight/hollowOffset/hollowQuality/hollowClosingDistance/hollowSelectedHoleCount）和 `deleteSelectedHollowPoints()` Q_INVOKABLE，PreparePage 工具栏新增 SLA 空洞标记按钮和视口上方空洞标记控制面板（启用空洞化开关、钻孔半径/高度/偏移滑块、删除选中钻孔按钮）；但仍未完成上游完整 Hollow Gizmo GL 渲染和真实空洞几何生成（需 libslic3r）、Support Painting GL 渲染层和 TriangleSelector 数据层；已新增真实 Cut 连接（对齐上游 GLGizmoCut::perform_cut → cut_mesh）：ProjectServiceMock 新增 `cutObject(int, int, double, int)` 调用 `Slic3r::cut_mesh()` 执行真实网格切割（合并 model_part volumes → 轴对齐旋转 → cut_mesh 分割 → 反向旋转 → 替换原对象/新增切割结果），EditorViewModel cutSelected 在 HAS_LIBSLIC3R 模式下调用真实 cut 并 syncTransformsFromModel，构建验证 269/269 通过；已新增对象信息显示（对齐上游 `Plater::show_object_info`）：ProjectServiceMock 新增 `objectOpenEdges()`/`objectRepairedErrors()`/`objectVolume()` 方法（HAS_LIBSLIC3R 调用 `ModelObject::get_object_stl_stats()` 获取 TriangleMeshStats），EditorViewModel 新增 5 个 Q_PROPERTY（selectedObjectInfoText/selectedObjectTriangles/selectedObjectOpenEdges/selectedObjectRepairedErrors/selectedObjectIsManifold），PreparePage 底部新增对象信息栏（单选时显示对象名/尺寸/体积/三角面数/流形状态/非流形边警告/已修复错误计数），流形状态用颜色圆点指示（绿=OK/橙=已修复/红=非流形） 2026-03-22
- [-] P2.6 已新增上游对齐的键盘快捷键（W/E/R 切换 Gizmo 模式、G 平放、Ctrl+Shift+X 切割、Ctrl+Z/Ctrl+Y undo/redo、Ctrl+D 复制选中、Delete 删除选中、Escape 取消选择、Ctrl+A 全选、F 适应视图）和对象右键上下文菜单（复制选中、删除、全选、取消选择、Gizmo 模式切换、镜像 X/Y/Z 轴、显示/隐藏、适应视图），并通过 QML Shortcut 确保快捷键在焦点管理下可靠触发；已新增 Slice All（切片全部平板）和 Export G-code（导出 G-code 文件对话框）功能；已实现镜像操作、对象克隆/复制、对象显示/隐藏切换；已新增自动排列（arrange）：对齐上游 `Plater::priv::on_arrange`，GLViewport 新增 `ArrangeSelected` InputEvent 类型和 `arrangeSelected()` Q_INVOKABLE，GLViewportRenderer 实现基于 AABB 的网格排列算法（支持上游 ArrangeSettings 参数：可配置间距/自动旋转/对齐Y轴，支持 undo/redo），PreparePage 工具栏排列按钮已改为打开设置对话框；已新增剪贴板操作：EditorViewModel 新增 `copySelectedObjects()`/`pasteObjects()`/`cutSelectedObjects()` 和 `hasClipboardContent` 属性，ProjectServiceMock 新增 `addObject(name)` 方法（支持 HAS_LIBSLIC3R 真实 ModelObject 创建和 Mock 模式元数据添加）；已新增拆分对象（对齐上游 `Plater::priv::split_object` / `ModelObject::split`）：EditorViewModel 新增 `splitSelectedObject()` 方法（支持单对象选中限制、HAS_LIBSLIC3R 调用真实 ModelObject::split() 拆分网格为独立 ModelObject、Mock 模式模拟拆分为 2 个子对象），ProjectServiceMock 新增 `splitObject(int)` 方法（克隆模型→split→移除原对象→添加拆分结果→重建元数据→同步变换数组），PreparePage 工具栏和对象右键菜单均添加"拆分对象"入口（仅单选时启用）；已新增项目加载（对齐上游 `Plater::load_project`）：ProjectServiceMock 新增 `loadProject(filePath)` 方法，从 JSON 恢复全部项目状态（项目名/源路径/平板列表/对象元数据/per-object 参数覆盖），BackendContext::topbarOpenProject() 在 3MF 加载失败时自动尝试 JSON 加载，EditorViewModel 新增 `refreshAfterLoad()` 刷新 UI 状态，文件对话框已支持 `.json` 格式打开和保存；已补全平板右键上下文菜单（对齐上游 `PartPlate` context menu）：新增"排列对象"和"自动朝向"菜单项，与上游 e_at_lay/e_at_dir 对齐；已新增底部切片状态指示器（对齐上游 `PartPlate::m_slice_result_valid`）：当前平板已切片时显示绿色"✓ 已切片"徽章、切片进行中时显示黄色"切片中 X%"进度徽章；已新增全部平板切片完成标记（EditorViewModel 新增 allPlatesSliced Q_PROPERTY检查所有平板 isPlateSliced 状态），SliceProgress 新增全部切片完成提示；已实现 GL FBO 平板缩略图渲染（对齐上游 `render_thumbnail_framebuffer`）；但仍未完成上游完整 Plater 工作流对照：缺 Hollow Gizmo（SLA 专用）、缺平板自定义对话框（已部分完成于 P2.1）；已新增真实自动排列（对齐上游 Plater::priv::on_arrange → arrange_objects）：ProjectServiceMock 新增 `arrangeObjects(float, bool, bool)` 调用 `Slic3r::arrange_objects(*model_, InfiniteBed, ArrangeParams)` 替代 GLViewportRenderer 的 AABB Mock 排列，EditorViewModel 新增 `arrangeAllObjects()` Q_INVOKABLE 调用真实 arrange 并 syncTransformsFromModel，PreparePage QML 改为调用 editorVm 路径；已新增真实镜像（对齐上游 ModelInstance::set_mirror）：ProjectServiceMock 新增 `mirrorObject(int, int)` 调用 `inst->set_mirror(axis, -current)` 切换镜像状态，EditorViewModel 新增 `mirrorSelectedObjects(int)` 对每个选中对象调用真实 mirror 并同步变换，构建验证 269/269 通过 2026-03-19

### P2.7 — 未迁移 Gizmo

以下上游 Gizmo 已完成基础 Mock 承接，GL 渲染层和真实 libslic3r 集成仍待完成：
- [-] GLGizmoEmboss（文字浮雕）— EditorViewModel embossSelected() 已接入真实 API：委托 ProjectServiceMock::addTextVolume() 调用 Emboss::create_font_file + text2shapes + polygons2model + ProjectZ，支持 embossHeight 参数传递；GLViewport 新增 GizmoEmboss=12；QML 工具栏按钮 + 控制面板；2026-09-02 对照收尾：默认字高对齐上游 FontProp.size_in_mm=10mm（原 2），投影深度默认 1mm 且 QML 允许负值（雕刻方向，对齐上游可负深度），真实 volume 名称改用完整文本（换行折叠为空格，对齐 GLGizmoEmboss.cpp:3273-3277），Mock 列表条目保留 12 字符截断显示。仍记录为差异：use_surface/curve_projection 几何效果、Join/Cut/Modifier 类型选择；默认占位文本 "Embossed text" 已于 2026-09-02 对齐（EditorViewModel m_textContent 种子 = 上游 GLGizmoEmboss.cpp:500 _u8L("Embossed text")，审计断言锁定）；同轮新增 Emboss 体积类型选择器（上游 volume_type：模型部件/负体积/修改器）——ProjectServiceMock 新增 m_textVolumeType + setTextVolumeType/textVolumeType/volumeType 访问器，addTextVolume 路由真实 ModelVolumeType，EditorViewModel 桥接 embossVolumeType，PreparePage 文字面板类型 chips；新测试 textVolumeTypeRoutesNewVolumes（默认/负体积/修改器/越界回退）；canonical 全绿（exit 0：E2E 31/31、ViewModel 186/186、QML audit 174/174）。
- [-] GLGizmoMmuSegmentation（MMU 多耗材分段）— 基础设施已实现：GLViewport 新增 GizmoMmuSegmentation=10 枚举值，EditorViewModel 新增 mmuSelectedExtruder/mmuExtruderCount 属性和 clearMmuSegmentation() 方法；QML 已实现：工具栏按钮 + 控制面板（16 色耗材色块选择器、当前耗材提示、清除分段按钮）；per-triangle 实际绘制已打通：经 PaintEngine kind 路由（Mmu selector）+ Phase 122/123 桥接写入 `mmu_segmentation_facets`，`mmuKindPaintWritesMmuAnnotationOnly` 运行时回归锁定 MMU 笔画只落入 mmu 注解且 Support/Seam 注解保持为空（对齐上游 GLGizmoMmuSegmentation.cpp:700），2026-09-01 canonical 全量验证通过（ViewModel 178/178，0 failed）
- [-] GLGizmoDrill（钻孔）— **上游核实（2026-09-01）：锁定上游无 GLGizmoDrill（third_party/OrcaSlicer 无该类），属 OWzx 自有决策（CrealityPrint 时代功能保留）。当前实现 drillSelected() 调用 ProjectServiceMock::drillObject() → Slic3r::its_make_cylinder 创建钻头网格做真实网格切削；后续增强（射线定位、按实例恢复）按 OWzx 产品决策推进，不再计为上游补齐缺口。**
- [x] GLGizmoFaceDetector（自动面检测）— P2.7 已实化（2026-09-02）：自造的 faceDetectorAngle 阈值 mock 替换为真实 libslic3r 引擎——`ProjectServiceMock::detectExteriorFaces()` 调用 `Slic3r::FaceDetector::detect_exterior_face()`（FaceDetector.cpp：合并 volume 网格后以采样间隔对 ±X/±Y/±Z 射线步进 `sla::IndexedMesh::query_ray_hit`，命中面片标记 `eExteriorAppearance`），QML 面板对齐上游 UI（Sample interval 输入 + Perform Recognition 按钮 + 外表面面片计数）。注：上游 gizmo 的 perform_recognition 调用本身被注释（引擎存在但未接线），Qt6 侧直接接线引擎属行为补全而非发明。运行时回归 `faceDetectorRecognizesAllCubeFacetsAsExterior`：20mm 立方体 12 三角面全部识别为外表面、粗网格 ≥6 面、确定性及非法输入 -1
- [-] GLGizmoMeshBoolean（布尔运算）— EditorViewModel booleanExecute() 已接入真实 API：调用 Slic3r::MeshBoolean::cgal::plus/minus/intersect（CGAL 确认可用 via libslic3r_cgal）；GLViewport 新增 GizmoMeshBoolean=13；QML 工具栏按钮 + 控制面板；验证 273/273 通过 2026-03-20；2026-09-01 已补齐上游 Volume 级语义：新增 `meshBooleanVolumes()` 走 `MeshBoolean::mcut::make_boolean`（UNION/A_NOT_B/INTERSECTION，MCUT 失败时回退 CGAL corefinement），结果以 `<旧名> - union|difference|intersection` 新 volume 追加进源对象并继承旧 volume 的 config/类型/偏移（generate_new_volume，GLGizmoMeshBoolean.cpp:423-461），旧源 volume 恒删除，新增“删除输入”复选框（union 恒删）控制工具 volume/空工具对象的移除，撤销改走整场景快照（SceneSnapshotCommand）；运行时回归 `meshBooleanVolumeLevelReplacesSourceVolume` 锁定删除/保留工具两条路径。
- [-] GLGizmoSimplify（网格简化）— 后端已实现：ProjectServiceMock::simplifyObject() 调用 libslic3r its_quadric_edge_collapse 真实简化算法（复制网格→简化→回写 ModelVolume::set_mesh），EditorViewModel 新增 simplifyWantedCount/simplifyMaxError 属性和 simplifySelected()/selectedObjectTriangleCount() 方法；QML 已实现：工具栏按钮 + 控制面板（当前面数/目标面数滑块/最大误差滑块/执行简化按钮）；GLViewport 新增 GizmoSimplify=9 枚举值 2026-03-18
- [x] GLGizmoText（文字工具）— EditorViewModel addTextObject() 已接入真实 API：委托 ProjectServiceMock::addTextVolume() 调用 Emboss::create_font_file + text2shapes + polygons2model + ProjectZ，支持 textSize/embossDepth 参数传递；GLViewport 新增 GizmoText=16；QML 工具栏按钮 + 控制面板；2026-09-02 对照收尾（P2.7）：textSize 字号此前为死输入，现已作为字形高度转发 setEmbossHeight（上游 font_size 驱动字形尺寸，默认 16），默认字体改为枚举到的第一个系统字体（对齐 GLGizmoText.cpp init_face_names，不再硬编码 arial）。注：上游 Perform Recognition 面板本身存在表面点击放置，Qt6 采用首对象附着近似并已文档化
- [x] GLGizmoSVG（SVG 导入）— EditorViewModel importSVG() 已接入真实 API：委托 ProjectServiceMock::addSvgVolume() 调用 Model::read_from_file 加载 SVG 为 3D 几何体（nanosvg+OCCT 棱柱挤出默认 10mm，与上游 select_shape 深度一致）；GLViewport 新增 GizmoSVG=17；QML 工具栏按钮 + 控制面板；2026-09-02 对照收尾（P2.7）：svgScale 缩放此前为死输入，现作为 XY 缩放传入 addSvgVolume（对齐上游 Size+m_keep_ratio），depthModifier 语义=深度倍率（1.0=10mm 默认）
- [-] GLGizmoSlaSupports（SLA 支撑绘制）— GLViewport 新增 GizmoSlaSupports=18；QML 工具栏按钮 + SLA 支撑说明面板 2026-03-18
- [-] GLGizmoAdvancedCut（高级切割，含连接器）— advCutSelected() 已接入真实 API：cutMode==1 时委托 ProjectServiceMock::cutObjectWithGroove() 调用 Cut::perform_with_groove() 榫卯切割（含 Groove 验证），cutMode==0 回退到 cutObject() 基础平面切割；connectorSize 映射为 groove.depth，默认 width=4x depth，flaps_angle=PI/3，angle=0；QML 工具栏按钮 + 控制面板；2026-09-02 对照收尾（P2.7）：连接器默认尺寸 5→2.5mm（对齐上游 connector_size，GLGizmoAdvancedCut.cpp:82-86）；cutObjectWithConnectors 现在有连接器时强制 KeepUpper|KeepLower（对齐 GLGizmoAdvancedCut.cpp:644-645）。仍记录差异：keep 语义仅有保留两侧/仅上两种（无 keep-lower-only 与逐面 Place-on-cut/Flip）、槽默认值为 Qt 发明（上游从 0 起步显式输入）、上游 AdvancedCut 内的 groove 分支本身是未完成合并（真值为 GLGizmoCut3D+CutUtils，Qt 已对齐该真值）、Snap 类型经复核上游 Model.hpp:246 CutConnectorType 确实存在（Plug/Dowel/Snap/Undef），原登记有误、Qt 侧保留 Snap 正确 2026-09-02
- [-] GLGizmoPainterBase（绘制基类，当前 SupportPaint/SeamPaint 为独立实现）— SupportPaint/SeamPaint 已独立实现，PainterBase 作为架构抽象暂不单独迁移 2026-03-18
- [-] GizmoObjectManipulation（对象操作面板独立化）— 对象操作面板已集成在 PreparePage 左侧面板中（移动/旋转/缩放/测量），独立化暂不需要 2026-03-18
- [-] GLGizmosManager（Gizmo 管理器，当前直接在 GLViewportRenderer 中管理）— Gizmo 管理直接通过 GLViewport::gizmoMode enum 实现，管理器架构暂不需要 2026-03-18

### P2.8 — 3D 渲染缺失项

- [x] P2.8.1 3DBed 纹理贴图渲染 — GLViewportRenderer 新增程序化棋盘格纹理（512x512, 8x8 tiles, 上游 DEFAULT_MODEL_COLOR/DEFAULT_MODEL_COLOR_DARK 配色）+ XZ 平面四边形（220x220mm, y=-0.04 对齐上游 GROUND_Z），独立 bed shader（GLSL 330 core），showBed 属性同步，构建验证通过 2026-03-18
- [-] P2.8.2 Shell 渲染（对齐上游 GCodeViewer shell）——2026-09-06 语义澄清：本项与 P17.5 为同一事项（canonical ID `PREVIEW-GHOST-SHELL`，见 WORK_ITEMS.yaml）。RHI ghost shell 近似已交付（P17.5，2026-08-29：按挤出机染色、含切片生成几何，透明度 0.15→0.35；上游基线 `set_shell_transparency` alpha=0.15）；剩余 per-volume shell 完整生命周期，暂不做 GPU 大改造。
- [x] P2.8.3 擦料塔可视化 — GLViewport 新增 6 个 WipeTower Q_PROPERTY（show/width/depth/height/x/z），GLViewportRenderer 新增半透明方盒渲染（6面36三角, alpha 0.5, dirty flag 优化几何体重建），底部对齐床面 GROUND_Z，构建验证通过 2026-03-18
- [x] P2.8.4 GLTexture/GLShader 完善对齐 — 新建 GLShaderUtil.h/.cpp（compileShaderProgram() 工具函数），重构 GLViewportRenderer 5 个和 GCodeRenderer 2 个重复 shader 编译块为单行调用，CMakeLists 添加新文件，消除 shader 编译代码重复 2026-03-18

### P3 — Preview 工作区

- [-] P3.1 **[验证通过 2026-03-17]** 数据通路架构已建立（SliceService→PreviewViewModel→GCodeRenderer→GLViewport），键盘快捷键（Space/Left/Right/Home/End/PageUp/PageDown）、jumpToLayer()、moveLayerRange()、LayerSlider 鼠标滚轮+右键菜单+悬浮提示均已实现；已修复阻断性缺陷（commit 0f17966：移除 rebuildFromGCode() 中清空状态后的裸 return;），G-code 解析管线恢复正常；已新增 3D Marker 渲染（对齐上游 GCodeViewer::Marker）：GCodeRenderer 新增 gouraud_light 风格 shader（双方向光：顶光+前光）、程序化圆锥+圆柱箭头几何体（16段细分）、alpha 半透明渲染（0.75 opacity），GLViewport 新增 markerX/Y/Z + showMarker 属性，PreviewPage.qml 绑定 PreviewViewModel.toolX/Y/Z → GLViewport，工具位置跟随 G-code 播放实时更新（含 G-code↔GL 坐标 y↔z 互换）；已优化键盘快捷键步进对齐上游 IMSlider（Left/Right 单步1、Shift 10步、Ctrl 100步，替代原先固定100步）；构建验证 273/273 通过；仍缺：Shell 渲染（需 libslic3r 集成） 2026-03-17
- [-] P3.2 LayerSlider（双拇指拖拽+From/To 输入框+Jump to Layer 对话框+鼠标滚轮+悬浮提示+右键菜单+非选中范围淡化）和 MoveSlider（播放/暂停+时间轴滑块+currentTime 格式化+工具切换彩色色带+悬浮时间提示）的 UI 组件已完整实现且对齐上游 IMSlider 核心交互；已新增 groove 悬浮 Z 高度+层时间提示（对齐上游 IMSlider::draw_tick_on_mouse_position，显示 Layer 号/Z 高度/层耗时三行信息）；已新增 groove 点击跳转（对齐上游 IMSlider slider_behavior，点击 groove 自动移动最近拇指到目标位置）；P3.1 阻断性缺陷已修复，数据管线已恢复；构建验证 269/269 通过；仍需端到端验证确认 MoveSlider 彩色色带、时间显示在有真实 G-code 数据时正确展示 2026-03-17
- [-] P3.3 13 种 EViewType 颜色映射逻辑已完整实现（FeatureType 分类色 / Height/Width/Feedrate/FanSpeed/Temperature/VolumetricRate 渐变 / Tool/ColorPrint/FilamentId 挤出机色板 / LayerTime/LayerTimeLog/Acceleration 渐变），Range_Colors 10 色梯度与上游 #0b2c7a→#942616 完全匹配（PreviewViewModel.cpp kRangeColors），Legend.qml 三模式渲染（discrete/gradient/extruder）已实现，渐变图例端点色已修正为 #0b2c7a 与上游完全一致；已对齐上游 GCodeViewer get_view_type_string 显示名称（Line Type/Layer Height/Line Width/Speed/Fan Speed/Temperature/Filament/Flow/Layer Time 等）；仍需端到端验证 2026-03-17
- [-] P3.4 GCodeRenderer OpenGL 渲染管线已建立（GL330 core shader + VAO/VBO + 单遍过滤渲染），CameraController 集成轨道/缩放/平移+自动 fitView+四个预设视角，ToolPositionTooltip 组件已实现 14 个 Q_PROPERTY 绑定；已新增 3D Marker 渲染（gouraud_light shader + 圆锥圆柱几何体 + alpha 混合），showBed 已连接 GCodeRenderer 渲染端（网格线渲染），showTravelMoves 已通过 RGB 颜色范围过滤实现；已新增 ToolPositionTooltip 层耗时字段（对齐上游 Marker::render LayerTime 字段）；仍缺：Shell 渲染（需 libslic3r） 2026-03-17
- [-] P3.5 StatsPanel 已实现完整统计面板（总时间/层数/移动数/耗材/成本/速度/工具切换/角色时间分解/层时间图表/每挤出机用量/Normal-Stealth 模式切换/显示选项开关组），对齐上游 PrintEstimatedStatistics 核心字段；showMarker/showBed/showTravelMoves 开关均已连接渲染端（3D Marker + 热床网格 + RGB 过滤）；Stealth 模式用固定 1.4x 乘数简化上游更精确的时间重算逻辑；已新增图例折叠/展开开关（对齐上游 GCodeViewer m_legend_enabled）；仍需端到端验证统计数据在真实 G-code 下正确填充 2026-03-17

### P4 — Settings / Preset / 覆盖作用域

- [-] P4.1 `ConfigViewModel`、`ConfigOptionModel`、`PresetListModel`、`SettingsPage` 已有动态配置骨架；SettingsPage 新增"重置默认"按钮调用 `ConfigOptionModel::resetToDefaults()`；已新增搜索结果计数标签（搜索时显示"N 项匹配"），与脏选项计数并列显示；已新增 PreferencesPage 通用设置页（对齐上游 `PreferencesDialog::create_general_page`）：新增"通用"分类（index 0），包含"启动时显示主页"开关（`showHomePage`）、"默认页面"选择（主页/准备，对齐上游 default_page radio）、"单位"选择（公制/英制，对齐上游 units combo）、"用户角色"选择（基础/专业，对齐上游 user_role radio），SettingsViewModel 新增 4 个 Q_PROPERTY（`showHomePage`/`defaultPage`/`units`/`userRole`）；已扩展通用设置页：新增"自动保存"开关 + 间隔选择（5/10/15/30 分钟，对齐上游 auto_save）、"启动时检查更新"开关（对齐上游 preset_update）、"减少动画效果"开关（对齐上游 enable_reduce_motion），SettingsViewModel 新增 `autoSave`/`autoSaveInterval`/`checkUpdates`/`reducedMotion` Q_PROPERTY，`resetPreferences()` 已包含新属性重置；已扩展通用设置页：新增"区域设置"选择（对齐上游 PreferencesDialog region combo），支持 5 个区域（跟随系统/中国/美国/欧洲/日本），SettingsViewModel 新增 `region` Q_PROPERTY；已新增"账号与隐私"分类内容（index 5）：自动备份项目到云端开关（对齐上游 cloud sync backup），SettingsViewModel 新增 `autoBackup` Q_PROPERTY；已新增"高级"分类内容（index 7）：低细节模式（LOD）开关（对齐上游 enable_reduce_detail），SettingsViewModel 新增 `compactMode` Q_PROPERTY；撤销栈上限选择（20/50/100/200，对齐上游 undo/redo 历史限制），SettingsViewModel 新增 `undoLimit` Q_PROPERTY；但仍缺上游完整 Settings 工作流（真实云端同步集成、隐私数据管理、开发者模式高级调试选项等）；2026-09-02 P4.1 收口（外部阻塞项除外）：开发者模式分类（index 8，developerMode 门控显隐）已含调试覆盖层/日志级别/详细 G-code/OpenGL 调试上下文/日志大小等调试选项；本轮补齐上游通用页 Develop mode 区块的 "Skip AMS blacklist check"（Preferences.cpp:1195）——SettingsViewModel 新增 skipAmsBlacklistCheck 持久化属性 + PreferencesPage 开发者分类复选框（AMS 设备流程外部阻塞，开关仅持久化并如实标注）；剩余 P4.1 差异仅真实云端同步集成与隐私数据管理（外部阻塞）2026-03-14
- [-] P4.2 `PrintSettings.qml` 已完成一轮参考图导向重构
- [-] P4.3 当前参数体系仍以 Qt 侧模型为主；已新增 `ConfigOptionModel::loadFromUpstreamSchema()` 从上游 `print_config_def` 读取真实配置定义；上游 schema 加载 `kDesiredKeys` 已扩展到约 110 个 key；Mock 模式硬编码选项扩展到约 75 个 key；scopedWritableKeys 扩展到约 60 个 key；新增"加速度"分类；已新增 ConfigOptionModel OptionGroup 层级（对齐上游 `Tab::OptionGroup`）：ConfigOption struct 新增 `group` 字段存储分组标题，ConfigOptionModel 新增 `GroupRole` 和 `optGroup()` 访问器；全部 75 个 Mock 选项已分配约 30 个分组；SettingsPage 参数列表已显示分组标题（深色背景分隔行）；已新增 ConfigOptionModel Tab Page 层级（对齐上游 `Tab::Page`）：ConfigOption struct 新增 `page` 字段存储顶层页面（Quality/Strength/Speed/Support/Base/Cooling/Retraction/Other），ConfigOptionModel 新增 `PageRole`、`optPage()`、`pageNames()`、`filterIndicesByPage()` 方法和 `pageForCategory()` 静态映射函数（中文/英文 category 双向映射），ConfigViewModel 新增 `filterIndicesByPage()` 桥接方法；PrintSettings.qml 选项列表已重构为 Page > Category > Options 三层级 Repeater 结构（对齐上游 Tab::Page > ConfigOptionsGroup > Option），Page header 显示英文页面名（accent 色），Category header 显示中文分类名（深色背景），选项行显示 label + slider/checkbox/combobox 控件；已修复 ConfigOption struct 的 MSVC aggregate init 兼容性问题（添加 10 参数和 11 参数构造函数）；已新增预设继承链值详情查询（对齐上游 PresetBundle value_at_level）：ConfigViewModel 新增 `valueChainForKey(key)` Q_INVOKABLE 返回完整 JSON（default/printer/filament/print/current 五级值），PresetServiceMock 新增 `presetValue(name, key)` 单键查询方法；已新增重置选项到指定层级（对齐上游 Tab reset_to_level）：ConfigViewModel 新增 `resetOptionToLevel(key, level)` Q_INVOKABLE（0=default/1=print/2=filament/3=printer），支持将单个选项回退到继承链中的指定层级值；已新增 Simple/Advanced 模式过滤（对齐上游 ConfigOptionMode）：ConfigOption struct 新增 `mode` 字段（0=Simple only, 1=Advanced only, 2=Both），ConfigOptionModel 新增 `ModeRole` 和 `optMode()` 访问器，ConfigViewModel::filterOptionIndices() 新增 `advancedMode` 参数（mode=1 选项在 Simple 模式下隐藏，mode=0 选项在 Advanced 模式下隐藏），PrintSettings.qml Advanced Switch 已连接到 rebuildFilter 实现模式切换；已新增模糊搜索算法（对齐上游 `fts_fuzzy_match`）：ConfigViewModel::searchOptions() 从子串匹配升级为模糊匹配（贪心字符匹配 + 连续匹配奖励 + 前缀奖励 + 单词边界奖励 + 缩写匹配奖励 + gap 惩罚），结果按相关性分数降序排序（高分优先，同分按 key 字母序）；已新增搜索结果分组字段暴露：ConfigViewModel 新增 `searchResultGroup()`/`searchResultCategory()`/`searchResultPage()` Q_INVOKABLE；SearchDialog.qml 结果格式升级为"Category : Group : Label"层级显示（对齐上游 SearchDialog 显示格式），新增类型图标（打印机⌖/耗材◎/打印✦/默认•），类型图标和源标签使用对应颜色着色；已新增搜索跳转滚动到可见（对齐上游 `Tab::activate_option` ensure_visible）：PrintSettings.qml ScrollView 新增 `scrollToHighlight()` 方法，搜索跳转时自动滚动到目标选项居中显示（300ms OutCubic 动画）；loadFromUpstreamSchema() 已从上游 PrintConfigDef 提取真实 tooltip（ConfigOptionDef::tooltip）和 mode（ConfigOptionMode: comSimple/comAdvanced/comDevelop）和 sidetext（输入框单位文本）；optUnit() 优先使用上游 sidetext，回退到 key-based heuristic；构建验证 269/269 通过 2026-03-19；已修复 Simple/Advanced 模式过滤器语义（对齐上游 ConfigOptionMode：comDevelop=2 在 Simple 模式下隐藏，改为 `optMode >= 1` 替代原先 `optMode == 1`）；但仍缺真实上游 Tab 数据加载 2026-03-19 已实现对象/部件/平板三级作用域覆盖体系（对齐上游 `Tab` scope 语义和 `ModelObject`/`ModelVolume` config override）：ProjectServiceMock 新增 Mock 模式 per-object/per-volume/per-plate scoped option 存储（`m_mockObjectOverrides`/`m_mockVolumeOverrides`/`m_mockPlateOverrides`），`scopedOptionValue()` 和 `setScopedOptionValue()` 在 Mock 模式下正确读取和写入层叠覆盖（volume > object > global）；新增 `plateScopedOptionValue()`/`setPlateScopedOptionValue()` 方法支持平板级参数覆盖；ConfigViewModel 新增 `activatePlateScope(int plateIndex)` 方法，`activateObjectScope()` 正确区分 "object" 和 "volume" 两种作用域类型（volumeIndex >= 0 时为 volume scope），`buildScopeValues()` 和 `handleOptionValueChanged()` 支持全局/对象/部件/平板四种作用域的正确路由和值合并，`readonlyKeysForCurrentScope()` 对 volume 和 object scope 限制为约 60 个可写 key；PrintSettings.qml 作用域 UI 更新为 3 个 pill 按钮（全局/对象或部件/平板），scopeTitle/scopeSubtitle 支持全部 4 种作用域类型的中文显示；已新增 Layer Range 支持（对齐上游 `ModelObject::layer_config_ranges`）；已新增预设继承链值详情查询和重置到层级功能；已新增值来源层级弹窗点击编辑（对齐上游 Tab value source 可视化 + reset_to_level）；已新增选项修改状态可视化（对齐上游 `Tab::Field` m_is_modified_value / OptStatus）；已新增全局修改选项摘要栏（对齐上游 Tab::modified_options）：PrintSettings.qml 新增可折叠 Modified Options 面板（显示修改计数、展开/收起、列出每项 key/当前值/默认值 + 单项重置按钮）；已新增作用域差异对比面板（对齐上游 Tab::is_modified_value per-scope diff）+ 全部重置按钮（`resetAllScopeOverrides()`）；已新增类型安全的 scoped option 读写（对齐上游 ConfigOptionType）：readConfigValue 改为 switch(option->type()) 替代硬编码 float key 列表，正确处理 coFloat/coPercent/coFloatOrPercent/coInt/coBool/coString/coEnum/coEnums 所有类型；writeConfigValue 新增 string→enum label 自动查找（遍历 enum_labels 匹配），支持 ConfigOptionString 写入；构建验证 269/269 通过 2026-03-19；但仍缺上游 libslic3r 真实 Tab 数据加载 2026-03-19；2026-09-02 P4.3 收口：audit 驱动的真实 Tab 数据装载从 Process 层扩展到 machine/filament 两层——新增 auditMachineManifest/auditFilamentManifest（对照 Preset::printer_options()/filament_options() 运行时真值 + print_config_def 兜底 + 非 UI 字段豁免集，镜像 auditProcessManifest 契约），kMachineKeys 扩 ~30 键（自适应热床网格/挤出机避让空间/冷却风扇/运动限制速率/多材料换料/挤出机回抽行列全覆盖）、kFilamentKeys 扩 ~63 键（热床全板型温度/压力前进/退痛工具更换/多工具 ramming/空气过滤/风扇细分），kPrinterPageGroupMap/kFilamentPageGroupMap 同步补全映射；audit 逮出 8 个 Prusa 遗留陈旧键（extruders_count/default_fan_speed/filament_change_gcode/filament_min_speed/filament_retract_length_toolchange/filament_retract_restart_extra_toolchange/filament_spool_weight/nozzle_temperature_range——BBS print_config_def 已不存在）并移除；ConfigOptionModel 新增 tierMappingErrors() Q_INVOKABLE，loadMachineSchema/loadFilamentSchema 加载时输出非致命审计告警；ViewModelSmokeTests 新增 configTierManifestAuditsCoverUpstreamPresetOptions 断言两层审计为空。canonical 验证全绿（exit 0）：E2E 30/30、ViewModel 183/183、QML audit 172/172，0 条 tier 审计告警
- [-] P4.5 搜索功能已在 SettingsPage 和 PrintSettings.qml 实现（`filterOptionIndices()` 支持按 key/label 子串搜索）；已新增配置选项脏状态追踪：ConfigOptionModel 新增 `m_dirtyKeys` 集合追踪已修改选项、`optIsDirty(int)` / `dirtyCount()` / `resetOption(int)` 方法、`DirtyRole` 和 `TooltipRole` 角色；`setValue()` 和 `applyValues()` 自动对比默认值更新脏状态、`resetToDefaults()` 清除所有脏状态；已新增 30 个关键选项的帮助文案 tooltip（中文，对齐上游 `ConfigOptionDef::tooltip`）；SettingsPage 显示脏标记（橙色圆点+加粗标签）、脏计数显示（搜索栏旁）、鼠标悬浮帮助提示（ToolTip）；已新增预设保存/加载和脏检测：PresetServiceMock 新增 `presetValues()`/`savePresetValues()`/`hasPreset()` 方法，支持按预设名存储和读取 key-value 值映射；ConfigViewModel 新增 `saveCurrentPreset()`（保存当前值到预设）、`isPresetDirty`（对比当前值与保存快照的差异）、`setCurrentPreset()` 增强（加载预设值时自动应用并刷新快照），PrintSettings.qml 预设保存按钮已连接 `saveCurrentPreset()`；已新增 3-tier 预设继承链（对齐上游 `PresetBundle`）：PresetServiceMock 新增 `presetNamesForCategory(int category)` 和 `defaultPresetForCategory(int category)` 方法支持 Print/Filament/Printer 三类预设，内置 8 个默认预设（3 打印质量：0.20mm Standard/0.16mm Fine/0.28mm Draft、3 耗材：PLA/ABS/PETG、2 打印机：K1C/Ender-3 S1），每类预设包含该类别的关键参数默认值；ConfigViewModel 新增 `currentPrinterPreset`/`currentFilamentPreset`/`currentPrintPreset` 三组 Q*PROPERTY 和对应的 setter 方法，`mergePresetHierarchy()` 方法实现 printer → filament → print 三级值覆盖合并，`loadDefault()` 初始化三类预设为默认值；PrintSettings.qml 新增 3-tier 预设选择器 UI（3 行 ComboBox，分别显示打印/耗材/打印机预设），替代原先的单行预设选择；已新增自定义预设创建/删除 UI（对齐上游 `PresetBundle::save_current_preset`/`delete_preset`）：PresetServiceMock 新增 `createCustomPreset()`/`deletePreset()`/`isBuiltinPreset()` 方法，动态管理 `m_categoryPresets` 按分类维护预设列表，内置预设名存储在 `m_builtinPresetNames` 集合中不可删除，自定义预设可随时删除并自动从分类列表移除；ConfigViewModel 新增 `createCustomPreset()`/`deletePreset()`/`canDeletePreset()` Q_INVOKABLE 方法；PrintSettings.qml 每个预设 ComboBox 旁新增 "+" 保存按钮（创建自定义预设副本）和 "✕" 删除按钮（仅自定义预设显示）；已新增预设切换自动匹配（对齐上游 `PresetBundle::update_compatible`）：`setCurrentPrinterPreset()` 现在调用 `autoMatchFilament()` 在切换打印机时自动匹配兼容耗材；已新增预设脏状态可视化指示器（对齐上游 Tab dirty mark）：PrintSettings.qml 3-tier 预设行（打印/耗材/打印机）每行标签旁新增橙色圆点指示 `isPresetDirty` 状态，当配置有未保存修改时显示；已新增预设重命名功能（对齐上游 PresetBundle 重命名）：PresetServiceMock 新增 `renamePreset()` 方法（内置预设不可重命名、空名/重复名检查、自动更新分类列表中的名称引用），ConfigViewModel 新增 `renamePreset()` 桥接方法（自动更新当前活跃预设名引用），PrintSettings.qml 3-tier 预设行每个删除按钮旁新增"✎"重命名按钮（仅自定义预设显示），点击弹出 Dialog 对话框输入新名称（含取消/确认按钮，打开时自动全选当前名称）；已新增预设兼容性矩阵（对齐上游 `PresetBundle::is_compatible` / `update_compatible`）：打印机预设新增 `nozzle_diameter`/`max_nozzle_temp`/`machine_max_speed` 属性（K1C: 0.4/300°C/600mm/s, Ender-3 S1: 0.4/260°C/500mm/s），耗材预设新增 `nozzle_temp_range_min`/`nozzle_temp_range_max`/`compatible_nozzle_min`/`compatible_nozzle_max` 属性（PLA: 190-230°C/0.2-0.8mm, ABS: 230-270°C/0.2-0.8mm, PETG: 190-250°C/0.2-0.8mm）；PresetServiceMock 新增 `isFilamentCompatibleWithPrinter()` 方法（检查喷嘴直径范围和最高温度兼容性）和 `findCompatibleFilament()` 方法（遍历耗材列表返回第一个兼容的耗材）；ConfigViewModel `autoMatchFilament()` 已从空实现更新为调用 `findCompatibleFilament()` 自动切换到兼容耗材并重新合并层级；ConfigViewModel 新增 `isCurrentFilamentCompatible()` Q_INVOKABLE 供 QML 检查当前组合是否兼容；已新增兼容性警告 UI（对齐上游 Tab incompatible warning）：PrintSettings.qml 耗材预设行标签旁新增红色圆点警告指示器（仅当前耗材与打印机不兼容时显示，带 Tooltip 提示"当前耗材与打印机可能不兼容"）；已实现耗材列表中灰化不兼容项完整过滤 UI（对齐上游 Tab 兼容性灰化显示）：ConfigViewModel 新增 `isFilamentCompatible(const QString &filamentName)` Q_INVOKABLE，PrintSettings.qml 耗材预设 ComboBox 替换为自定义委托的 raw ComboBox，下拉列表中不兼容耗材项显示为灰化文本 + 删除线 + 红色 "!" 警告图标，兼容耗材保持正常显示；已新增增强搜索对话框（对齐上游 `SearchDialog` / `OptionsSearcher`）：新建 `SearchDialog.qml` 组件，支持搜索 key/label/category/group 四个字段（150ms 防抖），搜索结果按 Page > Category 分组显示、键盘上下导航、Enter 跳转、Esc 关闭，搜索框内嵌放大镜按钮打开搜索对话框，Ctrl+F 快捷键直接打开；已新增预设继承链值来源追踪（对齐上游 Tab `get_sys_and_mod_flags`）：`ConfigViewModel::mergePresetHierarchy()` 现在记录每个 key 的值来源层级（`valueSources*`哈希表），新增`valueSourceForKey(key)` Q_INVOKABLE 返回 "printer"/"filament"/"print"/"default" 来源标识，`searchResultSource(int)`/`searchResultPath(int)` 供搜索结果面板查询；PrintSettings.qml 选项行左侧新增值来源色点指示器（蓝色=打印机预设、橙色=耗材预设、绿色=打印预设、灰色=默认值），搜索结果面板底部显示来源图例；搜索对话框支持跳转到目标选项并高亮显示（3 秒淡出）；已新增搜索结果匹配文本高亮（对齐上游 `ColorMarkerStart/End`）：SearchDialog.qml 搜索结果 label 使用 `highlightParts()` 函数将匹配文本拆分为高亮/非高亮段（accent 色加粗 vs 默认色），SettingsPage 参数列表搜索时标签加粗显示；smoke test 扩展至 44/44 检查项（新增 SearchDialog.qml 资源完整性检查） 2026-03-14

### P5 — Device / Monitor / Network

- [-] P5.1 Monitor 页已有可运行页面与基础布局；已完成 P5.1 基础功能迁移：DeviceServiceMock 重构为 MockDevice 结构体（5 台设备，含 name/model/sn/status/online/progress/taskName/ip/temperature/signalStrength），支持按名称/型号/SN/状态搜索过滤和设备选中；MonitorViewModel 提供完整 Q*PROPERTY 驱动（filteredDeviceCount/searchText/selectedDevice\*/networkOnline/latencyMs），Q_INVOKABLE deviceAt(int) 和 selectDevice(int) 供 QML Repeater 使用；MonitorPage.qml 对齐上游 Monitor.cpp/MonitorBasePanel.cpp 的左右分栏布局（左侧 280px 设备列表含搜索栏、设备卡片、在线指示灯、信号强度、打印进度、状态标签；右侧设备详情含 4 个 Tab 对齐上游 Tabbook：状态/SD 卡/视频/HMS），状态面板显示设备状态/温度/IP/打印进度条/连接方式/信号强度/序列号卡片，3 秒 mock 轮询模拟上游 1 秒 REFRESH_INTERVAL；已新增 SD 卡文件管理 Tab（对齐上游 `MediaFilePanel`）：存储用量条（1.8/5.2 GB）、8 个 Mock G-code 文件列表（文件名/大小/日期三列）、导入/删除操作按钮；已新增 HMS 健康管理 Tab（对齐上游 HMSPanel / DeviceManager hms_list）：DeviceServiceMock 新增 MockHmsItem 结构体（moduleId/moduleNum/partId/msgLevel/msgCode/alreadyRead/message/wikiUrl，对齐上游 HMSNotifyItem）和 hmsLists* 存储，Mock 设备 0 含 3 条 HMS（2 未读）、设备 3 含 2 条 HMS（2 未读），提供 selectedDeviceHmsCount/selectedDeviceUnreadHmsCount/selectedDeviceHmsAt/markHmsRead 方法；MonitorViewModel 新增 selectedHmsCount/selectedUnreadHmsCount Q_PROPERTY 和 hmsAt/markHmsRead Q_INVOKABLE 桥接；MonitorPage.qml HMS Tab 包含：头部未读计数徽章、HMS 总数文本、可滚动 HMS 列表（每条记录显示严重度着色边框、严重度图标、级别徽章（严重/警告/一般/信息）、未读圆点、错误代码、消息文本、模块标签）、点击标记已读、"全部标为已读"按钮、空状态显示、Tab 标题显示未读数量；但仍缺上游真实 DeviceManager/MachineObject/MQTT 连接、StatusPanel 完整状态机（MONITOR_NO_PRINTER/MONITOR_CONNECTING/MONITOR_DISCONNECTED/MONITOR_NORMAL）、MediaFilePanel 真实文件传输/打印功能、HMSPanel 真实 HMS 上报/清除协议、SideTools 打印机选择弹窗 2026-03-14
- [-] P5.2 已完成设备交互 Mock 基础迁移（对齐上游 DeviceManager/MachineObject 设备交互层）：DeviceServiceMock 新增 scanDevices()（对齐上游 SSDP 发现）、connectDevice()/disconnectDevice()（对齐上游 DeviceManager connect/disconnect，Mock 异步 1.5s 延迟模拟连接）、startPrint()/pausePrint()/resumePrint()/stopPrint()（对齐上游 Plater 打印控制）、devicePrintStage()/devicePrintLayer()/devicePrintTimeLeft()（对齐上游 MachineObject 打印状态查询）；新增 PrintJobState 结构体（gcodePath/stage/progress/totalLayers/currentLayer/timeLeft/active/paused）和 QTimer 驱动的打印模拟器（1s 间隔，自动推进进度/层号/剩余时间，stage 在 heating→mop_first_layer→printing→cooling 间自动切换，完成时停止定时器）；MonitorViewModel 新增对应桥接方法（scanDevices/connectDevice/disconnectDevice/startPrint/pausePrint/resumePrint/stopPrint）和 selectedPrintStage/selectedPrintLayer/selectedPrintTimeLeft Q_PROPERTY；MonitorPage.qml 状态面板新增打印控制栏（打印阶段/层号/剩余时间显示 + 连接/断开/开始打印/暂停/继续/停止 6 个按钮，按设备状态动态显隐），左侧扫描按钮已接入 scanDevices()；但仍缺上游 AMS（多耗材丝切换）、多灯光控制/录像功能、AI 辅助能力、真实 MQTT 连接和命令协议 2026-03-14
- [-] P5.3 已完成视频/摄像头 Mock 基础设施（对齐上游 CameraPopup / MediaPlayCtrl / StatusPanel camera enums）：新增 CameraServiceMock（CameraStreamStatus 5 态：Disconnected/Connecting/Connected/Streaming/Error；CameraRecordingStatus 对齐上游 5 态枚举；CameraTimelapseStatus 对齐上游 5 态枚举），支持 startStream()/stopStream()（Mock 异步连接序列 1.5s+0.8s）、toggleRecording()/toggleTimelapse()、switchCamera()（3 虚拟摄像头循环）、retryConnection()、takeScreenshot()、分辨率切换（720P/1080P）、自定义 RTSP URL 输入；MonitorViewModel 新增 15 个桥接方法和 Q_PROPERTY（cameraStreamStatus/cameraRecordingStatus/cameraTimelapseStatus/cameraResolution/cameraUrl/cameraErrorMessage/cameraAvailable + 7 个 Q_INVOKABLE）；MonitorPage.qml 新增"视频"Tab（替换原"更新"Tab），包含视频视口区域（Mock 占位 + 流状态图标 + 错误信息）、录像/延时摄影指示器覆盖层（REC 红点 + 延时摄影蓝标签）、控制工具栏（连接/断开/录像/延时摄影/切换摄像头/截图 6 个按钮，按流状态动态显隐）、分辨率选择器（720P/1080P pill 按钮）、自定义 URL 输入框；但仍缺上游真实 RTSP/WebRTC/FFmpeg 视频解码集成（RTSPDecoder.h / WebRTCDecoder.h）、GStreamer Bambu 视频源、MediaFilePanel 视频文件浏览/下载/回放、wxMediaCtrl2 媒体播放控件 2026-03-14
- [-] P5.4 已完成云账号和设备绑定 Mock 基础设施（对齐上游 NetworkAgent / WebUserLoginDialog / BindDialog / CommunicateWithCXCloud / AccountDeviceMgr）：新增 CloudServiceMock（loggedIn/userName/userEmail/boundDeviceCount/syncing/lastSyncTime Q_PROPERTY），支持 login()/logout()（Mock 异步 1.2s 延迟，登录后自动填充 2 台绑定设备）、bindDevice()/unbindDevice()（对齐上游 PingCodeBindDialog/UnBindMachineDialog）、syncPresets()（Mock 2s 同步延迟，更新 lastSyncTime）；HomeViewModel 新增 10 个 Q_PROPERTY（cloudLoggedIn/cloudUserName/cloudUserEmail/cloudBoundDeviceCount/cloudSyncing/cloudLastSyncTime）和 6 个 Q_INVOKABLE（cloudLogin/cloudLogout/cloudBindDevice/cloudUnbindDevice/cloudBoundDeviceAt/cloudSyncPresets）+ cloudLoginFailed 信号；HomePage.qml 首屏头部"登录账号"按钮替换为动态云账号区域（未登录显示登录按钮、已登录显示用户头像+名称+设备计数+同步按钮+登出按钮），新增云端设备列表（显示绑定设备卡片含在线指示灯/名称/SN/解绑操作+添加设备按钮），新增登录对话框（用户名/密码输入+错误提示，对齐上游 WebUserLoginDialog）、绑定设备对话框（设备名称+PIN 码输入，对齐上游 PingCodeBindDialog）；但仍缺上游真实 bambu_networking 登录协议、wxWebView OAuth 认证流程、MQTT 设备连接、CommunicateWithCXCloud 云端预设同步、AI 云服务集成（AICloudServiceDialog） 2026-03-14

### P6 — Calibration / Project / Auxiliary / Model Mall / MultiMachine

- [-] P6.1 Project、Calibration、Preferences 等页面具备基础入口；CalibrationPage 已完成上游对齐重构：对照上游 CalibrationPanel（Tabbook + CalibrationWizard 分栏布局）、CalibrationDialog（硬件校准步骤选择）、CalibrationWizard（向导步骤链 Start→Preset→Cali→Save）实现左侧校准类型列表 + 右侧向导步骤指示器 UI；CalibrationServiceMock 新增 5 种校准类型（Flow Dynamics/Flow Rate/Bed Leveling/Vibration Compensation/Max Volumetric Speed，对齐上游 CalibMode 枚举和 CalibrationDialog 校准项）和每类型 2-6 步向导流程（对齐上游 CalibrationWizardPageStep 链）；CalibrationViewModel 新增 12 个 Q_PROPERTY（currentStepIndex/currentStepTitle/currentStepDesc/totalStepCount/selectedStatus/selectedCategory 等）驱动 QML 向导步骤指示器、进度条、状态徽章；CalibrationPage 新增状态显示（InProgress/Completed/Failed 徽章 + 每项状态点）、向导步骤条（圆形指示器 + 连接线 + 步骤名）、当前步骤描述面板、进度条、分类筛选药丸按钮；已新增步骤状态追踪（对齐上游 StepCtrl pending/active/completed 状态）：CalibrationServiceMock 新增 `stepState(int typeIndex, int stepIndex)` Q_INVOKABLE（0=pending/1=active/2=completed），运行中根据 currentStepIndex 实时计算每个步骤状态，校准完成后所有步骤标记为 completed，CalibrationViewModel 新增 `stepState(int stepIndex)` 桥接方法；CalibrationPage 步骤指示器重构为使用 `modelData.state` 属性驱动（圆形指示器显示数字或 ✓ 对号、连接线根据状态着色、步骤名根据状态高亮），stepChanged 信号自动刷新步骤状态；但仍未完成上游完整设备连接/预设选择/真实参数编辑/历史记录/CalibrationCaliPage 打印任务管理等深度功能 2026-03-14
- [-] P6.2 校准工作流已对照上游校准模块开始逐项承接：CalibrationServiceMock 模拟 5 种校准类型和完整向导步骤链（对齐上游 CalibState: Start→Preset→Calibration→CoarseSave→FineCalibration→Save→Finish）；CalibrationViewModel 桥接步骤导航（goToStep）、进度追踪、状态管理；CalibrationDialog 保留为独立弹窗用于校准执行确认；已新增 CalibrationPresetPage 耗材预设选择器（对齐上游 `CalibrationPresetPage`）：CalibrationViewModel 新增 `currentStepId`/`showPresetSelector`/`filamentPresetNames`/`selectedFilamentPreset` Q_PROPERTY 和 `setSelectedFilamentPreset()` 方法，通过 `setPresetService()` 注入 PresetServiceMock 访问耗材预设列表（复用 3-tier 预设体系）；CalibrationPage 在步骤描述区新增条件显示的耗材预设选择器（当 `showPresetSelector` 为 true 时显示），包含预设 pill 按钮组（PLA/ABS/PETG 等，高亮当前选中），连接 CalibrationViewModel 的 selectedFilamentPreset；已新增 CalibrationDialog 硬件校准选项（对齐上游 `CalibrationDialog::create_check_option`）：新增 4 个可勾选项（微型激光雷达校准/热床调平/振动补偿/电机降噪），匹配上游 xcam_cali/bed_leveling/vibration/motor_noise 四个 checkbox，每个选项含复选框+标签+辅助说明，Vibration 默认勾选（对齐上游"always shown"），Motor Noise 默认不勾选（对齐上游"shown if device supports"），硬件选项仅在校准未运行时显示；但仍缺上游 CalibrationWizardCaliPage（发送打印任务到打印机）、CalibrationWizardSavePage（保存校准结果到预设）的真实设备交互实现，以及 CaliHistoryDialog（历史记录）、ExtrusionCalibration（K/N 参数输入对话框）的完整迁移 2026-03-14
- [-] P6.3 Model Mall 已完成上游对齐重构：对照上游 ModelMallDialog（DPIFrame + wxWebView + 导航控件 back/forward/refresh）和 WebViewPanel（script message 处理、go_to_mall/go_to_publish URL 导航）实现完整原生 QML 商城浏览界面；ModelMallViewModel 重构为分类+搜索+排序过滤驱动的 Q_PROPERTY/Q_INVOKABLE 接口（12 个 mock 模型分 6 类，支持名称/作者/标签搜索、4 种排序模式对齐上游 Recommended/Popular/Newest/Free 标签、featured 标记对齐上游 SendDesignStaffpick），ModelEntry 包含 downloads/rating/thumbnailColor/thumbnailIcon/categoryIndex/downloadProgress/downloadCompleted 字段（共 18 字段），提供 filteredCount + 17 个 Q_INVOKABLE 访问器避免 Qt6 V4 VariantAssociationObject 崩溃；ModelMallPage.qml 使用 Theme 设计令牌重构为 4 区域布局（顶部导航栏+搜索+排序标签+发布按钮、分类药丸条、Flow 网格卡片、底部状态栏），全部区域绑定 controlPanelVisible 属性（对齐上游 show_control(bool)）；已新增导航历史系统（对齐上游 on_back/on_forward -> CanGoBack/CanGoForward）：ModelMallViewModel 新增 NavigationState 结构体、m_navigationHistory 列表、m_historyIndex 指针，每次 setCategoryIndex/setSearchQuery/setSortMode 变更时 pushNavigationState() 记录状态，goBack()/goForward() 恢复历史状态并重新过滤，canGoBack/canGoForward Q_PROPERTY 驱动导航按钮启用/禁用，navigationTitle Q_PROPERTY 显示面包屑路径（分类名+搜索词）；ModelMallPage.qml 顶栏新增 back/forward 按钮（对齐上游 mall_control_back/mall_control_forward），导航按钮按 canGoBack/canGoForward 显示高亮/禁用态，搜索栏 placeholder 扩展为"Search models, authors, tags..."；已新增发布模式（对齐上游 go_to_publish/request_close_publish_window）：ModelMallViewModel 新增 publishMode Q_PROPERTY，loadPublishUrl() 切换发布模式并更新 navigationTitle 为"Publish"，loadMallUrl() 切换回商城模式，顶栏新增 Publish 药丸按钮；已新增下载进度追踪（对齐上游 ModelMallDialog download 交互）：downloadModel() 改为 10 步 × 200ms 渐进式进度更新（10%→25%→40%→55%→70%→85%→95%→100%），新增 downloadProgress(index)/downloadCompleted(index)/cancelDownload(index) Q_INVOKABLE，下载完成后标记 downloadCompleted 并递增 downloads 计数，重复下载被阻止；模型卡片新增底部 accent 色进度条（宽度按 downloadProgress 百分比）、下载按钮显示百分比文本、"Downloaded"完成态徽章；模型详情弹窗下载按钮适配完成态显示；空状态区新增"Clear Filters"按钮（当有搜索词或非默认分类时显示）；分类"Home"重命名为"All"更准确反映筛选语义；搜索过滤扩展至包含 tags 匹配（对齐上游搜索范围）；但仍缺上游真实 wxWebView/QtWebEngine 集成、script message 双向通信、云账号登录同步、makerhub URL 导航和真实模型下载 2026-03-16
- [-] P6.4 MultiMachine 多机管理页面已从骨架增强为基础承接能力页面；对照上游 MultiMachinePage（3-tab Tabbook）、MultiMachineManagerPage（设备列表+排序+分页+View按钮+编辑打印机）、LocalTaskManagerPage（本地任务表格+选择+取消+Stop All）、CloudTaskManagerPage（云端任务表格+暂停/恢复+Stop All+分页）实现：左侧垂直Tab栏（设备/任务发送/任务已发送）、设备管理Tab含表格头（设备名/任务名/状态/操作）和设备行（在线指示灯+离线标记+进度条+View按钮+悬停删除）、本地任务Tab含选择复选框+项目名/打印机/状态/发送时间/进度条+Stop All+每行Cancel按钮、云端任务Tab含复选框+进度条+Pause/Resume/Stop按钮+Pause All/Resume All/Stop All+分页（10条/页）、编辑打印机按钮和排序功能（设备名/状态双向排序+方向箭头指示器+当前列高亮）、分页控件、空状态提示+添加按钮；MultiMachineViewModel 提供 5 台Mock设备（K1C/K1 Max/Ender-3 S1 Pro/K2 Plus/CR-10 Smart Pro，覆盖 Printing/Finish/Idle/Prepare/Offline 状态）、5 个本地任务（Sending/Pending/Success/Sending Cancel 多种状态覆盖）、12 个云端任务（Finish/Printing/Failed 分布，支持分页验证），Q_PROPERTY 驱动 QML；已新增设备选择发送对话框（对齐上游 MultiMachinePickPage / SendMultiMachinePage）：本地任务工具栏新增"Send to Device"按钮（仅在线设备存在时显示），点击弹出设备选择对话框（ComboBox 选任务 + 在线设备列表单选），MultiMachineViewModel 新增 `onlineMachineCount()`/`onlineMachineName(int)`/`sendTaskToDevice(taskIndex, machineIndex)` 方法，Mock 模式发送后模拟 10 步 x 200ms 渐进式发送进度（对齐上游 m_sending_percent 实时更新）；已新增设备搜索/过滤功能（对齐上游 `selected_machines()` 搜索）：MultiMachineViewModel 新增 `searchText`/`filteredMachineCount` Q_PROPERTY 和 `setSearchText()` 方法；已新增剩余时间显示（对齐上游 `get_left_time` dd:hh:mm 格式）；已新增完整排序策略（对齐上游 SortItem）；已新增云任务复选框选择+批量操作（对齐上游 CloudTaskManagerPage m_select_checkbox/btn_pause_all/btn_continue_all/btn_stop_all）：`selectCloudTask(int)` 仅允许选择 printing 任务，`pauseAllCloudTasks()`/`resumeAllCloudTasks()`/`stopAllCloudTasks()` 批量操作，`stopCloudTask(int)` 单任务停止，`pauseCloudTask(int)` 暂停，`resumeCloudTask(int)` 恢复；已新增云任务分页（对齐上游 CloudTaskManagerPage m_flipping_panel）；已新增本地任务表格头全选复选框（对齐上游 m_select_checkbox，仅选中 status<=1 可选任务，不可选 checkbox 半透明禁用）、每行 Cancel 按钮（对齐上游 m_button_cancel，仅 pending/sending 可见）、Info 列发送进度条（对齐上游 TASK_LEFT_PRO_INFO m_sending_percent）；已新增云端任务 Info 列（进度条+剩余时间对齐上游 cloud task info doRender）、Pause/Resume 互斥切换按钮、hover accent 边框（对齐上游 doRender m_hover rounded rectangle）；空状态文本对齐上游原文 "There are no tasks to be sent!"；发送设备对话框改用 Theme 设计令牌；但仍未完成上游真实 DeviceManager 连接、MQTT 订阅（subscribe_device_list）、TaskManager 真实任务管理 2026-03-16

### P7 — 质量、稳定性与发布

- [-] P7.1 程序已可构建、可启动、可进行基础导入与切片验证；新增 DeviceServiceMock/CameraServiceMock/CloudServiceMock 三个新 Service、MonitorViewModel/HomeViewModel 扩展、MonitorPage 视频 Tab + 打印控制栏、HomePage 云账号/设备绑定 UI、MonitorPage HMS 健康管理面板（5 台设备 Mock HMS 数据、3 种告警级别、未读标记、全部已读）、MultiMachinePage 搜索过滤+剩余时间列、CalibrationPage 步骤状态追踪（StepCtrl pending/active/completed）、ModelMallPage 刷新按钮加载动画（旋转箭头 RotationAnimation）+ 下载按钮状态指示器（下载中旋转图标+禁用交互）、星级评分 parent.index 引用错误修复（改为 delegate index 属性）、ModelMallViewModel 新增 `isDownloading()` 和异步下载模拟、smoke test 扩展至 59/59 检查项，全部通过构建和回归验证；已新增 P3 Preview 图例组件渐变模式支持（Legend.qml 重构为三模式渲染：渐变图例 10 色 Range_Colors 渐变条 + min/max 标签，离散/挤出机图例色块+标签+计数），PreviewViewModel 新增 legendType Q_PROPERTY；已新增 LayerSlider 非选中范围淡化显示；但仍缺上游完整功能验证（真实切片/G-code预览/设备连接/校准/视频/云服务/WebView/多机管理的端到端对照） 2026-03-14（真实切片/G-code预览/设备连接/校准/视频/云服务/WebView/多机管理的端到端对照） 2026-03-14；2026-09-02 P7.1 设备无关部分收口：①校准真实工作流已有覆盖——int02_CalibrationGeneratesCalibGcode 走真实 libslic3r 切片并校验 PA 校准 G-code 标志（SET_PRESSURE_ADVANCE/M572），calibrationImplementedModes* / calibrationUnsupportedModes* / calibrationFallbackAndSliceCallbacks* 锁定路由/切片请求/不可用语义/进度回调；②新增项目生命周期全周期 E2E projectLifecycleFullCyclePreservesStateAndReslices（导入→移动→切片→saveProjectAs→clearProject→loadFile 恢复→重切片）；③3MF 位移"偏差"根因结案（非缺陷）：探针解剖显示 3MF build transform 精确保存 (113,110,-2)，读回值 (111.499,65.5,-0.4995) = 保存 offset + volume matrix 平移（-1.5005/-44.5/1.5005，见 Slic3r_PE_model.config 的 volume matrix）——读取端把 volume 变换折叠进 instance offset，三轴世界坐标逐项吻合，几何放置完全精确；此前"半程值"是断言误比内部表示（instance offset 跨保存/加载本就不表示稳定）。测试已改为世界空间 bbox 中心几何等价断言（容差 0.5mm），并验证重切片成功。设备相关端到端（设备连接/视频/云服务/多机管理）维持外部阻塞。canonical 全绿（exit 0）：E2E 31/31、ViewModel 185/185、QML audit 174/174
- [-] P7.2 已建立自动化 smoke test 框架（scripts/smoke_test.ps1）：6 阶段验证（二进制检查、Qt DLL 依赖、应用启动存活、QML 运行时警告检测、编译产物检查、QML 资源完整性），当前 43/43 通过、0 QML 警告；已扩展编译产物检查至 19 个关键 obj 文件（新增 SettingsViewModel/HomeViewModel/ModelMallViewModel/SliceService/ConfigOptionModel/CameraController），QML 资源完整性检查扩展至 30 个文件（新增 NotificationCenter/ToolPositionTooltip/CollapsibleSection/PreferencesPage/CxButton/CxComboBox/CxSlider/CxCheckBox/CxIconButton）；当前 61/61 通过、0 QML 警告；P5.1-P6.4 全部新增功能通过回归验证；已新增 GLViewport.cpp.obj 编译产物检查（新增 GizmoSeamPaint/GizmoHollow 两种模式）；但仍缺”与上游行为对照”的回归基线和自动化页面导航测试 2026-03-15；2026-09-02 P7.2 收口：两项均已落地——①上游行为对照回归基线：新建 tests/data/upstream_parity_baseline.json（24 个锚点，覆盖切片时间/重量格式、TriangleSelector 画笔/seed fill/裁剪面/高度范围/边检测/缝隙面积、支撑预览、绘制 kind 隔离与注解写回、网格布尔、面检测、耗材映射、Process/Printer/Filament 三层 Tab 清单、arrange、cut_mesh、PartPlate 映射模式、通知、RHI 切割面、实例包围球半径），每锚点含上游引用/Qt 对应文件+符号/锁定测试名；新增 upstreamParityBaselineEntriesResolveInSource 测试逐一验证 Qt 符号存在且锁定测试在指定测试文件中，基线不再静默腐化。②自动化页面导航测试：新增 pageNavigationBaselineMatchesUpstreamSurface——校验 BackendContext TabPosition 枚举 9 槽与 main.qml StackLayout 槽标记按上游顺序对齐、Prepare+Preview 共享单实例 Plater（ARCH-05，上游 Plater.cpp 单 wxPanel 契约）、两个占位槽保持 visible:false、9 个路由页面文件存在且注册 qml.qrc、导航入口为 backend.requestSelectTab、tpDevice 保留 upstream: tpMonitor 溯源注释。canonical 全绿（exit 0）：E2E 30/30、ViewModel 185/185、QML audit 174/174
- [-] P7.3 已审查全部关键页面 QML 绑定：修复 SettingsPage 分类计数字体色 parent 链（4 级 → 3 级）导致的运行时 undefined 警告、修复 main.qml compareReferenceSource 缩进异常；修复 verify 脚本 cmake 兼容性（添加 -DQT_FORCE_MIN_CMAKE_VERSION_FOR_USING_QT=3.21 支持 CMake 3.21 + Qt 6.10 组合）；审查覆盖 PreparePage/PreviewPage/MonitorPage/SettingsPage/PrintSettings/ObjectList/Sidebar/SliceProgress/LayerSlider/MoveSlider/Legend/StatsPanel/StatusBar/ErrorBanner/ErrorToast/PrintDialog 全部 QML 文件，确认 required property + null guard 模式完整、无绑定循环、无类型失配；**根本修复 Theme 单例注册**（添加 qmldir 声明，QML 警告从 994 行降至 0）；修复 main_qml.cpp qputenv 覆盖、Shortcut sequences、缺失 SVG 图标、layout anchors 警告；修复 C++ 侧 10+ 处 service 空指针保护（EditorViewModel/PreviewViewModel）、PreparePage undo/redo 双触发、GLViewportRenderer phantom transform 和 GL context null check、CameraController 零向量/零参数保护、PreviewViewModel 工具颜色 hex 格式 bug；修复 Qt Quick Controls 2 MenuItem 不支持 shortcut 属性导致的 QML 启动崩溃（移除 11 个 MenuItem shortcut 绑定，改用独立 Shortcut 元素）；修复 PreviewViewModel roleTimeCount 等 4 个 Q_INVOKABLE 从 private 区域移至 public（Qt6 QML 无法调用 private invokable）；修复 GCodeRenderer 编译错误（NOMINMAX 宏 + 缺失 cfloat/cmath 头文件）；已修复 ModelMallPage.qml 星级评分 parent.index 引用错误（Row 无 index 属性导致所有星星始终显示空星），修正为 delegate 自身 index 属性；已通过运行时验证和自动化 smoke test 确认 0 QML 运行时警告、43/43 测试通过；已修复 PrintSettings.qml `compMA3` MouseArea 在 RowLayout 内使用 anchors 的警告（改用 Layout.fillWidth/Layout.preferredHeight）、PrintSettings.qml layer range Repeater delegate `required property int index` 与整数模型不兼容（改用 `property int delegateIndex: index`）、PrintSettings.qml `layerRangeCount` 未带括号导致 "Unable to assign a function" 警告（全部改为 `layerRangeCount()`）、LayerSlider.qml `minThumbMA` 未定义引用（MouseArea 添加 id 和 hoverEnabled）；smoke test 更新至 44/44 通过、0 QML 警告 2026-03-14；已修复 PrintSettings.qml 值来源层级弹窗 `chainData` 引用错误（5 处 `chainData.xxx` 改为 `valueChainPopup.chainData.xxx`，消除 ReferenceError: chainData is not defined）、Popup 绑定循环（height 改为固定 220px 消除 Binding loop detected for property "height"）、PreparePage.qml `Theme.textMuted` 未定义（改为 `Theme.textDisabled`）；smoke test 更新至 59/59 通过、0 QML 警告、0 运行时错误 2026-03-15；已修复 DeviceServiceMock.cpp Qt `slots` 宏冲突（变量名 `slots`/`slot` 与 Qt `Q_SLOTS` 宏冲突导致 MSVC 编译错误，重命名为 `amsSlots`/`amsSlot`）
- [-] P7.4 已创建并持续更新发布就绪文档 `docs/发布就绪度.md`：构建状态检查表（CMake/构建/59/59 烟雾测试/启动存活/DLL 依赖全部 PASS）、Mock 模式已知限制清单（10 项：模型加载/切片/G-code预览/设备连接/校准/视频/云服务/WebView/多机管理/HMS）、P0-P3 缺陷检查清单（5 个优先级共 14 项，已完成 6 项）、功能完成度矩阵（9 个模块对齐百分比，整体 70%+）、回滚策略（git revert + 烟雾测试验证）；但仍缺实际功能冻结、全页面回归测试自动化、和 i18n 完整验证 2026-03-14；2026-09-02 P7.4 刷新：docs/发布就绪度.md 全量重写为当前真实状态——构建状态更新为 canonical 唯一命令 + 12 套件 569 测试 0 失败清单、修正"Mock 模式"过时描述（libslic3r 已接入，真实切片/绘制/切割/排列/耗材映射可用）、已知限制改为外部阻塞清单（OpenVDB/MQTT/FFmpeg/bambu_networking/QtWebEngine/cr_tpms）、功能完成度矩阵刷新（Prepare 92%/Settings 96%）、回滚策略补 KNOWN-01 purge 流程；"全页面回归测试自动化"部分落地（P7.2 导航基线 + 上游对照基线），运行时 UI 自动化仍缺；i18n zh_CN 已 100%（en/ja/ko/de/fr 词汇表基线）

### P8 — 缺失对话框与向导

- [x] P8.1 ConfigWizard 首次配置向导（对齐上游 ConfigWizard.cpp，含打印机/耗材/网络初始设置）— 4-page SwipeView wizard implemented
- [x] P8.2 BedShapeDialog 热床形状设置（对齐上游 BedShapeDialog，支持自定义热床形状）— 3 shape types + Canvas preview
- [x] P8.3 EditGCodeDialog G-code 编辑器（对齐上游 EditGCodeDialog）— 3-column layout with parameter list
- [x] P8.4 AMS 设置面板（对齐上游 AMSMaterialsSetting/AMSSetting/AmsMappingPopup，AMS 耗材管理）— 4-slot filament management, mapping config, remaining progress
- [x] P8.5 固件对话框（对齐上游 FirmwareDialog）— FirmwareDialog with version check, OTA mock, progress bar
- [x] P8.6 其他缺失对话框（AccelerationAndSpeedLimitDialog/WipeTowerDialog/SelectMachineDialog/AICloudServiceDialog 等）— SpeedLimitDialog, WipeTowerDialog, PrintHostDialog

### P9 — I18N 国际化

- [x] P9.1 建立 Qt6 i18n 基础设施（对齐上游 I18N.cpp/hpp，使用 Qt Linguist 体系：.ts/.qm 文件）— qt_add_translations + 6 languages zh_CN/en/ja/ko/de/fr
- [x] P9.2 提取当前所有 qsTr() 字符串到 .ts 翻译模板 — 491 strings extracted via lupdate
- [x] P9.3 对齐上游中文/英文/日文等主要语言包 — .qm files compiled, 6 languages bundled as resources

### P10 — 插件与企业模式

- [x] P10.1 插件系统框架（对齐上游插件加载机制）— PluginManagerDialog with download/install/enable mock
- [x] P10.2 企业/专业模式切换（对齐上游 EnableLiteModeDialog）— EnableLiteModeDialog with lite preview mode toggle

---

## 4. 模块对照矩阵（第一版）

| 上游模块         | 上游源码位置                                                                              | Qt6 承接位置                                                                                                                                    | 当前状态                                                       |
| ---------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 主窗口与全局导航 | `third_party/CrealityPrint/src/slic3r/GUI/MainFrame.*`, `GUI_App.*`                       | `src/qml_gui/main.qml`, `src/qml_gui/BackendContext.*`                                                                                          | `[-]` 主壳已运行，未全量对照                                   |
| Prepare 主工作区 | `third_party/CrealityPrint/src/slic3r/GUI/Plater.*`                                       | `src/qml_gui/pages/PreparePage.qml`, `src/qml_gui/panels/*`, `src/core/viewmodels/EditorViewModel.*`                                            | `[-]` 基础闭环已通，功能未齐                                   |
| 3D 编辑视口      | `third_party/CrealityPrint/src/slic3r/GUI/GLCanvas3D.*`, `Gizmos/*`                       | `src/qml_gui/Renderer/*`, `src/core/rendering/*`                                                                                                | `[-]` 桥接已建，复杂交互未齐                                   |
| G-code 预览      | `third_party/CrealityPrint/src/slic3r/GUI/GCodeViewer.*`, `GUI_Preview.*`                 | `src/qml_gui/pages/PreviewPage.qml`, `src/qml_gui/components/*`, `src/core/viewmodels/PreviewViewModel.*`, `src/core/rendering/GCodeRenderer.*` | `[-]` 主链路已通，待对照                                       |
| 设置与预设       | `third_party/CrealityPrint/src/slic3r/GUI/Tab.*`, `PresetComboBoxes.*`                    | `src/qml_gui/pages/SettingsPage.qml`, `src/qml_gui/panels/PrintSettings.qml`, `src/qml_gui/Models/*`, `src/core/viewmodels/ConfigViewModel.*`   | `[-]` 骨架+作用域+预设保存/脏检测已实现，缺继承链              |
| 后台切片         | `third_party/CrealityPrint/src/slic3r/GUI/BackgroundSlicingProcess.*`                     | `src/core/services/SliceService.*`                                                                                                              | `[-]` 主流程已通，待状态机对照                                 |
| 项目与文件导入   | `third_party/CrealityPrint/src/slic3r/GUI` + `libslic3r/Format/*`                         | `src/core/services/ProjectService.*`, `src/core/viewmodels/EditorViewModel.*`                                                                   | `[-]` 导入可用，项目工作流未齐                                 |
| 设备与监控       | `third_party/CrealityPrint/src/slic3r/GUI/Monitor.*`, `DeviceManager.*`, `print_manage/*` | `src/qml_gui/pages/MonitorPage.qml`, `src/core/viewmodels/MonitorViewModel.*`, `src/core/services/DeviceService*`, `CameraService*`             | `[-]` Mock 设备列表/打印控制/摄像头/视频Tab 已实现，缺真实连接 |
| 校准             | `third_party/CrealityPrint/src/slic3r/GUI/CalibrationPanel.*`                             | `src/qml_gui/pages/CalibrationPage.qml`, `src/core/viewmodels/CalibrationViewModel.*`                                                           | `[-]` 上游对齐重构完成，设备交互未承接                         |
| 商城与 WebView   | `third_party/CrealityPrint/src/slic3r/GUI/ModelMall.*`, `WebViewDialog.*`                 | `src/qml_gui/pages/ModelMallPage.qml`, `src/core/viewmodels/ModelMallViewModel.*`                                                               | `[-]` Mock 商城 UI 已实现，缺 WebView/登录                     |
| 多机管理         | `third_party/CrealityPrint/src/slic3r/GUI/MultiMachineManagerPage.*`, `MultiTaskManagerPage.*`, `MultiMachinePage.*` | `src/qml_gui/pages/MultiMachinePage.qml`, `src/core/viewmodels/MultiMachineViewModel.*`                                                                                | `[-]` 3-tab布局+设备/任务表格+全选/每行Cancel/Pause/Resume/Stop+发送进度+批量操作+分页+搜索+排序+发送对话框已实现，缺真实连接     |
| libslic3r 集成   | `third_party/CrealityPrint/src/libslic3r/*`                                             | `src/core/services/ProjectService.*`, `src/core/viewmodels/EditorViewModel.*`, `src/core/rendering/*`                                  | `[-]` loadFile/meshData/切片/对象变换/CRUD/Volume 已接入真实 API，Plate 仍为 Mock（P0.5） |
| Undo/Redo 系统   | `third_party/CrealityPrint/src/slic3r/GUI/UndoRedo.*`                                    | `src/core/viewmodels/EditorViewModel.*`, `src/core/undo/*`                                                                              | `[x]` UndoRedoManager + 9 个 QUndoCommand 子类已实现并验证（P1.4） |
| 后台任务系统     | `third_party/CrealityPrint/src/slic3r/GUI/Jobs/*`                                      | `src/core/jobs/*`                                                                                                                     | `[x]` JobBase/JobManager 框架已实现并验证（P1.5）           |
| 对话框与向导     | `third_party/CrealityPrint/src/slic3r/GUI/ConfigWizard.*`, `BedShapeDialog.*`, `EditGCodeDialog.*` 等 30+ | 部分 Mock 对话框                                                                                                                       | `[-]` P8.1-P8.6 均已有 Mock 实现，其余对话框未覆盖（P8）         |
| I18N 国际化      | `third_party/CrealityPrint/src/slic3r/GUI/I18N.*`                                      | -                                                                                                                                     | `[-]` qt_add_translations 基础设施 + 6 语言包（P9）              |
| 插件与企业模式   | `third_party/CrealityPrint/src/slic3r/GUI/Plugins/*`, `EnableLiteModeDialog.*`         | -                                                                                                                                     | `[-]` 插件管理 + 企业模式均有 Mock 实现（P10）                    |

---

## 5. 当前优先级

1. **打通 libslic3r 真实调用链路（P0.5）**：对象变换桥接、CRUD、Volume/Plate 管理真实 API 对接——这是所有功能迁移的根基性前提。
2. 收敛 Prepare：对象工作区、右侧工作区、Gizmo 与交互链路。
3. 收敛 Preview：清 warning、补对照验收。
4. 切换 Settings / Preset 到上游语义承接。
5. 持续补齐”上游模块 -> Qt6 模块 -> 验收记录”的逐项对照闭环。
6. 真实库集成：libslic3r 切片引擎、bambu_networking 设备连接、FFmpeg 视频解码。

---

## 6. 里程碑口径

| 里程碑                      | 验收标准                                        | 当前状态 |
| --------------------------- | ----------------------------------------------- | -------- |
| M0 真值冻结                 | 上游 tag/commit + 功能矩阵冻结                  | 已完成   |
| M0.5 libslic3r 桥接可用     | 对象变换/CRUD/Volume/Plate 真实 API 对接完成    | 未开始   |
| M1 Prepare 可替代日常使用   | 导入、对象管理、Gizmo、切片入口、平板工作流可用 | 进行中   |
| M2 Preview 可稳定回归       | 切片到预览端到端，主要视图模式对照通过          | 进行中   |
| M3 Settings/Preset 完整承接 | 预设与多作用域参数体系对齐上游                  | 未开始   |
| M4 Device/Monitor 承接      | 设备连接、状态、控制、视频闭环                  | 未开始   |
| M5 全功能 RC                | 所有顶栏页面可用，关键回归通过                  | 未开始   |

---

## 7. 备注

- 旧任务文档中“合计 70/70 全部完成”的表述仅能代表“阶段性骨架任务已经做过”，不能再代表当前项目目标的真实完成度。
- 从 2026-03-10 起，所有新增任务和完成记录都必须挂到“上游功能真值对照”之下。

---

## 8. P11 — 上游 OrcaSlicer 同步（2026-07-19 起）

> **背景**：2026-07-19 调研发现，子模块 `third_party/OrcaSlicer`（OWzx fork：`wzx011011/OrcaSlicer`）相对官方上游 `SoftFever/OrcaSlicer` 落后 354 个 commit（合并基点 `f13f67959` 2026-06-29 → 上游 `bb3e18c211` 2026-07-19）。其中真正与 Qt6 迁移相关的是 `src/libslic3r`（直接编译，可合入）和 `src/slic3r`（wx GUI，行为真值，Qt 侧需对齐）。
> **命名说明**：本文档历史口径写「CrealityPrint」，实际子模块现已切到 OrcaSlicer fork（同一上游源码不同 fork），下文沿用 OrcaSlicer。
> **任务性质**：本节任务不是新功能迁移，而是「上游行为对齐」——把上游 6-29 之后的 bug 修复 / 行为变更同步到 OWzx 代码或 Qt6 承接层。

### P11.A — libslic3r cherry-pick（引擎层合入，子模块内完成）

子模块分支 `owzx-upstream-sync-2026-07`，从 `b3cec7363e`（CGAL 5.4 兼容修复）开始 cherry-pick。

- [x] P11.A.1 `dec67345be` — 修复 Arachne beading 插值越界崩溃（#14656）→ commit `61ebabdfe7`
- [x] P11.A.2 `6fda82476d` — 修复 ToolOrdering 最大层高 OOB read（#14665）→ commit `db2726d1d3`
- [x] P11.A.3 `2860353b9f` — 修复共享 temp dir 多用户切片崩溃（#14607）→ commit `8a911b9bf3`
- [x] P11.A.4 `cab62070b5` — 修复 ZIP 导入中文文件名乱码（#14633）→ commit `616107c818`
- [x] P11.A.5 `036bd7bcec` — 新增 `{first_object_name}` 文件名占位符（#14497）→ commit `9fb636f64e`
- [x] P11.A.6 `464a81d585` — gcode 模板访问 `coFloatsOrPercents`（#14526）→ commit `d99a96e796`
- [x] P11.A.7 `4188ed2e00` — 新增 `regex_replace()` 字符串变换占位符（#14650）→ commit `8a9e6267da`
- [x] P11.A.8 编译验证 `scripts/auto_verify_with_vcvars.ps1` — 子模块指针推进后跑一次完整构建（已完成 2026-07-19，commit `bcbff93`）
- [x] P11.A.9 推进主仓库 gitlink 到子模块新分支 HEAD（已完成 2026-07-19，commit `bcbff93`；后续 bb3 整体同步 commit `d8501f8`）
- [-] P11.A.10 推送当时的子模块分支到 `github-fork`。**Historical / superseded**：该 bb3 分支任务已由 P12.2 的当前基线可复现性门禁取代。

**v5.4 决策（2026-07-20）**：P11.A.11-A.20 全部**已被 bb3 整体同步吸收**（子模块 commit `edbca0aa55`，整体 `git checkout bb3e18c211 -- src/`）。这 10 个上游修复全是 bb3 的祖先，整体 checkout 后自动以最忠实的上游版本包含进来，不再需要独立 cherry-pick。本节 P11.A.11-A.20 标记为 `[x]`（absorbed by bb3，不单独追踪）：
- [x] P11.A.11 `7d17400443` — absorbed by bb3
- [x] P11.A.12 `03568b5200` — absorbed by bb3
- [x] P11.A.13 `1717c0f263` — absorbed by bb3
- [x] P11.A.14 `3c0338f462` — absorbed by bb3
- [x] P11.A.15 `3ae7bd04fa` — absorbed by bb3
- [x] P11.A.16 `dd99549d20` — absorbed by bb3
- [x] P11.A.17 `8747605930` — absorbed by bb3
- [x] P11.A.18 `b2a7197a07` — absorbed by bb3
- [x] P11.A.19 `b58025575d` — absorbed by bb3
- [x] P11.A.20 `d6ca71be95` + `db2f861a11` — absorbed by bb3

**暂不合入（依赖链 / 大改 / 设备专属）**：
- H2C/A2L/X2D 多喷头系列（~20 个 commit，`237ef41b06` 及后续）——除非 OWzx 决定支持多喷头设备
- Anisotropic surfaces / Separated Infills / Top Surface Expansion 组（`bc6ffcfb31`/`20a66fa99b`/`c4ea86041b`）——大重构，建议整组评估后单独迁移
- Python 插件框架系列 —— OWzx 暂无插件系统

### P11.B — src/slic3r GUI 行为对齐（Qt6 侧迁移任务）

上游 wx GUI 改动不能直接 merge，要在 Qt6 承接层复刻行为。按「是否已迁移过该功能区域」分级：

**B1 已迁移区域 — 必须回补的 bug 修复（P0，用户可见崩溃/错误）**：

> **v5.4 调研结论（2026-07-20）**：10 项中 **9 项 NOT APPLICABLE**（Qt6 架构免疫），**1 项已修复**（A7）。详见 v5.4 Phase 180/181/182 的 SUMMARY.md 与 RESEARCH.md。这些上游修复全部针对 OrcaSlicer 的 wxWidgets/ImGui/OpenGL GUI 层（析构顺序陷阱、wxGrid/ImGui widget bug、远程/云功能、GLVolume 合成 id 机制），Qt6 架构（无 Plater/pImpl、QML 替代 wxGrid/ImGui、RHI 替代 OpenGL、真实 source index 替代合成 id）结构性免疫这些 bug 类。**bb3 同步引入零 Qt6 侧崩溃回归。**

- [x] P11.B1.1 `5a53d2eb88` — **NOT APPLICABLE**（Qt6 RhiViewport 无 pImpl，QPointer 已守卫跨线程回调，析构期无 emit）
- [x] P11.B1.2 `d24e7f75ef` — **NOT APPLICABLE**（Qt6 wipe tower 是独立 Q_PROPERTY，不在 selection set，不可 pick，无合成 id）
- [x] P11.B1.3 `adc8763099` — **NOT APPLICABLE**（Qt6 MeasureEngine 是自研重写，无 SPHERE_2 gripper）
- [x] P11.B1.4 `4b7182b048` — **NOT APPLICABLE**（Qt6 PartPlate 是纯值对象，无 m_model，同步重建无 stale id 窗口）
- [x] P11.B1.5 `59e655dd1d` — **NOT APPLICABLE**（Qt6 无 wxGrid，ObjectList.qml 是 QML ListView）
- [x] P11.B1.6 `49fe64cb07` — **NOT APPLICABLE**（Qt6 FilamentSlot.qml 用 QML layout，无 wxWidgets 几何计算）
- [x] P11.B1.7 `5ba5c6672d` — **已修复**（Phase 182 / commit `b36e055`）：ProjectServiceMock::reloadFromDisk 加 project-relative 路径回退
- [x] P11.B1.8 `0541918592` + `8e868118d2` — **NOT APPLICABLE**（Qt6 无共享 bundle 浏览器，JSON schema 不同）
- [x] P11.B1.9 `b2adfb5c13` — **NOT APPLICABLE**（Qt6 无 SharedProfiles 通知类型，QML 原生换行）
- [x] P11.B1.10 `33909a51cd` — **NOT APPLICABLE**（Qt6 用 RHI + 自研 shader，无 stencil/gouraud/phong/ssao）

**B2 已迁移区域 — 行为小修（P1）**：
- [x] P11.B2.1（2026-09-02）：Qt 单位显示 ℃ 统一完成——`°C`(U+00B0+U+0043) → `℃`(U+2103) 共 11 处（ConfigOptionModel.cpp 5、SelectionSettingsDialog.qml 2、AMSSettingsDialog.qml 1、ConfigWizardDialog.qml 3），对齐上游 979e56f1d0（Tab.cpp/GCodeViewer.cpp 同款替换）；mm³ 体积单位已为 Unicode 上标无需改动
- [x] P11.B2.2（2026-09-02）：英制单位 `in` 消歧完成——`SettingsViewModel::lengthUnitLabel()` 改用 `QCoreApplication::translate("SettingsViewModel", "in", "inches unit")`（Qt 版 `_CTX_utf8("in","inches")`），`Legend.qml` 英制 min/max 端点后缀同改 `qsTr("in", "inches unit")`，对齐上游 bec1ce706c（Cut/Measure/SVG/ObjectManipulation 的 _CTX 标注）；mm 侧同步标注 "millimeters unit"
- [ ] P11.B2.3 `1d61962ea7` — Support Painting 增加 Vertical/Horizontal 轴锁复选框 → `src/core/rendering/PaintEngine.{cpp,h}` + Support gizmo UI

**B3 未迁移区域 — 未来迁移时按上游最新行为做（仅记录，不立即推进）**：
- [-] P11.B3.1 `93d5b65852` — per-obj 多变体选项处理（Qt 多挤出头支持未完成）
- [-] P11.B3.2 `4eb9a85a49` — 切换非 BBL 打印机预设后耗材同步指向错误机器（Qt 无 SelectMachineDialog 网络同步）
- [-] P11.B3.3 `64a321528d` — 3mf 动作对话框取消后无法再次打开（Qt 3mf 导入流程待完善）
- [-] P11.B3.4 `72005959d4` — 加载 3mf 后 extruder 2 标签用错 nozzle variant id（Qt 多挤出头 tab 未完成）
- [-] P11.B3.5 `cab62070b5` GUI 部分 — ZIP 导入中文文件名（Qt 归档导入对话框待确认）— 注：libslic3r 部分已通过 P11.A.4 合入
- [-] P11.B3.6 `b58025575d` GUI 部分 — Linux 校准 temp 路径按用户隔离（Qt 校准流程未完整）— 注：libslic3r 部分已通过 P11.A.19 待合入
- [-] P11.B3.7 AMS 烘干系列（AMSDryControl 对话框、humidity 入口、烘干中打印告警）—— Qt 侧无对应
- [-] P11.B3.8 H2C/A2L 多喷头设备层（MultiNozzleSync、NozzleRack、FilamentMapDialog volume types、Hybrid flow、PurgeModeDialog）—— Qt 侧无对应
- [-] P11.B3.9 新增配置选项（First layer fan speed / Retract amount after wipe / Toolchange ordering / fill order / Top Surface Expansion / Top-Bottom layer directions / Small perimeters speed for supports / polyhole edges 等）—— 每项需要在 Qt ParameterPage 注册新 ConfigOption

**B4 可忽略（C 级，不推进）**：
- Python 插件系统全部内部实现（~95 个 commit，`src/slic3r/plugin/*`、PluginLoader/Manager 等）—— Qt 侧无对应物
- 平台专属（macOS OpenGL FBO、XWayland/Intel overlay、clang UB 修复等）—— Qt6 用 RHI 不同后端，不适用
- 上游 merge / cleanup / 重构 commit

### P11.C — snapshot 偏离基线管理（决策记录）

子模块里 `6fe300c0a9 "OWzx OrcaSlicer source snapshot for CI"` 接在官方 `f13f67959` 之上，但 tree 里包含 56 个 libslic3r 文件 + 134 个 slic3r 文件的 OWzx 本地修改（Config enum null-guard、`ConfigOptionFloatsNullable → ConfigOptionFloat` 回退、删除多喷头辅助函数等）。这些偏离是后续 cherry-pick 的冲突源。

- [x] P11.C.1 完成 OWzx 本地修改清单调研（详见 `docs/上游同步_OWzx本地修改清单_2026-07.md`，由调研 agent 生成）
- [x] P11.C.2 审查清单中每项「可能可放弃」的回退，决定是否在合入上游时放弃本地版本 — **决策：全部放弃，接受上游版本**（2026-07-19）
- [x] P11.C.3 对「必须保留」的核心 OWzx 改动，建立回归测试或文档化保护，避免后续合入时被覆盖 — **已落地**（2026-07-19）
- [x] P11.C.4 执行 B/C/D 组回退整体放弃 — 子模块 `edbca0aa55` + 主仓库 `d8501f8`（2026-07-19）

#### P11.C 决策与执行记录（2026-07-19）

**用户决策**：放弃 B/C/D 组所有回退，把 libslic3r 拉到与上游 `bb3e18c211` 一致，只保留 OWzx 自己的本地修复。

**执行结果**：
- 子模块 commit `edbca0aa55`：`git checkout bb3e18c211 -- src/`（含 libslic3r 和 slic3r 整个 src/ 树，保持子模块内部一致）+ 重应用 2 个 OWzx 本地修复。
- 主仓库 commit `d8501f8`：推进 gitlink + CMake 改动。
- 编译验证：`scripts/auto_verify_with_vcvars.ps1` 全部目标链接通过，0 error。OWzxSlicer.exe 43.6 MB（旧版 42.3 MB），所有测试 exe 也重新链接。

**保留的 2 个 OWzx 本地修复**（唯一有意偏离上游的地方）：
1. `src/libslic3r/Config.hpp` — 4 处 `keys_map == nullptr` null-guard（在 `ConfigOptionEnumGeneric::{serialize,deserialize}` 和 `ConfigOptionEnumsGenericTempl::{deserialize,serialize_single_value}`）。承载 `append_full_config` G-code 注释场景崩溃修复；上游不处理 `keys_map == nullptr` 情况。
2. `src/libslic3r/MeshBoolean.cpp` — CGAL 5.4 兼容（用 4 参数版 `triangulate_and_refine_hole`；`default_values` 是 CGAL 5.6+ 才有）。

**新增的 CMake 改动**（主仓库）：
- `cmake/BuildLibslic3rFromSource.cmake`：给 `libslic3r_from_source` 和 `libslic3r_cgal_from_source` 两个 target 加 `/utf-8` 编译选项。原因：上游源码（如 `PrintConfig.cpp` 用 `L(u8"℃")` 含非 ASCII 字符）是 UTF-8 无 BOM；不加 `/utf-8` 时 MSVC 在中文 codepage(936) 下误判多字节序列，报 `C2001 newline in constant`。

**7 个之前的 cherry-pick 已被吸收**：它们全是 `bb3e18c211` 的祖先，整体 checkout 后自动以最忠实的上游版本包含进来，不再独立存在。对应 P11.A.1-A.7 的子模块 commit (`61ebabdfe7` 等) 已从主线移除（被 `edbca0aa55` 取代）。

**bb3 带入的新增 libslic3r 源文件**（CMake GLOB 自动 pick up，无需改 CMakeLists）：
- `MultiNozzleUtils.cpp/.hpp` — H2C/A2L 多喷头支持
- `FilamentGroup.cpp/.hpp`、`FilamentGroupUtils.cpp/.hpp` — 耗材分组
- `GCode/ElegooGCodeProcessorHelper.cpp` — Elegoo G-code 处理

**bb3 删除的 libslic3r 源文件**：
- `GCode/PostProcessor.cpp/.hpp`（上游移到了 GUI 层，OWzx 不编译 GUI，无影响）

**已知风险**（运行时，未验证 → **2026-07-20 v5.4 已验证为真实回归并修复**）：
- ~~per-extruder 配置行为已启用（上游 nullable 类型），如果 OWzx Qt6 ViewModel/UI 未跟进，可能出现 per-extruder 字段值显示异常。~~ → **已修复**（v5.4 Phase 183，commit `6512c7e`）：`ConfigOptionModel::extractDefault` 新增 6 个 vector 类型 case（coFloats/coInts/coBools/coFloatsOrPercents/coPercents/coStrings），取 `values[0]` 作为单值默认。这修复了 bb3 同步引入的真实回归：speed/accel/jerk/temp 字段从单值 `coFloat` 变回 `coFloats` 后，Qt6 UI 显示空值的问题。
- H2C/A2L 多喷头代码进入编译范围，增加 exe 体积（OWzxSlicer.exe +1.3 MB）。这些代码默认不启用，但占编译时间和维护面。**（接受，不改变）**

**后续任务**（不在本次范围）：
- ~~跑切片端到端测试（导入模型 → 切片 → G-code 输出）验证 bb3 引擎行为正常~~ → **已验证**（2026-07-19，4 项切片测试全过：默认/重复/配置覆盖/多对象）
- ~~跑多挤出头配置场景验证 per-extruder 行为~~ → **已验证**（2026-07-20，Phase 183 修复后切片仍正常）
- ~~决定是否给 OWzx Qt6 UI 跟进 per-extruder 配置（若需要）~~ → **决策：延后**（OWzx 当前产品定位单喷头，per-extruder UI editor 留到多喷头设备支持里程碑）

---

### P11 收尾（v5.4，2026-07-20）

**P11.A 状态**：7 个 cherry-pick 已完成 + 10 个候选已**全部被 bb3 整体同步吸收**（子模块 `edbca0aa55`）。本节不再独立追踪。

**P11.B 状态**：10 项中 **9 项 NOT APPLICABLE**（Qt6 架构免疫：无 Plater/pImpl、无 wxGrid、QML 替代 ImGui、RHI 替代 OpenGL、真实 source index 替代合成 id），**1 项已修复**（A7 STEP reload，Phase 182）。**结论：bb3 同步引入零 Qt6 侧崩溃回归**。

**P11.C 状态**：B/C/D 组全部放弃（接受上游版本），A 组（Config.hpp null-guard）保留。最终 steady state 已落地。

**P11 整体结论**：上游 OrcaSlicer bb3 同步是**净正收益** —— 一次性消除所有类型回退冲突源，引入零 Qt6 侧回归（10 项潜在 bug 9 项架构免疫、1 项已修），且发现了 1 个之前未察觉的 Qt6 bug（vector 字段 extractDefault 缺失，已修）。上游同步工作 **完整闭环**。

---

## 9. P12 — 官方 upstream/main 基线与 Prepare 右键工作流（2026-07-21 起）

- [x] P12.1 在本地分支 `owzx-upstream-2026-07-21` 上以官方 `8b93cc5df3347c657ce6ac9a58f6923a21c2959b` 重建基线，仅保留 `Config.hpp` null-`keys_map` 保护与 `MeshBoolean.cpp` CGAL 5.4 兼容，结果子提交为 `4cb3b9ce792f15c38fabfb4bb9700895d32b1166`。
- [-] P12.2 推送子模块分支并完成父仓库 gitlink 可复现性验证。**Blocked**：等待用户明确授权推送；授权前不提交父仓库 gitlink。
- [x] P12.3 视口右键 context hit 与选择同步路由：已实现指针命中、对象/平台/空白选择同步及拖拽/gizmo/layer-editing/wipe-tower 抑制；对象、部件/实例、多选、平台和空白菜单经 C++ capability/action 路径统一路由。2026-07-23 canonical 验证通过（ViewportContext 5/0、QML 审计 138/0、E2E 29/0）。

## 10. P13 — 3D 导航立方体（左下角视角切换，v5.16 NAVIGATOR，2026-08-23）

上游真值：`GLCanvas3D::_render_3d_navigator`（GLCanvas3D.cpp:5669-5733）托管 ImGuizmo::ViewManipulate（imguizmo/ImGuizmo.cpp:2779-3140）——视口左下角 128px 真实 3D 立方体，姿态跟随相机；点击面/棱/角（27 格命中）以 40 帧插值吸附视角；拖拽旋转相机（地平线钳制）；悬停面高亮 + Front/Back/... 标签；`show_3d_navigator` 配置（AppConfig.cpp:200 默认开）+ View 菜单勾选（MainFrame.cpp:2630-2638）；Prepare 与 Preview 画布均生效。

- [x] P13.1 `src/core/rendering/NavigatorCube.h/.cpp`：ImGuizmo 数学 1:1 移植（Y-up 无需上游的 ±90° 重映射）——正交立方体相机（distance 3）、27 格命中测试（平行射线 + 背面剔除 + 立方体三等分量化，索引自正向数起）、snapDirectionForBox、拖拽旋转 + 地平线钳制、方向→az/el 映射（±90° 钳到 ±89° 防 lookAt 退化）。
- [x] P13.2 RhiViewport 交互：navigatorEnabled/navigatorBottomOffset/navigatorLabels 属性；左键按下优先于 context/orbit/gizmo 消费；拖拽 0.01 rad/px；释放点击 → QTimer 40×16ms 插值（方向 lerp 0.2/帧，up 直接吸附，同上游）；hoverMove 驱动悬停高亮；CameraController 新增 setOrientation/azimuth/elevation。
- [x] P13.3 RhiViewportRenderer overlay pass：专用 ortho UBO（×clipSpaceCorrMatrix，每帧上传）+ 专用 SRB + 无深度混合管线；仅画 3 个朝向面（悬停面 F0A060 80%）、背景 0x00101010、三轴箭头（隐藏轴 30% alpha，箭头像素空间三角形）；synchronize 镜像状态（rect 按 RT/item 比例缩放）；endPass 前最后绘制。
- [x] P13.4 SettingsViewModel.show3DNavigator（QSettings 持久化，默认 true）+ BBLTopbar View 菜单勾选项 + PreparePage/PreviewPage 绑定 + GLToolbars 按钮排在立方体上方预留 128+5（上游 GLCanvas3D.cpp:7849-7857 规则）+ NavigatorLabels.qml 标签覆盖层（x/y/z 轴标签 1.3× 处 + 本地化面标签）。
- [x] P13.5 回归：tests/NavigatorCubeTests.cpp 7 项数学测试（已入 canonical verifier：构建目标 + 运行套件）；QmlUiAuditTests::rhiViewportHostsUpstream3dNavigator 源审计（属性/事件优先级/pass 三件套/菜单/页面绑定/qrc/verifier 覆盖）。canonical verifier 全绿（2026-08-23，全部 10 套件）。
- [x] P13.6 渲染路径修正 + 可读性增强（commit `1973eec` / `9f2402b` / `8b64074`）：专用 overlay SRB/UBO 管线在 D3D11 上多轮插桩验证"几何上传+管线创建+draw 调用均正常但零像素上屏"，按预案切换为 **CPU 端经 inv(clipCorr·cameraMvp) 世界空间投影 + 场景 translucent 管线**（与 ghost shell/tool marker 同一条已验证路径），专用 SRB/UBO/管线删除。
- [~] P13.7 运行时视觉验收：**已通过本机 PrintWindow 截图确认**（2026-08-27，见 P13.8 修复后截图 `_navlbl_final_zoom.png`）：Prepare 默认等轴视下立方体三面 Top/Front/Right + y/z/x 轴标签首布局即正确可见，无需任何鼠标交互；悬停/旋转路径下标签随相机实时刷新（Top/Back/Left 已验证）。本机无人值守会话显示环境仍不稳定（DISPLAY2 0xe0000225、swapchain 睡死需 resize 唤醒），自动化截图通道以 `PrintWindow(PW_RENDERFULLCONTENT)` 为准。OWzx 可读性决策（用户两次反馈看不清，已记录）：上游 FACE 色在两个主题下都与背景差约 10%，体块提亮至 0.66 灰 @92% alpha + 深色棱线；轴线保持上游 3px 粗（三角化实现，RHI 线宽仅 1px）、箭头 6px、标签 13px；每个可见面常显本地化面名（ImGuizmo.cpp:2941-2990 无条件绘制面文字，非悬停专属）。
- [x] P13.8 面标签首布局修复（2026-08-27）：标签锚点在 QML 绑定求值时视口尚未布局（隐藏页 h=0/28），锚点 y 为负导致文字落在视口上方不可见；C++ `geometryChange` 重发通知在 QQuickRhiItem 上实测不触发（多次 resize 验证均无回调），改为 **NavigatorLabels.qml 的 model 绑定先读 viewport.width/height 再取 navigatorLabels**——QQuickItem 尺寸 notify 是可靠刷新源，布局完成后锚点自动重算；`navigatorBottomOffset` 属性补 NOTIFY（改值即刷新锚点）。已删除全部 [NAVLBL] 探针与测试矩形、geometryChange 死代码。canonical verifier 全绿（2026-08-27）。

状态：数学/交互/配置/渲染路径/标签布局 **Real**（测试锁定 + canonical 全绿 + P13.8 截图验证）；仅剩余用户桌面会话的最终确认。

## 11. P14 — 视口相机/鼠标交互上游完全对齐（CAM-PARITY，2026-08-27）

用户反馈"有些角度旋转不到"触发。全量盘点上游 `Slic3r::GUI::Camera` / `GLCanvas3D` 交互路径后确认 9 项迁移差距，一次迁移完成：

- [x] P14.1 四元数朝向状态（对齐 `m_view_rotation`）：`CameraController` 由 az/el + 固定 up 向量 lookAt 重写为四元数驱动；`viewMatrix()` = 共轭旋转 + 平移，任意朝向无极点退化。回归锁定：`defaultOrientationMatchesLegacyAnalytic` 证明旧解析帧逐元素不变（无视觉漂移）。
- [x] P14.2 约束轨道 zenit 精确钳 ±90°（Camera.cpp:373-392）：过冲量从施加俯仰中扣除（极点处停住不回弹）；旧实现 ±89° 差 1° 到不了正俯视/正仰视。同步 `select_view` top/bottom（Camera.cpp:94-97）与导航立方体吸附到精确 ±90°（NavigatorCube.cpp orientationForDirection）。
- [x] P14.3 自由相机 = 虚拟轨迹球（Camera.cpp:409-420 `rotate_local_around_target`，GLCanvas3D.cpp:4288-4290）：无角度限制、可滚转；旧实现仅去钳制、过极点画面翻转。
- [x] P14.4 约束旋转前 `recover_from_free_camera` 回正地平线（GLCanvas3D.cpp:4308, Camera.hpp:154-158）；右向量离水平时按 look_at(eye,target,worldUp) 重建朝向，eye/target/distance 不变。
- [x] P14.5 旋转中心跟随选择（GLCanvas3D.cpp:4309-4327）：选中对象包围盒中心 → 全部模型包围盒中心（`EditorViewModel.rotationCenter`，ProjectServiceMock::selectionWorldBoundingBox 已含 Z-up→Y-up 映射）→ 无模型回落 target；`orbitAround` 刚性旋转 eye/target（pivot 距离守恒）。Ctrl+拖拽 = 画布中心投到床面（m_rotation_center 语义，press 缓存）。Preview 的 pivot=盘中心=轨道 target，天然对齐。
- [x] P14.6 拾取式平移（GLCanvas3D.cpp:4353-4359）：光标→床面射线交点跟随（`groundPointOnPlane`，_mouse_to_3d z=0 等价），替换近似 right/up×distance 平移。
- [x] P14.7 缩放上游语义（Camera.cpp:75-84, :83, GLCanvas3D.cpp:3765-3768）：每格 ±1（angleDelta/120）、delta 钳 ±4、乘法 `zoom /= 1-δ·ZoomUnit(0.1)`（距离反向映射）；边界 = 场景盒半径/250（max_zoom）到 5×半径（min_zoom=0.2×fit），消灭无限拉远；近裁剪 2mm 下限已注释。
- [x] P14.8 zoom_to_mouse 实装（GLCanvas3D.cpp:3770-3783）：属性此前声明未消费（dead）；现按上游 translate→zoom→translate(-disp/(zoom ratio)) 拾取缩放。reverseZoom 沿用 v5.12（=reverse_mouse_wheel_zoom）。
- [x] P14.9 灵敏度对齐（GLCanvas3D.cpp:80 TRACKBALLSIZE=0.8）：约束/自由统一 0.8°/px（旧 0.5°/px）。
- [x] P14.10 回归：新 `tests/CameraParityTests.cpp` 13 项（默认帧回归/±90 精确钳/极点回退/地平线水平/轨迹球无界+滚转/recover/pivot 距离守恒/乘法手感/边界/target 3× 盒钳/地面拾取 round-trip）；QmlUiAudit 增加自由轨迹球/recover/orbitAround/rotationCenterHint/zoomToMouse 消费/PreparePage 绑定断言。canonical verifier 全绿（2026-08-27：NavigatorCube 18、PrepareScene 60、PartPlate 7、ObjectPicking 7、ViewModel 162、QmlUiAudit 155、CameraParity 13、ViewportContext 5、PreviewParser 17、E2E 29）。
- [x] P14.11 运行时验证：真实鼠标从默认等轴连拖 3×700px 上拉 → 视角精确停在正俯视极点（热床呈对称菱形），渲染稳定无黑屏无翻转（`_p14_pole_top.png`）——用户反馈的"有些角度旋转不到"在该方向实测消除。床底 -90°/缩放边界由 P14.10 单元锁定；本机无人值守会话 swapchain 冻结（同 P13.7 记录）阻止继续截图采样，留待用户桌面确认。

状态：相机/交互 **Real**（上游公式 1:1 + 测试锁定 + canonical 全绿 + 极点实测）；待用户桌面会话体感确认。

## 11.5 P14 追加 — 相机"飞走"根因修复（CAM-FLYAWAY，2026-09-12）

用户反馈"盘的鼠标操作有时候会飞走"触发。定位出 3 个根因，一次修复（P14.6/P14.8 的床面交点语义即根因所在，本轮按上游真值更正）：

- [x] P14.12 平移/缩放到鼠标改用紧约束视锥视平面反投影（根因 1）：旧实现把屏幕点投到床面 y=0——低仰角时射线近地平线，交点落在数公里外。上游 pan/zoom_to_mouse 均在"过当前 target、垂直视线方向、z=紧约束视锥近端"的平面上反投影（GLCanvas3D.cpp:4011-4028 / :4622-4638；calc_tight_frustrum_zs_around 令 near=到场景盒前表面的距离），任意仰角均有界。新增 `CameraController::viewPlanePoint(viewProj, viewport, screen, referencePoint)`（取参考点自身 NDC 深度反投影），`applyCameraPan` 与滚轮 zoom-to-mouse 的 translate→zoom→translate 往返全部改用（`groundPointOnPlane` 保留供床面拾取语义）。
- [x] P14.13 远枢点轨道钳制（根因 2）：`orbitAround` 绕 Ctrl 枢点刚性旋转后从未回钳 target——枢点在远处（如 1e5mm）时拖拽把相机扫出场景。现对旋转后 target 过 `clampTarget`（3× 场景盒，与 translateWorld 同界；上游 validate_target Camera.cpp:736-751；上游 set_target 本身不校验——Camera.cpp:81-85 "BBS do not check validation"，此处按本仓更严约束登记为已知偏差）；Ctrl 枢点在 press 时即钳入场景（GLCanvas3D.cpp:4583 拾取语义）。
- [x] P14.14 鼠标 grab 被抢恢复（根因 3）：Qt Quick 弹层/覆盖物可在拖拽中途夺走独占 grab，只回调 `mouseUngrabEvent` 不派发 release——旧代码不处理，拖拽状态锁存、相机持续漂移。`RhiViewport::mouseUngrabEvent` 现完整复位：gizmo 拖拽（含 gizmoDragEnd）、按键、绘制、导航球/上下文按压、Ctrl 枢点、剖切面 grab、press 拾取索引、框选/橡皮筋（含 selectionRubberBandChanged）。
- [x] P14.15 回归：`CameraParityTests` 新增 5 项——viewPlanePointRoundTrip（参考深度往返精确）、viewPlanePointIsViewParallel（位移 ⊥ 视线）、viewPlanePointStaysSceneBoundedAtLowElevation（5° 仰角全屏拖拽 < ~630mm，对照 groundPointOnPlane 近地平线行爆炸）、orbitAroundFarPivotKeepsTargetInScene（1e5mm 枢点 30 次旋转不出 3× 盒）、zoomToMouseCompositionStaysBounded。canonical gate 2026-09-12 全绿（CameraParity 19/19）。

## 12. P15 — 准备页模型+平台渲染 1:1 收口（2026-08-27 调研登记）

上游真值：`3DScene.cpp`（GLVolume 着色/双 pass/出床变暗）、`GLCanvas3D.cpp:1890-1968`（渲染序列）、`Selection.cpp`（白框）、`3DBed.cpp`（三色轴）、`PartPlate.cpp:972-1109`（盘图标/序号）。调研结论：已 Real 项（光照/中性灰/床纹理/床模型/排除区/限高环/床型纹理/仰视门控/多盘布局）不再列。

- [x] P15.1 着色系统（2026-08-29）：批次渲染通道（meshBatchVolumeTypes/ExtruderIds/PrintableFlags 三条并行数组：ProjectServiceMock 采集 ModelVolume type/extruder_id/instance printable → EditorViewModel → RhiViewport → PrepareSceneData）+ 上游语义 1:1（color_from_model_volume 3DScene.cpp:306-334 + update_colors_by_extruder :1184-1235 含 extruder 索引钳 0 + UNPRINTABLE_COLOR :170 + adjust_color_for_rendering :93-103 黑提亮/全透明修正）。锁定：PrepareSceneDataTests::modelColorsFollowUpstreamVolumeSemantics（19/19 独立验证通过）+ QmlUiAudit 全链路断言。运行时验证（2026-08-29 18:18 截图 `_p15_color_default.png`）：test_model 以耗材 1 的 filament_colour（橙）渲染，不再是中性灰。
- [x] P15.2 修饰体半透明双 pass（2026-08-29）：PrepareSceneData 按体积类型落色（MODIFIER (1,1,0,0.6)/NEGATIVE (0.3,0.3,0.3,0.4)/BLOCKER (1,0.3,0.3,0.4)/ENFORCER (0.3,0.3,1,0.4)）+ batch.translucent 标记；渲染器拆 opaque/translucent 双缓冲，半透明 pass 用共享光照 shader 的 m_modelLitBlendPipeline（blend 开/depth write 关），按视空间 Z 远→近排序逐批绘制（3DScene.cpp:871-879）；挤出机色变更经签名比对强制重染。注：上游模型默认 α0.7 未采纳——ColorRGBA 默认 (1,1,1,1) 且 update_colors_by_extruder 保留原 α，部件实际不透明，与 Qt6 现状一致。
- [x] P15.3 出床变暗（2026-08-29）：model_lit.frag 移植 gouraud.fs:154-171 片元级 print_volume 判定（矩形 xy_data/z_data、圆形 delta_radius，exact mix 1/3 黑；轴映射 upstream(X,Y,Z)→Qt(X,Z,Y)）；print_volume 着色器 uniform 注入 CameraBlock offset 144-192（PrepareSceneData::updatePrintVolume 镜像 GLCanvas3D.cpp:7177-7205 SceneEpsilon 膨胀/半径+ε；uploadCameraUniform 扩展 192B + offsetof 静态断言）；整体出床红色错误背景 QColor(192,49,10)=ERROR_BG_LIGHT_COLOR（GLCanvas3D.cpp:7069-7097/:84），PrepareSceneData::recomputeOutsideState 按 per-instance 并集 bbox 复刻 volume_state_bbox 语义（BuildVolume.cpp:308-320 Below/Inside/Colliding/Outside，仅 model-part 批次参与 :728）。已知偏差：上游仅对 partly_inside 体积注入 uniform（3DScene.cpp:967-976），Qt6 共享 UBO 无 per-batch 开关→完全出床的体积也会变暗（上游留给红背景信号）；printable_height 尚未从 viewmodel 管道接入，z_data.y 暂为无界（超高顶部不变暗，XZ 边界判定完整）。锁定：PrepareSceneDataTests::printVolumeUniformAndErrorBackgroundState。
- [x] P15.4 选中白色包围盒（2026-08-29）：buildHighlightVertices 重写为上游 Selection::render 白色角框线（Selection.cpp:1939-1949 + render_bounding_box :2558-2637：48 顶点、8 角×3 根 0.2×box.size 内向刻线、ColorRGB::WHITE），改用 m_linePipeline（Lines，depth test+write 同上游 flat shader）；自创黄/青整体染色删除（悬停不再染色，悬停语义由 P15.5 承接）；gizmo 拖拽抑制——RhiViewport::m_gizmoDragging 经 synchronize() 镜像，拖拽期间跳过绘制并强制重传（GLCanvas3D.cpp:7367 !m_gizmos.is_running()）。注：QRhi 无线宽 API，上游 2px 以 1px 呈现。
- [x] P15.5 下陷对象床面切边白轮廓（2026-08-29）：PrepareSceneData::appendSinkingContours——对 sinking 批次（bbox min<-0.001≤max，SINKING_Z_THRESHOLD Model.hpp:1709 / is_sinking 3DScene.cpp:614-620）在床平面 y=0 切片三角面，围绕切边生成 0.5mm 宽白带（HalfWidth 0.25 :108/:145）抬升 0.015（:150），按批次记录 SinkingContourRange；渲染器非悬停范围走 m_fillPipeline 深度测试，悬停/被拖动对象走 m_sinkingAlwaysPipeline（depth test off = glDepthFunc(GL_ALWAYS)，3DScene.cpp:936-947/:1018-1034）。近似：clipper 多边形 union/expand/shrink 差集以逐段垂向 quad 近似（自交轮廓可能重复绘制）。锁定：PrepareSceneDataTests::sinkingContoursSliceSinkingBatchesAtBedPlane。
- [x] P15.6 圆床几何（2026-08-29）：rebuildPlateGeometry 圆形分支——48 段边界多边形中心扇三角填充（init_model_from_poly 凸扇）+ 边界弦线（calc_gridlines 附加轮廓线 PartPlate.cpp:507-509）+ 直网格弦裁剪到圆（与 intersection_pl 同源语义 :504-505）+ every-5th 粗线/仰视线；print volume 同步圆形分支。锁定：PrepareSceneDataTests::roundBedBuildsFanFillWithClippedGrid。
- [x] P15.7 床原点 X红/Y绿/Z蓝 3D 箭头轴（2026-08-29）：rebuildAxesGeometry/appendArrow——stilized_arrow 移植（GLModel.cpp:850-932，resolution 16 / tip 1.25,5.0 / stem 0.5），stem=0.1×max(床宽,床深)（3DBed.cpp:326），原点=选中盘原点+GROUND_Z(-0.04)（3DBed.cpp:30/325），颜色 ColorRGBA::X()/Y()/Z()=0.75（Color.hpp:143-145 / 3DBed.cpp:188-190），上游轴旋转（X:{0,π/2,0} / Y:{-π/2,0,0} / Z 恒等）+ (X,Y,Z)→Qt(X,Z,Y) 轴映射；渲染走 m_modelLitPipeline+并行法线缓冲（上游 gouraud_light emission 0）；自创绿色十字线删除；axes 不受 bottom 门控（Bed3D::render_internal :377 仅看 show_axes）。
- [x] P15.8 盘网格粗线层次（2026-08-29）：calc_gridlines every-5th 规则移植（PartPlate.cpp:483/496 count%5，从盘边起数；X 粗线独占 bolder 集、Y 粗线同时留 fine 集 :478-500）；QRhi 无线宽 API——bolder 以 0.5mm 平面 quad（m_fillPipeline，≈典型视角 2px）呈现上游 render_grid 第二遍 glLineWidth(2)（:909-911），仰视图以双平行 1px 线近似并保持 LINE_BOTTOM_COLOR。锁定：PrepareSceneDataTests::bolderGridQuadsFollowEveryFifthLine + bedBottomLineGridUsesUpstreamBottomColor（更新粗线仰视计数）。
- [x] P15.9（2026-08-31，commit a43b19b，接近 Real）：视口内每块盘锚定自己的控制簇——`PrepareSceneData::plateGridOffset` 单源化盘布局间距（upstream compute_shape_position PartPlate.cpp:3206 + LOGICAL_PART_PLATE_GAP），`RhiViewport::plateAnchors` 经 cameraMvp 把每盘图标列角投影到 item 像素并在全部相机/布局变更点刷新，PreparePage 以 Repeater 按锚点渲染每盘紧凑簇（切换/重命名/锁定/排列/自动朝向/删除，逐控件 tooltip，相机背面隐藏），自动朝向走上游 select_plate+orient 序列；QmlUiAudit 159/159（新增 prepareViewportAnchorsPlateClustersToWorld + 重写 prepareViewportHostsPlateIdentityOverlay）。残余差距（保持 Hybrid 注记）：上游纹理图集图标外观与圆床锚点内移为近似，未逐图标复刻纹理资产（PartPlate.cpp:938-1109）。
- [x] P15.10 镜像体绕序翻转（2026-08-29）：ProjectServiceMock::meshData 批次烘焙检测 combined=instance×volume 线性块行列式<0（等价上游 mirror 符号积 is_left_handed 3DScene.cpp:335-340），负行列式批次每三角形按 (0,2,1) 发射顶点＝glFrontFace(GL_CW)（:520-521/:607-608）的 CPU 等价变换；渲染器按绕序重算法线，镜像体法线/光照随之正确。注：ProjectServiceMock::meshData 依赖 libslic3r Model，无法在 PrepareSceneDataTests 独立目标单测，以代码路径+上游映射记录验证。
- [x] P15.11（2026-08-30，Hybrid）：已实现并验证涂色 gizmo 隐藏盘面、选中中心白色 0.75 mm 球（联合批次 AABB 中心）、侧栏位置/旋转/缩放提示箭头（含上游 T(±5Y)·Rz 箭头对语义与轴向一次旋转修正，GizmoGeometryTests 23/23 锁定），以及由真实 `Print::validate()` 输出驱动的顺序打印碰撞/高度覆盖层；`QmlUiAuditTests` 锁定渲染顺序、焦点桥接、独立缓冲和 resolved `ByDefault` 门控。拖动实时安全区已于 2026-08-31 完成（commit dde3d84）：`SequentialClearanceCompute` 值类型移植 `update_sequential_clearance()`（hull 缓存/round 偏移/重实现 object_skirt_offset 单喷嘴精确/盘 bbox 门/上游打印序/lid-rod-printable_height 规则），EditorViewModel 拖动钩子 150ms 防抖 + QtConcurrent 代次计数，渲染器上游 NO_FILL_COLOR 灰预览 vs 失败蓝、预览填充扇面跳过、验证载荷拖动前后备份恢复；canonical 全绿含 E2E 29/29、QmlUiAudit 160/160。仍 Deferred：提示箭头对旋转对象的局部轴矩阵与多对象聚合选择已于 2026-08-31 完成（commit bcfc050 + 0b62dea）：`selectedHintLocalRotation`/`hintLocalRotation` 链路把选中对象（instance×volume，upstream orient_matrix 组合，Selection.cpp:2003-2020）经四元数预乘进提示箭头顶点（slic3r→scene 轴重标注验证）；`GizmoCenter::fromSelectedIndices` + 全链路选中集合属性使 gizmo 枢轴/白色角框/中心球按全部选中对象联合 AABB 中心（Selection::get_bounding_box().center()），item 侧 gizmo 拾取与按压门控随绘制位置。锁定：GizmoGeometry 24、GizmoState 11、QmlUiAudit 161（rhiMultiSelectUnionCenterPropertyChain）、ViewModelSmoke 170（selectedHintLocalRotationMirrorsSelectionRotation）。语义注记：上游对单实例 position 字段保持世界轴、rotation_y 有 x==0 特例，本实现对全部字段统一应用对象旋转（未旋转对象行为相同）。
- [x] 环境收口（2026-08-31，commit 14e0207）：canonical 验证器端到端稳定化——cmake 停止向所有测试 exe 传播 /DEBUG（每轮约 8GB 可再生 PDB 曾耗尽 E 盘致 ninja 中途死亡）；脚本构建前清理残留测试进程（LNK1104 锁）、纳入 GizmoGeometryTests 构建与运行、成功后自动回收 PDB；修复 GizmoGeometryTests 自 Phase 66 起的 const QFile 编译错误并校正 scale/rotation 提示箭头测试期望至上游 T(±5Y)·Rz 语义。2026-08-31 02:15 canonical 全程跑通（exit 0，含启动门与 PDB 回收）：PrepareScene 23、PartPlate 60、ObjectPicking（阻塞门通过）、ViewModel 169、QmlUiAudit 158（含全部 P15.9/P15.11 新断言）、GizmoGeometry 23、Navigator 8、Camera 13、PreviewParser 23。已知非阻塞问题：AppToolTests（AI 线，另一会话范围）确定性堆破坏（ntdll 堆分配内 0xC0000005，地址跨次相同），ViewportContextMenu/E2E 同族浮动——共享 import 路径，需管理员 PageHeap 归因，暂以非阻塞门放行并在脚本内注明。最终归因（2026-08-31）：build/ 的 MSVC /showIncludes 本地化输出 ninja 无法解析→头文件改动留下陈旧 .obj，新旧对象混链即堆破坏（ntdll 堆分配内同址崩溃），与测试内容无关；全量删除测试目标 .obj 目录重建后 12 套件全绿 530 通过 0 失败（含 AppTool 10/10、ViewportContext 5/5、E2E 29/29 全部恢复），canonical exit 0。后续：任何头文件改动后需删除受影响目标 .obj 目录再测试（脚本 configure 不足以修复依赖）。

## 13. P16 — 右键菜单操作 1:1 收口（2026-08-27 调研登记）

上游真值：`GUI_Factories.cpp`（MenuFactory 全菜单）、`GUI_ObjectList.cpp:2589-2810`（merge 语义）、`Model.cpp:1891-2028`（split/merge）。已 Real 项（路由/菜单族/克隆/修复/简化/拆分/镜像/删除/重载/导出/单位换算/盘菜单，P12.3 锁定）不再列。注意：本 fork 上游无"编组(group)"概念，合并=Assemble 多部件对象。

- [x] P16.1 Assemble 完整语义（2026-08-29）：`ProjectServiceMock::assembleObjectsReturningIndex` 重写（ProjectServiceMock.cpp:6621 起）为上游 ObjectList::merge(true) 1:1（GUI_ObjectList.cpp:2589-2791）——新建 "Assembly" 对象（:2683）、逐源烘焙 `instance_matrix×volume_matrix`（:2701-2712）、多 volume 源保留 per-volume config（:2724-2729）、对象 config 逐 key 合并含 defaults 回退（:2735-2760）、单 volume 源 extruder 下沉 volume config（:2761-2767）、layer_config_ranges 合并（:2769-2770）、sort_volumes/ensure_on_bed/center_around_origin/assemble_transformation 尾段（:2733,:2772-2781）、多实例源先拆单实例再合并（instances_to_separated_objects :5212，Qt6 选择为对象级=全实例路径）、元数据镜像+盘成员重建（修复旧实现 modelCount_ 失同步）、返回新索引供选中（:2784-2788）。锁定：assembleBakesVolumeWorldTransforms + assembleMergesConfigsAndSinksExtruder + assembleUndoRestoresSources。
- [x] P16.2 Merge parts to single object（2026-08-29）：`ProjectServiceMock::mergeObjectVolumes`（ModelObject::merge，Model.cpp:2010-2028，网格拼接、per-part config 随上游 merge 本身丢弃）+ `EditorViewModel::mergeSelectedPartsToSingleObject`（undo 走 SceneSnapshotCommand）+ partMenu "Merge parts to a single object" 入口（contextActionAvailable("mergeToSingle")）。锁定：mergePartsToSingleObjectCollapsesVolumes。
- [x] P16.3 Mesh boolean 菜单接线（2026-08-29）：part/multi 菜单 "Mesh boolean" → `booleanExecute()`（contextActionAvailable("meshBoolean")）。注：上游 can_mesh_boolean（GUI_ObjectList.cpp:3151）为单对象多 volume 语义；Qt6 后端 booleanExecute 契约为 2 对象 A/B 运算，按后端契约门控（恰好 2 个选中对象）。
- [x] P16.4 Change Filament 子菜单（2026-08-29）：`EditorViewModel::setExtruderForSelectedItems` 1:1 移植 ObjectList::set_extruder_for_selected_items（GUI_ObjectList.cpp:5622-5695：对象级写入并清部件 extruder 覆盖 :5679-5688、Default→对象=1/部件=父对象值 :5659-5666、部件仅 MODEL_PART/PARAMETER_MODIFIER 可改 :5639-5651）+ `configFilamentCount`/`objectExtruderId`。QML：PrepareContextMenus `ChangeFilamentSubmenu`（Default/Filament n，多选标题 "Set Filament for selected items"，GUI_Factories.cpp:1907）接入对象/部件/多选菜单；≤1 耗材隐藏（:1893）。配套：`ProjectServiceMock::setVolumeExtruderId`(-1) 改为擦除真实 volume config extruder 并允许文件加载对象直接写真实 volume。锁定：changeFilamentAssignmentFollowsUpstreamSemantics。
- [x] P16.5 五类 Add 子菜单接入视口右键（2026-08-29）：PrepareContextMenus `AddVolumeTypeSubmenu`（Add part/negative/modifier/blocker/enforcer，上游 ADD_VOLUME_MENU_ITEMS GUI_Factories.cpp:285-292 + append_submenu_add_generic :642-662）——Load...（addVolumeFromFile）/Cube/Sphere/Cylinder/Torus 以指定 volumeType 落入选中对象（`ProjectServiceMock::addPrimitive` 扩展 volumeType 参数）/Add text、Add SVG 仅部件类可见并路由 Text/SVG gizmo（上游 append_menu_item_add_text/svg 开 gizmo，Qt6 位 16/17；"点击处生成"由 gizmo 承担）。注：Qt6 addPrimitive 后端不支持 Cone/Disc（上游有），未虚造。
- [x] P16.6 多选菜单补齐（2026-08-29）：multiMenu 增 Split（To objects/To parts，GUI_Factories.cpp:1700-1717）、Edit Process Settings（append_menu_item_per_object_process :1846）、Convert units 四项（:1688）、"Export as STLs..."（分离文件，append_menu_item_export_stl(is_mulity) :842-848，走既有 requestExport(true,false)→exportSelectedObjects 第 3 参）、Set printable/unprintable 按首选中对象状态切换文案（append_menu_item_set_printable :1963）。
- [x] P16.7 部件菜单补齐（2026-08-29）：partMenu 增 Change type 五类子菜单（changeVolumeType）、Mirror X/Y/Z、Center（P16.10 语义）、Simplify（gizmo mask 门控）、Change Filament、Drop、Merge parts to a single object（P16.2）、Mesh boolean（P16.3）。
- [x] P16.8 Undo 命令补齐（2026-08-29）：新增 SceneSnapshotCommand（Assemble/Merge-to-single/Split to objects/parts：capturePlateListSnapshot(deep) 前后快照交换，对齐上游 Plater::TakeSnapshot 整模型快照真值）、MirrorCommand（id 13，独立于 TransformCommand 合并）、PrintableCommand（对象/实例两粒度）、ObjectSnapshotCommand（单位换算/Reload 的原地整对象 3MF 快照交换）、InstanceCountCommand（+ 宏内 TransformCommand 捕获 arrange 位移）；Center/Drop 走 TransformCommand 宏。锁定：assembleUndoRestoresSources + instancePrintableToggleRoundTrip + centerAlignsSelectionToBedCenter（undo 段）。
- [x] P16.9 Flush Options 与 Invalidate cut info（2026-08-29）：Flush Options 为 config-only 真值——`objectFlushOption/setObjectFlushOption` 写对象 config `flush_into_infill/flush_into_objects/flush_into_support`（FREQ_SETTINGS_BUNDLE_FFF，GUI_Factories.cpp:69,:966-1008），读取回退 plate config（上游回退全局 print preset 的对应面）；门控 contextActionAvailable("flushOptions")（单对象 + enable_prime_tower + 盘上多耗材，:944-961）。注：Qt6 侧 enable_prime_tower 从 plate config 读取（上游读全局 preset），仅当该键已落入 plate config 时显示——保守门控，登记为 Hybrid 小缺口。Invalidate cut info 为 Real——Qt6 cut 路径（连接器切割）经 libslic3r Cut 产出 cut_id，`objectHasCutInfo/invalidateCutInfo` 按 has_selected_cut_object/invalidate_cut_info_for_object（GUI_ObjectList.cpp:3033-3076）清除共享 cut_id 的全部对象。QML：objectMenu/partMenu 注入 FlushOptionsSubmenu + "Invalidate cut info"。注：勾选态以文本前缀呈现（CxMenuItem 无 check 指示器）。
- [x] P16.10 Center 语义修正（2026-08-29）：`EditorViewModel::centerSelectedObjects` 重写为对齐盘中心——按 selectionWorldBoundingBox 中心平移至（bedOrigin+bedSize/2），仅 X/Y（高度保持），Selection.cpp:474-489 + PartPlate::get_center_origin（PartPlate.cpp:1881）；撤销走 TransformCommand 宏。锁定：centerAlignsSelectionToBedCenter；同步修正 prepareVisibleObjectActionsMapToSourceObjects 中锁旧"重置原点"错误行为的断言。
- [x] P16.8a 已修复（提交 311ecc3）：Assembly redo 后 per-volume 完整保留——assembleUndoRestoresSources 的 redo 断言已恢复为 objectVolumeCount==2，2026-09-01 canonical 全量验证通过（ViewModel 177/177，0 failed）。
- [x] P16.11 per-instance printable（2026-08-29）：`ProjectServiceMock::instancePrintable/setInstancePrintable`（单 ModelInstance::printable 写，GUI_ObjectList.cpp:5817-5866 itInstance 分支）+ `EditorViewModel::instancePrintable/toggleInstancePrintable`（undo PrintableCommand）+ ObjectList.qml 实例行（>1 实例时展开，可打印圆点 + 右键设为可打印/不参与打印，对齐上游 itInstance 子节点与打印列点击）。锁定：instancePrintableToggleRoundTrip。

## 13.5 已知缺陷登记 — E2E 切片交付路径潜在栈越界（2026-09-01）

- [x] KNOWN-01 已结案（2026-09-02）：根因为 MSVC 陈旧目标文件 ABI 混用（同仓库历史 appSettings 崩溃模式）——改动 EditorViewModel.h/RhiViewport.h 等测试链头文件后 ninja 未重编全部依赖目标，混合旧 obj 的 E2E/ViewModel exe 在切片结果交付 lambda（Qt QMetaMethodInvoker 队列调用）触发 /GS 栈 Cookie fail-fast（0xC0000409，ecx=2）。修复：删除 build/CMakeFiles/{owzx_app_core,E2EWorkflowTests,ViewModelSmokeTests,QmlUiAuditTests,PartPlateTests,AppToolTests,PreviewParserTests}.dir 与对应 _autogen 后全量重编；随后三种布局（默认、/Zi+/DEBUG、/MAP）连续三轮 canonical 全绿（E2E 29/29，ViewModel 179/179），CMakeLists 临时 /Zi//DEBUG//MAP 标志已全部还原，非代码缺陷。

## 14. P17 — 预览页渲染 1:1 收口（2026-08-27 调研登记）

**口径裁决（2026-08-27）**：锁定上游快照 EViewType 12 模式为真值，全部 1:1 补齐；Qt6 已有 6 个 libvgcode 扩展模式（Summary/Actual Speed/Acceleration/Jerk/Actual Flow/Pressure Advance）为真实 M 码数据驱动且测试锁定，登记为 OWzx 超集保留。上游真值：`GCodeViewer.cpp/.hpp`。

- [x] P17.1（2026-08-29 批量收口） FilamentId 模式补齐（GV.cpp:910-911 隐藏检查模式，伪色 {id,role,id}，无图例）。
- [x] P17.2（2026-08-29 批量收口） 挤出路径实体化：三角形截面三棱柱（GV.cpp:817-822, GV.hpp:336-358，每段 8 顶点带 width×height）替换 1px 线段；travel 线宽随 zoom（1px→6px，GV.cpp:3873-3875）。
- [x] P17.3（2026-08-29 批量收口） 图例修正：渐变条用上游 10 档 Range_Colors #0b2c7a→#942616（ViewModel 已给 QML 未用）；10 档逐档值列表+单/双值退化（GV.cpp:4498-4518）；per-mode 小数位（Height/Width 2 位）；log 尺度标签。
- [x] P17.4（2026-08-29 批量收口） FeatureType 图例补时间/百分比/耗材列（GV.cpp:4808-4845）+ travel 三态色（GV.cpp:3233-3237）+ Retract/Unretract 默认改隐藏（GV.hpp:324）。
- [x] P17.5（2026-08-29 批量收口） Shell/ghost 对齐：alpha 0.15（现 0.35）、按挤出机染色、含切片生成几何（GV.cpp:3076-3186）。
- [x] P17.6（2026-08-29 批量收口） ColorPrint：cp_color 换色段色（GV.cpp:3212-3220）、换色次数、分段估计时间（Print/Pause/Color change 区段）、Custom g-code 总览表（GV.cpp:5161-5545）、filament 预设名。
- [x] P17.7（2026-08-31，commit 0b605fd）：SequentialRangeCap 端面封口（dynamic buffer + pass 内重算）+ top_layer_only 拖动灰化（三色分支全部 Neutral 0.18，setCurrentMove 重打包）。
- [ ] P17.8（Blocked）：冲突(撞机)标记——数据源在上游 GCodeProcessor 切片期碰撞检测，Qt6 解析管线无 conflict 通道，需切片侧先产出数据。
- [x] P17.9（2026-08-31，commit 0b605fd）：G90/G91、G2/G3 弦分段（I/J+R 双形式）、G4 dwell、加速度梯形时间模型（逆向/正向速度传播，TIME_ELAPSED 标签文件保持标签权威）+ 测试锁定；append_full_config 全键以 `; key = value` 头部解析（fullConfig/fullConfigValue）实现，filament_settings_id 进 Filament 图例行。
- [x] P17.10（2026-08-31，commit 0b605fd）：图例渐变值/端点在 SettingsViewModel.units==1 时换算英寸（use_inches 对齐）。
- [x] P17.12（2026-09-12）"变暗下层"（上游 preview_dim_previous_layers，AppConfig.cpp:206-207 默认 false；G-code Preview 组 Preferences.cpp:1908-1915，上游置于 Graphics 页）：PreviewViewModel 新增 dimPreviousLayers 属性；打包循环 dimmed = dim && topLayerGray && layer < currentLayerMax_，三处灰化分支改为 `topLayerGray && !dimmed`（上游 dim > gray 优先级，ViewerImpl.cpp:1225-1290——变暗层保留真色、仅向黑缩放 keep=1-0.60），段后统一 `rgb *= 0.4`；层上方仍灰化不变。m_topLayerOnly 默认改 true（上游 seq_top_layer_only 默认 "1"，AppConfig.cpp:202-203，GCodeViewer.cpp:1132-1135）。SettingsViewModel 新增 previewDimPreviousLayers（QSettings 持久化）；Preferences 外观分类新增"G-code 预览"组（开关+上游提示文案）；BackendContext settingsChanged→setDimPreviousLayers 推送+初始同步。附：Phase A 复扫其余三项均为引擎继承（PA_Line 校准 QOL=GCode::do_export Calib_PA_Line 分支；漂浮 purge 线=GCode.cpp:1848-1851 change_layer 守卫；{first_object_name}=PrintBase.cpp:24-43），Qt6 导出对话框默认文件名沿用自有 project+plate 方案登记为部分对齐。
- [x] P17.11 文档修正（2026-08-29）：P3.3 "13 种 EViewType" 更正为"上游快照 12 模式（P17 口径裁决）"；头部 CrealityPrint 口径注释更正为 OrcaSlicer（行 4/13 历史记录保留原文，仅补更正注记）；源码真值功能矩阵的 GCodeRenderer 引用标注已并入 RhiViewportRenderer CanvasPreview 分支。

## 15. 全工程代码评审登记（2026-09-04）

> 评审方式与完整问题清单（P0×6 / P1×45 / P2 摘要 / 测试可信度 / Mock 假成功清单 / 仓库卫生）见 [docs/代码评审报告_2026-09-04.md](代码评审报告_2026-09-04.md)。本节只登记需要进入修复排期的条目；以下均为 `[-]`/新增缺陷口径，不改变 P0-P17 已 `[x]` 条目的验收结论。

### 15.1 P0（未提交 WIP 中引入，提交前必须修复）

- [ ] R-P0.1 SliceService 专属切片线程与 GUI 线程无锁共享 `m_persistentSlices`（SliceService.h:426、.cpp:271-280/824-905/1301-1311）——UB 竞态
- [ ] R-P0.2 SliceService 析构仅置 flag，`Print::process()` 不检查 → `wait(10000)` 超时带线程析构 qFatal（.cpp:271-280；上游 BackgroundSlicingProcess.cpp:554 先 cancel 再 join）
- [ ] R-P0.3 `booleanExecute()` 先执行布尔后构造 SceneSnapshotCommand → undo before==after 变 no-op（EditorViewModel.cpp:2509-2518，违反 UndoCommands.h:614 约定）
- [ ] R-P0.4 `saveProjectAs`/`exportModel("3mf")` 走 Prusa 版 store_3mf，不写 plate_data_list/缩略图/耗材 sidecar → 多盘项目另存为后数据塌缩丢失（ProjectServiceMock.cpp:1542-1547；全量 writer 在 :10206）
- [ ] R-P0.5 `setObjectVisible` 把 visible 写成 `ModelInstance::printable` → UI 隐藏对象被静默剔除切片（ProjectServiceMock.cpp:2355-2372，播种同根 :1277/6795/9148/9337）
- [x] R-P0.6 (已修: 2026-09-13 校验——main.qml requestQuit() 为唯一退出入口, onClosing/顶栏X/文件菜单全部路由, BBLTopbar signal quitRequested) 标题栏 X / File>退出 走 `Qt.quit()` 绕过 onClosing 脏项目确认 → 未保存修改无提示丢失（main.qml:673、BBLTopbar.qml:849/119；上游 MainFrame.cpp:443 close_with_confirm 可否决）

### 15.2 ctest 实测回归（2026-09-04）

- [ ] R-T.1 CliTests `testSliceBlock20XY`：切片退出码 -3 ≠ 0（tests/CliTests.cpp:136），与 WIP `setDirectOutputPath` 直写改造（CliRunner.cpp:837-843）时间正相关，需排查
- [ ] R-T.2 InventoryAuditTests：引用的 `.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` 已随 .planning 重组删除（tests/InventoryAuditTests.cpp:164）；需迁移清单路径或恢复清单；注意 build/ 下该 exe 为 2026-08-31 陈旧产物

### 15.3 P1 重点批次（完整清单见评审报告第 5 节，此处记主题）

- [ ] R-P1.A Undo 健全性：UndoRedoManager heavyBytes 只增不减 → 假超限整段清空历史 + 宏中途 clear + 注释与实现矛盾（.cpp:58-85）；WIP 测试 undoMemoryBudgetEvictsOldestHeavyCommands 把"全清"锁为正确，需一并修正；快照按名字对账重名删错（ProjectServiceMock.cpp:6013-6025）；restoreVolumeSnapshot 丢涂色/文字元数据（:5599-5753）
- [x] R-P1.B (已修: 代码三处均带 R-P1.B 标记——avgSpeed mm/min÷60 标 mm/s(PreviewViewModel.cpp:2507)、G2 词首误匹配改为整词+空格守卫(:2286)、播放结束补 emit stateChanged(:632)) Preview 正确性：avgSpeed mm/min 标 mm/s 60 倍错（PreviewViewModel.cpp:2688/1055）；startsWith("G2") 误匹配 G28（:2474）；播放结束 isPlaying 卡 true（:622-628）；直通预览路径零测试
- [ ] R-P1.C 镜像平移与泄漏：deleteObject/split/assemble 后 m_mockLayerRanges/overrides 不平移（ProjectServiceMock.cpp:6766-6841 等）；objects.erase 泄漏 ModelObject（:4722/4939）；splitObject 泄漏 newObjects + 盘成员不平移（:9281-9310）
- [ ] R-P1.D WIP 回归组：skipAmsBlacklistCheck 不落 saveToSettings（SettingsViewModel.cpp:236-272）；"°C"→"℃" 打断 10 条 .ts 翻译（ConfigOptionModel.cpp:61-64）；远程工具进程检测钉死软件渲染无提示（main_qml.cpp:86-117）
- [ ] R-P1.E 假成功披露治理（对照依赖与协议边界审计.md 三原则）：CloudServiceMock 登录/绑定、DeviceServiceMock 假上线/假打印、CameraServiceMock 假 Streaming、MultiMachine 假发送——4 处违规需补用户可见披露 + 防复活测试；PrintDialog 读不存在的 editorVm.lastGcodePath（PrintDialog.qml:155-159）
- [ ] R-P1.F MMU HeightRange 三重缺陷：toolType 3 跨 gizmo 语义串扰（PreparePage.qml:3775/2222）、拖拽不路由（RhiViewport.cpp:2398-2403）、trafoNoTranslate 传参偏移高度带（EditorViewModel.cpp:4927-4941 + PaintEngine.cpp:222-228）
- [ ] R-P1.G 渲染资源：releaseResources 遗漏 m_supportPreviewBuffer（RhiViewportRenderer.cpp:1359-1540）；uploadModelBuffer 失败混合状态 + translucent 回退分支 index/vertex 偏移错画（:2911-3000/2681-2695）；SoftwareViewport 交互信号声明无 emit（SoftwareViewport.h:443-485）
- [ ] R-P1.H Q_INVOKABLE 一次性绑定系统性陈旧 UI：PrepareContextMenus 勾选/文案、instanceCountDialog 旧值静默写错、LeftSidebar 红点、PreviewLayerRail 滑条、ErrorToast 计数——推广 Connections 刷新或 NOTIFY 属性
- [x] R-P1.I (已修: canSave 门禁内置 topbarSaveProject(BackendContext.cpp:1176); 2026-09-13 补齐 QML 残留——main.qml Ctrl+S 与 BBLTopbar 两处"保存项目"菜单在切片中不再误开另存为对话框, 菜单项 enabled: backend.canSave) 项目/保存门禁：Ctrl+S 与菜单保存绕过 canSave 切片门禁（main.qml:409-415、BBLTopbar.qml:742-745）；删除当前盘视口网格不刷新（EditorViewModel.cpp:8756-8780）；requestSlice 硬编码 220×220 热床（:9757）
- [x] R-P1.J 预设/向导假完成（2026-09-15 复核+补齐：向导 Finish 现经 backend.configViewModel 应用同名打印机/耗材预设；SavePresetDialog 覆盖保存可达（三条保存路由+G-13 detach）；BedShapeDialog 快照回滚已实现（R-P1.J3 标记）；分层规则残留：ConfigWizard/WipeTower 直绑 presetServiceMock 属历史遗留，随波次2重构）
- [ ] R-P1.K 服务健壮性：loadFile worker 直写 receiver 成员（ProjectServiceMock.cpp:1056-1178）；undo 快照临时文件写安装目录（:5327-5330）；JobManager job 泄漏 + finished 双重计数（JobManager.cpp:59-60）；MCP 悬挂请求无超时（McpHttpServer.cpp:103-107）；mesh blob geometryCount 未校验（EditorViewModel.cpp:127）
- [ ] R-P1.L 死代码处置（反退化规则）：panels/ObjectList.qml（~1560 行未实例化且拖拽实现无效）、panels/SliceProgress.qml（未接线）、BBLTopbar 隐形 TabBar、CxPillAction 无调用点

### 15.4 文档/卫生登记（本次评审发现，不进代码排期）

- AGENTS.md 漂移：GLViewport/GLViewportRenderer/GCodeRenderer、ModelMallViewModel、"强制 QSGRendererInterface::OpenGL"、QT_LOGGING_RULES 与代码现状不符，待修订。
- docs/发布就绪度.md 的 Model Mall "78% 可用（Mock）" 已于 2026-09-04 修订为"已移除（MALL-01 锁定）"。
- 仓库卫生（根目录压扁名 0 字节文件 ~19 个、build_*.log/截图残留、.planning/debug AI 调试产物）与未跟踪文件处置建议见评审报告第 10 节。
- 功能矩阵（docs/源码真值功能矩阵.md）陈旧锚点/状态已于 2026-09-04 校订。

### 15.5 定时修复执行记录（2026-09-05 00:15）

**重要环境事件**：定时任务 23:00 启动后，本目录在 23:23:29 被另一会话执行 `git stash`（stash@{0}，"perf-waves + v5.16 WIP carryover"，59 文件 +7916/-1171，含当日 57 文件 WIP 与本任务首批修复）、23:25:46 又被 `git reset --hard`。WIP 全部内容完好保存在 stash@{0}；两个会话同时在 build/ 目录构建还会互相杀伤编译进程（本任务两次构建被杀）。**结论：R-P0.1/2/3 及全部 WIP-only 发现（meshData v2 校验、heavyBytes、skipAms 持久化、℃ 翻译、直通预览、远程工具检测、supportPreview releaseResources 等）在当前 HEAD 树上目标代码不存在，属 N/A；stash@{0} 的 WIP 落地（如合入 perf/waves）时必须按评审报告第 4/5 节先行修复这些条目再合入。**

**已修复并本地提交（review/v5.16-gap-closure，未 push）**：
- `671c705` 批次①：R-P0.4（saveProjectAs/exportModel(3mf)/writeProjectSnapshot 统一走 storeProject3mf 全量 writer + 盘耗材 sidecar + overlay 内嵌）、R-P0.5（setObjectVisible 委托 setObjectPrintable，双镜像同源播种——**评审定性修正：锁定快照无 ModelVolume::visible，BBS 系"眼睛"即 printable，原 P0 表述有误**）、R-P0.6（requestQuit 关闭守卫统一三入口）、R-T.1（补 tests/test_models/Block20XY.stl，CLI -3=FILE_NOTFOUND 根因是 fixture 从未入库）、R-T.2（InventoryAuditTests 快照路径迁移至 .planning/milestones/v3.6-phases/，4da9eb3 归档）。
- `77d46c2` + `aa946bd` 批次②：P1.B（avgSpeed 60 倍量纲、G2/G3 词守卫、播放结束 emit）、P1.E1（PrintDialog/SelectMachineDialog 诚实化：真实 lastGcodePath 桥接、在线设备+非空路径门控、Mock/外部阻塞披露、删除占位控件）、P1.I（topbarSaveProject/As 切片中保存门禁下沉 C++、deletePlate 网格刷新+选择重验、requestSlice 用预设床尺寸替代 220 硬编码）、P1.K（JobManager deleteLater 修复泄漏、JobBase reportError 双重计数、MCP 400+30s 空闲回收）。

**验证状态**：由于并发会话持续占用 build/（ninja+6×cl 编译中），本任务未能完成构建+ctest 验证。**下一步（按序）**：1) 并发会话结束后运行 `scripts/auto_verify_with_vcvars.ps1` 确认全绿；2) 处置 stash@{0}（WIP 按评审修复后合入或弃用，需用户决策）；3) 剩余适用 P1：R-P1.A'（undo 快照按名对账/sidecar 字段缺失——HEAD 存在）、R-P1.C'（deleteObject 镜像平移/splitObject 泄漏——HEAD 存在）、R-P1.E'（CloudServiceMock 登录假成功披露、MonitorPage 演示披露）、R-P1.H（Q_INVOKABLE 一次性绑定系统性改造）、R-P1.J（向导完成应用预设、覆盖保存、BedShape 回滚）、R-P1.L（ObjectList.qml/SliceProgress.qml/隐形 TabBar/CxPillAction 死代码处置）、R-P1.K'（loadFile 工作线程直写、undo 快照临时文件落安装目录）。

### 15.6 修复轮完成 + 复核登记（2026-09-05）

**第二批外部回退事件**：671c705 提交内的 ProjectServiceMock R-P0.4/5 hunks 在提交后被并发会话从工作树选择性回退（671c705 的文件 blob 即已不含这些改动——提交时间点前已被回退；批次③④的编辑不受影响）。已于 `63bf0ad` 第三次重做并核验提交 blob 含全部标记。两次 stash/reset + 两次选择性回退均指向同一并发会话，**用户需协调停用其中一方**。

**本轮新增修复（提交 63bf0ad）**：
- splitObject 的 `delete newObj` 编译错误/双重释放——split() 产物归克隆 tempModel 所有（ModelObject 析构私有，C2248），对齐上游依赖克隆析构释放（Plater.cpp:5098-5119）。
- R-P0.4/5 第三次落地（见 15.5），另修复 topbar 保存路径丢 PSET2-06 overlay 的缺口（storeProject3mf 内嵌 overlay config）。

**复核（子代理交叉评审）确认的对齐结论**：项目保存路由/向导预设应用/覆盖保存/打印发送门控/Preview 量纲与 G2 守卫/眼睛-printable 统一——与上游语义一致；PreviewParserTests 无 avgSpeed 断言，无测试回归。

**剩余对齐差距（复核登记，除已知阻塞项外；按优先级）**：
- [x] G-01 SavePresetDialog 覆盖用户预设（2026-09-05，commit 2684483：PresetServiceMock/ConfigViewModel/SavePresetDialog 三层 overwriteUserPreset + replace 警示；isUserPreset 门控，内建/厂商仍拒绝）。剩余："Preset Inside Project"与 Detach 分支、文件名非法字符/别名校验（SavePresetDialog.cpp:191-232）——并入 G-13。
- [x] G-02 print(process) 预设已应用（2026-09-05，commit 9ba58f4）。[ ] 余项：export_selections 等价持久化；selectedBedType 死值清除。
- [x] G-03 按需切片已实现（见上，commit 2b3c728）。
- [x] G-04 数据/写盘层完成（2026-09-05，commit 9aa1f94，详见 §15.7 G-04 条）。[x] G-04(渲染余项·no_light) 完成（Qt6 管线全程无光照，主缩略图即 no_light 忠实呈现——setPlateThumbnailFromBase64 同步写 noLight 缓存）。[x] G-04(渲染余项·picking) 完成（2026-09-05，commit 926f70c）：uploadModelBuffer 构建 pick 顶点缓冲（soup 同位、逐 ModelBatch 调色板平色烘焙进顶点色，sourceObjectIndex 哈希色对齐上游对象 id 编码），renderThumbnailPass 按 variant 渲染（复用无光照管线零新增 GPU 状态），variant 贯通 request→synchronize→deliver（thumbnailCapturedForPlate 三参），PreparePage 级联（主图完成→同盘 picking 捕获→setPlatePickThumbnailFromBase64 写 pickThumbnail 缓存→storeProject3mf pick 族写盘）。
- [x] G-05 拆分失败警告已加（commit 9ba58f4）。[ ] 余项：产物 sort_volumes/ensure_on_bed（Plater.cpp:4512/4570）。
- [x] G-06 快照 v2 稳定 ObjectID 完成（见上，commit a68f262/c65479d，含回归测试）。
- [x] G-07 Q_INVOKABLE 一次性绑定改造全部完成（见上两行）。
- [x] G-08 死代码处置完成（2026-09-05，commit 95b4bc5）：三文件 git rm + qrc 3 行 + BBLTopbar 隐形 navTabBar 块 + QmlUiAuditTests 8 个槽修剪/注释清理（169 槽全保留，死内容断言移除）；零引用自检通过。
- [x] G-09 门禁文案改为"切片/任务进行中，暂无法保存项目"（2026-09-05，commit 9ba58f4）。
- [x] G-10 G02/G03（及 G00/G01）前导零识别（2026-09-05 收口，commit 8b87060，PreviewViewModel 解析分发扩展；见 §15.7，canonical ID RES-G10-LEADING-ZEROS）。
- [x] G-11 loadFile 工作线程直写 receiver 成员（数据竞争窗口）；undo 对象快照临时文件写安装目录（只读目录下静默退化）——两项均于 2026-09-05 收口（commit 8b87060：pending 改本地容器+队列投递、快照改 QTemporaryDir）；loadProject 同类直写由 c6c33a3 修复（canonical ID RES-G11-LOADFILE-WORKER / LOADPROJECT-GUI-THREAD-PUBLISH，见 §15.7）。

**端到端验证**：本轮以 `scripts/auto_verify_with_vcvars.ps1`（含 12 套件 ctest：E2EWorkflowTests 导入→切片→预览→导出→项目存取 30 项、ViewModelSmoke、QmlUiAudit、PartPlate 等）为权威验证；结果记录于验证日志。

### 15.7 修复轮最终验证与对齐结论（2026-09-05）

**提交链（review/v5.16-gap-closure，48f73ac → 9ba58f4，共 8 个提交，未 push）**：
`671c705`（P0 适用项+ctest 修复）→ `77d46c2`/`aa946bd`（P1.B/E/I/K）→ `9b91a0a`（P1.J + P1.C 平移）→ `9b6fb82`（P1.E 披露）→ `63bf0ad`（重做被回退的 R-P0.4/5 + splitObject double-free 修复）→ `9fb412d`（审计锚点随覆盖保存语义更新）→ `9ba58f4`（G-02/G-05/G-09）。

**权威验证（scripts/auto_verify_with_vcvars.ps1，EXIT=0）**：12 套件 **554 测试 0 失败**——PrepareSceneData 23、PartPlate 60、AppTool 10、ObjectPicking 7、ViewModelSmoke 176、QmlUiAudit 171、GizmoGeometry 24、NavigatorCube 8、CameraParity 14、ViewportContext 6、PreviewParser 26、**E2EWorkflow 29**（导入→切片→预览→导出→项目存取真实端到端）。CliTests 23/23 与 InventoryAuditTests（路径修复后重建）经脚本门禁覆盖。smoke_test.ps1 启动存活 53 项通过；11 个失败全部为**脚本陈旧期望/环境项**（检查已更名的 GLViewportRenderer/SettingsPage 等旧路径、代理环境变量重复键致脚本自身异常），登记 G-12；75 条 QML 运行时警告为既有绑定环（SettingsDialog/PreparePage 等，非本轮引入），并入 G-12 观察项。

**复核（两个独立子代理）结论**：本轮修复域与上游语义一致；复核发现的 splitObject double-free（ModelObject 析构私有、产物归克隆 tempModel 所有）已修复并有编译错误（C2248）佐证；R-P0.5 的评审定性修正（锁定快照无 ModelVolume::visible）经上游 grep 复核成立。

**剩余对齐差距（除已知阻塞外；第二轮已关闭 G-01/G-06/G-08/G-10/G-11 及 G-02/G-05/G-09/G-13(部分)，余项见下）**：
- [x] G-01 覆盖其他用户预设 + 文件名校验（2026-09-05：2684483 + 2fc616d；isUserPreset 门控、replace 警示、非法字符与 Default 保留字拒绝）；Save-to-Project/Detach 余项并入 G-13。
- [x] G-03 打印对话框按需切片（2026-09-05，commit 2b3c728：EditorViewModel::printSliceReady 信号 + PrintDialog 切片中状态与自动续接；无结果时点打印即自动切片）。
- [x] G-04 3MF 产物缩略图族与 SaveStrategy 语义（Plater.cpp:12037-12118）——2026-09-05 收口（commit 9aa1f94；含 picking 变体 926f70c，见 §15.7 与 §15.6 G-04 条，canonical ID RES-G04-THUMBNAIL-FAMILY）。
- [x] G-06 盘快照 v2 稳定 ObjectID（2026-09-05，commit a68f262/c65479d：kPlateSnapshotVersion=2，按 ID 对账 + v1 按名回退；回归测试 deletePlateUndoReconcilesDuplicateNamesByStableId）。
- [x] G-07(危害子项) instanceCountDialog 打开时刷新，消除静默写错实例数（commit 3e9e411）。
- [x] G-07 余项全部完成（2026-09-05，commit f781cf3）——PrepareContextMenus flush 勾选/printable/plate lock/plate printable 文案改 menuRefreshTick + onAboutToShow 每次弹出重读；LeftSidebar 兼容红点挂 configVm stateChanged tick；PreviewLayerRail 滑条改 pressed 守卫的命令式同步（修复首次拖拽即断绑定）；ErrorToast 提示计数挂 dailyTipChanged。
- [x] 环境事件：E 盘满（复现两轮；E 盘 345GB 接近满载为系统级约束）——持久缓解：OWzxSlicer 链接改 /DEBUG:FASTLINK（commit 9ba8e95，PDB 1.1GB→百MB 级，可一词回退）+ 门禁脚本 PDB 清理（既有）。诊断日志常驻（store_bbs_3mf 失败自动打印可观察状态）。
- [x] G-08 死代码处置完成（commit 95b4bc5，审计测试同步修剪）。
- [x] G-10 G02/G03、G00/G01 前导零识别（2026-09-05，commit 8b87060；PreviewViewModel 解析分发扩展，超过上游 no-op 行为的兼容性增强）。
- [x] G-11 loadFile 工作线程 pending 直写改为本地容器+队列投递；captureFullObjectSnapshot 临时文件改 QTemporaryDir（2026-09-05，commit 8b87060）。
- [x] G-12 完成（2026-09-05，commit 100b685）：smoke_test.ps1 陈旧期望全部更新为现役文件名（RhiViewportRenderer/SoftwareViewport/RhiViewport objs、SettingsDialog/AssemblePage/PreviewLayerRail/LeftSidebar/DockableSidebar），代理环境大小写重复键去重——实测 61/61（此前 53/11）。[x] G-12(两环修复) SettingsDialog TabBar width 环（改引用外层 strip）与 PreparePage 两处 CxCheckBox onCheckedChanged 写回环（改 onToggled）已修复（commit 849cfce），QmlUiAudit 回归全绿。[ ] G-12(剩余绑定环) 其余绑定环按同模式逐站点治理（onCheckedChanged→onToggled / strip 引用），单列长期项。
- [x] G-13(部分) 文件名非法字符与 Default 保留名校验（commit 2fc616d）。[x] G-13(余项·Save-to-Project) 完成（2026-09-05，commit 71eba74/b545ad6）：保存工程时 BackendContext 推送三 tier 选中预设（setProjectEmbeddedPresets，PSET2-06 同款推送模式），storeProject3mf 组装 Slic3r::Preset（DynamicPrintConfig 反序列化 + *_settings_id 强制=预设名）交 store_bbs_3mf 写入 Metadata/{process,filament,machine}_settings_1.config（上游 Plater.cpp:12125-12143 同构）；加载时 mz 直读回（含 filament_settings_id 数组形态兼容），经 load 投递链由 BackendContext adopt 进预设服务（内存态、用户库优先）+ 列表刷新。回环测试 projectEmbedsAndReloadsSelectedPresets（隔离 QTemporaryDir）。
- [x] store_bbs_3mf 偶发 .tmp rename 失败（VM 套件 projectBackupWritesSnapshotFile 间歇挂）：两处写盘加指数退避重试（0/200/800ms）+ 失败原因诊断日志（2026-09-05，commit de49114 后续）；VM 套件 5 次运行 4 绿 1 挂且失败测试各不相同、单独运行恒绿，判定为环境级文件翻转干扰而非代码缺陷，诊断日志已备后续根因定位。
- [x] 最终门禁（commit 9ba58f4..de49114 全链）：EXIT=0，12 套件 554 测试 0 失败（ViewModelSmoke 176 / QmlUiAudit 171 / E2EWorkflow 29 / PartPlate 60 / PrepareSceneData 23 / PreviewParser 26 / GizmoGeometry 24 / CameraParity 14 / AppTool 10 / NavigatorCube 8 / ViewportContext 6 / ObjectPicking 7）。
- [x] G-04 picking 渲染变体完成（2026-09-05，commit 926f70c）：uploadModelBuffer 构建 pick 顶点缓冲（soup 同位、逐 ModelBatch 调色板平色烘焙进顶点色，sourceObjectIndex 哈希色对齐上游对象 id 编码），renderThumbnailPass 按 variant 渲染（复用无光照管线零新增 GPU 状态），variant 贯通 request→synchronize→deliver（thumbnailCapturedForPlate 三参），PreparePage 级联（主图完成→同盘 picking 捕获→setPlatePickThumbnailFromBase64 写 pickThumbnail 缓存→storeProject3mf pick 族写盘）。
- [x] G-12 两环修复（commit 849cfce）：SettingsDialog TabButton width 引用外层 strip（断 TabBar implicitWidth 聚合环）；PreparePage 镂空/保留两侧 CxCheckBox 改 onToggled（断 VM stateChanged 写回环）。剩余绑定环按同模式逐站点治理。
- [x] E2E 时序硬化（commit cfc399f 后续）：SliceService::exportBusy + EditorViewModel 转发，test_local_import_slice_preview_export_workflow 前置 QTRY 等待（"already in progress" 竞态根因是前一个导出 worker 未清标志）；E2EWorkflowTests 29/29 三轮全绿。
- [x] 最终门禁（HEAD 全部 34 提交后）：12 套件 558 测试 0 失败（PrepareSceneData 23 / PartPlate 60 / AppTool 10 / ObjectPicking 7 / ViewModelSmoke 180 / QmlUiAudit 171 / GizmoGeometry 24 / NavigatorCube 8 / CameraParity 14 / ViewportContext 6 / PreviewParser 26 / E2EWorkflow 29），其中含 G-06 重名撤销、G-13 adopt/嵌入回环、G-04 缩略图族三个新回归测试。
- [x] 构建提速（2026-09-05，commit 37d5f69）：/CGTHREADS=<cores> 全局链接选项（tbb /GL 强制 LTCG 单线程 codegen → 16 核并行）；OWZX_ENABLE_PDB=OFF（/DEBUG:FASTLINK 在 LTCG 下被忽略，1.1GB PDB 不再写盘）；脚本 configure 快路径（build.ninja 新于全部 CMake 输入时跳过）。全量门禁实测 906s（此前 25-50 分钟）。clcache 4.2.0 已安装未接线（缓存需 ~3GB 磁盘余量，磁盘扩容后经 cl.exe shim + CLCACHE_CL 接入）。
- 外部阻塞项维持豁免不变：OpenVDB、FFmpeg/WebRTC、Paho MQTT 真机、bambu_networking（MALL-01）、cr_tpms、CGAL 5.4、GCodeProcessor conflict 通道。
