# CrealityPrint Qt6 — 源码对照迁移任务追踪

> 更新日期：2026-03-14
> 新口径：以上游 `third_party/CrealityPrint` 源码为功能真值；当前 Qt6 工程是承接层，不再使用“阶段骨架已完成 = 全功能完成”的记账方式。
> 本文用于追踪“功能承接度”，不是追踪“文件是否创建过”。

---

## 0. 当前结论

- 当前工程已经具备可运行的 Qt6/QML 主壳、基础组件、3D 视口桥接、3MF 导入、切片链路、G-code 预览链路的基础承接能力。
- Monitor / Device / Camera / Cloud / Model Mall / MultiMachine 模块已从骨架升级为具备 Mock 交互能力的功能页面。
- 但距离”基于 CrealityPrint 7.x 开源源码完整还原功能”仍有明显差距，主要差距在：
  - Prepare 工作区的对象树、平板、Gizmo、工具架、业务动作仍未完整对齐上游
  - Preview 页面虽然已打通链路，但交互、统计、告警与细节行为还未完成对照验收
  - Settings / Preset 体系目前仍偏向动态 Mock 模型，尚未完整承接上游 `Tab`、`PresetBundle`、多作用域覆盖
  - 所有模块仍处于 Mock 模式，缺乏真实上游库集成（libslic3r、bambu_networking、FFmpeg、MQTT 等）
- 因此，旧文档中的”70/70 全部完成”不再作为真实进度口径。

---

## 1. 进度记账规则

- `[x]`：已与上游功能真值完成对照，具备可用能力，并通过回归或端到端验证。
- `[-]`：已完成基础承接或局部闭环，但尚未完成上游对照和完整验收。
- `[ ]`：尚未开始，或仅有占位 UI / Mock 能力。

---

## 2. 已完成的基础承接

以下事项已经完成，且可作为后续全功能迁移的基础：

- [x] CMake + Qt6 + QML 主入口与构建链路稳定可用
- [x] `BackendContext`、基础 ViewModel 注入、页面导航框架已建立
- [x] 共享设计令牌与基础控件体系已建立：`Theme`、`CxButton`、`CxComboBox`、`CxSlider`、`CxIconButton`、`CxPillAction`、`CxPanel` 等
- [x] `GLViewport`、`GLViewportRenderer`、`CameraController` 3D 视口桥接骨架可运行
- [x] `ProjectService` 已打通 3MF / BBS 3MF 导入主链路
- [x] `SliceService` 已打通真实切片与 G-code 导出主链路
- [x] `GCodeRenderer` 已具备真实 G-code 预览渲染主链路
- [x] `PreviewViewModel` 已可消费切片结果并驱动预览页面基础数据
- [x] 程序当前可构建并成功启动，`scripts/auto_verify_with_vcvars.ps1` 仍是权威完整验证路径

---

## 3. 真实功能迁移状态

### P0 — 真值基线与迁移框架

- [x] P0.1 锁定上游功能真值版本（tag / commit）2026-03-10
  - 基线文档：[docs/SOURCE_TRUTH_BASELINE.md](SOURCE_TRUTH_BASELINE.md)
  - 锁定结果：`third_party/CrealityPrint` `v7.0.1` / `0d4ac73a6f3224a2bf753d7b9e67d7d515bc8557`
- [x] P0.2 建立“上游模块 -> Qt6 模块 -> 当前状态 -> 验收方式”功能矩阵 2026-03-10
  - 功能矩阵文档：[docs/FEATURE_MATRIX.md](FEATURE_MATRIX.md)
  - 说明：已建立统一执行入口，后续按矩阵继续细化子任务和验收项
- [x] P0.3 梳理闭源依赖、动态库、云服务与协议边界 2026-03-10
  - 审计文档：[docs/DEPENDENCY_BOUNDARY_AUDIT.md](DEPENDENCY_BOUNDARY_AUDIT.md)
  - 说明：已识别 `bambu_networking`、WebView2、MQTT/SSDP/HTTP、登录/商城、媒体播放等关键边界
- [x] P0.4 将任务追踪切换为“功能承接度”口径

### P1 — 主框架与全局工作流

- [-] P1.1 顶栏导航、页面切换、文件入口、最近文件基础闭环已存在；已新增项目保存功能：ProjectServiceMock 新增 `saveProject(filePath)` 方法（Mock 模式保存为 JSON，包含项目名/源路径/平板配置/对象元数据/per-object 参数覆盖），BackendContext::topbarSaveProject()/topbarSaveProjectAs() 现在调用 ProjectServiceMock::saveProject() 实际写入磁盘并显示保存成功/失败通知（ErrorToast）；已新增项目加载功能：ProjectServiceMock 新增 `loadProject(filePath)` 从 JSON 恢复全部项目状态（项目名/源路径/平板列表/对象元数据/per-object 参数覆盖），BackendContext::topbarOpenProject() 在 3MF 加载失败时自动尝试 JSON 加载，EditorViewModel 新增 `refreshAfterLoad()`；已新增新建项目确认对话框（对齐上游清空确认），防止误操作丢失未保存更改；文件对话框已支持 `.json` 格式；已新增全局通知系统增强：BackendContext 新增 `postNotification(message, title, severity)` 方法支持带标题的分级通知（severity 0=info/1=warning/2=error），新增 `lastErrorTitle` Q_PROPERTY；已新增通知队列系统（FIFO）：BackendContext 新增 `QQueue<NotificationEntry>` 和 `dismissNotification()`/`pendingNotificationCount` 方法，`postNotification()` 在当前通知正在显示时自动入队，`dismissNotification()` 关闭当前通知并自动显示队列中下一条；ErrorToast 更新为使用 `dismissNotification()` 替代 `clearError()`，新增待处理通知计数徽章（9+ 封顶）；ErrorBanner 同步更新为显示待处理计数和使用 `dismissNotification()`；已新增持久通知系统（对齐上游 `notification_manager`）：NotificationEntry 扩展为包含 persistent/progressValue/progressMin/progressMax/hasProgress/requiresConfirm/confirmAction 字段，BackendContext 新增 `m_currentNotification` 追踪当前通知状态、`currentNotificationProgress`/`currentNotificationHasProgress`/`currentNotificationPersistent` Q_PROPERTY、`updateNotificationProgress(int)`/`confirmCurrentNotification()`/`cancelCurrentNotification()` Q_INVOKABLE；ErrorToast 新增进度条可视化（动画宽度跟随进度值，200ms 过渡）、确认/取消按钮行（仅持久且非进度通知时显示，调用 confirm/cancel）；持久通知不自动关闭；但仍缺上游完整通知管理器 UI（通知历史列表、通知偏好设置等）2026-03-14
- [-] P1.2 已新增 Edit 菜单（全选/取消选择/删除选中/清空全部）；已新增 View 菜单（适应视图、相机预设子菜单：俯视/前视/右视/等轴视、线框模式切换）；已新增 Help 菜单（快捷键一览对话框显示 16 个快捷键映射、关于 Creality Print 对话框显示版本和上游基线信息）；原有 File 菜单包含新建/打开/导入/保存/另存为/最近文件/退出，工具栏含保存/撤销/重做按钮和页面 Tab 切换；线框模式已完整实现；已补全 Edit 菜单上游完整项：撤销/重做/剪切(Ctrl+X)/复制(Ctrl+C)/粘贴(Ctrl+V)/删除/清空/克隆(Ctrl+D)/全选/取消选择，EditorViewModel 新增 `copySelectedObjects()`/`pasteObjects()`/`cutSelectedObjects()` 剪贴板操作和 `hasClipboardContent` 属性，ProjectServiceMock 新增 `addObject(name)` 方法；已新增 Settings 菜单（偏好设置 Ctrl+P/参数设置）并在标题栏添加设置菜单按钮；View 菜单已添加快捷键（Ctrl+0/1/3/6）；Help 菜单已添加快捷键提示（?）；快捷键对话框已扩展到 24 个映射（新增 PageUp/Down 层范围±1层、Shift+PageUp/Down 层范围±10层、Ctrl+0/1/3/6 预设视角）；已新增 View 菜单"显示标签"子菜单（对齐上游 MainFrame View → Show），支持逐个切换工作流标签页的显示/隐藏（在线模型/准备/预览/设备），通过 `hiddenTabs` 字典和 `toggleTabVisibility()` 函数控制，`buildWorkflowTabs()` 过滤隐藏标签；已新增全局通知管理器（对齐上游 `notification_manager`）：顶栏新增 Bell 图标（带未读红点徽章），点击弹出通知中心面板（NotificationCenter.qml）显示通知历史列表（最近 100 条，含时间戳/标题/消息/严重级别颜色标记），支持标记全部已读和清空历史；通知级别从 3 级扩展到 9 级（对齐上游 NotificationLevel 枚举：Info/Success/Warning/Error/SeriousWarning/Hint/PrintInfo/PrintInfoShort/Progress），ErrorToast 每个级别有独立颜色方案和图标；已新增通知历史追踪：`postNotification()` 和 `postError()` 自动为每个通知记录时间戳，`dismissNotification()` 将通知移入历史队列，新增 `historyCount`/`unreadHistoryCount`/`historyMessage(int)`/`historyTitle(int)`/`historySeverity(int)`/`historyTime(int)` Q_INVOKABLE 和 `clearHistory()`/`markHistoryRead()` 方法；已新增悬浮暂停功能（对齐上游 hover 行为）：ErrorToast 鼠标悬浮时暂停自动消失计时器，离开后 2 秒延迟关闭；已新增通知偏好设置（对齐上游 notification_manager preferences）：新增"启用通知"开关、 "显示提示"开关、 "自动消失时间"选择（3s/5s/8s/10s/15s）在 PreferencesPage 通用分类；通知偏好存储在 BackendContext（`notificationsEnabled`/`hintsEnabled`/`autoDismissSec`），禁用通知时 `postError()`/`postNotification()` 静默跳过；已新增专用通知类型（对齐上游 NotificationType 枚举和 NotificationManager 专用通知）：新增 NotificationType 枚举（16 种类型，对齐上游 CustomNotification/ExportFinished/SlicingProgress/SlicingError/PlaterWarning/ValidateError/DidYouKnowHint 等），新增 NotificationLevel.NotiSlicingProgress（第 10 级）；BackendContext 新增 `postSlicingProgress()`（支持就地更新进度条，不排队替换）、`postSlicingComplete()`（切片完成通知含导出/预览按钮）、`postExportFinished()`、`postExportOngoing()`、`postPlaterWarning/Error()`、`postValidateWarning/Error()`、`postArrangeOngoing()` 等 10 个专用通知方法；SliceService 信号已连接到专用通知（progressUpdated → postSlicingProgress，sliceFinished → postSlicingComplete，sliceFailed → postError）；NotificationEntry 扩展 type/showExportButton/showPreviewButton/hintHasNext/hintHasPrev 字段；已新增 HintDatabase（对齐上游 HintDatabase/HintNotification）：内置 12 条 "Did you know" 提示（层高/填充/支撑/速度/裙边/温度/回退/壁/自适应层高/多平板/预览/可变层高），每条提示含 id/text/weight/hypertext/documentationLink/callbackType/callbackTarget（对齐上游 HintData 结构）；加权随机选择算法（优先显示未读提示），已显示提示集合追踪防止重复，60 秒自动周期显示（对齐上游 push_delayed_notification_data 间隔）；BackendContext 新增 `postHint()`/`nextHint()`/`prevHint()`/`hintCount()`/`currentHintIndex()`/`currentHintText()`/`currentHintHypertext()`/`currentHintFollowText()` 9 个 Q_INVOKABLE；ErrorToast 重构为支持 4 种通知模式：普通通知、进度通知、提示通知（含 < > 导航箭头 + 计数器 + "不再提示"按钮，对齐上游 HintNotification 渲染）、切片完成通知（含预览/导出/关闭按钮，对齐上游 SlicingProgressNotification 导出/预览链接）；但仍缺上游 HintNotification 文档链接按钮点击跳转、HintData 文件加载（从 JSON/cereal）、以及部分 BBL 扩展通知类型（BBLPlateInfo/BBL3MFInfo/BBLObjectInfo 等） 2026-03-14
- [-] P1.3 需要建立统一的上游 GUI 迁移适配层，禁止页面逻辑直接散落在 QML 内联脚本中；已将 SettingsPage 的 `rebuildFilter()` 内联 JavaScript 过滤逻辑迁移到 `ConfigViewModel::filterOptionIndices()` C++ 方法，QML 端简化为 1 行调用；已完成 PrintSettings.qml 全部 5 个内联函数迁移到 C++：`rebuildFilter()` → `filterOptionIndices("", searchText)`、`materialPresetAt()` → `materialPresetName()`（同时修复"耗材丝"→"耗材"分类名 bug）、`groupedIndices()` → `filterIndicesByCategory()`、`enumOptionLabels()` → `ConfigOptionModel::optEnumLabelsList()`、`oUnit` regex → `ConfigOptionModel::optUnit()`（基于 key 映射替代 label regex）；QML 中仅保留 `scopeTitle()` 和 `scopeSubtitle()` 两个纯展示文本函数；已完成 main.qml `currentProjectTitle()` 迁移到 `BackendContext::displayProjectTitle`（C++ Q_PROPERTY，路径分割逻辑从 JS `split(/[\\/]/)` 改为 `QFileInfo::fileName()`）；已审计全部 QML 页面内联函数：PreparePage 全部为 UI 适配逻辑（委托给 ViewModel/Viewport 方法），PrintSettings/SettingsPage 的 `rebuildFilter()` 已是 1 行 C++ 代理，main.qml `buildWorkflowTabs()` 是静态数据；但仍需持续监控新增 QML 页面是否引入业务逻辑 2026-03-14

### P2 — Prepare 工作区

- [-] P2.1 已具备模型导入、视口显示、基础对象列表、平板切换、切片入口；已新增平板管理操作（添加/删除平板）：对齐上游 `PartPlateList`，ProjectServiceMock 新增 `addPlate()`/`deletePlate(int)`，EditorViewModel 新增对应桥接方法，PreparePage 左侧面板新增"添加平板"和"删除平板"按钮（至少保留 1 个平板），删除当前平板时自动切换到有效平板索引；已新增平板右键上下文菜单对齐上游 `GUI_Factories::create_plate_menu()`：选择全部对象、清空平板、重命名（Dialog 弹窗）、锁定/解锁、删除平板；ProjectServiceMock 新增 `renamePlate()`、`removeAllOnPlate()`、`isPlateLocked()`/`setPlateLocked()`、`plateObjectIndices(int)`；EditorViewModel 新增对应桥接方法；平板卡片显示锁定图标；已新增平板缩略图预览（对齐上游 `PartPlate::thumbnail_data`）：平板卡片左侧新增 32x32 彩色缩略图区域，每个平板使用独特颜色区分，中心显示对象数量；EditorViewModel 新增 `plateThumbnailColor(int)` Q_INVOKABLE；已新增对象跨平板拖拽（对齐上游 `Plater::priv::on_arrange` 跨平板拖拽）：ObjectList 对象项支持 Drag.source（key="object-drag"），平板卡片 DropArea 接收拖拽并调用 `moveSelectedObjectToPlate()`；EditorViewModel 新增 `moveSelectedObjectToPlate(int targetPlateIndex)` Q_INVOKABLE；ProjectServiceMock 新增 `setObjectPlateForIndex(int, int)` 方法从源平板移除并添加到目标平板；已新增平板设置对话框（对齐上游 `PlateSettingsDialog`）：平板右键菜单新增"平板设置"项，Dialog 包含热床类型选择（PEI/EP/PC/纹理PEI/自定义，对齐上游 BedType 枚举）、打印顺序选择（按层/按对象，对齐上游 PrintSequence）、螺旋花瓶模式（跟随全局/开启/关闭，对齐上游 spiral_vase），ProjectServiceMock 新增 `plateBedTypes_`/`platePrintSequences_`/`plateSpiralModes_` 存储和对应 getter/setter，EditorViewModel 新增 6 个桥接 Q_INVOKABLE；但仍未完成上游完整 Plater 工作区对照（缺真实 3D 缩略图渲染、缺首层/其他层耗材顺序高级配置） 2026-03-14
- [-] P2.2 已完成准备页顶部工具条和左右侧栏的第一轮视觉重构；已新增底部状态指示器显示选中对象数量和当前状态文本；已新增 ProcessBar 分类标签栏（对齐上游 `ProcessBar::GLToolbar`）；已新增 CxIconButton 工具栏动画反馈（对齐上游 GLToolbar 按钮状态机）；已新增对象列表拖拽重排序（对齐上游 `GUI_ObjectList` OnBeginDrag/OnDrop）：ObjectList ListView 新增 drag reorder 支持，对象行通过 Drag.keys="object-reorder" 启动拖拽，目标行 DropArea 接收并调用 `EditorViewModel::moveObject()`，ProjectServiceMock::moveObject() 交换所有平行数组（names/moduleNames/printableStates/visibleStates/transforms/overrides）并更新 plateObjectIndices_ 引用；已新增对象行内重命名（对齐上游 `GUI_ObjectList::rename_item`）：对象名称 Text 支持双击切换为 TextField 行内编辑（Enter/失焦确认），右键菜单新增"重命名"项；已增强拖拽排序视觉反馈（对齐上游 GUI_ObjectList 拖拽放置位置指示）：拖拽源行自动降低不透明度至 40%（opacity 状态机）；DropArea 新增 `onEntered`/`onExited` 追踪悬浮状态，目标行高亮背景色（8% accent 透明度）+ 顶部 2px accent 插入指示线；拖拽结束时自动清除所有视觉状态；已增强跨平板拖拽放置视觉反馈（对齐上游 GUI_ObjectList 拖拽放置指示）：平板卡片 delegate 新增 `dragHover` 状态属性，拖拽悬浮时显示 2px accent 边框高亮 + 半透明叠加层 + 居中 "+" 图标（150ms 淡入动画），DropArea 改用 `parent.dragHover` 属性驱动视觉状态（替代原先直接修改 `parent.color` 的脆弱方式），onDropped 正确清除 dragHover 状态；但仍缺上游 Plater 完整视觉细节对齐（缺真实 3D 缩略图渲染等） 2026-03-14
- [-] P2.3 左侧对象工作区已补入 plate 卡片切换、对象所属 plate 信息、基础右键动作，以及基于 ViewModel 的多选与批量操作；对象列表中的状态切换已从本地假显隐改为承接上游 `printable` 语义，并已支持按平板 / 按模块两种组织方式、分组头展开/收起，以及对象节点下的 volume 子层级展示；当前还新增了 volume 子项的同对象内多选、高亮、批量删除，并让删除快捷键与顶部工具栏删除动作跟随当前选中节点，同时加入了基于上游规则的基础删除约束；此外已补入与上游 `Edit in Parameter Table` 对应的单对象 / 单部件参数表入口，并让右侧打印参数面板开始对部分上游真实 key 承接对象 / 部件 scope 的读取与写回；本轮进一步把 `initial_layer_print_height`、`infill_wall_overlap`、`chamber_temperature` 等真实 key 纳入 Qt6 参数表承接；已新增 ObjectManipulation 面板（对齐上游 `ObjectManipulation`）：Sidebar 对象 Tab 顶部新增变换面板，单对象选中时显示位置 X/Y/Z（mm）、旋转 X/Y/Z（°）、缩放 X/Y/Z（%）三组数值输入框，RGB 分色标注（红X/绿Y/蓝Z），统一缩放锁定按钮、重置变换按钮、底部尺寸信息显示（W/D/H 只读），ProjectServiceMock 新增 per-object transform 存储（`objectPositions_`/`objectRotations_`/`objectRotations_`/`objectScales_`），EditorViewModel 新增 9 个 Q_PROPERTY（objectPosX/Y/Z、objectRotX/Y/Z、objectScaleX/Y/Z）+ `uniformScale`/`hasObjectManipSelection`/`resetObjectTransform()`/`applyScaleFactor()`，项目保存/加载已序列化 transform 数据；已补全对象右键上下文菜单（对齐上游 `create_object_menu`/`create_extra_object_menu`）：新增重命名（行内编辑 + 右键菜单项 + ProjectServiceMock::renameObject）、复制（copySelectedObjects）、剪切（cutSelectedObjects）、克隆（duplicateSelectedObjects）、居中到热床（centerSelectedObjects）、铺满热床副本（fillBedWithCopies，Mock 模式 3x3 网格）、导出为 STL（exportSelectedAsStl，Mock 占位）、设为可打印/不参与打印切换（复用已有 setSelectedObjectsPrintable）；已新增 Add Volume 子菜单（对齐上游 `GUI_Factories::append_menu_items_add_volume` 和 `ADD_VOLUME_MENU_ITEMS`）：ObjectList.qml 右键菜单新增"添加部件"子菜单，包含 5 个菜单项（添加部件/添加负体积/添加修改器/添加支撑屏蔽/添加支撑增强），仅单对象选中且无部件选中时启用；ProjectServiceMock 新增 `MockVolumeType` 枚举（ModelPart=0/NegativeVolume=1/ParameterModifier=2/SupportBlocker=3/SupportEnforcer=4，对齐上游 `ModelVolumeType`）和 `MockVolumeEntry` 结构体；新增 `m_mockVolumes` 哈希表存储 Mock 模式下 per-object volume 数据；新增 `addVolume(int objectIndex, int volumeType)` 方法，自动生成部件名称（按类型计数）；Mock 模式下 `objectVolumeCount`/`objectVolumeName`/`objectVolumeTypeLabel` 已从返回空值更新为从 `m_mockVolumes` 读取；`deleteObjectVolume` Mock 模式已实现（含最后一个实体部件保护约束，对齐上游 `solidPartCount <= 1` 检查）；删除对象时自动清理关联 volume 数据；EditorViewModel 新增 `addVolumeToObject(int volumeType)` 桥接方法，添加后自动展开对象节点并选中新部件；已新增 volume 类型转换（对齐上游 `GUI_ObjectList::load_generic_subobject` 类型转换和 `ModelVolume::type_to_string`/`type_from_string`）：ProjectServiceMock 新增 `changeVolumeType(int objectIndex, int volumeIndex, int newVolumeType)` 方法，转换时保留编号更新类型名前缀；EditorViewModel 新增 `changeVolumeType(int newVolumeType)` 桥接方法（需单部件选中）；ObjectList.qml 右键菜单新增"转换为"子菜单（部件/负体积/修改器/支撑屏蔽/支撑增强，仅部件选中时显示，对齐上游 change_type 菜单）；上游确认无内置原始体创建功能（立方体/球体等通过外部 mesh 加载），volume 类型 5 种已完整对齐上游 ModelVolumeType 枚举；但仍缺 volume 从外部文件导入、support painting / seam painting gizmo 上下文、以及 Text/SVG emboss volume 类型（对齐上游 GLGizmoText/GLGizmoSVG） 2026-03-14
- [-] P2.4 右侧工作区已开始承接真实切片结果摘要；已新增 per-plate 切片状态追踪（`m_slicedPlateIndices`），切片完成后自动标记对应 plate，对象变更时清除当前 plate 标记，清空工作区时全部重置；`EditorViewModel::isPlateSliced(int)` 提供给 QML 查询；平板卡片显示绿色圆点标记已切片的 plate；已新增 SliceInfoPanel 对齐的切片结果摘要面板：SliceProgress.qml 重构为使用 Theme 设计令牌，新增切片结果分区标题、分隔线、耗材用量（长度）、预估成本（$xx.xx）等字段显示；SliceService 新增 `resultFilamentLabel()`/`resultCostLabel()` 存储切片结果；EditorViewModel 新增 `sliceResultFilament`/`sliceResultCost` 桥接属性；Mock 模式新增模拟切片流程（5 步进度 + 400ms 延迟 + 基于对象数量生成模拟结果），替代原先直接失败的错误提示；已新增切片后操作栏（对齐上游 SliceInfoPanel 后续操作）：切片完成后显示"预览"、"导出 G-code"和"全部切片"三个操作按钮，预览按钮调用 `EditorViewModel::switchToPreview()` 发射 `previewRequested()` 信号，PreparePage 通过 Connections 转发到 `backend.setCurrentPage(2)` 切换到 Preview 页面（对齐上游 `Plater::priv::on_preview`）；耗材重量显示旁已新增耗材颜色标记小方块（对齐上游 SliceInfoPopup filament color badge）；已新增切片层数显示（对齐上游 SliceInfoPanel）：EditorViewModel 新增 `sliceResultLayerCount` Q_PROPERTY（从 SliceService::resultLayerCount() 桥接），SliceProgress.qml 切片结果摘要新增"切片层数"行（显示 N 层，仅切片完成后可见）；已新增模型尺寸信息显示（对齐上游 SliceInfoPanel model dimensions）：EditorViewModel 新增 `modelSizeText` Q_PROPERTY（从 measureDimensions 桥接，格式 "W × D × H mm"），SliceProgress.qml 切片结果摘要新增"模型尺寸"行（等宽字体显示，有选中对象时可见）；已新增右侧面板三段式可折叠结构（对齐上游 `Plater::Sidebar` Printer/Material/ParamsPanel 三段式布局）：新建 `CollapsibleSection.qml` 可复用组件（标题栏点击切换展开/折叠，高度动画200ms OutCubic + 透明度渐变 + 箭头旋转指示器），`PrintSettings.qml` 从扁平堆叠重构为 3 个 `CollapsibleSection`：Section 1 打印机（打印机预设选择 + CRUD + 热床类型选择器，对齐上游 Sidebar Printer Section），Section 2 耗材（耗材槽位网格 + 兼容性警告指示器 + 自动匹配按钮，对齐上游 Sidebar FilamentSection），Section 3 打印设置（作用域选择器 + 打印/耗材预设行 + 分类选项列表，对齐上游 ParamsPanel）；但仍缺上游 SliceInfoPanel 完整交互（真实切片结果与面板联动）2026-03-14
- [-] P2.5 已实现 Move/Rotate/Scale 三模式 Gizmo 切换系统和线框模式：GLViewport 新增 gizmoMode 属性支持 Move/Rotate/Scale 三种模式切换，GLViewportRenderer 新增旋转环面几何体（torus rings）和缩放立方体手柄几何体，Mesh shader 升级为 mat4 模型矩阵支持完整的平移+旋转+缩放变换，对象拾取更新为变换后 AABB 检测，undo/redo 支持完整 TransformCmd 记录，PreparePage 工具栏新增移动/旋转/缩放模式按钮；线框模式通过"更多"菜单切换，使用 glPolygonMode + DARK_GRAY 颜色匹配上游 Plater::toggle_show_wireframe；已新增 Measure Gizmo 基础实现（对齐上游 `GLGizmoMeasure`）：GLViewport 新增 `GizmoMeasure=3` 模式，EditorViewModel 新增 `measureDimensions` Q_PROPERTY（dx/dy/dz/体积），`refreshMeshCacheAndFitHint()` 中从 TLV 边界框数据计算尺寸，PreparePage 工具栏新增测量按钮、Ctrl+U 快捷键、视口上方尺寸信息面板（X/Y/Z 维度 + 体积，等宽字体，RGB 分色标注）；已新增自动朝向（对齐上游 `Plater::orient()` / `AutoOrienter`）：EditorViewModel 新增 `autoOrientSelected()` 方法，Mock 模式使用异步模拟，PreparePage 工具栏和对象右键菜单均添加"自动朝向"入口；已新增 Flatten Gizmo（对齐上游 `GLGizmoFlatten`）：GLViewport 新增 `GizmoFlatten=4` 模式，EditorViewModel 新增 `flattenSelected()` 方法（Mock 模式模拟凸包面检测，6 个候选面，将选中对象最大面朝下平放）、`flattenFaceCount` Q_PROPERTY，PreparePage 工具栏新增平放按钮（G 快捷键）、视口上方 Flatten 设置面板（候选面计数 + 平放按钮 + 提示文本）；已新增 Cut Gizmo（对齐上游 `GLGizmoCut`）：GLViewport 新增 `GizmoCut=5` 模式，EditorViewModel 新增 `cutSelected()` 方法（Mock 模式将对象切割为 2 个部分）、`cutAxis`/`cutPosition`/`cutKeepMode` Q_PROPERTY + `flipCutPlane()`/`centerCutPlane()` Q_INVOKABLE，PreparePage 工具栏新增切割按钮（Ctrl+Shift+X 快捷键）、视口上方 Cut 控制面板（X/Y/Z 轴选择 pill、位置滑块 -50~50mm、保留模式全部/上半/下半 pill、翻转/居中/执行切割按钮）；新增 scissors.svg 和 layers-subtract.svg 图标资源；但仍未完成上游完整 Hollow Gizmo（SLA 专用，优先级低）、高级 Measure 面拾取、Cut 连接器（Dowel/Snap/Hole）、裁切舌槽模式和视口告警显示 2026-03-14
- [-] P2.6 已新增上游对齐的键盘快捷键（W/E/R 切换 Gizmo 模式、G 平放、Ctrl+Shift+X 切割、Ctrl+Z/Ctrl+Y undo/redo、Ctrl+D 复制选中、Delete 删除选中、Escape 取消选择、Ctrl+A 全选、F 适应视图）和对象右键上下文菜单（复制选中、删除、全选、取消选择、Gizmo 模式切换、镜像 X/Y/Z 轴、显示/隐藏、适应视图），并通过 QML Shortcut 确保快捷键在焦点管理下可靠触发；已新增 Slice All（切片全部平板）和 Export G-code（导出 G-code 文件对话框）功能；已实现镜像操作、对象克隆/复制、对象显示/隐藏切换；已新增自动排列（arrange）：对齐上游 `Plater::priv::on_arrange`，GLViewport 新增 `ArrangeSelected` InputEvent 类型和 `arrangeSelected()` Q_INVOKABLE，GLViewportRenderer 实现基于 AABB 的网格排列算法（自动计算对象边界框、网格列数、间距 6mm、支持 undo/redo），PreparePage 工具栏布局按钮已绑定 arrangeSelected；已新增剪贴板操作：EditorViewModel 新增 `copySelectedObjects()`/`pasteObjects()`/`cutSelectedObjects()` 和 `hasClipboardContent` 属性，ProjectServiceMock 新增 `addObject(name)` 方法（支持 HAS_LIBSLIC3R 真实 ModelObject 创建和 Mock 模式元数据添加）；已新增拆分对象（对齐上游 `Plater::priv::split_object` / `ModelObject::split`）：EditorViewModel 新增 `splitSelectedObject()` 方法（支持单对象选中限制、Mock 模式模拟拆分为 2 个子对象、HAS_LIBSLIC3R 占位待接真实 mesh 分割），PreparePage 工具栏和对象右键菜单均添加"拆分对象"入口（仅单选时启用）；已新增项目加载（对齐上游 `Plater::load_project`）：ProjectServiceMock 新增 `loadProject(filePath)` 方法，从 JSON 恢复全部项目状态（项目名/源路径/平板列表/对象元数据/per-object 参数覆盖），BackendContext::topbarOpenProject() 在 3MF 加载失败时自动尝试 JSON 加载，EditorViewModel 新增 `refreshAfterLoad()` 刷新 UI 状态，文件对话框已支持 `.json` 格式打开和保存；已补全平板右键上下文菜单（对齐上游 `PartPlate` context menu）：新增"排列对象"和"自动朝向"菜单项，与上游 e_at_lay/e_at_dir 对齐；已新增底部切片状态指示器（对齐上游 `PartPlate::m_slice_result_valid`）：当前平板已切片时显示绿色"✓ 已切片"徽章、切片进行中时显示黄色"切片中 X%"进度徽章；但仍未完成上游完整 Plater 工作流对照：缺 Hollow Gizmo（SLA 专用）、缺平板缩略图预览、缺平板自定义对话框 2026-03-14

### P3 — Preview 工作区

- [-] P3.1 已打通切片结果到预览页面的数据通路；已新增 Preview 页面键盘快捷键（Space 播放/暂停、左/右箭头跳转 100 步、Home/End 跳到头/尾、Page Up/Down 层范围平移 ±1 层、Shift+Page Up/Down ±10 层）和动画状态属性 `isPlaying`；MoveSlider 改为播放/暂停切换按钮；PreviewViewModel 新增 `jumpToLayer(int)`（1-indexed 对齐上游 `IMSlider::do_go_to_layer`）和 `moveLayerRange(int delta)`；已新增 LayerSlider 鼠标滚轮层范围导航（对齐上游 `IMSlider::on_mouse_wheel`，Shift+滚轮 ±10 层）、右键上下文菜单（对齐上游 IMSlider 右键菜单"跳转到层..."）、滑块悬浮提示（显示层号和 Z 高度 mm，对齐上游 IMSlider hover tooltip）；PreviewViewModel 新增 `layerZAt(int)` Q_INVOKABLE 和 `m_layerZs` 向量在 G-code 解析时记录每层 Z 高度；上游 GCodeViewer 视口本身无鼠标拾取/悬浮高亮等高级交互（仅有 Marker 工具位置标记，已在 P3.4 实现）；当前预览交互已基本对齐上游 IMSlider 全部核心功能 2026-03-14
- [-] P3.2 已具备 LayerSlider、MoveSlider、Legend、StatsPanel 等基础组件；已增强 LayerSlider：添加起始层/结束层可编辑输入框（1-indexed 显示对齐上游 `IMSlider::get_label`）、层范围摘要显示（min - max / total）、跳转到层对话框（输入 1-indexed 层号 + Enter 确认 + Escape 取消，对齐上游 `IMSlider::render_go_to_layer_dialog`）、滑块悬浮提示（层号+Z高度 mm）和右键上下文菜单（对齐上游 IMSlider 右键菜单）、鼠标滚轮层范围导航（对齐上游 `IMSlider::on_mouse_wheel`）；已增强 MoveSlider（对齐上游 `IMSlider` 时间轴）：新增 `currentTime` Q_PROPERTY 显示当前移动位置的累计时间、`formatTime()` 辅助函数对齐上游 `short_and_splitted_time` 格式、播放/暂停图标按钮 + 时间轴滑块 + 当前时间 + 总时间 + 移动计数器；已新增 MoveSlider 彩色色带（对齐上游 `IMSlider::draw_colored_band`）：PreviewViewModel 新增 `toolChangePositionCount()`/`toolChangePositionAt(i)`/`toolChangeExtruderIdAt(i)`/`extruderColor(id)` Q_INVOKABLE 和 `m_toolChangePositions` 向量在 G-code 解析时记录 T 指令位置，MoveSlider 在滑块轨道下方用 Repeater 渲染按挤出机 ID 着色的工具切换色带（8 色循环，60% 透明度）；已新增 MoveSlider 鼠标悬浮时间提示（对齐上游 IMSlider hover 时间提示）：MoveSlider 滑块轨道上方新增悬浮提示框（深色背景 + 箭头指示器），当鼠标悬浮在轨道上时显示该位置对应的累计时间（格式化对齐 `formatTime()`），PreviewViewModel 新增 `timeAtMove(int moveIndex)` Q_INVOKABLE 方法从 `m_moveAccumulatedTime` 向量查找任意移动位置的时间值；已实现双拇指层范围拖拽滑块（对齐上游 IMSlider 双拇指交互）：LayerSlider 重构为单轨道双拇指范围滑块，轨道背景 + accent 高亮活跃范围 + 两个可独立拖拽的拇指手柄（14x20px，带边框和 | 指示器），拖拽时手柄高亮为 accent 色，两个拇指约束不交叉（min <= max），鼠标悬浮显示层号和 Z 高度提示（300ms 延迟），起始/结束层输入框紧凑排列在一行（From / To / total），右键上下文菜单保留跳转到层功能；但上游部分高级功能仍未实现（如 IMSlider 颜色条上方层范围高亮等） 2026-03-14
- [-] P3.3 已完成全部 13 种 EViewType 的数据源和颜色映射：FeatureType(固定分类色)/Height(Z渐变)/Width(解析WIDTH注释,渐变)/Tool(挤出机色板)/Feedrate(渐变)/FanSpeed(渐变)/Temperature(渐变)/ColorPrint(挤出机色板)/FilamentId(挤出机色板)/VolumetricRate(feedrate*width渐变)/LayerTime(TIME_ELAPSED渐变)/LayerTimeLog(log渐变)/Acceleration(M204渐变)；渐变色阶已替换为上游 GCodeViewer 的 10 色 Range_Colors（线性插值，完整匹配上游 bluish→greenish→yellowish→reddish 色阶）；G-code解析器已新增WIDTH注释、TIME_ELAPSED注释、T-指令(工具切换)、M204(加速度)的解析；PackedSegment已扩展width/layer_time/acceleration/extruder_id字段并同步GCodeRenderer二进制布局；recolorAndPackSegments()支持按模式动态着色并重建二进制数据；buildLegendItems()支持分类图例、挤出机图例和渐变范围图例三种图例类型；Legend.qml 使用 Repeater 动态渲染图例项（颜色方块+标签+百分比/值）；13 种视图模式均已通过端到端验证 2026-03-14
- [-] P3.4 已修复 GCodeRenderer 多项运行时问题：补入缺失的 `initializeOpenGLFunctions()` 调用防止 GL 函数未就绪、补入 shader link 错误日志输出、消除 render() 中每帧无条件调用 update() 导致的不必要重绘循环、将顶点过滤从双遍历（先计数再过滤）改为单遍历提升性能；PreviewPage.qml 本身无 QML 绑定警告；已新增 CameraController 集成支持预览视口的轨道/缩放/平移相机控制（左键拖拽轨道、中键拖拽平移、滚轮缩放），并自动计算数据包围盒适配初始视角；已新增相机预设视角按钮（顶/前/右/等轴），CameraController 新增 viewTop/viewFront/viewRight/viewIso 方法，GLViewport 新增 requestViewPreset() 接口和 ViewPreset 事件类型，GLViewportRenderer 和 GCodeRenderer 均已处理 ViewPreset 事件；已新增工具位置提示框（对齐上游 `GCodeViewer::Marker::render` 的 ExtruderPosition 窗口）：PreviewViewModel 新增 14 个工具位置 Q_PROPERTY（X/Y/Z/feedrate/fan/temperature/width/layerTime/acceleration/extruderId/layer/moveIndex/isExtrusion），`updateToolPositionData()` 在 `setCurrentMove()` 和 `rebuildFromGCode()` 中调用；新建 `ToolPositionTooltip.qml` 组件，显示当前位置（等宽字体）、速度、层号，以及视图模式特定字段（风扇/温度/线宽/加速度/挤出机）的条件显示；PreviewPage 在视口左下角叠加提示框；已新增预览页头部摘要信息显示（层数 + 移动步数徽章，对齐上游 GCodeViewer header）；上游 GCodeViewer 视口本身无鼠标拾取点高亮和视口内层范围拖拽功能（确认上游代码），当前实现已完整对齐上游视口交互能力 2026-03-14
- [-] P3.5 已实现视图模式切换时的动态颜色重计算；渐变色阶已替换为上游 GCodeViewer 的 10 色 Range_Colors；统计面板已扩展为显示总时间、层数、总移动数、挤出移动数、空驶移动数、耗材长度和耗材重量；已新增 3 个上游对齐统计项：平均打印速度、工具切换次数、预计成本（耗材重量 × $20/kg 默认单价）；已新增按角色时间分解（对齐上游 `PrintEstimatedStatistics::roles_times`），支持 20 种 ExtrusionRole 中文标签映射；已新增 Normal/Stealth 双模式（对齐上游 `PrintEstimatedStatistics::modes`），Stealth 模式 ×1.4 倍时间；已新增按层时间分布图表（对齐上游 IMSlider `m_layers_times`），绿/黄/红三色柱状图；已新增每挤出机耗材用量分解（对齐上游 `render_all_plates_stats`）：PreviewViewModel 新增 `extruderCount()`/`extruderUsedLength(int)`/`extruderUsedWeight(int)` Q_INVOKABLE 和 `m_extruderUsedLength`/`m_extruderUsedWeight` 在 G-code 解析时按 extruder_id 分类追踪耗材长度，StatsPanel 新增"耗材用量"分区，显示挤出机颜色方块+名称+长度(m)+重量(g)；Preview 工作区统计面板已基本对齐上游 PrintEstimatedStatistics 核心字段 2026-03-14

### P4 — Settings / Preset / 覆盖作用域

- [-] P4.1 `ConfigViewModel`、`ConfigOptionModel`、`PresetListModel`、`SettingsPage` 已有动态配置骨架；SettingsPage 新增"重置默认"按钮调用 `ConfigOptionModel::resetToDefaults()`；已新增搜索结果计数标签（搜索时显示"N 项匹配"），与脏选项计数并列显示；已新增 PreferencesPage 通用设置页（对齐上游 `PreferencesDialog::create_general_page`）：新增"通用"分类（index 0），包含"启动时显示主页"开关（`showHomePage`）、"默认页面"选择（主页/准备，对齐上游 default_page radio）、"单位"选择（公制/英制，对齐上游 units combo）、"用户角色"选择（基础/专业，对齐上游 user_role radio），SettingsViewModel 新增 4 个 Q_PROPERTY（`showHomePage`/`defaultPage`/`units`/`userRole`）；已扩展通用设置页：新增"自动保存"开关 + 间隔选择（5/10/15/30 分钟，对齐上游 auto_save）、"启动时检查更新"开关（对齐上游 preset_update）、"减少动画效果"开关（对齐上游 enable_reduce_motion），SettingsViewModel 新增 `autoSave`/`autoSaveInterval`/`checkUpdates`/`reducedMotion` Q_PROPERTY，`resetPreferences()` 已包含新属性重置；已扩展通用设置页：新增"区域设置"选择（对齐上游 PreferencesDialog region combo），支持 5 个区域（跟随系统/中国/美国/欧洲/日本），SettingsViewModel 新增 `region` Q_PROPERTY；已新增"账号与隐私"分类内容（index 5）：自动备份项目到云端开关（对齐上游 cloud sync backup），SettingsViewModel 新增 `autoBackup` Q_PROPERTY；已新增"高级"分类内容（index 7）：低细节模式（LOD）开关（对齐上游 enable_reduce_detail），SettingsViewModel 新增 `compactMode` Q_PROPERTY；撤销栈上限选择（20/50/100/200，对齐上游 undo/redo 历史限制），SettingsViewModel 新增 `undoLimit` Q_PROPERTY；但仍缺上游完整 Settings 工作流（真实云端同步集成、隐私数据管理、开发者模式高级调试选项等）2026-03-14
- [-] P4.2 `PrintSettings.qml` 已完成一轮参考图导向重构
- [-] P4.3 当前参数体系仍以 Qt 侧模型为主；已新增 `ConfigOptionModel::loadFromUpstreamSchema()` 从上游 `print_config_def` 读取真实配置定义；上游 schema 加载 `kDesiredKeys` 已扩展到约 110 个 key；Mock 模式硬编码选项扩展到约 75 个 key；scopedWritableKeys 扩展到约 60 个 key；新增"加速度"分类；已新增 ConfigOptionModel OptionGroup 层级（对齐上游 `Tab::OptionGroup`）：ConfigOption struct 新增 `group` 字段存储分组标题，ConfigOptionModel 新增 `GroupRole` 和 `optGroup()` 访问器；全部 75 个 Mock 选项已分配约 30 个分组；SettingsPage 参数列表已显示分组标题（深色背景分隔行）；已新增 ConfigOptionModel Tab Page 层级（对齐上游 `Tab::Page`）：ConfigOption struct 新增 `page` 字段存储顶层页面（Quality/Strength/Speed/Support/Base/Cooling/Retraction/Other），ConfigOptionModel 新增 `PageRole`、`optPage()`、`pageNames()`、`filterIndicesByPage()` 方法和 `pageForCategory()` 静态映射函数（中文/英文 category 双向映射），ConfigViewModel 新增 `filterIndicesByPage()` 桥接方法；PrintSettings.qml 选项列表已重构为 Page > Category > Options 三层级 Repeater 结构（对齐上游 Tab::Page > ConfigOptionsGroup > Option），Page header 显示英文页面名（accent 色），Category header 显示中文分类名（深色背景），选项行显示 label + slider/checkbox/combobox 控件；已修复 ConfigOption struct 的 MSVC aggregate init 兼容性问题（添加 10 参数和 11 参数构造函数）；但尚未完整迁移上游 `Tab` 的完整搜索过滤 UI 和 `PresetBundle` 多预设继承链 2026-03-14
- [-] P4.4 已实现对象/部件/平板三级作用域覆盖体系（对齐上游 `Tab` scope 语义和 `ModelObject`/`ModelVolume` config override）：ProjectServiceMock 新增 Mock 模式 per-object/per-volume/per-plate scoped option 存储（`m_mockObjectOverrides`/`m_mockVolumeOverrides`/`m_mockPlateOverrides`），`scopedOptionValue()` 和 `setScopedOptionValue()` 在 Mock 模式下正确读取和写入层叠覆盖（volume > object > global）；新增 `plateScopedOptionValue()`/`setPlateScopedOptionValue()` 方法支持平板级参数覆盖；ConfigViewModel 新增 `activatePlateScope(int plateIndex)` 方法，`activateObjectScope()` 正确区分 "object" 和 "volume" 两种作用域类型（volumeIndex >= 0 时为 volume scope），`buildScopeValues()` 和 `handleOptionValueChanged()` 支持全局/对象/部件/平板四种作用域的正确路由和值合并，`readonlyKeysForCurrentScope()` 对 volume 和 object scope 限制为约 60 个可写 key；PrintSettings.qml 作用域 UI 更新为 3 个 pill 按钮（全局/对象或部件/平板），scopeTitle/scopeSubtitle 支持全部 4 种作用域类型的中文显示；已新增 Layer Range 支持（对齐上游 `ModelObject::layer_config_ranges`）：新增 `MockLayerRange` 结构体（minZ/maxZ/overrides）和 `m_mockLayerRanges` 存储（objectIndex → list of MockLayerRange），ProjectServiceMock 新增 5 个方法（objectLayerRanges/addObjectLayerRange/removeObjectLayerRange/setLayerRangeValue/layerRangeValue），ConfigViewModel 新增 8 个桥接 Q_INVOKABLE（layerRangeCount/layerRangeMinZ/layerRangeMaxZ/addLayerRange/removeLayerRange/setLayerRangeValue/layerRangeValue），PrintSettings.qml 新增 Layer Range 面板（仅在 object/volume scope 下可见），显示已有 range 列表（编号 + Z 范围 + 删除按钮）和添加按钮（默认扩展最后一个 range 5mm），删除对象时自动清理 layer range 数据；但仍未实现上游完整 `PresetBundle` 多预设继承链的完整搜索和层叠覆盖可视化编辑 2026-03-14
- [-] P4.5 搜索功能已在 SettingsPage 和 PrintSettings.qml 实现（`filterOptionIndices()` 支持按 key/label 子串搜索）；已新增配置选项脏状态追踪：ConfigOptionModel 新增 `m_dirtyKeys` 集合追踪已修改选项、`optIsDirty(int)` / `dirtyCount()` / `resetOption(int)` 方法、`DirtyRole` 和 `TooltipRole` 角色；`setValue()` 和 `applyValues()` 自动对比默认值更新脏状态、`resetToDefaults()` 清除所有脏状态；已新增 30 个关键选项的帮助文案 tooltip（中文，对齐上游 `ConfigOptionDef::tooltip`）；SettingsPage 显示脏标记（橙色圆点+加粗标签）、脏计数显示（搜索栏旁）、鼠标悬浮帮助提示（ToolTip）；已新增预设保存/加载和脏检测：PresetServiceMock 新增 `presetValues()`/`savePresetValues()`/`hasPreset()` 方法，支持按预设名存储和读取 key-value 值映射；ConfigViewModel 新增 `saveCurrentPreset()`（保存当前值到预设）、`isPresetDirty`（对比当前值与保存快照的差异）、`setCurrentPreset()` 增强（加载预设值时自动应用并刷新快照），PrintSettings.qml 预设保存按钮已连接 `saveCurrentPreset()`；已新增 3-tier 预设继承链（对齐上游 `PresetBundle`）：PresetServiceMock 新增 `presetNamesForCategory(int category)` 和 `defaultPresetForCategory(int category)` 方法支持 Print/Filament/Printer 三类预设，内置 8 个默认预设（3 打印质量：0.20mm Standard/0.16mm Fine/0.28mm Draft、3 耗材：PLA/ABS/PETG、2 打印机：K1C/Ender-3 S1），每类预设包含该类别的关键参数默认值；ConfigViewModel 新增 `currentPrinterPreset`/`currentFilamentPreset`/`currentPrintPreset` 三组 Q_PROPERTY 和对应的 setter 方法，`mergePresetHierarchy()` 方法实现 printer → filament → print 三级值覆盖合并，`loadDefault()` 初始化三类预设为默认值；PrintSettings.qml 新增 3-tier 预设选择器 UI（3 行 ComboBox，分别显示打印/耗材/打印机预设），替代原先的单行预设选择；已新增自定义预设创建/删除 UI（对齐上游 `PresetBundle::save_current_preset`/`delete_preset`）：PresetServiceMock 新增 `createCustomPreset()`/`deletePreset()`/`isBuiltinPreset()` 方法，动态管理 `m_categoryPresets` 按分类维护预设列表，内置预设名存储在 `m_builtinPresetNames` 集合中不可删除，自定义预设可随时删除并自动从分类列表移除；ConfigViewModel 新增 `createCustomPreset()`/`deletePreset()`/`canDeletePreset()` Q_INVOKABLE 方法；PrintSettings.qml 每个预设 ComboBox 旁新增 "+" 保存按钮（创建自定义预设副本）和 "✕" 删除按钮（仅自定义预设显示）；已新增预设切换自动匹配（对齐上游 `PresetBundle::update_compatible`）：`setCurrentPrinterPreset()` 现在调用 `autoMatchFilament()` 在切换打印机时自动匹配兼容耗材；已新增预设脏状态可视化指示器（对齐上游 Tab dirty mark）：PrintSettings.qml 3-tier 预设行（打印/耗材/打印机）每行标签旁新增橙色圆点指示 `isPresetDirty` 状态，当配置有未保存修改时显示；已新增预设重命名功能（对齐上游 PresetBundle 重命名）：PresetServiceMock 新增 `renamePreset()` 方法（内置预设不可重命名、空名/重复名检查、自动更新分类列表中的名称引用），ConfigViewModel 新增 `renamePreset()` 桥接方法（自动更新当前活跃预设名引用），PrintSettings.qml 3-tier 预设行每个删除按钮旁新增"✎"重命名按钮（仅自定义预设显示），点击弹出 Dialog 对话框输入新名称（含取消/确认按钮，打开时自动全选当前名称）；已新增预设兼容性矩阵（对齐上游 `PresetBundle::is_compatible` / `update_compatible`）：打印机预设新增 `nozzle_diameter`/`max_nozzle_temp`/`machine_max_speed` 属性（K1C: 0.4/300°C/600mm/s, Ender-3 S1: 0.4/260°C/500mm/s），耗材预设新增 `nozzle_temp_range_min`/`nozzle_temp_range_max`/`compatible_nozzle_min`/`compatible_nozzle_max` 属性（PLA: 190-230°C/0.2-0.8mm, ABS: 230-270°C/0.2-0.8mm, PETG: 190-250°C/0.2-0.8mm）；PresetServiceMock 新增 `isFilamentCompatibleWithPrinter()` 方法（检查喷嘴直径范围和最高温度兼容性）和 `findCompatibleFilament()` 方法（遍历耗材列表返回第一个兼容的耗材）；ConfigViewModel `autoMatchFilament()` 已从空实现更新为调用 `findCompatibleFilament()` 自动切换到兼容耗材并重新合并层级；ConfigViewModel 新增 `isCurrentFilamentCompatible()` Q_INVOKABLE 供 QML 检查当前组合是否兼容；已新增兼容性警告 UI（对齐上游 Tab incompatible warning）：PrintSettings.qml 耗材预设行标签旁新增红色圆点警告指示器（仅当前耗材与打印机不兼容时显示，带 Tooltip 提示"当前耗材与打印机可能不兼容"）；已实现耗材列表中灰化不兼容项完整过滤 UI（对齐上游 Tab 兼容性灰化显示）：ConfigViewModel 新增 `isFilamentCompatible(const QString &filamentName)` Q_INVOKABLE，PrintSettings.qml 耗材预设 ComboBox 替换为自定义委托的 raw ComboBox，下拉列表中不兼容耗材项显示为灰化文本 + 删除线 + 红色 "!" 警告图标，兼容耗材保持正常显示；已新增增强搜索对话框（对齐上游 `SearchDialog` / `OptionsSearcher`）：新建 `SearchDialog.qml` 组件，支持搜索 key/label/category/group 四个字段（150ms 防抖），搜索结果按 Page > Category 分组显示、键盘上下导航、Enter 跳转、Esc 关闭，搜索框内嵌放大镜按钮打开搜索对话框，Ctrl+F 快捷键直接打开；已新增预设继承链值来源追踪（对齐上游 Tab `get_sys_and_mod_flags`）：`ConfigViewModel::mergePresetHierarchy()` 现在记录每个 key 的值来源层级（`valueSources_` 哈希表），新增 `valueSourceForKey(key)` Q_INVOKABLE 返回 "printer"/"filament"/"print"/"default" 来源标识，`searchResultSource(int)` / `searchResultPath(int)` 供搜索结果面板查询；PrintSettings.qml 选项行左侧新增值来源色点指示器（蓝色=打印机预设、橙色=耗材预设、绿色=打印预设、灰色=默认值），搜索结果面板底部显示来源图例；搜索对话框支持跳转到目标选项并高亮显示（3 秒淡出）；smoke test 扩展至 44/44 检查项（新增 SearchDialog.qml 资源完整性检查） 2026-03-14

### P5 — Device / Monitor / Network

- [-] P5.1 Monitor 页已有可运行页面与基础布局；已完成 P5.1 基础功能迁移：DeviceServiceMock 重构为 MockDevice 结构体（5 台设备，含 name/model/sn/status/online/progress/taskName/ip/temperature/signalStrength），支持按名称/型号/SN/状态搜索过滤和设备选中；MonitorViewModel 提供完整 Q_PROPERTY 驱动（filteredDeviceCount/searchText/selectedDevice*/networkOnline/latencyMs），Q_INVOKABLE deviceAt(int) 和 selectDevice(int) 供 QML Repeater 使用；MonitorPage.qml 对齐上游 Monitor.cpp/MonitorBasePanel.cpp 的左右分栏布局（左侧 280px 设备列表含搜索栏、设备卡片、在线指示灯、信号强度、打印进度、状态标签；右侧设备详情含 4 个 Tab 对齐上游 Tabbook：状态/SD 卡/视频/HMS），状态面板显示设备状态/温度/IP/打印进度条/连接方式/信号强度/序列号卡片，3 秒 mock 轮询模拟上游 1 秒 REFRESH_INTERVAL；已新增 SD 卡文件管理 Tab（对齐上游 `MediaFilePanel`）：存储用量条（1.8/5.2 GB）、8 个 Mock G-code 文件列表（文件名/大小/日期三列）、导入/删除操作按钮；已新增 HMS 健康管理 Tab（对齐上游 HMSPanel / DeviceManager hms_list）：DeviceServiceMock 新增 MockHmsItem 结构体（moduleId/moduleNum/partId/msgLevel/msgCode/alreadyRead/message/wikiUrl，对齐上游 HMSNotifyItem）和 hmsLists_ 存储，Mock 设备 0 含 3 条 HMS（2 未读）、设备 3 含 2 条 HMS（2 未读），提供 selectedDeviceHmsCount/selectedDeviceUnreadHmsCount/selectedDeviceHmsAt/markHmsRead 方法；MonitorViewModel 新增 selectedHmsCount/selectedUnreadHmsCount Q_PROPERTY 和 hmsAt/markHmsRead Q_INVOKABLE 桥接；MonitorPage.qml HMS Tab 包含：头部未读计数徽章、HMS 总数文本、可滚动 HMS 列表（每条记录显示严重度着色边框、严重度图标、级别徽章（严重/警告/一般/信息）、未读圆点、错误代码、消息文本、模块标签）、点击标记已读、"全部标为已读"按钮、空状态显示、Tab 标题显示未读数量；但仍缺上游真实 DeviceManager/MachineObject/MQTT 连接、StatusPanel 完整状态机（MONITOR_NO_PRINTER/MONITOR_CONNECTING/MONITOR_DISCONNECTED/MONITOR_NORMAL）、MediaFilePanel 真实文件传输/打印功能、HMSPanel 真实 HMS 上报/清除协议、SideTools 打印机选择弹窗 2026-03-14
- [-] P5.2 已完成设备交互 Mock 基础迁移（对齐上游 DeviceManager/MachineObject 设备交互层）：DeviceServiceMock 新增 scanDevices()（对齐上游 SSDP 发现）、connectDevice()/disconnectDevice()（对齐上游 DeviceManager connect/disconnect，Mock 异步 1.5s 延迟模拟连接）、startPrint()/pausePrint()/resumePrint()/stopPrint()（对齐上游 Plater 打印控制）、devicePrintStage()/devicePrintLayer()/devicePrintTimeLeft()（对齐上游 MachineObject 打印状态查询）；新增 PrintJobState 结构体（gcodePath/stage/progress/totalLayers/currentLayer/timeLeft/active/paused）和 QTimer 驱动的打印模拟器（1s 间隔，自动推进进度/层号/剩余时间，stage 在 heating→mop_first_layer→printing→cooling 间自动切换，完成时停止定时器）；MonitorViewModel 新增对应桥接方法（scanDevices/connectDevice/disconnectDevice/startPrint/pausePrint/resumePrint/stopPrint）和 selectedPrintStage/selectedPrintLayer/selectedPrintTimeLeft Q_PROPERTY；MonitorPage.qml 状态面板新增打印控制栏（打印阶段/层号/剩余时间显示 + 连接/断开/开始打印/暂停/继续/停止 6 个按钮，按设备状态动态显隐），左侧扫描按钮已接入 scanDevices()；但仍缺上游 AMS（多耗材丝切换）、多灯光控制/录像功能、AI 辅助能力、真实 MQTT 连接和命令协议 2026-03-14
- [-] P5.3 已完成视频/摄像头 Mock 基础设施（对齐上游 CameraPopup / MediaPlayCtrl / StatusPanel camera enums）：新增 CameraServiceMock（CameraStreamStatus 5 态：Disconnected/Connecting/Connected/Streaming/Error；CameraRecordingStatus 对齐上游 5 态枚举；CameraTimelapseStatus 对齐上游 5 态枚举），支持 startStream()/stopStream()（Mock 异步连接序列 1.5s+0.8s）、toggleRecording()/toggleTimelapse()、switchCamera()（3 虚拟摄像头循环）、retryConnection()、takeScreenshot()、分辨率切换（720P/1080P）、自定义 RTSP URL 输入；MonitorViewModel 新增 15 个桥接方法和 Q_PROPERTY（cameraStreamStatus/cameraRecordingStatus/cameraTimelapseStatus/cameraResolution/cameraUrl/cameraErrorMessage/cameraAvailable + 7 个 Q_INVOKABLE）；MonitorPage.qml 新增"视频"Tab（替换原"更新"Tab），包含视频视口区域（Mock 占位 + 流状态图标 + 错误信息）、录像/延时摄影指示器覆盖层（REC 红点 + 延时摄影蓝标签）、控制工具栏（连接/断开/录像/延时摄影/切换摄像头/截图 6 个按钮，按流状态动态显隐）、分辨率选择器（720P/1080P pill 按钮）、自定义 URL 输入框；但仍缺上游真实 RTSP/WebRTC/FFmpeg 视频解码集成（RTSPDecoder.h / WebRTCDecoder.h）、GStreamer Bambu 视频源、MediaFilePanel 视频文件浏览/下载/回放、wxMediaCtrl2 媒体播放控件 2026-03-14
- [-] P5.4 已完成云账号和设备绑定 Mock 基础设施（对齐上游 NetworkAgent / WebUserLoginDialog / BindDialog / CommunicateWithCXCloud / AccountDeviceMgr）：新增 CloudServiceMock（loggedIn/userName/userEmail/boundDeviceCount/syncing/lastSyncTime Q_PROPERTY），支持 login()/logout()（Mock 异步 1.2s 延迟，登录后自动填充 2 台绑定设备）、bindDevice()/unbindDevice()（对齐上游 PingCodeBindDialog/UnBindMachineDialog）、syncPresets()（Mock 2s 同步延迟，更新 lastSyncTime）；HomeViewModel 新增 10 个 Q_PROPERTY（cloudLoggedIn/cloudUserName/cloudUserEmail/cloudBoundDeviceCount/cloudSyncing/cloudLastSyncTime）和 6 个 Q_INVOKABLE（cloudLogin/cloudLogout/cloudBindDevice/cloudUnbindDevice/cloudBoundDeviceAt/cloudSyncPresets）+ cloudLoginFailed 信号；HomePage.qml 首屏头部"登录账号"按钮替换为动态云账号区域（未登录显示登录按钮、已登录显示用户头像+名称+设备计数+同步按钮+登出按钮），新增云端设备列表（显示绑定设备卡片含在线指示灯/名称/SN/解绑操作+添加设备按钮），新增登录对话框（用户名/密码输入+错误提示，对齐上游 WebUserLoginDialog）、绑定设备对话框（设备名称+PIN 码输入，对齐上游 PingCodeBindDialog）；但仍缺上游真实 bambu_networking 登录协议、wxWebView OAuth 认证流程、MQTT 设备连接、CommunicateWithCXCloud 云端预设同步、AI 云服务集成（AICloudServiceDialog） 2026-03-14

### P6 — Calibration / Project / Auxiliary / Model Mall / MultiMachine

- [-] P6.1 Project、Calibration、Preferences 等页面具备基础入口；CalibrationPage 已完成上游对齐重构：对照上游 CalibrationPanel（Tabbook + CalibrationWizard 分栏布局）、CalibrationDialog（硬件校准步骤选择）、CalibrationWizard（向导步骤链 Start→Preset→Cali→Save）实现左侧校准类型列表 + 右侧向导步骤指示器 UI；CalibrationServiceMock 新增 5 种校准类型（Flow Dynamics/Flow Rate/Bed Leveling/Vibration Compensation/Max Volumetric Speed，对齐上游 CalibMode 枚举和 CalibrationDialog 校准项）和每类型 2-6 步向导流程（对齐上游 CalibrationWizardPageStep 链）；CalibrationViewModel 新增 12 个 Q_PROPERTY（currentStepIndex/currentStepTitle/currentStepDesc/totalStepCount/selectedStatus/selectedCategory 等）驱动 QML 向导步骤指示器、进度条、状态徽章；CalibrationPage 新增状态显示（InProgress/Completed/Failed 徽章 + 每项状态点）、向导步骤条（圆形指示器 + 连接线 + 步骤名）、当前步骤描述面板、进度条、分类筛选药丸按钮；已新增步骤状态追踪（对齐上游 StepCtrl pending/active/completed 状态）：CalibrationServiceMock 新增 `stepState(int typeIndex, int stepIndex)` Q_INVOKABLE（0=pending/1=active/2=completed），运行中根据 currentStepIndex 实时计算每个步骤状态，校准完成后所有步骤标记为 completed，CalibrationViewModel 新增 `stepState(int stepIndex)` 桥接方法；CalibrationPage 步骤指示器重构为使用 `modelData.state` 属性驱动（圆形指示器显示数字或 ✓ 对号、连接线根据状态着色、步骤名根据状态高亮），stepChanged 信号自动刷新步骤状态；但仍未完成上游完整设备连接/预设选择/真实参数编辑/历史记录/CalibrationCaliPage 打印任务管理等深度功能 2026-03-14
- [-] P6.2 校准工作流已对照上游校准模块开始逐项承接：CalibrationServiceMock 模拟 5 种校准类型和完整向导步骤链（对齐上游 CalibState: Start→Preset→Calibration→CoarseSave→FineCalibration→Save→Finish）；CalibrationViewModel 桥接步骤导航（goToStep）、进度追踪、状态管理；CalibrationDialog 保留为独立弹窗用于校准执行确认；已新增 CalibrationPresetPage 耗材预设选择器（对齐上游 `CalibrationPresetPage`）：CalibrationViewModel 新增 `currentStepId`/`showPresetSelector`/`filamentPresetNames`/`selectedFilamentPreset` Q_PROPERTY 和 `setSelectedFilamentPreset()` 方法，通过 `setPresetService()` 注入 PresetServiceMock 访问耗材预设列表（复用 3-tier 预设体系）；CalibrationPage 在步骤描述区新增条件显示的耗材预设选择器（当 `showPresetSelector` 为 true 时显示），包含预设 pill 按钮组（PLA/ABS/PETG 等，高亮当前选中），连接 CalibrationViewModel 的 selectedFilamentPreset；已新增 CalibrationDialog 硬件校准选项（对齐上游 `CalibrationDialog::create_check_option`）：新增 4 个可勾选项（微型激光雷达校准/热床调平/振动补偿/电机降噪），匹配上游 xcam_cali/bed_leveling/vibration/motor_noise 四个 checkbox，每个选项含复选框+标签+辅助说明，Vibration 默认勾选（对齐上游"always shown"），Motor Noise 默认不勾选（对齐上游"shown if device supports"），硬件选项仅在校准未运行时显示；但仍缺上游 CalibrationWizardCaliPage（发送打印任务到打印机）、CalibrationWizardSavePage（保存校准结果到预设）的真实设备交互实现，以及 CaliHistoryDialog（历史记录）、ExtrusionCalibration（K/N 参数输入对话框）的完整迁移 2026-03-14
- [-] P6.3 Model Mall 已完成上游对齐重构：对照上游 ModelMallDialog（DPIFrame + wxWebView + 导航控件 back/forward/refresh）和 WebViewPanel（script message 处理、go_to_mall/go_to_publish URL 导航）实现完整原生 QML 商城浏览界面；ModelMallViewModel 重构为分类+搜索+排序过滤驱动的 Q_PROPERTY/Q_INVOKABLE 接口（12 个 mock 模型分 6 类，支持名称/作者搜索、4 种排序模式对齐上游 Recommended/Popular/Newest/Free 标签、featured 标记对齐上游 SendDesignStaffpick），ModelEntry 新增 downloads/rating/thumbnailColor/thumbnailIcon/categoryIndex 字段，提供 filteredCount/modelName/modelAuthor/modelDownloads/modelRating/modelFree/modelPrice/modelThumbnailColor/modelThumbnailIcon/modelFeatured 共 11 个 Q_INVOKABLE 访问器避免 Qt6 V4 VariantAssociationObject 崩溃；ModelMallPage.qml 使用 Theme 设计令牌重构为 4 区域布局（顶部搜索栏+排序标签、分类药丸条、Flow 网格卡片、底部状态栏），模型卡片包含彩色缩略图、Featured/Free 徽章、下载量、5 星评分、作者、价格、下载按钮，空状态显示搜索提示，状态栏显示 WebView 不可用提示（对齐上游 WebView2 依赖）；已新增模型详情弹窗（对齐上游 OpenModelDetail）：点击模型卡片弹出 Dialog 显示大缩略图、完整名称/作者/评分/价格/描述、元数据网格（文件格式/大小/打印时间/材料用量）、标签 Flow 列表、下载/购买按钮；ModelMallViewModel 新增 selectedModelIndex/detailDialogOpen Q_PROPERTY 和 6 个详情访问器（modelDescription/modelFileFormat/modelFileSizeKB/modelPrintTime/modelMaterialUsage/modelTags）；ModelEntry 扩展为 16 个字段；已新增刷新按钮加载动画（对齐上游 mall_control_refresh）：刷新按钮在 isLoading 时显示旋转箭头动画（RotationAnimation 1s 循环），禁用重复点击；已新增下载状态指示器（对齐上游 ModelMallDialog download 交互）：下载按钮在 isDownloading 时显示旋转加载图标+"Downloading..."文本，禁用重复点击，下载完成后自动恢复；ModelMallViewModel 新增 `downloading` 字段和 `isDownloading(index)` Q_INVOKABLE，`downloadModel()` 模拟 2 秒异步下载后自动递增 downloads 计数；`refresh()` 改为 800ms 异步加载模拟；但仍缺上游真实 wxWebView/QtWebEngine 集成、script message 双向通信、云账号登录同步、makerhub URL 导航和真实模型下载 2026-03-14
- [-] P6.4 MultiMachine 多机管理页面已从骨架增强为基础承接能力页面；对照上游 MultiMachinePage（3-tab Tabbook）、MultiMachineManagerPage（设备列表+排序+分页+View按钮+编辑打印机）、LocalTaskManagerPage（本地任务表格+选择+取消+Stop All）、CloudTaskManagerPage（云端任务表格+暂停/恢复+Stop All+分页）实现：左侧垂直Tab栏（设备/任务发送/任务已发送）、设备管理Tab含表格头（设备名/任务名/状态/操作）和设备行（在线指示灯+离线标记+进度条+View按钮+悬停删除）、本地任务Tab含选择复选框+项目名/打印机/状态/发送时间/进度条+Stop All、云端任务Tab含进度条+暂停按钮+Stop All、编辑打印机按钮和排序功能（设备名/状态双向排序）、分页控件、空状态提示+添加按钮；MultiMachineViewModel 提供 5 台Mock设备（K1C/K1 Max/Ender-3 S1 Pro/K2 Plus/CR-10 Smart Pro，覆盖 Printing/Finish/Idle/Prepare/Offline 状态）、3 个本地任务（Sending/Pending/Success）、3 个云端任务（Finish/Printing/Failed），Q_PROPERTY 驱动 QML；已新增设备选择发送对话框（对齐上游 MultiMachinePickPage / SendMultiMachinePage）：本地任务工具栏新增"Send to Device"按钮（仅在线设备存在时显示），点击弹出设备选择对话框（ComboBox 选任务 + 在线设备列表单选），MultiMachineViewModel 新增 `onlineMachineCount()`/`onlineMachineName(int)`/`sendTaskToDevice(taskIndex, machineIndex)` 方法，Mock 模式发送后 2 秒模拟转为 printing 状态；已新增设备搜索/过滤功能（对齐上游 `selected_machines()` 搜索）：MultiMachineViewModel 新增 `searchText`/`filteredMachineCount` Q_PROPERTY 和 `setSearchText()` 方法，搜索支持按设备名/型号/状态/IP 过滤，所有设备访问器（machineName/Model/Status/Progress/Remaining 等）使用 `filteredMachineAt()` 仅返回匹配结果，分页基于过滤后数量重算；已新增剩余时间显示（对齐上游 `get_left_time` dd:hh:mm 格式）：设备列表新增"Remaining"列（仅活动状态显示时间），本地任务和云端任务表格均新增"Remaining"列（sending/printing 状态显示时间），LocalTaskEntry/CloudTaskEntry 新增 `remaining` 字段，MultiMachineViewModel 新增 `localTaskRemaining(int)`/`cloudTaskRemaining(int)` Q_INVOKABLE；设备管理Tab工具栏新增搜索框（带清除按钮和焦点高亮）；但仍未完成上游真实 DeviceManager 连接、MQTT 订阅（subscribe_device_list）、TaskManager 真实任务管理、以及 SortItem 完整排序策略 2026-03-14

### P7 — 质量、稳定性与发布

- [-] P7.1 程序已可构建、可启动、可进行基础导入与切片验证；新增 DeviceServiceMock/CameraServiceMock/CloudServiceMock 三个新 Service、MonitorViewModel/HomeViewModel 扩展、MonitorPage 视频 Tab + 打印控制栏、HomePage 云账号/设备绑定 UI、MonitorPage HMS 健康管理面板（5 台设备 Mock HMS 数据、3 种告警级别、未读标记、全部已读）、MultiMachinePage 搜索过滤+剩余时间列、CalibrationPage 步骤状态追踪（StepCtrl pending/active/completed）、ModelMallPage 刷新按钮加载动画（旋转箭头 RotationAnimation）+ 下载按钮状态指示器（下载中旋转图标+禁用交互）、星级评分 parent.index 引用错误修复（改为 delegate index 属性）、ModelMallViewModel 新增 `isDownloading()` 和异步下载模拟、smoke test 扩展至 59/59 检查项，全部通过构建和回归验证；但仍缺上游完整功能验证（真实切片/G-code预览/设备连接/校准/视频/云服务/WebView/多机管理的端到端对照） 2026-03-14
- [-] P7.2 已建立自动化 smoke test 框架（scripts/smoke_test.ps1）：6 阶段验证（二进制检查、Qt DLL 依赖、应用启动存活、QML 运行时警告检测、编译产物检查、QML 资源完整性），当前 43/43 通过、0 QML 警告；已扩展编译产物检查至 19 个关键 obj 文件（新增 SettingsViewModel/HomeViewModel/ModelMallViewModel/SliceService/ConfigOptionModel/CameraController），QML 资源完整性检查扩展至 30 个文件（新增 NotificationCenter/ToolPositionTooltip/CollapsibleSection/PreferencesPage/CxButton/CxComboBox/CxSlider/CxCheckBox/CxIconButton）；当前 59/59 通过、0 QML 警告；P5.1-P6.4 全部新增功能通过回归验证；但仍缺”与上游行为对照”的回归基线和自动化页面导航测试 2026-03-14
- [-] P7.3 已审查全部关键页面 QML 绑定：修复 SettingsPage 分类计数字体色 parent 链（4 级 → 3 级）导致的运行时 undefined 警告、修复 main.qml compareReferenceSource 缩进异常；修复 verify 脚本 cmake 兼容性（添加 -DQT_FORCE_MIN_CMAKE_VERSION_FOR_USING_QT=3.21 支持 CMake 3.21 + Qt 6.10 组合）；审查覆盖 PreparePage/PreviewPage/MonitorPage/SettingsPage/PrintSettings/ObjectList/Sidebar/SliceProgress/LayerSlider/MoveSlider/Legend/StatsPanel/StatusBar/ErrorBanner/ErrorToast/PrintDialog 全部 QML 文件，确认 required property + null guard 模式完整、无绑定循环、无类型失配；**根本修复 Theme 单例注册**（添加 qmldir 声明，QML 警告从 994 行降至 0）；修复 main_qml.cpp qputenv 覆盖、Shortcut sequences、缺失 SVG 图标、layout anchors 警告；修复 C++ 侧 10+ 处 service 空指针保护（EditorViewModel/PreviewViewModel）、PreparePage undo/redo 双触发、GLViewportRenderer phantom transform 和 GL context null check、CameraController 零向量/零参数保护、PreviewViewModel 工具颜色 hex 格式 bug；修复 Qt Quick Controls 2 MenuItem 不支持 shortcut 属性导致的 QML 启动崩溃（移除 11 个 MenuItem shortcut 绑定，改用独立 Shortcut 元素）；修复 PreviewViewModel roleTimeCount 等 4 个 Q_INVOKABLE 从 private 区域移至 public（Qt6 QML 无法调用 private invokable）；修复 GCodeRenderer 编译错误（NOMINMAX 宏 + 缺失 cfloat/cmath 头文件）；已修复 ModelMallPage.qml 星级评分 parent.index 引用错误（Row 无 index 属性导致所有星星始终显示空星），修正为 delegate 自身 index 属性；已通过运行时验证和自动化 smoke test 确认 0 QML 运行时警告、43/43 测试通过；已修复 PrintSettings.qml `compMA3` MouseArea 在 RowLayout 内使用 anchors 的警告（改用 Layout.fillWidth/Layout.preferredHeight）、PrintSettings.qml layer range Repeater delegate `required property int index` 与整数模型不兼容（改用 `property int delegateIndex: index`）、PrintSettings.qml `layerRangeCount` 未带括号导致 "Unable to assign a function" 警告（全部改为 `layerRangeCount()`）、LayerSlider.qml `minThumbMA` 未定义引用（MouseArea 添加 id 和 hoverEnabled）；smoke test 更新至 44/44 通过、0 QML 警告 2026-03-14
- [-] P7.4 已创建并持续更新发布就绪文档 `docs/RELEASE_READINESS.md`：构建状态检查表（CMake/构建/59/59 烟雾测试/启动存活/DLL 依赖全部 PASS）、Mock 模式已知限制清单（10 项：模型加载/切片/G-code预览/设备连接/校准/视频/云服务/WebView/多机管理/HMS）、P0-P3 缺陷检查清单（5 个优先级共 14 项，已完成 6 项）、功能完成度矩阵（9 个模块对齐百分比，整体 65%+）、回滚策略（git revert + 烟雾测试验证）；但仍缺实际功能冻结、全页面回归测试自动化、和 i18n 完整验证 2026-03-14

---

## 4. 模块对照矩阵（第一版）

| 上游模块         | 上游源码位置                                                                              | Qt6 承接位置                                                                                                                                    | 当前状态                         |
| ---------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| 主窗口与全局导航 | `third_party/CrealityPrint/src/slic3r/GUI/MainFrame.*`, `GUI_App.*`                       | `src/qml_gui/main.qml`, `src/qml_gui/BackendContext.*`                                                                                          | `[-]` 主壳已运行，未全量对照     |
| Prepare 主工作区 | `third_party/CrealityPrint/src/slic3r/GUI/Plater.*`                                       | `src/qml_gui/pages/PreparePage.qml`, `src/qml_gui/panels/*`, `src/core/viewmodels/EditorViewModel.*`                                            | `[-]` 基础闭环已通，功能未齐     |
| 3D 编辑视口      | `third_party/CrealityPrint/src/slic3r/GUI/GLCanvas3D.*`, `Gizmos/*`                       | `src/qml_gui/Renderer/*`, `src/core/rendering/*`                                                                                                | `[-]` 桥接已建，复杂交互未齐     |
| G-code 预览      | `third_party/CrealityPrint/src/slic3r/GUI/GCodeViewer.*`, `GUI_Preview.*`                 | `src/qml_gui/pages/PreviewPage.qml`, `src/qml_gui/components/*`, `src/core/viewmodels/PreviewViewModel.*`, `src/core/rendering/GCodeRenderer.*` | `[-]` 主链路已通，待对照         |
| 设置与预设       | `third_party/CrealityPrint/src/slic3r/GUI/Tab.*`, `PresetComboBoxes.*`                    | `src/qml_gui/pages/SettingsPage.qml`, `src/qml_gui/panels/PrintSettings.qml`, `src/qml_gui/Models/*`, `src/core/viewmodels/ConfigViewModel.*`   | `[-]` 骨架+作用域+预设保存/脏检测已实现，缺继承链 |
| 后台切片         | `third_party/CrealityPrint/src/slic3r/GUI/BackgroundSlicingProcess.*`                     | `src/core/services/SliceService.*`                                                                                                              | `[-]` 主流程已通，待状态机对照   |
| 项目与文件导入   | `third_party/CrealityPrint/src/slic3r/GUI` + `libslic3r/Format/*`                         | `src/core/services/ProjectService.*`, `src/core/viewmodels/EditorViewModel.*`                                                                   | `[-]` 导入可用，项目工作流未齐   |
| 设备与监控       | `third_party/CrealityPrint/src/slic3r/GUI/Monitor.*`, `DeviceManager.*`, `print_manage/*` | `src/qml_gui/pages/MonitorPage.qml`, `src/core/viewmodels/MonitorViewModel.*`, `src/core/services/DeviceService*`, `CameraService*`                               | `[-]` Mock 设备列表/打印控制/摄像头/视频Tab 已实现，缺真实连接 |
| 校准             | `third_party/CrealityPrint/src/slic3r/GUI/CalibrationPanel.*`                             | `src/qml_gui/pages/CalibrationPage.qml`, `src/core/viewmodels/CalibrationViewModel.*`                                                           | `[-]` 上游对齐重构完成，设备交互未承接 |
| 商城与 WebView   | `third_party/CrealityPrint/src/slic3r/GUI/ModelMall.*`, `WebViewDialog.*`                 | `src/qml_gui/pages/ModelMallPage.qml`, `src/core/viewmodels/ModelMallViewModel.*`                                                               | `[-]` Mock 商城 UI 已实现，缺 WebView/登录 |
| 多机管理         | `third_party/CrealityPrint/src/slic3r/GUI/MultiMachineManagerPage.*`                      | `src/qml_gui/pages/MultiMachinePage.qml`, 相关 ViewModel/Service                                                                                | `[-]` 3-tab 布局+设备/任务表格+Mock 数据已实现，缺真实连接 |

---

## 5. 当前优先级

1. 收敛 Prepare：对象工作区、右侧工作区、Gizmo 与交互链路。
2. 收敛 Preview：清 warning、补对照验收。
3. 切换 Settings / Preset 到上游语义承接。
4. 持续补齐”上游模块 -> Qt6 模块 -> 验收记录”的逐项对照闭环。
5. 真实库集成：libslic3r 切片引擎、bambu_networking 设备连接、FFmpeg 视频解码。

---

## 6. 里程碑口径

| 里程碑                      | 验收标准                                        | 当前状态 |
| --------------------------- | ----------------------------------------------- | -------- |
| M0 真值冻结                 | 上游 tag/commit + 功能矩阵冻结                  | 已完成   |
| M1 Prepare 可替代日常使用   | 导入、对象管理、Gizmo、切片入口、平板工作流可用 | 进行中   |
| M2 Preview 可稳定回归       | 切片到预览端到端，主要视图模式对照通过          | 进行中   |
| M3 Settings/Preset 完整承接 | 预设与多作用域参数体系对齐上游                  | 未开始   |
| M4 Device/Monitor 承接      | 设备连接、状态、控制、视频闭环                  | 未开始   |
| M5 全功能 RC                | 所有顶栏页面可用，关键回归通过                  | 未开始   |

---

## 7. 备注

- 旧任务文档中“合计 70/70 全部完成”的表述仅能代表“阶段性骨架任务已经做过”，不能再代表当前项目目标的真实完成度。
- 从 2026-03-10 起，所有新增任务和完成记录都必须挂到“上游功能真值对照”之下。
