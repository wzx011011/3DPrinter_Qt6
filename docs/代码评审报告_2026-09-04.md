# OWzx Qt6 迁移 — 全工程代码评审报告（2026-09-04）

> 评审对象：分支 `review/v5.16-gap-closure` @ `48f73ac`（与 main 同点）+ 工作区 57 个已跟踪文件未提交改动（约 +7585/-963）+ 未跟踪新文件。
> 评审方式：11 个只读评审子代理按模块分区覆盖全部 `src/`（约 11.5 万行）与 `tests/`（约 3 万行），统一维度为正确性缺陷（P0/P1/P2）、分层违规、上游对齐抽查（真值 `third_party/OrcaSlicer/src/slic3r/GUI/`，锁定 `8b93cc5`）、死代码与 Mock 假成功；并实际运行 ctest 核对测试数据。
> 本文为只读评审产物，不包含代码修改；配套更新了 `docs/源码对照迁移任务追踪.md`、`docs/发布就绪度.md`、`docs/源码真值功能矩阵.md`。

---

## 1. 总体结论

1. **架构纪律好于典型迁移项目。** 分层边界（backend context property + ViewModel 信号转发）基本没有 QML 直调 service 内部的破口；Undo 两段式构造、值捕获并发、上游 file:line 锚点注释执行得非常一致；WIP 的 meshData TLV v2、printId 体系、BVH 拾取、索引化绘制等改造方向正确、推导严谨且有等价性测试锁定。
2. **但当前未提交 WIP 引入了 6 个 P0 级问题**（切片线程竞态、undo 快照顺序颠倒、另存为丢盘数据、visible/printable 语义混淆等），**必须在提交前修复**（第 4 节）。
3. **测试体系存在系统性假绿面**：QmlUiAuditTests 约 93% 的断言是"源码字符串存在性"；ViewModelSmokeTests/E2E 100% 绕过 QML 直调 C++，QML 绑定/信号转发层运行时零覆盖（第 7 节）。
4. **服务层假成功与《依赖与协议边界审计》登记表原则仍有 4 处实质冲突**（云登录/设备上线/打印仿真/假摄像头流），违反"不允许 mock 假成功"的项目规则（第 8 节）。
5. **迁移进度总体判断**：v5.16 "Full Gap Closure" 已于 2026-09-01 收口（45 项 UI 审计差距 = 29 implemented / 3 blocked / 13 declined）；当前处于收口后的新一轮**视口性能化（meshData v2 / 索引化绘制 / 专用切片线程 / undo 内存预算）+ 测试基建（parity 基线 / PerfBench）** 工作期，WIP 完成度约 80%，尚未按主题拆分提交。

## 2. ctest 实测结果（2026-09-04，build/ 现有产物，未重编）

| 项目 | 结果 |
| --- | --- |
| ctest 注册项 | 16 项，**14 通过 / 2 失败** |
| 通过 | ViewModelSmokeTests（31.5s 全绿）、E2EWorkflowTests、QmlUiAuditTests、PartPlate、PreviewParser、PrepareSceneData、CameraParity、ObjectPicking、Gizmo×3、NavigatorCube、ViewportContextMenu、AppTool |
| CliTests | **22/23 通过**，`testSliceBlock20XY` 失败：切片退出码 -3（CLI_SLICING_ERROR）≠ 0（tests/CliTests.cpp:136）。时间上与 WIP 的 `setDirectOutputPath` 直写改造（src/cli/CliRunner.cpp:837-843）正相关，需排查是否为该模型切片真实失败 |
| InventoryAuditTests | **失败**：引用的清单文档 `.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` 已在 .planning 目录重组中不存在（tests/InventoryAuditTests.cpp:164）；且该 exe 为 2026-08-31 03:58 陈旧产物（与根目录压扁文件名事故同一时刻），其余 exe 均为 09-04 01:55 重建 |
| 与文档口径差异 | `docs/发布就绪度.md` 记载"12 套件 569 测试 0 失败"（2026-09-02）在当前 WIP 工作区**不再成立**（见下节修订） |

## 3. 迁移进度现状（截至 2026-09-04）

### 3.1 三层进度账本快照

| 账本 | 位置 | 当前快照 |
| --- | --- | --- |
| GSD 里程碑 | `.planning/STATE.md`、`MILESTONES.md` | v5.16 "Full Gap Closure" 11/11 phases 100%（2026-08-16 收口）；Phase 231-241 全部 `[x]`；227-230 保留给搁置的 v5.11 Process Settings（未跟踪的 227 目录表明可能重启） |
| 主追踪账本 | `docs/源码对照迁移任务追踪.md` | P0-P17：**130 `[x]` / 49 `[-]` / 3 `[ ]`**（`[ ]`：P2.8.2 Shell 渲染、P12.2 子模块推送 Blocked、P17.8 冲突标记 Blocked） |
| UI 审计差距 | `.planning/debug/global-ui-audit-gap-inventory-2026-08-31.md` | Wave 0-7 于 2026-09-01 收口：45 项 = **29 implemented / 3 blocked / 13 declined** |

### 3.2 功能域覆盖评估（本次评审汇总）

| 功能域 | 状态 | 依据（本次评审分区结论） |
| --- | --- | --- |
| 组合根/通知系统/AI 桥 | 已实现 | R1：质量较高，边角缺陷见 P1-3/4/5 |
| 3D 视口（RHI 路径） | 已实现 | R9：v2 序列化/索引化绘制边界校验完整，正面路径无 P0；软件路径部分实现（交互信号未发射） |
| 相机 | 已实现（有记录偏差） | R9：与 Camera.cpp 语义基本一致；默认方位 35°/45°、fitView 复位朝向、zoom 远界为近似——建议入"已知偏差清单"以免与 P14 1:1 口径矛盾 |
| 几何/拾取/绘制引擎 | 已实现 | R10：BVH/Moller-Trumbore/seed-fill 与上游一致；HeightRange trafo 一处偏差（P1-30） |
| Prepare 页 | 已实现 | R2：gizmo/绘画/切片/平板体系覆盖极广；默认值/步长级打磨项若干 |
| Preview 页 | 已实现（有正确性缺陷） | R6：解析器覆盖远超同期，但速度单位 60 倍错、G2 前缀误匹配等（P1 组） |
| Settings/Preset | 已实现 | R6/R8：三层 Tab 审计完整；兼容性规则两处偏差（P2） |
| 校准 | 已实现 | R6：步骤机/两段 Flow/历史完整，无阻断缺陷 |
| Monitor/设备 | 部分实现（Mock 无披露） | R6/R8：MQTT 真连路径存在，mock 假上线/假打印无用户可见披露 |
| 多机管理 | 未实现（纯模拟） | R6：硬编码假数据 + 假发送成功无披露 |
| 云/商城 | 明确豁免（Blocked） | Model Mall 已移除（MALL-01 锁定）；CloudServiceMock 登录假成功仍从 HomePage 可达（违规） |
| 摄像头/媒体 | 明确豁免（Blocked） | FFmpeg/WebRTC 不可用；CameraServiceMock 假 Streaming 状态需披露 |
| 项目/3MF/盘管理 | 已实现（有 P0） | R7：主链路还原度高；saveProjectAs 丢盘数据、visible/printable 混淆两处 P0 |
| 切片引擎桥 | 已实现（WIP 有 P0） | R8：真实 Print::process 全链路；WIP 专用线程两处竞态/取消缺口 |
| Undo/Redo | 已实现（WIP 有 P0/P1） | R5/R8：命令体系完整；booleanExecute 快照顺序颠倒、heavyBytes 漂移 |
| 测试基建 | 部分实现 | R11：行为测试质量高，QML 运行时零覆盖，审计测试存在性冒充正确性 |

### 3.3 规模对比

上游 GUI（`src/slic3r/GUI`，499 文件）约 28.7 万行；Qt6 侧 `src/core` + `src/qml_gui`（218 文件）约 11.25 万行（≈39%）。上游约 1/4 行数为 wxWidgets 控件包装（Widgets/、wxExtensions、BitmapCache、dark_mode），由 QML/Quick Controls 天然替代，不需等量迁移；切片引擎直接复用上游 libslic3r 子模块，不计入对比。

## 4. P0 问题清单（提交 WIP 前必须修复）

| # | 位置 | 问题 |
| --- | --- | --- |
| P0-1 | `src/core/services/SliceService.h:426`、`.cpp:271-280, 824-905, 1301-1311, 1520-1534` | WIP 专属切片线程与 GUI 线程**无锁共享** `std::map m_persistentSlices`（worker emplace/erase、GUI find/erase），取消后立即重启即数据竞态（UB）。方向：加 mutex 或槽所有权随 payload 单线程持有 |
| P0-2 | `SliceService.cpp:271-280` | 析构只置 cancel flag，`print.process()` 内部不检查该 flag（上游 BackgroundSlicingProcess.cpp:554 先 `Print::cancel()` 再 join）→ 切片 >10s 时 `wait(10000)` 超时，QThread 带线程析构 = qFatal 进程终止。方向：dtor 先 `activePrint_->cancel()` |
| P0-3 | `src/core/viewmodels/EditorViewModel.cpp:2509-2518` | WIP `booleanExecute()` **先执行布尔运算后构造 SceneSnapshotCommand**（构造即捕获 BEFORE 快照，UndoCommands.h:614 明确要求先构造）→ before==after，Ctrl+Z 变 no-op。方向：命令构造移到 meshBooleanVolumes 之前，失败时 delete cmd |
| P0-4 | `src/core/services/ProjectServiceMock.cpp:1542-1547` | `saveProjectAs`/`exportModel("3mf")` 走 Prusa 版 `store_3mf`，不写 `plate_data_list`/缩略图/盘配置/耗材 sidecar（全量 writer 是 :10206 `storeProject3mf`）→ 多盘项目"另存为"后重载全部对象塌缩到板 1、盘数据全丢（静默数据丢失）。方向：统一走 storeProject3mf + sidecar |
| P0-5 | `ProjectServiceMock.cpp:2355-2372`（同根 ：1277/6795/9148/9337） | `setObjectVisible` 把 `visible` 写成 `ModelInstance::printable`：UI"隐藏对象"被**静默剔除出切片**。上游语义：visible 仅渲染（ModelVolume::visible），printable 才影响切片。方向：解耦两字段 |
| P0-6 | `src/qml_gui/main.qml:673`、`BBLTopbar.qml:849,119` | 标题栏 X / File>退出 走 `Qt.quit()`，**绕过 onClosing 脏项目确认**（Qt.quit 不产生 QCloseEvent；main.qml:41-42 注释是对 Qt 行为的误解）→ 未保存修改无提示丢失。上游：BBLTopbar.cpp:544 `Close()` → MainFrame.cpp:443 close_with_confirm 可否决。方向：三处统一走带守卫的关闭流 |

## 5. P1 问题清单（按分区，修复方向见文内引用）

### R1 组合根/Shell
- P1-1 `main.qml:409-415`、`BBLTopbar.qml:742-745`：Ctrl+S 与菜单保存绕过 `canSave()` 切片门禁（BackendContext.cpp:526 注释明言切片中保存不安全）→ 切片线程读 Model 时并发序列化。工具栏按钮有门禁，菜单/快捷键没有。
- P1-2 `BackendContext.cpp:1394-1397`：`clearError()` 实现为删最新任意通知，`loadFinished` 每次成功加载都调用 → 成功加载顶掉无关 toast。
- P1-3 `BackendContext.cpp:1258-1313`：溢出队列不参与 type-only 去重 → 栈满时切片进度每 tick 入队，结束后成串过期 toast。
- P1-4 `BackendContext.cpp:1813-1833`：nextHint/prevHint 无条件覆写 lastError* 单例语义。

### R2 pages
- P1-5 `PreparePage.qml:4274`（WIP）：面检测采样间隔 SpinBox `from:1 to:100` int 刻度与 double 0.5 值错位，显示与实际值脱节。
- P1-6 `PreparePage.qml:3775 vs :2222`（WIP）：`supportPaintToolType===3` 在 MMU 面板标"高度范围"、支撑面板标"缝隙填充"，共享状态跨 gizmo 串扰；上游 MMU height-range 是 cursor 类型而非 tool（GLGizmoPainterBase.hpp:252）。
- P1-7 `MultiMachinePage.qml:484/491`：分页输入框声明式绑定被 `applyPage()` 命令式赋值永久打断。

### R3 dialogs
- P1-8 `PrintDialog.qml:155-159`：读取**不存在的** `editorVm.lastGcodePath`（undefined）→ 打印对话框永远以空 G-code 路径打开；实例化时也未注入 `monitorVm`。整条"打印发送"链路为假完成。
- P1-9 `BedShapeDialog.qml:115-216`：所有输入即时写穿 ViewModel，取消不回滚（偏离上游 OK/Cancel 语义）。
- P1-10 `ConfigWizardDialog.qml:501-509`：向导完成仅持久化标志，**所选打印机/耗材预设从未被应用**（wizardFinished 空壳；上游 ConfigWizard::apply_config 会选中预设）。
- P1-11 `ConfigWizardDialog.qml:44-73`、`WipeTowerDialog.qml:28`：QML 直绑 `backend.presetServiceMock`，违反 `.codex/rules/qml-boundaries.md`（WIP 的 FilamentGroupPopup 已示范正确桥接方式）。
- P1-12 `SavePresetDialog.qml:62-69`：禁止与现有名同名 → "覆盖保存当前预设"（上游主路径）不可达；文件头声称的 saveCurrentPreset 分支缺失。

### R4 panels/components/Models
- P1-13 `PrepareContextMenus.qml:61-73,256,556,674,719-726`：**系统性模式**——Q_INVOKABLE 在绑定中一次性求值：Flush Options 勾选、Set printable、Lock plate 文案永不刷新；instanceCountDialog 对对象 B 打开仍显示旧值并静默写错。同类：`LeftSidebar.qml:171,242-249` 耗材兼容红点、`PreviewLayerRail.qml:104-105`（滑条绑定被首次拖动打断）、`ErrorToast.qml:228`。方向：推广 LeftSidebar WIP 的 Connections 刷新策略或改 NOTIFY 属性。
- P1-14 `panels/ObjectList.qml`：~1560 行**未被任何 QML 实例化的死代码**（测试还断言 LeftSidebar 不得引用它），内含拖拽排序实现无效（:1332-1369 无 drag.target）、假按钮（:1438）等；`panels/SliceProgress.qml`（656 行）同样未接线。按仓库反退化规则应删除或接线。
- P1-15 `ConfigOptionModel.cpp`（WIP）：移除 `extruders_count` 使 TabPrinter 多挤出机计数行（Tab.cpp:902）无 UI 承载（已注释披露，需补回或登记）。

### R5 EditorViewModel
- P1-16 `EditorViewModel.cpp:2520-2521`（WIP）：布尔后残留卷选择索引，后续删除可能删错卷。
- P1-17 `EditorViewModel.cpp:127`（WIP）：mesh blob `geometryCount` 未校验即 `resize`（畸形数据可触发 ~48GB 分配崩溃）。
- P1-18 `EditorViewModel.cpp:8756-8780`：删除当前盘后视口网格缓存不刷新（`setCurrentPlateIndex` 有刷新，deletePlate/clonePlate/movePlate 没有）→ 视口持续渲染已删盘网格。
- P1-19 `EditorViewModel.cpp:9757, 9502-9556`：`requestSlice`/`checkViewportWarnings` 硬编码 220×220 热床 + 假半径估算，无视真实 preset bed shape，且遍历所有盘（上游 check_outside_state 用真实 bed + 实例 bbox）。

### R6 其余 viewmodels
- P1-20 `PreviewViewModel.cpp:2688, 1055`：avgSpeed 把 mm/min 的 F 值标成 "mm/s"（60 倍错误，F3000 显示 3000 mm/s）；VT_Speed 同源同错。
- P1-21 `PreviewViewModel.cpp:2474`：`startsWith("G2")/"G3"` 无空格守卫，`G28 X0 Y0` 被当 R=0 圆弧解析（:2536 G0/G1 是精确写法）。
- P1-22 `SettingsViewModel.cpp:236-272`（WIP）：`skipAmsBlacklistCheck` 只接了 load/setter，`saveToSettings` 漏写 → 重启丢失，与自身注释矛盾。
- P1-23 `PreviewViewModel.cpp:622-628`：播放结束不发 stateChanged，QML isPlaying 永卡 true（每次播放必现）。
- P1-24 `MultiMachineViewModel.cpp:60-101,649-761`：全页硬编码假数据 + 随机在线状态 + 2s 假"Task sent"，无披露；`MonitorViewModel.cpp:291-299` mock 分支 1.5s 假上线无披露。
- P1-25 `ConfigViewModel.cpp:2294-2306`：`applyProjectConfig` 用 PrusaSlicer 旧键名（speed_print/infill_density 等），OrcaSlicer 3MF 配置不刷新摘要属性。
- P1-26 `ConfigOptionModel.cpp:61-64`（WIP）：tr 源串 "°C"→"℃" 打断 6 个 .ts 共 10 条既有翻译匹配。

### R7 ProjectServiceMock
- P1-27 `ProjectServiceMock.cpp:6766-6841`（split/assemble/cut 同病）：HAS 路径重建 objectNames_ 平行数组但不迁移 `m_mockLayerRanges`/overrides → 删除对象 0 后层范围/覆盖错配到移位后的对象。方向：仿 meshBooleanVolumes 的 shift 抽 helper。
- P1-28 `ProjectServiceMock.cpp:4722, 4939`：`objects.erase` 绕过 `Model::delete_object` → 整个 ModelObject 泄漏（两处，后者为 WIP）。
- P1-29 `ProjectServiceMock.cpp:9281-9310`：splitObject 全模型深拷贝 + `newObjects` 永不 delete（上游用后即删）+ 盘成员索引不平移。
- P1-30 `ProjectServiceMock.cpp:6013-6025`：undo 快照 Phase 2a 按 `indexOf(name)` 对账，**重名对象删错**。方向：对象稳定句柄（ObjectID）参与快照。
- P1-31 `ProjectServiceMock.cpp:1056-1178`：loadFile worker 线程直写 receiver 的 QList/QImage 成员（非原子），cancelLoad 后 GUI 读取即数据竞争。
- P1-32 `ProjectServiceMock.cpp:5327-5330`：undo 快照临时文件写 `applicationDirPath()`（只读安装目录下 capture 失败 → 删盘 undo 静默退化为无网格恢复）。方向：QTemporaryDir。
- P1-33 `ProjectServiceMock.cpp:5599-5753`：restoreVolumeSnapshot 仅存网格+类型+挤出机+矩阵，涂色/缝线/MMU FacetsAnnotation、文字/SVG 元数据全丢 → 删除带涂色 volume 后 undo 恢复为裸网格。

### R8 其余 services
- P1-34 `UndoRedoManager.cpp:58-85`：`m_heavyBytes` 只增不减（QUndoStack 驱逐旧命令无回调扣减）→ 必然"假超限" `clear()` **整段清空仍有效的 undo 历史**；且可在 beginMacro/endMacro 之间触发，破坏宏配对；`.h:66-70` 注释（"缩小 undoLimit 逐出最旧"）与实现（全清）矛盾。R11 注：WIP 新测试 `undoMemoryBudgetEvictsOldestHeavyCommands` 把"超限全清"锁为正确行为——语义降级被测试固化，需一并修正。
- P1-35 `SliceService.cpp:697-741`：`cloneCurrentPlateModel()` 失败返回 nullptr 被当"复用上一轮"哨兵 → 用旧几何模型切片静默过期。方向：payload 显式 reuse 布尔。
- P1-36 `JobManager.cpp:59-60` + `JobBase.cpp:29-38`：job `setAutoDelete(false)` 后所有完成分支不 deleteLater → 每次 enqueue 泄漏一个 QObject；`reportError()` 不中止 run() → finished 双重计数可为负。

### R9 Renderer
- P1-37 `RhiViewportRenderer.cpp:1359-1540`（WIP）：`releaseResources()` 遗漏 `m_supportPreviewBuffer` 及 bytes/count/uploaded 标志 → RHI 重建后绑定已失效资源 + 永不重传（唯一可能崩溃点）。
- P1-38 `RhiViewport.cpp:2398-2403`：HEIGHT_RANGE 仅 press 瞬间路由，拖拽落入普通笔刷分支 → 错误涂刷会进入切片（上游为 cursor 类型，拖拽同效）。
- P1-39 `RhiViewportRenderer.cpp:2911-3000, 2681-2695`（WIP）：uploadModelBuffer 失败半途留下新旧混合状态；translucent 回退分支把 index 偏移当 vertex 偏移错画。
- P1-40 `SoftwareViewport.h:443-485`：画笔/量测/切割等交互信号**声明但全文件无 emit** → 软件/远程模式下画笔 gizmo 点击静默无效；`SoftwareViewport.cpp:800` batchCount>10000 即 return（producer 上限 100000）、`plateAnchors` 为 CONSTANT 无 NOTIFY（RhiViewport 有 Changed 信号）。

### R10 core 渲染/AI
- P1-41 `EditorViewModel.cpp:4927-4941` + `PaintEngine.cpp:222-228`：`paintHeightRangeAt` 把 **trafoNoTranslate** 传给 cursor_factory，上游传**全量 trafo**（GLGizmoPainterBase.cpp:731；TriangleSelector.cpp:1103 用 `trafo*point` 的 z 与世界 z 比对）→ 物体有 Z 平移时高度带整体错位；Circle 光栅方向同根受染（:1940-1951）。方向：factory 收全量 trafo、select_patch 收 no-translate，与上游拆分一致。
- P1-42 `McpHttpServer.cpp:103-107`：Content-Length 缺失/非法或 body 永不齐时 socket 永久挂起无超时（本地进程可耗尽句柄）。方向：非法即 400 + 每 socket 挂 30s 定时器。

### R11 WIP changeset
- P1-43 `EditorViewModel.cpp:10674+`（WIP）：`onSupportPreviewReady` 未校验 `segmentVerts.size()==segmentCount*6` → 畸形 payload 在渲染端定长消费越界读。
- P1-44 `main_qml.cpp:86-117`（WIP）：进程名扫描检测到本机装有 ToDesk/向日葵等即**永久钉死软件渲染**（本机本地会话也命中），性能悬崖且无 UI 提示（仅 OWZX_FORCE_RHI=1 逃生门）。
- P1-45 工程管理：单个未提交 changeset 混杂 ≥6 个主题（perf Wave2b/3a/4a/4b、P2.1/2.3/2.7、P4.1/4.3、P7.1/P11.B2.3、v5.15/5.16），blob v1→v2 格式变更与功能修复交织 → 回滚/二分困难。建议按主题拆分提交。

## 6. P2 摘要（择要，完整清单见各分区评审记录）

- **上游对齐小偏差（建议集中登记"已知偏差清单"）**：绘画 cursor 默认 Circle（上游 SPHERE）、gap_area 默认 1.0（上游 0）、高度范围步长 0.1（上游 0.2）、duplicateObject 固定 +20mm X 且落在当前盘、G92 仅处理 E、整圆 G2/G3 丢失、梯形时间模型减速分支符号错、layer_time 为部分累计值、G4 dwell 不进滑条时间轴、Preset 兼容性条件整判不兼容 + inherits 只回溯一级、.ini 导入无 schema 校验、相机默认方位/fitView 复位/zoom 远界近似。
- **健壮性**：CrashHandlerWin 栈溢出时在耗尽栈上执行 Qt 分配（拿不到 dump）、rayXZIntersect 背面交点、BVH 栈满静默丢弃、AI 工具路径无工作目录约束、AiAgentService `waitForStarted(5000)` 阻塞 GUI、applySlicePreview 角色无钳制、Monitor 状态机过粗、`ProjectViewModel::saveProject()` 不写文件即清 dirty（假成功）、exportModel OBJ 忽略 WriteOBJFile 返回值、裸版 cutObject 切平面在 mesh-local 空间且不登记盘、loadFile 重建不发 plateRemoved（旧 printId 派生状态孤儿）、meshData v2 不再校验索引越界、直通预览路径丢失静默时间/价格（29.99 兜底报价未确认）。
- **UI/控件一致性**：硬编码颜色（CalibrationDialog/PrintDialog/AboutDialog 等 10+ 处）、伪按钮（Rectangle+MouseArea）无键盘可达性、CxMenu 200 魔数两处重复、CxPopup modeless 全屏遮罩、CxPillAction 为无调用点死控件、Skirt/Brim 同译"裙边"、statusText_ 中英混杂未走 tr()、FPS 覆盖层与导航立方体位置重叠。
- **行为/打磨**：主题偏好近乎假功能（只改 shell 4 色不接 Theme 单例）、requestSelectTab 可切入 7 号空页、备份定时器初始间隔未夹紧（0 → 事件循环级写盘轰炸）、导出路径未 decodeURIComponent、拖放导入无 isBusy 门禁且后缀分流业务在 QML、MonitorPage 刷新 Timer 常驻且注释与实际（3000ms）不符、ProjectPage"保存"与"另存为"等价 + 文件树无点击处理、函数级 static FPS 计时器跨实例串味、stash setter 无相等守卫、PerfBench `/FORCE:MULTIPLE` 坏味道。
- **死代码**：BBLTopbar 隐形 TabBar（:319-393）、topbarOpenProject 失败重试分支（BackendContext.cpp:963-972）、LeftSidebar `bedTypeName`/`exportRequested`、ErrorToast hover 三元同分支、CalibrationDialog 硬件校准死状态节、`src/core/DelayLoadHook.cpp`（已知）、legacy Widgets UI（OWZX_QML_GUI=OFF 分支）。

## 7. 测试可信度评估

1. **QmlUiAuditTests（174 用例）**：2578 个 QVERIFY 中约 93% 是 `QString::contains` 源码字符串断言——证明"代码存在"而非"代码正确"。假绿模式：注释锚点当断言（:10187/:10141）、泛化字符串（:3197 `state < 0`）、自引用审计（:4521 断言测试自身存在）、逐字符锁死表达式（等价重构假红、语义回归假绿）。
2. **ViewModelSmokeTests（185）/ E2EWorkflowTests（30）**：真实行为测试、质量高（WIP 的 Paint 系列带 `indexed_triangle_set` 断言、printId、BVH 等价性测试是最佳实践），但 **100% 绕过 QML 直调 C++**（0 个 QQmlEngine）→ QML 绑定/信号转发层运行时零覆盖；WIP 的切片直通预览通路（applySlicePreview）零测试。
3. **parity 基线**（`tests/data/upstream_parity_baseline.json`，24 锚点）：有自动校验消费（upstreamParityBaselineEntriesResolveInSource）防静默腐烂，但只验存在性，且 :10212 只要求 ≥20 条。
4. **E2EWorkflowTests:1884**：首次切片失败走 QSKIP → 环境坏了静默变 skip，应至少计数告警。
5. **结论**：两套体系互补但各有一侧盲区——"QML 层行为正确性"当前没有任何运行时防线。建议中期补 QML 引擎测试基建（发布就绪度.md 下一步 3 已登记同项）。

## 8. Mock 假成功与登记表原则冲突清单

对照 `docs/依赖与协议边界审计.md` "不允许 mock 假成功 + 用户可见不可用披露 + 防复活测试"三原则：

| API | 行为 | 用户可见披露 | 判定 |
| --- | --- | --- | --- |
| CloudServiceMock::login/bindDevice/syncPresets | 恒成功 + 伪造绑定设备 | 无（HomePage.qml:403/492 登录路径可达） | **违规** |
| DeviceServiceMock::connectDevice | 1.5s 后自动"在线" | 无 | **违规** |
| DeviceServiceMock::startPrint/pause/stop | 伪造打印任务 + 温度仿真定时器 | 无（MonitorPage.qml:1256 可达） | **违规** |
| CameraServiceMock::startStream | 无真实流也推进 Connected→Streaming | 仅代码注释 | **违规** |
| PluginService::install/uninstallPlugin | 仅翻转标志，无下载 | 头注释有、UI 未验证 | 存疑 |
| MultiMachineViewModel 发送任务 | 2s 假"Task sent"+printing | 仅 add/edit 两条 "(mock)" toast | **违规** |
| SliceService 无 libslic3r mock 分支 | 伪造时间/重量 | 输出名带 "(mock)" 前缀 | 可接受 |
| PresetServiceMock::bedTypesForPrinterModel | 静态 4 表面列表 | 代码注释声明数据缺口 | 可接受 |

另：`docs/发布就绪度.md` 旧表将 Model Mall 记为"78% 可用（Mock）"，与 MALL-01 移除锁定冲突（本次已修订）。

## 9. 现有差距（更新后口径）

### 9.1 新发现差距（本次评审新增登记，此前未在账本中）
1. **关闭/保存门禁击穿**（P0-6、P1-1）：最高频用户路径的安全门禁只在部分入口生效。
2. **WIP 线程模型缺口**（P0-1/2、P1-31/35/43）：专用切片线程引入的共享可变状态与取消/析构窗口。
3. **项目数据完整性**（P0-4/5、P1-27/30/32/33）：另存为 writer 分叉、visible/printable 混淆、undo 快照对账/字段缺失。
4. **Preview 正确性组**（P1-20/21/23 + P2 解析组）：单位量纲、G2 前缀、播放状态、直通路径统计。
5. **打印/发送假成功链**（P1-8、P1-24、第 8 节）：PrintDialog 读不存在属性、Monitor/MultiMachine/Cloud 假成功无披露。
6. **MMU HeightRange 双缺陷**（P1-6、P1-38、P1-41）：tool/cursor 语义、拖拽路由、trafo 语义三处叠加。
7. **预设/向导假完成**（P1-10/12、P1-9）：向导完成不入库、覆盖保存不可达、BedShape 取消不回滚。
8. **Q_INVOKABLE 一次性绑定系统性陈旧 UI**（P1-13）：菜单/进度/侧栏多处显示不刷新，是本分区最大结构性风险。
9. **测试假绿面**（第 7 节）：QML 运行时零覆盖 + 审计存在性断言。
10. **WIP 回归组**（P1-22/26/44/45 + 第 2 节 ctest 2 失败）：需在拆分提交时逐一闭环。

### 9.2 已登记差距（存量账本，状态不变）
- UI-Axx declined 13 项（locale 截断、主题 token 化等视觉硬化，沿用原 ID 追踪）。
- 主账本 49 项 `[-]`：集中在 P0.5.5b（Print/G-code 结果完整生命周期）、P2.3 残余差异（TriangleSelectorPatch 级 edge_detection 视觉、支撑体积自动生成收尾）、P2.7（Emboss use_surface/curve_projection、AdvancedCut keep 语义）、P3 预览端到端细节、P5.3 相机（外部阻塞）等。
- `[ ]` 3 项：P2.8.2 Shell 渲染（P17.5 已收口 shells，此项待复核）、P12.2 子模块推送（待授权）、P17.8 冲突标记（需切片侧 conflict 通道）。
- 相机已知偏差（第 6 节）建议并入 P14 偏差清单，避免与 1:1 口径矛盾。

### 9.3 阻塞豁免（维持《依赖与协议边界审计》登记表）
OpenVDB（Hollow/SLA）、FFmpeg/WebRTC/MetaRTC（视频流）、`bambu_networking`（Model Mall/云，MALL-01 防复活）、Paho MQTT 真机连接、固件 OTA、SD 卡文件系统、cr_tpms（部署时删除）、CGAL 5.4（mesh boolean/cut surface 上限）、GCodeProcessor 无 conflict 通道。

## 10. 仓库卫生与未跟踪文件处置建议（仅登记，本次未改动）

| 类别 | 内容 | 建议 |
| --- | --- | --- |
| 压扁文件名副本 | 根目录 ~19 个 0 字节、文件名含 `\r`（`srccoreaiAppToolRegistry.cpp`、`srcqml_guiBackendContext.cpp` 等，mtime 2026-08-31 03:58，脚本事故产物，非内容拷贝） | 删除（`find -name '*\r'` 处理） |
| 会话残留 | 根目录 `_*.png/_*.ps1/_*.log/_*.zip/_l5d/_orbit*/`、`build_*.log`（~百个）、`00000.log`、`vpctest*.txt`、`cdb_2m*.txt` | 删除或 .gitignore（`_*/`、`_*.{png,log,zip,ps1,txt}`） |
| AI 调试产物 | `.planning/debug/` 下 `probe_*.json`、`req02_body.json`、`anthropic_*.py`、`anthropic_proxy_log/`、`drive_agent_*.py` | 不入库，补 gitignore |
| 应提交的新工作产物 | `tests/PerfBench.cpp`、`src/core/util/PerfLog.h`、`scripts/perf/`、`tests/data/upstream_parity_baseline.json`、`.planning/phases/227-*/227-CONTEXT.md`、`.planning/debug/*.md`（崩溃排查笔记） | 随拆分后的对应主题提交 |
| 运行产物 | `tests/param_regression/__pycache__/`（.py 源在侧分支 `codex/param-regression-automation`，非 HEAD 祖先）、`tests/output/` | 本地清理，不入库 |
| 文档漂移 | AGENTS.md 仍描述 GLViewport/GLViewportRenderer/GCodeRenderer（QQuickFramebufferObject）、ModelMallViewModel、"强制 QSGRendererInterface::OpenGL"；实际为 RhiViewport/SoftwareViewport + RhiBackendSelector（D3D11-first/软件回退）；QT_LOGGING_RULES 亦不一致 | 修订 AGENTS.md stack/entry-point 段 |

## 11. 建议收口顺序

1. **提交 WIP 前（P0）**：修 P0-1~6（约集中在 SliceService/EditorViewModel/ProjectServiceMock/main.qml+BBLTopbar 六处，改动量小）。
2. **ctest 复绿**：排查 `testSliceBlock20XY`（-3）是否 WIP 直写路径回归；修复或迁移 InventoryAuditTests 的清单路径。
3. **拆分提交**：按主题（perf Wave2b/3a/4a/4b、P2.x、P4.x、v5.15/5.16）拆分当前 changeset 后分别提交。
4. **P1 批次一（正确性）**：undo 健全性（P1-34 + 测试语义修正）、Preview 单位/G2/播放（P1-20/21/23）、镜像平移/泄漏（P1-27/28/29）、WIP 回归（P1-22/26）。
5. **P1 批次二（假成功治理）**：按第 8 节给 4 处违规补用户可见披露 + 防复活测试；接通或显式披露打印发送链（P1-8）。
6. **结构性改造**：Q_INVOKABLE 一次性绑定 → NOTIFY 属性（P1-13 系统性）；死代码处置（ObjectList/SliceProgress/隐形 TabBar/CxPillAction）。
7. **长期**：QML 运行时测试基建；EditorViewModel 巨类下沉（blob 解析/裁剪面/警告检查 → service/geometry 层）；仓库卫生批量清理。

---

*评审执行：11 个只读评审子代理（分区：组合根/Shell、pages、dialogs+controls、panels+components+Models、EditorViewModel、其余 viewmodels、ProjectServiceMock、其余 services、Renderer、core 渲染/AI、测试+WIP diff）+ ctest 实测；汇总交叉比对三层进度账本。所有问题均带 file:line 证据，可直接定位。*
