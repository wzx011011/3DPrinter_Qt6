# OrcaSlicer 源码真值基线

> 更新日期：2026-07-03（Phase 57-02：上游路径从 CrealityPrint 重写为 OrcaSlicer；历史项目名 CrealityPrint 保留为本节说明。）
> 用途：冻结本仓库当前采用的上游 OrcaSlicer 功能真值版本，作为后续所有迁移、对照、回归和任务验收的统一基线。

## 冻结结论

| 项目         | 值                                         |
| ------------ | ------------------------------------------ |
| 上游仓库路径 | `third_party/OrcaSlicer`                   |
| 锁定方式     | Git tag + commit                           |
| 锁定 tag     | `v7.0.1`                                   |
| 锁定 commit  | `0d4ac73a6f3224a2bf753d7b9e67d7d515bc8557` |
| 锁定日期     | 2026-03-10                                 |

## 版本观察

- 通过 `git describe --tags --always` 获取到的上游标签为 `v7.0.1`。
- 通过 `git rev-parse HEAD` 获取到的上游提交为 `0d4ac73a6f3224a2bf753d7b9e67d7d515bc8557`。
- `third_party/OrcaSlicer/version.inc` 中仍可见 `ORCASLICER_VERSION "7.0.0"` 与 `PROJECT_VERSION_EXTRA "Alpha"` 默认值；该文件属于构建脚本使用的版本配置，不能替代 Git 冻结基线。

## 使用规则

从本文件建立起，以下规则生效：

1. 所有“与上游一致”或“功能真值”表述，默认都指向 `v7.0.1` / `0d4ac73a6f3224a2bf753d7b9e67d7d515bc8557`。
2. 若后续需要升级真值版本，必须先更新本文件，再更新 [docs/源码对照迁移任务追踪.md](源码对照迁移任务追踪.md) 相关任务和验收口径。
3. 若某功能来源于上游不同提交、分支或后补 patch，必须在对应任务中显式标注，不得隐式混用。

## 当前影响

- `docs/源码对照迁移任务追踪.md` 中 `P0.1` 可以据此记为已完成。
- 后续 `P0.2` 功能矩阵的所有上游模块引用，都应以本文件冻结的基线为准。
- 架构文档、结构文档和 prompt / agent 规则中的“上游真值”均以本文件为补充说明。
