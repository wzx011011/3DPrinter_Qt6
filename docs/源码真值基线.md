# OrcaSlicer 源码真值基线

> 更新日期：2026-07-21（Quick Task 260721-jjs：重建到官方 `upstream/main` 的确切提交）
> 用途：冻结本仓库当前采用的上游 OrcaSlicer 功能真值版本，作为后续所有迁移、对照、回归和任务验收的统一基线。

## 冻结结论

| 项目 | 值 |
| --- | --- |
| 上游仓库路径 | `third_party/OrcaSlicer` |
| 官方引用 | `upstream/main` |
| 官方锁定 commit | `8b93cc5df3347c657ce6ac9a58f6923a21c2959b` |
| 官方 commit 日期 | 2026-07-21 |
| OWzx 本地分支 | `owzx-upstream-2026-07-21` |
| OWzx 兼容子提交 | `4cb3b9ce792f15c38fabfb4bb9700895d32b1166` |
| 最新稳定 tag（仅供参考） | `v2.4.2` / `8500fcdccaa10b5099ac20d252af3a7c560046f1` |
| 基线日期 | 2026-07-21 |

## 版本观察

- 官方行为真值按 `upstream/main` 的确切 commit 锁定，不按浮动分支名或最新 tag 推断。
- `v2.4.2` 是基线日期可见的最新稳定 tag，只用于版本观察，不是本仓库的行为锁定。
- OWzx 子提交以官方 commit 为直系祖先，只增加下述两类构建兼容差异。

## 保留的 OWzx 兼容差异

1. `src/libslic3r/Config.hpp`：为 `ConfigOptionEnumGeneric` 和 `ConfigOptionEnumsGenericTempl` 保留 null-`keys_map` 保护。无映射表时序列化回退为数字，符号反序列化返回 `false`。对应本地 commit `ad3135ce0dd08987734e629d9c1c8920f0a98e4d`。
2. `src/libslic3r/MeshBoolean.cpp`：使用显式 face/vertex output iterator 的 `triangulate_and_refine_hole` 重载，保持当前 CGAL 5.4 构建兼容。对应本地 commit `4cb3b9ce792f15c38fabfb4bb9700895d32b1166`。

`git diff --name-only 8b93cc5df3347c657ce6ac9a58f6923a21c2959b...4cb3b9ce792f15c38fabfb4bb9700895d32b1166` 必须严格等于上述两个文件。

## 可复现性状态

- 子模块分支与两个兼容 commit 已在本地创建。
- 用户尚未授权推送 `owzx-upstream-2026-07-21`，因此该子提交尚未证明可从配置的 fork 访问。
- 在 `git ls-remote origin refs/heads/owzx-upstream-2026-07-21` 返回确切子 SHA，且新鲜父仓库 clone 能完成 `git submodule update --init third_party/OrcaSlicer` 前，不得提交或发布父仓库 gitlink。

## 使用规则

从本文件建立起，以下规则生效：

1. 所有“与上游一致”或“功能真值”表述，默认都指向官方 `upstream/main` commit `8b93cc5df3347c657ce6ac9a58f6923a21c2959b`；OWzx 构建使用其上仅含两类兼容差异的子提交 `4cb3b9ce792f15c38fabfb4bb9700895d32b1166`。
2. 若后续需要升级真值版本，必须先更新本文件，再更新 [docs/源码对照迁移任务追踪.md](源码对照迁移任务追踪.md) 相关任务和验收口径。
3. 若某功能来源于上游不同提交、分支或后补 patch，必须在对应任务中显式标注，不得隐式混用。

## 当前影响

- `docs/源码对照迁移任务追踪.md` 中 `P0.1` 可以据此记为已完成。
- 后续 `P0.2` 功能矩阵的所有上游模块引用，都应以本文件冻结的基线为准。
- 架构文档、结构文档和 prompt / agent 规则中的“上游真值”均以本文件为补充说明。
