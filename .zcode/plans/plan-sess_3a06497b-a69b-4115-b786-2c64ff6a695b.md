## 修复 + UI 中度完善方案（单文件，无外部依赖）

### A. 缺陷修复
1. **删除重复的 `</main>` 标签**（`index.html:1816`，当前为 `</main>  </main>`）

### B. 启用顶部锚点导航（当前 `hidden`，未使用）
2. 去掉 `<nav class="nav" hidden>` 的 `hidden` 属性，让顶部锚点导航可见
3. 调整 `.nav` 与 `.brand`/`.actions` 在顶栏的排布，窄屏自动换行
4. JS：点击 nav 锚点时自动切换到对应 Tab 并滚动（现有 `activateFromHash` + `sectionToTab` 已支持 hash 联动，无需重写，仅验证/微调）
5. 高亮当前所在 Tab 对应的 nav 项（与 Tab 状态联动）

### C. 键盘可达性（WAI-ARIA tab pattern）
6. Tab 按钮：选中项 `tabindex="0"`，其余 `tabindex="-1"`
7. Tab 容器监听方向键（←/→/↑/↓），实现相邻 Tab 切换
8. 全局统一 `:focus-visible` 焦点轮廓（teal 色 outline），覆盖按钮/链接/输入框/checkbox

### D. Hero 指标卡布局优化
9. 桌面端 `.hero-grid` 由 4 列改为 **3 列 × 2 行**（6 个 metric 排列更整齐；窄屏自动降为 2 列/1 列）
10. 每个 `.metric` 的数字（`<b>`）按类别加强调色（3集/7天=teal，9:16/1套=blue，本地/云端=coral/gold），提升信息层级

### E. 进度条 + 计算器交互反馈增强
11. 进度条按完成度分段着色：<33% coral、33–66% gold、>66% green；并加里程碑文字（如「已过半」）
12. ROI 计算器的 `#verdict` 按建议类型显示不同背景色与图标前缀：
    - 继续扩写 → 绿
    - 重做开头 → 橙
    - 换标题/换题材 → 红
    - 先修合规 → 红警告
    - 改版样片 → 蓝

### F. 统一卡片视觉层级
13. 给 `.card` 加左侧 3px 强调条，颜色取该卡 `.tagline` 的色系（teal/blue/coral），增强分类识别
14. 统一 `.card`/`.step`/`.check-card`/`.episode`/`.status-card` 的 hover 与过渡（轻微上浮 + 阴影），保持一致交互语言
15. `.section-head h2` 加左侧短色条装饰，强化章节锚点感

### G. 细节打磨
16. checkbox 选中时，对应 `.task` 文字加轻微淡化 + 左侧勾选色条
17. 响应式断点复核：980px/680px 下 nav、hero-grid、verdict 颜色块等均正常
18. 打印样式复核：新增的色条/图标在打印下不破坏布局

### 验证方式
- 浏览器打开本地 `index.html`，逐项点击验证：Tab 切换、方向键、nav 锚点联动、进度条分段色、计算器 5 种 verdict 颜色、响应式三档、打印预览
- 用 WAVE/Automation 不强制；以肉眼 + 键盘 Tab 键走查为主

### 不做的事（控制范围）
- 不引入图标库/字体 CDN
- 不重塑整体配色与排版系统
- 不新增 Tab、不改信息架构
- 不改动 `localStorage` 存储结构（避免清空已有勾选进度）