# Roadmap: v3.7 Screenshot-Level UI Parity Closure

## Goal

Close the visual parity gap left by v3.6. The target is not more structural
coverage; the target is running-app visual parity against the four screenshots
in `shotScreen/`, with OrcaSlicer upstream behavior preserved where controls
are interactive.

## Verification Rule

Per user instruction, phases 59-63 are executed as a batch. During those phases
only source reads, encoding checks, and focused static checks are allowed.
The canonical full build and visual acceptance run happen once, after all phase
edits are complete.

## Success Criteria

- Prepare page, Preview page, Printer Settings, and Material Settings match the
  supplied screenshots at module layout, spacing, density, and visible-state
  level.
- No visible in-scope UI shows unavailable placeholder copy.
- Every remaining unavailable control in scope is either hidden or disabled in a
  way that matches the source-truth screenshot behavior.
- The final run uses only:
  `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
- The final visual acceptance captures running-app screenshots and compares
  them against:
  - `shotScreen/准备页.png`
  - `shotScreen/预览页.png`
  - `shotScreen/打印机参数设置页.png`
  - `shotScreen/材料参数设置页.png`

## Phases

- [ ] **Phase 59:** v3.6 status correction and visual baseline matrix
- [ ] **Phase 60:** Prepare sidebar and viewport chrome parity
- [ ] **Phase 61:** Settings dialog compact OrcaSlicer tab layout parity
- [ ] **Phase 62:** Preview right panel, sliders, and G-code text parity
- [ ] **Phase 63:** Placeholder removal, static audit hardening, and visual UAT assets
- [ ] **Phase 64:** Unified canonical build and running-app visual acceptance

## Scope Fence

In scope:
- `src/qml_gui/pages/PreparePage.qml`
- `src/qml_gui/pages/PreviewPage.qml`
- `src/qml_gui/panels/LeftSidebar.qml`
- `src/qml_gui/dialogs/SettingsDialog.qml`
- Shared visual controls needed by those surfaces
- Static audit tests that lock visible parity and forbid placeholder regressions

Out of scope:
- Changing libslic3r slicing algorithms
- Device send/cloud/Monitor workflows
- AssembleView completion
- D3D12/Vulkan backend work
- Broad unrelated cleanup outside the screenshot-visible surfaces
