# Plan 78-01: Prepare Final Cleanup And Verification

## Goal

Close v3.9 by locking restored Prepare paths with automated source/QML audits, removing the remaining startup diagnostics warning found during verification, running the canonical verifier, launching the app, and recording visual evidence.

## Steps

1. Add a Phase 78 QML audit test that verifies restored Prepare files are in `qml.qrc`, stale Prepare paths are absent from resources and disk, Phase 75-77 audit coverage remains present, and final restored binding tokens remain wired.
2. Fix the `CxTextArea.qml` invalid `ScrollBar.vertical` attachment that pollutes runtime diagnostics.
3. Run focused source checks, `git diff --check`, and the encoding guard.
4. Run the canonical verifier:
   `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
5. Launch `build/OWzxSlicer.exe`, capture runtime visual evidence, inspect diagnostics, and record the result.
6. Update ROADMAP, STATE, REQUIREMENTS, and Phase 78 summary/review artifacts.
7. Stage and commit the Phase 78 cleanup/verification closure.

## Verification

- `QmlUiAuditTests` contains the new Phase 78 source audit.
- `git diff --check`
- `python %USERPROFILE%\.coding-encoding-guard\encoding_guard.py`
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
- Runtime launch of `build/OWzxSlicer.exe`
- Runtime screenshot saved under this phase directory.

## Acceptance

- CLEAN-01, VERIFY-01, and VERIFY-02 are complete.
- v3.9 ROADMAP and STATE show the milestone complete.
- No new stale Prepare UI path is left behind.
