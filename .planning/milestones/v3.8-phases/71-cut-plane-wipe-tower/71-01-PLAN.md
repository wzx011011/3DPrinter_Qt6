---
phase: 71-cut-plane-wipe-tower
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/rendering/GizmoGeometry.h
  - src/core/rendering/GizmoGeometry.cpp
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/pages/PreparePage.qml
  - tests/GizmoGeometryTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [GCUT-01, GWT-01]
user_setup: []

must_haves:
  truths:
    - "Cut plane geometry is generated by pure CPU GizmoGeometry helpers and tested without GL/RHI."
    - "Cut plane fill uses two triangles, axis color alpha 0.30, selected-bounds expansion of five percent, and cutAxis/cutPosition placement."
    - "Cut plane outline is separate line geometry with alpha 0.90."
    - "RhiViewportRenderer draws the cut plane only in Cut or AdvancedCut gizmo mode and only when a source object is selected."
    - "Wipe tower geometry is generated by pure CPU GizmoGeometry helpers as a 36-vertex translucent box on bed y=-0.04."
    - "RhiViewportRenderer::synchronize reads showWipeTower, width, depth, height, X, and Z from RhiViewport and marks wipe geometry dirty on changes."
    - "RhiViewportRenderer uses an alpha-blended no-depth-write fill pipeline for translucent cut/wipe surfaces."
    - "PreparePage only binds cut/wipe values into the viewport; it does not own geometry, bbox, or QRhi pipeline logic."
  artifacts:
    - path: "src/core/rendering/GizmoGeometry.cpp"
      provides: "Pure CPU cut plane and wipe tower vertex generation"
      contains: "buildCutPlaneVertices"
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      provides: "RHI cut plane and wipe tower upload/draw integration"
      contains: "renderCutPlane"
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      provides: "Wipe tower property synchronization"
      contains: "m_showWipeTower = viewport->m_showWipeTower"
    - path: "tests/GizmoGeometryTests.cpp"
      provides: "Geometry regression tests for cut plane and wipe tower"
      contains: "testCutPlaneGeometry"
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Source-audit guard for RHI cut/wipe ownership boundaries"
      contains: "rhiCutPlaneAndWipeTowerStayCppOwned"
---

<objective>
Complete cut plane and wipe tower rendering parity on the default D3D11 RHI
path. The cut plane updates when `cutAxis` or `cutPosition` changes, and the
wipe tower renders as a translucent box when scene properties say it is
present.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-cut-plane-wipe-tower/71-CONTEXT.md
@.planning/phases/70-rotate-scale-gizmos/70-CONTEXT.md
@src/core/rendering/GizmoGeometry.h
@src/core/rendering/GizmoGeometry.cpp
@src/core/rendering/GizmoVertex.h
@src/qml_gui/Renderer/RhiViewportRenderer.h
@src/qml_gui/Renderer/RhiViewportRenderer.cpp
@src/qml_gui/Renderer/RhiViewport.h
@src/qml_gui/Renderer/RhiViewport.cpp
@src/qml_gui/Renderer/GLViewportRenderer.cpp
@src/qml_gui/pages/PreparePage.qml
@tests/GizmoGeometryTests.cpp
@tests/QmlUiAuditTests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for cut/wipe geometry and RHI ownership</name>
  <files>tests/GizmoGeometryTests.cpp, tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - tests/GizmoGeometryTests.cpp
    - tests/QmlUiAuditTests.cpp
    - src/core/rendering/GizmoGeometry.h
    - src/qml_gui/Renderer/RhiViewportRenderer.cpp
    - src/qml_gui/pages/PreparePage.qml
  </read_first>
  <action>
    Add `GizmoGeometryTests` coverage for:
    - `testCutPlaneGeometry()`: builds fill and outline for bounds
      min `(-10, -5, 0)`, max `(30, 15, 40)`, axis `2`, position `12.5`;
      asserts 6 fill vertices, 8 outline vertices, all fill `z == 12.5`,
      X/Y extents expanded by five percent, fill alpha `0.30`, and outline
      alpha `0.90`.
    - `testCutPlaneAxisColors()`: verifies X/Y/Z fill colors are red/green/blue
      with alpha `0.30`.
    - `testWipeTowerGeometry()`: builds a tower at X `100`, Z `25`, width `20`,
      depth `10`, height `50`; asserts 36 vertices, x range `90..110`, z range
      `20..30`, y range `-0.04..49.96`, and alpha `0.50`.
    - `testWipeTowerRejectsInvalidDimensions()`: width/depth/height values
      less than or equal to zero return no vertices.

    Add `QmlUiAuditTests::rhiCutPlaneAndWipeTowerStayCppOwned()`:
    - Assert `RhiViewportRenderer.h` declares cut/wipe buffers, draw helpers,
      and dirty state.
    - Assert `RhiViewportRenderer.cpp` reads all wipe tower properties in
      `synchronize()`, uses `buildCutPlaneVertices`, `buildCutPlaneOutlineVertices`,
      and `buildWipeTowerVertices`, has alpha blending configured through
      `QRhiGraphicsPipeline::TargetBlend`, and gates cut rendering to modes
      5 and 14 with a selected object.
    - Assert `PreparePage.qml` binds `cutAxis` and `cutPosition`, and does not
      contain geometry or pipeline tokens such as `buildCutPlane`, `buildWipeTower`,
      `QRhi`, `bboxMin`, or `bboxMax`.
  </action>
  <verify>
    <automated>cmake --build build --target GizmoGeometryTests QmlUiAuditTests</automated>
  </verify>
  <acceptance_criteria>
    - The targeted build fails before implementation because `GizmoGeometry`
      does not expose the new cut/wipe builders and the RHI audit symbols are
      absent.
    - Tests are deterministic and do not need a live renderer or GPU.
  </acceptance_criteria>
  <done>RED tests exist and fail for the missing Phase 71 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure cut plane and wipe tower geometry builders</name>
  <files>src/core/rendering/GizmoGeometry.h, src/core/rendering/GizmoGeometry.cpp</files>
  <read_first>
    - src/core/rendering/GizmoGeometry.h
    - src/core/rendering/GizmoGeometry.cpp
    - tests/GizmoGeometryTests.cpp
  </read_first>
  <action>
    Add static helpers to `GizmoGeometry`:
    - `buildCutPlaneVertices(const QVector3D &boundsMin, const QVector3D &boundsMax, int cutAxis, float cutPosition)`
    - `buildCutPlaneOutlineVertices(const QVector3D &boundsMin, const QVector3D &boundsMax, int cutAxis, float cutPosition)`
    - `buildWipeTowerVertices(float x, float z, float width, float depth, float height)`

    Clamp cut axis to `[0, 2]`, expand the two non-cut bbox axes by five
    percent, generate six fill vertices for two triangles, and eight outline
    vertices for four line segments. Use GL source-truth cut colors:
    X `{1.0, 0.3, 0.3, 0.30}`, Y `{0.3, 1.0, 0.3, 0.30}`, Z `{0.3, 0.3, 1.0, 0.30}`;
    outline alpha is `0.90`.

    Generate wipe tower geometry as the GL path's 36 triangle vertices. The
    base y is `-0.04`, the top is base plus height, X/Z ranges are centered
    around the provided X/Z with width/depth halves, and color is
    `{0.35, 0.60, 0.85, 0.50}`.
  </action>
  <acceptance_criteria>
    - All new `GizmoGeometryTests` pass.
    - Existing move/rotate/scale geometry tests remain unchanged and pass.
  </acceptance_criteria>
  <done>Cut/wipe geometry can be tested and consumed without renderer dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate cut plane and wipe tower into RhiViewportRenderer</name>
  <files>src/qml_gui/Renderer/RhiViewportRenderer.h, src/qml_gui/Renderer/RhiViewportRenderer.cpp, src/qml_gui/pages/PreparePage.qml</files>
  <read_first>
    - src/qml_gui/Renderer/RhiViewportRenderer.h
    - src/qml_gui/Renderer/RhiViewportRenderer.cpp
    - src/qml_gui/Renderer/RhiViewport.h
    - src/qml_gui/Renderer/RhiViewport.cpp
    - src/qml_gui/pages/PreparePage.qml
  </read_first>
  <action>
    Add renderer members for cut fill/outline buffers and wipe tower buffer:
    byte-size fields, vertex-count fields, upload-state booleans, and dirty
    flags. Add helper declarations and definitions for `uploadCutPlaneBuffers`,
    `uploadWipeTowerBuffer`, `renderCutPlane`, and `renderWipeTower`.

    Extend `synchronize()` to read `m_showWipeTower`, `m_wipeTowerWidth`,
    `m_wipeTowerDepth`, `m_wipeTowerHeight`, `m_wipeTowerX`, and
    `m_wipeTowerZ` from `RhiViewport`. Mark wipe tower dirty when any value
    changes. Mark cut plane dirty when gizmo mode, cut axis, cut position,
    selection, or model data changes.

    Extend pipeline setup with alpha blending for translucent fills. Use depth
    test on and depth write off. The existing mesh shader can be reused because
    cut/wipe vertices are already in scene coordinates. Upload cut/wipe buffers
    before `beginPass()` and draw after model/highlight geometry:
    - Cut fill with transparent fill pipeline, then cut outline with line
      pipeline or transparent line pipeline.
    - Wipe tower with transparent fill pipeline when `showBed` and
      `showWipeTower` are true.

    Ensure `PreparePage.qml` keeps thin bindings for cut axis/position and, if
    wipe tower properties are available from backend state, binds them without
    embedding geometry or QRhi logic. If no viewmodel wipe tower state exists,
    leave RhiViewport property API ready for existing/future dialog wiring and
    document that this phase renders when properties are present.
  </action>
  <acceptance_criteria>
    - `QmlUiAuditTests::rhiCutPlaneAndWipeTowerStayCppOwned` passes.
    - Cut render helper gates to modes 5/14 and requires a selected source
      object.
    - Wipe tower render helper requires bed visibility, showWipeTower, and
      non-empty geometry.
    - No geometry or QRhi implementation logic is added to QML.
  </acceptance_criteria>
  <done>RHI path renders cut plane and wipe tower from synchronized viewport state.</done>
</task>

<task type="auto">
  <name>Task 4: Verify and close Phase 71</name>
  <files>.planning/phases/71-cut-plane-wipe-tower/71-01-SUMMARY.md, .planning/phases/71-cut-plane-wipe-tower/71-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/71-cut-plane-wipe-tower/71-01-PLAN.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Run focused verification through the MSVC environment:
    - Build `GizmoGeometryTests` and `QmlUiAuditTests`.
    - Run the new geometry tests by name.
    - Run `QmlUiAuditTests::rhiCutPlaneAndWipeTowerStayCppOwned`.
    - Run `git diff --check`.
    - Run the encoding guard on touched files.
    - Run the canonical verifier:
      `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`.

    Write `71-01-SUMMARY.md` with implementation details, tests, deviations,
    and self-check. Write `71-VERIFICATION.md` with RED, focused, hygiene, and
    canonical verifier evidence. Mark Phase 71 complete through
    `gsd-sdk query phase.complete 71` and commit code plus planning artifacts.
  </action>
  <acceptance_criteria>
    - All focused tests pass.
    - Canonical verifier passes using only the project-approved command and
      `build/` directory.
    - Phase 71 is marked complete in ROADMAP/STATE/REQUIREMENTS.
  </acceptance_criteria>
  <done>Phase 71 is complete and committed.</done>
</task>

</tasks>

<verification>
- RED: `cmake --build build --target GizmoGeometryTests QmlUiAuditTests` fails
  on missing Phase 71 APIs before implementation.
- Focused: `GizmoGeometryTests` cut/wipe tests and
  `QmlUiAuditTests::rhiCutPlaneAndWipeTowerStayCppOwned` pass.
- Hygiene: `git diff --check` and global encoding guard pass.
- Canonical: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` passes.
</verification>

<success_criteria>
- GCUT-01: Cut plane renders as translucent fill plus outline and updates from
  `cutAxis`/`cutPosition`.
- GWT-01: Wipe tower renders as a translucent box when present, and
  `synchronize()` reads wipe tower properties from `RhiViewport`.
- QML remains a thin binding/forwarding layer for this behavior.
</success_criteria>
