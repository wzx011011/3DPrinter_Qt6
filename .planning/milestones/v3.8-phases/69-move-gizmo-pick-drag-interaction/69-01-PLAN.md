---
phase: 69-move-gizmo-pick-drag-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/Renderer/RhiViewport.h
  - src/qml_gui/Renderer/RhiViewport.cpp
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - src/qml_gui/pages/PreparePage.qml
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [GMOV-03, GMOV-04]
user_setup: []

must_haves:
  truths:
    - "RhiViewport mousePressEvent tests the move gizmo before object picking when gizmoMode is GizmoMove and a source object is selected."
    - "RhiViewport uses GizmoMath::computeRay, GizmoMath::pickMoveAxis, and GizmoMath::rayToAxisT for move-axis pick and drag delta math."
    - "A move-gizmo drag consumes press, move, and release events so camera orbit is not triggered during the drag."
    - "RhiViewport emits gizmoDragBegin, per-frame gizmoMoveRequested(QVector3D), and gizmoDragEnd signals."
    - "PreparePage forwards those RhiViewport signals to EditorViewModel begin/apply/end methods; QML does not own ray, intersection, or transform math."
    - "EditorViewModel applies incremental move deltas immediately but pushes exactly one TransformCommand for the whole drag on end."
  artifacts:
    - path: "src/qml_gui/Renderer/RhiViewport.h"
      provides: "Move-gizmo drag signals and state declarations"
      contains: "gizmoMoveRequested"
    - path: "src/qml_gui/Renderer/RhiViewport.cpp"
      provides: "Move-axis pick and drag event handling using GizmoMath"
      contains: "GizmoMath::pickMoveAxis"
    - path: "src/core/viewmodels/EditorViewModel.cpp"
      provides: "begin/apply/end move-gizmo drag coalescing"
      contains: "EditorViewModel::applyGizmoMoveDelta"
    - path: "src/qml_gui/pages/PreparePage.qml"
      provides: "Signal forwarding from GLViewport to EditorViewModel"
      contains: "onGizmoMoveRequested"
    - path: "tests/ViewModelSmokeTests.cpp"
      provides: "Undo-coalescing regression test"
      contains: "gizmoMoveDragCoalescesIntoSingleUndoCommand"
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Source-audit guard for RHI gizmo drag ownership boundaries"
      contains: "rhiMoveGizmoDragBridgeStaysCppOwned"
---

<objective>
Close the move-gizmo interaction loop on the default D3D11 RHI path. A user can press a rendered move-gizmo axis, drag along that axis, and have the selected object translate only on that axis without camera orbit firing. The drag is represented as one undo entry even when many per-frame deltas arrive.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-move-gizmo-pick-drag-interaction/69-CONTEXT.md
@src/qml_gui/Renderer/RhiViewport.h
@src/qml_gui/Renderer/RhiViewport.cpp
@src/core/viewmodels/EditorViewModel.h
@src/core/viewmodels/EditorViewModel.cpp
@src/qml_gui/pages/PreparePage.qml
@src/core/rendering/GizmoMath.h
@src/core/rendering/GizmoCenter.h
@tests/ViewModelSmokeTests.cpp
@tests/QmlUiAuditTests.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for move-gizmo drag coalescing and ownership</name>
  <files>tests/ViewModelSmokeTests.cpp, tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - tests/ViewModelSmokeTests.cpp
    - tests/QmlUiAuditTests.cpp
    - src/core/viewmodels/EditorViewModel.h
    - src/qml_gui/pages/PreparePage.qml
  </read_first>
  <action>
    Add `gizmoMoveDragCoalescesIntoSingleUndoCommand()` to ViewModelSmokeTests. It should create ProjectServiceMock, SliceService, EditorViewModel, and UndoRedoManager; select one primitive; call beginGizmoMoveDrag, apply two incremental deltas, and endGizmoMoveDrag. Assert the object position reflects the cumulative delta, one undo entry exists, undo restores the original position, and redo restores the dragged position.

    Add `rhiMoveGizmoDragBridgeStaysCppOwned()` to QmlUiAuditTests. It should assert RhiViewport declares the three drag signals, RhiViewport.cpp contains the GizmoMath pick/ray calls and accepts drag events, PreparePage forwards signals to EditorViewModel, and PreparePage does not contain ray/intersect/pickMoveAxis logic.
  </action>
  <verify>
    <automated>cmake --build build --target ViewModelSmokeTests QmlUiAuditTests</automated>
  </verify>
  <acceptance_criteria>
    - The targeted test build fails before implementation because the new EditorViewModel drag methods are not defined or the QML bridge is absent.
    - The two tests are narrow: no live renderer, no GPU, no file fixtures.
  </acceptance_criteria>
  <done>RED tests exist and fail for the missing phase 69 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EditorViewModel move-drag coalescing</name>
  <files>src/core/viewmodels/EditorViewModel.h, src/core/viewmodels/EditorViewModel.cpp</files>
  <read_first>
    - src/core/viewmodels/EditorViewModel.cpp
    - src/core/services/UndoCommands.h
    - src/core/services/UndoRedoManager.h
  </read_first>
  <action>
    Implement `beginGizmoMoveDrag()`, `applyGizmoMoveDelta(float dx, float dy, float dz)`, and `endGizmoMoveDrag()`.

    Begin records the selected source object and its starting position. Apply validates that the same object is still selected, applies an incremental world-space position delta immediately through ProjectServiceMock, invalidates current-plate slice results, and emits stateChanged. End compares the current position with the recorded start position and, if changed, pushes one TransformCommand from start to current while preserving rotation and scale.

    If apply is called outside an active drag, it should still move the selected object by the delta and push a single TransformCommand for that single call. This keeps the method robust when invoked from QML tests or future direct callers.
  </action>
  <acceptance_criteria>
    - ViewModelSmokeTests::gizmoMoveDragCoalescesIntoSingleUndoCommand passes.
    - Multiple apply calls between begin/end create exactly one undo step.
    - Undo and redo restore the expected positions.
  </acceptance_criteria>
  <done>EditorViewModel owns move-gizmo transform application and undo coalescing.</done>
</task>

<task type="auto">
  <name>Task 3: Finish RhiViewport move-axis pick/drag event handling</name>
  <files>src/qml_gui/Renderer/RhiViewport.h, src/qml_gui/Renderer/RhiViewport.cpp</files>
  <read_first>
    - src/qml_gui/Renderer/RhiViewport.h
    - src/qml_gui/Renderer/RhiViewport.cpp
    - src/core/rendering/GizmoMath.h
    - src/core/rendering/GizmoCenter.h
  </read_first>
  <action>
    Keep the existing partial RhiViewport phase 69 wiring, but make it production-safe: use ASCII-only comments, guard invalid axis indexes before indexing the axis array, ensure pickGizmoAxisAt refreshes the picking scene before reading selected batches, and remove unused drag accumulation state if it is not needed.

    Preserve event consumption: on a picked axis, press emits gizmoDragBegin and returns accepted; move emits only the axis-constrained delta and returns accepted; release emits gizmoDragEnd and returns accepted.
  </action>
  <acceptance_criteria>
    - RhiViewport.cpp contains GizmoMath::computeRay, GizmoMath::pickMoveAxis, and GizmoMath::rayToAxisT.
    - No camera orbit branch runs while m_gizmoDragging is true.
    - All new or modified source comments are English ASCII.
  </acceptance_criteria>
  <done>RhiViewport owns RHI move-axis pick and drag event consumption.</done>
</task>

<task type="auto">
  <name>Task 4: Wire PreparePage signal forwarding</name>
  <files>src/qml_gui/pages/PreparePage.qml</files>
  <read_first>
    - src/qml_gui/pages/PreparePage.qml
  </read_first>
  <action>
    Add handlers on the GLViewport instance:
    - `onGizmoDragBegin` calls `root.editorVm.beginGizmoMoveDrag()`
    - `onGizmoMoveRequested` calls `root.editorVm.applyGizmoMoveDelta(worldDelta.x, worldDelta.y, worldDelta.z)`
    - `onGizmoDragEnd` calls `root.editorVm.endGizmoMoveDrag()`

    Keep QML as signal forwarding only. Do not add ray, pick, axis, or transform math to QML.
  </action>
  <acceptance_criteria>
    - QmlUiAuditTests::rhiMoveGizmoDragBridgeStaysCppOwned passes.
    - PreparePage contains the three signal handlers and no gizmo math.
  </acceptance_criteria>
  <done>QML forwards RHI move-gizmo drag events into EditorViewModel.</done>
</task>

<task type="auto">
  <name>Task 5: Targeted verification and phase closeout</name>
  <files>.planning/phases/69-move-gizmo-pick-drag-interaction/69-SUMMARY.md, .planning/phases/69-move-gizmo-pick-drag-interaction/69-VERIFICATION.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Run targeted verification only for this phase: targeted test build/run for ViewModelSmokeTests and QmlUiAuditTests if available from the existing build tree, `git diff --check`, and the global encoding guard. Per current v3.8 state, do not run the full canonical build after phase 69; full build and visual UAT are deferred to Phase 73.

    Write a concise SUMMARY and VERIFICATION file documenting the implementation, verification commands, and any deferred manual visual validation.
  </action>
  <acceptance_criteria>
    - Targeted tests pass or any environment blocker is explicitly documented.
    - `git diff --check` is clean.
    - `python %USERPROFILE%\.coding-encoding-guard\encoding_guard.py` reports no introduced encoding issues in touched files.
    - VERIFICATION.md status is `passed` if automated checks pass, or `human_needed` only if live GUI drag UAT remains the only unverified item.
  </acceptance_criteria>
  <done>Phase 69 has summary, verification evidence, and clean text checks.</done>
</task>

</tasks>

<verification>
Phase 69 proves GMOV-03 and GMOV-04:
- GMOV-03: RhiViewport performs move-axis picking with GizmoMath::pickMoveAxis before object picking when the move gizmo is active.
- GMOV-04: RhiViewport consumes drag events while m_gizmoDragging is true, emits axis-constrained deltas, and EditorViewModel applies those deltas with one undo command per drag.

Full running-app visual drag UAT remains part of the final Phase 73 verification pass unless the app is launched during this phase.
</verification>
