---
milestone: v5.1
name: v5.0 Deferred Items Closure
audited: 2026-07-17
status: clean
scores:
  requirements: 7/7 (0 deferred, 2 with documented scope refinements)
  phases: 6/6
  integration: 6/6 closure chains wired (PSET-05/EMB-06/PLATE-05/PLATE-06 closed)
  flows: 6/6 reachable
  source_audit_slots: 7 (6 per-phase + 1 consolidated v51RegressionLocked)
gaps:
  requirements: []
  integration: []
tech_debt:
  - phase: 158
    items:
      - "EMBO-F01 use-surface + curve-projection: Q_PROPERTYs exposed + persisted into TextConfiguration for round-trip, but geometry deformation deferred. Upstream Emboss.hpp has NO ProjectCurve class (only ProjectZ/ProjectScale/ProjectTransform + create_transformation_onto_surface) — there is no primitive to call. UI tooltips document this. Boldness + italic are fully wired (real FontProp fields). This is an honest scope refinement, not a missing feature — the v5.1 'no new architecture' rule forbids inventing a projection math upstream lacks."
      - "EMBO-F02 SVG curve-projection: same scope rule as EMBO-F01. SVG depth-modifier IS shipped (real Z-scale on the imported mesh)."
---

# v5.1 Milestone Audit — v5.0 Deferred Items Closure

**Audited:** 2026-07-17
**Status:** clean — 7/7 requirements satisfied, 6/6 phases verified, 6/6 closure chains wired.

## Original intent (from ROADMAP.md)

Close the 4 documented v5.0 partials (PSET-05 / EMB-06 / PLATE-05 / PLATE-06) plus Emboss style/SVG follow-ups. Each item is a 1-phase completion of a primitive already shipped in v5.0 — low-risk closures, no new architecture.

## Phase-by-phase outcome

| Phase | Name | Status | Requirement | Notes |
|---|---|---|---|---|
| 154 | QML Preset Diff-View Dialog | ✅ | CLOS-01 (PSET-05) | Wires the deferred QML consumer for the Phase 149 comparePresets primitive. PresetDiffDialog + ConfigViewModel.comparePresetsDetailed proxy + SettingsDialog toolbar entry. |
| 155 | Emboss 3MF Text Metadata Round-Trip | ✅ | CLOS-02 (EMB-06) | attachEmbossMetadata writes upstream TextConfiguration so store_bbs_3mf emits `<slic3rpe:text>`; load side reads text_configuration to restore TextEmboss tag (mirrors upstream ModelVolume::is_text()). |
| 156 | Runtime Plate Thumbnail Capture Scheduler | ✅ | CLOS-03 (PLATE-05) | setPlateThumbnailFromBase64 write path + per-plate thumbnailCapturedForPlate signal (deliverThumbnail no longer Q_UNUSED's plateIndex) + PreparePage session-capture scheduler. |
| 157 | Live Multi-Plate Round-Trip ctest Fixture | ✅ | CLOS-04 (PLATE-06) | Exploration revealed the harness gap was already closed — multiPlate3mfRoundTripPreservesState runs a real store_bbs_3mf + read_from_archive. Phase 157 ships multiPlateFullStateRoundTrip extending coverage to all 5 dimensions + thumbnail. |
| 158 | Emboss Style Controls + SVG Advanced Features | ✅ | EMBO-F01 + EMBO-F02 | boldness + italic fully wired (real FontProp fields); use-surface + curve-projection + SVG curve-projection scope-refined per upstream gap (see tech_debt). SVG depth-modifier shipped (real Z-scale). |
| 159 | v5.1 Cross-Workstream Regression Gate | ✅ | REGRESS-05 | v51RegressionLocked slot locks all v5.1 anchors + re-asserts v5.0/v4.8/v4.7/v4.6. |

## Closure verification

Each of the 4 v5.0 partials is now closed:
- **PSET-05** (Phase 149 shipped the primitive; QML consumer deferred) → **closed by Phase 154** (PresetDiffDialog).
- **EMB-06** (Phase 146 shipped geometry round-trip; text metadata deferred) → **closed by Phase 155** (TextConfiguration persistence).
- **PLATE-05** (Phase 151 shipped persisted thumbnails; session-capture deferred) → **closed by Phase 156** (setPlateThumbnailFromBase64 + scheduler).
- **PLATE-06** (Phase 152 shipped source-audit only; live ctest deferred) → **closed by Phase 157** (multiPlateFullStateRoundTrip live ctest).

## Verification gate

- Canonical build target (ninja OWzxSlicer + ninja QmlUiAuditTests + ViewModelSmokeTests): exit 0 across all 6 phases.
- 5/5 ctest groups PASS: QmlUiAuditTests, ViewModelSmokeTests, PartPlateTests, PrepareSceneDataTests, PreviewParserTests.
- 12 v5.0 regression slots still pass (no regression from v5.1 work).
- 7 new v5.1 source-audit slots all pass (v51PresetDiffDialogWired, v51EmbossTextMetadataRoundTripWired, v51PartPlateSessionThumbnailWired, v51MultiPlateRoundTripLiveCtest, v51EmbossStyleControlsAndSvgAdvancedWired, v51RegressionLocked + the live multiPlateFullStateRoundTrip ctest).

## Honest scope refinements (documented, not silent)

1. **CLOS-02 depth round-trip**: requirement text mentions "depth match" — depth is a projection property upstream (not a FontProp field), so it round-trips via the mesh Z-extent (geometry already persisted as MODEL_PART by Phase 146). Functionally equivalent; documented in PLAN.
2. **EMBO-F01 use-surface + curve-projection**: exposed as Q_PROPERTYs + persisted into TextConfiguration for round-trip, but geometry deformation deferred — upstream Emboss.hpp has no ProjectCurve class. Boldness + italic are fully wired. Documented in PLAN + QML tooltips.
3. **EMBO-F02 SVG curve-projection**: same scope rule. SVG depth-modifier IS shipped (real Z-scale).

These are honest deferrals grounded in upstream API reality, not missing features. The v5.1 "low-risk closures, no new architecture" principle forbids inventing projection math upstream lacks.

## Next milestone

v5.2 SLA Print Path — wire SLAPrint into SliceService via PrinterTechnology dispatch (FFF vs SLA), close VDB-06, unblock SlaSupports + FaceDetector. Research at `.planning/research/sla-scope.md`.
