---
milestone: v4.4
milestone_name: Wipe-Tower Geometry Readback And Real Rendering
audited: 2026-07-11
status: tech_debt
scores:
  requirements: 8/8
  phases: 4/4
  integration: 4/4
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  verification_coverage:
    - "Phases 100-102 lack formal VERIFICATION.md files (executors produced SUMMARY.md + REVIEW.md instead). Equivalent verification performed via code review (0 critical across all 3) + regression ctest 4/4 + empirical harness probes + canonical verifier + launch liveness."
  evidence:
    - "Runtime VISUAL evidence (Windows capture API screenshot) blocked per STATE.md deferred items. Reachability verified via process-liveness (OWzxSlicer.exe 5s no-crash, PID 34240) + canonical verifier + regression ctest."
  deferred:
    - "Option B (real wipe-tower mesh via wipe_tower_mesh_data + convex_hull_3d) LOCKED as future upgrade per Phase 99 Frozen Decision 2; not implemented in v4.4."
    - "v3.2 Nyquist VALIDATION.md files not produced for v4.4 phases (carry-forward)."
    - "Canonical build libslic3r reconfigure (~8 min) times out executor wrapper during test-target compilation — documented prior-phase pattern; production code compiles/links clean; ctest passes via incremental path."
---

# v4.4 Milestone Audit — Wipe-Tower Geometry Readback And Real Rendering

## Summary

**Status: tech_debt** — all requirements met (8/8), all phases complete (4/4), cross-phase integration verified end-to-end (4/4), E2E flow complete (1/1). No critical blockers. Accumulated tech debt is verification-coverage (missing formal VERIFICATION.md artifacts) + the documented Windows-capture-API evidence blocker + Option B deferral, all consistent with v4.3's closeout pattern.

## Requirements Coverage

| REQ-ID | Description | Phase | Status | Evidence |
|--------|-------------|-------|--------|----------|
| WTAUDIT-01 | Wipe-tower capture + rendering surface inventory | 99 | ✓ satisfied | 99-GAP-MATRIX.md (8 WT-* regions + upstream anchors); 99-VERIFICATION.md (passed) |
| WTAUDIT-02 | 3 frozen designs (readback point, Option A baseline, has_wipe_tower gate) | 99 | ✓ satisfied | 99-GAP-MATRIX Frozen Decisions section; 99-VERIFICATION.md (passed) |
| WTREAD-01 | Post-slice wipe-tower geometry readback from Print::wipe_tower_data() | 100 | ✓ satisfied | 100-01-SUMMARY; WipeTowerGeometry POD captured by value in worker; EditorViewModel 6 Q_PROPERTYs; PreparePage.qml:1670-1675 bindings; ctest pass |
| WTREAD-02 | has_wipe_tower() gate (no placeholder leak on single-material) | 100 | ✓ satisfied | 100-01-SUMMARY + REVIEW; success-branch-only emit; showWipeTower gate enforced; SoftwareViewport default show=false |
| WTRENDER-01 | Rendered wipe-tower reflects real sliced dims | 101 | ✓ satisfied | 101-01-SUMMARY + REVIEW; RHI pipeline unchanged (already correct end-to-end); wipeTowerRealDimsReachRendererPipeline regression test; W1 corner→center fix (commit b12d0e5) |
| WTRENDER-02 | Option A baseline locked; Option B documented as deferred | 101 | ✓ satisfied | 101-01-SUMMARY + REVIEW; GizmoGeometry.cpp Option A comment + Option B deferral; zero Option B code in src/ |
| WTVERIFY-01 | Source/QML audits cover readback + rendering anchors | 102 | ✓ satisfied | 102-01-SUMMARY + REVIEW; wipeTowerReadbackAndRenderAnchorsPresent QmlUiAuditTests slot locks all 8 WT-* regions |
| WTVERIFY-02 | Canonical verifier + launch + regression ctest | 102 | ✓ satisfied | 102-01-SUMMARY; OWzxSlicer.exe links clean; PID 34240 5s liveness; regression ctest 4/4 pass |

**Score: 8/8 requirements satisfied.** Orphan detection: none.

## Phase Completion

| Phase | Name | Plans | Summary | Verification | Status |
|-------|------|-------|---------|--------------|--------|
| 99 | Wipe-Tower Geometry Gap Audit | 1/1 | ✓ | VERIFICATION.md (passed) + GAP-MATRIX | ✓ Complete |
| 100 | Wipe-Tower Geometry Readback | 1/1 | ✓ | REVIEW.md (0 critical, 1 warning W1 fixed) + ctest | ✓ Complete (verification-gap) |
| 101 | Wipe-Tower Real Rendering Upgrade | 1/1 | ✓ | REVIEW.md (0 critical, 0 warning, 4 info) + ctest | ✓ Complete (verification-gap) |
| 102 | Wipe-Tower Verification And Regression | 1/1 | ✓ | REVIEW.md (0 critical, 0 warning, 3 info) + ctest + verifier + launch | ✓ Complete (verification-gap) |

**Score: 4/4 phases complete.** All produced SUMMARY.md with Self-Check: PASSED. Phases 100-102 lack the formal VERIFICATION.md artifact — equivalent verification was performed via code review (0 critical findings across all 3) + regression ctest + empirical harness probes + canonical verifier + launch liveness.

## Cross-Phase Integration

The v4.4 milestone is a single linear dependency chain (99 → 100 → 101 → 102), each phase's `provides` feeding the next's `requires`. Verified end-to-end:

| Integration Point | From → To | Status | Evidence |
|-------------------|-----------|--------|----------|
| Frozen decisions → real implementation | 99 → 100 | ✓ wired | 100-01-SUMMARY requires-block cites the 3 WTAUDIT-02 decisions; implementation matches (Print-lifetime-bounded readback, has_wipe_tower gate, capture-by-value) |
| Captured geometry → EditorViewModel Q_PROPERTYs → QML binding | 100 → 101 | ✓ wired | SliceService WipeTowerGeometry POD → wipeTowerGeometryReady signal → EditorViewModel onWipeTowerGeometryReady slot → 6 Q_PROPERTYs → PreparePage.qml:1670-1675 GLViewport bindings |
| Real dims → RHI render pipeline | 100 + 101 | ✓ wired | Phase 101 verified RhiViewportRenderer::uploadWipeTowerBuffer unchanged (already correct end-to-end); W1 corner→center fix ensures the box sits on the true position |
| SoftwareViewport rendering gap → consistency | 100 + 101 | ✓ wired | Phase 101 QPainter box mirrors the RHI box geometry + color; gated on m_showWipeTower (WTREAD-02) |
| All anchors locked → regression | 99 + 100 + 101 → 102 | ✓ wired | Phase 102 wipeTowerReadbackAndRenderAnchorsPresent locks all 8 WT-* region anchors across 7 source files |

**Score: 4/4 integration points verified.** No orphaned references, no broken wiring.

## E2E Flows

| Flow | Steps | Status |
|------|-------|--------|
| Slice → capture → deliver → render (real wipe-tower geometry) | print.process() (SliceService worker) → capture WipeTowerGeometry by value (has_wipe_tower gate) → wipeTowerGeometryReady signal (success branch only) → EditorViewModel onWipeTowerGeometryReady (GUI thread) → 6 Q_PROPERTYs NOTIFY → PreparePage.qml:1670-1675 bindings → RhiViewport Q_PROPERTYs → RhiViewportRenderer::synchronize() pulls dims → uploadWipeTowerBuffer rebuilds box on m_wipeTowerDirty with real dims (Option A) → renderWipeTower draws via m_translucentFillPipeline | ✓ Complete (regression-locked by wipeTowerRealDimsReachRendererPipeline + wipeTowerReadbackAndRenderAnchorsPresent + wipeTowerGeometryReadbackAppliesValidAndInvalidGate tests) |

**Score: 1/1 E2E flow complete.**

## Tech Debt

### Verification Coverage (non-blocking)
- **Phases 100-102 lack formal VERIFICATION.md** — executors produced SUMMARY.md + REVIEW.md but not VERIFICATION.md. Equivalent verification (code review 0-critical across all 3 + regression ctest 4/4 + empirical harness probes + canonical verifier + launch liveness) was performed. The Phase 100 W1 corner-vs-center position bug was caught by code review and fixed + re-verified. Process gap (artifact not produced), not a substance gap.

### Evidence (carry-forward)
- **Runtime VISUAL evidence blocked** — Windows capture API unavailable; reachability verified via process-liveness (OWzxSlicer.exe PID 34240, 5s no-crash) + canonical verifier (production link clean) + regression ctest (4/4). This is the documented STATE.md evidence bar.

### Deferred (carry-forward + v4.4-specific)
- **Option B (real wipe-tower mesh)** — LOCKED as future upgrade per Phase 99 Frozen Decision 2; not implemented in v4.4. The real-mesh path via `wipe_tower_mesh_data->real_wipe_tower_mesh` + `real_brim_mesh` (Print.hpp:745-746) using `convex_hull_3d()` (mirroring upstream `3DScene.cpp:887-925 load_real_wipe_tower_preview`) requires ITS vertex format extension in `GizmoGeometry` + `RhiViewportRenderer`. Documented in the Phase 101 GizmoGeometry.cpp comment.
- **Nyquist VALIDATION.md** — v4.4 phases have SUMMARY/REVIEW verification but no separate Nyquist validation files (carry-forward from v4.2/v4.3).
- **Canonical build libslic3r reconfigure timeout** — documented prior-phase pattern.

### Deferred Backlog (out of scope for v4.4, per ROADMAP)
- Auto filament-map recommendation (the wipe-tower geometry readback shipped; the auto filament-map recommendation remains future).
- Missing CLI fixtures + deterministic argv GUI fixture loading (FIXTURE-02 unblocked by v4.3).
- D3D12 root-cause + future Vulkan/D3D12 backend.
- Full GLGizmoMeasure engine + AssembleViewDataPool clipper.
- LAN/device/cloud/network/Monitor/ModelMall/camera/printer-hardware (removed scope).

## Conclusion

Milestone v4.4 (Wipe-Tower Geometry Readback And Real Rendering) achieved its definition of done:
- **WTREAD-01/02: closed** — real wipe-tower geometry now flows from `Print::wipe_tower_data()` (captured by value in the SliceService worker, no Print* escape) through EditorViewModel Q_PROPERTYs into the PreparePage.qml GLViewport bindings, with the `has_wipe_tower()` gate enforced data-driven (Phase 100).
- **WTRENDER-01/02: closed** — Option A dimensioned-box baseline LOCKED as the v4.4 contract; the RHI render pipeline was already correct end-to-end (Phase 101 verified + regression-locked); SoftwareViewport QPainter box closes the fallback-path rendering gap; Option B (real mesh) documented as future upgrade (Phase 101).
- **WTVERIFY-01/02: closed** — 8 WT-* region source audits consolidated into a regression-lock test; canonical verifier passes; OWzxSlicer.exe launch liveness confirmed; regression ctest 4/4 (Phase 102).

All 8 requirements satisfied, all 4 phases complete, cross-phase integration verified, 1 E2E flow (slice → capture → deliver → render) complete with exact regression locks. The Phase 100 code review caught a real geometric-position bug (W1: corner-vs-center mismatch, would have rendered the tower offset by ~30mm) and it was fixed + re-verified before Phase 101. No critical blockers. Tech debt is verification-artifact coverage (documented) + Option B deferral (LOCKED future) + carry-forward evidence/deferred items. Recommend proceeding to complete-milestone.

Full report: `.planning/v4.4-MILESTONE-AUDIT.md`
