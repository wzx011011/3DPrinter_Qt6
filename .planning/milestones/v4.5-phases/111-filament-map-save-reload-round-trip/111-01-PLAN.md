---
phase: 111-filament-map-save-reload-round-trip
plan: 01
type: test
wave: 1
depends_on:
  - phase: 107-filament-map-mode-enum-widening-and-3mf-migration
    provides: 4-value enum + 3MF read-side migration (the R-01 legacy branch).
  - phase: 108-filament-map-auto-recommendation-readback
    provides: auto-recommended map readback.
  - phase: 110-filament-map-popup-ui-and-mode-surfacing
    provides: setPlateFilamentMapMode Q_INVOKABLE + R-02 enum range validation.
files_modified:
  - tests/PartPlateTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [FMAP-04]
---

# Phase 111 Plan: Filament-Map Save-Reload Round-Trip

## Objective
Close FMAP-04 (full save→reload round-trip test) + Phase 107 REVIEW R-01 (legacy raw-int-1 → fmmManual branch test). The migration code was correct by inspection but never executed at runtime; this phase proves it.

## Must Haves
<must_haves>
  <truth id="FT-01">A `filamentMapSaveReloadRoundTrip` (or similar) slot is added to PartPlateTests that: (a) loads a real model (kStlPath); (b) sets a known filament-map mode + maps on plate 0 (e.g. fmmAutoForFlush + {1,2}); (c) saveProject to a temp .3mf; (d) fresh ProjectServiceMock + loadFile; (e) asserts the mode + maps survived the round-trip. Mirrors the Phase 97 thumbnailSaveReloadRoundTrip pattern. Covers at minimum fmmAutoForFlush + fmmManual.</truth>
  <truth id="FT-02">R-01 FIX: a test exercises the legacy raw-int-1 → fmmManual migration branch. Either (a) factor the migration predicate into a small testable helper OR (b) construct a synthetic legacy 3MF fixture with a non-coEnum int filament_map_mode=1 OR (c) construct a DynamicPrintConfig with a non-coEnum int option set to 1 and feed it through the migration. The test asserts the legacy int-1 maps to fmmManual (NOT the new fmmAutoForMatch=1). This is the Phase 107 REVIEW R-01 coverage gap closure.</truth>
  <truth id="FT-03">A QmlUiAuditTests source-audit slot `filamentMapSaveReloadRoundTripCoversModesAndLegacyMigration` asserts the round-trip test exists + the legacy-branch test exists. FMAP-04 + R-01-named QVERIFY2 messages.</truth>
  <truth id="FT-04">Canonical build + regression ctest passes; the new slots PASS.</truth>
  <truth id="FT-05">`git diff --check` exit 0; encoding guard clean.</truth>
</must_haves>

<threat_model>
Test-only phase. No production code changes (unless factoring the migration helper). No new attack surface. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="111-01-01" type="test">
    <title>Add filamentMapSaveReloadRoundTrip + legacy-migration tests</title>
    <read_first>
      - .planning/phases/111-filament-map-save-reload-round-trip/111-CONTEXT.md
      - .planning/phases/107-filament-map-mode-enum-widening-and-3mf-migration/107-REVIEW.md (R-01)
      - tests/PartPlateTests.cpp (Phase 97 thumbnailSaveReloadRoundTrip + Phase 107 filamentMapModeRoundTripManualPreserved patterns)
      - src/core/services/ProjectServiceMock.cpp:4997, 5413 (write + read/migration sites)
      - src/core/model/PartPlate.h:96-101 (enum)
    </read_first>
    <files>
      - tests/PartPlateTests.cpp
      - src/core/services/ProjectServiceMock.h (optional — if factoring the migration helper for testability)
      - src/core/services/ProjectServiceMock.cpp (optional — same)
    </files>
    <action>
      Add `filamentMapSaveReloadRoundTrip` (FT-01) + the legacy-migration test (FT-02). For the legacy branch: prefer the cleanest approach — if factoring the migration into a `migrateLegacyFilamentMapMode(int)` helper is straightforward, do that + unit-test it directly. Otherwise construct a synthetic legacy config. The test MUST assert legacy-int-1 → fmmManual (NOT fmmAutoForMatch).
    </action>
    <verify>
      `rg -n "filamentMapSaveReloadRoundTrip\|migrateLegacy\|legacy.*filament" tests/PartPlateTests.cpp`
      ctest PartPlateTests passes incl. new slots.
    </verify>
    <acceptance_criteria>
      - Round-trip test covers fmmAutoForFlush + fmmManual (FT-01).
      - Legacy-int-1 → fmmManual migration tested at runtime (FT-02 / R-01 closure).
    </acceptance_criteria>
  </task>

  <task id="111-01-02" type="test">
    <title>Add source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp, tests/PartPlateTests.cpp</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add `filamentMapSaveReloadRoundTripCoversModesAndLegacyMigration` source-audit slot asserting the round-trip + legacy-branch tests exist. FMAP-04 + R-01-named messages. FT-03.
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes (FT-03).</acceptance_criteria>
  </task>

  <task id="111-01-03" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest. Document the round-trip coverage + R-01 closure in SUMMARY.</action>
    <verify>SUMMARY records build + ctest pass + R-01 closed.</verify>
    <acceptance_criteria>Production link clean, ctest pass (FT-04).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest.
2. Source audit: `rg -n "filamentMapSaveReloadRoundTrip\|migrateLegacy" tests/`
3. Encoding guard.

## Success Criteria
- FMAP-04 closed + R-01 closed; every FT truth.

## Expected Non-Changes
- No FMAP-01/02/03 changes (those are Phases 107/108/110).
- No libslic3r changes.
- The migration helper factoring (if chosen) is additive — the existing migration call site is unchanged.
