---
phase: 106-d3d12-crash-root-cause-and-backend-readiness
plan: 01
type: investigation
wave: 1
depends_on:
  - phase: 105-d3d12-debug-layer-wiring
    provides: OWZX_D3D12_DEBUG env flag + QSG_RHI_DEBUG forwarding (the triage tooling).
files_modified: []
autonomous: true
requirements: [D3D12-02, D3D12-03]
---

# Phase 106 Plan: D3D12 Crash Root-Cause And Backend Readiness

## Objective
Close D3D12-02 (root cause identified OR time-boxed) + D3D12-03 (no default promotion; Vulkan SDK-blocked). Investigation phase — primary output is a root-cause report (.planning/research/D3D12-CRASH-ROOT-CAUSE.md), NOT code. Time-boxed: if the repro+triage doesn't converge, ship a documented hypothesis + next steps (D3D12-03's acceptable outcome).

## Must Haves
<must_haves>
  <truth id="DR-01">A repro procedure is documented: the exact env flags (`OWZX_RHI_RENDERER=d3d12 OWZX_D3D12_DEBUG=1`), the build state, and the observed crash signature (the `0xc0000005` access violation in `RhiViewportRenderer.cpp:282-298` beginPass). The repro is verified to actually crash (or documented why it can't be reproduced in the test environment).</truth>
  <truth id="DR-02">A root-cause report is written to `.planning/research/D3D12-CRASH-ROOT-CAUSE.md` documenting: (a) the debug-layer output captured during repro; (b) the isolated failure point (which D3D12 API call / resource); (c) the hypothesized root cause (the leading explanation); (d) whether the BUG-V31-1 hypothesis at `RhiViewportRenderer.cpp:283-285` is confirmed, refuted, or superseded; (e) recommended next steps (code fix path, additional investigation, or accept-as-opt-in).</truth>
  <truth id="DR-03">D3D12-03 is documented in PROJECT.md or STATE.md: default-backend promotion to D3D12 stays OUT OF SCOPE until the root cause is resolved; D3D12 remains opt-in via `OWZX_RHI_RENDERER=d3d12`; Vulkan backend is SDK-blocked (Qt disables `vulkan` feature) and is evaluation-only, not a v4.5 deliverable.</truth>
  <truth id="DR-04">If the investigation is time-boxed (repro doesn't converge or debug output is inconclusive within the time budget): the report documents (a) what was attempted; (b) the leading hypothesis with evidence; (c) what additional tooling/time would be needed; (d) explicit acceptance that D3D12 stays opt-in per D3D12-03. This is the documented acceptable outcome — NOT a failure.</truth>
  <truth id="DR-05">No default-backend promotion to D3D12 ships (D3D12-03 hard rule). The `defaultWindowsCandidates()` order at `RhiBackendSelector.cpp:42-45` keeps D3D11 first. A source-audit confirms this.</truth>
</must_haves>

<threat_model>
Investigation phase — no production code changes unless a clear fix emerges. Threat surface unchanged. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="106-01-01" type="investigation">
    <title>Repro + triage D3D12 crash; write root-cause report</title>
    <read_first>
      - .planning/phases/106-d3d12-crash-root-cause-and-backend-readiness/106-CONTEXT.md
      - .planning/research/PITFALLS.md (pitfall 5: BUG-V31-1 is a hypothesis)
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp:282-298 (the beginPass crash site + BUG-V31-1 comment)
      - src/qml_gui/Renderer/RhiBackendSelector.cpp (the OWZX_D3D12_DEBUG wiring from Phase 105)
      - .planning/debug/qrhi-d3d12-crash.md (if exists — prior crash characterization)
    </read_first>
    <files>
      - .planning/research/D3D12-CRASH-ROOT-CAUSE.md
    </files>
    <action>
      **Step 1 — repro:** Launch `build/OWzxSlicer.exe` with `OWZX_RHI_RENDERER=d3d12 OWZX_D3D12_DEBUG=1` (PowerShell: `$env:OWZX_RHI_RENDERER='d3d12'; $env:OWZX_D3D12_DEBUG='1'; Start-Process build/OWzxSlicer.exe -PassThru`). Capture stderr/stdout (debug-layer output goes to debug output on Windows; use `dbghelp` or a debugger-attached capture if needed). Document the crash signature (exit code, faulting address if available).

      **Step 2 — triage:** Read the debug-layer output. Identify which D3D12 API call / resource access triggers the access violation. Hypothesize root cause. Compare against the BUG-V31-1 comment at `RhiViewportRenderer.cpp:283-285` — is it the same root cause, a different one, or inconclusive?

      **Step 3 — document:** Write `.planning/research/D3D12-CRASH-ROOT-CAUSE.md` with: repro procedure, debug output excerpts, isolated failure point, hypothesized root cause, BUG-V31-1 verdict (confirmed/refuted/superseded), recommended next steps.

      **Step 4 — time-box guard:** If the repro can't be captured in the test environment (e.g. no display, debugger unavailable, env doesn't actually crash in CI) OR the debug output is inconclusive: document what was attempted + the leading hypothesis + what additional tooling would be needed. This is DR-04's acceptable outcome.

      Be honest about the investigation's limits — don't fabricate a root cause to look done. A documented "inconclusive with hypothesis X + needs tool Y" is more valuable than a false positive.
    </action>
    <verify>
      `.planning/research/D3D12-CRASH-ROOT-CAUSE.md` exists with: repro procedure, debug output (or "could not capture" + reason), hypothesis, BUG-V31-1 verdict, next steps.
    </verify>
    <acceptance_criteria>
      - Root-cause report exists with the DR-02 required sections (DR-02) OR DR-04 time-box documentation (DR-04).
      - The BUG-V31-1 hypothesis is explicitly addressed (confirmed/refuted/superseded/inconclusive) (DR-02).
      - Honest about investigation limits (no fabricated root cause).
    </acceptance_criteria>
  </task>

  <task id="106-01-02" type="documentation">
    <title>Document D3D12-03 (no default promotion; Vulkan SDK-blocked)</title>
    <read_first>
      - .planning/PROJECT.md (Out of Scope section)
      - .planning/STATE.md (Deferred Items)
      - src/qml_gui/Renderer/RhiBackendSelector.cpp:37-46 (defaultWindowsCandidates order)
    </read_first>
    <files>
      - .planning/STATE.md
    </files>
    <action>
      Add a D3D12-03 entry to STATE.md's Deferred Items (or PROJECT.md Out of Scope) documenting: D3D12 default-backend promotion stays out of scope until D3D12-02 root cause is resolved; D3D12 remains opt-in via `OWZX_RHI_RENDERER=d3d12`; Vulkan is SDK-blocked (Qt `vulkan` feature disabled) and is evaluation-only.

      Do NOT change `defaultWindowsCandidates()` order (D3D11 stays first). A source-audit QmlUiAuditTests slot confirms this (DR-05).
    </action>
    <verify>
      `rg -n "D3D12-03\|D3D11 first\|d3d11.*first" .planning/STATE.md src/qml_gui/Renderer/RhiBackendSelector.cpp`
    </verify>
    <acceptance_criteria>
      - D3D12-03 documented in STATE.md or PROJECT.md (DR-03).
      - defaultWindowsCandidates() order unchanged (D3D11 first) (DR-05).
    </acceptance_criteria>
  </task>

  <task id="106-01-03" type="test">
    <title>Add d3d12StaysOptInBehindEnvFlag source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp, src/qml_gui/Renderer/RhiBackendSelector.cpp</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add a `d3d12StaysOptInBehindEnvFlag` (or similar) source-audit slot asserting: (a) `defaultWindowsCandidates()` keeps D3D11 before D3D12 (position-ordered assertion like Phase 105); (b) the D3D11-first comment is still present (locks DR-05). D3D12-03-named QVERIFY2 messages.
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes.</acceptance_criteria>
  </task>

  <task id="106-01-04" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest 4/4. Document the investigation outcome (root cause found / time-boxed) in the SUMMARY.</action>
    <verify>SUMMARY records build + ctest + investigation outcome.</verify>
    <acceptance_criteria>Production link clean, ctest 4/4 pass.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest 4/4.
2. `.planning/research/D3D12-CRASH-ROOT-CAUSE.md` exists with required sections.
3. STATE.md / PROJECT.md has D3D12-03 entry.
4. Source audit: D3D11-first order unchanged.

## Success Criteria
- D3D12-02 closed (root cause OR time-boxed with hypothesis + next steps); D3D12-03 documented; every DR truth; no default promotion.

## Expected Non-Changes
- No D3D12 default promotion (D3D12-03 hard rule).
- No Vulkan production backend (SDK-blocked).
- No libslic3r changes.
