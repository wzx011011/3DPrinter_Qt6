---
phase: 109-option-b-wipe-tower-mesh-readback-and-real-rendering
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 100-wipe-tower-geometry-readback
    provides: WipeTowerGeometry POD + wipeTowerGeometryReady signal + EditorViewModel Q_PROPERTYs + PreparePage.qml bindings + RhiViewportRenderer uploadWipeTowerBuffer (Option A baseline).
files_modified:
  - src/core/services/SliceService.h
  - src/core/services/SliceService.cpp
  - src/core/rendering/GizmoGeometry.h
  - src/core/rendering/GizmoGeometry.cpp
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/Renderer/SoftwareViewport.cpp
  - src/qml_gui/Renderer/SoftwareViewport.h
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [WTMESH-01, WTMESH-02, WTMESH-03]
---

# Phase 109 Plan: Option B Wipe-Tower Mesh Readback And Real Rendering

## Objective
Close WTMESH-01/02/03: implement Option B real wipe-tower mesh rendering (the LOCKED-future-upgrade from Phase 99 Frozen Decision 2). When `wipe_tower_mesh_data` is populated, capture the merged+convex-hull mesh by value, render it as a real triangle set instead of the Option A dimensioned box. Option A preserved as fallback. Re-opens Phase 99 Frozen Decision 2.

## Must Haves
<must_haves>
  <truth id="WM-01">`WipeTowerGeometry` POD (SliceService.h:42) is EXTENDED (not replaced) with `bool hasRealMesh = false` and `std::vector<float> meshVertices` (flattened XYZ triples). The existing fields (valid/x/z/width/depth/height/etc.) are unchanged so the Option A fallback path still works. The new fields default to false/empty so a v4.4-style capture (dims only) produces hasRealMesh=false.</truth>
  <truth id="WM-02">The worker capture (SliceService.cpp, existing wipe-tower block at ~:634-658) EXTENDS to: when `print.wipe_tower_data().wipe_tower_mesh_data != std::nullopt`, merge `real_wipe_tower_mesh` + `real_brim_mesh` (mirror upstream 3DScene.cpp:906-909 `mesh.merge(brim_mesh)`), run `convex_hull_3d()` on the merged mesh (mirror :914), extract the resulting `its.vertices` into `meshVertices` as flattened XYZ floats. Set `hasRealMesh = true`. NO `TriangleMesh*` or `its*` escapes — pure float vector. The existing dims capture is unchanged (still runs for the Option A fallback).</truth>
  <truth id="WM-03">A new `GizmoGeometry::buildWipeTowerMeshVertices(const std::vector<float> &meshVertices, float x, float z)` helper is ADDED (GizmoGeometry.h/.cpp), PARALLEL to the existing `buildWipeTowerVertices` (Option A — DO NOT MODIFY). It builds a triangle-set vertex buffer (the same GizmoVertex format) from the captured mesh vertices, applying the same bed-plane transform (upstream Y → Qt Z, the v4.4 W1 corner→center convention is already in the captured x/z). Returns a `QVector<GizmoVertex>` sized to the triangle count × 3.</truth>
  <truth id="WM-04">`uploadWipeTowerBuffer` (RhiViewportRenderer.cpp:1064-1095) is EXTENDED with a branch: when `m_wipeTowerHasRealMesh` is true (new member, set from the captured geometry via synchronize), call `buildWipeTowerMeshVertices(m_wipeTowerMeshVertices, m_wipeTowerX, m_wipeTowerZ)` instead of `buildWipeTowerVertices(m_wipeTowerX, m_wipeTowerZ, m_wipeTowerWidth, ...)`. The existing Option A path is the `else` branch — UNCHANGED. The dynamic-size buffer path (ensureBuffer + uploadStaticBuffer) already handles variable vertex counts.</truth>
  <truth id="WM-05">`RhiViewportRenderer` (and the `RhiViewport` Q_PROPERTY chain + `synchronize()`) is extended to carry the `hasRealMesh` + `meshVertices` from the EditorViewModel through to the renderer. New members `m_wipeTowerHasRealMesh` + `m_wipeTowerMeshVertices` in RhiViewportRenderer.h; new Q_PROPERTYs or fields in RhiViewport; PreparePage.qml binds them (mirroring the v4.4 wipe-tower Q_PROPERTY bindings). The `m_wipeTowerDirty` flag fires on mesh change too.</truth>
  <truth id="WM-06">Option A regression-locked: when `wipe_tower_mesh_data == std::nullopt` (single-material / pre-slice / mock slice), `hasRealMesh = false`, `meshVertices` empty, and the renderer takes the Option A `else` branch (the v4.4 dimensioned box). A source-audit confirms both paths exist + the gate is `hasRealMesh`.</truth>
  <truth id="WM-07">SoftwareViewport (the fallback renderer) mirrors Option B: when hasRealMesh, the QPainter path draws the mesh projection (or, if non-trivial, documents the deferral with a justification — SoftwareViewport is a fallback; the primary path is RhiViewport). Either path closes the consistency gap.</truth>
  <truth id="WM-08">A regression test (ViewModelSmokeTests or PartPlateTests) asserts: (a) the Option B path fires only when hasRealMesh=true; (b) the Option A fallback fires when hasRealMesh=false; (c) the capture-by-value invariant (no TriangleMesh* escape). Mirrors the v4.4 readback test pattern.</truth>
  <truth id="WM-09">A QmlUiAuditTests source-audit slot `optionBWipeTowerMeshCoexistsWithOptionA` asserts: buildWipeTowerMeshVertices exists (parallel to buildWipeTowerVertices), uploadWipeTowerBuffer has the hasRealMesh branch, Option A is preserved. WTMESH-named QVERIFY2 messages.</truth>
  <truth id="WM-10">Canonical build + regression ctest 4+/4-equivalent passes; OWzxSlicer.exe links clean. The v4.4 Option A regression ctest (Phase 100-102 tests) still passes unchanged.</truth>
  <truth id="WM-11">`git diff --check` exit 0; encoding guard clean; English ASCII comments.</truth>
</must_haves>

<threat_model>
No new attack surface. The mesh is captured from trusted libslic3r state (wipe_tower_mesh_data, computed by the slicing engine). Capture-by-value (no TriangleMesh* escape — Frozen Decision 1 extended). The renderer uploads the captured vertices to an existing GPU buffer pattern. No network, no external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="109-01-01" type="implementation">
    <title>Extend WipeTowerGeometry + worker capture for real mesh</title>
    <read_first>
      - .planning/phases/109-option-b-wipe-tower-mesh-readback-and-real-rendering/109-CONTEXT.md
      - src/core/services/SliceService.h:42-60 (WipeTowerGeometry POD)
      - src/core/services/SliceService.cpp:634-693 (existing worker capture)
      - third_party/OrcaSlicer/src/libslic3r/Print.hpp:740-786 (WipeTowerData + WipeTowerMeshData)
      - third_party/OrcaSlicer/src/slic3r/GUI/3DScene.cpp:887-925 (upstream Option B merge + convex_hull_3d)
    </read_first>
    <files>
      - src/core/services/SliceService.h
      - src/core/services/SliceService.cpp
    </files>
    <action>
      Extend WipeTowerGeometry with `hasRealMesh` + `meshVertices`. Extend the worker capture: when `wipe_tower_mesh_data != nullopt`, merge real_wipe_tower_mesh + real_brim_mesh, convex_hull_3d(), extract its.vertices as flattened XYZ floats. Set hasRealMesh=true. No TriangleMesh* escapes. WM-01/WM-02.
    </action>
    <verify>
      `rg -n "hasRealMesh\|meshVertices" src/core/services/SliceService.h src/core/services/SliceService.cpp`
      Build clean.
    </verify>
    <acceptance_criteria>
      - POD extended (WM-01). Worker captures mesh by value (WM-02). Option A fields unchanged (WM-06).
    </acceptance_criteria>
  </task>

  <task id="109-01-02" type="implementation">
    <title>Add buildWipeTowerMeshVertices + uploadWipeTowerBuffer Option B branch</title>
    <read_first>
      - src/core/rendering/GizmoGeometry.h:74 + .cpp:449-533 (buildWipeTowerVertices — Option A, DO NOT MODIFY)
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp:1064-1095 (uploadWipeTowerBuffer)
      - src/qml_gui/Renderer/RhiViewportRenderer.h:216-222 (m_wipeTower* members)
    </read_first>
    <files>
      - src/core/rendering/GizmoGeometry.h
      - src/core/rendering/GizmoGeometry.cpp
      - src/qml_gui/Renderer/RhiViewportRenderer.h
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp
    </files>
    <action>
      Add `buildWipeTowerMeshVertices(const std::vector<float>& meshVertices, float x, float z)` to GizmoGeometry (parallel to buildWipeTowerVertices). Add the `hasRealMesh` branch to uploadWipeTowerBuffer (WM-03/WM-04). Add m_wipeTowerHasRealMesh + m_wipeTowerMeshVertices members + synchronize() pull (WM-05).
    </action>
    <verify>
      `rg -n "buildWipeTowerMeshVertices\|m_wipeTowerHasRealMesh\|m_wipeTowerMeshVertices" src/core/rendering/ src/qml_gui/Renderer/`
      Build clean.
    </verify>
    <acceptance_criteria>
      - New helper added (WM-03). Option B branch in uploadWipeTowerBuffer (WM-04). Option A path unchanged (WM-06).
    </acceptance_criteria>
  </task>

  <task id="109-01-03" type="implementation">
    <title>Wire EditorViewModel + RhiViewport Q_PROPERTYs + PreparePage binding + SoftwareViewport mirror</title>
    <read_first>
      - src/core/viewmodels/EditorViewModel.h/.cpp (v4.4 wipeTower Q_PROPERTYs — extend)
      - src/qml_gui/Renderer/RhiViewport.h:54-59 (Q_PROPERTYs — extend)
      - src/qml_gui/pages/PreparePage.qml:1670-1675 (GLViewport bindings — extend)
      - src/qml_gui/Renderer/SoftwareViewport.cpp:447-498 (Phase 101 QPainter box — mirror Option B or document deferral)
    </read_first>
    <files>
      - src/core/viewmodels/EditorViewModel.h
      - src/core/viewmodels/EditorViewModel.cpp
      - src/qml_gui/Renderer/RhiViewport.h
      - src/qml_gui/Renderer/RhiViewport.cpp
      - src/qml_gui/Renderer/SoftwareViewport.cpp
      - src/qml_gui/Renderer/SoftwareViewport.h
      - src/qml_gui/pages/PreparePage.qml
    </files>
    <action>
      Wire the hasRealMesh + meshVertices from EditorViewModel through RhiViewport Q_PROPERTYs to the renderer + PreparePage.qml bindings (WM-05). SoftwareViewport mirrors Option B (WM-07) — if QPainter mesh projection is non-trivial, document deferral with justification (SoftwareViewport is fallback; primary is RhiViewport).
    </action>
    <verify>
      `rg -n "hasRealMesh\|meshVertices\|wipeTowerHasRealMesh" src/core/viewmodels/ src/qml_gui/`
      Build clean.
    </verify>
    <acceptance_criteria>
      - Q_PROPERTY chain wired (WM-05). SoftwareViewport mirror or documented deferral (WM-07).
    </acceptance_criteria>
  </task>

  <task id="109-01-04" type="test">
    <title>Add regression test + source-audit slot</title>
    <read_first>tests/ViewModelSmokeTests.cpp, tests/QmlUiAuditTests.cpp</read_first>
    <files>
      - tests/ViewModelSmokeTests.cpp
      - tests/QmlUiAuditTests.cpp
    </files>
    <action>
      ViewModelSmokeTests: assert hasRealMesh gate (true → mesh path; false → Option A fallback) + capture-by-value invariant. QmlUiAuditTests: `optionBWipeTowerMeshCoexistsWithOptionA` source-audit asserting both paths + the gate. WM-08/WM-09.
    </action>
    <verify>ctest passes incl. new slots.</verify>
    <acceptance_criteria>Test + source-audit exist, deterministic, pass (WM-08, WM-09).</acceptance_criteria>
  </task>

  <task id="109-01-05" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest. Document the Option B implementation + the Option A fallback coexistence + the Frozen Decision 2 re-opening in SUMMARY.</action>
    <verify>SUMMARY records build + ctest pass + Option A no-regress.</verify>
    <acceptance_criteria>Production link clean, ctest pass (WM-10), Option A regression ctest unchanged.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest 4+/4-equivalent.
2. Source audit: `rg -n "buildWipeTowerMeshVertices\|hasRealMesh\|m_wipeTowerHasRealMesh" src/`
3. Encoding guard.
4. v4.4 Option A regression ctest (Phase 100-102 tests) unchanged.

## Success Criteria
- WTMESH-01/02/03 closed; every WM truth; Option A preserved (WTMESH-02); SoftwareViewport mirror or documented deferral (WTMESH-03).

## Expected Non-Changes
- Option A `buildWipeTowerVertices` / `uploadWipeTowerBuffer` structure unchanged (the frozen v4.4 baseline).
- No per-extruder color slabs on Option B mesh (anti-feature — upstream chose silhouette over stripes).
- WTMESH-04 regression lock — Phase 116.
- No libslic3r changes.
