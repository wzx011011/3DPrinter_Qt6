---
phase: 108-filament-map-auto-recommendation-readback
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 107-filament-map-mode-enum-widening-and-3mf-migration
    provides: 4-value FilamentMapMode enum (fmmAutoForFlush/fmmAutoForMatch/fmmManual/fmmDefault) at PartPlate.h:96-101.
files_modified:
  - src/core/services/SliceService.h
  - src/core/services/SliceService.cpp
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [FMAP-01]
---

# Phase 108 Plan: Filament-Map Auto Recommendation Readback

## Objective
Close FMAP-01: wire post-slice filament-map readback end-to-end, mirroring the v4.4 WipeTowerGeometry capture-by-value pattern. After `print.process()` succeeds, capture `Print::get_filament_maps()` + the resolved mode into a `FilamentMapResult` POD (no `Print*` escape), deliver via `filamentMapReady` signal (success branch only), expose on EditorViewModel for Phase 110 UI binding.

## Must Haves
<must_haves>
  <truth id="FR-01">A `FilamentMapResult` POD struct is declared in SliceService.h (immediately after the existing `WipeTowerGeometry` struct at :42), with fields: `bool valid`, `FilamentMapMode mode` (the Phase 107 enum), `std::vector<int> maps` (the auto-recommended per-extruder mapping). No `Print*` or libslic3r-types beyond the enum; pure value type. Documented as mirroring the v4.4 WipeTowerGeometry pattern.</truth>
  <truth id="FR-02">A `filamentMapReady(const FilamentMapResult &)` signal is declared in SliceService.h alongside the existing `wipeTowerGeometryReady` signal (:167). Emitted on the GUI thread via the same queued-invokeMethod pattern, on the success branch of sliceFinished ONLY (cancel/error branches do not emit — mirrors v4.4 WTREAD-02 gate).</truth>
  <truth id="FR-03">The SliceService worker captures `FilamentMapResult` BY VALUE in the same Print-lifetime window as v4.4 (between `print.process()` at SliceService.cpp:590 and `activePrint_.store(nullptr)` at :664). The capture reads `print.get_filament_maps()` (Print.cpp:3051) + the resolved mode. `FilamentMapResult.valid` is set true only when the auto-recommendation actually ran (mode < fmmManual per upstream Print.cpp:2485) — when the user picked Manual, valid stays false (no auto-map to surface).</truth>
  <truth id="FR-04">An `onFilamentMapReady` private slot is added to EditorViewModel (in the same `private slots:` block as `onWipeTowerGeometryReady`), wired to `SliceService::filamentMapReady`. The slot stores the result + emits a `filamentMapChanged` NOTIFY signal. Q_PROPERTY surface exposes: `bool hasAutoFilamentMap` (READ valid), `int filamentMapMode` (READ mode), `QVariantList filamentMaps` (READ maps). READ-only + NOTIFY (one-way libslic3r→GUI flow, mirroring v4.4).</truth>
  <truth id="FR-05">A regression test `filamentMapAutoRecommendationReadbackWired` (or similar) is added to ViewModelSmokeTests mirroring the v4.4 `wipeTowerGeometryReadbackAppliesValidAndInvalidGate` pattern: QSignalSpy + QMetaObject::invokeMethod to simulate the worker emit, assert the EditorViewModel Q_PROPERTYs reflect the captured result (valid path + invalid/manual path). Document the capture-by-value invariant in the test.</truth>
  <truth id="FR-06">A QmlUiAuditTests source-audit slot `filamentMapAutoRecommendationReadbackPresent` asserts the FilamentMapResult POD + signal + worker capture site + EditorViewModel slot exist. FMAP-01-named QVERIFY2 messages. Mirrors Phase 102/103/107 pattern.</truth>
  <truth id="FR-07">Canonical build + regression ctest 4/5-equivalent passes (PartPlateTests, PrepareSceneDataTests, ViewModelSmokeTests incl. new slot, QmlUiAuditTests incl. new slot). OWzxSlicer.exe links clean.</truth>
  <truth id="FR-08">`git diff --check` exit 0; encoding guard clean; English ASCII comments.</truth>
</must_haves>

<threat_model>
No new attack surface. The readback reads trusted libslic3r state (Print::get_filament_maps, already-computed by the slicing engine). Capture-by-value (no Print* escape). Delivered to GUI thread via the same queued-invokeMethod pattern as v4.4 (Frozen Decision 1). No network, no external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="108-01-01" type="implementation">
    <title>Add FilamentMapResult POD + filamentMapReady signal + worker capture</title>
    <read_first>
      - .planning/phases/108-filament-map-auto-recommendation-readback/108-CONTEXT.md
      - src/core/services/SliceService.h:42-60 (WipeTowerGeometry POD — mirror exactly)
      - src/core/services/SliceService.h:167 (wipeTowerGeometryReady signal — mirror)
      - src/core/services/SliceService.cpp:425, 634-658, 805 (worker capture + emit — add filament-map alongside)
      - src/core/model/PartPlate.h:96-101 (Phase 107 FilamentMapMode enum)
      - third_party/OrcaSlicer/src/libslic3r/Print.cpp:2484-2493, 3051 (auto-recommendation + get_filament_maps)
    </read_first>
    <files>
      - src/core/services/SliceService.h
      - src/core/services/SliceService.cpp
    </files>
    <action>
      **Step 1 — FilamentMapResult POD (SliceService.h, after WipeTowerGeometry):** Add `struct FilamentMapResult { bool valid = false; FilamentMapMode mode = fmmDefault; std::vector<int> maps; }`. Include the PartPlate.h header for the enum (or forward-declare + include in the .cpp). Document the v4.4 pattern lineage.

      **Step 2 — filamentMapReady signal (SliceService.h, near :167):** Add `void filamentMapReady(const FilamentMapResult &result);` alongside `wipeTowerGeometryReady`.

      **Step 3 — worker capture (SliceService.cpp):** Near the WipeTowerGeometry capture block (around :634-658), add a `FilamentMapResult capturedFilamentMap{};` declaration. After `print.process()` succeeds, capture: read `print.get_filament_maps()` into `capturedFilamentMap.maps`; resolve the mode (from the plate's filamentMapMode or the print config); set `valid = true` ONLY when the auto-recommendation ran (mode < fmmManual per upstream Print.cpp:2485). NO `Print*` escapes.

      **Step 4 — emit on success branch (SliceService.cpp, near :805):** Add the emit of `filamentMapReady(capturedFilamentMap)` alongside the existing `wipeTowerGeometryReady` emit, on the SAME success branch of the sliceFinished queued-invokeMethod lambda. Cancel/error branches do NOT emit.
    </action>
    <verify>
      `rg -n "FilamentMapResult\|filamentMapReady" src/core/services/SliceService.h src/core/services/SliceService.cpp`
      Build clean.
    </verify>
    <acceptance_criteria>
      - FilamentMapResult POD declared (FR-01).
      - filamentMapReady signal declared (FR-02).
      - Worker captures by value in the Print-lifetime window (FR-03).
      - Emit on success branch only (FR-02).
    </acceptance_criteria>
  </task>

  <task id="108-01-02" type="implementation">
    <title>Add EditorViewModel onFilamentMapReady slot + Q_PROPERTY surface</title>
    <read_first>
      - src/core/viewmodels/EditorViewModel.h (onWipeTowerGeometryReady slot + the 6 wipeTower Q_PROPERTYs — mirror)
      - src/core/viewmodels/EditorViewModel.cpp (onWipeTowerGeometryReady impl + signal connection — mirror)
    </read_first>
    <files>
      - src/core/viewmodels/EditorViewModel.h
      - src/core/viewmodels/EditorViewModel.cpp
    </files>
    <action>
      Add `onFilamentMapReady(const FilamentMapResult &)` private slot (in the same block as `onWipeTowerGeometryReady`). Wire it to `SliceService::filamentMapReady` in the constructor (mirror the wipeTower connection). Add 3 Q_PROPERTYs: `hasAutoFilamentMap` (bool READ), `autoFilamentMapMode` (int READ), `autoFilamentMaps` (QVariantList READ), all NOTIFY `filamentMapChanged`. The slot stores the result + emits filamentMapChanged. READ-only (one-way libslic3r→GUI).
    </action>
    <verify>
      `rg -n "onFilamentMapReady\|hasAutoFilamentMap\|autoFilamentMapMode\|autoFilamentMaps\|filamentMapChanged" src/core/viewmodels/EditorViewModel.h src/core/viewmodels/EditorViewModel.cpp`
      Build clean.
    </verify>
    <acceptance_criteria>
      - Slot + 3 Q_PROPERTYs + NOTIFY wired (FR-04).
    </acceptance_criteria>
  </task>

  <task id="108-01-03" type="test">
    <title>Add filamentMapAutoRecommendationReadbackWired regression test + source-audit slot</title>
    <read_first>
      - tests/ViewModelSmokeTests.cpp (the v4.4 wipeTowerGeometryReadbackAppliesValidAndInvalidGate slot — the pattern to mirror; the qRegisterMetaType<WipeTowerGeometry> static initializer)
      - tests/QmlUiAuditTests.cpp (Phase 102/103/107 source-audit pattern)
    </read_first>
    <files>
      - tests/ViewModelSmokeTests.cpp
      - tests/QmlUiAuditTests.cpp
    </files>
    <action>
      **Test (ViewModelSmokeTests):** Add `filamentMapAutoRecommendationReadbackWired` slot mirroring the v4.4 wipe-tower readback test. Use QSignalSpy + QMetaObject::invokeMethod to simulate the worker emit with both a valid result (mode=fmmAutoForFlush, maps={1,2,3}) and an invalid/manual result (valid=false). Assert the EditorViewModel Q_PROPERTYs reflect the captured result. If `qRegisterMetaType<FilamentMapResult>` is needed for the by-name invokeMethod, add a file-scope static initializer (mirror the WipeTowerGeometry one).

      **Source-audit (QmlUiAuditTests):** Add `filamentMapAutoRecommendationReadbackPresent` slot asserting: FilamentMapResult POD in SliceService.h, filamentMapReady signal, worker capture site (get_filament_maps), EditorViewModel onFilamentMapReady slot. FMAP-01-named QVERIFY2 messages.
    </action>
    <verify>
      ctest ViewModelSmokeTests + QmlUiAuditTests pass incl. new slots.
    </verify>
    <acceptance_criteria>
      - Test + source-audit slots exist, deterministic, pass (FR-05, FR-06).
    </acceptance_criteria>
  </task>

  <task id="108-01-04" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest. Document the readback wiring (capture-by-value invariant, success-branch-only gate, the Phase 107 enum integration) in SUMMARY.</action>
    <verify>SUMMARY records build + ctest pass + readback contract.</verify>
    <acceptance_criteria>Production link clean, ctest pass (FR-07).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest.
2. Source audit: `rg -n "FilamentMapResult\|filamentMapReady\|onFilamentMapReady\|get_filament_maps" src/`
3. Encoding guard.

## Success Criteria
- FMAP-01 closed; every FR truth; capture-by-value invariant preserved; success-branch-only gate.

## Expected Non-Changes
- No FMAP-03 UI (Phase 110).
- No FMAP-04 full round-trip (Phase 111).
- No change to the Phase 107 enum.
- No libslic3r changes.
- No Print* escape (Frozen Decision 1).
