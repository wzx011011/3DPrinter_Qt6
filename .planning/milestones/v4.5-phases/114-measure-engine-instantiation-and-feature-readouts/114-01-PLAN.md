---
phase: 114-measure-engine-instantiation-and-feature-readouts
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 112-per-volume-its-accessor-and-mesh-cache
    provides: volumeMeshIts accessor (the Measuring input).
  - phase: 113-scene-and-mesh-raycaster-port
    provides: SceneRaycaster hit result (facetIdx + point) that feeds get_feature.
files_modified:
  - src/core/rendering/MeasureEngine.h
  - src/core/rendering/MeasureEngine.cpp
  - src/core/rendering/AssemblyMeasureGeometry.h
  - src/core/rendering/AssemblyMeasureGeometry.cpp
  - src/core/rendering/CMakeLists.txt
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/PartPlateTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [MEASURE-03]
---

# Phase 114 Plan: Measure Engine Instantiation And Feature Readouts

## Objective
Close MEASURE-03: instantiate `Measure::Measuring` per-volume, wire it to the Phase 113 raycaster hit, produce real measurements (angle/distance/etc.) replacing the AABB stub. SurfaceFeature boundary scrubbed (pitfall 6).

## Must Haves
<must_haves>
  <truth id="ME-01">A new `MeasureEngine` helper in `src/core/rendering/MeasureEngine.h/.cpp` instantiates `Measure::Measuring` per-volume from the Phase 112 ITS, cached alongside the Phase 113 raycaster (lazy + invalidated on model change). NOT a reimplementation of the math — instantiate `Measure::Measuring` directly (Measure.hpp:119).</truth>
  <truth id="ME-02">The engine wires `Measuring::get_feature(face_idx, point, world_tran, only_select_plane)` (Measure.hpp:124) to the Phase 113 SceneRaycaster hit result (facetIdx + point). Returns the SurfaceFeature.</truth>
  <truth id="ME-03">SurfaceFeature boundary scrubbing (pitfall 6): the Qt layer extracts the measurement VALUES (angle, distance, position, feature type) into Qt-owned types (floats + an enum + QVector3D) BEFORE the libslic3r SurfaceFeature goes out of scope. NO raw `void* volume` or `vector<int>* plane_indices` escapes into Qt. A grep confirms no SurfaceFeature member pointer is stored on the Qt side.</truth>
  <truth id="ME-04">A measurement readout API is exposed (EditorViewModel Q_PROPERTYs OR a new MeasureEngine accessor): at minimum angle (radians or degrees), direct distance, perpendicular distance, distance XYZ. These are the values the Phase 115 UI will display. Mirrors upstream GLGizmoMeasure.cpp:1990-2048 readouts.</truth>
  <truth id="ME-05">The current AABB stub at AssemblyMeasureGeometry is either REPLACED by the real Measuring path OR augmented (AABB as a coarse fallback, Measuring as the precise path). Document the relationship — the user must understand what the readouts mean post-Phase-114.</truth>
  <truth id="ME-06">MeasureEngine.h/.cpp added to CMakeLists (the rendering helpers target).</truth>
  <truth id="ME-07">A regression test (PartPlateTests) asserts: (a) the engine produces a real measurement for a known hit on a known mesh (e.g. unit cube — assert the distance/angle matches expected); (b) the boundary scrubbing (no SurfaceFeature pointer stored). Deterministic.</truth>
  <truth id="ME-08">A QmlUiAuditTests source-audit slot `measureEngineInstantiatedPerVolume` asserts MeasureEngine exists + Measuring instantiation + the scrubbing guard. MEASURE-03-named QVERIFY2 messages.</truth>
  <truth id="ME-09">Canonical build + regression ctest passes; OWzxSlicer.exe links clean.</truth>
  <truth id="ME-10">`git diff --check` exit 0; encoding guard clean.</truth>
</must_haves>

<threat_model>
Reads trusted libslic3r state (Measure::Measuring on the ITS). The ITS is geometry data. SurfaceFeature scrubbing prevents UAF/pointer-escape (pitfall 6). No network, no external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="114-01-01" type="implementation">
    <title>Add MeasureEngine + per-volume Measuring cache + get_feature wiring + boundary scrubbing</title>
    <read_first>
      - .planning/phases/114-measure-engine-instantiation-and-feature-readouts/114-CONTEXT.md
      - .planning/research/PITFALLS.md (pitfall 6)
      - src/core/rendering/SceneRaycaster.h (Phase 113 hit result)
      - src/core/services/ProjectServiceMock.h (Phase 112 volumeMeshIts)
      - src/core/rendering/AssemblyMeasureGeometry.h/.cpp (the AABB stub)
      - third_party/OrcaSlicer/src/libslic3r/Measure.hpp:119-200 (Measuring + SurfaceFeature + DistAndPoints + AngleAndEdges)
      - third_party/OrcaSlicer/src/slic3r/GUI/GLGizmoMeasure.cpp:1990-2048 (upstream readouts)
      - src/core/rendering/CMakeLists.txt (registration)
    </read_first>
    <files>
      - src/core/rendering/MeasureEngine.h
      - src/core/rendering/MeasureEngine.cpp
      - src/core/rendering/AssemblyMeasureGeometry.h
      - src/core/rendering/AssemblyMeasureGeometry.cpp
      - src/core/rendering/CMakeLists.txt
    </files>
    <action>
      Create MeasureEngine: per-volume Measuring cache + get_feature wiring + boundary scrubbing. Wire to SceneRaycaster hit. Extract measurement values to Qt-owned types. Document the AABB-stub relationship (replace or augment). Add to CMakeLists. ME-01/02/03/05/06.
    </action>
    <verify>
      `ls src/core/rendering/MeasureEngine.h`
      `rg -n "Measuring\|get_feature\|SurfaceFeature" src/core/rendering/MeasureEngine.h src/core/rendering/MeasureEngine.cpp`
      `rg -n "void\*.*volume\|plane_indices" src/core/rendering/MeasureEngine.h src/core/rendering/MeasureEngine.cpp` (should be ZERO — scrubbed)
      Build clean.
    </verify>
    <acceptance_criteria>
      - MeasureEngine exists, instantiates Measuring per-volume (ME-01/02). Boundary scrubbed (ME-03). AABB relationship documented (ME-05). CMake registered (ME-06).
    </acceptance_criteria>
  </task>

  <task id="114-01-02" type="implementation">
    <title>Expose measurement readouts on EditorViewModel</title>
    <read_first>
      - src/core/viewmodels/EditorViewModel.h/.cpp (Q_PROPERTY pattern)
      - src/core/rendering/MeasureEngine.h (the readout source)
    </read_first>
    <files>
      - src/core/viewmodels/EditorViewModel.h
      - src/core/viewmodels/EditorViewModel.cpp
    </files>
    <action>
      Expose angle + direct/perpendicular distance + distance XYZ as EditorViewModel Q_PROPERTYs (or a dedicated accessor) for Phase 115 UI binding. NOTIFY signal on measurement update. ME-04.
    </action>
    <verify>
      `rg -n "measureAngle\|measureDistance\|measurePerpDistance" src/core/viewmodels/EditorViewModel.h`
      Build clean.
    </verify>
    <acceptance_criteria">Readout API exposed (ME-04).</acceptance_criteria>
  </task>

  <task id="114-01-03" type="test">
    <title>Add regression test + source-audit slot</title>
    <read_first>tests/PartPlateTests.cpp, tests/QmlUiAuditTests.cpp</read_first>
    <files>
      - tests/PartPlateTests.cpp
      - tests/QmlUiAuditTests.cpp
    </files>
    <action>
      Test: known hit on unit cube → real measurement (assert distance/angle matches expected). Source-audit: `measureEngineInstantiatedPerVolume` asserting MeasureEngine + Measuring + scrubbing. ME-07/08.
    </action>
    <verify>ctest passes incl. new slots.</verify>
    <acceptance_criteria>Test + source-audit exist, deterministic, pass (ME-07/08).</acceptance_criteria>
  </task>

  <task id="114-01-04" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest. Document the real-measurement path + boundary scrubbing + AABB-stub relationship in SUMMARY.</action>
    <verify>SUMMARY records build + ctest pass.</verify>
    <acceptance_criteria>Production link clean, ctest pass (ME-09).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest.
2. Source audit: `rg -n "MeasureEngine\|Measuring\|get_feature" src/core/rendering/` + `rg -n "void\*.*volume\|plane_indices" src/core/rendering/` (zero).
3. Encoding guard.

## Success Criteria
- MEASURE-03 closed; every ME truth; real measurements (not AABB stub); boundary scrubbed.

## Expected Non-Changes
- No Phase 115 snap UX (depends on this).
- No MEASURE-06 Assembly actions (LOCKED future).
- No libslic3r changes (instantiate, don't reimplement).
