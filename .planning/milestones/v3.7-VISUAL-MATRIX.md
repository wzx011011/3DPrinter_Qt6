# v3.7 Visual Acceptance Matrix

Milestone: Screenshot-Level UI Parity Closure

Build rule: phases 59-63 use source review, static checks, and encoding checks only. Phase 64 runs the single canonical full build and launches the app for visual acceptance.

## Targets

| Target | Reference | Acceptance |
| --- | --- | --- |
| Prepare page | `shotScreen/准备页.png` | Left sidebar matches upstream density, process options are inline, and no unavailable placeholder is visible. |
| Printer settings | `shotScreen/打印机参数设置页.png` | Independent compact settings window, no left group nav, top tabs, dense full-width option rows. |
| Material settings | `shotScreen/材料参数设置页.png` | Independent compact settings window, material tab strip and dense option rows match the reference. |
| Preview page | `shotScreen/预览页.png` | Left sidebar remains visible, right legend/stat panel is compact, layer and bottom sliders are visible. |

## Carry-Forward Gaps

| Gap | Phase | Status |
| --- | --- | --- |
| Left sidebar still exposes a separate parameter-list block instead of process inline options. | 60 | Closed - process options are inline under the process area. |
| Selected-object sidebar can show unavailable variable-layer placeholder text. | 60 | Closed - unavailable variable-layer placeholder copy was removed. |
| Settings dialog still includes group navigation and bottom action/search bars not present in target screenshots. | 61 | Closed - compact settings dialog removed the group nav and bottom bars. |
| Option rows remain too tall and slider-heavy for screenshot parity. | 60/61 | Closed structurally - compact rows and reduced numeric fields are in place. |
| Preview page needs screenshot-level comparison after settings/sidebar fixes. | 62/64 | Partially covered - source/static tests cover the layout; a reliable runtime preview screenshot was not captured in this pass. |

## Phase 64 Runtime Evidence

| Surface | Runtime artifact | Status |
| --- | --- | --- |
| Prepare page | `.planning/milestones/v3.7-runtime-prepare-final.png` | Passed structural acceptance: inline process options are visible and the old unavailable parameter-list copy is gone. Residual: option labels are still mostly English. |
| Process settings | `.planning/milestones/v3.7-runtime-process-settings.png` | Passed structural acceptance: compact independent dialog, top tabs, no left group nav, no bottom action bar. Residual: option labels are still mostly English. |
| Preview page | `.planning/milestones/v3.7-runtime-preview-final.png` | Not accepted as runtime evidence: the captured window remained on Prepare, so this artifact must not be used as proof of preview-page visual parity. |
| Build/test gate | `scripts/auto_verify_with_vcvars.ps1` | Passed: PrepareScene, PartPlate, ViewModel smoke, QML UI audit, PreviewParser, and E2E pipeline completed. |

## Residual Visual Gaps

| Gap | Severity | Next milestone target |
| --- | --- | --- |
| Config option display names are still mostly English in the left sidebar and settings dialog. | High | Localize/present upstream option labels through `ConfigOptionModel` instead of exposing raw internal labels. |
| Runtime preview-page screenshot was not reliably captured during Phase 64. | Medium | Add deterministic navigation/visual smoke for `tpPreview` or load a fixture G-code and capture the real Preview state. |
| QML startup still emits existing layout warnings for anchors and recursive layout rearrange. | Medium | Clean visible-surface layout warnings in the next visual hardening pass. |
