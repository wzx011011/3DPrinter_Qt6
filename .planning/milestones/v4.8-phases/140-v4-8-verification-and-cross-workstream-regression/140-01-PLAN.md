---
phase: 140-v4-8-verification-and-cross-workstream-regression
plan: 01
type: standard
wave: 1
depends_on: []
autonomous: true
requirements: [REGRESS-03]
files_modified:
  - tests/QmlUiAuditTests.cpp
  - .planning/phases/140-v4-8-verification-and-cross-workstream-regression/140-VERIFICATION.md
---

<objective>
Add the v4.8 cross-workstream regression lock slot (`v48CrossWorkstreamRegressionLocked`) to QmlUiAuditTests, mirroring the v4.6/v4.7 pattern, then run the canonical build + all ctest groups + app launch liveness as the final v4.8 milestone gate (REGRESS-03).

The slot source-audits the v4.8 workstream anchors: CGAL MeshBoolean/Drill activation (WS1), Assembly ASM-01 accessors/routing/renderer (WS2), en.ts i18n completion (WS3), plus re-assertion that v4.7 anchors still hold (no regression).
</objective>

<must_haves>
- `v48CrossWorkstreamRegressionLocked()` slot exists in QmlUiAuditTests.cpp and asserts all v4.8 workstream anchors.
- Canonical build (`powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`) exit 0.
- All ctest groups PASS (PrepareScene/PartPlate/ViewModel/UI/PreviewParser) including the new v4.8 slot.
- App launch liveness (`APP_RUNNING_PID`).
- Phase 140 VERIFICATION.md maps evidence to REGRESS-03.
</must_haves>
