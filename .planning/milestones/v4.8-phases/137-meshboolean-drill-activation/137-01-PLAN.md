# Plan 137-01: MeshBoolean + Drill Activation

**Phase:** 137 — MeshBoolean + Drill Activation
**Status:** Complete (backfilled from commit `a875c65`)
**Requirements:** CGAL-02, CGAL-03

## Objective

Activate the already-written MeshBoolean + Drill logic end-to-end: flip the availability flag, remove the mcut stub, rewire the boolean/drill call sites to the real `MeshBoolean` engine, and align gizmo status text + smoke tests.

## Tasks

1. **Flag flip** — `kCgalMeshBooleanAvailable = true`.
2. **Stub removal** — delete `MeshBoolean_mcut_stub.cpp` (LNK2005 with real `MeshBoolean.cpp` after Phase 136).
3. **`ProjectServiceMock::booleanObject`** — replace the stub branch with real `MeshBoolean::minus` / `self_union` calls for union/subtract/intersect.
4. **`ProjectServiceMock::drillObject`** — replace the stub with real `MeshBoolean::minus` (subtract tool mesh from target).
5. **`EditorViewModel::gizmoStatusText`** — drop the "CGAL unavailable" message for cases 11 (MeshBoolean) and 13 (Drill).
6. **`tests/ViewModelSmokeTests.cpp`** — update gizmo-availability assertions for the activated path.
7. **Verification** — canonical build + 5 ctest groups + E2E + app launch liveness.

## Verification

- `kCgalMeshBooleanAvailable` is `true`.
- `MeshBoolean_mcut_stub.cpp` no longer present; no LNK2005.
- `booleanObject` / `drillObject` invoke real `MeshBoolean` calls.
- gizmoStatusText cases 11/13 reflect availability.
- Canonical build exit 0; 5/5 ctest PASS; E2E PASS; `APP_RUNNING_PID` captured.

## Risk

- **Low–Medium.** The MeshBoolean engine itself was already written; this phase only flips a flag and rewires 2 call sites. Main risk was the LNK2005 duplicate symbol, resolved by deleting the stub.

## Out of scope

- Visual/UX polish of the MeshBoolean/Drill gizmo panels.
- OpenVDB-dependent hollow/support paint (separate blocker).
