# Milestone Audit: v5.8 Submodule Baseline Alignment + Hollow Editing

**Closed:** 2026-07-25
**Status:** Complete
**Result:** The v5.7→main merge build break is repaired (submodule aligned to
a 0632bae8-based compat branch); MMU extruder colours/count are now config-
driven; the Hollow gizmo edits/persists drain holes end to end. All
verification gates PASS.

## Original Intent vs Outcome

This milestone was **reactive, not planned**. The v5.7→main merge
(`36f951d`) pinned the submodule to the 4cb3b9ce baseline while origin/main's
ProjectServiceMock.cpp had migrated to 0632bae8's API surface, breaking the
build. The "complete the five suggestions" request then surfaced two feature
gaps (MMU config-driven state, Hollow editing) whose code-truth status was
misrepresented by stale `.planning/` docs. v5.8 captures both the repair and
the feature delivery.

| Phase | Intent | Outcome |
|---|---|---|
| 212 | Repair submodule alignment | 0632bae8 + 5 OWzx-compat commits on branch `owzx-cgal54-on-0632`; ProjectServiceMock adapted (split/auto_drop) |
| 213 | MMU config-driven colours/count | plateFilamentColours/filamentCount + mmuExtruderColors + single-material notice |
| 214 | Hollow data + logic layer | drain hole accessors + placeHollowPoint + hollowMarkerData + real delete |
| 215 | Hollow viewport routing + render + gates | RhiViewport routing + renderer markers + QML wiring + regression re-verify |

## Verification Evidence

| Gate | Result |
|---|---|
| Canonical build (OWzxSlicer.exe + all test exes) | Green |
| `multiPlateFullStateRoundTrip` | 3 passed, 0 failed |
| `E2EWorkflowTests::test_slice_produces_gcode_file` | 3 passed, 0 failed (real libslic3r slice; cereal resolved) |
| `mmuSegmentationPaintFeedsSlice` | 3 passed, 0 failed |
| `v50PresetIniAndCreateDialogWired` | 3 passed, 0 failed |
| `v50UnsavedChangesAndFilterWired` | 3 passed, 0 failed |
| `v50CompareDiffAndRoundTripWired` | 3 passed, 0 failed |

Note: the canonical verify script (`auto_verify_with_vcvars.ps1`) was
observed to be killed by the background-task runtime before completing its
full test sweep across two runs; the per-target incremental build +
per-test `-o file,txt` runs above constitute the verification evidence. A
single clean end-to-end canonical run remains desirable but is environment-
limited, not code-limited.

## Process Notes (Honest)

- This milestone was executed **without** the upfront GSD `/gsd-new-milestone`
  + `/gsd-execute-phase` entry points. Phases were backfilled from commits
  after the fact. The code is verified; the process artefacts are retrofitted.
- The earlier "merge then pin to 4cb3b9ce" decision (`36f951d`) was the root
  cause of the build break — it did not validate that origin/main's code had
  already moved to 0632bae8's API surface. AGENTS.md's "OWzx local compat
  child commit 4cb3b9ce" lock was stale relative to main's migration.
- Two stale artefacts misled investigation: `tests/output/test_results.txt`
  (2026-06-02, referenced the renamed `E2EPipelineTests` + resolved cereal
  SKIP) and the 07-23 `ViewModelSmokeTests.exe` binary (predated the 07-24
  print_sequence fix). Both are now removed / documented.

## Carry-Forward / Deferred

- **SLA slice path** (SLAPrint, PNG layers, SLA presets) — Hollow edits/persists
  holes but does not slice them. Remains a future milestone.
- **Hollow per-point selection / cylinder rendering** — MVP renders disc
  markers; full upstream `DrainHole::to_mesh()` geometry + selection-rect is a
  later refinement.
- **Push of main-repo v5.8 commits** — submodule compat branch is pushed; the
  main-repo commits remain local pending explicit push confirmation.
- **AGENTS.md submodule lock** — should be updated from `4cb3b9ce` to the new
  compat commit `bdbc7ec` (on branch `owzx-cgal54-on-0632`).
