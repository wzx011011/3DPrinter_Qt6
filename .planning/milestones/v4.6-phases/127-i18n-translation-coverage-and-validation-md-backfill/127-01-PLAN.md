---
phase: 127-i18n-translation-coverage-and-validation-md-backfill
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - i18n/zh_CN.ts
  - i18n/en.ts
  - i18n/ja.ts
  - i18n/ko.ts
  - i18n/de.ts
  - i18n/fr.ts
  - docs/i18n-workflow.md
  - .planning/phases/*/117-VALIDATION.md (and other backfills)
autonomous: true
requirements: [I18N-01, PROC-01]
---

# Phase 127 Plan: i18n Translation Coverage And VALIDATION.md Backfill

## Objective
Close I18N-01 (translation pipeline + core-string zh_CN translation) + PROC-01 (VALIDATION.md backfill). Honest scoping: proof-of-pipeline + v4.6-touched strings, not false 100%.

## Must Haves
<must_haves>
  <truth id="I18-01">The lupdate→translate→lrelease workflow is documented (docs/i18n-workflow.md) so future phases refresh .ts files. lupdate is run to capture all v4.6 strings (TickCode/paint/calibration) into the 6 .ts files.</truth>
  <truth id="I18-02">zh_CN.ts has the v4.6-touched UI strings translated (finished, not unfinished): PreviewLayerRail menu items (Add Pause/Add Color Change/Add Template/Edit/Delete), CustomGcodeDialog labels, paint gizmo panel text, calibration range labels. This is the proof-of-pipeline.</truth>
  <truth id="I18-03">ja/ko/de/fr .ts files are refreshed via lupdate (new strings captured) with a documented baseline + remaining-work estimate. NOT claimed as fully translated.</truth>
  <truth id="I18-04">lrelease produces non-empty .qm files for each language (the build/deploy picks them up). A translationPipelineDocumented source-audit slot asserts the workflow doc exists + zh_CN has finished translations for the v4.6 menu strings.</truth>
  <truth id="VAL-01">VALIDATION.md files are produced for v4.6 phases 117-126 (Phase 128 produces its own as the milestone verification). Backfill VALIDATION.md for previously-shipped phases missing them (v4.4/v4.5 carry-forward) where the phase directory still exists.</truth>
  <truth id="VAL-02">Canonical build (j6) + regression ctest passes (the .ts changes must not break the build; lrelease is part of the build).</truth>
</must_haves>

<threat_model>
No attack surface. Translation files are static text resources. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="127-01-01" type="implementation">
    <title>Document i18n workflow + lupdate refresh + translate v4.6 core strings to zh_CN</title>
    <read_first>
      - .planning/phases/127-i18n-translation-coverage-and-validation-md-backfill/127-CONTEXT.md
      - i18n/zh_CN.ts (current state — 0% finished)
      - CMakeLists.txt (Qt Linguist lupdate/lrelease wiring)
      - src/qml_gui/components/PreviewLayerRail.qml (v4.6 strings: Add Pause/Add Color Change/Add Template/Edit/Delete Custom G-code etc.)
    </read_first>
    <files>i18n/zh_CN.ts, i18n/en.ts, docs/i18n-workflow.md</files>
    <action>
      **Step 1 — workflow doc.** Write docs/i18n-workflow.md: how to run lupdate (extract new strings), how to translate in the .ts (Qt Linguist GUI or manual XML), how lrelease compiles .qm, and the rule that each phase touching qsTr strings must refresh the .ts.
      **Step 2 — lupdate.** Run lupdate on src/qml_gui to refresh all 6 .ts files (capture v4.6 strings). Confirm new strings appear as unfinished.
      **Step 3 — zh_CN translate core.** In zh_CN.ts, mark-as-finished the v4.6-touched strings with correct Chinese translations: "Add Pause"→"添加暂停", "Add Color Change"→"添加换色", "Add Template"→"添加模板", "Edit Custom G-code"→"编辑自定义 G-code", "Delete Pause/Color Change/Filament Change/Custom Template"→对应中文, CustomGcodeDialog prompts, calibration range labels. Translate the en.ts source strings too if they're marked untranslated.
      **Step 4 — ja/ko/de/fr refresh.** lupdate already refreshed them in Step 2. Mark a baseline (document the finished count). Do NOT translate all — leave unfinished with a remaining-work note in the workflow doc.
    </action>
    <verify>zh_CN.ts has finished translations for the v4.6 menu strings; workflow doc exists.</verify>
    <acceptance_criteria>I18-01, I18-02, I18-03.</acceptance_criteria>
  </task>

  <task id="127-01-02" type="test">
    <title>Add translationPipelineDocumented source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add translationPipelineDocumented slot: assert docs/i18n-workflow.md exists; assert zh_CN.ts contains at least N finished translations for v4.6 strings (grep for "添加暂停" or type="finished" count > 0 in zh_CN.ts). I18N-01-named QVERIFY2.</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>I18-04.</acceptance_criteria>
  </task>

  <task id="127-01-03" type="documentation">
    <title>Backfill VALIDATION.md for v4.6 + missing prior phases</title>
    <read_first>.planning/phases/117-*/117-VERIFICATION.md (pattern; VALIDATION is the Nyquist per-task map complement)</read_first>
    <files>.planning/phases/117-*/117-VALIDATION.md (and 118-126)</files>
    <action>Produce a VALIDATION.md (Nyquist per-task verification map) for each v4.6 phase 117-126. Backfill for v4.4/v4.5 phases missing them where the phase dir still exists in .planning/milestones/v4.5-phases/.</action>
    <verify>Files exist; per-task map populated.</verify>
    <acceptance_criteria>VAL-01.</acceptance_criteria>
  </task>

  <task id="127-01-04" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build (lrelease compiles .qm). Regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>VAL-02.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6) incl lrelease; 2. Regression ctest; 3. zh_CN finished-count > 0 for v4.6 strings; 4. VALIDATION.md files exist.

## Success Criteria
- I18N-01 + PROC-01 closed; pipeline documented; zh_CN core strings translated; VALIDATION.md backfilled.

## Expected Non-Changes
- No 100% ja/ko/de/fr translation (documented baseline + future work).
- No new languages.
- No product behavior change (translations only).
