---
phase: 121-painted-facet-overlay-render-and-brush-interaction
plan: 01
type: implementation
wave: 1
depends_on: [120]
files_modified:
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/Renderer/RhiViewport.h
  - src/qml_gui/Renderer/RhiViewport.cpp
  - src/qml_gui/Renderer/SoftwareViewport.h
  - src/qml_gui/Renderer/SoftwareViewport.cpp
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - src/qml_gui/pages/PreparePage.qml
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [PAINT-02, PAINT-03]
---

# Phase 121 Plan: Painted-Facet Overlay Render And Brush Interaction

## Objective
Close PAINT-02 (overlay render) + PAINT-03 (brush interaction). Reuse mesh pipeline; new buffer + reverse data channel + brush param wiring + sphere cursor + Software mirror.

## Must Haves
<must_haves>
  <truth id="OV-01">EditorViewModel gains `Q_PROPERTY(QByteArray paintOverlayData READ paintOverlayData NOTIFY paintDataChanged)`. Getter flattens the selected object's painted facets (Enforcer + Blocker; MMU: Extruder1..N) into a byte stream `[state:int, vx,vy,vz ×3 per triangle]`, world-transformed (reuse the paintAtFacet world transform). Dirty-gated on paintDataChanged.</truth>
  <truth id="OV-02">RhiViewport gains `Q_PROPERTY(QByteArray paintOverlayData ...)` + brush Q_PROPERTYs (brushRadius float, brushCursorType int, paintState int). QML binds these from editorVm (by gizmoMode). emitPaintPickIfActive reads these instead of the hardcoded 2.0/1/1.</truth>
  <truth id="OV-03">RhiViewportRenderer: new m_paintOverlayBuffer + uploadPaintOverlayBuffer (parse bytes → GizmoVertex with per-state color → ensureBuffer + uploadStaticBuffer) + renderPaintOverlay(cb) (gate m_gizmoMode in {6,7,10}; setGraphicsPipeline m_fillPipeline; draw). Render call point: after model mesh draw (RhiViewportRenderer.cpp:335), before highlight.</truth>
  <truth id="OV-04">Color mapping: Support/Seam Enforcer=light green (0.5,1,0.5,1), Blocker=light red (1,0.5,0.5,1). MMU=per-extruder filament colors (EditorViewModel exposes extruders_colors Q_PROPERTY).</truth>
  <truth id="OV-05">Brush sphere cursor: a translucent sphere (m_translucentFillPipeline) following the mouse at brushRadius (world space). GizmoGeometry::buildBrushSphereVertices helper. Color: left-button blue (0,0,1,0.25) / right-button red (1,0,0,0.25) / hover black (upstream cursor colors).</truth>
  <truth id="OV-06">SoftwareViewport mirror: paintScene (SoftwareViewport.cpp:460-483) appends painted faces to the `faces` vector with per-state color, participating in depth sort. paintOverlayData Q_PROPERTY added to SoftwareViewport too.</truth>
  <truth id="OV-07">PreparePage.qml: Support paint panel gains brush controls (tool selector Enforcer/Blocker, radius CxSlider, cursor type) — modeled on the existing seam panel (:2514-2606). Bind editorVm.supportPaint* properties.</truth>
  <truth id="OV-08">A `paintedFacetOverlayAndBrushInteraction` source-audit slot asserts: (a) EditorViewModel has paintOverlayData Q_PROPERTY; (b) RhiViewport has paintOverlayData + brush Q_PROPERTYs; (c) RhiViewportRenderer has renderPaintOverlay; (d) emitPaintPickIfActive no longer hardcodes brushRadius=2.0 (reads the Q_PROPERTY). PAINT-02/PAINT-03-named QVERIFY2.</truth>
  <truth id="OV-09">Canonical build (j6) + regression ctest passes; encoding guard clean.</truth>
</must_haves>

<threat_model>
No attack surface. Overlay renders local mesh data; brush params are user-controlled via QML controls. No network/auth. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="121-01-01" type="implementation">
    <title>Build reverse data channel: paintOverlayData Q_PROPERTY (EditorViewModel + RhiViewport + SoftwareViewport)</title>
    <read_first>
      - .planning/phases/121-painted-facet-overlay-render-and-brush-interaction/121-CONTEXT.md
      - src/core/viewmodels/EditorViewModel.h:530-547 (brush props), :1028 (paintDataChanged), .cpp:2519-2631 (paintAtFacet world transform)
      - src/core/rendering/PaintEngine.cpp:89-104 (getFacets)
      - src/qml_gui/Renderer/RhiViewport.h:29 (meshData Q_PROPERTY pattern), :310-315 (paintPickRequested)
      - src/qml_gui/Renderer/SoftwareViewport.cpp:53-61 (setMeshData pattern)
    </read_first>
    <files>src/core/viewmodels/EditorViewModel.h, src/core/viewmodels/EditorViewModel.cpp, src/qml_gui/Renderer/RhiViewport.h, src/qml_gui/Renderer/RhiViewport.cpp, src/qml_gui/Renderer/SoftwareViewport.h, src/qml_gui/Renderer/SoftwareViewport.cpp</files>
    <action>
      **Step 1 — EditorViewModel.paintOverlayData.** Getter: for selected object's volumes, call paintEngine_->getFacets for Enforcer + Blocker (MMU: Extruder1..N), flatten to `[state, vx,vy,vz ×3]` bytes with world transform applied. Return QByteArray. NOTIFY paintDataChanged (already emits).
      **Step 2 — extruders_colors Q_PROPERTY** (for MMU color mapping): expose the filament color list.
      **Step 3 — RhiViewport.paintOverlayData + brush Q_PROPERTYs.** Setter stores + update(). Add brushRadius/brushCursorType/paintState Q_PROPERTYs.
      **Step 4 — SoftwareViewport.paintOverlayData.** Same setter pattern.
      **Step 5 — comments.** English ASCII-only. Cite the meshData pattern + reverse-channel rationale.
    </action>
    <verify>`rg -n "paintOverlayData|brushRadius|brushCursorType" src/core/viewmodels/EditorViewModel.h src/qml_gui/Renderer/RhiViewport.h`</verify>
    <acceptance_criteria>OV-01, OV-02, OV-04 (extruders_colors).</acceptance_criteria>
  </task>

  <task id="121-01-02" type="implementation">
    <title>renderPaintOverlay + uploadPaintOverlayBuffer + brush sphere cursor (RhiViewportRenderer)</title>
    <read_first>
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp:236 (render), :314-367 (draw block), :938-965 (uploadModelBuffer), :1500 (ensureBuffer), :1823 (renderMoveGizmo gate pattern)
      - src/qml_gui/Renderer/RhiViewportRenderer.h:108-160 (buffer members)
      - src/core/rendering/GizmoVertex.h:12 (vertex format)
      - src/core/rendering/GizmoGeometry.cpp (build*Vertices helpers — add buildBrushSphereVertices)
      - src/qml_gui/Renderer/RhiViewport.cpp:996-1044 (emitPaintPickIfActive — replace hardcoded brush)
    </read_first>
    <files>src/qml_gui/Renderer/RhiViewportRenderer.h, src/qml_gui/Renderer/RhiViewportRenderer.cpp, src/qml_gui/Renderer/RhiViewport.cpp, src/core/rendering/GizmoGeometry.h, src/core/rendering/GizmoGeometry.cpp</files>
    <action>
      **Step 1 — m_paintOverlayBuffer + members** (mirror wipeTower buffer pattern).
      **Step 2 — uploadPaintOverlayBuffer.** Parse m_paintOverlayData bytes → GizmoVertex (per-state color from OV-04) → ensureBuffer + uploadStaticBuffer. World transform applied in EditorViewModel (bytes are already world-space), so renderer just copies.
      **Step 3 — renderPaintOverlay(cb).** Gate m_gizmoMode in {6,7,10}. setGraphicsPipeline(m_fillPipeline). draw(m_paintOverlayVertexCount). Insert call at RhiViewportRenderer.cpp:335 (after model mesh, before highlight).
      **Step 4 — brush sphere cursor.** GizmoGeometry::buildBrushSphereVertices(center, radius) → translucent sphere. renderBrushCursor(cb) draws it (m_translucentFillPipeline) gated on paint gizmo + mouse position. Color by button state.
      **Step 5 — emitPaintPickIfActive.** Replace hardcoded brushRadius=2.0/cursorType=1/paintState with the new Q_PROPERTYs.
      **Step 6 — comments.** English ASCII-only.
    </action>
    <verify>`rg -n "renderPaintOverlay|uploadPaintOverlayBuffer|buildBrushSphereVertices|m_paintOverlayBuffer" src/qml_gui/Renderer/`</verify>
    <acceptance_criteria>OV-03, OV-05.</acceptance_criteria>
  </task>

  <task id="121-01-03" type="implementation">
    <title>SoftwareViewport mirror + PreparePage.qml Support paint panel brush controls</title>
    <read_first>
      - src/qml_gui/Renderer/SoftwareViewport.cpp:428-598 (paintScene — append painted faces)
      - src/qml_gui/pages/PreparePage.qml:1946-1987 (Support info row — extend), :2514-2606 (seam panel template)
    </read_first>
    <files>src/qml_gui/Renderer/SoftwareViewport.cpp, src/qml_gui/Renderer/SoftwareViewport.h, src/qml_gui/pages/PreparePage.qml</files>
    <action>
      **Step 1 — SoftwareViewport paint overlay.** In paintScene, after mesh faces, append painted facets (from m_paintOverlayData) to the `faces` vector with per-state color; they participate in depth sort.
      **Step 2 — PreparePage Support paint panel.** Add brush controls (tool selector Enforcer/Blocker bound to supportPaintTool; radius CxSlider bound to supportPaintCursorRadius; cursor type combo). Model on the seam panel.
      **Step 3 — bind paintOverlayData + brush Q_PROPERTYs in QML** (RhiViewport + SoftwareViewport instances).
      **Step 4 — comments.** English ASCII-only.
    </action>
    <verify>Build clean; PreparePage Support panel has brush controls.</verify>
    <acceptance_criteria>OV-06, OV-07.</acceptance_criteria>
  </task>

  <task id="121-01-04" type="test">
    <title>Add paintedFacetOverlayAndBrushInteraction source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add slot asserting OV-08 (a/b/c/d).</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>OV-08.</acceptance_criteria>
  </task>

  <task id="121-01-05" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>OV-09.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- PAINT-02 + PAINT-03 closed; every OV truth satisfied; overlay reuses mesh pipeline (no new shader).

## Expected Non-Changes
- No Support/Seam/MMU slice integration (Phase 122/123).
- No screen-space circle cursor (sphere covers Phase 121).
- No TriangleSelector changes (Phase 120).
