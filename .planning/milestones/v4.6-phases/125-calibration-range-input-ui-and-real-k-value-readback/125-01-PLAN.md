---
phase: 125-calibration-range-input-ui-and-real-k-value-readback
plan: 01
type: implementation
wave: 1
depends_on: [124]
files_modified:
  - src/qml_gui/dialogs/CalibrationDialog.qml
  - src/core/viewmodels/CalibrationViewModel.h
  - src/core/viewmodels/CalibrationViewModel.cpp
  - src/core/services/CalibrationServiceMock.h
  - src/core/services/CalibrationServiceMock.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [CALIB-02, CALIB-03]
---

# Phase 125 Plan: Calibration Range Input UI And Real K-Value Readback

## Objective
Close CALIB-02 (range input UI) + CALIB-03 (real K-value readback). Phase 124 added the modes with hardcoded ranges; Phase 125 makes ranges user-editable + reads real results.

## Must Haves
<must_haves>
  <truth id="CR-01">CalibrationDialog.qml gains range input fields (start/end/step) — CxTextField or CxSpinBox with numeric validators. Default values come from the selected CalibrationType's calibStart/calibEnd/calibStep (the Phase 124 hardcoded defaults). The user-set values flow to CalibrationServiceMock → setCalibParams.</truth>
  <truth id="CR-02">CalibrationViewModel exposes the selected mode's default range + the user-editable range (Q_PROPERTY for calibStart/calibEnd/calibStep + setters that forward to CalibrationServiceMock before startSlice).</truth>
  <truth id="CR-03">The mock K-value writeback (CalibrationServiceMock.cpp:514 `0.04f + item*0.01`) is replaced with a real readback path: after a PA slice, parse the K-value from the sliced G-code (`; pressure_advance` / `M900 K` markers) and store it. For modes where libslic3r provides no machine-readable result, document the limitation honestly (the user reads the calibration print manually) — no fabricated readback.</truth>
  <truth id="CR-04">A `calibrationRangeInputAndKValueReadback` source-audit slot asserts: (a) CalibrationDialog.qml has range input fields; (b) CalibrationViewModel exposes the range Q_PROPERTYs; (c) the mock `0.04f + item*0.01` writeback is gone (replaced by a parse path or an honest "manual interpretation" doc). CALIB-02/CALIB-03-named QVERIFY2.</truth>
  <truth id="CR-05">Canonical build (j6) + regression ctest passes; encoding guard clean.</truth>
</must_haves>

<threat_model>
No new attack surface. Range inputs are numeric-validated (IntValidator/DoubleValidator bounds). K-value readback parses the locally-generated G-code. No network/auth. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="125-01-01" type="implementation">
    <title>Add range input UI to CalibrationDialog + ViewModel range properties</title>
    <read_first>
      - .planning/phases/125-calibration-range-input-ui-and-real-k-value-readback/125-CONTEXT.md
      - src/qml_gui/dialogs/CalibrationDialog.qml (current progress-only dialog)
      - src/core/viewmodels/CalibrationViewModel.h:32-56 (currentKValue/currentNValue + calibItem* accessors)
      - src/core/services/CalibrationServiceMock.h (CalibrationType struct with calibStart/calibEnd/calibStep)
      - src/core/services/CalibrationServiceMock.cpp:444 (setCalibParams flow)
    </read_first>
    <files>src/qml_gui/dialogs/CalibrationDialog.qml, src/core/viewmodels/CalibrationViewModel.h, src/core/viewmodels/CalibrationViewModel.cpp, src/core/services/CalibrationServiceMock.h, src/core/services/CalibrationServiceMock.cpp</files>
    <action>
      **Step 1 — ViewModel range properties.** Add Q_PROPERTY for calibStart/calibEnd/calibStep (read from the selected CalibrationType defaults; WRITE setters that update the service's pending range before startSlice).
      **Step 2 — CalibrationDialog range inputs.** Add a row of start/end/step CxSpinBox/CxTextField (numeric validators), bound to the ViewModel range properties. Show the selected mode's defaults, editable.
      **Step 3 — flow to setCalibParams.** When the user starts a calibration, the edited range flows: CalibrationViewModel → CalibrationServiceMock (override the hardcoded default) → setCalibParams(editedStart, editedEnd, editedStep) → startSlice.
      **Step 4 — comments.** English ASCII-only. Cite the Phase 124 hardcoded-defaults-as-seed rationale.
    </action>
    <verify>`rg -n "calibStart|calibEnd|calibStep|range" src/qml_gui/dialogs/CalibrationDialog.qml src/core/viewmodels/CalibrationViewModel.h`</verify>
    <acceptance_criteria>CR-01, CR-02.</acceptance_criteria>
  </task>

  <task id="125-01-02" type="implementation">
    <title>Replace mock K-value writeback with real readback (PA) + honest manual-interpretation doc (others)</title>
    <read_first>
      - src/core/services/CalibrationServiceMock.cpp:514 (mock `0.04f + item*0.01`)
      - src/core/services/SliceService.cpp (G-code output path — where to parse K from)
      - third_party/OrcaSlicer/src/slic3r/CalibUtils.cpp (upstream result parsing reference)
    </read_first>
    <files>src/core/services/CalibrationServiceMock.cpp, src/core/services/CalibrationServiceMock.h, src/core/viewmodels/CalibrationViewModel.cpp</files>
    <action>
      **Step 1 — PA K-value readback.** After a successful PA slice, parse `; pressure_advance` / `M900 K` markers from the sliced G-code. Store the real K-value in the calibration history (replace the mock `0.04f + item*0.01`).
      **Step 2 — honest manual-interpretation doc.** For FlowRate/TempTower/Vol_speed/VFA/Retraction (where the result is read from the physical print), document that the user interprets the calibration print manually (no machine-readable readback). No fabricated values. The history entry stores a status/notes field, not a fake K.
      **Step 3 — comments.** English ASCII-only. Cite upstream CalibUtils result parsing.
    </action>
    <verify>`rg -n "0.04f|pressure_advance|M900" src/core/services/CalibrationServiceMock.cpp`</verify>
    <acceptance_criteria>CR-03.</acceptance_criteria>
  </task>

  <task id="125-01-03" type="test">
    <title>Add calibrationRangeInputAndKValueReadback source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add slot asserting CR-04 (a/b/c).</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>CR-04.</acceptance_criteria>
  </task>

  <task id="125-01-04" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>CR-05.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- CALIB-02 + CALIB-03 closed; every CR truth satisfied; WS3 complete (with Phase 124).

## Expected Non-Changes
- No new calibration modes (Phase 124).
- No hardware modes.
- No fabricated readback for non-PA modes.
