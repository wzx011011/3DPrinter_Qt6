---
phase: 126-legacy-dead-code-page-cleanup
plan: 01
type: cleanup
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/main.qml
  - src/qml_gui/BBLTopbar.qml
  - src/qml_gui/qml.qrc
  - src/qml_gui/BackendContext.cpp
  - src/qml_gui/BackendContext.h
  - CMakeLists.txt
  - tests/QmlUiAuditTests.cpp
files_deleted:
  - src/qml_gui/pages/DeviceListPage.qml
  - src/qml_gui/pages/AuxiliaryPage.qml
  - src/qml_gui/pages/ModelMallPage.qml
  - src/qml_gui/panels/AuxiliaryListPanel.qml
  - src/core/services/AuxiliaryService.h
  - src/core/services/AuxiliaryService.cpp
autonomous: true
requirements: [CLEAN-01]
---

# Phase 126 Plan: Legacy Dead-Code Page Cleanup

## Objective
Close CLEAN-01: delete dead/orphaned pages + panels + the now-unreferenced AuxiliaryService, clean routes/qml.qrc/CMake/test residuals. Per No-Deprecated-UI rule. These are in the removed LAN/device/cloud scope — deletion, not repair.

## Must Haves
<must_haves>
  <truth id="CL-01">Delete DeviceListPage.qml, AuxiliaryPage.qml, ModelMallPage.qml, AuxiliaryListPanel.qml (4 dead QML files). Remove their qml.qrc entries.</truth>
  <truth id="CL-02">main.qml StackLayout slot 7 (AuxiliaryPage) is removed or replaced with a structural placeholder consistent with slots 2/8. BBLTopbar tab list no longer references tpPlaceholder1 if that tab was only for Auxiliary.</truth>
  <truth id="CL-03">AuxiliaryService.h/.cpp deleted (after confirming its only would-be consumer AuxiliaryListPanel is gone); BackendContext.cpp instantiation + BackendContext.h member removed; CMakeLists.txt compile entry removed. If any other consumer exists, KEEP the service and document.</truth>
  <truth id="CL-04">A `legacyDeadCodePagesRemoved` source-audit slot asserts: (a) none of the 4 deleted page names appear in qml.qrc; (b) main.qml has no AuxiliaryPage Loader; (c) AuxiliaryService is absent from BackendContext.h (or documented as retained with a reason). CLEAN-01-named QVERIFY2.</truth>
  <truth id="CL-05">Canonical build (j6) + regression ctest passes; no broken QML imports; the existing inventory/calibration audit tests updated if they asserted the deleted pages existed.</truth>
</must_haves>

<threat_model>
No new attack surface (pure deletion of unreachable code). Deleting dead code REDUCES attack surface. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="126-01-01" type="cleanup">
    <title>Delete 4 dead QML files + AuxiliaryService + clean routes/qrc/CMake</title>
    <read_first>
      - .planning/phases/126-legacy-dead-code-page-cleanup/126-CONTEXT.md
      - src/qml_gui/main.qml:476-571 (StackLayout), :553-564 (slot 7 AuxiliaryPage)
      - src/qml_gui/BBLTopbar.qml:58-64, 306-315 (tab lists)
      - src/qml_gui/qml.qrc (entries to remove: DeviceListPage:17, AuxiliaryListPanel:50, ModelMallPage:19)
      - src/qml_gui/BackendContext.cpp:112, BackendContext.h:20/464 (AuxiliaryService)
      - CMakeLists.txt (AuxiliaryService compile entry)
      - src/core/services/AuxiliaryService.h/.cpp (the service to delete — confirm no consumer first via grep)
    </read_first>
    <files>see files_modified/files_deleted frontmatter</files>
    <action>
      **Step 0 — confirm AuxiliaryService has no other consumer.** `grep -rn "auxiliaryService\|AuxiliaryService" src/ tests/`. If only AuxiliaryListPanel (being deleted) + BackendContext reference it, proceed to delete.
      **Step 1 — git rm the 4 QML files + AuxiliaryService.h/.cpp.**
      **Step 2 — main.qml slot 7.** Remove the AuxiliaryPage Loader (:553-564). Replace with a structural placeholder Item (like slot 2/8) OR remove tpPlaceholder1 from the StackLayout and BBLTopbar tab list. Prefer minimal: keep the slot count stable with a placeholder to avoid renumbering TabPosition.
      **Step 3 — BBLTopbar.qml.** If the "辅助" tab (:314) maps to tpPlaceholder1, remove that tab entry + its workflowTabs ref (:58-64). 
      **Step 4 — qml.qrc.** Remove the 4 deleted-file entries.
      **Step 5 — BackendContext.** Remove auxiliaryService_ instantiation (:112) + member (:464) + any forward decl (:20).
      **Step 6 — CMakeLists.txt.** Remove AuxiliaryService.cpp/.h from the source list.
      **Step 7 — comments.** English ASCII-only. Cite the No-Deprecated-UI rule + the removed-scope rationale.
    </action>
    <verify>`grep -rn "DeviceListPage\|AuxiliaryPage\|AuxiliaryListPanel\|ModelMallPage\|AuxiliaryService" src/` → empty (except removal-doc comments). Build clean.</verify>
    <acceptance_criteria>CL-01, CL-02, CL-03.</acceptance_criteria>
  </task>

  <task id="126-01-02" type="test">
    <title>Add legacyDeadCodePagesRemoved slot + fix any inventory/calibration audit residuals</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern; check :2009 ModelMallPage ref, :3179 ConfigPage residual), tests/InventoryAuditTests.cpp (:428/:552 ConfigPage comments)</read_first>
    <files>tests/QmlUiAuditTests.cpp, tests/InventoryAuditTests.cpp</files>
    <action>Add legacyDeadCodePagesRemoved slot. Update any test that asserted the deleted pages existed (e.g. if QmlUiAuditTests:2009 references ModelMallPage positively, remove/flip it). Update InventoryAuditTests ConfigPage comments if they list it as existing.</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>CL-04.</acceptance_criteria>
  </task>

  <task id="126-01-03" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Confirm no broken QML imports (the QML audit catches dangling refs). Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>CL-05.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest (QML audit catches broken imports); 3. grep for deleted names = empty; 4. Encoding guard.

## Success Criteria
- CLEAN-01 closed; every CL truth satisfied; 4 dead QML files + AuxiliaryService gone; build + QML audit clean.

## Expected Non-Changes
- No active page removed (Home/Prepare/Preview/Monitor/MultiMachine/Project/Calibration/Preferences/Assemble untouched).
- No product-feature change (these pages were dead/removed-scope already).
