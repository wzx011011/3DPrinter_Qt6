---
phase: 122-support-and-seam-paint-end-to-end
plan: 01
type: implementation
wave: 1
depends_on: [121]
files_modified:
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [PAINT-04]
---

# Phase 122 Plan: Support And Seam Paint End-To-End

## Objective
Close PAINT-04: Support/Seam painted facets write to ModelVolume FacetsAnnotation → slice consumes them → support/seam placement affected.

## Must Haves
<must_haves>
  <truth id="SS-01">ProjectServiceMock gains `writePaintToModelVolume(int obj, int vol, PaintKind kind, const Slic3r::TriangleSelector& selector)` (HAS_LIBSLIC3R guarded) that calls `mv->supported_facets.set(selector)` (Support) / `mv->seam_facets.set(selector)` (Seam). PaintKind enum {Support, Seam, Mmu}. FacetsAnnotation::set handles serialize + touch(). Also `clearPaintOnModelVolume(obj, vol, kind)` calling FacetsAnnotation::reset().</truth>
  <truth id="SS-02">EditorViewModel::paintAtFacet writes back to ModelVolume via writePaintToModelVolume after PaintEngine.paintAt (the selector is the authoritative source; FacetsAnnotation::set serializes it). Trigger slice invalidation (existing invalidateSliceResultsForCurrentPlate). Support: only write when gizmoMode==SupportPaint; Seam: gizmoMode==SeamPaint.</truth>
  <truth id="SS-03">clearSupportPaintOnSelection + clearSeamPaintOnSelection TODO stubs (EditorViewModel.cpp:985/:1063) implemented: PaintEngine.clearObject + ProjectServiceMock.clearPaintOnModelVolume(Support/Seam) + invalidate slice + emit stateChanged.</truth>
  <truth id="SS-04">A `supportAndSeamPaintFeedsSlice` source-audit slot asserts: (a) ProjectServiceMock has writePaintToModelVolume referencing supported_facets/seam_facets; (b) EditorViewModel paintAtFacet calls writePaintToModelVolume; (c) the clear stubs no longer have TODO-only bodies. PAINT-04-named QVERIFY2.</truth>
  <truth id="SS-05">Canonical build (j6) + regression ctest passes; encoding guard clean.</truth>
</must_haves>

<threat_model>
No attack surface. Paint data is user's own mesh editing flowing into the local slice engine. No network/auth. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="122-01-01" type="implementation">
    <title>Add writePaintToModelVolume + clearPaintOnModelVolume to ProjectServiceMock + wire EditorViewModel</title>
    <read_first>
      - .planning/phases/122-support-and-seam-paint-end-to-end/122-CONTEXT.md
      - src/core/services/ProjectServiceMock.h:388-462 (volume accessors), :112-115 (rawModel)
      - src/core/viewmodels/EditorViewModel.cpp:2519-2631 (paintAtFacet), :985-990 (clearSupport TODO), :1063-1065 (clearSeam TODO)
      - third_party/OrcaSlicer/src/libslic3r/Model.hpp:870/873/876 (FacetsAnnotation members), Model.cpp:3524-3540 (set/reset)
    </read_first>
    <files>src/core/services/ProjectServiceMock.h, src/core/services/ProjectServiceMock.cpp, src/core/viewmodels/EditorViewModel.h, src/core/viewmodels/EditorViewModel.cpp</files>
    <action>
      **Step 1 — PaintKind enum + writePaintToModelVolume.** In ProjectServiceMock, add enum PaintKind{Support,Seam,Mmu}. writePaintToModelVolume(obj,vol,kind,selector): get rawModel()->objects[obj]->volumes[vol]; if kind==Support mv->supported_facets.set(selector); Seam→seam_facets; Mmu→mmu_segmentation_facets. HAS_LIBSLIC3R guarded.
      **Step 2 — clearPaintOnModelVolume.** Reset the corresponding FacetsAnnotation (FacetsAnnotation::reset / clear).
      **Step 3 — EditorViewModel write-back.** In paintAtFacet, after paintEngine_->paintAt, call projectService_->writePaintToModelVolume(obj, vol, paintKindForCurrentGizmo(), selector). paintKindForCurrentGizmo maps gizmoMode 6→Support, 7→Seam, 10→Mmu.
      **Step 4 — implement clear stubs.** clearSupportPaintOnSelection/clearSeamPaintOnSelection: PaintEngine.clearObject + clearPaintOnModelVolume + invalidate + emit.
      **Step 5 — comments.** English ASCII-only. Cite FacetsAnnotation::set + the automatic slice/3MF flow.
    </action>
    <verify>`rg -n "writePaintToModelVolume|supported_facets|seam_facets|clearSupportPaintOnSelection" src/core/`</verify>
    <acceptance_criteria>SS-01, SS-02, SS-03.</acceptance_criteria>
  </task>
  <task id="122-01-02" type="test">
    <title>Add supportAndSeamPaintFeedsSlice source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add slot asserting SS-04 (a/b/c).</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>SS-04.</acceptance_criteria>
  </task>
  <task id="122-01-03" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>SS-05.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- PAINT-04 closed; every SS truth satisfied.

## Expected Non-Changes
- No MMU (Phase 123).
- No TriangleSelector/PaintEngine changes (Phase 120).
- No 3MF layer changes (automatic via FacetsAnnotation).
- No SliceService changes (clone carries FacetsAnnotation).
