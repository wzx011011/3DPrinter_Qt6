# Milestone v2.9 Requirements Archive — Implementation Realignment and Stabilization

**Archived:** 2026-06-25
**Milestone:** v2.9
**Total requirements:** 28
**Satisfied:** 28
**Outcome:** All requirements satisfied with source/test/planning evidence (see traceability below). No requirements were dropped or changed during the milestone.

> This is the archived requirements snapshot for v2.9. The active `.planning/REQUIREMENTS.md` was removed at milestone close; the next milestone (`/gsd-new-milestone`) will create a fresh one.

## Requirement Status Terms (in force during v2.9)

- **Real:** source-truth behavior is implemented and verified with deterministic evidence.
- **Hybrid:** a real path exists, but fallback/mock behavior remains or verification is incomplete.
- **Mock:** local simulation only.
- **Blocked:** requires an unavailable dependency, protocol, credential, or product decision.
- **Placeholder:** visible UI or enum exists but has no meaningful backend behavior.

## Planning Realignment

- [x] **PLAN-01** (Phase 10): Maintainer can read `.planning/INDEX.md`, `PROJECT.md`, `STATE.md`, `REQUIREMENTS.md`, and `ROADMAP.md` and see the same active milestone, baseline status, and next phase.
  - **Outcome:** Validated. All entry files agree on v2.9 as the current (now complete) milestone.
- [x] **PLAN-02** (Phase 10): Current git history through v2.8 is reflected in planning as shipped history, not as future v2.7/v2.8 scope.
  - **Outcome:** Validated. MILESTONES.md and entry files describe v2.7/v2.8 as landed history.
- [x] **PLAN-03** (Phase 10): Every active service/workflow surface is classified as Real, Hybrid, Mock, Blocked, or Placeholder with evidence.
  - **Outcome:** Validated. Classification matrix present in REQUIREMENTS.md baseline + INDEX.md.
- [x] **PLAN-04** (Phase 10): Missing rule-file references from AGENTS are either restored or redirected to existing canonical rules.
  - **Outcome:** Validated. `.codex/rules/{source-truth-migration,build-rules,qml-boundaries}.md` created. Non-blocking portability gap: references use capital-C `.Codex` while git tracks lowercase `.codex` (Windows-safe; case-sensitive-FS risk tracked as tech debt).
- [x] **PLAN-05** (Phase 10): Old CrealityPrint-era references outside historical evidence are classified as intentional history, vendor data, or cleanup debt.
  - **Outcome:** Validated. CrealityPrint references treated as historical evidence/vendor data; active source truth is OrcaSlicer.

## Source Hygiene

- [x] **HYGIENE-01** (Phase 11): Literal escape artifacts such as `\r\n` inside source comments are removed or converted to real line breaks where they affect behavior.
  - **Outcome:** Validated. Targeted `rg` returned no matches in touched active files.
- [x] **HYGIENE-02** (Phase 11): Encoding-damaged source comments and user-visible strings in active files are repaired or explicitly scoped for follow-up.
  - **Outcome:** Validated for touched files. Broad repo-wide scan deferred to future hygiene pass.
- [x] **HYGIENE-03** (Phase 11): Residual backup/source artifacts under `src/`, including `SliceService.cpp.backup`, are removed or moved to an explicit archive after ownership is confirmed.
  - **Outcome:** Validated. `SliceService.cpp.backup` removed; no `.backup` files remain under `src/`.
- [x] **HYGIENE-04** (Phase 11): Untracked baseline files introduced by recent implementation work are either committed as intentional work, ignored intentionally, or documented as external artifacts.
  - **Outcome:** Validated. `AppSettingsService.*` and `SoftwareViewport.*` are tracked implementation files (referenced by CMake + BackendContext). `.agents/`, `AGENTS.md`, `IMPLEMENTATION_SUMMARY.md` documented as untracked external artifacts.

## Calibration Stabilization

- [x] **CAL-01** (Phase 12): User can launch implemented calibration modes from visible UI paths rather than only through backend/internal wiring.
  - **Outcome:** Validated. BBLTopbar routes Flow Dynamics/Flow Rate/Temp Tower via `selectItemById` (`BBLTopbar.qml:678-701`).
- [x] **CAL-02** (Phase 12): PA, Flow Rate, and Temp Tower calibration paths have deterministic regression coverage for job creation or generated slice requests.
  - **Outcome:** Validated. `calibrationImplementedModesEmitSliceRequests` asserts mode/range/step/project-name for all three modes.
- [x] **CAL-03** (Phase 12): Calibration mock fallback behavior still works when `SliceService` is unavailable.
  - **Outcome:** Validated. `calibrationFallbackAndSliceCallbacksDriveProgress` runs the fallback timer path without SliceService.
- [x] **CAL-04** (Phase 12): Calibration modes not implemented in Qt6 are explicitly marked Pending or Blocked with upstream references.
  - **Outcome:** Validated. Bed Leveling (Blocked: hardware), Vibration Compensation (Blocked: resonance), Max Volumetric Speed (Pending) asserted unavailable by `calibrationUnsupportedModesAreExplicitlyUnavailable`.
- [x] **CAL-05** (Phase 12): Calibration progress and completion reporting are driven by the real slice path when slicing is active and by the fallback timer only in mock mode.
  - **Outcome:** Validated. `CalibrationServiceMock.cpp:353-357` emits `calibrationSliceRequested` then dispatches real `startSlice()`; mock timer path covered separately.

## Hybrid Integration Verification

- [x] **INT-01** (Phase 13): SSDP discovery parsing and timeout/error paths are covered by deterministic tests or fixtures.
  - **Outcome:** Validated. `int01_SsdpDiscoveryParsesMockResponse` covers Bambu/Creality datagrams, sender fallback, ports, serial normalization, timeout signal.
- [x] **INT-02** (Phase 13): MQTT status parsing and pause/resume/stop publish payloads are covered without requiring a live printer.
  - **Outcome:** Validated. `int04_MqttConnectionParamsAndTelemetryFields` + `int05_MqttCommandConstructionAndControlFlow` cover telemetry routing + command envelopes/topics. Live publish still requires hardware.
- [x] **INT-03** (Phase 13): FTP send-print path has deterministic success/error-path coverage without requiring a live printer.
  - **Outcome:** Validated. `int06_FtpUrlAndSendPrintRouting` covers access-code URL encoding, remote path, refusal without MQTT, missing-file error. Live upload still requires hardware.
- [x] **INT-04** (Phase 13): Camera stream behavior has deterministic no-stream/error-path coverage and an explicit note for real RTSP verification requirements.
  - **Outcome:** Validated. `int03_CameraStateMachineAndFrameToken` covers no-camera error, default RTSP URL, state transitions, frame token. Live RTSP decode still requires hardware.
- [x] **INT-05** (Phase 13): Software viewport / OpenGL fallback behavior is documented and covered by startup smoke evidence.
  - **Outcome:** Validated. `mainRegistersSoftwareViewportByDefault` enforces SoftwareViewport default + OpenGL behind `OWZX_OPENGL`.
- [x] **INT-06** (Phase 13): App settings and bed-shape persistence introduced by recent work are verified or explicitly deferred.
  - **Outcome:** Validated. `appSettingsAndEditorBedShapePersistDeterministically` covers QSettings clamping/persistence + EditorViewModel bed-shape reload.

## Visible Placeholder Triage

- [x] **UI-01** (Phase 14): Export project and export model menu actions either call real backend behavior or are visibly classified as deferred/blocked in planning.
  - **Outcome:** Validated. `main.qml` export handlers remain wired to save/export dialogs + backend/viewmodel calls.
- [x] **UI-02** (Phase 14): Preferences menu action opens a real Preferences workflow or is explicitly deferred without silent no-op behavior.
  - **Outcome:** Validated. `onPreferencesRequested: backend.openSettings()` wired; update-check button disabled instead of empty no-op handler.
- [x] **UI-03** (Phase 14): BBLTopbar calibration entries for implemented modes are enabled and routed to calibration viewmodel actions.
  - **Outcome:** Validated. Stable-id routing preserved from Phase 12; QmlUiAuditTests asserts it.
- [x] **UI-04** (Phase 14): Placeholder account, model store, publish, layer editing, AssembleView, and ModelMall/WebView surfaces are reclassified so broad UI presence is not counted as feature completion.
  - **Outcome:** Validated. ModelMall copy = local preview/unavailable; publish disabled; hidden topbar placeholders remain hidden.
- [x] **UI-05** (Phase 14): QML-side logic that affects durable behavior is identified and moved to C++ viewmodels/services or documented as presentation-only.
  - **Outcome:** Validated. No new durable business logic added in QML; empty runtime TODO handlers removed.

## Verification Gate

- [x] **VERIFY-01** (Phase 15): `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` passes after v2.9 planning and implementation stabilization.
  - **Outcome:** Validated. Exited 0 on 2026-06-25; QML UI audit + E2E pipeline passed.
- [x] **VERIFY-02** (Phase 15): `ViewModelSmokeTests.exe` is either run explicitly or documented as built-only with a reason.
  - **Outcome:** Validated. Run explicitly: `Totals: 32 passed, 0 failed, 0 skipped, 0 blacklisted`.
- [x] **VERIFY-03** (Phase 15): The final v2.9 evidence links each completed requirement to build/test output, source references, or an explicit manual verification note.
  - **Outcome:** Validated. Each requirement above cites source/test evidence or an explicit manual-verification note.

## Traceability (final status)

| Requirement | Phase | Status |
|---|---|---|
| PLAN-01 | Phase 10 | Complete (Validated) |
| PLAN-02 | Phase 10 | Complete (Validated) |
| PLAN-03 | Phase 10 | Complete (Validated) |
| PLAN-04 | Phase 10 | Complete (Validated — portability gap noted) |
| PLAN-05 | Phase 10 | Complete (Validated) |
| HYGIENE-01 | Phase 11 | Complete (Validated) |
| HYGIENE-02 | Phase 11 | Complete (Validated — repo-wide scan deferred) |
| HYGIENE-03 | Phase 11 | Complete (Validated) |
| HYGIENE-04 | Phase 11 | Complete (Validated) |
| CAL-01 | Phase 12 | Complete (Validated) |
| CAL-02 | Phase 12 | Complete (Validated) |
| CAL-03 | Phase 12 | Complete (Validated) |
| CAL-04 | Phase 12 | Complete (Validated) |
| CAL-05 | Phase 12 | Complete (Validated) |
| INT-01 | Phase 13 | Complete (Validated) |
| INT-02 | Phase 13 | Complete (Validated — live publish manual) |
| INT-03 | Phase 13 | Complete (Validated — live upload manual) |
| INT-04 | Phase 13 | Complete (Validated — live RTSP manual) |
| INT-05 | Phase 13 | Complete (Validated) |
| INT-06 | Phase 13 | Complete (Validated) |
| UI-01 | Phase 14 | Complete (Validated) |
| UI-02 | Phase 14 | Complete (Validated) |
| UI-03 | Phase 14 | Complete (Validated) |
| UI-04 | Phase 14 | Complete (Validated) |
| UI-05 | Phase 14 | Complete (Validated) |
| VERIFY-01 | Phase 15 | Complete (Validated) |
| VERIFY-02 | Phase 15 | Complete (Validated) |
| VERIFY-03 | Phase 15 | Complete (Validated) |

**Coverage:** 28 total · 28 mapped · 28 complete · 0 unmapped · 0 dropped.

*Archived 2026-06-25 via `/gsd-complete-milestone v2.9`.*
