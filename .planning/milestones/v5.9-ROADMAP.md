# Roadmap: v5.9 ConfigWizard Multi-Vendor Selection

**Defined:** 2026-07-25
**Status:** Completed (all 4 phases delivered, 2026-07-25)
**Verification gate:** owzx_app_core + test exes build green;
v56CrossWorkstreamRegressionLocked (incl. new WIZ-03 checks),
mmuSegmentationPaintFeedsSlice, v50PresetIniAndCreateDialogWired,
multiPlateFullStateRoundTrip all PASS.

The wizard was single-vendor by design (Phase 200 WIZ-02 fixed activeVendor to
vendorList[0]). v5.9 makes the vendor user-selectable with on-demand loading +
AppConfig-lite persistence. 46 upstream vendor JSONs are available
(Anker/Anycubic/BBL/Creality/Dremel/Elegoo/...).

## Phase 216 — Multi-Vendor Preset Loading (Backend)

**Status:** Complete · **Requirement:** WIZ-01, WIZ-02 · **Commit:** `0073308`

`loadSingleVendor(profilesDir, vendorFileName)` extracted from the
Creality-specific parse (vendor-agnostic: reads `<vendor>.json` + `<vendor>/`
subdir). `availableVendorNames()` scans `profiles/*.json`.
`loadVendor(name)` loads on demand. Idempotent via `m_loadedVendors`.

## Phase 217 — AppConfig-lite Persistence

**Status:** Complete · **Requirement:** WIZ-03 · **Commit:** `0073308`

`selectedVendor()` / `selectedPrinterModel()` + setters (QSettings
`wizard/selectedVendor`, `wizard/selectedPrinterModel`).

## Phase 218 — ConfigWizard Multi-Vendor UI

**Status:** Complete · **Requirement:** WIZ-04, WIZ-05 · **Commit:** `0073308`

`activeVendor` user-selectable; vendor picker combo (CxComboBox) at the top of
the Printer page; completion persists vendor + model.

## Phase 219 — Regression + Test Hardening

**Status:** Complete · **Requirement:** WIZ-06, VER-01..04 · **Commit:** `0073308`

v56CrossWorkstreamRegressionLocked gains WIZ-03 source-audit checks. All
regression gates PASS.
