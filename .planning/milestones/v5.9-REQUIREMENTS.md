# Requirements: v5.9 ConfigWizard Multi-Vendor Selection

**Defined:** 2026-07-25
**Core Value:** Promote the ConfigWizard from single-vendor (hard-coded
Creality) to multi-vendor selection, driven by on-demand vendor loading +
AppConfig-lite persistence. PresetUpdater (network) remains deferred.
**Status:** Active.

## WIZ: Multi-Vendor Wizard

- [x] **WIZ-01** (Phase 216): `PresetServiceMock::loadSingleVendor` is a
  vendor-agnostic loader (extracted from the Creality-specific parse); reads
  `<vendor>.json` + `<vendor>/` subdir machine/filament/process. Idempotent
  via `m_loadedVendors`.
- [x] **WIZ-02** (Phase 216): `availableVendorNames()` scans `profiles/*.json`
  for the vendor picker (filename scan, no preset load). `loadVendor(name)`
  loads a vendor on demand from the wizard.
- [x] **WIZ-03** (Phase 217): `selectedVendor()` / `selectedPrinterModel()` +
  setters persist the wizard choice (QSettings `wizard/selectedVendor`,
  `wizard/selectedPrinterModel`).
- [x] **WIZ-04** (Phase 218): ConfigWizardDialog.qml `activeVendor` is
  user-selectable (was fixed to `vendorList[0]`); a vendor picker combo at the
  top of the Printer page lists `availableVendorNames()` and loads the chosen
  vendor's presets on demand.
- [x] **WIZ-05** (Phase 218): Wizard completion persists `selectedVendor` +
  `selectedPrinterModel`.
- [x] **WIZ-06** (Phase 219): v56CrossWorkstreamRegressionLocked gains WIZ-03
  source-audit checks; existing DLG-01 vendor-enum checks still pass.

## VER: Regression

- [x] **VER-01** (Phase 219): v56CrossWorkstreamRegressionLocked PASS.
- [x] **VER-02** (Phase 219): mmuSegmentationPaintFeedsSlice PASS.
- [x] **VER-03** (Phase 219): v50PresetIniAndCreateDialogWired PASS.
- [x] **VER-04** (Phase 219): multiPlateFullStateRoundTrip PASS.

## Out of Scope

- **PresetUpdater** (HTTP vendor-index sync) — network scope, deferred.
- **Full 3-way material compatibility filtering** (upstream
  PageMaterials::update_lists) — future refinement.
- **SLA slice path** — separate milestone.
- **AMS real device data** — explicitly removed scope.
