---
phase: 56-parameter-settings-dialogs-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/Models/ConfigOptionModel.h
  - src/qml_gui/Models/ConfigOptionModel.cpp
  - src/qml_gui/controls/CxSpinBox.qml
  - tests/ViewModelSmokeTests.cpp
  - tests/E2EWorkflowTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [SETTINGS-03, SETTINGS-06]
user_setup: []

must_haves:
  truths:
    - "ConfigOptionModel exposes all 7 typed option categories: bool, int, float, enum, string, percent, nullable/multi-value"
    - "CxSpinBox renders a unit suffix when suffix property is set"
    - "Every SETTINGS-01..07 behavior has a declared test method (ViewModelSmokeTests / E2EWorkflowTests / QmlUiAuditTests) that currently fails predictably (RED), so no requirement can silently regress during execution"
  artifacts:
    - path: "src/qml_gui/Models/ConfigOptionModel.h"
      provides: "ConfigOption struct with nullable, isVector fields; NullableRole, IsVectorRole, SidetextRole"
      contains: "bool nullable"
    - path: "src/qml_gui/Models/ConfigOptionModel.cpp"
      provides: "mapType handles coPercent; loadSchemaFromKeys populates nullable/isVector/sidetext"
      contains: "coPercent"
    - path: "src/qml_gui/controls/CxSpinBox.qml"
      provides: "suffix property for unit text rendering"
      contains: "property string suffix"
    - path: "tests/ViewModelSmokeTests.cpp"
      provides: "RED test stubs for SETTINGS-01..06"
      contains: "testConfigOptionModelSevenTypes"
  key_links:
    - from: "src/qml_gui/Models/ConfigOptionModel.cpp::loadSchemaFromKeys"
      to: "Slic3r::print_config_def"
      via: "opt->nullable and (opt->type & coVectorType) reads"
      pattern: "opt->nullable|coVectorType"
---

<objective>
Wave 0 foundation: extend ConfigOptionModel to cover all 7 typed option kinds (bool, int, float, enum, string, percent, nullable/multi-value), add a unit-suffix property to CxSpinBox, and lay down the Wave 0 RED test scaffolds that downstream plans will turn green. No QML dialog work in this plan -- it is the typed-option-model foundation everything else renders.

Purpose: Every subsequent plan (C++ viewmodel, QML dialogs, integration) depends on a complete typed option model and the Wave 0 test scaffolding. Building this first means later waves can compile-test-iterate against a stable schema.
Output: Extended ConfigOptionModel (nullable/isVector/sidetext/percent), CxSpinBox.suffix, RED test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-CONTEXT.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md

@src/qml_gui/Models/ConfigOptionModel.h
@src/qml_gui/Models/ConfigOptionModel.cpp
@src/qml_gui/controls/CxSpinBox.qml
@tests/ViewModelSmokeTests.cpp

<interfaces>
<!-- Existing ConfigOption struct fields (ConfigOptionModel.h:11-51). This plan ADDS nullable + isVector. -->
struct ConfigOption {
  QString key, label, type;      // type currently in {"double","int","bool","enum"}
  QVariant value;
  double min, max, step;
  QStringList enumLabels;
  QString category, group, page;
  bool readonly = false;
  QString tooltip, sidetext;
  int mode = 2;
  // PLANNED ADDITIONS (this plan):
  //   bool nullable = false;   // accepts nil (inherit from parent preset)
  //   bool isVector = false;   // multi-value per-extruder (coFloats/coInts/coBools/coEnums/coPercents/coStrings)
};

<!-- Existing Roles enum (ConfigOptionModel.h:60-77). This plan ADDS NullableRole, IsVectorRole, SidetextRole. -->
enum Roles {
  KeyRole = Qt::UserRole + 1, LabelRole, TypeRole, ValueRole,
  MinRole, MaxRole, StepRole, EnumLabelsRole, CategoryRole,
  GroupRole, PageRole, ReadonlyRole, DirtyRole, TooltipRole, ModeRole
  // PLANNED ADDITIONS: NullableRole, IsVectorRole, SidetextRole
};

<!-- Upstream ConfigOptionType bitflags (Config.hpp:158-197): coVectorType = 0x4000. coFloats = coFloat + coVectorType. -->
<!-- Upstream ConfigOptionDef (Config.hpp:2217-2527) HAS: nullable bool, sidetext string, min/max float, mode, enum_values/labels, readonly. NO step field (synthesize). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ConfigOptionModel for 7 typed option kinds</name>
  <files>src/qml_gui/Models/ConfigOptionModel.h, src/qml_gui/Models/ConfigOptionModel.cpp</files>
  <read_first>
    - src/qml_gui/Models/ConfigOptionModel.h (current struct + Roles enum)
    - src/qml_gui/Models/ConfigOptionModel.cpp (loadSchemaFromKeys ~line 961, mapType ~line 588, setValue/resetOption ~line 337-390, data() role switch)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md (ConfigOptionDef struct, ConfigOptionType enum, Pitfalls 4/5 on nullable/isVector)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 277-403: ConfigOption struct extension + mapType changes + loadSchemaFromKeys additions)
  </read_first>
  <behavior>
    - Test testConfigOptionModelSevenTypes: after loadFromUpstreamSchema, at least one option exists for each of these type strings: "bool", "int", "double", "enum", "string", "percent".
    - Test testNullableAndVectorOptions: ConfigOptionModel exposes optNullable(i) returning true for at least one option, and optIsVector(i) returning true for at least one option (e.g., a coFloats key like "nozzle_temperature").
    - Test testPercentTypeDetected: a known coPercent key (e.g., "outer_wall_line_width" if percent, or "sparse_infill_density") reports type "percent" not "double".
    - Test testSidetextExposed: optSidetext(i) returns a non-empty string for at least one option whose ConfigOptionDef::sidetext is non-empty (e.g., "mm", "%").
  </behavior>
  <action>
    Add two fields to struct ConfigOption in ConfigOptionModel.h: `bool nullable = false;` and `bool isVector = false;`. Document nullable as "accepts nil value = inherit from parent preset (upstream ConfigOptionDef::nullable)" and isVector as "multi-value per-extruder (upstream coFloats/coInts/coBools/coEnums/coPercents/coStrings where type has coVectorType=0x4000 bit set)". ASCII-only comments.

    Extend Roles enum: append `NullableRole, IsVectorRole, SidetextRole` after ModeRole. Update roleNames() to map them to "nullable", "isVector", "sidetext".

    Update mapType() in ConfigOptionModel.cpp: add a case branch for `coPercent` and `coPercents` returning QStringLiteral("percent") BEFORE the coFloat catch-all (so coPercent no longer falls through to "double"). Keep coFloatOrPercent mapped to "double" (it is a float that may carry a percent suffix in sidetext). Reference upstream Config.hpp:158-197 for the enum values.

    Update loadSchemaFromKeys() to populate the new fields per option after computing entry.type: set `entry.nullable = opt->nullable;` and `entry.isVector = ((opt->type & Slic3r::coVectorType) != 0);`. The sidetext is already extracted to entry.sidetext -- verify it stays populated.

    Update data() switch to return for the new roles: NullableRole -> m_options[row].nullable, IsVectorRole -> m_options[row].isVector, SidetextRole -> m_options[row].sidetext.

    Add Q_INVOKABLE accessors matching the existing optXxx pattern:
    - `Q_INVOKABLE bool optNullable(int i) const;` returns m_options[i].nullable (bounds-checked, false if out of range).
    - `Q_INVOKABLE bool optIsVector(int i) const;` returns m_options[i].isVector.
    - `Q_INVOKABLE QString optSidetext(int i) const;` returns m_options[i].sidetext.
    - `Q_INVOKABLE QStringList groupNames() const;` returns the unique sorted group strings currently present in m_options (used by GroupNavSidebar).
    - `Q_INVOKABLE int dirtyCountForGroup(const QString &group) const;` counts keys in m_dirtyKeys whose m_options[key].group == group.

    Do NOT change setValue/resetOption semantics; existing dirty tracking is reused. Do NOT touch loadMachineSchema/loadFilamentSchema beyond what mapType/loadSchemaFromKeys already do for them.
  </action>
  <verify>
    <automated>cmake --build build --config Release</automated>
  </verify>
  <acceptance_criteria>
    - ConfigOptionModel.h contains `bool nullable` and `bool isVector` field declarations.
    - ConfigOptionModel.h Roles enum contains NullableRole, IsVectorRole, SidetextRole.
    - ConfigOptionModel.cpp mapType has a `coPercent`/`coPercents` case branch returning QStringLiteral("percent").
    - ConfigOptionModel.cpp loadSchemaFromKeys reads `opt->nullable` and `opt->type & Slic3r::coVectorType`.
    - ConfigOptionModel.h declares optNullable, optIsVector, optSidetext, groupNames, dirtyCountForGroup as Q_INVOKABLE.
    - `cmake --build build --config Release` exits 0 (compiles under HAS_LIBSLIC3R=1).
  </acceptance_criteria>
  <done>ConfigOptionModel exposes 7 typed kinds (bool/int/double/enum/string/percent + nullable + isVector flags); data() serves the 3 new roles; optNullable/optIsVector/optSidetext/groupNames/dirtyCountForGroup accessors compile and are Q_INVOKABLE.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit-suffix property to CxSpinBox</name>
  <files>src/qml_gui/controls/CxSpinBox.qml</files>
  <read_first>
    - src/qml_gui/controls/CxSpinBox.qml (current 49 lines; contentItem is a single TextInput)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 455-513: suffix pattern)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-UI-SPEC.md (Typed Control Matrix int row: unit suffix rendered as Text right of CxSpinBox)
  </read_first>
  <action>
    Add `property string suffix: ""` at the top of CxSpinBox (after `id: root`).

    Restructure contentItem so the TextInput and an optional suffix Text live inside a RowLayout (or an Item with anchored Text). Concretely: wrap the existing TextInput inside a RowLayout that fills the contentItem area, and append a `Text { visible: root.suffix !== ""; text: root.suffix; color: Theme.textTertiary; font.pixelSize: Theme.fontSizeXS; Layout.rightMargin: root.up.indicator.width + 4 }`. Keep the existing horizontalAlignment, validator, color, and selectionColor behavior of the TextInput. The suffix is rendered ONLY when suffix is non-empty (default "" preserves current behavior, so other consumers of CxSpinBox are unaffected).

    Do NOT change background, up.indicator, down.indicator, or implicitWidth. The suffix sits inside the content row only.

    Verify no other consumer of CxSpinBox breaks: the property defaults to empty string and the Text is invisible when suffix is "".

    ASCII-only comments. Reference the UI-SPEC int-type row in a one-line comment near the property declaration.
  </action>
  <verify>
    <automated>cmake --build build --config Release</automated>
  </verify>
  <acceptance_criteria>
    - CxSpinBox.qml contains `property string suffix` declaration.
    - When suffix === "" (default), the visual layout matches the previous behavior (suffix Text invisible).
    - When suffix is set (e.g. "mm"), a Text element appears to the right of the numeric input, colored Theme.textTertiary, font Theme.fontSizeXS.
    - `cmake --build build --config Release` exits 0; no QML warnings introduced for existing CxSpinBox consumers.
  </acceptance_criteria>
  <done>CxSpinBox supports an optional unit suffix; existing consumers see no visual change because suffix defaults to empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Wave 0 RED test scaffolds for SETTINGS-01..07</name>
  <files>tests/ViewModelSmokeTests.cpp, tests/E2EWorkflowTests.cpp, tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - tests/ViewModelSmokeTests.cpp (existing test setup pattern lines 340-428; dirty tracking pattern lines 612-648; testMachineOptionsLoaded lines 389-410)
    - tests/E2EWorkflowTests.cpp (test_config_injection_applies_preset_values pattern lines 154-174)
    - tests/QmlUiAuditTests.cpp (any existing raw-control audit method, if present)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md (Wave 0 Requirements section: full list of test method names)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 612-697: ViewModelSmokeTests + E2EWorkflowTests patterns; AUTOMOC caveat lines 763-769)
  </read_first>
  <behavior>
    - Each new test method is declared in the relevant private slots section and stubbed with a QFAIL("not yet implemented - Wave 0 scaffold") body so it FAILS (RED) immediately.
    - Test names exactly match the VALIDATION.md Wave 0 list so downstream plans can flip them green by replacing the QFAIL body.
  </behavior>
  <action>
    Add the following private slot declarations AND stub implementations to ViewModelSmokeTests.cpp (each body: ScopedApplicationIdentity + ScopedSettingsSnapshot setup matching the existing pattern, then `QFAIL("Wave 0 scaffold - implemented in 56-02/56-03");`):
    - testSettingsDialogOpenFromSidebar (SETTINGS-01)
    - testTabsAndGroupNavPerTier (SETTINGS-02)
    - testConfigOptionModelSevenTypes (SETTINGS-03 -- depends on Task 1; can already pass for type coverage but leave scaffold body until 56-02)
    - testPerOptionDirtyAndValueSource (SETTINGS-04)
    - testReadonlyBuiltinGating (SETTINGS-04)
    - testSaveSaveAsResetOptionResetGroupResetAll (SETTINGS-05)
    - testUnsavedChangesGuardOnDirtyClose (SETTINGS-05)
    - testPerDialogSearchAndFourLevelMode (SETTINGS-06)
    - testNullableAndVectorOptions (SETTINGS-03 -- depends on Task 1)

    Add to E2EWorkflowTests.cpp:
    - testSettingsEditInvalidatesSlice (SETTINGS-07 -- asserts option edit -> stalePlateIndices updates; depends on 56-03 wiring but the test body can be scaffolded now)
    - testDirtyOverridesPersistAcrossProjectSaveRestore (SETTINGS-07)

    Add to QmlUiAuditTests.cpp (if this file exists; if not, the planner accepts a placeholder method in the existing test target that registers a settingsDialogUsesOnlyCxControls check):
    - settingsDialogUsesOnlyCxControls
    - settingsDialogNoRawControls
    - settingsDialogStringsQsTr
    - settingsDialogMainQmlDispatchStructural (asserts main.qml declares Connections { target: backend; function onSettingsRequested(category) } dispatching to printerSettingsDialog.show() / materialSettingsDialog.show() / processSettingsDialog.show() -- structural half of SETTINGS-01; the runtime half is testSettingsDialogOpenFromSidebar in 56-04 Task 2)

    Each stub method MUST use the ScopedApplicationIdentity + ScopedSettingsSnapshot pattern from existing tests so it does not corrupt user settings when run later. Each method body ends with `QFAIL("Wave 0 scaffold");` so the suite is RED until later plans implement it.

    AUTOMOC caveat: after adding private slots, the executor MUST re-run cmake configure or delete `build/ViewModelSmokeTests_autogen/timestamp` before rebuilding (documented in tests/ViewModelSmokeTests.cpp header comment). Note this in a comment near the new declarations.

    Update tests/CMakeLists.txt (or wherever the test target's source list lives -- check existing test registration) ONLY if the new methods require new mocs; the automoc should pick them up automatically since they are private slots in an existing Q_OBJECT class.
  </action>
  <verify>
    <automated>ctest --test-dir build -R 'ViewModelSmokeTest|E2E|QmlUiAudit' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - tests/ViewModelSmokeTests.cpp declares all 9 listed test methods as private slots.
    - tests/E2EWorkflowTests.cpp declares testSettingsEditInvalidatesSlice and testDirtyOverridesPersistAcrossProjectSaveRestore.
    - tests/QmlUiAuditTests.cpp (or equivalent test target) declares settingsDialogUsesOnlyCxControls, settingsDialogNoRawControls, settingsDialogStringsQsTr.
    - Each new method body contains `QFAIL("Wave 0 scaffold` (so ctest is RED).
    - `ctest --test-dir build -R ViewModelSmokeTest` runs and the new test methods show as FAIL (not crash, not pass).
  </acceptance_criteria>
  <done>All Wave 0 RED scaffolds exist, compile, and fail predictably with the Wave 0 marker. Downstream plans replace the QFAIL bodies with real assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Local desktop settings UI; no network input, no auth surface, no persistence beyond local preset files already handled by PresetServiceMock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-SC | Tampering | (none) | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: N/A -- all components project-internal). No supply-chain surface. |
| T-56-01 | All | ConfigOptionModel + CxSpinBox + test scaffolds | accept | Local desktop UI extension; no network/auth/persistence surface added. ASVS L1 categories do not apply. |
</threat_model>

<verification>
- `cmake --build build --config Release` exits 0 after Task 1 + Task 2.
- `ctest --test-dir build -R 'ViewModelSmokeTest|E2E|QmlUiAudit' --output-on-failure` runs and the Wave 0 scaffolds fail predictably with the "Wave 0 scaffold" marker (RED state, ready for 56-02/56-03/56-04 to turn green).
- After Wave 1 (this plan) completes, run the canonical `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` to confirm full pipeline (configure + build + smoke) is green for everything except the new RED scaffolds.
</verification>

<success_criteria>
- ConfigOptionModel exposes all 7 typed kinds via type + nullable + isVector.
- CxSpinBox accepts a unit suffix without breaking existing consumers.
- Wave 0 RED test scaffolds exist for every SETTINGS-01..07 test declared in VALIDATION.md.
- Build is green; only the new Wave 0 tests are intentionally RED.
</success_criteria>

<output>
Create `.planning/phases/56-parameter-settings-dialogs-restoration/56-01-SUMMARY.md` when done.
</output>
