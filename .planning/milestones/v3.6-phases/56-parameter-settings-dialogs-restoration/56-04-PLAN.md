---
phase: 56-parameter-settings-dialogs-restoration
plan: 04
type: execute
wave: 4
depends_on: ['56-03']
files_modified:
  - tests/E2EWorkflowTests.cpp
  - tests/ViewModelSmokeTests.cpp
  - .planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md
autonomous: true
requirements: [SETTINGS-07, SETTINGS-03, SETTINGS-04, SETTINGS-05, SETTINGS-06]
user_setup: []

must_haves:
  truths:
    - "Editing any option inside a SettingsDialog triggers ConfigViewModel::stateChanged which (via Phase-52 connect) calls EditorViewModel::invalidateAllSliceResults, surfacing hasStaleSliceResults=true"
    - "Dirty user overrides persist via the existing 3MF scoped-config path across project save/restore (no forced Save As)"
    - "All Wave 0 RED scaffolds for SETTINGS-03/04/05/06 ViewModelSmokeTests are now GREEN with real assertions"
    - "VALIDATION.md per-task map is finalized and frontmatter set to nyquist_compliant: true"
  artifacts:
    - path: "tests/E2EWorkflowTests.cpp"
      provides: "testSettingsEditInvalidatesSlice + testDirtyOverridesPersistAcrossProjectSaveRestore (GREEN)"
      contains: "testSettingsEditInvalidatesSlice"
    - path: "tests/ViewModelSmokeTests.cpp"
      provides: "All remaining Wave 0 scaffolds flipped GREEN"
      contains: "testNullableAndVectorOptions"
    - path: ".planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md"
      provides: "Finalized per-task map + nyquist_compliant frontmatter"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/E2EWorkflowTests.cpp::testSettingsEditInvalidatesSlice"
      to: "BackendContext connect (Phase-52)"
      via: "ConfigOptionModel.setValue -> stateChanged -> editorVm.invalidateAllSliceResults"
      pattern: "invalidateAllSliceResults|stalePlateIndices|hasStaleSliceResults"
---

<objective>
Wave 4 integration + verification: turn the remaining Wave 0 RED scaffolds GREEN with real assertions covering SETTINGS-03/04/05/06/07. The two E2E tests (testSettingsEditInvalidatesSlice, testDirtyOverridesPersistAcrossProjectSaveRestore) prove the SETTINGS-07 integration paths -- option edit -> slice invalidation via the Phase-52 connect, and dirty overrides persisting via the 3MF scoped-config path. Finalize 56-VALIDATION.md (per-task map + frontmatter) per the Phase-55 finalize pattern.

Purpose: Settings edits must measurably flow through to slice invalidation and project persistence. These tests are the automated evidence for SETTINGS-07 and lock in the remaining ViewModelSmokeTest coverage for SETTINGS-03..06.
Output: GREEN integration tests for SETTINGS-07; GREEN unit tests for remaining SETTINGS-03..06 scaffolds; finalized VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-CONTEXT.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md

@src/core/viewmodels/ConfigViewModel.h
@src/core/services/ProjectServiceMock.h
@src/core/viewmodels/EditorViewModel.h
@src/qml_gui/BackendContext.cpp
@tests/E2EWorkflowTests.cpp
@tests/ViewModelSmokeTests.cpp

<interfaces>
<!-- Phase-52 already-wired connect (BackendContext.cpp:95-109). This plan asserts it fires; it does NOT add a new connect. -->
connect(configViewModel_, &ConfigViewModel::stateChanged, editorViewModel_,
        [this]() { editorViewModel_->invalidateAllSliceResults(); emit editorViewModel_->stateChanged(); });

<!-- EditorViewModel state surfaced for assertion (already exists, used by Phase 52) -->
class EditorViewModel {
  Q_PROPERTY(QVariantList stalePlateIndices ...)
  Q_PROPERTY(bool hasStaleSliceResults ...)
  Q_INVOKABLE void invalidateAllSliceResults();
};

<!-- ProjectServiceMock existing scoped-config API for SETTINGS-07 persistence (already exists, used by Phase 52/46) -->
class ProjectServiceMock {
  // Scoped config overrides (per-plate / per-object) -- dirty user-overrides persist via this path
  void applyScopeConfigValues(const QHash<QString, QVariant> &scoped);
  QHash<QString, QVariant> scopedConfigValues() const;
  // 3MF save/load (already existed):
  bool saveProject(const QString &path);
  bool loadProject(const QString &path);
};

<!-- ConfigViewModel API for SETTINGS-07 (existing + 56-02 additions) -->
Q_INVOKABLE QHash<QString, QVariant> mergedConfigValues() const;
Q_INVOKABLE void applyProjectConfig(const QHash<QString, QVariant> &config);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SETTINGS-07 integration tests (slice invalidation + persistence)</name>
  <files>tests/E2EWorkflowTests.cpp</files>
  <read_first>
    - tests/E2EWorkflowTests.cpp (existing test_config_injection_applies_preset_values pattern lines 154-174; the test target's setup/teardown conventions)
    - src/qml_gui/BackendContext.cpp (Phase-52 connect at lines 95-109 -- the wiring under test)
    - src/core/viewmodels/EditorViewModel.h (stalePlateIndices, hasStaleSliceResults, invalidateAllSliceResults)
    - src/core/services/ProjectServiceMock.h (scopedConfigValues / applyScopeConfigValues; saveProject/loadProject)
    - src/core/viewmodels/ConfigViewModel.h (mergedConfigValues, applyProjectConfig, setValue path via option models)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 676-697 E2E pattern)
  </read_first>
  <behavior>
    - testSettingsEditInvalidatesSlice: construct ConfigViewModel + EditorViewModel + ProjectServiceMock; wire the same connect that BackendContext wires (configVm.stateChanged -> editorVm.invalidateAllSliceResults); assert initial hasStaleSliceResults is false; mutate an option via the option model's setValue(row, v); assert QSignalSpy caught stateChanged; assert editorVm.hasStaleSliceResults is now true and stalePlateIndices contains at least one plate id.
    - testDirtyOverridesPersistAcrossProjectSaveRestore: set a dirty override on a print/printer/filament option; call configVm.mergedConfigValues() to capture; simulate project save via ProjectServiceMock (applyScopeConfigValues with the merged values, saveProject to a temp path); reload into a fresh ConfigViewModel via applyProjectConfig(loadProject); assert the dirty override value is restored.
  </behavior>
  <action>
    Replace the RED scaffold bodies of testSettingsEditInvalidatesSlice and testDirtyOverridesPersistAcrossProjectSaveRestore (added in 56-01) with real implementations:

    For testSettingsEditInvalidatesSlice:
    1. ScopedApplicationIdentity + ScopedSettingsSnapshot setup per existing pattern.
    2. Construct PresetServiceMock preset; ProjectServiceMock project; ConfigViewModel config(&preset, &project); EditorViewModel editor; wire editor.setConfigViewModel(&config) and the connect `QObject::connect(&config, &ConfigViewModel::stateChanged, &editor, [&](){ editor.invalidateAllSliceResults(); });` (mirrors BackendContext.cpp:95-109).
    3. QSignalSpy spyStateChanged(&config, &ConfigViewModel::stateChanged); QSignalSpy spyStale(&editor, &EditorViewModel::stateChanged).
    4. QVERIFY(!editor.hasStaleSliceResults()) at baseline.
    5. Get the print option model (qobject_cast<ConfigOptionModel*>(config.printOptions())); find a known dirty-able row (e.g., "layer_height"); read original value; call printOpts->setValue(idx, originalValue + 0.01).
    6. QVERIFY(spyStateChanged.count() >= 1); QVERIFY(editor.hasStaleSliceResults()); QVERIFY(editor.stalePlateIndices().size() >= 1) (or whichever plate count PresetServiceMock seeds by default).
    7. Reset the option (printOpts->resetOption(idx)); verify stateChanged fires again and (per current invalidateAllSliceResults semantics) the stale flag either stays true until next slice or clears -- assert the actual behavior, do NOT invent. Document the observed semantic in the test comment.

    For testDirtyOverridesPersistAcrossProjectSaveRestore:
    1. ScopedApplicationIdentity + ScopedSettingsSnapshot setup.
    2. Construct ConfigViewModel with PresetServiceMock and ProjectServiceMock.
    3. Pick an option key (e.g., "layer_height"), set it via the option model to a non-default value (dirty).
    4. Capture merged = config.mergedConfigValues(); QVERIFY(merged.contains("layer_height")).
    5. project.applyScopeConfigValues(merged); save via QTemporaryFile path: project.saveProject(tmpPath).
    6. Construct fresh ConfigViewModel config2(&preset, &project2); load project2.loadProject(tmpPath); config2.applyProjectConfig(project2.scopedConfigValues()).
    7. Read the restored value for "layer_height" via config2.mergedConfigValues(); QCOMPARE(restored, mutatedValue).

    Use QTemporaryFile/QTemporaryDir for any file paths so the test does not leave artifacts. Use ScopedApplicationIdentity to avoid corrupting the user's real OWzxSlicer QSettings.

    Follow the AUTOMOC caveat from tests/ViewModelSmokeTests.cpp header comment when adding private slots: re-run cmake configure or delete the autogen timestamp file before rebuilding if moc does not pick up the new methods.

    ASCII-only comments.
  </action>
  <verify>
    <automated>cmake --build build --config Release &amp;&amp; ctest --test-dir build -R E2E --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - testSettingsEditInvalidatesSlice asserts: option edit -> ConfigViewModel::stateChanged fires -> EditorViewModel::hasStaleSliceResults is true and stalePlateIndices non-empty.
    - testDirtyOverridesPersistAcrossProjectSaveRestore asserts: dirty override survives save/reload via the ProjectServiceMock scoped-config path.
    - Both tests use QTemporaryFile/QTemporaryDir for any file paths.
    - Both tests use ScopedApplicationIdentity.
    - `cmake --build build --config Release` exits 0 and `ctest --test-dir build -R E2E` passes for both methods.
  </acceptance_criteria>
  <done>SETTINGS-07 has automated integration evidence: option edits invalidate stale slice results via the Phase-52 connect, and dirty overrides persist via the 3MF scoped-config path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip remaining Wave 0 ViewModelSmokeTests GREEN for SETTINGS-03/04/05/06</name>
  <files>tests/ViewModelSmokeTests.cpp</files>
  <read_first>
    - tests/ViewModelSmokeTests.cpp (existing dirty tracking pattern lines 612-648; machine options pattern lines 389-410; the Wave 0 scaffolds added in 56-01 that are still RED: testSettingsDialogOpenFromSidebar if not already GREEN from 56-02, testConfigOptionModelSevenTypes, testPerOptionDirtyAndValueSource, testReadonlyBuiltinGating, testSaveSaveAsResetOptionResetGroupResetAll, testUnsavedChangesGuardOnDirtyClose, testPerDialogSearchAndFourLevelMode, testNullableAndVectorOptions)
    - src/core/viewmodels/ConfigViewModel.h (final API after 56-02)
    - src/qml_gui/Models/ConfigOptionModel.h (final API after 56-01)
    - src/core/services/PresetServiceMock.h (isBuiltinPreset, save/delete gating)
  </read_first>
  <behavior>
    - testConfigOptionModelSevenTypes: after loadFromUpstreamSchema, foreach type in {"bool","int","double","enum","string","percent"} at least one option exists with that optType.
    - testNullableAndVectorOptions: at least one option has optNullable true; at least one option has optIsVector true (a coFloats key like "nozzle_temperature").
    - testPerOptionDirtyAndValueSource: setValue(row, v) makes optIsDirty true; valueSourceForKey returns a non-empty source string; dirtyCountForGroup(tier, group) reflects the edit.
    - testReadonlyBuiltinGating: when active preset is builtin (seed PresetServiceMock with a builtin), requestSavePendingChanges returns false and the saveAsRequired signal (or equivalent existing mechanism) fires; saveCurrentPreset is NOT called.
    - testSaveSaveAsResetOptionResetGroupResetAll: resetOption, resetGroup(tier, groupName), resetAllGlobalOptions each leave dirtyCount at 0 for the affected scope.
    - testUnsavedChangesGuardOnDirtyClose: not a UI test -- assert the ConfigViewModel precondition (isPresetDirty true after edit) that the QML close path checks; the actual UnsavedChangesDialog visual is manual/Phase 58.
    - testPerDialogSearchAndFourLevelMode: filterOptionIndices(tier, "", false) excludes mode>=1 options; filterOptionIndices(tier, "", true) includes them; filterOptionIndices(tier, "layer", false) returns matching basic-mode options; filterOptionIndices(tier, "nonexistent", false) returns empty.
    - testSettingsDialogOpenFromSidebar (SETTINGS-01 automated proof of the open path): construct BackendContext (or the minimal wiring exposing configViewModel_ + the settingsRequested signal); create a QSignalSpy on `&backend, &BackendContext::settingsRequested`; for each category in {"printer", "filament", "print"}: (a) call `backend.forwardSettingsRequest(category)`, (b) QVERIFY(spy.count() == 1), (c) QVERIFY the emitted argument at index 0 equals category, (d) QVERIFY(backend.configViewModel()->activePresetTier() == category) -- proving setActivePresetTier ran BEFORE the emit (the two-step contract from 56-02 Task 2). Clear the spy between categories (spy.clear()). This is the automated evidence for SETTINGS-01; the structural QML dispatch (main.qml onSettingsRequested -> dialog.show()) is confirmed separately by QmlUiAudit in 56-03 Task 3.
  </behavior>
  <action>
    For each remaining RED Wave 0 scaffold listed in <behavior>, replace the `QFAIL("Wave 0 scaffold...")` body with real assertions implementing the behavior. Use the ScopedApplicationIdentity + ScopedSettingsSnapshot pattern at the top of each method. Use QSignalSpy where state changes need to be observed.

    Specific implementation notes:
    - testPerOptionDirtyAndValueSource and testReadonlyBuiltinGating depend on PresetServiceMock seeding a builtin preset. Check ProjectServiceMock/PresetServiceMock constructors for how builtins are seeded; if a builtin is not seeded by default, use the existing preset-loading helper used by other tests (look at how testMachineOptionsLoaded or config_default_and_switch_preset sets up its presets).
    - testSaveSaveAsResetOptionResetGroupResetAll must use the new ConfigViewModel::resetGroup(tier, groupName) added in 56-02. Read the option model for a known group (e.g., "Layer height" on the print tier), dirty multiple options in that group, then resetGroup and assert dirtyCountForGroup drops to 0.
    - testUnsavedChangesGuardOnDirtyClose: this is a backend-precondition test (assert isPresetDirty true after edit, false after reset); the dialog visual interaction is in the VALIDATION.md Manual-Only table for Phase 58. Add a comment documenting this scope split.
    - testPerDialogSearchAndFourLevelMode must call filterOptionIndices with all three tier strings ("printer", "filament", "print") to exercise the per-tier dispatch added in 56-02.
    - testSettingsDialogOpenFromSidebar is the AUTOMATED proof for SETTINGS-01 (the sidebar open path). Build the minimal BackendContext wiring (or use the existing test helper that constructs BackendContext for other tests, if one exists). The assertion is NOT a UI test -- it verifies the C++ two-step contract that the QML Connections handler depends on. Concretely: (1) QSignalSpy spy(&backend, &BackendContext::settingsRequested); (2) for category in {"printer","filament","print"}: backend.forwardSettingsRequest(category); QCOMPARE(spy.count(), 1); QCOMPARE(spy.takeFirst().at(0).toString(), category); QCOMPARE(backend.configViewModel()->activePresetTier(), category); spy.clear(); The takeFirst().at(0) check proves the emitted string matches, and the activePresetTier()==category check AFTER the call proves setActivePresetTier ran before the emit. Do NOT instantiate the QML dialogs in this test -- the structural QML side is covered by QmlUiAudit (56-03 Task 3).

    Follow the AUTOMOC caveat: if private slots were added in 56-01, automoc should already have picked them up; if moc fails to find the new symbols, delete `build/ViewModelSmokeTests_autogen/timestamp` and rebuild.

    ASCII-only comments. Reference the requirement ID each test covers at the top of the method (`// SETTINGS-03: ...`).
  </action>
  <verify>
    <automated>cmake --build build --config Release &amp;&amp; ctest --test-dir build -R ViewModelSmokeTest --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - No Wave 0 scaffold body remains in tests/ViewModelSmokeTests.cpp (grep for `QFAIL("Wave 0 scaffold` returns 0 matches).
    - All listed test methods pass via `ctest --test-dir build -R ViewModelSmokeTest --output-on-failure`.
    - Each test references the requirement ID it covers in a leading comment.
    - testSettingsDialogOpenFromSidebar asserts ALL THREE categories (printer/filament/print): spy.count()==1 each, emitted string matches category, AND configViewModel->activePresetTier()==category after the call (the SETTINGS-01 automated open-path proof). This is the concrete evidence that satisfies SETTINGS-01 alongside the QmlUiAudit structural proof in 56-03 Task 3.
    - `cmake --build build --config Release` exits 0.
  </acceptance_criteria>
  <done>All Wave 0 RED scaffolds are flipped GREEN with real assertions; SETTINGS-03/04/05/06 have full ViewModelSmokeTest coverage; SETTINGS-07 has E2E coverage from Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize 56-VALIDATION.md (per-task map + frontmatter)</name>
  <files>.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md</files>
  <read_first>
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md (current draft frontmatter + Per-Task Verification Map TBD rows)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (Phase-55 finalized VALIDATION pattern -- mirror its frontmatter and per-task map fill)
  </read_first>
  <action>
    Update the 56-VALIDATION.md frontmatter:
    - `status: draft` -> `status: approved`
    - `nyquist_compliant: false` -> `nyquist_compliant: true`
    - `wave_0_complete: false` -> `wave_0_complete: true`
    - Update `created:` line stays; add `finalized: 2026-07-02` (or the actual finalize date).

    Fill the Per-Task Verification Map table -- replace the single TBD row with one row per actual task across the four plans (56-01 T1/T2/T3, 56-02 T1/T2, 56-03 T1/T2/T3, 56-04 T1/T2/T3). Each row gets: Task ID (e.g., 56-01-T1), Plan (56-01), Wave (1), Requirement (the IDs from that task's plan frontmatter), Threat Ref (N/A -- local desktop UI, cite T-56-XX-SC accept rows), Secure Behavior (N/A), Test Type (unit/smoke/integration), Automated Command (the actual `<automated>` from that task), File Exists (yes, after the plan runs), Status (passed after the plan completes).

    Tick all Validation Sign-Off checkboxes that are now true:
    - All tasks have automated verify or Wave 0 dependencies: YES (every task has `<verify><automated>`).
    - Sampling continuity: no 3 consecutive tasks without automated verify: YES (every task has automated verify).
    - Wave 0 covers all MISSING references (CxSpinBox suffix + test stubs): YES (56-01 Task 2 + Task 3).
    - No watch-mode flags: YES.
    - Feedback latency < 210s: YES (incremental build ~30-90s; E2E worst case ~210s).
    - nyquist_compliant: true in frontmatter: YES (set above).

    Set **Approval:** from "pending" to "approved (automated)" (or note manual visual parity items deferred to Phase 58 per the Manual-Only table).

    ASCII-only comments; UTF-8 without BOM (per the global encoding guard).
  </action>
  <verify>
    <automated>python -c "import pathlib,re; t=pathlib.Path('.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md').read_text(encoding='utf-8'); assert 'nyquist_compliant: true' in t; assert 'TBD' not in t; assert 'status: approved' in t; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - 56-VALIDATION.md frontmatter has `nyquist_compliant: true`, `status: approved`, `wave_0_complete: true`.
    - Per-Task Verification Map table has one row per task across the four plans (11 rows total) -- no TBD rows remain.
    - All Validation Sign-Off checkboxes are ticked.
    - Approval is no longer "pending".
  </acceptance_criteria>
  <done>VALIDATION.md is signed off: frontmatter reflects completion, per-task map is fully populated, all sign-off checkboxes ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only additions (E2E + ViewModelSmoke) and a Markdown validation doc. No production code changes; no network/auth/persistence surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-SC | Tampering | (none) | accept | No package installs. Tests use ScopedApplicationIdentity + QTemporaryFile to avoid corrupting user state. |
| T-56-04 | All | Test files + VALIDATION.md | accept | Tests are local Qt Test binaries; no external input crosses a trust boundary. VALIDATION.md is a planning artifact, not runtime code. |
</threat_model>

<verification>
- `cmake --build build --config Release` exits 0 after Tasks 1 + 2.
- `ctest --test-dir build --output-on-failure` runs the full suite GREEN (all Wave 0 scaffolds now flipped to real assertions; E2E tests pass).
- The canonical `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` completes successfully (full pipeline green at phase gate).
- VALIDATION.md passes the Task 3 automated check (frontmatter + no TBD rows).
</verification>

<success_criteria>
- SETTINGS-07 has automated integration evidence (option edit -> slice invalidation; dirty overrides persist via 3MF scoped-config path).
- All Wave 0 RED scaffolds for SETTINGS-03/04/05/06 are GREEN with real assertions.
- 56-VALIDATION.md is finalized with `nyquist_compliant: true` and a fully populated per-task map.
- The phase is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/56-parameter-settings-dialogs-restoration/56-04-SUMMARY.md` when done.
</output>
