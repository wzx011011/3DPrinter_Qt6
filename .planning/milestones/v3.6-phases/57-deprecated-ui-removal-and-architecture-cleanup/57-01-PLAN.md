---
phase: 57-deprecated-ui-removal-and-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/qml.qrc
  - src/qml_gui/qmldir
  - src/qml_gui/pages/SettingsPage.qml
  - src/qml_gui/pages/ConfigPage.qml
  - src/qml_gui/components/ParamsPage.qml
  - src/qml_gui/components/SearchDialog.qml
  - src/qml_gui/panels/Sidebar.qml
  - src/qml_gui/panels/FilamentPanel.qml
  - src/qml_gui/panels/PrintSettings.qml
  - src/qml_gui/BackendContext.h
  - src/qml_gui/BackendContext.cpp
  - src/core/viewmodels/ConfigViewModel.h
  - src/core/viewmodels/ConfigViewModel.cpp
  - src/qml_gui/panels/LeftSidebar.qml
  - tests/ViewModelSmokeTests.cpp
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "qml.qrc contains ZERO entries for SettingsPage/ConfigPage/ParamsPage/SearchDialog/Sidebar/FilamentPanel/PrintSettings"
    - "The 7 deleted QML files do not exist on disk"
    - "BackendContext no longer exposes canLeaveSettingsPage / requestConfigPageExitIfNeeded / executeDeferredConfigExit / clearDeferredConfigExit / DeferredConfigExitKind"
    - "ConfigViewModel no longer exposes requestLeaveSettingsPage; the leave-settings-page action branch is removed from applyPendingAction"
    - "No source file under src/ contains a live reference (Loader/source/sourceComponent/import/qmlRegisterType) to any deleted path"
    - "The pending-unsaved queue infrastructure (switch-printer/filament/print-preset, scope-global/object/plate) is intact and ViewModelSmokeTests still pass"
    - "main.qml still resolves backend.tpProject -> ProjectPage (the Project tab itself is NOT removed)"
    - "scripts/auto_verify_with_vcvars.ps1 builds cleanly with ZERO new QML warnings vs Phase 56 baseline"
  artifacts:
    - path: "src/qml_gui/qml.qrc"
      provides: "Resource manifest with 7 obsolete entries removed"
      not_contains: ["SettingsPage.qml", "ConfigPage.qml", "ParamsPage.qml", "SearchDialog.qml", "panels/Sidebar.qml", "FilamentPanel.qml", "PrintSettings.qml"]
    - path: "src/qml_gui/BackendContext.h"
      provides: "BackendContext header with dead config-exit machinery removed"
      not_contains: ["canLeaveSettingsPage", "requestConfigPageExitIfNeeded", "DeferredConfigExitKind", "deferredConfigExitKind_"]
    - path: "src/qml_gui/BackendContext.cpp"
      provides: "BackendContext impl with dead config-exit call sites simplified"
      not_contains: ["requestConfigPageExitIfNeeded", "clearDeferredConfigExit", "executeDeferredConfigExit"]
    - path: "src/core/viewmodels/ConfigViewModel.cpp"
      provides: "ConfigViewModel with requestLeaveSettingsPage and leave-settings-page branch removed"
      not_contains: ["requestLeaveSettingsPage", "leave-settings-page"]
  key_links:
    - from: "src/qml_gui/main.qml"
      to: "backend.tpProject -> ProjectPage"
      via: "Loader sourceComponent"
      pattern: "tpProject.*ProjectPage"
    - from: "src/qml_gui/BackendContext.cpp::setCurrentPage"
      to: "currentPage_ = page; emit currentPageChanged();"
      via: "simplified page-setter without deferred-exit indirection"
      pattern: "void BackendContext::setCurrentPage"
    - from: "src/core/viewmodels/ConfigViewModel.cpp::queuePendingAction"
      to: "switch-* preset / scope-* pending actions still queued"
      via: "shared queue infrastructure retained"
      pattern: "queuePendingAction.*switch-"
---

<objective>
Remove the 4 off-design Settings files locked by Phase 50 §1.6, the 3 legacy
sidebar panels deferred by Phase 52, and the now-dead ConfigPage-embedding route
machinery in BackendContext/ConfigViewModel. Every deletion is preceded by a
reference grep proving zero live references; every reference that IS live
(main.qml ProjectPage tab, pending-unsaved queue, the one smoke test) is
migrated/updated BEFORE the symbol is removed.

Purpose: This is CLEAN-01 (remove obsolete files/routes/registrations/imports/
tests) and the deletion half of CLEAN-02 (no active UI path retains parallel
old/legacy/disconnected copies of replaced components). The replacement dialogs
shipped in Phase 56 (SettingsDialog/OptionRow/GroupNavSidebar) are the live
settings surface; ConfigPage/SettingsPage/ParamsPage/SearchDialog are dead
embedding that must not linger. The legacy Sidebar/FilamentPanel/PrintSettings
are panels superseded by LeftSidebar.qml in Phase 52 and explicitly deferred to
Phase 57.

Output: 7 deleted QML files, cleaned qml.qrc/qmldir, simplified BackendContext
page-router (no deferred-config-exit), ConfigViewModel with the leave-settings-
page action removed, an updated smoke test, and a clean canonical build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-CONTEXT.md
@.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-03-SUMMARY.md
@.claude/rules/build-rules.md
@.claude/rules/qml-boundaries.md
@.claude/rules/core-architecture.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase audit.
     Executor should NOT need to re-grep to understand what to delete. -->

## Locked deletion list (Phase 50 §1.6 + Phase 52 deferrals)

QML files to delete (all exist on disk; all are in qml.qrc; NONE are loaded
by an active Loader except SettingsPage↔ParamsPage which only reference each
other):

  1. src/qml_gui/pages/SettingsPage.qml      (replaced by SettingsDialog.qml)
  2. src/qml_gui/pages/ConfigPage.qml        (off-design Project embedding)
  3. src/qml_gui/components/ParamsPage.qml   (replaced by OptionRow + GroupNavSidebar)
  4. src/qml_gui/components/SearchDialog.qml (in-dialog search now in SettingsDialog)
  5. src/qml_gui/panels/Sidebar.qml          (452 lines, NOT in PreparePage; replaced by LeftSidebar.qml)
  6. src/qml_gui/panels/FilamentPanel.qml    (embedded only by Sidebar.qml)
  7. src/qml_gui/panels/PrintSettings.qml    (embedded only by Sidebar.qml)

qml.qrc entries to remove (lines verified by audit):
  - Line 13: <file>pages/ConfigPage.qml</file>
  - Line 44: <file>pages/SettingsPage.qml</file>
  - Line 47: <file>panels/PrintSettings.qml</file>
  - Line 49: <file>panels/Sidebar.qml</file>
  - Line 50: <file>panels/FilamentPanel.qml</file>
  - Line 69: <file>components/SearchDialog.qml</file>
  - Line 70: <file>components/ParamsPage.qml</file>

qmldir entries to remove (src/qml_gui/qmldir):
  - Line 2: `ParamsPage components/ParamsPage.qml`
  (Line 1 `singleton Theme Theme.qml` stays. qmldir has no entry for the
  other 6 deleted files.)

## Route machinery to remove (dead after ConfigPage deletion)

3 named routes from the locked checklist, plus the dead-code the CONTEXT
authorizes removing at Claude's discretion:

In src/qml_gui/BackendContext.h:
  - Line 279: `bool canLeaveSettingsPage() const;`
  - Line 504: `bool requestConfigPageExitIfNeeded();`
  - Line 506: `void clearDeferredConfigExit();`
  - Line 507: `bool executeDeferredConfigExit();`
  - Line 505: `void handleConfigPendingActionApplied(const QString &action, const QString &target);`
    (ONLY this slot — it is connected at BackendContext.cpp:92 to
    ConfigViewModel::pendingActionApplied and exists ONLY to call
    executeDeferredConfigExit. Remove the connect too.)
  - Lines 535-545: the `int deferredConfigExitPage_`, the
    `enum class DeferredConfigExitKind { None, PageChange, NewProject,
    OpenProject, ImportModel }`, and `deferredConfigExitKind_`/`deferredConfigExitPath_`
    member variables.

In src/qml_gui/BackendContext.cpp:
  - Lines 411-414: `BackendContext::canLeaveSettingsPage` impl
  - Lines 416-423: `BackendContext::requestConfigPageExitIfNeeded` impl
  - Lines 324-329: `BackendContext::clearDeferredConfigExit` impl
  - Lines 331-402: `BackendContext::executeDeferredConfigExit` impl
  - Lines 404-409: `BackendContext::handleConfigPendingActionApplied` impl
  - Line 92: the `connect(configViewModel_, &ConfigViewModel::pendingActionApplied,
              this, &BackendContext::handleConfigPendingActionApplied);` line
  - Lines 429-435 (inside setCurrentPage): the `if (page != tpProject)` branch
    that calls requestConfigPageExitIfNeeded. SIMPLIFY setCurrentPage to:
    de-dup, set currentPage_, emit currentPageChanged(). The page-guard is no
    longer needed because the only page that needed an unsaved-state guard
    (ConfigPage) is gone.
  - Lines 627-631, 643-648, 680-685 (inside topbarNewProject/topbarOpenProject/
    topbarImportModel): the 3 `deferredConfigExitKind_ = ...; ...
    if (!requestConfigPageExitIfNeeded()) return; clearDeferredConfigExit();`
    blocks. SIMPLIFY these 3 topbar methods by deleting the deferred-exit
    blocks entirely; the methods become: latency-clock start, do-the-action,
    setCurrentPage(tp3DEditor), latency sample.
  - Line 540-541 comment "中 Settings 内嵌于 Project 页 (index 5 = tpProject),
    直接跳转该页" inside BackendContext::openSettings — UPDATE the comment
    because openSettings now emits settingsRequested(category) which routes
    to the new SettingsDialog (NOT to tpProject). Leave the emit
    settingsRequested(category) (line 576) intact.

In src/core/viewmodels/ConfigViewModel.h:
  - Line 153: `Q_INVOKABLE bool requestLeaveSettingsPage();`

In src/core/viewmodels/ConfigViewModel.cpp:
  - Lines 971-974: `bool ConfigViewModel::requestLeaveSettingsPage() { return
    queuePendingAction(QStringLiteral("leave-settings-page"), QString()); }`
  - Lines 385-... inside `applyPendingAction`: the
    `if (action == QStringLiteral("leave-settings-page")) { ... }` branch.
    Read the surrounding context to find the exact block and remove ONLY that
    branch (do NOT remove the switch-printer/filament/print-preset or scope-*
    branches — they are live).

In tests/ViewModelSmokeTests.cpp:
  - Lines 733-737 (the `leaveOk` block inside the unsaved-state test): remove
    ONLY the `requestLeaveSettingsPage` invocation + QVERIFY + QCOMPARE on
    `pendingUnsavedAction == "leave-settings-page"`. Leave the surrounding
    `requestCancelPendingChanges` assertions intact.

## What STAYS (do NOT touch)

- `main.qml` line 528: `Loader { active: backend.currentPage === backend.tpProject;
  sourceComponent: Component { ProjectPage { ... } } }` — the tpProject tab
  still hosts ProjectPage. The TabPosition::tpProject enum value, the
  BBLTopbar.qml "项目" tab, and `backend.tpProject` accessor all STAY.
- `SoftwareViewport.{h,cpp}` and its registration at `main_qml.cpp:134` — it is
  the explicit GCODE-04 fallback path, NOT dead code. Phase 55 hardened the
  D3D11 default; SoftwareViewport stays as the documented fallback.
- `ConfigViewModel::queuePendingAction`, `pendingUnsavedAction_`,
  `clearPendingAction`, `applyPendingAction`, and all switch-* preset and
  scope-* action branches — these back the live preset/scope pending-unsaved
  queue used by Phase 56 SettingsDialog and the LeftSidebar.
- Phase 56 files (SettingsDialog.qml, OptionRow.qml, GroupNavSidebar.qml) —
  the replacement surface.
- The 4 additional docs that reference CrealityPrint
  (`docs/v3.6-ui-inventory.md`, `docs/依赖与协议边界审计.md`,
  `docs/源码对照迁移任务追踪.md`, `docs/项目结构.md`) — NOT in the locked
  cleanup checklist (only `源码真值功能矩阵.md` and `源码真值基线.md` are
  locked; those are Plan 57-02).
- The AuxiliaryPage, CalibrationPage, MonitorPage etc. — not deletion targets.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion reference grep audit (read-only proof of safety)</name>
  <files>(no file changes — audit recorded in plan SUMMARY)</files>
  <read_first>
    - src/qml_gui/qml.qrc (full file — confirms the 7 lines to remove)
    - src/qml_gui/qmldir (full file — confirms only ParamsPage entry)
    - src/qml_gui/main.qml (lines 470-555 — confirms no Loader references deleted files)
    - src/qml_gui/BackendContext.h (lines 270-290, 500-550)
    - src/qml_gui/BackendContext.cpp (lines 85-100, 320-440, 620-700)
    - src/core/viewmodels/ConfigViewModel.h (lines 145-160, 215-265)
    - src/core/viewmodels/ConfigViewModel.cpp (lines 330-410, 960-1000)
    - tests/ViewModelSmokeTests.cpp (lines 700-745)
  </read_first>
  <action>
    Run the following reference greps to PROVE every deletion candidate has
    zero live references outside the deletion set. Record results in the
    plan SUMMARY (count + verdict per file). Use ripgrep via the Grep tool
    (do NOT use Bash grep — the encoding guard forbids raw shell text IO):

    For each of the 7 QML files, search `src/` and `tests/` for the bare
    filename token. The ONLY allowed hits are: (a) the qml.qrc <file> line,
    (b) the qmldir entry (ParamsPage only), (c) self-references inside the
    file itself, (d) the internal SettingsPage→ParamsPage Loader (3 places
    inside SettingsPage.qml), (e) the internal Sidebar.qml→FilamentPanel/
    PrintSettings references, (f) the "Extracted from Sidebar.qml" /
    "from PrintSettings.qml lines" / "from ParamsPage.qml" comment citations
    in LeftSidebar.qml/GroupNavSidebar.qml/OptionRow.qml (these are HISTORY
    comments, not imports — they will be re-worded in Task 3 to remove the
    stale cross-references).

    For the 3 routes, confirm the ONLY call sites are the dead-machinery
    internal callers listed in <interfaces>. The audit grep must show:
    `canLeaveSettingsPage` — only BackendContext.h:279 + BackendContext.cpp:411.
    `requestConfigPageExitIfNeeded` — only BackendContext.h:504 +
      BackendContext.cpp:{416,432,629,646,683}.
    `requestLeaveSettingsPage` — only ConfigViewModel.h:153 +
      ConfigViewModel.cpp:971 + tests/ViewModelSmokeTests.cpp:734.

    If the grep reveals ANY live reference NOT enumerated above (e.g., a
    Loader in main.qml, a setContextProperty, a qmlRegisterType, a new
    smoke test, a different C++ caller), STOP — do not proceed to Task 2.
    Surface the finding in the SUMMARY under "Audit Discrepancies" and flag
    the plan blocked.

    This is the Phase 57 CONTEXT "audit discipline (CLEAN-02)" gate: a file
    is only deleted if it has ZERO live references; any live reference
    blocks deletion until resolved.
  </action>
  <verify>
    <automated>grep -c "(SettingsPage|ConfigPage|ParamsPage|SearchDialog|Sidebar|FilamentPanel|PrintSettings)\.qml" must show ZERO non-allowed hits; grep -c "(canLeaveSettingsPage|requestConfigPageExitIfNeeded|requestLeaveSettingsPage)" must match the enumerated-call-site counts exactly</automated>
  </verify>
  <done>
    Reference audit complete. SUMMARY records per-file match count and the
    verdict "safe to delete" or "blocked: <reason>". If any block found,
    plan is paused before Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove qml.qrc/qmldir entries + delete the 7 QML files</name>
  <files>
    src/qml_gui/qml.qrc, src/qml_gui/qmldir, src/qml_gui/pages/SettingsPage.qml, src/qml_gui/pages/ConfigPage.qml, src/qml_gui/components/ParamsPage.qml, src/qml_gui/components/SearchDialog.qml, src/qml_gui/panels/Sidebar.qml, src/qml_gui/panels/FilamentPanel.qml, src/qml_gui/panels/PrintSettings.qml, src/qml_gui/panels/LeftSidebar.qml
  </files>
  <read_first>
    - src/qml_gui/qml.qrc (full)
    - src/qml_gui/qmldir (full)
    - src/qml_gui/panels/LeftSidebar.qml (lines 1-20, 150-165, 410-420 — to find the 3 history comments referencing Sidebar.qml/PrintSettings.qml)
    - src/qml_gui/components/GroupNavSidebar.qml (line 2 — "Extracted from ParamsPage.qml")
    - src/qml_gui/components/OptionRow.qml (line 2 — "Extracted from ParamsPage.qml")
  </read_first>
  <behavior>
    - After this task, qml.qrc contains zero entries for the 7 deleted files.
    - After this task, qmldir contains only the Theme singleton line.
    - After this task, the 7 deleted files do not exist on disk.
    - After this task, LeftSidebar.qml / GroupNavSidebar.qml / OptionRow.qml history comments no longer name the deleted files (they say "Extracted from earlier sidebar code" / "Extracted from earlier params code" — past-tense, no dangling path).
    - Build via auto_verify_with_vcvars.ps1 still passes (QML resources all resolve).
  </behavior>
  <action>
    Step 1 — Edit `src/qml_gui/qml.qrc` and delete exactly these 7 lines:
      line 13 `<file>pages/ConfigPage.qml</file>`
      line 44 `<file>pages/SettingsPage.qml</file>`
      line 47 `<file>panels/PrintSettings.qml</file>`
      line 49 `<file>panels/Sidebar.qml</file>`
      line 50 `<file>panels/FilamentPanel.qml</file>`
      line 69 `<file>components/SearchDialog.qml</file>`
      line 70 `<file>components/ParamsPage.qml</file>`
    Preserve all other entries, including the Phase 56 additions
    (SettingsDialog.qml, OptionRow.qml, GroupNavSidebar.qml). Preserve the
    indentation style of the surrounding entries.

    Step 2 — Edit `src/qml_gui/qmldir`. Delete line 2
    (`ParamsPage components/ParamsPage.qml`). Keep line 1
    (`singleton Theme Theme.qml`). qmldir now has exactly 1 non-blank line.

    Step 3 — Delete the 7 QML files using `rm` via Bash (the encoding guard
    permits rm — it only forbids PowerShell redirection/echo for text IO;
    file deletion does not touch file contents):
      rm src/qml_gui/pages/SettingsPage.qml
      rm src/qml_gui/pages/ConfigPage.qml
      rm src/qml_gui/components/ParamsPage.qml
      rm src/qml_gui/components/SearchDialog.qml
      rm src/qml_gui/panels/Sidebar.qml
      rm src/qml_gui/panels/FilamentPanel.qml
      rm src/qml_gui/panels/PrintSettings.qml

    Step 4 — Re-word the stale history-comment cross-references in the 3
    files that cite the deleted panels. Use the Edit tool (NOT PowerShell):
      LeftSidebar.qml line 10: change
        "// Extracted from PreparePage leftPanel + Sidebar.qml content"
        to "// Extracted from earlier Prepare sidebar code (Phase 52)."
      LeftSidebar.qml line 157: change
        "// -- Section 2: Filament (from PrintSettings.qml lines 626-704) --"
        to "// -- Section 2: Filament (Prepare sidebar section, Phase 52) --"
      LeftSidebar.qml line 413: change
        "// -- Section 5: Objects (from PreparePage leftPanel + Sidebar.qml) --"
        to "// -- Section 5: Objects (Prepare sidebar section, Phase 52) --"
      GroupNavSidebar.qml line 2: change
        "// Extracted from ParamsPage.qml category sidebar (Phase 56-03)."
        to "// Left option-group navigation (Phase 56-03)."
      OptionRow.qml line 2: change
        "// Extracted from ParamsPage.qml (Phase 56-03)."
        to "// Typed option renderer (Phase 56-03)."
      ParamsPage.qml line 324 (comment "对齐上游 SpinCtrl；Sidebar.qml 变换输入
      同款模式") is INSIDE ParamsPage.qml which is being deleted — no edit
      needed.

    The re-worded comments stay English ASCII-only and UTF-8 no BOM per the
    global encoding guard. Do not change any code logic in this step.

    Do NOT touch main.qml — the tpProject Loader at line 528 loads
    ProjectPage, NOT ConfigPage. It is unaffected.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && test ! -f src/qml_gui/pages/SettingsPage.qml && test ! -f src/qml_gui/pages/ConfigPage.qml && test ! -f src/qml_gui/components/ParamsPage.qml && test ! -f src/qml_gui/components/SearchDialog.qml && test ! -f src/qml_gui/panels/Sidebar.qml && test ! -f src/qml_gui/panels/FilamentPanel.qml && test ! -f src/qml_gui/panels/PrintSettings.qml && echo PASS_DELETE</automated>
  </verify>
  <done>
    7 files deleted, qml.qrc has 0 entries for them, qmldir is single-line
    (Theme only), 3 history comments re-worded to remove dangling paths.
    `test ! -f` chain prints PASS_DELETE.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove the 3 named routes + dead deferred-config-exit machinery</name>
  <files>
    src/qml_gui/BackendContext.h, src/qml_gui/BackendContext.cpp, src/core/viewmodels/ConfigViewModel.h, src/core/viewmodels/ConfigViewModel.cpp, tests/ViewModelSmokeTests.cpp
  </files>
  <read_first>
    - src/qml_gui/BackendContext.h (lines 140-230 for TabPosition enum tpProject stays; lines 270-290 for canLeaveSettingsPage decl; lines 500-550 for the deferred-exit method decls + enum + members)
    - src/qml_gui/BackendContext.cpp (lines 85-100 for the connect; lines 320-440 for clear/execute/canLeave/requestConfig + setCurrentPage; lines 620-700 for topbar methods; lines 537-580 for openSettings)
    - src/core/viewmodels/ConfigViewModel.h (lines 145-160 for requestLeaveSettingsPage decl; lines 215-265 for pending-action infra)
    - src/core/viewmodels/ConfigViewModel.cpp (lines 330-410 for queue/clear/applyPendingAction including the leave-settings-page branch; lines 965-980 for the requestLeaveSettingsPage impl)
    - tests/ViewModelSmokeTests.cpp (lines 700-745 — full unsaved-state test that contains the leaveOk block at 733-737)
  </read_first>
  <action>
    Use the Edit tool for every change (NEVER PowerShell redirection —
    global encoding guard). Preserve UTF-8 no BOM and ASCII-only comments.

    Step A — src/qml_gui/BackendContext.h deletions:
      - Delete the declaration `bool canLeaveSettingsPage() const;` (line 279).
      - Delete the declaration `bool requestConfigPageExitIfNeeded();` (line 504).
      - Delete the declaration `void handleConfigPendingActionApplied(const QString &action, const QString &target);` (line 505).
      - Delete the declarations `void clearDeferredConfigExit();` and `bool executeDeferredConfigExit();` (lines 506-507).
      - Delete the member `int deferredConfigExitPage_ = -1;` (line 535).
      - Delete the enum `DeferredConfigExitKind` (lines 536-543) and the members `deferredConfigExitKind_` and `deferredConfigExitPath_` (lines 544-545).
      - Do NOT remove `tpProject` enum value (line 185), the `tpProject()` accessor (line 219), or anything else in TabPosition.

    Step B — src/qml_gui/BackendContext.cpp deletions / simplifications:
      - Delete the connect at line 92: `connect(configViewModel_, &ConfigViewModel::pendingActionApplied, this, &BackendContext::handleConfigPendingActionApplied);` (this slot becomes unreferenced once handleConfigPendingActionApplied is removed).
      - Delete the entire impl of `canLeaveSettingsPage` (lines 411-414).
      - Delete the entire impl of `requestConfigPageExitIfNeeded` (lines 416-423).
      - Delete the entire impl of `clearDeferredConfigExit` (lines 324-329).
      - Delete the entire impl of `executeDeferredConfigExit` (lines 331-402).
      - Delete the entire impl of `handleConfigPendingActionApplied` (lines 404-409).
      - SIMPLIFY `setCurrentPage` (lines 425-438): remove the `if (page != tpProject)` branch that sets/clears deferred state and calls requestConfigPageExitIfNeeded. The new body is:
          if (currentPage_ == page) return;
          currentPage_ = page;
          emit currentPageChanged();
      - SIMPLIFY `topbarNewProject` (lines 624-638): remove the 3-line deferred-exit block (deferredConfigExitKind_=NewProject; deferredConfigExitPage_=tp3DEditor; if(!requestConfigPageExitIfNeeded()) return; clearDeferredConfigExit();). Keep the latency clock start, the newProject()/clearWorkspace()/setCurrentPage(1)/latency sample.
      - SIMPLIFY `topbarOpenProject` (lines 640-675): remove the deferred-exit block (deferredConfigExitKind_=OpenProject; deferredConfigExitPage_=tp3DEditor; deferredConfigExitPath_=filePath; if(!requestConfigPageExitIfNeeded()) return false; clearDeferredConfigExit();). Keep everything else (latency, loadFile, JSON fallback, openProject, setCurrentPage(1), return loaded).
      - SIMPLIFY `topbarImportModel` (lines 677-699): remove the deferred-exit block (deferredConfigExitKind_=ImportModel; deferredConfigExitPage_=tp3DEditor; deferredConfigExitPath_=filePath; if(!requestConfigPageExitIfNeeded()) return false; clearDeferredConfigExit();). Keep everything else.
      - UPDATE the stale comment at line 540-541 inside `openSettings` from "中 Settings 内嵌于 Project 页 (index 5 = tpProject), 直接跳转该页" to "// Phase 56: emit settingsRequested; main.qml dispatches to the SettingsDialog ApplicationWindow." The `emit settingsRequested(category)` at line 576 stays.

      After these edits, `setCurrentPage` must still early-return on `currentPage_ == page`, set `currentPage_`, and emit `currentPageChanged()`. `requestSelectTab` (lines 440-459) is unchanged. The page router still works for tpHome/tp3DEditor/tpPreview/tpDevice/tpMultiDevice/tpProject/tpCalibration/tpPlaceholder1/tpPlaceholder2 — only the ConfigPage-embedding exit guard is gone.

    Step C — src/core/viewmodels/ConfigViewModel.h deletion:
      - Delete the declaration `Q_INVOKABLE bool requestLeaveSettingsPage();` (line 153).

    Step D — src/core/viewmodels/ConfigViewModel.cpp deletions:
      - Delete the entire impl of `requestLeaveSettingsPage` (lines 971-974).
      - Inside `applyPendingAction` (around line 385), delete ONLY the `if (action == QStringLiteral("leave-settings-page")) { ... return ...; }` branch. Read the surrounding code first to find the exact lines. Do NOT remove any other branch (switch-printer-preset, switch-filament-preset, switch-print-preset, scope-global, scope-object, scope-plate). The `pendingUnsavedAction_`, `queuePendingAction`, `clearPendingAction`, `applyPendingAction`, `hasPendingUnsavedChanges`, and `pendingUnsavedAction` Q_PROPERTY all STAY — they back the live preset/scope pending-unsaved queue used by Phase 56 dialogs and LeftSidebar.

    Step E — tests/ViewModelSmokeTests.cpp deletion:
      - Inside the unsaved-state test (around lines 733-737), delete the 5-line block:
            bool leaveOk = false;
            QVERIFY(QMetaObject::invokeMethod(&config, "requestLeaveSettingsPage",
                                              Q_RETURN_ARG(bool, leaveOk)));
            QVERIFY(!leaveOk);
            QCOMPARE(config.property("pendingUnsavedAction").toString(), QStringLiteral("leave-settings-page"));
        Leave the surrounding `requestCancelPendingChanges` assertions (lines 725-731) intact. The test now ends after asserting the cancel cleared the pending action.

    Do NOT touch the connect at BackendContext.cpp line 94 (it forwards
    ConfigViewModel::stateChanged to BackendContext::configStateChanged —
    unrelated to the leave-settings-page slot being removed at line 92).
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && grep -rn "canLeaveSettingsPage\|requestConfigPageExitIfNeeded\|executeDeferredConfigExit\|clearDeferredConfigExit\|DeferredConfigExitKind\|deferredConfigExit\|handleConfigPendingActionApplied\|requestLeaveSettingsPage\|leave-settings-page" src/ tests/ 2>/dev/null | wc -l | tr -d ' ' | grep -q '^0$' && echo PASS_ROUTES_GONE || echo FAIL_ROUTES_LEAK</automated>
  </verify>
  <done>
    The 3 named routes + the deferred-config-exit machinery + the leave-
    settings-page action are removed. Grep for the 9 dead tokens returns 0
    hits across src/ and tests/. The pending-unsaved queue infrastructure
    for preset/scope actions is intact.
  </done>
</task>

<task type="auto">
  <name>Task 4: Canonical build + smoke tests green</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - .claude/rules/build-rules.md (ONLY auto_verify_with_vcvars.ps1, ONLY build/)
  </read_first>
  <action>
    Run the canonical build exactly once via the project's sole sanctioned
    command (build-rules.md):

      powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1

    This re-configures cmake, builds with ninja, and runs the smoke tests.
    Capture the tail of the output for the SUMMARY.

    Expectations:
      - cmake configure succeeds with no "file not found" for any deleted
        qrc entry (proves qml.qrc cleanup is complete).
      - ninja build produces ZERO new compile errors. If a compile error
        appears in BackendContext.cpp or ConfigViewModel.cpp, it means a
        reference to the removed symbols was missed in Task 3 — read the
        error, find the missed call site, and Edit it out (do NOT re-add
        the removed symbols).
      - QML warnings: ZERO new warnings vs the Phase 56 baseline (any
        "file not found" qrc warning means a missed entry).
      - ViewModelSmokeTests: the previously-passing 85 tests should now be
        84 (we removed one assertion block, not a whole test — the count
        may stay 85 if the block was inside a larger test). All present
        tests pass; the THUMB-03 skip stays.
      - QmlUiAuditTests: 36 passed, 0 failed (unchanged — the deleted
        files were not tested by these audit tests, which target the new
        Phase 56 dialogs).

    If the build fails, root-cause and Edit the offending file. Do NOT
    bypass by re-adding deleted code. If a legitimately-missed live
    reference is discovered (e.g., a smoke test that wasn't in the audit
    enumeration), add a follow-up Edit to migrate/remove it, then re-run
    the canonical build.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1 -ExitOnBuildFailure</automated>
  </verify>
  <done>
    Canonical build green. ViewModelSmokeTests pass (THUMB-03 skip
    unchanged). QmlUiAuditTests 36/36. ZERO new QML warnings. The
    SUMMARY records the exact pass counts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none)   | This phase deletes local desktop QML/C++ UI code and simplifies a page-router. No network, auth, storage, IPC, or external-input surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | Deletion candidates | mitigate | Task 1 reference-grep audit proves zero live references BEFORE any delete; missed reference blocks deletion. |
| T-57-02 | Denial of Service | Build/qrc | mitigate | Task 4 canonical build via auto_verify_with_vcvars.ps1 catches any qrc/compile regression; failure blocks plan close. |
| T-57-03 | Information Disclosure | (n/a) | accept | No PII, secrets, telemetry, or logs touched. Local desktop cleanup only. |
| T-57-04 | Elevation of Privilege | (n/a) | accept | No privilege boundary crossed; all code is in-process desktop UI. |
| T-57-SC | Tampering | (n/a — no package installs) | accept | This plan installs no npm/pip/cargo packages. Package legitimacy gate N/A. |
</threat_model>

<verification>
- Task 1 reference audit recorded in SUMMARY with per-file verdicts.
- Task 2 `test ! -f` chain prints PASS_DELETE for all 7 files.
- Task 3 grep for the 9 dead tokens returns 0 hits across src/ and tests/.
- Task 4 canonical build (auto_verify_with_vcvars.ps1) green; smoke tests pass.
- Post-phase grep (in Plan 57-02 Task 2) locks the deletions in via a compiled
  QmlUiAudit regression test (CLEAN-02 regression guard).
</verification>

<success_criteria>
- 7 QML files deleted; qml.qrc and qmldir cleaned.
- 3 named routes + the dead deferred-config-exit machinery + the leave-settings-
  page action removed; pending-unsaved queue for preset/scope actions intact.
- The one smoke-test assertion block referencing requestLeaveSettingsPage removed.
- main.qml's tpProject→ProjectPage tab still loads (the Project tab itself is
  NOT a deletion target).
- Phase 56 SettingsDialog/OptionRow/GroupNavSidebar untouched and still
  building/testing green.
- Canonical auto_verify_with_vcvars.ps1 green with zero new QML warnings.
- CLEAN-01 fully delivered; CLEAN-02 deletion half delivered (broader-audit
  regression test is Plan 57-02 Task 2); CLEAN-03 scoped to changed areas
  (the changed areas contain no business logic — QML is wiring only).
</success_criteria>

<output>
Create `.planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-01-SUMMARY.md` when done
</output>
