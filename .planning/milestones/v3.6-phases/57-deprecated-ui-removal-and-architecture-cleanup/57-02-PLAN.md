---
phase: 57-deprecated-ui-removal-and-architecture-cleanup
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/源码真值功能矩阵.md
  - docs/源码真值基线.md
  - tests/QmlUiAuditTests.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [CLEAN-02, CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - "docs/源码真值功能矩阵.md contains ZERO occurrences of `third_party/CrealityPrint/` (all rewritten to third_party/OrcaSlicer/)"
    - "docs/源码真值基线.md contains ZERO occurrences of `third_party/CrealityPrint/` (all rewritten to third_party/OrcaSlicer/)"
    - "tests/QmlUiAuditTests.cpp contains a regression test that asserts the 7 deleted QML paths + 3 deleted routes are ABSENT from src/ (compiles + passes)"
    - "The QmlUiAudit regression test FAILS if any deleted path/route is reintroduced"
    - "A documented broader-UI sweep covers `legacy`/`deprecated`/`unused`/`old`/placeholder-only/disconnected UI; any newly-found dead UI is either deleted in this plan or logged for a future phase"
    - "All changed files in this phase (Wave 1 + Wave 2) pass the global encoding guard (UTF-8 no BOM, ASCII-only comments)"
    - "scripts/auto_verify_with_vcvars.ps1 still builds and the QmlUiAudit regression test runs green"
  artifacts:
    - path: "docs/源码真值功能矩阵.md"
      provides: "Doc with stale CrealityPrint paths rewritten to OrcaSlicer"
      not_contains: ["third_party/CrealityPrint/"]
    - path: "docs/源码真值基线.md"
      provides: "Doc with stale CrealityPrint paths rewritten to OrcaSlicer"
      not_contains: ["third_party/CrealityPrint/"]
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Compiled regression test asserting deleted paths/routes stay absent"
      contains: ["deletedSettingsPathsStayAbsent", "deletedRoutesStayAbsent"]
  key_links:
    - from: "tests/QmlUiAuditTests.cpp::deletedSettingsPathsStayAbsent"
      to: "src/qml_gui/qml.qrc"
      via: "QFile read of qml.qrc; QVERIFY(!content.contains(...))"
      pattern: "deletedSettingsPathsStayAbsent.*qml\\.qrc"
    - from: "tests/QmlUiAuditTests.cpp::deletedRoutesStayAbsent"
      to: "src/qml_gui/BackendContext.{h,cpp} + src/core/viewmodels/ConfigViewModel.{h,cpp}"
      via: "QFile read of these 4 source files; QVERIFY token not present"
      pattern: "deletedRoutesStayAbsent.*BackendContext"
---

<objective>
Finish Phase 57's CLEAN-02/03/04 work: rewrite the 2 locked stale doc paths
(CrealityPrint→OrcaSlicer), add the compiled regression audit test that locks
the Wave 1 deletions in (so they cannot silently regress), perform the broader
dead-UI sweep that CLEAN-02 also requires, and run the global encoding guard
over every file Wave 1 + Wave 2 touched.

Purpose: This plan converts the Wave 1 deletion (a one-time event) into a
permanent invariant via a compiled test. It also closes CLEAN-04 (encoding
guard) for the whole phase, and the doc rewrite closes CLEAN-01's documentation
clause. The broader-UI sweep is the audit half of CLEAN-02 — proving no OTHER
parallel/legacy/disconnected UI copy lingers beyond the locked deletion list.

Output: 2 rewritten docs, 1 new compiled regression test (2 test methods),
1 documented broader-UI sweep report (in SUMMARY), and a clean encoding-guard
pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-CONTEXT.md
@.planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-01-SUMMARY.md
@.claude/rules/build-rules.md

<interfaces>
<!-- Existing QmlUiAuditTests conventions the executor must follow. -->

## tests/QmlUiAuditTests.cpp established patterns (from Phase 56-03)

The file already exists and contains 36 passing audit tests, including:
  - settingsDialogUsesOnlyCxControls
  - settingsDialogNoRawControls
  - settingsDialogStringsQsTr
  - settingsDialogMainQmlDispatchStructural

The pattern (per Phase 56-03 SUMMARY): each test opens a source file via
QFile (or scans a directory), reads its content into a QString, and uses
QVERIFY(!content.contains(token)) / QVERIFY(content.contains(token)) to
assert structural invariants. The tests are registered as private slots in
the `QmlUiAuditTests` class and run by QtTest.

## Locked doc rewrite targets (Phase 50 §1.6)

Only 2 docs are in the locked cleanup checklist (the other 4 docs that
reference CrealityPrint — v3.6-ui-inventory.md, 依赖与协议边界审计.md,
源码对照迁移任务追踪.md, 项目结构.md — are NOT in this phase's locked
list; they are out of scope and stay as-is):

  - docs/源码真值功能矩阵.md  (rewrite every `third_party/CrealityPrint/`
    occurrence to `third_party/OrcaSlicer/`)
  - docs/源码真值基线.md      (same rewrite)

## Broader-UI sweep keywords (CLEAN-02 audit discipline)

Grep `src/` for: `legacy`, `deprecated`, `unused`, `Old<Name>` patterns,
`placeholder`, `disconnected`. Findings to triage in Task 3:
  - Known in-scope candidates (per Phase 57 CONTEXT): the legacy Sidebar/
    FilamentPanel/PrintSettings already deleted in 57-01.
  - Known out-of-scope (do NOT delete): SoftwareViewport (GCODE-04 fallback);
    ExportPresetBundleDialog.qml "简化版" (not in locked list); the
    `Q_UNUSED` markers in C++ (compiler noise, not dead UI); the "legacy
    category-derived fallback" comment in ConfigOptionModel.cpp:481 (the
    fallback is live logic, not dead UI).
  - Anything else found by grep: triage and either delete (if clearly dead
    QML UI with zero live refs — re-run the 57-01 Task 1 reference-grep
    discipline before deleting) or log in SUMMARY under "Phase 57+ Deferred
    UI Findings" with a reason.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the 2 locked doc paths (CrealityPrint → OrcaSlicer)</name>
  <files>docs/源码真值功能矩阵.md, docs/源码真值基线.md</files>
  <read_first>
    - docs/源码真值功能矩阵.md (full — likely small; capture every CrealityPrint line)
    - docs/源码真值基线.md (full — likely small; capture every CrealityPrint line)
  </read_first>
  <action>
    Use the Edit tool for every change (global encoding guard: UTF-8 no BOM,
    never PowerShell redirection). For each of the 2 docs:

      Replace every occurrence of the literal string `third_party/CrealityPrint/`
      with `third_party/OrcaSlicer/`.

    Also replace the bare token `CrealityPrint` (when it appears as a project
    name reference, not as part of an already-rewritten path) with
    `OrcaSlicer` — but ONLY in contexts that refer to the upstream source
    project. Do NOT touch:
      - historical references to "the CrealityPrint fork we migrated from"
        (these are historically accurate),
      - the brand line if any says "OWzx (forked from OrcaSlicer, previously
        CrealityPrint)" — keep the historical note.

    If unsure whether a `CrealityPrint` mention is path-vs-historical, leave
    it and record the line in the SUMMARY under "Ambiguous Doc Mentions" so
    the human can review.

    The 2 docs MUST end with ZERO occurrences of `third_party/CrealityPrint/`
    (the locked cleanup-clause target). Standalone historical `CrealityPrint`
    mentions are permitted but logged.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && ! grep -q "third_party/CrealityPrint/" "docs/源码真值功能矩阵.md" && ! grep -q "third_party/CrealityPrint/" "docs/源码真值基线.md" && echo PASS_DOC_REWRITE</automated>
  </verify>
  <done>
    Both docs have zero `third_party/CrealityPrint/` path occurrences.
    Historical standalone mentions (if any) are logged in SUMMARY.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add QmlUiAudit regression test locking the Wave 1 deletions</name>
  <files>tests/QmlUiAuditTests.cpp, tests/CMakeLists.txt</files>
  <read_first>
    - tests/QmlUiAuditTests.cpp (full — to see the class shape, includes, private slots, and existing test bodies to mirror)
    - tests/CMakeLists.txt (full — to confirm the test target already compiles QmlUiAuditTests.cpp and whether new helper sources are needed; if the test simply adds 2 private slots to the existing class, no CMake change is needed)
  </read_first>
  <behavior>
    - Test 1 (deletedSettingsPathsStayAbsent): opens src/qml_gui/qml.qrc via QFile, reads the full content, and QVERIFYs that the content does NOT contain each of the 7 deleted qrc entries: `pages/SettingsPage.qml`, `pages/ConfigPage.qml`, `components/ParamsPage.qml`, `components/SearchDialog.qml`, `panels/Sidebar.qml`, `panels/FilamentPanel.qml`, `panels/PrintSettings.qml`. Also verifies the 7 files do not exist on disk via QVERIFY(!QFileInfo::exists(path)).
    - Test 2 (deletedRoutesStayAbsent): opens the 4 source files (BackendContext.h, BackendContext.cpp, ConfigViewModel.h, ConfigViewModel.cpp) via QFile, reads each, and QVERIFYs the combined content does NOT contain each of the 5 dead tokens: `canLeaveSettingsPage`, `requestConfigPageExitIfNeeded`, `executeDeferredConfigExit`, `clearDeferredConfigExit`, `requestLeaveSettingsPage`, plus the action string `leave-settings-page`.
    - Both tests fail loudly if any deleted symbol/path is reintroduced.
    - Tests are registered as private slots in the existing QmlUiAuditTests class.
    - Tests pass on the post-57-01 tree (RED-flipped-to-GREEN by virtue of 57-01 having already deleted the things).
  </behavior>
  <action>
    Use the Edit tool to add 2 new private-slot test methods to the existing
    `QmlUiAuditTests` class in tests/QmlUiAuditTests.cpp. Mirror the file-
    reading helper pattern already used by settingsDialogUsesOnlyCxControls
    (QFile + QTextStream + QVERIFY on contains/not-contains). Use forward-
    slashes in paths (Qt cross-platform). Use QStringLiteral for the literal
    tokens. Comment headers English ASCII-only.

    Test method bodies (approximate shape — adapt to the existing helper
    conventions read in read_first):

      void QmlUiAuditTests::deletedSettingsPathsStayAbsent()
      {
        // Phase 57 CLEAN-02 regression: the 7 obsolete QML files locked by
        // Phase 50 §1.6 (+ legacy Sidebar/FilamentPanel/PrintSettings deferred
        // by Phase 52) must stay deleted. Asserts qml.qrc cleanliness AND
        // on-disk absence.
        const QStringList deletedPaths = {
            QStringLiteral("pages/SettingsPage.qml"),
            QStringLiteral("pages/ConfigPage.qml"),
            QStringLiteral("components/ParamsPage.qml"),
            QStringLiteral("components/SearchDialog.qml"),
            QStringLiteral("panels/Sidebar.qml"),
            QStringLiteral("panels/FilamentPanel.qml"),
            QStringLiteral("panels/PrintSettings.qml"),
        };
        // qml.qrc must not contain any of them.
        QFile qrc(QStringLiteral("src/qml_gui/qml.qrc"));
        QVERIFY(qrc.open(QIODevice::ReadOnly | QIODevice::Text));
        const QString qrcContent = QTextStream(&qrc).readAll();
        qrc.close();
        for (const QString &p : deletedPaths)
          QVERIFY2(!qrcContent.contains(p),
                   qPrintable(QStringLiteral("qml.qrc reintroduced deleted entry: %1").arg(p)));
        // Files must not exist on disk.
        for (const QString &p : deletedPaths) {
          const QString full = QStringLiteral("src/qml_gui/") + p;
          QVERIFY2(!QFileInfo::exists(full),
                   qPrintable(QStringLiteral("Deleted QML file reappeared on disk: %1").arg(full)));
        }
      }

      void QmlUiAuditTests::deletedRoutesStayAbsent()
      {
        // Phase 57 CLEAN-01 regression: the 3 named routes + their dead
        // deferred-config-exit machinery must stay removed.
        const QStringList deadTokens = {
            QStringLiteral("canLeaveSettingsPage"),
            QStringLiteral("requestConfigPageExitIfNeeded"),
            QStringLiteral("executeDeferredConfigExit"),
            QStringLiteral("clearDeferredConfigExit"),
            QStringLiteral("requestLeaveSettingsPage"),
            QStringLiteral("leave-settings-page"),
        };
        const QStringList sourceFiles = {
            QStringLiteral("src/qml_gui/BackendContext.h"),
            QStringLiteral("src/qml_gui/BackendContext.cpp"),
            QStringLiteral("src/core/viewmodels/ConfigViewModel.h"),
            QStringLiteral("src/core/viewmodels/ConfigViewModel.cpp"),
        };
        for (const QString &path : sourceFiles) {
          QFile f(path);
          QVERIFY2(f.open(QIODevice::ReadOnly | QIODevice::Text),
                   qPrintable(QStringLiteral("Cannot open %1").arg(path)));
          const QString content = QTextStream(&f).readAll();
          f.close();
          for (const QString &token : deadTokens)
            QVERIFY2(!content.contains(token),
                     qPrintable(QStringLiteral("%1 reintroduced deleted token: %2").arg(path, token)));
        }
      }

    Declare both as `private slots:` in the class header (next to the existing
    settings* tests). Do NOT touch tests/CMakeLists.txt unless the existing
    build does not auto-pick up new methods (it should — Qt moc + automoc
    handle this; verify in Task 4's build).
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && cmake --build build --target QmlUiAuditTests --config Release 2>&1 | tail -5 && ctest --test-dir build -R QmlUiAuditTests --output-on-failure</automated>
  </verify>
  <done>
    2 new private-slot tests compiled and pass. QmlUiAuditTests now reports
    38 passed (was 36). Grep for the deleted tokens via the test gives RED
    if any are reintroduced (test this manually if time permits: temporarily
    add a token, watch the test fail, remove it).
  </done>
</task>

<task type="auto">
  <name>Task 3: Broader dead-UI sweep (CLEAN-02 audit discipline)</name>
  <files>(no file changes unless a clearly-dead UI is found and deleted — otherwise SUMMARY-only)</files>
  <read_first>
    - .planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-CONTEXT.md (CLEAN-02 audit clause)
  </read_first>
  <action>
    Run the broader-UI sweep using the Grep tool (NOT Bash grep — encoding
    guard). Cover src/qml_gui/ and src/core/ for these patterns:

      Pattern 1: `\bold[A-Z][a-zA-Z]*Page\.qml|\bold[A-Z][a-zA-Z]*View\.qml|\bold[A-Z][a-zA-Z]*Panel\.qml|\bold[A-Z][a-zA-Z]*Dialog\.qml`
      Pattern 2: `legacy|deprecated|unused|placeholder-only|disconnected`  (case-insensitive)
      Pattern 3: `^.*//\s*TODO.*Phase\s*(5[0-9]|6[0-9])`  (stale deferral comments)

    For each hit, classify:
      (a) Already-deleted by 57-01 → record "addressed" (no action).
      (b) Explicitly out-of-scope (SoftwareViewport fallback, ExportPresetBundleDialog
          "简化版", Q_UNUSED C++ markers, ConfigOptionModel legacy-fallback
          comment) → record "out-of-scope: <reason>" (no action).
      (c) Newly-found dead QML UI with zero live references → re-run the
          57-01 Task 1 reference-grep discipline (Loader/sourceComponent/
          import/qmlRegisterType/qml.qrc). If genuinely dead AND small
          (<200 lines, no business logic), delete it in this task using
          the same Edit-then-rm flow as 57-01 Task 2, then update qml.qrc.
          If large or has business logic, log it in SUMMARY under
          "Phase 57+ Deferred UI Findings" with file path, line count,
          and reason for deferral.
      (d) Stale TODO comments referencing Phases 50-56 that are now complete
          → record; do NOT mass-edit (out of CLEAN-04 scope which is
          encoding/comments only on changed files).

    The sweep is documented in SUMMARY with: pattern, total hits, per-hit
    classification, action taken. The success criterion per CLEAN-02 is
    "No active UI path retains parallel old/legacy/deprecated/unused/
    placeholder-only or disconnected copies of replaced Prepare, Preview,
    or Settings components" — the sweep proves this by enumeration.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && grep -rln --include="*.qml" -E "old[A-Z].*Page\.qml|old[A-Z].*View\.qml|old[A-Z].*Panel\.qml|old[A-Z].*Dialog\.qml" src/qml_gui/ 2>/dev/null | wc -l | tr -d ' ' | grep -q '^0$' && echo PASS_NO_OLDUI || echo REVIEW_OLDUI</automated>
  </verify>
  <done>
    Broader-UI sweep documented in SUMMARY. Any newly-found dead UI either
    deleted (with the 57-01 reference-grep discipline applied first) or
    logged for a future phase. Pattern 1 grep returns 0 hits for old*
    QML files in src/qml_gui/.
  </done>
</task>

<task type="auto">
  <name>Task 4: Encoding guard + canonical build green</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - C:\Users\67376\.claude\CLAUDE.md (global encoding guard — UTF-8 no BOM)
  </read_first>
  <action>
    Step 1 — Run the global encoding guard over every file touched by Wave 1
    and Wave 2:

      python %USERPROFILE%/.coding-encoding-guard/encoding_guard.py \
        src/qml_gui/qml.qrc \
        src/qml_gui/qmldir \
        src/qml_gui/BackendContext.h \
        src/qml_gui/BackendContext.cpp \
        src/core/viewmodels/ConfigViewModel.h \
        src/core/viewmodels/ConfigViewModel.cpp \
        src/qml_gui/panels/LeftSidebar.qml \
        src/qml_gui/components/GroupNavSidebar.qml \
        src/qml_gui/components/OptionRow.qml \
        tests/ViewModelSmokeTests.cpp \
        tests/QmlUiAuditTests.cpp \
        docs/源码真值功能矩阵.md \
        docs/源码真值基线.md

    (Use the proper Windows path expansion; if USERPROFILE isn't set in
    Git Bash, substitute /c/Users/67376/.coding-encoding-guard/encoding_guard.py.)
    Expectation: 0 BOM, 0 mojibake across all listed files. If any flagged,
    fix with `encoding_guard.py --fix <file>` (NEVER a blind rewrite).

    Step 2 — Run the canonical build (build-rules.md — sole sanctioned command):

      powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1

    Expectations:
      - cmake configure + ninja build green. The 2 new QmlUiAudit test
        methods compile and link.
      - ViewModelSmokeTests: previously-passing count from 57-01 maintained.
      - QmlUiAuditTests: 38 passed, 0 failed (was 36 + 2 new).
      - ZERO new QML warnings vs 57-01 baseline.

    Record final pass counts + encoding-guard verdict in SUMMARY.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && python /c/Users/67376/.coding-encoding-guard/encoding_guard.py src/qml_gui/qml.qrc src/qml_gui/qmldir src/qml_gui/BackendContext.h src/qml_gui/BackendContext.cpp src/core/viewmodels/ConfigViewModel.h src/core/viewmodels/ConfigViewModel.cpp tests/QmlUiAuditTests.cpp "docs/源码真值功能矩阵.md" "docs/源码真值基线.md" && powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1 -ExitOnBuildFailure</automated>
  </verify>
  <done>
    Encoding guard passes (0 BOM, 0 mojibake) on all phase-touched files.
    Canonical auto_verify_with_vcvars.ps1 green. QmlUiAuditTests 38/38.
    SUMMARY records final pass counts and the encoding-guard verdict.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none)   | Doc rewrites and a local compiled regression test. No network, auth, IPC, or untrusted-input surface. The regression test reads project source files via QFile (trusted local inputs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Tampering | docs/源码真值*.md | mitigate | Only path strings rewritten; historical project-name mentions preserved; ambiguous mentions logged for human review (Task 1). |
| T-57-06 | Repudiation | Regression audit test | mitigate | Test compiles into ctest; any future reintroduction of deleted symbols fails CI deterministically (Task 2). |
| T-57-07 | Information Disclosure | (n/a) | accept | No PII/secrets in docs or tests. |
| T-57-SC | Tampering | (n/a — no package installs) | accept | No npm/pip/cargo installs in this plan. Package legitimacy gate N/A. |
</threat_model>

<verification>
- Task 1: `! grep -q "third_party/CrealityPrint/" docs/源码真值*.md` prints PASS_DOC_REWRITE.
- Task 2: 2 new QmlUiAuditTests methods compile; ctest -R QmlUiAuditTests passes; count = 38.
- Task 3: Broader-UI sweep SUMMARY complete; Pattern-1 grep returns 0.
- Task 4: encoding_guard.py 0 BOM/0 mojibake; auto_verify_with_vcvars.ps1 green.
</verification>

<success_criteria>
- 2 docs rewritten (locked cleanup clause satisfied).
- 2-method compiled regression audit test locks Wave 1 deletions as a permanent
  invariant (CLEAN-02 regression guard).
- Broader-UI sweep documented; any other dead UI either deleted (with reference-
  grep discipline) or logged.
- Encoding guard clean across all phase-touched files (CLEAN-04).
- Canonical build green with zero new QML warnings.
- Phase 57 closes with CLEAN-01/02/03/04 fully delivered.
</success_criteria>

<output>
Create `.planning/phases/57-deprecated-ui-removal-and-architecture-cleanup/57-02-SUMMARY.md` when done
</output>
