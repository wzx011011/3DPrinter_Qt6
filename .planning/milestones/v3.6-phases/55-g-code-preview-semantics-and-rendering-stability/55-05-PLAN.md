---
phase: 55-g-code-preview-semantics-and-rendering-stability
plan: 05
type: execute
wave: 3
depends_on: [55-03, 55-04]
files_modified:
  - tests/QmlUiAuditTests.cpp
  - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md
autonomous: true
requirements: [GCODE-04, GCODE-05]
user_setup: []

must_haves:
  truths:
    - "main_qml.cpp registers RhiViewport as the default GLViewport QML type; SoftwareViewport is registered only behind an explicit env-var gate (startup-policy audit guard)"
    - "PreviewPage.qml binds GLViewport (resolves to RhiViewport), never SoftwareViewport"
    - "Per-Task Verification Map in 55-VALIDATION.md is filled in with the actual Task IDs / Plan numbers from Plans 01-04"
    - "55-VALIDATION.md frontmatter has nyquist_compliant: true, wave_0_complete: true, status: ready-for-verify"
  artifacts:
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "D3D11 startup-policy audit (RhiViewport default registration, SoftwareViewport only behind gate)"
      contains: "RhiViewport"
    - path: ".planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md"
      provides: "Per-task verification map filled; frontmatter signed off"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/QmlUiAuditTests.cpp"
      to: "src/qml_gui/main_qml.cpp"
      via: "source-audit of qmlRegisterType calls"
      pattern: "qmlRegisterType<RhiViewport>"
---

<objective>
Close the GCODE-04 D3D11-no-regression gate with the final startup-policy audit (RhiViewport default registration + SoftwareViewport only behind an explicit gate) and finalize the phase VALIDATION.md so the Per-Task Verification Map reflects the actual Plan 01-04 task IDs and the frontmatter is signed off for `/gsd:verify-work`.

Purpose: Plan 04 closed the source-audit guards for PreviewPage.qml + computePreviewDrawRange + GcvPackedSegment sizeof. Plan 05 adds the one remaining GCODE-04 row from 55-VALIDATION.md — the startup-policy assertion that `main_qml.cpp` registers RhiViewport as the default — and finalizes VALIDATION.md. After this plan, all five GCODE requirements have green automated commands and the phase is ready for the verify-work gate.

Output: One new source-audit test in QmlUiAuditTests.cpp; VALIDATION.md frontmatter + Per-Task Verification Map updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-CONTEXT.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-03-SUMMARY.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-04-SUMMARY.md

@tests/QmlUiAuditTests.cpp
@src/qml_gui/main_qml.cpp

<interfaces>
<!-- Existing QmlUiAuditTests slots that already cover the registration policy (do NOT duplicate). -->
Existing test slots in QmlUiAuditTests.cpp:
  - mainRegistersRhiViewportByDefaultWithSoftwareFallback()
  - mainRegistersRhiViewportOnlyBehindExplicitGate()
  - renderBenchmarkMatchesRhiBackendPolicy()

These already assert the main_qml.cpp registration policy at a high level. Plan 05 adds ONE focused guard: an explicit, Phase-55-tagged assertion that ties the registration to the GCODE-04 requirement, so a future regression that flips the default is caught with a Phase-55-tagged failure message (clearer than the existing generic failure).

main_qml.cpp expected pattern (per 55-RESEARCH "D3D11 / SoftwareViewport Status"):
  qmlRegisterType<RhiViewport>("OWzx", 1, 0, "GLViewport");   // default path
  // SoftwareViewport registered only behind OWZX_OPENGL env or QRhi init failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-55-tagged D3D11 startup-policy audit guard</name>
  <files>tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - tests/QmlUiAuditTests.cpp (full file — locate existing mainRegistersRhiViewportByDefaultWithSoftwareFallback and mainRegistersRhiViewportOnlyBehindExplicitGate tests; the readSource helper)
    - src/qml_gui/main_qml.cpp (the qmlRegisterType calls for RhiViewport -> "GLViewport" and any SoftwareViewport registration; the env-var gate logic for the fallback)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (GCODE-04 rows — exact behaviors to assert)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md (Section "D3D11 / SoftwareViewport Status")
  </read_first>
  <action>
    Add one private slot `gcode04RhiViewportIsDefaultRegistrationNoSoftwareViewportInPreviewPath()` near the existing mainRegistersRhiViewport* slots. The test:

    1. readSource("src/qml_gui/main_qml.cpp").
    2. QVERIFY2(mainQml.contains("qmlRegisterType<RhiViewport>"), "main_qml.cpp must register RhiViewport as a QML type (GCODE-04)");
    3. QVERIFY2(mainQml.contains("\"GLViewport\""), "RhiViewport must be registered under the QML name 'GLViewport' (GCODE-04 default path)");
    4. QVERIFY2(src.contains("SoftwareViewport") ? mainQml.contains("OWZX_OPENGL") || mainQml.contains("qEnvironmentVariableIsSet") : true, "SoftwareViewport registration, if present, must be guarded behind an explicit env-var gate (GCODE-04 fallback-only policy)");
    5. readSource("src/qml_gui/pages/PreviewPage.qml") (re-assert; belt-and-suspenders vs Plan 04 Task 2): QVERIFY2(!previewPage.contains("SoftwareViewport"), "PreviewPage.qml must never reference SoftwareViewport (GCODE-04)");
    6. QVERIFY2(previewPage.contains("GLViewport"), "PreviewPage.qml must bind GLViewport (resolves to RhiViewport on the default D3D11 path) (GCODE-04)");

    The test method name is prefixed `gcode04` so a future regression produces a Phase-55-tagged failure pointing directly at the requirement. Add a `// Phase 55 (GCODE-04): D3D11 default + SoftwareViewport fallback-only policy` comment header.

    Encoding: ASCII/UTF-8 without BOM. AUTOMOC caveat applies.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "gcode04RhiViewportIsDefaultRegistrationNoSoftwareViewportInPreviewPath" tests/QmlUiAuditTests.cpp returns >= 2 (decl + impl).
    - `ctest --output-on-failure -R QmlUiAudit` passes with the new test green.
    - The test would FAIL if anyone (a) removes the RhiViewport -> "GLViewport" registration, (b) registers SoftwareViewport unconditionally, or (c) adds SoftwareViewport to PreviewPage.qml. (Verify by reading the assertions, not by actually breaking the code.)
  </acceptance_criteria>
  <done>GCODE-04 startup-policy guard is in place; the audit test is Phase-55-tagged so regressions point at the requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 55-VALIDATION.md frontmatter + Per-Task Verification Map</name>
  <files>.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md</files>
  <read_first>
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (full file — current frontmatter status: draft / nyquist_compliant: false / wave_0_complete: false; the empty Per-Task Verification Map table)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-01-SUMMARY.md
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-02-SUMMARY.md
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-03-SUMMARY.md
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-04-SUMMARY.md
  </read_first>
  <action>
    Update 55-VALIDATION.md as follows:

    1. Frontmatter (top of file): change `status: draft` -> `status: ready-for-verify`; change `nyquist_compliant: false` -> `nyquist_compliant: true`; change `wave_0_complete: false` -> `wave_0_complete: true`. Leave `created:` and `phase:`/`slug:` unchanged.

    2. Per-Task Verification Map table (the "TBD" rows): replace the single TBD row with one row per actual task across Plans 01-04, using the Plan/Task identifiers. Each row MUST reference a real ctest command. Example rows:
       | 55-01-T1 | 55-01 | 0 | GCODE-01 | Fixture coverage (12 roles, tags, layers, travel, tool-change) | source-audit | grep fixture (in task verify) | OK |
       | 55-01-T2 | 55-01 | 0 | GCODE-02 | PreviewParserTests target builds + RED scaffold | unit | ctest --output-on-failure -R PreviewParser | OK |
       | 55-02-T1 | 55-02 | 1 | GCODE-02 | Wire format role field + render-side skip | build | scripts/auto_verify_with_vcvars.ps1 | OK |
       | 55-02-T2 | 55-02 | 1 | GCODE-02 | 20-role parser + 17-mode audit (PreviewParser RED flips GREEN) | unit | ctest --output-on-failure -R PreviewParser | OK |
       | 55-03-T1 | 55-03 | 2 | GCODE-02 | VisibilityFilter.qml + qml.qrc registration | build | scripts/auto_verify_with_vcvars.ps1 | OK |
       | 55-03-T2 | 55-03 | 2 | GCODE-02 | PreviewPage.qml insertion + roleVisibility binding | build + audit | ctest --output-on-failure -R QmlUiAudit | OK |
       | 55-04-T1 | 55-04 | 2 | GCODE-03 | Role-toggle no-repack + legend + atomicity | unit | ctest --output-on-failure -R ViewModelSmoke | OK |
       | 55-04-T2 | 55-04 | 2 | GCODE-04 | SoftwareViewport/role-skip/sizeof audit guards | source-audit | ctest --output-on-failure -R QmlUiAudit | OK |
       | 55-04-T3 | 55-04 | 2 | GCODE-01, GCODE-05 | No-placeholder + reslice/export/page-switch invariants | unit/integration | ctest --output-on-failure -R E2EWorkflow | OK |
       | 55-05-T1 | 55-05 | 3 | GCODE-04 | D3D11 startup-policy audit guard | source-audit | ctest --output-on-failure -R QmlUiAudit | OK |
       Map each task to the requirement(s) it satisfies. Cross-check against the Requirement-Level Verification Map so every requirement row has at least one task row.

    3. Validation Sign-Off checklist at the bottom: tick every box:
       - [x] All tasks have `<automated>` verify or Wave 0 dependencies
       - [x] Sampling continuity: no 3 consecutive tasks without automated verify
       - [x] Wave 0 covers all MISSING references (fixture + parser test file)
       - [x] No watch-mode flags
       - [x] Feedback latency < 60s (quick audit)
       - [x] `nyquist_compliant: true` set in frontmatter
       Change **Approval:** pending -> **Approval:** ready-for-verify (Phase 55 planning complete).

    4. Add a "Phase 55 Test Index" section at the end listing the canonical commands to run for full phase verification:
       - ctest --output-on-failure -R PreviewParser  (parser + role + mode coverage)
       - ctest --output-on-failure -R ViewModelSmoke (no-repack + legend + atomicity)
       - ctest --output-on-failure -R QmlUiAudit      (D3D11 + SoftwareViewport + role-skip + sizeof)
       - ctest --output-on-failure -R E2EWorkflow     (no-placeholder + reslice/export/page-switch)
       - ctest --output-on-failure                   (full suite)
       - powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1  (canonical build+test gate)

    Encoding: UTF-8 without BOM. ASCII-only for the added English text. Do not modify the existing table structure or requirement-level rows — only the Per-Task table and the Sign-Off + new Test Index section.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true\|wave_0_complete: true\|ready-for-verify" .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "nyquist_compliant: true" .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md returns >= 1.
    - grep -c "wave_0_complete: true" .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md returns >= 1.
    - grep -c "TBD" .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md returns 0 (the Per-Task map is fully populated).
    - The Per-Task Verification Map contains >= 10 task rows covering all 5 requirement IDs (GCODE-01 through GCODE-05).
    - The Validation Sign-Off checklist has all 6 boxes ticked ([x]).
    - The Phase 55 Test Index section lists the 6 canonical commands.
    - File is UTF-8 without BOM; no mojibake introduced (project CLAUDE.md encoding rule).
  </acceptance_criteria>
  <done>55-VALIDATION.md is signed off, Per-Task map populated with real task IDs, frontmatter reflects nyquist_compliant + wave_0_complete + ready-for-verify; phase is ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local source files -> test process | Source-audit test reads main_qml.cpp and PreviewPage.qml as text; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05-SC | Tampering | none — no package installs | accept | No third-party packages; pure source-audit test + Markdown edit. |
| T-55-05-01 | Repudiation | VALIDATION.md sign-off | mitigate | The Sign-Off checklist is only ticked after every command is confirmed green in Plans 01-04 SUMMARYs. Do not tick boxes speculatively — source-truth honesty rule. |

No high-severity threats. No network, auth, or persistence surface.
</threat_model>

<verification>
- Canonical build script passes (QmlUiAuditTests rebuilds and the new gcode04* test is green).
- `ctest --output-on-failure` (full suite) green across PreviewParser, ViewModelSmoke, QmlUiAudit, E2EWorkflow.
- 55-VALIDATION.md frontmatter shows `nyquist_compliant: true`, `wave_0_complete: true`, `status: ready-for-verify`.
</verification>

<success_criteria>
- GCODE-04 fully closed: D3D11 startup-policy guard is in place alongside the Plan 04 source-audit guards. The Phase-55-tagged test name makes any future regression point directly at the requirement.
- Phase 55 VALIDATION.md is signed off and ready for the `/gsd:verify-work` gate.
- All five GCODE requirements (GCODE-01..05) have green automated commands mapped in the Per-Task Verification Map.
</success_criteria>

<output>
Create `.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-05-SUMMARY.md` when done.
</output>
