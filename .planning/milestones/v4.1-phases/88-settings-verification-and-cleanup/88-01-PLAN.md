# Phase 88 Plan: Settings Verification And Cleanup

## Goal

Finish v4.1 settings restoration with cleanup, final audit coverage, canonical verification, and visual evidence.

## Tasks

1. RED audit.
   - Add `settingsRestorationMilestoneHasFinalVerificationCoverage`.
   - Require restored settings resources in `qml.qrc` with normalized entries.
   - Require main.qml printer/material/process dialog dispatch.
   - Require Phase 85/86/87 audit anchors and deleted-path coverage.

2. Cleanup.
   - Normalize `qml.qrc` settings-related resource entries.
   - Do not touch unrelated network/device/cloud dialogs.

3. Visual evidence.
   - Launch the current app.
   - Capture at least one app/settings screenshot under the Phase 88 visual-evidence directory.

4. Verification.
   - Run targeted QML audit.
   - Run encoding guard and `git diff --check`.
   - Run canonical verifier.
   - Complete Phase 88 and update requirements.

## Expected Files

- `src/qml_gui/qml.qrc`
- `tests/QmlUiAuditTests.cpp`
- `.planning/phases/88-settings-verification-and-cleanup/*`
