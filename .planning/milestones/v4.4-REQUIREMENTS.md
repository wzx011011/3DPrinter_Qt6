# Requirements Archive: v4.4 Wipe-Tower Geometry Readback And Real Rendering

**Archived:** 2026-07-12
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: OWzx Slicer v4.4 Wipe-Tower Geometry Readback And Real Rendering

**Defined:** 2026-07-11
**Core Value:** OrcaSlicer upstream behavior is the product source of truth; Qt6 code must inherit that behavior and must not invent new product behavior without an explicit upstream mapping or documented block.

## v4.4 Requirements

### Source-Truth Inventory

- [x] **WTAUDIT-01**: The wipe-tower capture + rendering surface has a current inventory mapping the placeholder geometry path (`GizmoGeometry::buildWipeTowerVertices`, RhiViewport defaults 10/10/50/100/25), the upstream `Print::wipe_tower_data()` / `WipeTowerData` struct anchors, the readback design (post-slice Print → RhiViewport dims), and the rendering-upgrade decision (box dims vs real mesh) before implementation.
- [x] **WTAUDIT-02**: The post-slice readback integration point (where `Print::wipe_tower_data()` becomes available after slicing, and how it flows to `RhiViewport`/`EditorViewModel`) and the rendering-upgrade approach (real mesh via `wipe_tower_mesh_data`/convex_hull vs dimensioned box from bbx/depth/height) are frozen as locked decisions before implementation.

### Geometry Readback

- [ ] **WTREAD-01**: After a successful slice, the Qt6 app reads real wipe-tower geometry from `Print::wipe_tower_data()` — at minimum the bbx (position, width, depth), height, brim_width, and rib_offset — replacing the hardcoded placeholder defaults.
- [ ] **WTREAD-02**: The readback respects `Print::has_wipe_tower()` — when no wipe-tower exists (single-material / `enable_prime_tower` off), no wipe-tower geometry is pushed and the renderer shows none (graceful, no placeholder box leak).

### Real Rendering

- [ ] **WTRENDER-01**: The wipe-tower rendered in the Prepare view reflects the real sliced geometry dimensions (width/depth/height/position from `wipe_tower_data`), not the placeholder 10/10/50/100/25 defaults.
- [ ] **WTRENDER-02**: `GizmoGeometry::buildWipeTowerVertices` is upgraded from the 36-vertex placeholder box toward real geometry — either a dimensioned box from the real bbx/depth/height (minimum) or a real mesh from `wipe_tower_mesh_data` (mirroring upstream `3DScene.cpp:887 load_real_wipe_tower_preview`), with brim and rib_offset represented where applicable.

### Verification And Regression

- [ ] **WTVERIFY-01**: The placeholder wipe-tower defaults (10/10/50/100/25) are no longer the steady-state rendered values when a real slice exists; source/QML audits cover the readback wiring and the rendering-upgrade anchors.
- [ ] **WTVERIFY-02**: The canonical verifier passes, `build/OWzxSlicer.exe` launches, Prepare/Preview/AssembleView rendering is regression-free (PrepareSceneDataTests, ViewModelSmokeTests, QmlUiAuditTests pass), and the wipe-tower is visible at runtime when a multi-material slice produces one.

## Future Requirements

### Adjacent Local/Offline Work

- **AUTOMAP-FUTURE-01**: Auto filament-map recommendation — let libslic3r auto-compute in `Print::` (mode < fmmManual), widen the Qt6 mode enum to the upstream 4-value `FilamentMapMode`, surface `flush_volumes_matrix` + `extruder_ams_count` config, read back `filament_maps`, and build a `FilamentGroupPopup`-equivalent QML UI.
- **FIXTURE-FUTURE-01**: Add missing CLI fixtures (`hotend.stl`, `Block20XY.stl`) and deterministic GUI fixture loading for visual screenshots (unblocked by v4.3).
- **BACKEND-FUTURE-01**: Resolve the D3D12 QRhi crash and evaluate Vulkan only after an SDK/runtime path exists.
- **MEASURE-FUTURE-01**: Full GLGizmoMeasure feature-picking engine + AssembleViewDataPool clipper (needs per-volume ITS).

### Removed Product Scope

- **NETWORK-REMOVED-01**: LAN device discovery, device send/upload, cloud print, Monitor task lifecycle, ModelMall/Home WebView/cloud workflows, live camera/network streams, and printer-connected hardware workflows are not future requirements unless the user explicitly reopens them.

## Out of Scope

Explicitly excluded to keep v4.4 focused.

| Feature | Reason |
|---|---|
| Auto filament-map recommendation | Separate milestone; the cleanest impl leans on libslic3r's auto-firing in `Print::` + a QML popup. Loosely coupled to wipe-tower but deferred to avoid scope creep. |
| Per-plate wipe-tower architecture refactor | The v3.0 audit flags Qt6's per-plate filtered-copy slice path; multi-plate wipe-tower correctness depends on that deeper architecture. v4.4 reads geometry from the slice result that exists. |
| D3D12 or Vulkan backend promotion | Renderer backend work is blocked/future and not required for wipe-tower readback on D3D11. |
| libslic3r slicing algorithm changes | Wipe-tower readback must not change slicing engine behavior. |
| Live camera/network/device workflows | Removed from forward product scope by user direction on 2026-07-07. |
| New product behavior not mapped to OrcaSlicer upstream | Violates the project core value. |

## Traceability

| Requirement | Phase | Status |
|---|---|---|
| WTAUDIT-01 | Phase 99 | Complete |
| WTAUDIT-02 | Phase 99 | Complete |
| WTREAD-01 | Phase 100 | Not started |
| WTREAD-02 | Phase 100 | Not started |
| WTRENDER-01 | Phase 101 | Not started |
| WTRENDER-02 | Phase 101 | Not started |
| WTVERIFY-01 | Phase 102 | Not started |
| WTVERIFY-02 | Phase 102 | Not started |

**Coverage:**
- v4.4 requirements: 8 total
- Mapped to phases: 8
- Unmapped: 0

---
*Requirements defined: 2026-07-11*
*Last updated: 2026-07-11 after Phase 99 gap audit (WTAUDIT-01, WTAUDIT-02 complete)*
