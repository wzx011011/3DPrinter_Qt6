---
phase: 82-preview-gcode-roles-color-modes-and-rendering
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/core/viewmodels/PreviewViewModel.h
  - src/core/viewmodels/PreviewViewModel.cpp
  - src/qml_gui/pages/PreviewPage.qml
  - tests/PreviewParserTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [PVRENDER-01, PVRENDER-02, PVRENDER-03]
---

# Phase 82 Plan: Restore Preview Role And Color Mode Semantics

## Objective

Lock Preview role colors, role visibility, color/view modes, and payload
preservation to source-truth behavior while making unavailable modes visible to
the user as honest status.

## Tasks

1. Add RED tests:
   - parser/viewmodel tests for mode availability and GCV1 payload survival
     across all view modes,
   - QML audit for header availability status, role visibility, legend, and
     renderer bindings.

2. Extend `PreviewViewModel`:
   - expose `currentViewModeAvailable`,
   - expose `currentViewModeStatus`,
   - expose invokable `viewModeAvailable(index)` and `viewModeStatusText(index)`.

3. Update `PreviewPage.qml`:
   - add a compact status pill next to the view-mode combo,
   - keep combo activation routed through `setViewModeIndex`.

4. Preserve role/color paths:
   - no parser wire-format changes,
   - no role-mask shape changes,
   - no role visibility repack.

5. Verify:
   - focused parser/viewmodel and QML audit tests,
   - `git diff --check`,
   - encoding guard,
   - canonical verifier if touched C++/QRC paths require full confidence.

## Acceptance Criteria

- Role colors and visibility remain upstream-indexed and renderer-bound.
- Data-unavailable view modes are visibly honest in the Preview header.
- Every view mode keeps a valid GCV1 payload after fixture load.
- Phase 82 verification status is `passed`.
