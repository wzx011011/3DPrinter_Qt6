# Phase 83 Plan: Final Preview Cleanup And Verification

## Goal

Close v4.0 by proving the restored Preview page is active, clean, and
verifiable.

## Plan

1. Add a final `QmlUiAuditTests` source audit covering:
   - restored Preview QML resource registration,
   - renderer/ViewModel bindings,
   - actionable layer, move, stats, role, and legend controls,
   - absence of stale simple layer slider paths,
   - absence of direct `SoftwareViewport` usage in `PreviewPage.qml`,
   - absence of placeholder markers in restored Preview QML surfaces.
2. Run focused checks:
   - `git diff --check`,
   - encoding guard,
   - the focused `QmlUiAuditTests` test.
3. Run the canonical verifier:
   - `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`.
4. Launch or reuse `build/OWzxSlicer.exe`, navigate to Preview, and save runtime
   visual evidence under `visual-evidence/`.
5. Update Phase 83 verification, summary, roadmap, requirements, and state.
6. Commit Phase 83.

## Acceptance

- `PVCLEAN-01`: No stale Preview UI path remains in the active Preview page or
  resource list.
- `PVVERIFY-01`: Automated QML/source audits cover the restored bindings,
  placeholder cleanup, and upstream-mapped behavior paths.
- `PVVERIFY-02`: Canonical verifier passes, the executable launches, and Preview
  screenshot evidence is recorded.
