# Requirements: v5.5 Build/Run Parity and Dependency Provenance

**Defined:** 2026-07-23
**Core Value:** The build/run path must be explainable and reproducible before
new migration work resumes.
**Status:** Complete and verified by the canonical script on 2026-07-23.

## DOC: Planning State

- [x] **DOC-01** (Phase 188): Top-level planning files identify v5.5 as the
  active milestone and state the v5.4 handoff without contradictions. The
  handoff must say that v5.4 code/regression work closed, while the i18n long
  tail remains a deferred translation session.

## PROV: Dependency Provenance

- [x] **PROV-01** (Phase 189): A project planning artifact documents:
  - CI Qt install path source: `jurplel/install-qt-action@v4`, Qt `6.10.0`,
    `win64_msvc2022_64`, module `qtshadertools`.
  - Local Qt cache candidate: `.deps/Qt6.10/6.10.0/msvc2022_64`.
  - Legacy/local Qt candidates: `E:\Qt6.10` and `D:\Qt6.10`.
  - Default upstream source root: `third_party/OrcaSlicer`.
  - Local fallback source root, if used: `D:\work\OrcaSlicer`.
  - Dependency bundle source: OrcaSlicer deps prefix selected by the canonical
    script.
  - The rule that Qt is prebuilt and not compiled from source in this project.

## VERIFY: Canonical Script Classification

- [x] **VERIFY-01** (Phase 190): `scripts/auto_verify_with_vcvars.ps1` reports
  the selected Qt root, upstream source root, deps prefix, compiler, Ninja, and
  build directory before configure. Failure output must classify the stage as
  configure, build, test, deployment, launch, or optional E2E.

## RUN: Launch Liveness

- [x] **RUN-01** (Phase 191): The canonical script's launch gate starts
  `build/OWzxSlicer.exe`, waits at least 5 seconds, reports `APP_RUNNING_PID`
  when the process stays alive, and reports an app-start failure when it exits
  early.

## GATE: CI/Local Parity

- [x] **GATE-01** (Phase 192): A v5.5 audit file records:
  - GitHub CI Qt install mechanism and architecture.
  - Local selected Qt/upstream/deps paths from the script output.
  - Whether the app binary was produced.
  - Whether the app launched locally.
  - Any local canonical-script failure, classified as product, fixture,
    dependency, or local environment.
  - Follow-up action only when the difference affects product correctness.

## Out of Scope

- New product behavior.
- Upstream OrcaSlicer source patches.
- Alternate build scripts.
- Alternate build directories.
- Translation completion for de/fr/ja/ko.
- LAN/cloud/device/camera/printer-hardware workflows.

## Traceability

| Requirement | Phase | Primary Artifact |
|---|---:|---|
| DOC-01 | 188 | `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `.planning/INDEX.md` |
| PROV-01 | 189 | `.planning/milestones/v5.5-phases/189-dependency-provenance-contract/PROVENANCE.md` |
| VERIFY-01 | 190 | `scripts/auto_verify_with_vcvars.ps1` |
| RUN-01 | 191 | `scripts/auto_verify_with_vcvars.ps1` |
| GATE-01 | 192 | `.planning/milestones/v5.5-MILESTONE-AUDIT.md` |
