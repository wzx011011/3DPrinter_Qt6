# v5.5 Milestone Audit: Build/Run Parity and Dependency Provenance

**Date:** 2026-07-23
**Status:** Complete
**Canonical evidence:** `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`

## CI Evidence

- GitHub Actions checks out submodules recursively, then installs Qt through
  `jurplel/install-qt-action@v4`.
- The CI Qt contract is Qt `6.10.0`, host `windows`, target `desktop`,
  architecture `win64_msvc2022_64`, module `qtshadertools`.
- CI builds with MSVC and Ninja and provides `DEPS_PREFIX` from the checked-out
  OrcaSlicer dependency bundle.

## Local Evidence

- Canonical script exit code: `0`.
- Selected Qt root:
  `D:\work\qt\3DPrinter_Qt6\.deps\Qt6.10\6.10.0\msvc2022_64`.
- Selected upstream root: `D:\work\OrcaSlicer`.
- Selected deps prefix:
  `D:\work\OrcaSlicer\deps\build\OrcaSlicer_dep\usr\local`.
- Build directory: `D:\work\qt\3DPrinter_Qt6\build`.
- Binary produced: `build\OWzxSlicer.exe`.
- Launch liveness: the canonical script reported `APP_RUNNING_PID=104080`
  after five seconds.
- Regression evidence: PrepareScene, PartPlate, ViewModel, QML UI audit, and
  PreviewParser tests passed. The E2E workflow reported `All pipeline tests
  passed`.

## Differences And Classification

| Difference | Classification | Outcome |
|---|---|---|
| CI uses the recursively checked-out `third_party/OrcaSlicer` submodule; this local checkout lacked its marker file, so the canonical script selected `D:\work\OrcaSlicer`. | Local environment / reproducibility | The external checkout was refreshed to `d4580e7a3`. Local verification passed, but CI/local source identity requires a properly initialized submodule before claiming byte-for-byte parity. |
| The repository-owned multi-material 3MF fixture contained `--` in an XML comment and the E2E test used an uninitialized-submodule copy. | Fixture | The XML comment was corrected and E2E now uses `tests/data/multi_material_fixture.3mf`; the complete local E2E suite passed. |
| One ViewModelSmokeTests run returned non-zero without an assertion report. The immediately repeated test and the next full canonical run passed. | Local environment / transient | No persistent product failure was reproduced. |

## Follow-up

- Reinitialize `third_party/OrcaSlicer` at its pinned gitlink revision before
  treating local source provenance as identical to CI. This is a reproducibility
  follow-up, not a current product blocker.
- No product-correctness follow-up is open from v5.5 verification.

## Closure

v5.5 closes the local build/run trust gap: Qt is documented as prebuilt, the
canonical script reports its chosen inputs and failure stages, launch liveness
is explicit, and the CI/local difference is classified rather than hidden.
