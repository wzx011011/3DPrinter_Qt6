# v5.16 Gap Baseline — Full-Delta Re-Audit (Upstream vs Qt6)

**Date:** 2026-08-15
**Method:** 9 parallel read-only comparison agents (Gizmos, Dialogs, Plater/menus,
G-code preview, object tree/scopes, PartPlate/3MF IO, preset system, top-level
pages, engine/undo/notifications/CLI) + direct code verification of every P0
claim + canonical build with all test suites (exit 0, all green).
**Truth:** `third_party/OrcaSlicer` (submodule bf686c1 working tree), Qt6 side
`src/core` + `src/qml_gui`.
**Purpose:** Requirement source for milestone v5.16. Every item below carries
file:line evidence. Items marked VERIFIED were re-checked by direct inspection
after the agents reported them.

Canonical verification baseline: `scripts/auto_verify_with_vcvars.ps1` exit 0 on
2026-08-15 (PrepareScene/PartPlate/ObjectPicking/ViewModelSmoke/QmlUiAudit/
ViewportContext/PreviewParser/E2E pipeline all pass; optional target
`test-slice-direct` skipped). Tests are green but do NOT cover the P0 breaks
below — several tests bypass broken QML wiring by calling C++ directly, and
`tests/QmlUiAuditTests.cpp:5810-5815` locks a stale CalibMode mapping as an
assertion.

---

## P0 — Fake-green / broken circuits (clickable but functionally dead)

| # | Item | Evidence | Status |
|---|------|----------|--------|
| FIX-01 | Undo/redo UI dead: every entry (Ctrl+Z, topbar buttons, menus) ends at `RhiViewport.h:355 Q_INVOKABLE void undo() { update(); }` (repaint no-op). Real `EditorViewModel::undo()` (EditorViewModel.cpp:3791) has zero QML callers. Buttons are enabled via real `backend.canUndo` | main.qml:216 → PreparePage.qml:199-201 → RhiViewport.h:355 | VERIFIED |
| FIX-02 | CalibMode off-by-one: submodule deleted `Calib_Auto_PA_Line`, enum shifted; CalibrationServiceMock pins old ints — FlowRate=5 executes Temp tower, TempTower=6 executes Vol-speed, VolSpeed=7 executes VFA, VFA=8 executes Retraction, Retraction=9 matches nothing. Only PA_Line correct. QmlUiAuditTests.cpp:5810-5815 asserts the stale mapping | CalibrationServiceMock.cpp:77/105/131/202/234/265 vs calib.hpp:15-25 | VERIFIED |
| FIX-03 | AdvancedCut unreachable: sole executor `advCutSelected()` has zero QML callers; panel button binds plain `cutSelected()`; connector click signal only fires in GizmoAdvancedCut mode while the connector toggle lives in the GizmoCut panel | EditorViewModel.h:477, PreparePage.qml:3263, RhiViewport.cpp:752 | VERIFIED |
| FIX-04 | Multi-filament slots fake: all slot combos drive one `requestCurrentFilamentPreset`; `materialPresetName()` matches category via mojibake `tr("鑰楁潗")` (correct: "耗材") so lookup always fails → every slot shows unselected + red incompatible frame; slot colors hardcoded | ConfigViewModel.cpp:1332-1339, LeftSidebar.qml:196-201/482-485 | VERIFIED |
| FIX-05 | Plate first/other-layer filament sequence is UI-only: keys never written to config (slicing ignores), never persisted to 3MF, never read back | PartPlate.h:262-282, PreparePage.qml:602-1050 | agent-evidenced |
| FIX-06 | Plate bed-type & spiral don't reach slicing: setters write int fields only, no `p->config()` sync (contrast print_sequence sync at ProjectServiceMock.cpp:5944); 3MF round-trip itself OK | ProjectServiceMock.cpp:5923-5930/5956-5963 vs SliceService.cpp:553-564 | agent-evidenced |
| FIX-07 | 3MF load force-arranges on a 220x220 bed, destroying multi-plate membership (non-locked plates collapse to plate 0, empty tail plates recycled). Upstream never re-arranges on load | ProjectServiceMock.cpp:1092-1093/7919-7920 | agent-evidenced |
| FIX-08 | Delete-object undo loses mesh: `DeleteObjectsCommand::undo` restores via `addObject(name)` (empty object, no mesh); redo deletes by name (renames hit wrong object). AddObjectCommand::redo same | UndoCommands.cpp:187-268, ProjectServiceMock.cpp:6475-6497 | agent-evidenced |
| FIX-09 | Project dirty state never set by real edits (only importModel); no unsaved-changes guard on open/close (upstream close_with_confirm) | ProjectViewModel.cpp:93-98 | agent-evidenced |
| FIX-10 | Object layer-range edits never reach slicing: all CRUD lands in `m_mockLayerRanges`, no `layer_config_ranges` bridge, no HAS_LIBSLIC3R branch | ProjectServiceMock.cpp:4223-4272 | agent-evidenced |
| FIX-11 | User presets not persisted to disk (memory-only; upstream writes user-dir JSON) | PresetServiceMock.cpp:1619-1688 | agent-evidenced |
| FIX-12 | CreatePresetsDialog two-way broken: scope→category mapping inverted (choosing "printer" creates a print preset); inherits dropdown value discarded | CreatePresetsDialog.qml:38-39/137-151 vs PresetServiceMock.h:18 | agent-evidenced |
| FIX-13 | Dirty-guard triple dialog: 3 SettingsDialog instances share configVm and all receive `pendingUnsavedChangesRequested` → one dirty switch opens 3 modals | SettingsDialog.qml:288-304 × main.qml:584-601 | agent-evidenced |
| FIX-14 | importBundleIni category swap: printer→0/print→2 while enum is Print=0/Filament=1/Printer=2 | PresetServiceMock.cpp:1525-1528 | agent-evidenced |
| FIX-15 | "Export all objects as one STL" exports only the first object | ProjectServiceMock.cpp:1190 | agent-evidenced |
| FIX-16 | 9 implemented dialogs have no UI entry point: BedShape/EditGCode/AMS/Firmware/SpeedLimit/WipeTower/PluginManager/EnableLiteMode (instantiated in main.qml, `show*DialogRequested` never emitted) + CreatePresets/ExportBundle (never instantiated). EditGCode save is a no-op; WipeTower matrix OK doesn't persist | BackendContext.h:446-454, main.qml | agent-evidenced |
| FIX-17 | Export-failure notification title mojibake `tr("瀵煎嚭澶辫触")` | BackendContext.cpp:175 | agent-evidenced |
| FIX-18 | CLI `--plate 0=all plates` help claims all-plates; implementation slices one plate | CliRunner.cpp:67-71/277-280 | agent-evidenced |

## Missing features by domain (P1/P2)

**Menus/main frame:** View menu absent (6 view presets Ctrl+0..6, perspective/
orthographic, G-code window toggle, 3D navigator, overhang, outline —
BBLTopbar.qml:866-889 is a disabled stub); Delete All (Ctrl+D); Import Zip
Archive; Import Configs; `.gcode.3mf` sliced-file export; Scale-to-fit build
volume; window file drag&drop (no DropArea anywhere); 11 upstream shortcuts
unbound (Ctrl+N/Shift+S/A/Esc/P/E/C…); Show Configuration Folder; Tip of the Day.

**Import pipeline:** unit inference (meters/inches); ObjColorDialog (OBJ
multi-color); 3MF version warnings (Newer3mfVersion/MsgDataIncompatible/
Legacy); modified-G-code safety warning; embedded project presets; multi-file
progress & fault tolerance (one failure aborts batch); zero-volume-object
notice. Project open drops embedded config (main path uses LoadStrategy::
LoadModel; LoadConfig exists only as mock fallback).

**Preview:** ghost object shells (upstream always renders; Qt6 draws toolpaths
only); 3D tool-position marker (properties wired, renderer never consumes);
Seam/Retract/Unretract/Wipe moves unsupported; FilamentId view mode; SoftwareViewport
fallback renders no preview; tick gaps (no Change-Filament add entry —
`addFilamentChangeAtLayer` has zero QML callers, ColorChange has no
color/extruder picker, no hover tooltip, no Jump-to-Layer); stats gaps (no
Model/Flushed/Tower/Support filament split, Stealth = ×1.4 heuristic, cost
$20/kg hardcoded); Tool/ColorPrint use fixed 8-color cycle instead of
configured extruder_colors.

**Slicing engine:** no persistent Print / step-level incremental invalidation
(full re-slice each time); `loadGCodeFromPrevious()` reuse API has no UI entry;
no auto-reslice on switching to Preview with stale state (upstream do_reslice);
export runs on GUI thread (blocks UI on big files), no post-processing scripts /
thumbnail G-code injection; second slice request silently dropped; SEH hard
crash classification.

**Undo coverage** (upstream = whole-model snapshots): plate ops, facet paints
(FDM/Seam/MMU), volume delete (VolumeDeleteCommand declared, never
instantiated), object-config edits, arrange/orient have no undo;
SelectionCommand dead code; empty-plate aborts slice-all queue.

**Preset system:** preset combo sections (Project/User/System + incompatible
graying); inherits display & Detach; compatibility condition expressions
(presets with conditions systematically misjudged incompatible); per-extruder/
AMS multi-slot data model (`filament_presets` vector); UnsavedChanges Transfer
button & per-item checkboxes; preset rename/delete UI (service exists, zero QML
callers); per-field undo/lock-to-system; group collapse interaction (deferred
v5.11 Phase 227 scope — NOT double-counted here); compare diff of vector values
breaks (QVariant::toString empty); 5 permanently-empty settings tabs (Printer
Material/Extruder, Filament Overrides/Multimaterial/Dependencies).

**Object tree:** part drag-reorder (can_drop type rules); tree filament column
/ Change Filament menu (`setVolumeExtruderId` exists, zero QML callers); Add
settings category picker; batch object table (GUI_ObjectTable); multi-select
menu inside tree (merge etc. only in viewport menu); settings-digest nodes;
paste creates name-only object (no mesh/transform/overrides); show/hide writes
`inst->printable` (semantic clash with printability).

**Dialogs:** FileArchiveDialog, RecenterDialog, SingleChoiceDialog,
SysInfoDialog, MsgDataIncompatible/Legacy warnings, PrintHost upload/queue,
generic cancellable modal progress. AboutDialog license says LGPL v3 (upstream
AGPL). Recent files not persisted + 3 hardcoded mock entries; HomePage recent
hardcoded & unclickable.

**Gizmo interaction depth** (panels + real algorithms exist, interaction layer
missing): Flatten hover-highlight & face pick; Cut 3D drag/rotate plane;
painter family smart/seed fill + overhang filtering (VM props exist, dead
knobs); Measure 3D overlay annotation; Emboss in-place edit of existing text
volume (create-only); SVG drop workbench; Simplify async preview/cancel;
Move/Rotate/Scale numeric input (transformMiniPanel read-only).

**Top-level pages:** HomePage (recent hardcoded, quick actions dead, DailyTips
static single); ProjectPage semantic mismatch (upstream = 3MF thumbnail/
auxiliary viewer; Qt6 = file manager with hardcoded tree, "new" button is
console.log, Size/Modified always "—"); Calibration missing PA_Pattern/
PA_Tower, FlowRate pass1/pass2 two-stage (assets bundled, unwired), "save to
preset" doesn't write presets, history not persisted; Preferences 14/24
settings persisted but dead (default_page, use_inches, backup, …) and ~15
upstream options absent.

**Notifications:** single-slot serial queue (upstream stacked + 8-level
priority); no dedup/count, no Escalated, no hyperlink actions; ~16 of ~45
upstream types; validate/plater notification APIs have zero callers.

**CLI:** export_stl/3mf/slicedata, info, pipe; arrange/orient/cut/split/
assemble/repair/scale_to_fit; arbitrary PrintConfig key overrides (JSON
overlay only).

## Corrected prior-audit conclusions (do NOT re-implement)

1. Object group/ungroup is NOT a gap — upstream OrcaSlicer has no such menu
   action (zero grep hits in GUI sources); Qt6 matches upstream.
2. GLGizmoLayerHeight does not exist in this upstream snapshot — no migration
   debt (layer ranges substitute; FIX-10 covers the real layer-range issue).
3. MeshBoolean/Drill/Flatten/Measure are REAL implementations since the CGAL
   5.4 compat patch (June audit's fake-greens already fixed).

## Out of scope (2026-07-07 user decision; not gaps)

Device discovery/print send/cloud/network/camera streams/model-mall WebView/
multi-machine send. MonitorPage keeps real MQTT skeleton; DeviceListPage and
ModelMallPage deleted (ModelMallViewModel is an orphan); AuxiliaryPage removed
by recorded decision; MultiMachinePage mock.

## Suggested fix order

1. FIX-01/02/17 — one-line-ish user-visible circuit breaks (undo dispatch,
   CalibMode symbolic constants + decontaminate the stale test, mojibake).
2. FIX-04/05/06/07/08/13 — data-link breaks (multi-slot, plate config chain,
   load membership, delete-undo mesh, triple modal).
3. Remaining FIX + P1 batches by domain.
4. P2 interaction depth.
