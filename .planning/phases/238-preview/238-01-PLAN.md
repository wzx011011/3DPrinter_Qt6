# Phase 238 Plan — Preview Completion

**Requirements:** PREV-01..07
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` Preview section

## Plan 1 (single batch)

### Task 1 — PREV-01: ghost shells
- Render semi-transparent object meshes behind toolpaths in the preview
  canvas pass (RhiViewportRenderer preview block :680-726 currently bed+
  segments only). Reuse the prepare-pass mesh pipeline with alpha + no depth
  write (upstream render_shells, GCodeViewer.cpp:4023; shells always loaded
  :3076).
### Task 2 — PREV-02: tool marker
- RhiViewport has markerX/Y/Z/showMarker (RhiViewport.h:90-93) never consumed
  by the renderer. Render a small sphere at the marker position in the
  preview pass (upstream Marker::render :306-460).
### Task 3 — PREV-03: Seam/Retract/Unretract/Wipe moves
- PreviewViewModel parser skips no-movement lines (:1279-1288); add retract/
  unretract/wipe/seam segment types (feed E-only moves + G10/G11 + wipe
  path), role colors, and visibility toggles in VisibilityFilter (upstream
  options_items :913-921).
### Task 4 — PREV-04: tick completion
- ToolChange add entry on empty slot right-click → extruder/color picker
  (addFilamentChangeAtLayer exists, zero QML callers), ColorChange picker
  (replace hardcoded extruder1/red at PreviewLayerRail.qml:274), tick hover
  tooltip (time + gcode), Jump-to-Layer dialog (IMSlider.cpp:1221-1313).
### Task 5 — PREV-05: stats split + stealth + cost
- Split filament usage into Model/Support/Flushed/Tower (parser tags by role
  & flush events M620/unloading), stealth time from gcode comment fields if
  present else keep heuristic flagged, cost from filament preset price key
  (upstream statistics :5161-5277).
### Task 6 — PREV-06: configured extruder colors
- Tool/ColorPrint modes: use activeFilamentColours()/extruder_colors config
  (PreviewViewModel.cpp:1657-1702 fixed palette) + per-extruder visibility
  toggles in legend (upstream m_tool_visibles :5088).
### Task 7 — PREV-07: software fallback preview
- SoftwareViewport paintScene renders bed grid only (:446-473). Draw the
  GCV1 segments with a QPainter projection (or documented honest gate) so the
  preview isn't blank on the fallback path.

## Verification

- Canonical build + all suites; parser unit tests for new move types;
  screenshot check ghost shells + marker visible.
