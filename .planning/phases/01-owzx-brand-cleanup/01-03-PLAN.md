---
phase: 01-owzx-brand-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/cli/CliRunner.cpp
  - src/cli/main_cli.cpp
  - src/core/services/AuxiliaryService.cpp
  - src/core/services/CloudServiceMock.cpp
  - src/qml_gui/BackendContext.cpp
  - src/qml_gui/main_qml.cpp
  - src/MainWindow.cpp
  - cmake/BuildLibslic3r.cmake
  - cmake/BuildLibslic3rFromSource.cmake
  - cmake/buildinfo.h.in.stub
  - i18n/zh_CN.ts
  - i18n/en.ts
  - i18n/de.ts
  - i18n/fr.ts
  - i18n/ja.ts
  - i18n/ko.ts
  - CLAUDE.md
  - README.md
  - docs/CrealityPrint_Qt_GUI重写架构.md
  - scripts/verify_brand_cleanup.ps1
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-04
  - BRAND-05

must_haves:
  truths:
    - "C++ source brand strings (CLI runner, services, BackendContext, MainWindow) use OWzx"
    - "Env var names CREALITY_OPENGL and CREALITY_VISUAL_COMPARE_MODE renamed to OWZX_*"
    - "QSettings org/app name is OWzx/OWzxSlicer (no CrealityDemo/Print7Shell)"
    - "CMake build info version aligned to OrcaSlicer 2.4.0-dev (no v7.0.1 references)"
    - "cmake/BuildLibslic3r.cmake paths point to third_party/OrcaSlicer (no CrealityPrint)"
    - "third_party/CrealityPrint/ directory physically removed from disk"
    - "i18n .ts files regenerated via lupdate with new OWzx source strings"
    - "Docs/skills/CLAUDE.md updated to reference OrcaSlicer upstream and OWzx brand"
    - "scripts/verify_brand_cleanup.ps1 created — runs all VALIDATION.md grep assertions and exits 0"
    - "scripts/auto_verify_with_vcvars.ps1 (full clean build + smoke) passes — authoritative gate"
  artifacts:
    - path: "src/cli/main_cli.cpp"
      provides: "CLI app name/version with OWzx brand"
      contains: "owzx-cli"
    - path: "src/qml_gui/main_qml.cpp"
      provides: "OWzx QSettings org/app + OWZX_OPENGL env var"
      contains: "OWzx"
    - path: "cmake/BuildLibslic3r.cmake"
      provides: "Build path third_party/OrcaSlicer"
      contains: "third_party/OrcaSlicer"
    - path: "scripts/verify_brand_cleanup.ps1"
      provides: "Brand cleanup grep assertion aggregator"
      contains: "creality"
  key_links:
    - from: "scripts/verify_brand_cleanup.ps1"
      to: "VALIDATION.md rows 1-01-01 through 1-01-06"
      via: "Aggregated grep assertions"
      pattern: "exit 0"
    - from: "cmake/buildinfo.h.in.stub"
      to: "build/buildinfo.h (generated)"
      via: "CMake configure_file"
      pattern: "OWZX_VERSION"
---

<objective>
Complete brand cleanup: C++ source strings, env var names, QSettings, version alignment, submodule physical removal, i18n regeneration, docs/skills update, and create the brand-cleanup verification script that aggregates all VALIDATION.md grep assertions.

Purpose: BRAND-01 remainder (C++ strings + QSettings), BRAND-02 (submodule removal + cmake path references), BRAND-04 (resource/config cleanup), BRAND-05 (version alignment to OrcaSlicer main branch). This is the final wave and MUST run `scripts/auto_verify_with_vcvars.ps1` as the authoritative phase gate.

Output: ~20 files updated; `third_party/CrealityPrint/` removed; `scripts/verify_brand_cleanup.ps1` exits 0; authoritative build passes.
</objective>

<execution_context>
@.planning/phases/01-owzx-brand-cleanup/01-VALIDATION.md
@.planning/phases/01-owzx-brand-cleanup/01-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: C++ brand strings + env vars + QSettings + CMake paths + submodule removal</name>
  <files>src/cli/CliRunner.cpp, src/cli/main_cli.cpp, src/core/services/AuxiliaryService.cpp, src/core/services/CloudServiceMock.cpp, src/qml_gui/BackendContext.cpp, src/qml_gui/main_qml.cpp, src/MainWindow.cpp, cmake/BuildLibslic3r.cmake, cmake/BuildLibslic3rFromSource.cmake</files>
  <action>
Apply per RESEARCH Category 1 (C++ section) and Category 4 (path references), plus the QSettings rename:

C++ brand strings:
- src/cli/CliRunner.cpp: line 52 `"CrealityPrint CLI — headless slicing pipeline"` → `"OWzx CLI — headless slicing pipeline"`; line 152 `"Usage: creality-cli [options]"` → `"Usage: owzx-cli [options]"`
- src/cli/main_cli.cpp: line 7 `setApplicationName("creality-cli")` → `setApplicationName("owzx-cli")`; line 8 `setApplicationVersion("1.0.0")` → `setApplicationVersion("2.4.0-dev")` (BRAND-05)
- src/core/services/AuxiliaryService.cpp: line 14 `"/creality_auxiliary"` → `"/owzx_auxiliary"`
- src/core/services/CloudServiceMock.cpp: line 45 `"@creality.com"` → `"@owzx.com"`
- src/qml_gui/BackendContext.cpp: line 58 `qgetenv("CREALITY_VISUAL_COMPARE_MODE")` → `qgetenv("OWZX_VISUAL_COMPARE_MODE")`
- src/qml_gui/main_qml.cpp: line 83 `qEnvironmentVariableIsSet("CREALITY_OPENGL")` → `qEnvironmentVariableIsSet("OWZX_OPENGL")`; line 103 `setOrganizationName("CrealityDemo")` → `setOrganizationName("OWzx")`; line 104 `setApplicationName("Print7Shell")` → `setApplicationName("OWzxSlicer")`

QSettings migration (RESOLVED OPEN QUESTION Q1: accept loss — pre-release, no real users):
- src/MainWindow.cpp: line 198 `QSettings("CrealityDemo", "Print7Shell")` → `QSettings("OWzx", "OWzxSlicer")`; line 214 same rename.
- Document in commit message: "Existing user preferences stored under HKCU\Software\CrealityDemo\Print7Shell will be orphaned after this change (pre-release, accepted)."

CMake path references (Category 4):
- cmake/BuildLibslic3r.cmake: line 6 `third_party/CrealityPrint/src` → `third_party/OrcaSlicer/src`; line 11 `third_party/CrealityPrint/` → `third_party/OrcaSlicer/`
- cmake/BuildLibslic3rFromSource.cmake: line 6 `third_party/CrealityPrint/src/libslic3r/CMakeLists.txt` → `third_party/OrcaSlicer/src/libslic3r/CMakeLists.txt`

Submodule physical removal (BRAND-02):
1. Run `git ls-tree HEAD third_party/CrealityPrint` to check whether it's a tracked directory or submodule entry.
2. If submodule entry exists: `git submodule deinit -f third_party/CrealityPrint` (may be no-op per RESEARCH Submodule Status).
3. `git rm -rf third_party/CrealityPrint` to remove from git tracking.
4. If physical directory `third_party/CrealityPrint/` remains on disk after git rm, delete it manually: `rm -rf third_party/CrealityPrint`.
5. Verify `.gitmodules` contains only `third_party/OrcaSlicer` (RESEARCH already confirmed it's clean — sanity check only).

AVOID:
- Do NOT modify `src/core/services/PresetServiceMock.cpp` preset paths/names (lines 45, 65, 86, 105, 124, 210-234) — these are upstream vendor names ("Creality K1C", "Creality Ender-3", `Creality.json`, `Creality/` vendor subdir).
- Do NOT modify `src/qml_gui/Models/PresetListModel.cpp`, `src/core/viewmodels/ModelMallViewModel.cpp` (author "CrealityDesign"), `src/pages/PreparePage.cpp`, `src/pages/PreviewPage.cpp` vendor preset names.
- Do NOT modify third_party/ contents.
  </action>
  <verify>
    <automated>grep -rn "creality" src/ cmake/ --exclude-dir=third_party -i | grep -v "@Creality\|Creality K1\|Creality Ender\|Creality Generic\|Creality CR-10\|CrealityDesign\|Creality/\|Creality\.json\|fdm_creality\|Creality vendor" | grep -v "^src/core/services/PresetServiceMock" | grep -v "^src/qml_gui/Models/PresetListModel" | grep -v "^src/core/viewmodels/ModelMallViewModel" | grep -v "^src/pages/" && echo "WARN: residual C++ brand strings found" || echo "OK: no residual brand strings"</automated>
  </verify>
  <done>C++ source contains no Creality brand strings outside the upstream vendor name carve-outs; cmake path references point to third_party/OrcaSlicer; third_party/CrealityPrint/ directory no longer exists on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Version alignment + i18n regen + docs/CLAUDE.md update + brand cleanup verification script</name>
  <files>cmake/buildinfo.h.in.stub, i18n/zh_CN.ts, i18n/en.ts, i18n/de.ts, i18n/fr.ts, i18n/ja.ts, i18n/ko.ts, CLAUDE.md, README.md, docs/CrealityPrint_Qt_GUI重写架构.md, scripts/verify_brand_cleanup.ps1</files>
  <action>
BRAND-05 version alignment:
- cmake/buildinfo.h.in.stub: introduce OWZX_VERSION "2.4.0-dev" constant (mirror the CREALITYPRINT_VERSION pattern from BuildLibslic3rFromSource.cmake lines 55-65 — set OWZX_VERSION defaults "2.4.0-dev", OWZX_VERSION_MAJOR "2", OWZX_VERSION_MINOR "4", OWZX_VERSION_PATCH "0"). Verify AboutDialog and HomePage already reference 2.4.0-dev from plan 01 Task 1; this CMake-side constant makes the version single-sourced going forward.
- If AboutDialog/HomePage hardcode "2.4.0-dev" rather than reading from buildinfo, that's acceptable for v2.0 (single source refactor can be deferred — note in commit). The key deliverable is: no "v7.0.1" / "7.0.0" / "CrealityPrint v7.0.1" version strings remain anywhere in src/.

i18n regeneration (RESEARCH i18n section):
- Run `lupdate src/qml_gui -ts i18n/zh_CN.ts i18n/en.ts i18n/de.ts i18n/fr.ts i18n/ja.ts i18n/ko.ts` to regenerate source strings. This pulls in the new OWzx strings from plan 01 Task 1/2 and this plan's Task 1.
- Manually edit i18n/zh_CN.ts: replace `<name>CrealityPrint</name>` (translator context comment) → `<name>OWzx</name>`.
- Do NOT touch `@Creality K1C` preset name translations in any .ts file (upstream vendor name carve-out).

Docs/CLAUDE.md (RESOLVED OPEN QUESTION Q2: update path references in-place; defer file rename):
- CLAUDE.md: update any `third_party/CrealityPrint` references → `third_party/OrcaSlicer`; update brand mentions "OWzx Slicer" where appropriate. Verify the existing CLAUDE.md content (already mentions OWzx Slicer / OrcaSlicer — this is mostly a verification pass).
- README.md: same — `third_party/CrealityPrint` → `third_party/OrcaSlicer`; brand name OWzx.
- docs/CrealityPrint_Qt_GUI重写架构.md: path-reference update pass — replace `third_party/CrealityPrint` occurrences with `third_party/OrcaSlicer` throughout (~50 references per RESEARCH). Do NOT rename the file in v2.0 (renaming historical architecture docs mid-milestone risks breaking existing cross-references; defer to v2.1). Add a one-line header note: "注：本文档沿用 CrealityPrint 时代命名，路径已更新到 third_party/OrcaSlicer。"

Create brand cleanup verification script:
- scripts/verify_brand_cleanup.ps1: aggregate all VALIDATION.md row 1-01-01 through 1-01-06 grep assertions. Script structure:
  1. Define array of (name, command, expected_empty: bool) tuples mirroring VALIDATION.md.
  2. Run each, capture exit code / output presence.
  3. Exit 0 if all pass; exit 1 with summary table if any fail.
  4. Include carve-out filters exactly as specified in VALIDATION.md (vendor names, third_party/, archive dir).
- The script MUST call `scripts/auto_verify_with_vcvars.ps1` as its final step (or be invoked after that build passes — design as: standalone grep gate first, then advise running auto_verify separately if user wants combined run). Per VALIDATION.md row 1-01-07, the authoritative build is the phase gate.

AVOID:
- Do NOT rename `docs/CrealityPrint_Qt_GUI重写架构.md` filename (defer to v2.1 per Q2 resolution).
- Do NOT modify `.planning-v1-crealityprint-archive/` (historical archive carve-out).
- Do NOT modify `tests/data/` contents (test fixtures preserved).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/verify_brand_cleanup.ps1</automated>
  </verify>
  <done>scripts/verify_brand_cleanup.ps1 exits 0 with all 6 grep assertions passing (rows 1-01-01 through 1-01-06). Then run the authoritative build: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` and confirm full clean build + smoke test passes (row 1-01-07). Output binary is build/OWzxSlicer.exe.</done>
</task>

</tasks>

<verification>
Phase gate (run after both tasks complete):
1. `powershell -ExecutionPolicy Bypass -File scripts/verify_brand_cleanup.ps1` — exits 0 (all grep assertions pass).
2. `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` — authoritative full build + smoke test passes.
3. VALIDATION.md rows 1-01-01 through 1-01-07 all green.

Open questions resolved (documented in plan, no checkpoint needed):
- Q1 (QSettings loss): ACCEPT LOSS — pre-release, no real users. Documented in Task 1 commit.
- Q2 (docs file rename): DEFER to v2.1 — path references updated in-place, header note added.
- Q3 (FramelessDialogDemo → OWzxSlicer): RENAMED per research recommendation. Handled in plan 02 Task 1.

Manual verification (per VALIDATION.md "Manual-Only Verifications" — listed for end-of-phase review, not blocking task completion):
- Launch app, verify window title bar shows "OWzx Slicer"
- Open About dialog, verify brand + version "2.4.0-dev"
- Open shortcut dialog (Ctrl+?), verify no Creality strings
</verification>

<success_criteria>
- All VALIDATION.md grep assertions (rows 1-01-01 through 1-01-06) return empty
- scripts/auto_verify_with_vcvars.ps1 passes (full clean build + smoke test)
- third_party/CrealityPrint/ physically removed from disk
- C++ source has zero Creality brand strings (vendor names preserved)
- QSettings org/app is OWzx/OWzxSlicer
- Env vars renamed to OWZX_OPENGL / OWZX_VISUAL_COMPARE_MODE
- cmake paths reference third_party/OrcaSlicer exclusively
- Version strings use 2.4.0-dev (no v7.0.1 references)
- i18n .ts files regenerated with OWzx source strings
- CLAUDE.md / README.md / docs/ reference OrcaSlicer upstream
- scripts/verify_brand_cleanup.ps1 exists and exits 0
</success_criteria>

<output>
Create `.planning/phases/01-owzx-brand-cleanup/01-03-SUMMARY.md` when done.
</output>
