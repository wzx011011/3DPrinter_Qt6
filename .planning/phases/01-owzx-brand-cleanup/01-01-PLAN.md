---
phase: 01-owzx-brand-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/main.qml
  - src/qml_gui/dialogs/AboutDialog.qml
  - src/qml_gui/pages/HomePage.qml
  - src/qml_gui/pages/PreferencesPage.qml
  - src/qml_gui/pages/ModelMallPage.qml
  - src/qml_gui/dialogs/PrintHostDialog.qml
  - src/qml_gui/dialogs/ConfigWizardDialog.qml
  - src/qml_gui/data/hints.json
autonomous: true
requirements:
  - BRAND-01
  - BRAND-04

must_haves:
  truths:
    - "Window title, About dialog, HomePage, PreferencesPage display 'OWzx' brand with no 'Creality' strings"
    - "ModelMallPage and PrintHostDialog reference OrcaSlicer (not Creality Cloud)"
    - "hints.json hint text uses 'OWzx' not 'CrealityPrint'"
    - "Incremental build still compiles after string edits"
  artifacts:
    - path: "src/qml_gui/main.qml"
      provides: "Window title 'OWzx Slicer', OWzx About dialog content"
      contains: "OWzx Slicer"
    - path: "src/qml_gui/dialogs/AboutDialog.qml"
      provides: "About dialog with OWzx brand and OrcaSlicer upstream attribution"
      contains: "关于 OWzx"
    - path: "src/qml_gui/pages/HomePage.qml"
      provides: "HomePage brand banner + login label + version footer"
      contains: "OWzx Slicer"
    - path: "src/qml_gui/data/hints.json"
      provides: "Hint text with OWzx brand"
      contains: "OWzx"
  key_links:
    - from: "src/qml_gui/main.qml"
      to: "src/qml_gui/dialogs/AboutDialog.qml"
      via: "About dialog instantiation"
      pattern: "AboutDialog"
---

<objective>
Replace all user-visible "Creality Print" / "CrealityPrint" brand strings in QML UI and hint resources with "OWzx" branding, aligned to OrcaSlicer upstream.

Purpose: BRAND-01 requires window title, About dialog, shortcut dialog, HomePage, PreferencesPage, and other user-facing strings to use OWzx. BRAND-04 requires hint/resource file brand cleanup. This is the lowest-risk wave (pure string replacement in QML/JSON) and runs first so subsequent waves have a stable text baseline.

Output: Eight files with all brand strings migrated to OWzx; incremental compile passes.
</objective>

<execution_context>
@.planning/phases/01-owzx-brand-cleanup/01-VALIDATION.md
@.planning/phases/01-owzx-brand-cleanup/01-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- No C++ interfaces in this plan — pure QML/JSON string replacement. -->
<!-- Executor uses the String Residue Inventory in 01-RESEARCH.md (Category 1, ~20 hits across 8 files). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace brand strings in main.qml, AboutDialog, HomePage, PreferencesPage</name>
  <files>src/qml_gui/main.qml, src/qml_gui/dialogs/AboutDialog.qml, src/qml_gui/pages/HomePage.qml, src/qml_gui/pages/PreferencesPage.qml</files>
  <action>
Replace per BRAND-01 using 01-RESEARCH.md "Category 1: Brand Strings" inventory:

src/qml_gui/main.qml:
- Line 16: title `"Creality Print 7.0 - QML"` → `"OWzx Slicer"` (version suffix removed — BRAND-05 covers version separately)
- Line 278 and 406: `qsTr("关于 Creality Print")` → `qsTr("关于 OWzx")`
- Line 418: `"Creality Print 7.0 - QML"` → `"OWzx Slicer"`
- Line 424: `qsTr("基于 CrealityPrint v7.0.1 开源版本")` → `qsTr("基于 OrcaSlicer 开源版本")` (upstream attribution; version handled in BRAND-05)

src/qml_gui/dialogs/AboutDialog.qml:
- Line 12: `qsTr("关于 Creality Print")` → `qsTr("关于 OWzx")`
- Line 40: `"Creality Print"` → `"OWzx Slicer"`
- Line 46: `qsTr("版本 7.0.0.0  (Qt6 QML 重写)")` → `qsTr("版本 2.4.0-dev  (Qt6 QML)")` (aligned to OrcaSlicer version.inc)
- Line 73: `value: "www.creality.com"` → `value: "www.orcaslicer.org"` (upstream official URL)

src/qml_gui/pages/HomePage.qml:
- Line 63: `"Creality Print 7.0"` → `"OWzx Slicer"`
- Line 276 and 287: `qsTr("登录 Creality 账号")` → `qsTr("登录 OWzx 账号")`
- Line 520: `qsTr("版本 7.0.0  |  Qt 6.10  |  ©2026 Creality")` → `qsTr("版本 2.4.0-dev  |  Qt 6.10  |  OWzx")`

src/qml_gui/pages/PreferencesPage.qml:
- Line 582: `qsTr("上游基线：CrealityPrint v7.0.1 (0d4ac73)")` → `qsTr("上游基线：OrcaSlicer main branch")` (commit hash removed — OrcaSlicer main is rolling)
- Line 636: `qsTr("...连接 Creality 更新服务器...")` → `qsTr("...连接更新服务器...")` (no brand prefix in updater text)

AVOID:
- Do NOT replace the strings `"Creality K1C"`, `"Creality Ender"`, `"Creality Generic"` etc. — those are upstream vendor preset names (RESEARCH Category 5).
- Do NOT modify third_party/ contents.
- Do NOT touch historical comments referencing "CrealityPrint" as upstream project name (RESEARCH Category 3) — those are preserved per ROADMAP success criteria.
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -20</automated>
  </verify>
  <done>grep on the 4 files shows zero remaining `Creality Print`, `CrealityPrint v7.0.1`, or `Creality` brand strings (excluding vendor preset names); incremental build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Replace brand strings in ModelMallPage, PrintHostDialog, ConfigWizardDialog, hints.json</name>
  <files>src/qml_gui/pages/ModelMallPage.qml, src/qml_gui/dialogs/PrintHostDialog.qml, src/qml_gui/dialogs/ConfigWizardDialog.qml, src/qml_gui/data/hints.json</files>
  <action>
Replace per BRAND-01 and BRAND-04:

src/qml_gui/pages/ModelMallPage.qml:
- Line 756: `qsTr("Powered by Creality Cloud")` → `qsTr("Powered by OrcaSlicer Cloud")`

src/qml_gui/dialogs/PrintHostDialog.qml:
- Line 23: `presetName: "Creality CR-10 SE"` → `presetName: "OrcaSlicer Default"` (placeholder demo preset, not a vendor name)
- Line 24: `hostType: "Creality Cloud"` → `hostType: "OrcaSlicer Cloud"`
- Line 25: `hostUrl: "https://api.creality.com"` → `hostUrl: "https://api.orcaslicer.org"`
- Line 69: `"Creality Cloud"` (in ListModel) → `"OrcaSlicer Cloud"`

src/qml_gui/dialogs/ConfigWizardDialog.qml:
- Line 118 contains `"Creality K1C 0.4 nozzle"` — PRESERVE (upstream vendor preset name, RESEARCH Category 5). No change to this file unless other Creality brand strings exist. If grep finds only vendor preset names, skip the file and note in commit.

src/qml_gui/data/hints.json (BRAND-04):
- Line 88: `"CrealityPrint 支持腔体温度设置..."` → `"OWzx 支持腔体温度设置..."`
- Line 94: `"在 CrealityPrint 中可以使用内置校准..."` → `"可以使用内置校准..."` (rephrase to avoid awkward brand prefix)
- Line 100: `"CrealityPrint 支持辅助部件冷却风扇..."` → `"OWzx 支持辅助部件冷却风扇..."`
- Line 106: `"CrealityPrint 支持空气净化..."` → `"OWzx 支持空气净化..."`

AVOID: vendor preset names, third_party/, and historical comments.
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -20 && grep -ri "creality" src/qml_gui/pages/ModelMallPage.qml src/qml_gui/dialogs/PrintHostDialog.qml src/qml_gui/dialogs/ConfigWizardDialog.qml src/qml_gui/data/hints.json | grep -v "Creality K1\|Creality Ender\|Creality Generic\|Creality CR-10\|CrealityDesign\|fdm_creality" && echo "WARN: residual brand strings found" || echo "OK: no residual brand strings"</automated>
  </verify>
  <done>The 4 files contain no "Creality" brand strings outside the upstream vendor preset carve-outs; incremental build succeeds.</done>
</task>

</tasks>

<verification>
Task 1: Incremental build passes after QML string edits (QML strings don't require lupdate for build, only for i18n regeneration which is a later wave).
Task 2: Same; plus grep assertion on the 4 files returns no brand strings outside carve-outs.

Note: Full phase grep assertion suite (VALIDATION.md rows 1-01-01 through 1-01-05) is run by the final wave (plan 03). This plan's tasks run only incremental build to catch breakage fast.
</verification>

<success_criteria>
- main.qml title is "OWzx Slicer"
- AboutDialog shows "关于 OWzx" + version "2.4.0-dev"
- HomePage banner, login label, version footer all say OWzx
- PreferencesPage upstream baseline references OrcaSlicer main branch
- ModelMallPage and PrintHostDialog reference OrcaSlicer Cloud
- hints.json uses OWzx (or neutral rephrasing)
- ConfigWizardDialog vendor preset names preserved unchanged
- Incremental build passes
</success_criteria>

<output>
Create `.planning/phases/01-owzx-brand-cleanup/01-01-SUMMARY.md` when done.
</output>
