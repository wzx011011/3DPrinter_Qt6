---
phase: 01-owzx-brand-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - CMakeLists.txt
  - scripts/auto_verify_with_vcvars.ps1
  - scripts/quick_build.ps1
  - scripts/quick_run.ps1
  - scripts/quick_test.ps1
  - scripts/smoke_test.ps1
  - scripts/capture_qml_warnings.ps1
  - tests/CliTests.cpp
  - tests/ViewModelSmokeTests.cpp
  - tests/E2EWorkflowTests.cpp
  - src/core/rendering/SupportPaintTypes.h
  - src/core/rendering/TickCodeTypes.h
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - src/core/viewmodels/PreviewViewModel.h
  - src/core/viewmodels/PreviewViewModel.cpp
  - src/qml_gui/main_qml.cpp
  - src/qml_gui/pages/PreparePage.qml
  - src/qml_gui/pages/PreviewPage.qml
  - src/qml_gui/Renderer/GLViewport.h
autonomous: true
requirements:
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "CMake project name is OWzxSlicer, executable target outputs OWzxSlicer.exe"
    - "CMake option OWZX_QML_GUI replaces CREALITY_QML_GUI across CMakeLists.txt, cmake/*.cmake, and scripts"
    - "Library targets owzx_app_core and owzx_cli_core replace creality_app_core / creality_cli_core"
    - "CLI executable owzx-cli replaces creality-cli (target + binary + script references)"
    - "namespace Crality3D renamed to namespace OWzx across headers + qualified references"
    - "QML module CrealityGL renamed to OWzxGL (registration + both import statements)"
    - "Tests build and link against owzx_app_core / owzx_cli_core (no dangling target names)"
    - "Incremental build succeeds with the renamed targets"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Project name OWzxSlicer, OWZX_QML_GUI option, owzx_app_core/owzx_cli_core/owzx-cli targets"
      contains: "project(OWzxSlicer"
    - path: "scripts/auto_verify_with_vcvars.ps1"
      provides: "Build script using new target names and OWZX_QML_GUI flag"
      contains: "OWZX_QML_GUI"
    - path: "src/core/rendering/SupportPaintTypes.h"
      provides: "namespace OWzx declaration"
      contains: "namespace OWzx"
    - path: "src/qml_gui/main_qml.cpp"
      provides: "OWzxGL QML module registration"
      contains: "OWzxGL"
  key_links:
    - from: "CMakeLists.txt"
      to: "tests/ViewModelSmokeTests.cpp"
      via: "target_link_libraries(ViewModelSmokeTests PRIVATE owzx_app_core ...)"
      pattern: "owzx_app_core"
    - from: "src/qml_gui/main_qml.cpp"
      to: "src/qml_gui/pages/PreparePage.qml"
      via: "QML import OWzxGL 1.0"
      pattern: "import OWzxGL"
---

<objective>
Rename all CMake targets/options and migrate internal namespaces/QML module from Creality identifiers to OWzx.

Purpose: BRAND-02 requires removing all CMake-level Creality identifiers (target/option names, executable names). BRAND-03 requires migrating `Crality3D` namespace to `OWzx` and `CrealityGL` QML module to `OWzxGL`. This is the highest-risk wave because CMake target rename ripples through test link directives and PowerShell scripts — must be done atomically in a single plan to avoid broken intermediate state.

Output: 21 files updated; `cmake --build build --config Release` succeeds; `grep -ri "Crality3D\|CrealityGL\|creality_app_core\|creality-cli\|CREALITY_QML_GUI" src/ cmake/ tests/ scripts/ CMakeLists.txt` returns empty (excluding third_party/ and build/).
</objective>

<execution_context>
@.planning/phases/01-owzx-brand-cleanup/01-VALIDATION.md
@.planning/phases/01-owzx-brand-cleanup/01-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Verified CMake target references in CMakeLists.txt (line numbers from grep): -->
<!-- creality_app_core: lines 69 (add_library), 135, 141, 145, 147, 149, 158, 162, 170, 229, 238 -->
<!-- creality_cli_core: lines 250 (add_library), 265, 269, 273, 279, 284 -->
<!-- creality-cli: lines 30 (option text), 283 (add_executable), 284 (link), 294 (add_dependencies) -->
<!-- FramelessDialogDemo: lines 24 (project), 166/199 (qt_add_executable), 170/171/175/176/180/183/190/193/215/219 (links/options/post-build), 304 (qt_add_translations) -->
<!-- CREALITY_QML_GUI: lines 26 (option), 53 (if), 55 (warning msg), 145 (compile def), 223 (if BUILD_TESTING) -->
<!-- Test files link creality_app_core: ViewModelSmokeTests line 238, E2EWorkflowTests line 229 -->
<!-- scripts/auto_verify_with_vcvars.ps1 references: '-DCREALITY_QML_GUI=ON' (line 33), 'FramelessDialogDemo' lines 19/56/66/78/118, 'creality-cli' line 72 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename CMake targets, options, and executable name atomically</name>
  <files>CMakeLists.txt, scripts/auto_verify_with_vcvars.ps1, scripts/quick_build.ps1, scripts/quick_run.ps1, scripts/quick_test.ps1, scripts/smoke_test.ps1, scripts/capture_qml_warnings.ps1, tests/CliTests.cpp, tests/ViewModelSmokeTests.cpp, tests/E2EWorkflowTests.cpp</files>
  <action>
ATOMIC rename across CMakeLists.txt, scripts, and test code. Apply ALL replacements in this task before any build attempt — a partial state breaks configure.

CMakeLists.txt — rename these identifiers globally (use the verified line map in <interfaces>):
- `project(FramelessDialogDemo LANGUAGES C CXX)` (line 24) → `project(OWzxSlicer LANGUAGES C CXX)`
- `option(CREALITY_QML_GUI ...)` (line 26) → `option(OWZX_QML_GUI ...)`
- `option(BUILD_CLI "... creality-cli ...)"` (line 30) → `option(BUILD_CLI "... owzx-cli ...)"`
- Every `CREALITY_QML_GUI` reference (lines 53, 55, 145, 223) → `OWZX_QML_GUI`
- Every `creality_app_core` (lines 69, 135, 141, 145, 147, 149, 158, 162, 170, 229, 238) → `owzx_app_core`
- Every `creality_cli_core` (lines 250, 265, 269, 273, 279, 284) → `owzx_cli_core`
- Every `creality-cli` (lines 283, 284, 294) → `owzx-cli`
- Every `FramelessDialogDemo` (lines 166, 170, 171, 175, 176, 180, 183, 190, 193, 199, 215, 219, 304) → `OWzxSlicer`

Note: The compile-definition line 145 `target_compile_definitions(creality_app_core PUBLIC CREALITY_QML_GUI=1)` becomes `target_compile_definitions(owzx_app_core PUBLIC OWZX_QML_GUI=1)`. The C++ source code uses `#ifdef CREALITY_QML_GUI` — Task 2 of this plan updates those C++ guards. This is why both tasks live in the same plan: the option name AND the C++ guard must change together.

scripts/auto_verify_with_vcvars.ps1:
- Line 19: `Stop-Process -Name 'FramelessDialogDemo'` → `-Name 'OWzxSlicer'`
- Line 33: `'-DCREALITY_QML_GUI=ON'` → `'-DOWZX_QML_GUI=ON'`
- Line 56: `-Name 'FramelessDialogDemo'` → `-Name 'OWzxSlicer'`
- Line 66: `ninja -j16 FramelessDialogDemo.exe` → `ninja -j16 OWzxSlicer.exe`
- Line 72: `ninja -j16 creality-cli 2>$null` → `ninja -j16 owzx-cli 2>$null`
- Line 78: `--no-opengl-sw './FramelessDialogDemo.exe'` → `'./OWzxSlicer.exe'`
- Line 118: `Start-Process -FilePath './FramelessDialogDemo.exe'` → `'./OWzxSlicer.exe'`

scripts/quick_build.ps1, scripts/quick_run.ps1, scripts/quick_test.ps1, scripts/smoke_test.ps1, scripts/capture_qml_warnings.ps1:
- Grep each for `FramelessDialogDemo`, `creality-cli`, `CREALITY_QML_GUI`, `creality_app_core` and rename to OWzxSlicer / owzx-cli / OWZX_QML_GUI / owzx_app_core respectively. Apply only the exact matches found (do not invent new references).

tests/CliTests.cpp (per RESEARCH Category 4):
- Line 20: `QStringLiteral("/creality-cli")` → `QStringLiteral("/owzx-cli")`
- Line 118: `"creality_models/Block20XY.stl"` → `"test_models/Block20XY.stl"` (test data path cleanup; not strictly brand but matches the same rename pass)

tests/ViewModelSmokeTests.cpp and tests/E2EWorkflowTests.cpp:
- These link against `creality_app_core` via CMakeLists.txt lines 229/238 — those CMake lines are renamed in this same task, so the .cpp files themselves need no change unless they reference the target name in `#pragma comment(lib, ...)` or similar. Grep each for `creality_app_core`; if no direct references exist in the .cpp, leave the file alone (the rename is at the CMake level only). Record findings in commit message.

AVOID:
- Do NOT add backward-compat aliases (no `add_library(creality_app_core ALIAS owzx_app_core)`) — Phase 1 is a clean break per research Open Question #3 recommendation.
- Do NOT modify `cmake/BuildLibslic3rFromSource.cmake` CREALITYPRINT_VERSION in this task — that's a version var, handled in plan 03 (BRAND-05). Skip lines 55-65 of that file in this task.
  </action>
  <verify>
    <automated>grep -rn "CREALITY_QML_GUI\|creality_app_core\|creality_cli_core\|creality-cli\|FramelessDialogDemo" CMakeLists.txt scripts/ tests/ 2>&1 | grep -v "^Binary" | grep -v ".planning" | grep -v "build/" || echo "OK: no residual Creality CMake identifiers"</automated>
  </verify>
  <done>grep returns empty for all renamed identifiers in CMakeLists.txt, scripts/, tests/; cmake configure is not yet attempted (that happens after Task 2 to avoid intermediate broken state with C++ guards still using old name).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Crality3D namespace to OWzx + CrealityGL QML module to OWzxGL + C++ guard rename</name>
  <files>src/core/rendering/SupportPaintTypes.h, src/core/rendering/TickCodeTypes.h, src/core/viewmodels/EditorViewModel.h, src/core/viewmodels/EditorViewModel.cpp, src/core/viewmodels/PreviewViewModel.h, src/core/viewmodels/PreviewViewModel.cpp, src/qml_gui/main_qml.cpp, src/qml_gui/pages/PreparePage.qml, src/qml_gui/pages/PreviewPage.qml, src/qml_gui/Renderer/GLViewport.h</files>
  <action>
Apply per RESEARCH "Namespace Identifiers" + "Category 2":

Namespace Crality3D → OWzx (atomic across declarations and qualified refs):
- src/core/rendering/SupportPaintTypes.h: line 5 `namespace Crality3D {` → `namespace OWzx {`; line 162 `} // namespace Crality3D` → `} // namespace OWzx`
- src/core/rendering/TickCodeTypes.h: line 5 `namespace Crality3D {` → `namespace OWzx {`; line 26 `} // namespace Crality3D` → `} // namespace OWzx`
- src/core/viewmodels/EditorViewModel.h: line 737 `QList<Crality3D::ObjectPaintData>` → `QList<OWzx::ObjectPaintData>`
- src/core/viewmodels/EditorViewModel.cpp: lines 539, 549, 555, 558 — every `Crality3D::` → `OWzx::`
- src/core/viewmodels/PreviewViewModel.h: line 222 `QList<Crality3D::TickCode>` → `QList<OWzx::TickCode>`
- src/core/viewmodels/PreviewViewModel.cpp: lines 1098, 1100, 1114, 1116, 1154, 1156 — every `Crality3D::` → `OWzx::`

QML module CrealityGL → OWzxGL:
- src/qml_gui/main_qml.cpp: line 112 `qmlRegisterType<GLViewport>("CrealityGL", 1, 0, "GLViewport")` → `qmlRegisterType<GLViewport>("OWzxGL", 1, 0, "GLViewport")`
- src/qml_gui/pages/PreparePage.qml: line 10 `import CrealityGL 1.0` → `import OWzxGL 1.0`
- src/qml_gui/pages/PreviewPage.qml: line 4 `import CrealityGL 1.0` → `import OWzxGL 1.0`
- src/qml_gui/Renderer/GLViewport.h: line 10 comment `CrealityGL 1.0` → `OWzxGL 1.0`

C++ guard rename CREALITY_QML_GUI → OWZX_QML_GUI (required because Task 1 renamed the compile definition):
- Grep all of src/ for `#ifdef CREALITY_QML_GUI`, `#ifndef CREALITY_QML_GUI`, `#if defined(CREALITY_QML_GUI)`, and any `CREALITY_QML_GUI` literal — replace each with `OWZX_QML_GUI`. Expected locations: services/viewmodels with `#ifdef HAS_LIBSLIC3R` neighbors. Do an exhaustive grep; do not assume the list.

AVOID:
- Do NOT rename files (no file has Crality/Creality in its filename — research confirmed).
- Do NOT modify third_party/.
- Do NOT add `namespace Crality3D = OWzx;` alias (hard rename per research).
  </action>
  <verify>
    <automated>grep -rn "Crality3D\|CrealityGL\|CREALITY_QML_GUI" src/ --exclude-dir=third_party 2>&1 || echo "OK: no residual namespace/module/guard identifiers"</automated>
  </verify>
  <done>grep on src/ (excluding third_party/) returns zero matches for Crality3D, CrealityGL, or CREALITY_QML_GUI. Now build can be attempted: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` passes (full clean configure because CMake target names changed). If clean configure is impractical due to time, fall back to `rm -rf build/CMakeCache.txt build/CMakeFiles && cmake --build build --config Release` to force reconfigure without full clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMake configure → Build | Renamed target/option identifiers cross the configure→generate→build boundary; a partial rename causes configure failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | CMake target rename atomicity | mitigate | Both tasks of this plan execute atomically; no intermediate build between Task 1 (CMake rename) and Task 2 (C++ guard rename). CREALITY_QML_GUI option AND the matching #ifdef guard change together. |
| T-01-02 | Denial of Service | Build script references to old binary name | mitigate | Task 1 sweeps all 6 PowerShell scripts; grep assertion post-task confirms zero residuals |
| T-01-SC | Tampering | (N/A — no package installs in this phase) | accept | Brand cleanup phase installs zero external packages; slopcheck not applicable |
</threat_model>

<verification>
- After Task 1: grep returns zero residual Creality CMake identifiers in CMakeLists.txt/scripts/tests (build not yet attempted).
- After Task 2: grep returns zero residual Crality3D/CrealityGL/CREALITY_QML_GUI in src/ (excluding third_party/).
- After both tasks: `scripts/auto_verify_with_vcvars.ps1` (authoritative build) passes. Build output binary is now `build/OWzxSlicer.exe` instead of `FramelessDialogDemo.exe`.
- VALIDATION.md rows 1-01-02 (no CrealityPrint refs) and 1-01-03 (no Crality3D/CrealityGL) pass.
</verification>

<success_criteria>
- CMake project name is OWzxSlicer; executable is OWzxSlicer.exe
- CMake option is OWZX_QML_GUI (no CREALITY_QML_GUI anywhere)
- Library targets are owzx_app_core / owzx_cli_core
- CLI executable target/binary is owzx-cli
- namespace OWzx replaces Crality3D in all 6 files
- QML module OWzxGL replaces CrealityGL in registration + 2 imports + 1 comment
- All #ifdef CREALITY_QML_GUI guards migrated to OWZX_QML_GUI
- Full build via scripts/auto_verify_with_vcvars.ps1 passes
- All PowerShell build scripts use new target/binary names
</success_criteria>

<output>
Create `.planning/phases/01-owzx-brand-cleanup/01-02-SUMMARY.md` when done.
</output>
