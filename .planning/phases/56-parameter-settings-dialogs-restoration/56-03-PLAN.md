---
phase: 56-parameter-settings-dialogs-restoration
plan: 03
type: execute
wave: 3
depends_on: ['56-02']
files_modified:
  - src/qml_gui/components/OptionRow.qml
  - src/qml_gui/components/GroupNavSidebar.qml
  - src/qml_gui/dialogs/SettingsDialog.qml
  - src/qml_gui/main.qml
  - src/qml_gui/qml.qrc
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04, SETTINGS-05, SETTINGS-06]
user_setup: []

must_haves:
  truths:
    - "Three independent non-modal SettingsDialog instances (printer/material/process) can be opened from the Prepare sidebar"
    - "OptionRow dispatches by optType across all 7 typed kinds: bool/int/double/enum/string/percent + nullable + multi-value"
    - "GroupNavSidebar lists groups for the active tab + per-group dirty count, sourced from ConfigOptionModel"
    - "SettingsDialog contains preset bar, top tabs, group nav, option area, search bar, footer Save/Discard/Cancel -- all using Cx* controls only"
    - "QmlUiAuditTests confirm no raw QtQuick.Controls and qsTr() on every user-visible string in the new files"
  artifacts:
    - path: "src/qml_gui/dialogs/SettingsDialog.qml"
      provides: "ApplicationWindow non-modal parameterized settings dialog shell"
      contains: "ApplicationWindow"
    - path: "src/qml_gui/components/OptionRow.qml"
      provides: "Typed option renderer dispatching by optType"
      contains: "DoubleValidator"
    - path: "src/qml_gui/components/GroupNavSidebar.qml"
      provides: "Left option-group navigation with count + dirty badges"
      contains: "CxScrollView"
    - path: "src/qml_gui/qml.qrc"
      provides: "Registered SettingsDialog/OptionRow/GroupNavSidebar resources"
      contains: "dialogs/SettingsDialog.qml"
  key_links:
    - from: "src/qml_gui/main.qml"
      to: "backend.settingsRequested"
      via: "Connections handler that shows the correct SettingsDialog instance"
      pattern: "onSettingsRequested"
    - from: "src/qml_gui/dialogs/SettingsDialog.qml"
      to: "backend.configViewModel"
      via: "required property var configVm; reads printerOptions/filamentOptions/printOptions by presetTier"
      pattern: "configVm"
    - from: "src/qml_gui/components/OptionRow.qml"
      to: "ConfigOptionModel.setValue"
      via: "control.onChanged -> optionModel.setValue(row, v)"
      pattern: "optionModel.setValue"
---

<objective>
Wave 3 QML dialogs: build the three independent non-modal ApplicationWindow settings dialogs (printer, material, process) parameterized by a single SettingsDialog.qml shell. Extract OptionRow.qml (typed-option renderer dispatching across all 7 kinds) and GroupNavSidebar.qml (per-tab group list with dirty badges) from the existing ParamsPage.qml patterns. Register everything in qml.qrc and wire main.qml to show the correct dialog instance when backend emits settingsRequested(category).

Purpose: With the C++ backend complete (56-02), the QML layer is pure presentation + wiring. Extracting OptionRow/GroupNavSidebar keeps the dialog file lean and lets the typed-control matrix live in one place. The new dialogs live ALONGSIDE the old SettingsPage/ParamsPage/ConfigPage -- removal is Phase 57 per CONTEXT.md deferred decisions.
Output: Three working non-modal settings dialogs that render all 7 typed option kinds, drive save/reset/search through ConfigViewModel, and pass QmlUiAuditTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-CONTEXT.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-UI-SPEC.md

@src/qml_gui/components/ParamsPage.qml
@src/qml_gui/dialogs/SavePresetDialog.qml
@src/qml_gui/dialogs/UnsavedChangesDialog.qml
@src/qml_gui/controls/CxDialog.qml
@src/qml_gui/main.qml
@src/qml_gui/qml.qrc

<interfaces>
<!-- ConfigViewModel API available to QML (all Q_INVOKABLE / Q_PROPERTY, after 56-02) -->
class ConfigViewModel : public QObject {
  // Per-tier option models (already existed):
  Q_PROPERTY(QObject* machineOptions ...)        // tier="printer"
  Q_PROPERTY(QObject* filamentOptions ...)       // tier="filament"
  Q_PROPERTY(QObject* printOptions ...)          // tier="print"
  // Preset state (already existed):
  Q_PROPERTY(QStringList printerPresetNames ...)
  Q_PROPERTY(QStringList filamentPresetNames ...)
  Q_PROPERTY(QStringList printPresetNames ...)
  Q_PROPERTY(QString currentPrinterPreset ...)
  Q_PROPERTY(QString currentFilamentPreset ...)
  Q_PROPERTY(QString currentPrintPreset ...)
  Q_PROPERTY(bool isPresetDirty ...)
  Q_PROPERTY(bool currentPresetCombinationValid ...)
  Q_PROPERTY(QString currentPresetCompatibilityMessage ...)
  // Added in 56-02:
  Q_INVOKABLE void resetGroup(const QString &tier, const QString &groupName);
  Q_INVOKABLE QStringList groupNames(const QString &tier) const;
  Q_INVOKABLE int dirtyCountForGroup(const QString &tier, const QString &groupName) const;
  Q_INVOKABLE bool optNullable(const QString &tier, int index) const;
  Q_INVOKABLE bool optIsVector(const QString &tier, int index) const;
  Q_INVOKABLE QString optSidetext(const QString &tier, int index) const;
  Q_INVOKABLE QList<int> filterOptionIndices(const QString &category, const QString &searchText, bool advancedMode);
  Q_INVOKABLE bool requestSavePendingChanges();
  Q_INVOKABLE bool requestDiscardPendingChanges();
  Q_INVOKABLE void resetAllGlobalOptions();
  Q_INVOKABLE QString valueSourceForKey(const QString &key) const;
  // already existed:
  Q_INVOKABLE void setActivePresetTier(const QString &tier);
  Q_INVOKABLE void saveCurrentPreset();
  Q_INVOKABLE void createCustomPreset(const QString &category, const QString &name);
  Q_INVOKABLE bool canDeletePreset(const QString &name);
};

<!-- ConfigOptionModel API used by OptionRow/GroupNavSidebar (after 56-01/56-02) -->
Q_INVOKABLE QString optKey(int i);
Q_INVOKABLE QString optLabel(int i);
Q_INVOKABLE QString optType(int i);            // "bool"|"int"|"double"|"enum"|"string"|"percent"
Q_INVOKABLE QVariant optValue(int i);
Q_INVOKABLE double optMin(int i); Q_INVOKABLE double optMax(int i); Q_INVOKABLE double optStep(int i);
Q_INVOKABLE bool optReadonly(int i);
Q_INVOKABLE bool optIsDirty(int i);
Q_INVOKABLE bool optNullable(int i);           // added 56-01
Q_INVOKABLE bool optIsVector(int i);           // added 56-01
Q_INVOKABLE QString optSidetext(int i);        // added 56-01
Q_INVOKABLE QString optUnit(int i);            // already existed
Q_INVOKABLE QString optTooltip(int i);
Q_INVOKABLE QString optGroup(int i);
Q_INVOKABLE QString optPage(int i);
Q_INVOKABLE int optMode(int i);
Q_INVOKABLE QStringList optEnumLabelsList(int i);
Q_INVOKABLE int dirtyCount() const;
Q_INVOKABLE int dirtyCountForGroup(const QString &group) const;  // added 56-01
Q_INVOKABLE QStringList groupNames() const;                       // added 56-01
Q_INVOKABLE QStringList pageNames() const;
Q_INVOKABLE void setValue(int row, const QVariant &value);
Q_INVOKABLE void resetOption(int row);

<!-- BackendContext signal already exists (Phase 52); main.qml connects to it -->
signal settingsRequested(const QString &category);

<!-- Existing SavePresetDialog/UnsavedChangesDialog required properties -->
SavePresetDialog   { required property var configVm; required property string presetTier }
UnsavedChangesDialog { required property var configVm; property string presetTier; property string action }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract OptionRow.qml (typed-option renderer for all 7 kinds)</name>
  <files>src/qml_gui/components/OptionRow.qml</files>
  <read_first>
    - src/qml_gui/components/ParamsPage.qml (lines 206-432 -- the existing delegate to extract; lines 311-419 typed control dispatch; lines 278-303 dirty/readonly; lines 224-247 group header; lines 336-363 numeric TextInput pattern)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 94-222 -- full extract guidance with code excerpts)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-UI-SPEC.md (Typed Control Matrix; Numeric Input Detail; Value-Source/Inheritance Indicator tables)
    - .claude/rules/debugging.md (SpinBox.stepSize is int-only -- use TextInput+DoubleValidator for float/percent)
  </read_first>
  <action>
    Create src/qml_gui/components/OptionRow.qml as a self-contained QML item (top-level `Item` or `Rectangle`). Required properties (declared at top, mirroring how ParamsPage passes row data):
    - `required property var optionModel` -- the ConfigOptionModel instance
    - `required property int optIdx` -- row index in optionModel
    - `property string searchText: ""` -- for label bolding on search match
    - `property bool showGroupHeader: false` -- whether to render the group header bar above this row
    - `property string oGroup: ""` -- the group name to render in the header (passed in by SettingsDialog's ListView delegate)

    Expose computed accessors mirroring ParamsPage.qml's `paramRow` pattern (use readonly properties or functions: oType, oVal, oMin, oMax, oStep, oLabel, oRO, oDirty, oNullable, oIsVector, oUnit, oTip, oEnumLabels -- all read via optionModel.optXxx(optIdx)).

    Layout (heights per UI-SPEC): bool/enum rows 44px; numeric (int/double/percent) rows 56px; string rows 80px. Use `(oType === "double" || oType === "int" || oType === "percent") ? 56 : oType === "string" ? 80 : 44` for the content area height, plus 28px when showGroupHeader is true for the group header band.

    Group header (visible when showGroupHeader): Rectangle 28px tall, color Theme.bgSurface, with Text (oGroup, color Theme.textTertiary, font.pixelSize Theme.fontSizeSM, bold) at left margin 20px, plus a 1px bottom border (Theme.borderSubtle).

    Dirty dot (visible when oDirty): 6x6 Rectangle radius 3 color Theme.statusWarning, anchored left of the label column with Theme.spacingXS gap.

    Label column (width 180px, left margin 20px): Text label, color state-dependent (Theme.textSecondary normal; Theme.statusWarning when dirty; Theme.textDisabled when oRO). Add a small Text below for value-source when oNullable -- text from a passed-in `valueSource` property (string) colored Theme.textTertiary, font.pixelSize Theme.fontSizeXS. Add a `qsTr("Read-only")` tag when oRO.

    Control column (right of label, fill remaining width): a Loader whose sourceComponent switches on oType:
    - "bool" -> CxSwitch bound to oVal, enabled !oRO, onToggled -> optionModel.setValue(optIdx, checked)
    - "int" -> RowLayout { CxSlider (fillWidth, from oMin, to oMax, value oVal, enabled !oRO, onMoved -> setValue) ; CxSpinBox (width 90, height Theme.controlHeightSM, value oVal, suffix oUnit, enabled !oRO, onValueModified -> setValue) }
    - "double" / "percent" -> RowLayout { CxSlider (fillWidth, from/to/value as int) ; Rectangle (width 64, height 24, radius Theme.radiusSM, color Theme.bgInset, border 1px border.color activeFocus?Theme.borderFocus:oRO?Theme.borderSubtle:Theme.borderInput) wrapping a TextInput (anchors.fill margins 6, validator DoubleValidator{decimals:3}, horizontalAlignment AlignRight, color Theme.textPrimary, font.pixelSize Theme.fontSizeSM, bold, readOnly oRO, text formatted to 2 decimals for double / integer+suffix for percent, onEditingFinished -> clamp to [oMin,oMax] then setValue) ; Text suffix (visible oUnit !== "", text oUnit, color Theme.textTertiary, font.pixelSize Theme.fontSizeXS) } -- DO NOT use SpinBox for float/percent (debugging rule)
    - "enum" -> CxComboBox (width 160, model oEnumLabels, currentIndex oVal, enabled !oRO, onActivated -> setValue(optIdx, index))
    - "string" -> ScrollView (height 60) wrapping a TextArea (text oVal, font Consolas per existing ParamsPage, color Theme.textSecondary, readOnly oRO, wrapMode TextArea.Wrap, onTextChanged -> setValue when activeFocus)

    Row zebra striping: top-level Rectangle color (index + (showGroupHeader?1:0)) % 2 == 0 ? "transparent" : "#080b10" (the index is passed in by the ListView delegate -- accept it as `property int rowIndex`).

    Tooltip: MouseArea hover (no accepted buttons, z: -1) per existing pattern; ToolTip.visible when oTip !== "" && hover; ToolTip.delay 500.

    Read-only state: opacity 0.6 when oRO; controls inside set enabled false / readOnly true per their type.

    All user-visible strings via qsTr() per the UI-SPEC copywriting contract. All controls MUST be Cx* (CxSwitch/CxSlider/CxSpinBox/CxComboBox) -- no raw QtQuick.Controls Switch/Slider/SpinBox/ComboBox. TextArea is allowed (no CxTextArea covers the multiline string case in the existing control set; if CxTextArea exists in src/qml_gui/controls/, use it instead -- check the controls directory first).

    ASCII-only comments. Reference UI-SPEC region IDs in a header comment block (SETPRINT-OPTIONS / SETMAT-OPTIONS).

    Note: percent type renders the same as double but with unit always "%". The optUnit(i) for percent options returns "%" already from ConfigOptionModel (or fallback). Keep the suffix Text driven by oUnit so all numeric types share the same suffix rendering.
  </action>
  <verify>
    <automated>cmake --build build --config Release</automated>
  </verify>
  <acceptance_criteria>
    - src/qml_gui/components/OptionRow.qml exists with the required properties declared.
    - Dispatch covers oType values "bool", "int", "double", "percent", "enum", "string" (6 dispatch branches; nullable and isVector are surfaced via the value-source indicator and a per-extruder selector respectively, not separate dispatch branches).
    - No raw QtQuick.Controls Switch/Slider/SpinBox/ComboBox -- only Cx* (grep for `Switch {`, `Slider {`, `SpinBox {`, `ComboBox {` returns 0 matches in this file).
    - Float/percent input uses TextInput + DoubleValidator, NOT SpinBox.
    - Int input uses CxSpinBox with the `suffix` property wired to oUnit.
    - Every user-visible string wrapped in qsTr().
    - `cmake --build build --config Release` exits 0 (QML compiles; no import errors).
  </acceptance_criteria>
  <done>OptionRow.qml renders every typed option kind using Cx* controls only, with dirty/readonly/value-source indicators and unit suffixes; compiles standalone before being used by SettingsDialog.</done>
</task>

<task type="auto">
  <name>Task 2: Extract GroupNavSidebar.qml + build SettingsDialog.qml shell</name>
  <files>src/qml_gui/components/GroupNavSidebar.qml, src/qml_gui/dialogs/SettingsDialog.qml, src/qml_gui/qml.qrc</files>
  <read_first>
    - src/qml_gui/components/ParamsPage.qml (lines 39-111 -- the existing category sidebar to extract as GroupNavSidebar)
    - src/qml_gui/dialogs/SavePresetDialog.qml (required-property injection pattern)
    - src/qml_gui/dialogs/UnsavedChangesDialog.qml (dirty-guard reuse pattern; openDialog function)
    - src/qml_gui/controls/CxDialog.qml (header pattern lines 26-76 -- the close button visual)
    - src/qml_gui/qml.qrc (existing dialog entries around lines 21-43, components section around line 69)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 26-91 SettingsDialog shell pattern; lines 226-271 GroupNavSidebar pattern; lines 580-608 qml.qrc additions)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-UI-SPEC.md (Dialog Shell Contract; Region-by-Region Specification; Copywriting Contract)
  </read_first>
  <action>
    Create src/qml_gui/components/GroupNavSidebar.qml. Extract the existing ParamsPage.qml lines 39-111 category sidebar pattern into a self-contained component with these required properties:
    - `required property var optionModel` (ConfigOptionModel)
    - `required property QStringList groups` (the list of group names for the active tab; passed in by SettingsDialog from configVm.groupNames(tier) filtered by active page)
    - `property string selectedGroup: ""` with `signal groupSelected(string groupName)`
    Render a CxScrollView wrapping a ColumnLayout of group items (Rectangle 34px tall, radius Theme.radiusSM, selected bg "#1c2a3e" + accent border, unselected transparent, hover "#161d28"). Each item shows the group name (Text Theme.fontSizeMD) on the left and a count badge (Rectangle Theme.fontSizeXS) on the right showing optionModel.countForCategory(group) or optionModel.dirtyCountForGroup(group) per the UI-SPEC SETPRINT-GROUPNAV region. Add an "All" item (qsTr("All")) as the first entry. Group item color: selected Theme.accent, unselected Theme.textSecondary. All user-visible strings via qsTr().

    Create src/qml_gui/dialogs/SettingsDialog.qml as an `ApplicationWindow` (NOT CxDialog -- CxDialog is modal-only per PATTERNS lines 30-31). Required properties:
    - `required property var configVm`
    - `required property string presetTier` -- one of "printer" / "filament" / "print"
    - `required property var optionModel` -- the per-tier ConfigOptionModel (configVm.machineOptions / filamentOptions / printOptions depending on tier)
    Window properties: `flags: Qt.Window | Qt.WindowCloseButtonHint`; `modality: Qt.NonModal`; width 920 height 640 minimumWidth 720 minimumHeight 480 (per UI-SPEC Dialog Shell Contract); color Theme.bgElevated; title derived from presetTier via the UI-SPEC copywriting table ("Printer Settings" / "Material Settings" / "Process Settings").

    Layout top-to-bottom:
    1. Title bar Rectangle (height Theme.titleBarHeight 40px, color Theme.chromeSurface): on the left a CxComboBox (width 240) bound to the tier-appropriate preset name list (configVm.printerPresetNames/filamentPresetNames/printPresetNames) and currentPreset; onActivated -> the tier's requestCurrentXxxPreset setter. Center: a "Modified" badge (visible configVm.isPresetDirty, color Theme.statusWarning, bg Theme.bgWarningSubtle, text qsTr("Modified")). Right: a Save As CxButton (compact, opens SavePresetDialog scoped to tier), and a close Rectangle 28x28 with "X" Text + Theme.chromeDangerHover on hover (pattern from CxDialog header lines 50-59).
    2. Preset bar Rectangle (height Theme.controlHeightMD 34px, color Theme.bgPanel): a "Save" CxButton (visible only for user presets, enabled when dirty, calls configVm.requestSavePendingChanges()), "Delete" CxButton (visible for user presets, calls a small inline confirm via CxDialog then configVm.deletePreset), "Reset All" CxButton (calls configVm.resetAllGlobalOptions() with a CxDialog confirm per UI-SPEC Destructive Actions), and a compatibility indicator Text bound to configVm.currentPresetCombinationValid / currentPresetCompatibilityMessage.
    3. Tab strip Rectangle (height Theme.tabBarHeight 36px, color Theme.bgPanel): a Repeater over the tier's tab labels (qsTr-wrapped per UI-SPEC -- printer: "Basic information"/"Dependencies"/"Notes"; filament: "Filament"/"Cooling"/"Advanced"/"Multimaterial"/"Dependencies"/"Notes"; print: "Quality"/"Strength"/"Speed"/"Support"/"Multimaterial"/"Others"). Active tab has a 2px bottom Theme.accent underline + Theme.accent text; inactive tabs Theme.textSecondary with Theme.bgHover on hover. Tab click sets `property string activeTab` which drives the GroupNavSidebar group list.
    4. Main content RowLayout: GroupNavSidebar on the left (Layout.preferredWidth 200, fill height); the option editing area on the right (Rectangle fill, color Theme.bgBase) containing a ListView whose model is optionModel filtered by activeTab and selectedGroup via configVm.filterOptionIndices(tier, searchText, advancedMode). The ListView delegate loads OptionRow.qml passing optionModel, optIdx, showGroupHeader (true when previous delegate's group differs), rowIndex.
    5. Search bar Rectangle (height 44, color Theme.bgSurface): a CxTextField (placeholder qsTr("Search options..."), text -> searchText), a CxSwitch + qsTr("Advanced") label (checked -> advancedMode), a "Reset Group" CxButton (calls configVm.resetGroup(tier, selectedGroup)), and a match count Text + modified count Text per UI-SPEC.
    6. Footer Rectangle (height 44, color Theme.bgSurface, top border Theme.borderSubtle): a right-aligned RowLayout with a spacer Item (fillWidth), then Save CxButton (Primary, qsTr("Save"), calls configVm.requestSavePendingChanges()), Discard CxButton (Secondary, qsTr("Discard"), calls configVm.requestDiscardPendingChanges()), Cancel CxButton (Ghost, qsTr("Cancel"), triggers close path).

    Close path (onClosing handler or X-button click): if configVm.isPresetDirty -> open UnsavedChangesDialog (instance created as a child of the dialog, with configVm and presetTier bound); on UnsavedChangesDialog.action=="save" -> requestSavePendingChanges then close; "discard" -> requestDiscardPendingChanges then close; "cancel" -> abort close (reject). If not dirty -> close directly. Esc key handler matches the same close path.

    Add a `function show()` that calls `visible = true; requestActivate()`. Per RESEARCH Pitfall 6, requestActivate is needed so the non-modal dialog receives keyboard focus alongside the main ApplicationWindow.

    Multi-value (isVector) options: in the ListView delegate, when optionModel.optIsVector(optIdx) is true, render a small CxComboBox above the OptionRow that selects an extruder index (1..N where N comes from the printer preset); for v1 the OptionRow shows the value for the selected extruder only. (PresetBundle exposes extruder count; if not readily accessible from QML, read it via a small helper property on configVm -- if not present, leave the extruder selector as a single-item combo and note as a known limitation in 56-03 SUMMARY. The TYPE coverage is what matters; full multi-extruder editing fidelity is acceptable as a documented hybrid.)

    Register all three new files in src/qml_gui/qml.qrc: add `<file>dialogs/SettingsDialog.qml</file>` to the Dialogs section (alongside SavePresetDialog/UnsavedChangesDialog entries) and `<file>components/OptionRow.qml</file>` + `<file>components/GroupNavSidebar.qml</file>` to the components section (alongside the existing ParamsPage.qml entry). Do NOT remove any existing entries -- old files stay until Phase 57.

    All controls must be Cx* (no raw QtQuick.Controls in consuming code per v14-01 component-system gate). All user-visible strings via qsTr(). ASCII-only comments.
  </action>
  <verify>
    <automated>cmake --build build --config Release</automated>
  </verify>
  <acceptance_criteria>
    - src/qml_gui/components/GroupNavSidebar.qml exists, declares the required properties, uses CxScrollView, and renders group items + count badges.
    - src/qml_gui/dialogs/SettingsDialog.qml exists as an ApplicationWindow with modality Qt.NonModal, the 6 layout regions (title/preset/tabs/main/search/footer), and required properties configVm/presetTier/optionModel.
    - SettingsDialog close path opens UnsavedChangesDialog when configVm.isPresetDirty is true.
    - SettingsDialog exposes a `show()` function that sets visible=true and calls requestActivate().
    - qml.qrc registers all three new files; no existing entries removed.
    - No raw QtQuick.Controls in consuming code in the three new files (grep for `^\\s*Switch {|^\\s*Slider {|^\\s*SpinBox {|^\\s*ComboBox {|^\\s*TextField {|^\\s*Button {|^\\s*Dialog {` returns 0 in those three files -- TextArea is allowed for the string option since there is no CxTextArea-equivalent for multiline; document this exception if it applies).
    - `cmake --build build --config Release` exits 0; the QML loads without import errors (check build/startup_diagnostics.log if smoke test runs the app).
  </acceptance_criteria>
  <done>SettingsDialog + GroupNavSidebar + OptionRow form a working parameterized non-modal settings dialog shell; qml.qrc registers them; the old SettingsPage/ParamsPage files remain untouched (Phase 57 cleanup).</done>
</task>

<task type="auto">
  <name>Task 3: Wire main.qml to show the correct SettingsDialog on settingsRequested + flip QmlUiAuditTests GREEN</name>
  <files>src/qml_gui/main.qml, tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - src/qml_gui/main.qml (current top-level structure, existing Connections to backend, how other dialogs like SavePresetDialog are instantiated)
    - src/qml_gui/qml.qrc (registration confirmed by Task 2)
    - tests/QmlUiAuditTests.cpp (existing audit method patterns; if no QmlUiAuditTests.cpp exists, find the equivalent QML-audit test target)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 564-577 main.qml Connections pattern)
  </read_first>
  <action>
    In src/qml_gui/main.qml, instantiate three SettingsDialog instances as children of the root ApplicationWindow (or as top-level items, since they are themselves ApplicationWindows). Give them ids: printerSettingsDialog, materialSettingsDialog, processSettingsDialog. Bind their configVm to backend.configViewModel, presetTier to "printer"/"filament"/"print" respectively, and optionModel to backend.configViewModel.machineOptions / filamentOptions / printOptions respectively. Set initial `visible: false`.

    Add a `Connections { target: backend; function onSettingsRequested(category) { ... } }` block. Inside: if category == "printer" -> printerSettingsDialog.show(); "filament" -> materialSettingsDialog.show(); "process" (or "print") -> processSettingsDialog.show(). Use the exact same category strings BackendContext emits.

    Do NOT remove or alter any existing Connections to backend (e.g., other signal handlers). Only ADD this handler.

    In tests/QmlUiAuditTests.cpp, replace the Wave 0 RED scaffolds (settingsDialogUsesOnlyCxControls, settingsDialogNoRawControls, settingsDialogStringsQsTr) with real audit assertions:
    - settingsDialogUsesOnlyCxControls / settingsDialogNoRawControls: read each of the three new files (SettingsDialog.qml, OptionRow.qml, GroupNavSidebar.qml) as text and assert no occurrence of raw QtQuick.Controls control declarations (the regex from Task 2 acceptance criteria). Allow TextArea inside OptionRow for the string type (document the exception in the test comment).
    - settingsDialogStringsQsTr: assert that every user-visible literal English string in the three files is wrapped in qsTr() (grep for unquoted English near common QML properties; the audit can use a heuristic -- e.g., `text: "[A-Z][a-z]+` not preceded by qsTr).

    If a QmlUiAuditTests.cpp does not exist, check tests/CMakeLists.txt for the actual audit test target name and add the methods there. If still not found, document this in the SUMMARY and the planner accepts adding them to ViewModelSmokeTests.cpp as a fallback (test target consistency matters less than the audit existing).

    ASCII-only comments.
  </action>
  <verify>
    <automated>cmake --build build --config Release &amp;&amp; ctest --test-dir build -R 'QmlUiAudit|ViewModelSmokeTest' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - main.qml instantiates three SettingsDialog instances with the correct tier bindings, all initially invisible.
    - main.qml contains a Connections block on backend.settingsRequested that dispatches to the correct dialog's show().
    - QmlUiAuditTests settingsDialogUsesOnlyCxControls, settingsDialogNoRawControls, settingsDialogStringsQsTr are GREEN (RED scaffolds from 56-01 replaced with real audit assertions).
    - `cmake --build build --config Release` exits 0; the three audit tests pass via ctest.
    - The canonical `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` completes successfully (no smoke test regressions).
  </acceptance_criteria>
  <done>main.qml routes settingsRequested to the correct SettingsDialog instance; the QmlUiAuditTests confirm Cx-only controls and qsTr discipline across the new files. SETTINGS-01 is satisfied at the dialog-shell level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Local desktop QML dialogs; no network input, no auth surface, persistence is via existing PresetServiceMock local files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-SC | Tampering | (none) | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A -- all Cx* controls and Theme are project-internal, already vetted by v14-01). |
| T-56-03 | All | SettingsDialog / OptionRow / GroupNavSidebar / main.qml wiring | accept | QML presentation layer only; all business logic (save/reset/dirty/search) stays in ConfigViewModel per qml-boundaries.md rule. No external input crosses a trust boundary. Builtin-preset read-only gating is enforced in ConfigViewModel (56-02), not in QML. |
</threat_model>

<verification>
- `cmake --build build --config Release` exits 0 after each task.
- After Task 3: `ctest --test-dir build -R 'QmlUiAudit|ViewModelSmokeTest' --output-on-failure` runs GREEN for the three QmlUiAudit methods.
- The canonical `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` completes successfully after Task 3 (no smoke regressions; QML loads cleanly).
- Manual (deferred to Phase 58 per VALIDATION.md Manual-Only table): open each dialog from the Prepare sidebar and compare density/spacing/control placement to shotScreen/打印机参数设置页.png and 材料参数设置页.png.
</verification>

<success_criteria>
- Three independent non-modal SettingsDialog instances open from the Prepare sidebar via settingsRequested.
- All 7 typed option kinds render via OptionRow using Cx* controls only.
- GroupNavSidebar lists groups with count + dirty badges driven by ConfigOptionModel.
- QmlUiAuditTests confirm Cx-only and qsTr discipline.
- The old SettingsPage/ParamsPage/ConfigPage/SearchDialog files are untouched (Phase 57 cleanup).
</success_criteria>

<output>
Create `.planning/phases/56-parameter-settings-dialogs-restoration/56-03-SUMMARY.md` when done.
</output>
