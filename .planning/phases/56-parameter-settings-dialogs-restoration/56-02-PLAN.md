---
phase: 56-parameter-settings-dialogs-restoration
plan: 02
type: execute
wave: 2
depends_on: ['56-01']
files_modified:
  - src/core/viewmodels/ConfigViewModel.h
  - src/core/viewmodels/ConfigViewModel.cpp
  - src/qml_gui/Models/ConfigOptionModel.cpp
  - src/qml_gui/BackendContext.h
  - src/qml_gui/BackendContext.cpp
  - tests/ViewModelSmokeTests.cpp
autonomous: true
requirements: [SETTINGS-02, SETTINGS-04, SETTINGS-05, SETTINGS-06]
user_setup: []

must_haves:
  truths:
    - "ConfigViewModel exposes per-tier groupNames, resetGroup, per-group dirty count, and per-tier filterOptionIndices"
    - "Static page-to-group mapping is populated from upstream Tab.cpp for printer, filament, and process tiers"
    - "BackendContext.openSettings() sets the active preset tier and emits settingsRequested(category) (no more 'pending Phase 56' no-op)"
    - "ViewModelSmokeTests for SETTINGS-02/04/05/06 turn GREEN (RED scaffolds from 56-01 replaced with real assertions)"
  artifacts:
    - path: "src/core/viewmodels/ConfigViewModel.h"
      provides: "resetGroup, groupNames, dirtyCountForGroup, optNullable, optIsVector, optSidetext Q_INVOKABLE proxies"
      contains: "resetGroup"
    - path: "src/core/viewmodels/ConfigViewModel.cpp"
      provides: "per-tier filterOptionIndices dispatch; resetGroup impl"
      contains: "optionModelForTier"
    - path: "src/qml_gui/Models/ConfigOptionModel.cpp"
      provides: "static page->group mapping tables for printer/filament/process tiers"
      contains: "kPrinterPageGroupMap"
    - path: "src/qml_gui/BackendContext.cpp"
      provides: "settingsRequested forwarder calls setActivePresetTier before emit"
      contains: "setActivePresetTier"
  key_links:
    - from: "src/qml_gui/BackendContext.cpp::forwardSettingsRequest"
      to: "ConfigViewModel::setActivePresetTier"
      via: "direct call before emit settingsRequested"
      pattern: "setActivePresetTier.*emit settingsRequested|emit settingsRequested.*setActivePresetTier"
    - from: "src/core/viewmodels/ConfigViewModel.cpp::resetGroup"
      to: "ConfigOptionModel::resetOption"
      via: "loop over options in group, call resetOption(row)"
      pattern: "resetOption"
---

<objective>
Wave 2 C++ viewmodel + composition-root wiring: extend ConfigViewModel with the per-tier / per-group operations the new dialogs need (groupNames, resetGroup, per-group dirty count, per-tier filterOptionIndices, nullable/vector/sidetext proxies); build the static page-to-group mapping tables derived from upstream Tab.cpp (RESEARCH Pitfall 7 -- no upstream metadata, derive statically); replace the BackendContext "pending Phase 56" no-op with setActivePresetTier + emit settingsRequested.

Purpose: The QML dialog shell (Wave 3) needs a complete C++ backend API to render tabs/groups/options and to perform save/reset/search. Wiring the open path here means Wave 3 only has to react to the settingsRequested signal.
Output: Extended ConfigViewModel + ConfigOptionModel page-group tables + BackendContext openSettings; GREEN ViewModelSmokeTests for SETTINGS-02/04/05/06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-CONTEXT.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md
@.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md

@src/core/viewmodels/ConfigViewModel.h
@src/core/viewmodels/ConfigViewModel.cpp
@src/qml_gui/Models/ConfigOptionModel.h
@src/qml_gui/BackendContext.h
@src/qml_gui/BackendContext.cpp

<interfaces>
<!-- Existing ConfigViewModel Q_INVOKABLE methods (relevant subset, ConfigViewModel.h:98-177) -->
<!-- All of these already exist; this plan ADDS resetGroup + group proxies + per-tier filter. -->
Q_INVOKABLE bool requestSavePendingChanges();
Q_INVOKABLE bool requestDiscardPendingChanges();
Q_INVOKABLE bool requestCancelPendingChanges();
Q_INVOKABLE QList<int> filterOptionIndices(const QString &category, const QString &searchText, bool advancedMode = false) const;
Q_INVOKABLE QString valueSourceForKey(const QString &key) const;
Q_INVOKABLE bool resetOptionToLevel(const QString &key, int level);
Q_INVOKABLE bool resetGlobalOption(const QString &key);
Q_INVOKABLE void resetAllGlobalOptions();

// Existing private helper at ConfigViewModel.h:204 -- REUSED by the new per-tier filter:
ConfigOptionModel *optionModelForTier(const QString &tier) const;

<!-- BackendContext existing wiring (BackendContext.cpp:569-578 -- the no-op to replace) -->
void BackendContext::forwardSettingsRequest(const QString &category) {
  qInfo("[Backend] settingsRequested(%s) -- settings dialog pending Phase 56", qPrintable(category));
  emit settingsRequested(category);
}

<!-- Phase-52 already-wired connect (BackendContext.cpp:95-109) -- NOT touched, just confirmed still firing -->
connect(configViewModel_, &ConfigViewModel::stateChanged, editorViewModel_,
        [this]() { editorViewModel_->invalidateAllSliceResults(); emit editorViewModel_->stateChanged(); });

<!-- Tab.cpp page/group names (verified in RESEARCH Sources section, lines 2311-2788 print, 3892-4216 filament, 4438-4900 printer) -->
<!-- Printer (TabPrinter::build_fff) pages: Basic information, Machine G-code, Notes, Motion ability (extruder pages) -->
<!-- Filament (TabFilament::build) pages: Filament, Cooling, Advanced, Multimaterial, Dependencies, Notes -->
<!-- Print (TabPrint::build) pages: Quality, Strength, Speed, Support, Multimaterial, Others -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build static page-to-group mapping tables for printer/filament/process</name>
  <files>src/qml_gui/Models/ConfigOptionModel.cpp</files>
  <read_first>
    - src/qml_gui/Models/ConfigOptionModel.cpp (current loadMachineSchema, loadFilamentSchema, loadSchemaFromKeys, mapCategory)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-RESEARCH.md (Sources section lines 432-495: exact upstream Tab.cpp page+group names per tier)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 277-403)
    - third_party/OrcaSlicer/src/slic3r/GUI/Tab.cpp (lines 2311-2788 TabPrint, 3892-4216 TabFilament, 4438-4900 TabPrinter) -- ONLY for verification, do not modify upstream
  </read_first>
  <behavior>
    - Test testTabsAndGroupNavPerTier: after loadMachineSchema(), pageNames() returns at least the printer pages "Basic information" and "Notes"; groupNames() returns at least "Printable space" and "Extruder Clearance". After loadFilamentSchema(), pageNames() includes "Filament" and "Cooling"; groupNames() includes "Print temperature" and "Bed temperature". For the print tier, pageNames() includes "Quality" and "Speed"; groupNames() includes "Layer height" and "Infill".
  </behavior>
  <action>
    In ConfigOptionModel.cpp, add three file-scope static functions (or static const QHash<QString, QString>) that return the upstream-derived page->group mapping per tier. Mirror the upstream Tab.cpp page and group names EXACTLY as documented in 56-RESEARCH.md Sources section (do NOT invent new names):
    - `static const QHash<QString,QString>& kPrinterPageGroupMap();` -- key format "<page>::<group>" -> empty (use a QSet<QString> or QMultiHash<QString,QString> if cleaner; the goal is a known list of (page,group) pairs for the printer tier).
    - `static const QHash<QString,QString>& kFilamentPageGroupMap();` -- same shape, filament tier.
    - `static const QHash<QString,QString>& kPrintPageGroupMap();` -- same shape, print/process tier.

    Populate each table from the RESEARCH "Upstream Tab page and group names" section (the page/group pairs are listed there verbatim). For the printer tier, use the page names from `TabPrinter::build_fff` (Basic information, Machine G-code, Notes, Motion ability / Extruder). For filament, `TabFilament::build` (Filament, Cooling, Advanced, Multimaterial, Dependencies, Notes). For print, `TabPrint::build` (Quality, Strength, Speed, Support, Multimaterial, Others).

    Add a `void assignPageGroupForTier(const QString &tier);` method (private, called from loadMachineSchema/loadFilamentSchema/loadFromUpstreamSchema as appropriate) that, after the schema options are loaded, walks m_options and assigns `entry.page` and `entry.group` from the per-tier mapping table when the option key matches a known key in the table. For options not present in the mapping, leave page/group empty (the option will appear under an "Other" bucket -- this is acceptable, the dialog shows "All" by default).

    Wire assignPageGroupForTier("printer") into loadMachineSchema, assignPageGroupForTier("filament") into loadFilamentSchema, and assignPageGroupForTier("print") into loadFromUpstreamSchema, each AFTER the schema options are populated but BEFORE endResetModel().

    ASCII-only comments. Reference the upstream source line range (e.g., `// Upstream: Tab.cpp:4438-4900 (TabPrinter::build_fff)`) above each table.

    IMPORTANT: do NOT change the existing kMachineKeys / kFilamentKeys key arrays -- they drive loadSchemaFromKeys. Only add the page/group mapping metadata alongside them.
  </action>
  <verify>
    <automated>cmake --build build --config Release &amp;&amp; ctest --test-dir build -R ViewModelSmokeTest --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ConfigOptionModel.cpp contains kPrinterPageGroupMap, kFilamentPageGroupMap, kPrintPageGroupMap (or equivalent) static structures.
    - After loadMachineSchema, pageNames() returns at least "Basic information" and "Notes"; groupNames() returns at least "Printable space" and "Extruder Clearance".
    - After loadFilamentSchema, pageNames() returns at least "Filament" and "Cooling".
    - For the print tier, pageNames() returns at least "Quality" and "Speed".
    - testTabsAndGroupNavPerTier (declared in 56-01) is GREEN: its QFAIL body is replaced with real assertions using pageNames()/groupNames().
    - `cmake --build build --config Release` exits 0 and `ctest --test-dir build -R ViewModelSmokeTest` passes for testTabsAndGroupNavPerTier.
  </acceptance_criteria>
  <done>Static page/group tables for all three tiers are populated from upstream Tab.cpp and surfaced through pageNames()/groupNames(); the SETTINGS-02 RED scaffold is now GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ConfigViewModel with per-tier/per-group operations + open-path wiring</name>
  <files>src/core/viewmodels/ConfigViewModel.h, src/core/viewmodels/ConfigViewModel.cpp, src/qml_gui/BackendContext.h, src/qml_gui/BackendContext.cpp</files>
  <read_first>
    - src/core/viewmodels/ConfigViewModel.h (current Q_INVOKABLE list ~lines 98-177; optionModelForTier at line 204; setActivePresetTier at line 135)
    - src/core/viewmodels/ConfigViewModel.cpp (current filterOptionIndices implementation, requestSavePendingChanges, resetOptionToLevel)
    - src/qml_gui/BackendContext.h (settingsRequested signal lines 417-419)
    - src/qml_gui/BackendContext.cpp (forwardSettingsRequest lines 569-578; Phase-52 connect lines 95-109)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-PATTERNS.md (lines 405-451: needed additions; lines 517-577: BackendContext replacement)
  </read_first>
  <behavior>
    - Test testPerOptionDirtyAndValueSource: after setValue(row, v) on a printer/filament/print option, optIsDirty(row) is true; valueSourceForKey(key) returns a non-default source string; dirtyCountForGroup(tier, group) increments.
    - Test testSaveSaveAsResetOptionResetGroupResetAll: resetGroup(tier, groupName) returns all options in that group to reference values; subsequent dirtyCount() is 0 for those options.
    - Test testPerDialogSearchAndFourLevelMode: filterOptionIndices(tier, searchText, advancedMode=false) excludes mode>=1 options when advancedMode is false; includes them when advancedMode is true.
    - Test testReadonlyBuiltinGating: when the active preset is builtin (PresetServiceMock::isBuiltinPreset returns true), requestSavePendingChanges() returns false and emits saveAsRequired (or equivalent existing signal); it does NOT silently overwrite the builtin.
    - Test testSettingsDialogOpenFromSidebar: BackendContext::forwardSettingsRequest("printer") calls configViewModel->setActivePresetTier("printer") and emits settingsRequested("printer").
  </behavior>
  <action>
    In ConfigViewModel.h add Q_INVOKABLE declarations (signatures match 56-PATTERNS.md lines 423-439):
    - `Q_INVOKABLE void resetGroup(const QString &tier, const QString &groupName);`
    - `Q_INVOKABLE bool optNullable(const QString &tier, int index) const;`
    - `Q_INVOKABLE bool optIsVector(const QString &tier, int index) const;`
    - `Q_INVOKABLE QString optSidetext(const QString &tier, int index) const;`
    - `Q_INVOKABLE QStringList groupNames(const QString &tier) const;`
    - `Q_INVOKABLE int dirtyCountForGroup(const QString &tier, const QString &groupName) const;`

    Extend the existing `Q_INVOKABLE QList<int> filterOptionIndices(const QString &category, const QString &searchText, bool advancedMode = false) const;` so that the `category` argument selects the option model via `optionModelForTier(category)` instead of hard-wiring printOptions_. Preserve the existing call shape so any current QML caller still works. When category is "printer"/"filament"/"print" (or legacy "machine"/"process" aliases), dispatch to the matching model via optionModelForTier(category). Both the new tier strings AND the legacy aliases must be accepted so existing callers (QML, tests) that pass "machine"/"process" do not break -- the dispatch normalizes them internally. The Q_PROPERTY tier names on ConfigViewModel (machineOptions/filamentOptions/printOptions) intentionally retain their legacy names for binary compatibility; the category argument is a separate dispatch key, not a property name. The advancedMode filter logic stays the same (hide mode>=1 when advancedMode=false; hide mode==0 when advancedMode=true -- RESEARCH Pitfall 3 documents the 4-level ConfigOptionMode).

    Implement the new methods in ConfigViewModel.cpp by delegating to optionModelForTier(tier) for all the opt* proxies and groupNames/dirtyCountForGroup. resetGroup(tier, groupName) walks the option model, finds rows whose optGroup(row) == groupName, and calls optionModel->resetOption(row) for each. Emit stateChanged() once after the loop (bulk, not per-option).

    In BackendContext.cpp replace the no-op forwardSettingsRequest (lines 569-578) with a CONCRETE two-step contract. The method MUST be named `forwardSettingsRequest(const QString &category)` (kept as a Q_INVOKABLE/slot so it can be called directly from tests without going through QML) and MUST execute exactly two steps in this order:
    1. `configViewModel_->setActivePresetTier(category);` -- FIRST, before anything else. This is what makes the downstream option-model tier selection correct before any consumer reads it.
    2. `emit settingsRequested(category);` -- SECOND, after setActivePresetTier has returned.

    The "pending Phase 56" qInfo log line is removed (or changed to "[Backend] openSettings(category=%s)" if a log is still desired). The method body has no other branches or early returns -- it is the single, testable entry point for the SETTINGS-01 open path and is asserted verbatim by testSettingsDialogOpenFromSidebar in 56-04 Task 2 (spy on settingsRequested; assert spy.count()==1, the emitted string matches the category, AND configViewModel->activePresetTier()==category after the call, proving step 1 ran before step 2).

    In BackendContext.h update the doc comment on settingsRequested to reflect Phase 56 (no longer "pending"). The signal signature stays the same.

    Do NOT touch the Phase-52 connect (BackendContext.cpp:95-109). It already fires when ConfigViewModel emits stateChanged after any option edit or resetGroup; that is the SETTINGS-07 path 56-04 will assert.

    ASCII-only comments. Reference CONTEXT.md decision IDs in the new method doc comments (e.g., `// SETTINGS-05 reset-group: per upstream Tab.cpp reset_group`).
  </action>
  <verify>
    <automated>cmake --build build --config Release &amp;&amp; ctest --test-dir build -R ViewModelSmokeTest --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - ConfigViewModel.h declares resetGroup, optNullable, optIsVector, optSidetext, groupNames, dirtyCountForGroup as Q_INVOKABLE.
    - filterOptionIndices dispatches via optionModelForTier(category) for "printer"/"filament"/"print" (the print-only hard-wire is gone).
    - BackendContext exposes a Q_INVOKABLE/slot `forwardSettingsRequest(const QString &category)` with the exact two-step body: (1) `configViewModel_->setActivePresetTier(category);` then (2) `emit settingsRequested(category);` -- no other branches/early returns.
    - The "pending Phase 56" log line is removed from BackendContext::forwardSettingsRequest.
    - 56-04 Task 2 spy test (testSettingsDialogOpenFromSidebar) is the automated proof of the two-step ordering (setActivePresetTier runs BEFORE emit settingsRequested): it asserts spy.count()==1, emitted string matches category, AND configViewModel->activePresetTier()==category after the call.
    - testPerOptionDirtyAndValueSource, testSaveSaveAsResetOptionResetGroupResetAll, testPerDialogSearchAndFourLevelMode, testReadonlyBuiltinGating, testSettingsDialogOpenFromSidebar are GREEN (RED scaffolds from 56-01 replaced with real assertions).
    - `cmake --build build --config Release` exits 0 and the listed ViewModelSmokeTest methods pass via ctest.
  </acceptance_criteria>
  <done>ConfigViewModel exposes all per-tier/per-group operations the new dialogs need; BackendContext opens the dialog path correctly; SETTINGS-02/04/05/06 ViewModelSmokeTests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Local desktop settings viewmodel extension; no network input, no auth surface, persistence is local preset files already handled by PresetServiceMock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-SC | Tampering | (none) | accept | No package installs (RESEARCH Package Legitimacy Audit: N/A). |
| T-56-02 | All | ConfigViewModel + BackendContext extensions | accept | Local desktop C++ viewmodel/composition-root; no external input crosses a trust boundary. Builtin-preset read-only gating is preserved (testReadonlyBuiltinGating asserts requestSavePendingChanges refuses builtin). |
</threat_model>

<verification>
- `cmake --build build --config Release` exits 0 after both tasks.
- `ctest --test-dir build -R ViewModelSmokeTest --output-on-failure` runs GREEN for testTabsAndGroupNavPerTier, testPerOptionDirtyAndValueSource, testSaveSaveAsResetOptionResetGroupResetAll, testPerDialogSearchAndFourLevelMode, testReadonlyBuiltinGating, testSettingsDialogOpenFromSidebar.
- After this plan completes, run the canonical `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` for full configure+build+smoke confirmation.
</verification>

<success_criteria>
- All SETTINGS-02/04/05/06 ViewModelSmokeTests are GREEN.
- BackendContext.openSettings() forwards correctly with setActivePresetTier (no more "pending Phase 56" log).
- Static page/group tables exist for all three tiers, populated from upstream Tab.cpp source names.
- The Phase-52 stateChanged -> invalidateAllSliceResults connect still fires after option edits (asserted in 56-04).
</success_criteria>

<output>
Create `.planning/phases/56-parameter-settings-dialogs-restoration/56-02-SUMMARY.md` when done.
</output>
