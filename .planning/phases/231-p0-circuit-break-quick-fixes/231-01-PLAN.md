# Phase 231 Plan — P0 Circuit Break Quick Fixes

**Requirements:** CIRC-01, CIRC-02, CIRC-05, CIRC-06, CIRC-07
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` (all items VERIFIED)

## Plan 1 (single batch)

### Task 1 — CIRC-01: Undo/redo UI dispatch
- `src/qml_gui/pages/PreparePage.qml:199-204` `undoFromTopbar()/redoFromTopbar()`
  currently call `viewport3d.undo()/redo()` which are repaint no-ops
  (`src/qml_gui/Renderer/RhiViewport.h:355-356`). Route them to
  `editorVm.undo()/redo()` (real QUndoStack path, `EditorViewModel.cpp:3791`).
- Verify `EditorViewModel::undo()` triggers model sync + `stateChanged` +
  slice invalidation refresh so the UI actually reflects the undone state
  (upstream: undo restores and repaints whole model).
- Remove the dead `RhiViewport::undo/redo` no-ops (or make them forward) so the
  break cannot regress.

### Task 2 — CIRC-02: CalibMode symbolic constants
- `src/core/services/CalibrationServiceMock.cpp:77/105/131/202/234/265` pin
  stale ints after upstream deleted `Calib_Auto_PA_Line` (enum shifted by one).
  Replace ints with `Slic3r::CalibMode::…` symbolic constants (header include,
  HAS_LIBSLIC3R-guarded as the file already is).
- Fix `tests/QmlUiAuditTests.cpp:5810-5815` stale source-string assertion that
  locks the broken mapping.
- Add a ViewModel test asserting each calibration start path maps to the
  correct `Slic3r::CalibMode` (via `SliceService::setCalibParams` capture).

### Task 3 — CIRC-05: Export all objects as one STL
- `src/core/services/ProjectServiceMock.cpp:1190` `exportModel()` STL/OBJ
  branch exports only `model_->objects.front()`. Merge all object instance
  meshes (mirror `exportSelectedObjects` merge logic in
  `EditorViewModel.cpp:5750`) when no selection-driven caller requested
  single-object semantics.

### Task 4 — CIRC-06: Mojibake strings
- `src/qml_gui/BackendContext.cpp:175` `tr("瀵煎嚭澶辫触")` → English ASCII
  `tr("Export failed")`. Grep repo for other GBK-corrupted tr() literals and
  fix display-only ones (category-matching mojibake in
  ConfigViewModel::materialPresetName is deferred to Phase 232 CIRC-04 where
  the multi-slot model is reworked).

### Task 5 — CIRC-07: CLI --plate honesty
- `src/cli/CliRunner.cpp:69` help says `0=all plates`; implementation slices
  only one plate (`:277-280`). Implement plate-0 = iterate all plates
  (mirroring `requestSliceAll` semantics), or if loop infeasible in CLI flow,
  correct the help/behavior pair truthfully. Prefer implementation.

## Verification

- Canonical build + all suites: `powershell -ExecutionPolicy Bypass -File
  scripts/auto_verify_with_vcvars.ps1`
- New tests: calib-mode mapping test; undo dispatch QML-source audit assertion
  updated (undoFromTopbar → editorVm.undo); export-all merge test.
- Manual: undo button after a move actually reverts the object.
