# Phase 233 Plan — Plate Data Chain

**Requirements:** PLATE-01..05, ENGN-04
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` FIX-05/06/07/09 + ENGN gap (agent-evidenced, patterns pre-studied 2026-08-15)

## Plan 1 (single batch)

### Task 1 — PLATE-01/02: bed type & spiral reach slicing
- `ProjectServiceMock::setPlateBedType` (:5945) / `setPlateSpiralMode` (:5978)
  write only the int/bool field. Add `p->config()` sync exactly like
  buildPlateDataList (:7154-7158): `option("curr_bed_type", true)->setInt(...)`
  (coEnum, setInt valid) and `option<ConfigOptionBool>("spiral_mode", true)`.
  SliceService.cpp:553-564 already applies plate config at slice time.
- Note: load path 3MF restore (:752-762) reads config→field; after this change
  field→config on load must also run (setPlate* not called on load — check
  pendingPlate* restore path writes config like print_sequence does at :1050).

### Task 2 — PLATE-03: first/other-layer filament sequence reaches slicing
- Qt JSON round-trip exists (write :7420-7426, read :8055-8059) but the keys
  `first_layer_print_sequence` / `other_layers_print_sequence` are never
  written into `p->config()` → slicing never consumes them (upstream writes
  them into the plate config block, bbs_3mf.cpp:7458-7500, and GCode consumes).
- Add config sync in `setPlateFirstLayerSeqChoice/Order` and the
  `other_layers` setter (find at :6027+), plus load-path restore, plus
  upstream-format 3MF plate config block write/read (align :7143 block).

### Task 3 — PLATE-04: 3MF project load restores exact plate membership
- Load path force-arranges every load on a 220x220 bed (:1092-1093, :7919-7920)
  → non-locked plate membership collapses to plate 0, empty tail plates
  recycled. Upstream only arranges NEW model imports
  (arrange_loaded_object_to_new_position), never project restore.
- Fix: distinguish project-load from plain import; on project restore, rebuild
  plate membership from the saved per-plate objects_and_instances
  (PartPlate.h:191-220 instance-level membership API) and skip arrangeObjects.
  Keep the arrange for STL/OBJ/plain imports. Watch the thumbnail-restore
  ordering comments (:1097-1105, :7912-7918) — restore pass must still run
  after membership rebuild.
- Also fix instance-level restore: :827-842 dedupes objects_and_instances by
  objIndex and calls addInstance(objIdx, 0) — cross-plate instances lost.
  Store real (obj, instance) pairs.

### Task 4 — PLATE-05: dirty state + guards
- `ProjectViewModel::isDirty` only set by importModel (:93-98). Wire real
  edits: subscribe BackendContext (composition root) to
  ProjectServiceMock::projectChanged / undo stack push → set isDirty; clear on
  successful save/saveAs (BackendContext.cpp:627-666).
- Add unsaved-changes guard (ConfirmDialog) on new project / open project /
  window close when isDirty (upstream close_with_confirm, Plater.cpp:8901).

### Task 5 — ENGN-04: slice-all skips empty plates
- `requestSliceAll` (EditorViewModel.cpp:7155-7186) only excludes locked/
  unprintable; an empty plate fails → sliceFailed clears the whole queue
  (:3888-3895). Filter empty plates (no printable objects) before queueing,
  mirroring upstream slice_all.

## Verification

- Canonical build + all suites.
- New tests (PartPlateTests/ViewModelSmokeTests): setPlateBedType writes
  config key; spiral same; filament-seq keys land in plate config and 3MF
  round-trip; multi-plate 3MF save→load preserves per-plate membership
  without arrange; dirty set on transform, cleared on save; slice-all with an
  empty plate completes other plates.
