---
plan_id: 20-01
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
requirements: [PLATE-12, PLATE-13, PLATE-14]
autonomous: true
---

# Plan 20-01: v3.0 Verification and Handoff

<objective>
Final v3.0 evidence bundle: run canonical verification one last time after all v3.0 work, confirm smoke coverage spans the new PartPlate behavior, complete the requirement traceability, and record v3.1 handoff notes. No new features — pure verification + documentation.
</objective>

<must_haves>
  <truth>canonical auto_verify_with_vcvars.ps1 exits 0 after all v3.0 work</truth>
  <truth>ViewModelSmokeTests.exe run explicitly with documented counts (43 passed / 1 skipped)</truth>
  <truth>QmlUiAuditTests.exe run explicitly (7 passed)</truth>
  <truth>REQUIREMENTS.md traceability: all 14 PLATE requirements marked Complete with evidence</truth>
  <truth>v3.1 handoff notes captured (AssembleView, m_print_list caching, wipe-tower, test-fixture gap)</truth>
</must_haves>

<tasks>

  <task id="20-01-T1">
    <action>
Run the canonical verification command one final time to confirm the full v3.0 build is green: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`. Then run `build/ViewModelSmokeTests.exe` and `build/QmlUiAuditTests.exe` explicitly and capture the totals. This is the PLATE-12 gate.
    </action>
    <read_first>
- `.codex/rules/build-rules.md`
</read_first>
    <acceptance_criteria>
- `auto_verify_with_vcvars.ps1` exits 0
- `ViewModelSmokeTests.exe` reports 43 passed, 0 failed, 1 skipped (Phase 18 round-trip QSKIP)
- `QmlUiAuditTests.exe` reports 7 passed, 0 failed
- E2E pipeline passed
</acceptance_criteria>
  </task>

  <task id="20-01-T2">
    <action>
Complete the REQUIREMENTS.md traceability table for all 14 PLATE requirements — mark each Complete and cite the evidence (which phase + which files + which test). This is the PLATE-14 deliverable. Use the per-phase VERIFICATION.md + SUMMARY.md as the evidence source. For PLATE-09 (the partial), mark Complete with the honest note that the full round-trip test is QSKIP'd (test-fixture limitation) and the code path is verified by build + inspection.
    </action>
    <read_first>
- `.planning/REQUIREMENTS.md` (current traceability — all "Not started")
- `.planning/phases/16-*/16-VERIFICATION.md`, `17-VERIFICATION.md`, `18-VERIFICATION.md`, `19-VERIFICATION.md` (evidence sources)
- The phase SUMMARYs (requirements-completed frontmatter)
</read_first>
    <acceptance_criteria>
- REQUIREMENTS.md traceability table has all 14 PLATE requirements marked Complete (status + phase)
- Each row cites concrete evidence (file/test/phase)
- PLATE-09 marked Complete with the partial-test note
</acceptance_criteria>
  </task>

  <task id="20-01-T3">
    <action>
Write 20-01-SUMMARY.md (final v3.0 phase summary) and 20-VERIFICATION.md (phase-20 gate). Update STATE.md to "Phase 20 complete (5/5 phases); v3.0 milestone complete". Update ROADMAP.md progress (Phase 20 → 1/1). Add v3.1 handoff notes to STATE.md deferred items (AssembleView PLATE-15, m_print_list caching, per-plate wipe-tower, test-fixture gap for PLATE-09). This is the PLATE-13 deliverable.
    </action>
    <read_first>
- `.planning/STATE.md` (current state)
- `.planning/ROADMAP.md` (progress table)
- The phase 16-19 SUMMARY/VERIFICATION files (input for the final summary)
</read_first>
    <acceptance_criteria>
- 20-01-SUMMARY.md + 20-VERIFICATION.md exist with status passed
- STATE.md shows Phase 20 complete, v3.0 milestone complete
- ROADMAP.md Phase 20 → 1/1 complete
- v3.1 handoff notes recorded
</acceptance_criteria>
  </task>

</tasks>

<verification>
- Canonical build exits 0
- 43 smoke tests pass (1 skipped, documented)
- 7 QML audit tests pass
- All 14 PLATE requirements marked Complete in traceability
- v3.0 milestone is ready for /gsd-audit-milestone v3.0
</verification>
