---
phase: 89-assembleview-source-truth-gap-audit
plan: 01
type: audit
wave: 1
depends_on: []
files_modified:
  - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
  - .planning/phases/89-assembleview-source-truth-gap-audit/89-VERIFICATION.md
  - .planning/phases/89-assembleview-source-truth-gap-audit/89-01-SUMMARY.md
autonomous: true
requirements: [ASMAUDIT-01, ASMAUDIT-02]
---

# Phase 89 Plan: AssembleView Source-Truth Gap Audit

## Objective

Create the canonical v4.2 AssembleView gap matrix (`89-GAP-MATRIX.md`)
before any implementation. The matrix must let Phase 90-93 restore AssembleView
without re-discovering the three target screenshots, the OrcaSlicer source
anchors (`GUI_Preview.hpp`, `GLCanvas3D.hpp`, `GLGizmoAssembly.hpp`,
`GLGizmosCommon.hpp`, `Plater.cpp`), the current Qt placeholder gap
(`Plater.qml:102-115`, `BackendContext.h:159,193-199,227`), the replacement
decisions, the out-of-scope Arrange classification, the owner phases, or the
verification routes.

This phase is read-only: it modifies documentation only and produces no
production QML/C++ changes.

## Requirements

- `ASMAUDIT-01`: The AssembleView surface has a current inventory mapping
  screenshot-visible regions and upstream capabilities (explosion ratio,
  Assembly gizmo, data pool, Plater routing) to OrcaSlicer source files, Qt
  targets, replacement decisions, and verification evidence.
- `ASMAUDIT-02`: The existing `Plater.qml` placeholder (`AssembleView = 2` enum
  + "装配视图暂不可用" stub) is reconciled into a replacement plan with
  explicit removal of the placeholder code path.

## Must Haves

<must_haves>
  <truth id="D-01">The matrix uses `shotScreen/装配页.png`, `shotScreen/装配页_爆炸.png`, and `shotScreen/装配页_测量.png` as the three visual-truth screenshots.</truth>
  <truth id="D-02">Behavior truth maps to OrcaSlicer `GUI_Preview.hpp:180` (AssembleView class), `GLCanvas3D.hpp:509-513` (CanvasAssembleView enum), `GLCanvas3D.hpp:596,770-771` (explosion ratio), `Gizmos/GLGizmoAssembly.hpp:9` + `.cpp:25-68` (Assembly gizmo), `Gizmos/GLGizmosCommon.hpp:268,274,299` (data pool), and `Plater.cpp:4959,4431,7322,11601,11635,11744,11823` (routing branches).</truth>
  <truth id="D-03">Phase 89 does not claim runtime pixel parity; Phase 93 owns runtime visual proof.</truth>
  <truth id="D-04">The deliverable is `89-GAP-MATRIX.md` modeled after the Phase 84 settings gap matrix (`84-GAP-MATRIX.md`).</truth>
  <truth id="D-05">The matrix uses AssembleView-specific region IDs: `ASM-SHELL`, `ASM-CANVAS`, `ASM-EXPLODE-SLIDER`, `ASM-EXPLODE-RENDER`, `ASM-ASSEMBLY-GIZMO`, `ASM-MEASURE-OVERLAY`, `ASM-ASSEMBLY-INFO`, `ASM-DATA-POOL`, `ASM-PLATER-ROUTING`, `ASM-NAVIGATION`, `ASM-CLEANUP`.</truth>
  <truth id="D-06">The matrix uses a multi-column table: Region | Target Observation | Current Evidence | Qt Targets | Upstream Source | Decision | Gap | Severity | Owner | Requirement | Verification.</truth>
  <truth id="D-07">A region is incomplete if it is visually off-target, mojibake/raw/internal, disconnected, missing upstream mapping, or missing a verification route.</truth>
  <truth id="D-08">Arrange (auto-arrangement) and `assembleObjects` (multi-part merge) are explicitly classified as out-of-scope because they are already implemented; no later phase re-touches them.</truth>
  <truth id="D-09">The current `Plater.qml:102-115` placeholder (`Item` id `assembleSlot` + "装配视图暂不可用" `Text`) is classified for removal/replacement, owned by Phase 90.</truth>
  <truth id="D-10">The `BackendContext.h:159` `vmAssembleView` Q_PROPERTY and `BackendContext.h:199` `ViewMode::AssembleView = 2` enum entry are preserved as the canvas-type routing anchor (mirroring `ECanvasType::CanvasAssembleView = 2`), not removed.</truth>
  <truth id="D-11">The matrix records that no AssemblePage.qml, AssembleViewModel, explosion-ratio support, Assembly gizmo, data pool, or navigation entry currently exist on the Qt side — these are the gaps Phase 90-93 fill.</truth>
  <truth id="D-12">Phase 90 owns shell + canvas host + navigation + Plater routing (ASMSHELL-01, ASMSHELL-02, ASMROUTE-01); Phase 91 owns explosion ratio + assembly rendering (ASMEXPLODE-01, ASMEXPLODE-02); Phase 92 owns the Assembly measurement gizmo (ASMMEASURE-01, ASMMEASURE-02); Phase 93 owns data pool + verification + cleanup (ASMROUTE-02, ASMVERIFY-01, ASMVERIFY-02).</truth>
  <truth id="D-13">Removed network/device/cloud/Monitor/ModelMall/camera/printer-hardware scope is not reintroduced; the matrix must confirm it stays out of scope.</truth>
  <truth id="D-14">Phase 89 verification is source/document focused: gap-matrix completeness, requirement coverage, `git diff --check`, and the encoding guard. The canonical build is NOT run because no product source files change.</truth>
  <truth id="D-15">Phase 93 must record runtime AssembleView visual evidence against all three target screenshots.</truth>
  <truth id="D-16">The placeholder removal plan is explicit: the "装配视图暂不可用" `Text` and the placeholder `Item` slot are removed in Phase 90 and replaced by a real AssembleView canvas host; the `ViewMode::AssembleView` enum entry and `vmAssembleView` accessor are retained as the routing anchor.</truth>
</must_haves>

## Tasks

<tasks>
  <task id="89-01-01" type="audit">
    <title>Create AssembleView region matrix skeleton from visual truth</title>
    <read_first>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-CONTEXT.md
      - .planning/phases/84-settings-source-truth-gap-audit/84-GAP-MATRIX.md
      - .planning/ROADMAP.md
      - .planning/REQUIREMENTS.md
      - shotScreen/装配页.png
      - shotScreen/装配页_爆炸.png
      - shotScreen/装配页_测量.png
    </read_first>
    <files>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
    </files>
    <action>
      Create `89-GAP-MATRIX.md` modeled on `84-GAP-MATRIX.md`. Open with a
      summary that states Phase 89 is the v4.2 source-truth audit freezing the
      AssembleView region map before implementation, and that this matrix is the
      canonical routing artifact for Phase 90-93. Add a Target Evidence section
      naming all three screenshot paths exactly: `shotScreen/装配页.png`,
      `shotScreen/装配页_爆炸.png`, `shotScreen/装配页_测量.png`. Add a
      Canonical Region Matrix table with columns Region, Target Observation,
      Current Evidence, Qt Targets, Upstream Source, Decision, Gap, Severity,
      Owner, Requirement, Verification. Seed one row per region ID: `ASM-SHELL`,
      `ASM-CANVAS`, `ASM-EXPLODE-SLIDER`, `ASM-EXPLODE-RENDER`,
      `ASM-ASSEMBLY-GIZMO`, `ASM-MEASURE-OVERLAY`, `ASM-ASSEMBLY-INFO`,
      `ASM-DATA-POOL`, `ASM-PLATER-ROUTING`, `ASM-NAVIGATION`, `ASM-CLEANUP`.
      For `ASM-EXPLODE-SLIDER` and `ASM-EXPLODE-RENDER` cite the
      `shotScreen/装配页_爆炸.png` observation (Explosion Ratio = 3.00, radial
      separation, yellow dashed connector guide lines). For `ASM-ASSEMBLY-GIZMO`
      and `ASM-MEASURE-OVERLAY` cite the `shotScreen/装配页_测量.png`
      observation (right-side "测量" panel, white dashed dimension lines with
      arrowheads, teal measurement-value boxes e.g. `90.000°`, "选中 N 平面"
      indicators).
    </action>
    <verify>
      `rg -n "ASM-SHELL|ASM-CANVAS|ASM-EXPLODE-SLIDER|ASM-EXPLODE-RENDER|ASM-ASSEMBLY-GIZMO|ASM-MEASURE-OVERLAY|ASM-ASSEMBLY-INFO|ASM-DATA-POOL|ASM-PLATER-ROUTING|ASM-NAVIGATION|ASM-CLEANUP" .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md`
    </verify>
    <acceptance_criteria>
      - `89-GAP-MATRIX.md` exists.
      - The matrix table header lists all 11 columns in order: Region, Target Observation, Current Evidence, Qt Targets, Upstream Source, Decision, Gap, Severity, Owner, Requirement, Verification.
      - The matrix includes all 11 required `ASM-*` region IDs as rows.
      - The Target Evidence section names all three screenshot paths exactly.
      - The explosion rows reference `shotScreen/装配页_爆炸.png`; the measurement rows reference `shotScreen/装配页_测量.png`.
    </acceptance_criteria>
  </task>

  <task id="89-01-02" type="audit">
    <title>Map current Qt targets and OrcaSlicer source anchors per region</title>
    <read_first>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-CONTEXT.md
      - src/qml_gui/pages/Plater.qml
      - src/qml_gui/BackendContext.h
      - src/qml_gui/BackendContext.cpp
      - src/qml_gui/main.qml
      - third_party/OrcaSlicer/src/slic3r/GUI/GUI_Preview.hpp
      - third_party/OrcaSlicer/src/slic3r/GUI/GLCanvas3D.hpp
      - third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/GLGizmoAssembly.hpp
      - third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/GLGizmoAssembly.cpp
      - third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/GLGizmosCommon.hpp
      - third_party/OrcaSlicer/src/slic3r/GUI/Plater.cpp
    </read_first>
    <files>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
    </files>
    <action>
      Populate every matrix row with concrete Qt target paths and upstream
      source anchors. Specifically:
      - `ASM-SHELL`: Qt targets `src/qml_gui/pages/Plater.qml:102-115` (placeholder `assembleSlot` Item), a future `src/qml_gui/pages/AssemblePage.qml` (not yet present); upstream `third_party/OrcaSlicer/src/slic3r/GUI/GUI_Preview.hpp:180` (`class AssembleView : public wxPanel`).
      - `ASM-CANVAS`: Qt targets the GLViewport canvas host (`src/qml_gui/Renderer/GLViewport.*`, `src/qml_gui/Renderer/GLViewportRenderer.*`); upstream `third_party/OrcaSlicer/src/slic3r/GUI/GLCanvas3D.hpp:509-513` (`ECanvasType::CanvasAssembleView = 2`).
      - `ASM-EXPLODE-SLIDER`: upstream `third_party/OrcaSlicer/src/slic3r/GUI/GLCanvas3D.hpp:596` (`m_explosion_ratio = 1.0`), `770-771` (`get_explosion_ratio` / `reset_explosion_ratio`).
      - `ASM-EXPLODE-RENDER`: same upstream anchors plus per-volume separation rendering.
      - `ASM-ASSEMBLY-GIZMO`: upstream `third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/GLGizmoAssembly.hpp:9` and `Gizmos/GLGizmoAssembly.cpp:25-68` (`ONLY_ASSEMBLY` measure mode).
      - `ASM-MEASURE-OVERLAY`: upstream `GLGizmoAssembly.hpp` / `GLGizmoMeasure.hpp` overlay rendering.
      - `ASM-ASSEMBLY-INFO`: Qt target a future panel component (not yet present); upstream Assembly Info panel.
      - `ASM-DATA-POOL`: upstream `third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/GLGizmosCommon.hpp:268,274,299` (`AssembleViewDataID`, `AssembleViewDataPool`, `AssembleViewDataBase`).
      - `ASM-PLATER-ROUTING`: upstream `third_party/OrcaSlicer/src/slic3r/GUI/Plater.cpp:4959,4431,7322,11601,11635,11744,11823`; Qt target `src/qml_gui/BackendContext.h:159,199,227` and `src/qml_gui/BackendContext.cpp:362,376`.
      - `ASM-NAVIGATION`: Qt target `src/qml_gui/main.qml` (no navigation entry to AssembleView currently exists).
      - `ASM-CLEANUP`: Qt targets the placeholder removal candidates.
      Classify each region's Decision as `replace`, `add`, `preserve`, or
      `remove`. Mark the placeholder `assembleSlot` Item and "装配视图暂不可用"
      `Text` as `remove` (owned by Phase 90). Mark the `ViewMode::AssembleView`
      enum entry and `vmAssembleView` accessor as `preserve` (routing anchor).
      Assign Severity per gap: `ASM-CANVAS`, `ASM-PLATER-ROUTING`, and
      `ASM-EXPLODE-RENDER` as Critical; `ASM-SHELL`, `ASM-EXPLODE-SLIDER`,
      `ASM-ASSEMBLY-GIZMO`, `ASM-NAVIGATION`, `ASM-DATA-POOL` as High;
      `ASM-MEASURE-OVERLAY`, `ASM-ASSEMBLY-INFO` as Medium; `ASM-CLEANUP` as
      High. Assign Owner per the ROADMAP (Phase 90/91/92/93) and Requirement
      per REQUIREMENTS.md (ASMSHELL-01/02, ASMROUTE-01, ASMEXPLODE-01/02,
      ASMMEASURE-01/02, ASMROUTE-02, ASMVERIFY-01/02).
    </action>
    <verify>
      `rg -n "Plater.qml|BackendContext.h|GUI_Preview.hpp|GLCanvas3D.hpp|GLGizmoAssembly|GLGizmosCommon.hpp|Plater.cpp|CanvasAssembleView|m_explosion_ratio|ONLY_ASSEMBLY|AssembleViewDataPool" .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md`
    </verify>
    <acceptance_criteria>
      - Every matrix row has at least one Qt target path or an explicit `N/A` with a reason.
      - Every behavior row has at least one OrcaSlicer upstream source path with a line anchor.
      - `ASM-CANVAS` cites `GLCanvas3D.hpp:509-513` (`CanvasAssembleView = 2`).
      - `ASM-EXPLODE-SLIDER` cites `GLCanvas3D.hpp:596,770-771`.
      - `ASM-ASSEMBLY-GIZMO` cites `GLGizmoAssembly.hpp:9` and `.cpp:25-68`.
      - `ASM-DATA-POOL` cites `GLGizmosCommon.hpp:268,274,299`.
      - `ASM-PLATER-ROUTING` cites `Plater.cpp:4959,4431,7322,11601,11635,11744,11823` and `BackendContext.h:159,199,227`.
      - The placeholder `assembleSlot` Item and "装配视图暂不可用" Text are classified `remove`.
      - The `ViewMode::AssembleView` enum entry and `vmAssembleView` accessor are classified `preserve`.
    </acceptance_criteria>
  </task>

  <task id="89-01-03" type="audit">
    <title>Reconcile Plater.qml placeholder and out-of-scope classification (ASMAUDIT-02)</title>
    <read_first>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-CONTEXT.md
      - .planning/REQUIREMENTS.md
      - .planning/ROADMAP.md
      - src/qml_gui/pages/Plater.qml
      - src/qml_gui/BackendContext.h
      - src/core/services/ProjectServiceMock.cpp
      - src/core/viewmodels/EditorViewModel.h
    </read_first>
    <files>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
    </files>
    <action>
      Add a Placeholder Reconciliation section to `89-GAP-MATRIX.md` covering
      `ASMAUDIT-02`. State explicitly:
      - The `Plater.qml:102-115` placeholder `Item` (id `assembleSlot`) and its
        "装配视图暂不可用" `Text` are removed in Phase 90 and replaced by a
        real AssembleView canvas host.
      - The `BackendContext.h:199` `ViewMode::AssembleView = 2` enum entry, the
        `BackendContext.h:159` `vmAssembleView` Q_PROPERTY, and the
        `BackendContext.h:227` `vmAssembleView()` accessor are retained as the
        canvas-type routing anchor mirroring upstream
        `ECanvasType::CanvasAssembleView = 2`.
      - The `BackendContext.cpp:362,376` `kLastVm` boundary using AssembleView
        stays valid once Phase 90 wires the real host.
      Add an Out-of-Scope Classification section that records:
      - Arrange (auto-arrangement) is already implemented
        (`ProjectServiceMock.cpp:2521-2592` calls real
        `Slic3r::arrange_objects`; `EditorViewModel.h:595-656` arrange settings
        properties; `PreparePage.qml` full arrange UI) and is NOT re-touched by
        Phase 90-93.
      - `assembleObjects` (multi-part merge, `EditorViewModel.cpp:3217`) is
        already implemented and distinct from the AssembleView canvas; out of
        scope.
      - Removed network/device/cloud/Monitor/ModelMall/camera/printer-hardware
        scope stays out of scope unless the user explicitly reopens it.
      - D3D12/Vulkan backend promotion and libslic3r slicing algorithm changes
        stay out of scope.
      Ensure the v4.2 Requirement Routing table covers every requirement:
      ASMAUDIT-01, ASMAUDIT-02 (Phase 89), ASMSHELL-01, ASMSHELL-02, ASMROUTE-01
      (Phase 90), ASMEXPLODE-01, ASMEXPLODE-02 (Phase 91), ASMMEASURE-01,
      ASMMEASURE-02 (Phase 92), ASMROUTE-02, ASMVERIFY-01, ASMVERIFY-02
      (Phase 93), each mapped to its matrix region(s).
    </action>
    <verify>
      `rg -n "ASMAUDIT-01|ASMAUDIT-02|ASMSHELL|ASMROUTE|ASMEXPLODE|ASMMEASURE|ASMVERIFY|Arrange|assembleObjects|assembleSlot|装配视图暂不可用|ViewMode::AssembleView|CanvasAssembleView|Phase 90|Phase 91|Phase 92|Phase 93" .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md`
    </verify>
    <acceptance_criteria>
      - A Placeholder Reconciliation section exists and names `assembleSlot`, "装配视图暂不可用", `BackendContext.h:199`, `vmAssembleView`, and `CanvasAssembleView = 2`.
      - The section states the placeholder Item/Text are removed in Phase 90 while the enum entry/accessor are retained.
      - An Out-of-Scope Classification section names Arrange, `assembleObjects`, network/device/cloud scope, and D3D12/Vulkan as out of scope.
      - Arrange references the existing implementation anchors (`ProjectServiceMock.cpp`, `EditorViewModel.h`, `PreparePage.qml`).
      - The Requirement Routing table lists all 12 v4.2 requirements with their owner phase and matrix region(s).
      - `ASMAUDIT-01` and `ASMAUDIT-02` are explicitly covered in a Requirement Coverage section.
    </acceptance_criteria>
  </task>

  <task id="89-01-04" type="audit">
    <title>Verify and close the Phase 89 audit plan</title>
    <read_first>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md
      - .planning/phases/84-settings-source-truth-gap-audit/84-VERIFICATION.md
      - .codex/rules/source-truth-migration.md
      - .codex/rules/build-rules.md
    </read_first>
    <files>
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-VERIFICATION.md
      - .planning/phases/89-assembleview-source-truth-gap-audit/89-01-SUMMARY.md
    </files>
    <action>
      Create `89-VERIFICATION.md` recording the Phase 89 source/document
      checks: gap-matrix region-ID coverage (all 11 `ASM-*` IDs present),
      column completeness (11 columns), upstream-anchor citations present,
      placeholder reconciliation present, requirement coverage for ASMAUDIT-01
      and ASMAUDIT-02. Explicitly state that the canonical build
      (`scripts/auto_verify_with_vcvars.ps1`) was NOT run because Phase 89
      changed documentation only and touched no production QML/C++ source.
      Create `89-01-SUMMARY.md` listing completed tasks, artifacts produced
      (`89-GAP-MATRIX.md`, `89-VERIFICATION.md`, `89-01-SUMMARY.md`), exact
      commands run, deviations (none expected), and a self-check confirming
      Phase 89 produced no production source changes. Run `git diff --check`
      and the encoding guard against all three Phase 89 artifacts. Do not run
      `scripts/auto_verify_with_vcvars.ps1` unless product source files were
      modified unexpectedly.
    </action>
    <verify>
      `git diff --check`
      `python %USERPROFILE%\.coding-encoding-guard\encoding_guard.py .planning/phases/89-assembleview-source-truth-gap-audit/89-GAP-MATRIX.md .planning/phases/89-assembleview-source-truth-gap-audit/89-VERIFICATION.md .planning/phases/89-assembleview-source-truth-gap-audit/89-01-SUMMARY.md`
    </verify>
    <acceptance_criteria>
      - `89-VERIFICATION.md` records all source/document checks and states the canonical build was not run with the reason.
      - `89-01-SUMMARY.md` lists the three audit artifacts and the exact commands run.
      - `89-VERIFICATION.md` confirms all 11 `ASM-*` region IDs and all 11 columns are present in the matrix.
      - `89-VERIFICATION.md` confirms ASMAUDIT-01 and ASMAUDIT-02 are covered.
      - `git diff --check` exits 0.
      - The encoding guard exits 0 for all three Phase 89 artifacts.
    </acceptance_criteria>
  </task>
</tasks>

## Verification

Run these checks before marking the plan complete:

1. `rg -n "ASM-SHELL|ASM-CANVAS|ASM-EXPLODE-SLIDER|ASM-EXPLODE-RENDER|ASM-ASSEMBLY-GIZMO|ASM-MEASURE-OVERLAY|ASM-ASSEMBLY-INFO|ASM-DATA-POOL|ASM-PLATER-ROUTING|ASM-NAVIGATION|ASM-CLEANUP" .planning/phases/89-assembleview-source-truth-gap-audit/89-01-PLAN.md`
2. `rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|D-14|D-15|D-16" .planning/phases/89-assembleview-source-truth-gap-audit/89-01-PLAN.md`
3. `rg -n "ASMAUDIT-01|ASMAUDIT-02" .planning/phases/89-assembleview-source-truth-gap-audit/89-01-PLAN.md`
4. `git diff --check`
5. `python %USERPROFILE%\.coding-encoding-guard\encoding_guard.py .planning/phases/89-assembleview-source-truth-gap-audit/89-CONTEXT.md .planning/phases/89-assembleview-source-truth-gap-audit/89-01-PLAN.md`

## Success Criteria

- `89-01-PLAN.md` exists with valid frontmatter.
- The plan covers `ASMAUDIT-01` and `ASMAUDIT-02`.
- Every context decision `D-01` through `D-16` is represented in `must_haves`.
- Every task has `read_first`, `action`, `verify`, and `acceptance_criteria`.
- Phase 89 remains documentation/source audit only and does not modify product
  QML/C++.
- The plan routes final visual/build/runtime evidence to Phase 93.

## Expected Non-Changes

- No production QML/C++ changes in Phase 89.
- No canonical full build in Phase 89 unless execution unexpectedly touches
  production source files.
- No LAN/device/network/cloud scope.
- No D3D12/Vulkan backend work.
- No Arrange or `assembleObjects` changes (both already implemented).
