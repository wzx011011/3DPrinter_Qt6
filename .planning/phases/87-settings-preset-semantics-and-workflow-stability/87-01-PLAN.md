# Phase 87 Plan: Settings Preset Semantics And Workflow Stability

## Goal

Finish settings preset workflow semantics for v4.1 without touching LAN/device/network scope.

## Tasks

1. Add failing tests.
   - Add QML audit coverage that `SettingsDialog` listens to `ConfigViewModel::pendingUnsavedChangesRequested`.
   - Assert Cancel calls `requestCancelPendingChanges`.
   - Assert Discard calls `requestDiscardPendingChanges`.
   - Assert save-as remains connected through `saveAsRequired`.
   - Add ViewModel smoke coverage for dirty preset switch: queue, cancel, discard/apply, and save/apply.

2. Wire QML dirty-pending flow.
   - Add `Connections` in `SettingsDialog` for `pendingUnsavedChangesRequested`.
   - Open `UnsavedChangesDialog` when the active dirty tier queues a preset/scope action.
   - On dialog cancel/reject, call `requestCancelPendingChanges`.
   - Keep close dirty guard behavior unchanged.

3. Preserve reset/filter/slice semantics.
   - Keep `filterOptionIndices` + page filtering per dialog.
   - Keep dirty/error top-row markers outside the filtered option list.
   - Confirm option edits still emit `sliceAffectingConfigChanged`.
   - Confirm opening settings does not invalidate Prepare/Preview state.

4. Verify.
   - Targeted QML audit tests.
   - Targeted ViewModel smoke tests.
   - Encoding guard and `git diff --check`.
   - Canonical verifier: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`.

## Expected Files

- `src/qml_gui/dialogs/SettingsDialog.qml`
- `tests/QmlUiAuditTests.cpp`
- `tests/ViewModelSmokeTests.cpp`
- `.planning/phases/87-settings-preset-semantics-and-workflow-stability/*`
