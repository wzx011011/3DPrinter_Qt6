---
plan_id: 19-01
wave: 1
depends_on: []
files_modified:
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
  - src/core/services/SliceService.cpp
  - tests/ViewModelSmokeTests.cpp
requirements: [PLATE-10, PLATE-11]
autonomous: true
---

# Plan 19-01: Per-Plate Config Merge + Scoped-Value Stub Fix

<objective>
Honor the full per-plate config during slicing (PLATE-10) by replacing the 3-hardcoded-key patch with a full DynamicPrintConfig merge; fix the plateScopedOptionValue/setPlateScopedOptionValue HAS stubs so per-plate config is readable/writable end-to-end (D-16); confirm/document that Qt6's stack-local per-slice Print already satisfies PLATE-11's per-plate-isolation requirement (D-14).
</objective>

<must_haves>
  <truth>plateScopedOptionValue under HAS_LIBSLIC3R reads from PartPlate::config().option(key), not returns fallback stub</truth>
  <truth>setPlateScopedOptionValue under HAS_LIBSLIC3R writes into PartPlate::config() and returns true</truth>
  <truth>SliceService merges the full per-plate DynamicPrintConfig (config.apply) instead of 3 hardcoded keys</truth>
  <truth>PLATE-11 per-plate isolation documented: stack-local Print per startSlicePlate already isolates plates</truth>
  <truth>Deterministic test: set a per-plate override → slice config reflects it (not just bed_type/print_seq/spiral)</truth>
  <truth>canonical verify exits 0; existing tests don't regress</truth>
</must_haves>

<context_source>
Decisions D-14 (stack-local Print = per-plate context), D-15 (full config.apply merge), D-16 (plateScoped stub fix) from `.planning/phases/19-per-plate-slice-scheduling/19-CONTEXT.md`.
</context_source>

<tasks>

  <task id="19-01-T1">
    <action>
Fix the `plateScopedOptionValue` and `setPlateScopedOptionValue` HAS_LIBSLIC3R stubs in `ProjectServiceMock.cpp` (D-16).

`plateScopedOptionValue(plateIdx, key, fallback)`: under HAS_LIBSLIC3R, get `const OWzx::PartPlate* p = m_plateList ? m_plateList->plate(plateIdx) : nullptr;`. If p: `const Slic3r::DynamicPrintConfig& cfg = p->config();` then `if (const auto* opt = cfg.option(key.toUtf8().constData()))` → convert opt to QVariant by type: ConfigOptionInt→toInt, ConfigOptionFloat→toDouble, ConfigOptionBool→toBool, ConfigOptionString→toString. Use `dynamic_cast<const Slic3r::ConfigOptionInt*>(opt)` (or the option type check) to dispatch. Return the QVariant, or fallback if no opt. Remove the `return fallbackValue; // TODO` stub.

`setPlateScopedOptionValue(plateIdx, key, value)`: under HAS_LIBSLIC3R, get `OWzx::PartPlate* p = m_plateList ? m_plateList->plate(plateIdx) : nullptr;`. If !p return false. `Slic3r::DynamicPrintConfig& cfg = p->config(); auto* opt = cfg.option(key.toUtf8().constData(), true);` (create=true so missing keys are created). Dispatch by value.type(): QMetaType::Int/LongLong→opt must be ConfigOptionInt (cast + setInt), Double→ConfigOptionFloat (setDouble), Bool→ConfigOptionBool (setBool), QString→ConfigOptionString (setString). Return true. Remove the `return false` stub.

Add `#include <libslic3r/PrintConfig.hpp>` if not already (for ConfigOptionInt etc.). The non-HAS mock path (m_mockPlateOverrides) stays as-is.
    </action>
    <read_first>
- `src/core/services/ProjectServiceMock.cpp` plateScopedOptionValue/setPlateScopedOptionValue (~line 1312-1342) — the stubs being fixed
- `src/core/model/PartPlate.h` — config() accessor under HAS_LIBSLIC3R
- `third_party/OrcaSlicer/src/libslic3r/Config.hpp` — ConfigOptionInt/Float/Bool/String class hierarchy + getInt/getDouble/getBool/getString + setInt/setDouble/setBool/setString
- `.planning/phases/19-per-plate-slice-scheduling/19-CONTEXT.md` (D-16)
    </read_first>
    <acceptance_criteria>
- `plateScopedOptionValue` HAS branch no longer contains `return fallbackValue; // TODO` as the sole behavior (it reads p->config())
- `setPlateScopedOptionValue` HAS branch returns true on success (not `return false`)
- Both handle Int/Float/Bool/String ConfigOption types via dispatch
- File compiles (T4 build)
    </acceptance_criteria>
  </task>

  <task id="19-01-T2">
    <action>
Add a `plateDynamicConfig(int plateIdx)` accessor to ProjectServiceMock (D-15 enabler) returning the per-plate DynamicPrintConfig so SliceService can merge it without reaching into PartPlate internals. Declare in `ProjectServiceMock.h` (under HAS_LIBSLIC3R, Q_INVOKABLE not needed — internal): `const Slic3r::DynamicPrintConfig* plateDynamicConfig(int plateIdx) const;`. Implement in .cpp: `const OWzx::PartPlate* p = m_plateList ? m_plateList->plate(plateIdx) : nullptr; return p ? &p->config() : nullptr;`. Non-HAS returns nullptr.
    </action>
    <read_first>
- `src/core/services/ProjectServiceMock.h` (existing plate accessors — add alongside)
- `src/core/model/PartPlate.h` — config() returns DynamicPrintConfig&
    </read_first>
    <acceptance_criteria>
- `grep "plateDynamicConfig" src/core/services/ProjectServiceMock.h` returns a match
- Returns &p->config() under HAS_LIBSLIC3R, nullptr otherwise/invalid
    </acceptance_criteria>
  </task>

  <task id="19-01-T3">
    <action>
Replace the 3-hardcoded-key patch in SliceService with a full per-plate config merge (D-15). In `SliceService.cpp` at lines 379-403 (the "Apply per-plate config overrides" block), replace the 3 if-blocks (curr_bed_type/print_sequence/spiral_mode) with a full merge:

```cpp
// v3.0 Phase 19 (D-15): merge the FULL per-plate config (not just 3 keys),
// aligning with upstream update_slice_context_to_current_plate. Per-plate
// overrides take precedence over preset config.
if (receiver && receiver->projectService_) {
  const int plateIdx = targetPlateIndex >= 0 ? targetPlateIndex
                       : receiver->projectService_->currentPlateIndex();
  if (plateIdx >= 0) {
    if (const Slic3r::DynamicPrintConfig* plateCfg =
            receiver->projectService_->plateDynamicConfig(plateIdx)) {
      config.apply(*plateCfg);  // merge plate config into base; plate wins
    }
  }
}
```

Verify `DynamicPrintConfig::apply(const DynamicPrintConfig&)` is the correct merge API and direction (other's values override this's). If `apply`'s direction is reversed in libslic3r, use the equivalent (e.g. `plateCfg->apply(config); std::swap` or `config = *plateCfg + config`-style) — confirm by reading libslic3r Config.hpp apply() semantics: apply(other) typically means "for keys in other, set them in this". So `config.apply(*plateCfg)` makes plate values win. Confirm via Config.hpp before finalizing.

This block sits BEFORE `print.apply(*modelForSlice, config)` (line 450), so the merged config is what the Print slices with.
    </action>
    <read_first>
- `src/core/services/SliceService.cpp:370-405` (the config-build path; the 3-key block at 379-403 is the replacement target; it runs before print.apply at 450)
- `third_party/OrcaSlicer/src/libslic3r/Config.hpp` — DynamicPrintConfig::apply semantics (direction: which config wins)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp` update_slice_context — how upstream applies plate config
- `.planning/phases/19-per-plate-slice-scheduling/19-CONTEXT.md` (D-15)
    </read_first>
    <acceptance_criteria>
- The 3 if-blocks (curr_bed_type/print_sequence/spiral_mode hardcoded patch) are GONE from SliceService.cpp
- `config.apply(*plateCfg)` (or equivalent full merge) is present
- `grep -c "curr_bed_type\|print_sequence\|spiral_mode" src/core/services/SliceService.cpp` returns 0 (no hardcoded key patching)
- File compiles (T4 build)
    </acceptance_criteria>
  </task>

  <task id="19-01-T4">
    <action>
Run canonical verify: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`. Confirms DynamicPrintConfig API usage (apply direction, ConfigOption casts) compiles and the existing slice-all E2E still works. Fix any compile/link errors (likely apply-direction or ConfigOption-cast issues).
    </action>
    <read_first>
- `.codex/rules/build-rules.md`
    </read_first>
    <acceptance_criteria>
- `auto_verify_with_vcvars.ps1` exits 0
- Existing tests pass (no slice regression)
    </acceptance_criteria>
  </task>

  <task id="19-01-T5">
    <action>
Add deterministic tests to `tests/ViewModelSmokeTests.cpp`:

1. `projectServicePerPlateConfigOverrideRoundTrips()` (D-16): construct ProjectServiceMock; `setPlateScopedOptionValue(0, "infill_speed", 42)` → returns true; `plateScopedOptionValue(0, "infill_speed", 0).toInt()` == 42 (proves read-after-write works under HAS, stub fixed). Also test a bool and string key. Skip via QSKIP if HAS_LIBSLIC3R off.

2. `sliceServicePerPlateConfigMergeHonorsOverrides()` (D-15): this is harder to test in isolation (slicing needs geometry). Instead, add a lighter-weight assertion: after setPlateScopedOptionValue sets a key on a plate, verify that `plateDynamicConfig(plateIdx)->option(key)` returns the set value (proves the config SliceService will merge actually carries the override). This is a unit-level guarantee of the merge source; the full slice-path merge is verified by code inspection (T3) + the E2E slice tests that already exist. Document in the test comment that full slice-path verification needs a real-model fixture (same fixture gap as Phase 18 PLATE-09).

Add declarations + impls. Don't remove/modify existing tests.
    </action>
    <read_first>
- `tests/ViewModelSmokeTests.cpp` (existing patterns; the #ifdef HAS_LIBSLIC3R QSKIP)
- `src/core/services/ProjectServiceMock.h` (setPlateScopedOptionValue/plateScopedOptionValue/plateDynamicConfig signatures)
- `src/core/services/ProjectServiceMock.cpp` (from T1/T2 — the implementations under test)
    </read_first>
    <acceptance_criteria>
- `grep -c "projectServicePerPlateConfigOverrideRoundTrips\|sliceServicePerPlateConfigMergeHonorsOverrides" tests/ViewModelSmokeTests.cpp` returns 2
- The override round-trip test asserts set→read returns the set value (int/bool/string)
- `build/ViewModelSmokeTests.exe` reports 42 passed (41 prior + 1 new override test; the slice-merge test may QSKIP if it needs a fixture), 0 failed
- canonical verify exits 0
    </acceptance_criteria>
  </task>

</tasks>

<verification>
- Canonical build passes (exit 0)
- plateScopedOptionValue/setPlateScopedOptionValue no longer stub under HAS (read/write PartPlate::config())
- SliceService uses full config.apply(*plateCfg), no hardcoded 3-key patch
- PLATE-11 documented: stack-local Print per startSlicePlate = per-plate isolation (D-14)
- Override round-trip test passes; slice-merge unit test asserts config carries override (full slice-path needs real-model fixture, documented)
</verification>

<notes>
This phase is smaller than the plan estimate suggested: the scout (D-14) revealed Qt6's stack-local Print already provides per-plate isolation, so PLATE-11 needs only documentation. The real work is PLATE-10 (config merge) + the D-16 stub fix. Main risks: DynamicPrintConfig::apply() direction (T3) and ConfigOption type dispatch in D-16 (T1). The build-fix cycle should catch both.
</notes>
