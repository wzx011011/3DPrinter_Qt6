---
phase: 58-end-to-end-visual-and-functional-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/InventoryAuditTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [VERIFY-01]
tags: [inventory, audit-test, verify-01, regression-guard]

must_haves:
  truths:
    - "InventoryAuditTests target is registered in CMakeLists.txt and runs under ctest"
    - "All 9 Phase 50 §2 deterministic checks pass against docs/v3.6-ui-inventory.md (canonical live doc)"
    - "All 9 Phase 50 §2 deterministic checks pass against .planning/phases/50-.../50-INVENTORY.md (frozen snapshot)"
    - "A future drift in region count, schema, enum, ID format, coverage anchor, cleanup format, or upstream citation fails ctest"
    - "Phase 57 cleanup regression stays locked (reference only — already covered by QmlUiAuditTests::deletedSettingsPathsStayAbsent / deletedRoutesStayAbsent)"
  artifacts:
    - path: "tests/InventoryAuditTests.cpp"
      provides: "Qt Test target encoding the 9 Phase 50 §2 inventory checks against both canonical + snapshot"
      min_lines: 320
      contains: "class InventoryAuditTests"
    - path: "CMakeLists.txt"
      provides: "InventoryAuditTests target + CTest registration mirroring the QmlUiAuditTests pattern"
      contains: "qt_add_executable(InventoryAuditTests"
  key_links:
    - from: "tests/InventoryAuditTests.cpp"
      to: "docs/v3.6-ui-inventory.md"
      via: "readSource() + QT_TESTCASE_SOURCEDIR"
      pattern: "v3\\.6-ui-inventory"
    - from: "tests/InventoryAuditTests.cpp"
      to: ".planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md"
      via: "readSource() + QT_TESTCASE_SOURCEDIR"
      pattern: "50-INVENTORY"
    - from: "CMakeLists.txt"
      to: "tests/InventoryAuditTests.cpp"
      via: "qt_add_executable + add_test"
      pattern: "InventoryAuditTests"
---

<objective>
Create `tests/InventoryAuditTests.cpp` — a Qt Test target that encodes the 9 Phase 50 §2
deterministic inventory checks as a permanent ctest-level regression guard (VERIFY-01).
Mirror the existing `QmlUiAuditTests.cpp` pattern (readSource via QT_TESTCASE_SOURCEDIR,
QVERIFY2 assertions, single-file QtTest with Q_OBJECT). Register the new target in the
root `CMakeLists.txt` next to QmlUiAuditTests so it runs in every canonical verify pass.

Purpose: The Phase 50 inventory is the v3.6 source-of-truth contract between the 4
screenshots, the upstream OrcaSlicer code, and the Qt6 migration. Today the 9 checks
only exist as Bash/grep commands run once at Phase 50 close — any future drift in region
count, schema, enum, ID format, coverage anchors, cleanup format, or upstream citation
would silently pass. This plan locks those invariants at ctest time so the next
milestone (or a future screenshot/inventory edit) cannot regress them without a build
failure that points directly at VERIFY-01.

Output:
- `tests/InventoryAuditTests.cpp` (new) — 9 deterministic checks x 2 docs (canonical + snapshot) = 18 assertions.
- `CMakeLists.txt` (modified) — `qt_add_executable(InventoryAuditTests ...)` + `add_test` + envProperties mirroring QmlUiAuditTests lines 388-396.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-end-to-end-visual-and-functional-verification/58-CONTEXT.md

# Canonical inventory doc (the live doc the test reads)
@docs/v3.6-ui-inventory.md

# Phase 50 frozen snapshot + the 9 §2 checks verbatim (the spec for this plan)
@.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md

# Existing test pattern to mirror exactly
@tests/QmlUiAuditTests.cpp

<interfaces>
<!-- The readSource() helper to copy verbatim (one-line QFile + QT_TESTCASE_SOURCEDIR lookup). -->
<!-- From tests/QmlUiAuditTests.cpp:85-91 -->

From tests/QmlUiAuditTests.cpp (mirror this pattern):
```cpp
QString InventoryAuditTests::readSource(const QString &relativePath) const
{
  QFile file(QDir(QStringLiteral(QT_TESTCASE_SOURCEDIR)).filePath(relativePath));
  if (!file.open(QIODevice::ReadOnly | QIODevice::Text))
    return {};
  return QString::fromUtf8(file.readAll());
}
```

CMake target block to mirror (from CMakeLists.txt:388-396):
```cmake
qt_add_executable(QmlUiAuditTests tests/QmlUiAuditTests.cpp)
target_link_libraries(QmlUiAuditTests PRIVATE Qt6::Test)
target_compile_definitions(QmlUiAuditTests PRIVATE
    QT_TESTCASE_SOURCEDIR="${CMAKE_SOURCE_DIR}"
)
add_test(NAME QmlUiAuditTests COMMAND QmlUiAuditTests)
set_tests_properties(QmlUiAuditTests PROPERTIES
    ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}"
)
```

Region-table row anatomy (from 50-INVENTORY.md §2 note):
- 9 pipe-delimited cells per region row => 10 pipes => 11 fields under `split('|')`.
- §6 modify-vs-replace summary rows share the `| PREP-…` prefix but have only 3 cells
  (4 pipes => 5 fields) — so the region counter MUST filter on field-count == 11.
- Per-prefix expected counts (both docs): PREP- = 9, PREV- = 9, SETPRINT- = 8, SETMAT- = 8;
  total = 34 (within the 30-40 budget; each screenshot within 6-12).

Required coverage anchor globs (INV-02/03/04):
- INV-02 (Prepare, 1 anchor): Plater.* GLCanvas3D.* GUI_ObjectList.* GUI_ObjectSettings.* Gizmos/*
- INV-03 (Preview, 1 anchor): GUI_Preview.* GCodeViewer.* GLCanvas3D.* libslic3r/GCode/*
- INV-04 (Settings, 2 anchors — printer + material): Tab.* PresetComboBoxes.* ConfigManipulation.* UnsavedChangesDialog.* CreatePresetsDialog.* PrintConfig.* Preset.* PresetBundle.*

Allowed status enum (7 terms): Real, Hybrid, Mock, Blocked, Placeholder, Superseded, Missing
Allowed verification enum (6 terms): automated-test, deterministic-harness, manual-visual, manual-uat-checklist, build-only, upstream-parity-audit
Region ID regex: ^[A-Z]+-[A-Z0-9]+$ (single dash, ASCII only, unique per doc)
Allowed cleanup tags (6): file:, qrc:, route:, import:, test:, doc:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/InventoryAuditTests.cpp encoding the 9 Phase 50 §2 checks against both the canonical and snapshot inventory docs</name>
  <files>tests/InventoryAuditTests.cpp</files>
  <read_first>
    - .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md (§2 lines 170-286 — the literal 9 checks with exact numeric expectations)
    - tests/QmlUiAuditTests.cpp (lines 1-91 for the header/readSource pattern to copy; lines 1472-1535 for the Phase 57 deleted-paths regression that this plan references but does NOT duplicate)
    - docs/v3.6-ui-inventory.md (canonical live doc; confirm region prefixes PREP-/PREV-/SETPRINT-/SETMAT- and the 4 INV-02/03/04 coverage anchors at lines 150/176/205/233)
  </read_first>
  <action>
    Create `tests/InventoryAuditTests.cpp` as a single-file Qt Test (final class `InventoryAuditTests`, `Q_OBJECT`, `QTEST_MAIN`, `#include "InventoryAuditTests.moc"` — same shape as QmlUiAuditTests.cpp lines 17-91 + 1537-1538). UTF-8 no BOM, 2-space indent, ASCII-only comments.

    Add a private `readSource(const QString &relativePath) const` helper identical to QmlUiAuditTests.cpp:85-91 (QT_TESTCASE_SOURCEDIR + QFile + QString::fromUtf8). Add a second private helper `loadInventory(const QString &label, const QString &relativePath)` that calls readSource and QVERIFY2-nonempty with a labeled message; both canonical and snapshot paths reuse it.

    Hard-code the two target paths as private `static constexpr` (or `QStringLiteral`) members:
      - canonical: `"docs/v3.6-ui-inventory.md"`
      - snapshot:  `".planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md"`

    Define a single private helper `countRegionRowsByPrefix(const QString &src, const QString &prefix)` that splits each line on `'|'` and returns the number of rows whose field count is exactly 11 (`parts.size() == 11`) AND whose first non-empty trimmed cell starts with `prefix` (e.g. `"PREP-"`). This is the C++ port of the §2 `awk -F'|' 'NF==11'` filter — the §2 prose explicitly says a future compiled harness should use this row-counting approach because `grep -cE` with bracket alternation errors in this environment.

    Define `private slots:` test functions — ONE per §2 check, each running the assertion against BOTH the canonical and snapshot docs (load both once per test via the helper; assert on each, with a doc-labeled QVERIFY2 message so a failure says which doc drifted):

      1. `inventoryDocsArePresentAndNonEmpty()` — §2 check 1. QVERIFY2 that readSource returns non-empty for both paths (test -s equivalent). Label which file is missing in the message.

      2. `perScreenshotRegionCountsWithinBounds()` — §2 check 2. For EACH of the 4 prefixes (PREP-, PREV-, SETPRINT-, SETMAT-) on EACH doc, assert `count >= 6 && count <= 12`; assert `total >= 30 && total <= 40`. Use the §2 recorded values (PREP 9, PREV 9, SETPRINT 8, SETMAT 8, total 34) as expected-equality fallbacks ONLY if you want stronger lock-in — the §2 contract is the range check, so range is the minimum; equality is a stricter optional form. Implement range (the contract).

      3. `regionRowsUseNineColumnSchemaAndHeaderMatchesContract()` — §2 check 3. Assert the fixed 9-column header string `| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |` appears `>= 4` times in each doc (canonical expects 6 per §2; snapshot expects 4 per §2 — use `>= 4`). Then count rows with field-count==11 across all 4 prefixes and assert `== 34` for both docs (the §2 PASS result; this also catches any NF!=11 region row).

      4. `statusCellsUseSevenTermVocabulary()` — §2 check 4. For every region row (NF==11) in each doc, extract the status cell (cell index 6, 0-based) and assert it is one of the 7 allowed terms: Real, Hybrid, Mock, Blocked, Placeholder, Superseded, Missing. Tally per-term counts and assert the §2 distribution for documentation (Real 8, Hybrid 16, Mock 0, Blocked 0, Placeholder 8, Superseded 0, Missing 2; sum 34) as equality on both docs — this is the strongest form and matches §2 PASS exactly. Use a `QSet<QString>` of allowed terms.

      5. `verificationCellsUseSixTermVocabulary()` — §2 check 5. Same pattern, cell index 7 (0-based), allowed terms: automated-test, deterministic-harness, manual-visual, manual-uat-checklist, build-only, upstream-parity-audit. Assert §2 distribution (manual-visual 28, manual-uat-checklist 3, build-only 1, upstream-parity-audit 2, automated-test 0, deterministic-harness 0; sum 34) as equality on both docs.

      6. `regionIdsMatchAsciiSingleDashRegexAndAreUnique()` — §2 check 6. Collect every region_id (cell index 1, 0-based) from region rows in each doc. Assert each matches `QRegularExpression("^[A-Z]+-[A-Z0-9]+$")`. Assert no duplicates (`QSet` size == `QVector` size). Assert total count == 34. Assert no multi-dash, no slash, no non-ASCII (the regex already enforces ASCII + single dash, so the regex pass is the contract).

      7. `coverageAnchorsListAllRequiredClusterGlobs()` — §2 check 7. For each doc, assert exactly 1 line contains `<!-- INV-02 coverage` and that line contains all 5 Prepare globs (`Plater.`, `GLCanvas3D.`, `GUI_ObjectList.`, `GUI_ObjectSettings.`, `Gizmos/`). Assert exactly 1 `<!-- INV-03 coverage` line with all 4 Preview globs (`GUI_Preview.`, `GCodeViewer.`, `GLCanvas3D.`, `libslic3r/GCode/`). Assert exactly 2 `<!-- INV-04 coverage` lines (printer + material) each carrying all 8 Settings globs (`Tab.`, `PresetComboBoxes.`, `ConfigManipulation.`, `UnsavedChangesDialog.`, `CreatePresetsDialog.`, `PrintConfig.`, `Preset.`, `PresetBundle.`). Use `QString::count(QStringLiteral("<!-- INV-02 coverage"))` for the anchor-count assertions and `contains()` per glob.

      8. `cleanupFormatUsesOnlySixAllowedTagsAndEveryFilePathExists()` — §2 check 8. For each doc, scan every region-row cleanup cell (cell index 8, 0-based). Allow `n/a` and `n/a (no prior Qt target)` verbatim. For any cell not matching those literals, assert it uses ONLY tokens from the 6-tag set: `file:`, `qrc:`, `route:`, `import:`, `test:`, `doc:`. Implementation: split the cleanup cell on whitespace, for each token assert it startsWith one of the 6 tags. THEN — the §7 aggregate cleanup checklist (the §1.6 locked Settings cleanup targets) is the part that lists actual `file:` paths: scan the doc for the pattern `file:` followed by a path token, collect those paths, and assert each exists on disk via `QFileInfo::exists()` resolved against `QT_TESTCASE_SOURCEDIR`. The §2 PASS record lists exactly 4 `file:` paths: `src/qml_gui/pages/SettingsPage.qml`, `src/qml_gui/pages/ConfigPage.qml`, `src/qml_gui/components/ParamsPage.qml`, `src/qml_gui/components/SearchDialog.qml`. NOTE: these 4 files were DELETED in Phase 57 — they must NOT exist on disk today. So the existence check is: each `file:` token in a `replace` cleanup entry that is STILL listed in the §1.6/§7 cleanup aggregate must have been deleted (covered by QmlUiAuditTests::deletedSettingsPathsStayAbsent). The §2 check 8 PASS record was captured at Phase 50 close (before Phase 57 deleted them). To avoid contradiction: assert the cleanup-cell FORMAT (6-tag vocabulary) deterministically in this test, and add a comment block at the top of the test function explaining that the file-existence half of §2 check 8 is now covered by `QmlUiAuditTests::deletedSettingsPathsStayAbsent` (the post-Phase-57 invariant is "these paths must stay ABSENT", which supersedes the Phase 50 "they exist" half). Do NOT re-assert existence here — that would invert the Phase 57 regression. Assert format only.

      9. `noBlankUpstreamSourceCells()` — §2 check 9. For every region row (NF==11) in each doc, extract the upstream_source cell (cell index 4, 0-based), trimmed. Assert it is non-empty. Per §2, no row uses the `none-mapped (Owzx-only)` literal today, so do NOT require that literal — only assert non-blank.

    Add ONE additional test `phase57CleanupRegressionRemainsLockedByQmlUiAudit()` (no body work, just a documentation assertion). It does QVERIFY2 on a static `true` with a message that points the reader at `QmlUiAuditTests::deletedSettingsPathsStayAbsent` and `QmlUiAuditTests::deletedRoutesStayAbsent` as the permanent regression guards for the Phase 57 cleanup (CLEAN-01/02). This is the VERIFY-01 "QML route/resource registration contains only live components" half — it lives in QmlUiAuditTests, not here, and this slot exists only so a future reader running `ctest -R Inventory` sees the cross-reference. Do NOT duplicate the assertions.

    File header: short comment block naming the phase (58), requirement (VERIFY-01), the source spec (Phase 50 §2 lines 170-286 of 50-INVENTORY.md), and the AUTOMOC caveat already documented at the top of QmlUiAuditTests.cpp (single-file QtTest with cpp-internal Q_OBJECT has weak moc dependency tracking — after adding a new private slot, re-run cmake configure via the canonical verify script or delete `build/InventoryAuditTests_autogen/timestamp`). ASCII only.

    Use `QVERIFY2(condition, qPrintable(message))` consistently — never bare QVERIFY — so failures identify which doc (canonical vs snapshot) drifted. Messages should be of the form `"canonical: <reason>"` / `"snapshot: <reason>"`.

    NEVER place fenced code blocks inside this action. The C++ identifiers, signatures, regex, and enum terms above are the contract; the executor writes the implementation against them.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
    <human-check>After the canonical verify passes, run `ctest --test-dir build -R InventoryAuditTests --output-on-failure -V` and confirm all 10 slots (9 §2 checks x 1 doc-pair each + the Phase 57 cross-reference slot) report PASS. Confirm Qt Test output is read via the `-o file,txt` redirect (stdout is buffered on Windows — see CLAUDE.md).</human-check>
  </verify>
  <done>
    - tests/InventoryAuditTests.cpp exists, UTF-8 no BOM, ASCII-only comments, compiles cleanly under the canonical build.
    - All 9 Phase 50 §2 deterministic checks pass against BOTH docs/v3.6-ui-inventory.md AND .planning/phases/50-.../50-INVENTORY.md.
    - The Phase 57 cleanup regression is referenced (not duplicated) by phase57CleanupRegressionRemainsLockedByQmlUiAudit.
    - A deliberate edit to either inventory doc that violates any of the 9 checks (e.g. delete a region row, rename a status term, drop a coverage glob) causes the corresponding slot to FAIL with a doc-labeled message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register InventoryAuditTests target in root CMakeLists.txt mirroring the QmlUiAuditTests block</name>
  <files>CMakeLists.txt</files>
  <read_first>
    - CMakeLists.txt lines 388-396 (the QmlUiAuditTests block to mirror)
    - CMakeLists.txt lines 355-396 (the surrounding test-target section so the new entry lands in the right place)
  </read_first>
  <action>
    Add a new `qt_add_executable(InventoryAuditTests tests/InventoryAuditTests.cpp)` block immediately AFTER the QmlUiAuditTests block (CMakeLists.txt line ~396) and BEFORE the `qt_add_executable(PrepareSceneDataTests ...)` block (line ~398). The new block must mirror the QmlUiAuditTests lines 388-396 exactly:

      - `qt_add_executable(InventoryAuditTests tests/InventoryAuditTests.cpp)`
      - `target_link_libraries(InventoryAuditTests PRIVATE Qt6::Test)` (InventoryAuditTests is a pure file-content audit like QmlUiAuditTests — it does NOT link `owzx_app_core`; it only reads source/docs via QT_TESTCASE_SOURCEDIR. Keep the link set minimal.)
      - `target_compile_definitions(InventoryAuditTests PRIVATE QT_TESTCASE_SOURCEDIR="${CMAKE_SOURCE_DIR}")` (REQUIRED — readSource resolves inventory paths against this; without it the test cannot find the docs.)
      - `add_test(NAME InventoryAuditTests COMMAND InventoryAuditTests)`
      - `set_tests_properties(InventoryAuditTests PROPERTIES ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}")` (same Windows PATH gate every other Qt Test uses so Qt DLLs resolve at runtime.)

    Add a 3-line comment immediately above the new block (same style as the Phase 55-04 / Phase 56-03 comments in QmlUiAuditTests):
      `# Phase 58 (VERIFY-01): inventory audit harness. Encodes the 9 Phase 50 §2`
      `# deterministic checks (region counts, schema, status/verification enums, region-ID`
      `# format, INV-02/03/04 coverage anchors, cleanup format, no-blank-upstream) against`
      `# both docs/v3.6-ui-inventory.md (canonical) and the 50-INVENTORY.md snapshot.`

    ASCII only. Do NOT touch the QmlUiAuditTests block, the PrepareSceneDataTests block, or any other target — surgical insertion at one location.

    NEVER place fenced code blocks inside this action. The cmake directives above are the contract.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
    <human-check>Confirm `ctest --test-dir build -N -R Inventory` lists exactly one test named `InventoryAuditTests`. Confirm `ctest --test-dir build -R InventoryAuditTests --output-on-failure` runs all 10 slots green.</human-check>
  </verify>
  <done>
    - CMakeLists.txt contains the new InventoryAuditTests target block immediately after QmlUiAuditTests, with QT_TESTCASE_SOURCEDIR compile definition and the ENVIRONMENT property set.
    - `cmake --build build --config Release` builds InventoryAuditTests.exe with zero errors and zero new warnings.
    - `ctest --test-dir build -R InventoryAuditTests --output-on-failure` runs and reports 10/10 PASS (9 §2 checks + Phase 57 cross-reference).
    - The canonical verify script (auto_verify_with_vcvars.ps1) end-to-end run is green, proving the new target integrates with the existing smoke + ctest flow without disrupting other suites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan produces test code (a Qt Test that reads repo-local Markdown files via QT_TESTCASE_SOURCEDIR) and a build-system registration. No runtime user input, no network surface, no auth surface, no privileged file access. The test reads files inside the repo working tree only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-SC | Tampering | n/a (no package installs in this plan) | accept | No npm/pip/cargo installs; pure CMake + Qt Test target. T-58-SC package-legitimacy gate not applicable. |
| T-58-01-I | Information disclosure | InventoryAuditTests output | accept | Test failure messages may include file paths and inventory row contents — these are repo-local, non-sensitive (no PII, no secrets). Acceptable. |
| T-58-02-D | Denial of service | ctest runtime | accept | The test parses two ~30KB Markdown files; negligible runtime (<100ms). No DoS surface. |
</threat_model>

<verification>
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` end-to-end green (cmake configure + ninja build + smoke + ctest).
- `ctest --test-dir build -R InventoryAuditTests --output-on-failure -V` reports 10/10 PASS slots.
- Manual mutation check: temporarily delete one region row from a local copy of `docs/v3.6-ui-inventory.md` (do NOT commit), rerun the test, confirm `perScreenshotRegionCountsWithinBounds` and `regionRowsUseNineColumnSchemaAndHeaderMatchesContract` both FAIL with a "canonical:" labeled message; revert the edit.
</verification>

<success_criteria>
- VERIFY-01 is satisfied: automated tests cover screenshot/source inventory completeness (checks 1, 2, 3, 9), the 9-column schema and 7/6-term status/verification enums (checks 3, 4, 5), region-ID format and uniqueness (check 6), upstream coverage anchors INV-02/03/04 (check 7), and cleanup format (check 8). QML route/resource registration + cleanup-of-replaced-UI regression is covered by the existing QmlUiAuditTests slots (cross-referenced from this plan).
- The InventoryAuditTests target runs in every canonical verify pass from now on, so any future inventory drift is caught at ctest time.
- No new runtime surface, no new external dependencies, no behavior change to OWzxSlicer.exe.
</success_criteria>

<output>
Create `.planning/phases/58-end-to-end-visual-and-functional-verification/58-01-SUMMARY.md` when done
</output>
