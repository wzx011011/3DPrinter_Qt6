---
phase: 58-end-to-end-visual-and-functional-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-end-to-end-visual-and-functional-verification/58-UAT.md
  - .planning/phases/58-end-to-end-visual-and-functional-verification/58-VERIFICATION.md
autonomous: true
requirements: [VERIFY-02, VERIFY-03, VERIFY-04, VERIFY-05]
tags: [uat-checklist, gap-audit, canonical-verify, verify-02, verify-03, verify-04, verify-05]

must_haves:
  truths:
    - "Existing automated coverage for VERIFY-02 (workflow transitions) and VERIFY-03 (Preview stability) is audited and any real gap is documented or covered"
    - "58-UAT.md gives the user a runnable manual checklist against the 4 screenshots with each item mapping to an inventory region + a requirement"
    - "Canonical verification (auto_verify_with_vcvars.ps1 + ctest) is run end-to-end and every failure is classified with file / command / cause / follow-up owner"
    - "Pre-existing CliTests missing-fixture failures and the auto_verify smoke Qt-mismatch flake are classified as known/non-blocking carry-forward, not new defects"
  artifacts:
    - path: ".planning/phases/58-end-to-end-visual-and-functional-verification/58-UAT.md"
      provides: "User-runnable manual UAT checklist mapping each item to a region in docs/v3.6-ui-inventory.md and a VERIFY / SHELL / PREPSB / PREPWF / PREVLAY / GCODE / SETTINGS / CLEAN requirement"
      min_lines: 150
      contains: "shotScreen/准备页.png"
    - path: ".planning/phases/58-end-to-end-visual-and-functional-verification/58-VERIFICATION.md"
      provides: "Canonical verify run result + per-failure classification (file / command / cause / owner) + coverage matrix for VERIFY-02/03"
      min_lines: 80
      contains: "VERIFY-05"
  key_links:
    - from: ".planning/phases/58-end-to-end-visual-and-functional-verification/58-UAT.md"
      to: "docs/v3.6-ui-inventory.md"
      via: "region_id cross-reference per UAT item"
      pattern: "PREP-|PREV-|SETPRINT-|SETMAT-"
    - from: ".planning/phases/58-end-to-end-visual-and-functional-verification/58-VERIFICATION.md"
      to: "scripts/auto_verify_with_vcvars.ps1"
      via: "canonical verify command + classification"
      pattern: "auto_verify_with_vcvars"
---

<objective>
Close the milestone's verification surface in three steps:

1. **VERIFY-02/03 gap audit:** Read `tests/E2EWorkflowTests.cpp` (45+ test slots), `tests/PreviewParserTests.cpp`, `tests/QmlUiAuditTests.cpp`, and `tests/ViewModelSmokeTests.cpp`. Confirm the existing slots cover the import -> configure -> prepare -> slice -> preview -> export transitions (VERIFY-02) including slice invalidation after settings changes, AND the Preview layer/move/camera/regression class (VERIFY-03, the disappearing-preview bug). Per CONTEXT.md decision (Claude's Discretion): audit first; add a gap test ONLY if a real transition is uncovered that has no coverage. The expected outcome is "coverage exists; no new test code needed" — document that conclusion in 58-VERIFICATION.md with the mapping table. Do NOT redundantly re-test.

2. **VERIFY-04 manual UAT checklist:** Write `58-UAT.md` — a non-developer-runnable manual checklist. Each item maps to (a) one of the 4 screenshots under `shotScreen/`, (b) a region_id in `docs/v3.6-ui-inventory.md`, and (c) a requirement from REQUIREMENTS.md. The user runs `build/OWzxSlicer.exe` and walks the workflow: import -> configure -> prepare -> slice -> preview -> export, comparing visible regions against the screenshots and confirming upstream behavior parity. The checklist is the human sign-off gate; phase status will be `human_needed` after this plan completes (CONTEXT: expected, not a gap).

3. **VERIFY-05 canonical verify + classification:** Run `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` end-to-end and `ctest --test-dir build --output-on-failure`. Classify EVERY failure with file / command / cause / follow-up-owner in 58-VERIFICATION.md. The known carry-forwards (STATE.md): CliTests `testLoadHotend` / `testSliceHotend` / `testSliceBlock20XY` fail because the fixtures `profiles/hotend.stl` and `test_models/Block20XY.stl` are missing (separate future fixture milestone); the auto_verify smoke step occasionally emits a Qt version-mismatch warning + one-off crash (Phase 56 VALIDATION line 112: environmental, 5/5 direct reruns pass stable). These are classified, NOT fixed, in this phase.

Purpose: VERIFY-02/03 confirm the automation floor; VERIFY-04 hands the visual/behavioral sign-off to the user; VERIFY-05 produces the classified failure report. Together with Plan 01 (VERIFY-01), this closes the milestone.

Output:
- `58-UAT.md` (new) — manual UAT checklist.
- `58-VERIFICATION.md` (new) — coverage matrix + canonical verify result + failure classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-end-to-end-visual-and-functional-verification/58-CONTEXT.md

# Inventory (region IDs that the UAT checklist references)
@docs/v3.6-ui-inventory.md

# Existing green coverage to audit (DO NOT modify these files — read-only audit)
@tests/E2EWorkflowTests.cpp
@tests/PreviewParserTests.cpp

# Phase 56 VALIDATION — line 112 records the auto_verify Qt-mismatch flake note to carry forward
@.planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md

<interfaces>
<!-- E2E test slot inventory (the VERIFY-02/03 coverage evidence). Read-only. -->

From tests/E2EWorkflowTests.cpp (private slots, lines 67-100):
VERIFY-02 transition coverage already exists:
- test_config_injection_applies_preset_values          (configure step)
- test_import_format_coverage_matrix_real_fixtures      (import step)
- test_slice_produces_gcode_file                        (slice step)
- test_slice_results_propagate_to_editor_vm             (slice -> state)
- test_export_gcode_to_path                             (export step)
- test_export_gcode_rejects_unsafe_targets              (export safety)
- test_local_import_slice_preview_export_workflow       (FULL END-TO-END transition)
- test_preview_receives_gcode_data                      (slice -> preview transition)
- test_backend_switches_to_preview_after_slice          (preview routing)
- test_model_change_invalidates_slice_result            (slice invalidation)
- test_slice_affecting_bed_change_marks_current_result_stale
- test_import_invalidates_slice_output_and_preview_payload
- test_previous_gcode_reuse_marks_reused_result_and_refreshes_preview
- test_cancelled_slice_clears_active_result_and_blocks_preview_export
- test_slice_all_stores_outputs_for_printable_unlocked_plates_only
- testSettingsEditInvalidatesSlice                      (SETTINGS-07: settings edit -> slice invalidation)
- testDirtyOverridesPersistAcrossProjectSaveRestore     (SETTINGS-07: dirty override persistence)

VERIFY-03 Preview stability coverage already exists (lines 91-97):
- previewReceivesRealGcodeAfterLiveSliceNoPlaceholder   (GCODE-01: real data, no placeholder)
- sliceFailedClearsPreviewState                         (slice-failed transition)
- sliceResultClearedClearsPreviewState                  (slice-result-cleared transition)
- plateSwitchToInvalidClearsPreviewState                (plate-switch transition)
- resliceRebuildsGcodePreviewDataWithDifferentBytes     (reslice rebuild)
- exportWhilePreviewVisibleLeavesGcodePreviewDataIntact (export transition)
- pageSwitchPreparePreviewPreservesGcodePreviewData     (page-switch transition)

Plus QmlUiAuditTests::previewRhiInteractionSettersPreservePreviewPayload + 8 other Preview source-audit slots (GCODE-04/05).

Canonical verification commands (CLAUDE.md build-rules.md):
- Build + smoke: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
- Full ctest:    `ctest --test-dir build --output-on-failure`

Known carry-forward failures (STATE.md lines 97-98 + Phase 56 VALIDATION line 112):
- CliTests::testLoadHotend / testSliceHotend / testSliceBlock20XY — missing fixtures `profiles/hotend.stl`, `test_models/Block20XY.stl`
- auto_verify smoke step occasional Qt-mismatch one-off crash — environmental, 5/5 reruns pass stable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing E2E + Preview tests for VERIFY-02/03 coverage; write 58-VERIFICATION.md coverage matrix + canonical verify run result + failure classification</name>
  <files>.planning/phases/58-end-to-end-visual-and-functional-verification/58-VERIFICATION.md</files>
  <read_first>
    - tests/E2EWorkflowTests.cpp (lines 67-100 for the full slot list — read once, extract all slot names; lines 474-566 for test_local_import_slice_preview_export_workflow the full-workflow proof)
    - tests/PreviewParserTests.cpp (slot list — confirm Preview parser coverage; do NOT re-read bodies)
    - .planning/phases/56-parameter-settings-dialogs-restoration/56-VALIDATION.md (line 112 for the auto_verify Qt-mismatch flake note — the exact text to carry forward)
    - .planning/STATE.md (lines 97-98 for the missing-fixture carry-forward)
  </read_first>
  <action>
    STEP A — Coverage audit. Build a coverage matrix mapping each VERIFY-02/03 sub-claim to the existing test slot(s) that prove it. VERIFY-02 sub-claims (from REQUIREMENTS.md): import transition; configure transition; prepare transition; slice transition; preview transition; export transition; slice invalidation after settings changes. VERIFY-03 sub-claims: Preview visible during layer slider drag; Preview visible during move slider drag; Preview visible during camera orbit/pan/zoom; Preview visible across page switch; Preview visible across reslice; Preview visible across export. Map each sub-claim to its E2E slot name(s). The interfaces block above lists the slot inventory to use as the mapping source.

    STEP B — Decision per CONTEXT.md (Claude's Discretion): if every VERIFY-02/03 sub-claim has at least one covering slot, write "NO NEW TEST CODE NEEDED — full coverage exists" as the audit conclusion in 58-VERIFICATION.md. If a real gap is found (a sub-claim with zero covering slots), then and ONLY then create a minimal Qt Test slot in the existing E2EWorkflowTests.cpp private-slots block (one slot, <60 lines, following the existing fixture-setup pattern at lines 127-156) that asserts the missing transition. Re-run canonical verify to confirm the new slot passes. Do not invent synthetic gaps to justify new code — CONTEXT.md explicitly warns against redundant re-testing.

    STEP C — Canonical verify run. Run `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` end-to-end and `ctest --test-dir build --output-on-failure`. Capture the full output (cmake configure result, ninja build error/warning counts, smoke test result, ctest PASS/FAIL per target). If the smoke step emits the known Qt-mismatch one-off crash, rerun the canonical verify up to 2 more times — if the failure clears, classify as environmental (Phase 56 VALIDATION line 112 precedent); if it persists, escalate.

    STEP D — Failure classification. For EVERY ctest failure, produce a row in 58-VERIFICATION.md with columns: `test_name | command | cause | follow_up_owner | classification`. Classification values: `carry-forward` (pre-existing, tracked in STATE.md), `environmental` (Windows/Qt runtime, not a code defect), `regression` (new defect this phase — must be fixed before phase close). The expected carry-forwards:
      - CliTests::testLoadHotend           — carry-forward — missing fixture `profiles/hotend.stl`
      - CliTests::testSliceHotend          — carry-forward — missing fixture `profiles/hotend.stl`
      - CliTests::testSliceBlock20XY       — carry-forward — missing fixture `test_models/Block20XY.stl`
      - (auto_verify smoke Qt-mismatch, if observed) — environmental — rerun passes stable per Phase 56 VALIDATION line 112

    STEP E — Write 58-VERIFICATION.md with these sections (ASCII-only Markdown, UTF-8 no BOM):
      1. `## Canonical Verification Run` — date, command, exit status, summary line ("X/Y ctest targets PASS").
      2. `## Coverage Matrix — VERIFY-02 (Workflow Transitions)` — table: sub-claim | covering slot(s) | file:line.
      3. `## Coverage Matrix — VERIFY-03 (Preview Stability)` — same shape.
      4. `## Audit Conclusion` — the "NO NEW TEST CODE NEEDED" sentence OR the single new slot added with justification.
      5. `## Failure Classification (VERIFY-05)` — the per-failure table from STEP D.
      6. `## Carry-Forward` — bullet list pointing at STATE.md lines 97-98 and Phase 56 VALIDATION line 112.

    NEVER place fenced code blocks inside this action. Markdown table syntax is fine.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
    <human-check>58-VERIFICATION.md exists with all 6 sections. Every VERIFY-02/03 sub-claim maps to at least one E2E slot. Every ctest failure has a classification row. No failure is left unclassified.</human-check>
  </verify>
  <done>
    - 58-VERIFICATION.md exists, UTF-8 no BOM, ASCII-only comments, with the coverage matrix proving VERIFY-02/03 are already covered by existing automated tests (or one minimal gap-test added if a real gap was found).
    - The canonical verify run is captured end-to-end with date and exit status.
    - Every ctest failure is classified as carry-forward / environmental / regression with file + command + cause + owner.
    - The pre-existing CliTests missing-fixture failures and the auto_verify Qt-mismatch flake are explicitly classified as known/non-blocking per STATE.md and Phase 56 VALIDATION line 112.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 58-UAT.md manual checklist mapping each item to a screenshot region + a requirement</name>
  <files>.planning/phases/58-end-to-end-visual-and-functional-verification/58-UAT.md</files>
  <read_first>
    - docs/v3.6-ui-inventory.md (region IDs for all 4 screenshots: PREP-*, PREV-*, SETPRINT-*, SETMAT-*)
    - shotScreen/准备页.png, shotScreen/预览页.png, shotScreen/打印机参数设置页.png, shotScreen/材料参数设置页.png (the 4 visual-truth images)
    - .planning/REQUIREMENTS.md (SHELL-*, PREPSB-*, PREPWF-*, PREVLAY-*, GCODE-*, SETTINGS-*, CLEAN-* — the requirement IDs the UAT items map to)
  </read_first>
  <action>
    Create `58-UAT.md` as a non-developer-runnable manual checklist. The user runs `build/OWzxSlicer.exe` and walks the v3.6 workflow comparing what they see against the 4 screenshots. UTF-8 no BOM, ASCII-only comments, Markdown.

    Structure:
      1. `## Prerequisites` — build/OWzxSlicer.exe exists (built by canonical verify in Task 1); the 4 screenshots under shotScreen/ are accessible; an STL test model is available under tests/fixtures/ or tests/test_models/ (do NOT require the missing hotend.stl/Block20XY.stl fixtures — use whatever the existing E2E tests use; if uncertain, instruct the user to import any small STL they have).
      2. `## How to Run` — three steps: (a) launch `build/OWzxSlicer.exe`; (b) walk each section below in order; (c) for each item check PASS / FAIL / NOT-AVAILABLE and add a one-line note for any FAIL.
      3. `## Section A — Prepare Page vs shotScreen/准备页.png` — list one item per Prepare region in docs/v3.6-ui-inventory.md (PREP-SHELLTOP, PREP-SIDEBAR, PREP-OBJLIST, PREP-PLATES, PREP-VIEWPORT, PREP-GIZMOS, etc. — read the doc for the exact 9 region IDs). Each item:
         - `### A.N — <region name> (PREP-XXX)` — the user-visible region name as the screenshot shows it.
         - **Compare to:** `shotScreen/准备页.png` (specific quadrant — name it, e.g. "top menu bar", "left sidebar", "center viewport").
         - **Check:** the concrete visible/interactive behavior (e.g. "Import menu lists .3mf/.stl/.obj/.amf/.step/.stp", "Undo button is disabled when stack is empty", "Slice button shows progress and is disabled while slicing").
         - **Maps to:** requirement ID(s) — SHELL-01, PREPSB-01, PREPWF-01, etc.
         - **PASS / FAIL / N-A:** checkbox.
      4. `## Section B — Preview Page vs shotScreen/预览页.png` — same shape, one item per PREV-* region (9 regions). Maps to PREVLAY-01..05 + GCODE-01..05. CRITICAL items (the disappearing-preview regression class): drag the layer slider — Preview stays visible; drag the move slider — Preview stays visible; orbit the camera — Preview stays visible; switch Prepare->Preview->Prepare — Preview data preserved; reslice — Preview rebuilds with new bytes. Mark these items with "(regression-critical)" so the user prioritizes them.
      5. `## Section C — Printer Settings vs shotScreen/打印机参数设置页.png` — one item per SETPRINT-* region (8 regions). Maps to SETTINGS-01..07 + INV-04. Each item compares the dialog layout/density/controls to the screenshot and checks open-from-sidebar, tab navigation, option edit, dirty indicator, Save/Save As/Reset, search filter, basic/advanced toggle.
      6. `## Section D — Material Settings vs shotScreen/材料参数设置页.png` — same shape, one item per SETMAT-* region (8 regions). Maps to SETTINGS-01..07 + INV-04.
      7. `## Section E — Full Local Workflow (import -> configure -> prepare -> slice -> preview -> export)` — 6-10 end-to-end items that cross regions: import a model (PREPWF-01); select a printer preset (PREPSB-01); edit a process setting (SETTINGS-03); observe the slice-result-stale indicator (SETTINGS-07); slice (PREPWF); switch to Preview (PREVLAY); move the layer + move sliders (GCODE-05); export G-code to a temp path (SHELL-03 canExportGCode gate). Each item maps to a requirement and lists the expected visible result.
      8. `## Sign-Off` — three lines: "Visual parity against the 4 screenshots: PASS / FAIL"; "Behavior parity against upstream OrcaSlicer: PASS / FAIL"; "Outstanding gaps (list each with region + requirement + follow-up owner): ____".

    Mapping discipline: EVERY item in sections A-D carries one `PREP-*`/`PREV-*`/`SETPRINT-*`/`SETMAT-*` region ID (sourced from docs/v3.6-ui-inventory.md) AND one or more requirement IDs from REQUIREMENTS.md. Do not invent regions or requirements. If a screenshot region maps to multiple requirements, list them all.

    Tone: non-developer-runnable. Avoid internal class names, file paths, C++ identifiers. Replace with user-visible labels ("the Slice button", "the left sidebar", "the layer slider on the right edge"). The user is the product owner doing visual + behavioral sign-off, not an engineer reading the source.

    Length target: ~150-250 lines. Enough items to cover every region in the 4 screenshots (9+9+8+8 = 34 region-level items minimum) plus the 6-10 workflow items in section E, plus the prerequisites/sign-off shells. Do not pad — every item must carry a region + a requirement.

    NEVER place fenced code blocks inside this action.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
    <human-check>58-UAT.md exists with sections A-E + prerequisites + sign-off. Every item in sections A-D carries a region ID (PREP-/PREV-/SETPRINT-/SETMAT-) that exists in docs/v3.6-ui-inventory.md AND at least one requirement ID from REQUIREMENTS.md. Total region-level items >= 34 (9 Prepare + 9 Preview + 8 Printer + 8 Material). Section E has >= 6 workflow items. The 5 regression-critical Preview stability items (layer slider / move slider / camera / page switch / reslice) are present and tagged "(regression-critical)".</human-check>
  </verify>
  <done>
    - 58-UAT.md exists, UTF-8 no BOM, ASCII-only comments, runnable by a non-developer.
    - All 34 inventory regions across the 4 screenshots are covered by a UAT item mapping to a region ID + a requirement ID.
    - The 5 regression-critical Preview stability items are present and tagged.
    - Section E exercises the full local workflow at least once end-to-end.
    - The sign-off section captures PASS/FAIL + outstanding-gaps list for the user to fill in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan produces two Markdown documents under `.planning/phases/58-.../` and (optionally, only if a real gap is found) at most one small test slot added to an existing test file. No runtime user input crosses a trust boundary; no network surface; no auth surface; the test slot (if added) uses the same fixture-setup pattern as the existing E2E slots and runs in the same ctest sandbox.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-SC | Tampering | n/a (no package installs) | accept | No npm/pip/cargo installs. T-58-SC package-legitimacy gate not applicable. |
| T-58-02-E | Elevation | canonical verify script execution | accept | `auto_verify_with_vcvars.ps1` is the project's locked build command (CLAUDE.md build-rules.md); running it is the intended use. No new privilege introduced. |
| T-58-02-I | Information disclosure | 58-VERIFICATION.md failure classification | accept | File paths, command names, and cause notes are repo-local and non-sensitive. Acceptable. |
</threat_model>

<verification>
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` end-to-end green (or any failure classified in 58-VERIFICATION.md).
- `ctest --test-dir build --output-on-failure` — every target PASS or classified carry-forward / environmental.
- 58-VERIFICATION.md contains the VERIFY-02/03 coverage matrix, the audit conclusion, and the per-failure classification.
- 58-UAT.md contains the 4-screenshot manual checklist with region + requirement mapping for every item.
</verification>

<success_criteria>
- VERIFY-02: automated coverage proven (or one gap test added) for import -> configure -> prepare -> slice -> preview -> export transitions including slice invalidation after settings changes. Documented in 58-VERIFICATION.md.
- VERIFY-03: automated coverage proven for Preview layer/move/camera/regression class. Documented in 58-VERIFICATION.md.
- VERIFY-04: 58-UAT.md exists, runnable by a non-developer, mapping every region in the 4 screenshots to a requirement, with the 5 regression-critical Preview stability items tagged.
- VERIFY-05: canonical verify run captured end-to-end; every failure classified with file / command / cause / follow-up owner; pre-existing CliTests missing-fixture failures + auto_verify Qt-mismatch flake classified as carry-forward / environmental, NOT new defects.
- This plan completes with phase verification status `human_needed` — the user runs 58-UAT.md to close the milestone visually. That is the expected end state per CONTEXT.md, not a gap.
</success_criteria>

<output>
Create `.planning/phases/58-end-to-end-visual-and-functional-verification/58-02-SUMMARY.md` when done
</output>
