# Phase 240 Plan — Notification Stacking And Gizmo Interaction Depth

**Requirements:** NOTI-01, GIZ-01..06
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` notification + gizmo sections

## Plan 1 (single batch)

### Task 1 — NOTI-01: stacked notifications
- Replace single-slot queue (BackendContext.cpp:794-856) with a list model
  (QVariantList property): multiple visible toasts, 8-level importance
  ordering (upstream NotificationManager.cpp:1769), duplicate compression
  with counters (:284-286 Escalated counting), hyperlink action support
  (openHintDocumentation pattern generalized). ToastContainer QML renders
  the stack; history panel unchanged.

### Task 2 — GIZ-01: numeric transform input
- transformMiniPanel (PreparePage.qml:1651-1698) read-only → editable
  position/rotation/scale fields writing via setObjectPosition/Rotation/Scale
  (with undo commands, world/local toggle like upstream
  GizmoObjectManipulation).

### Task 3 — GIZ-02: painter smart fill + filters
- VM props supportPaintSmartFillAngle/OnOverhangsOnly have no UI/impl. Add
  Shift+click smart fill (seed fill via TriangleSelector::select_patch seed
  mode — PaintEngine already calls select_patch), angle threshold input,
  overhang-only filter toggle (upstream GLGizmoPainterBase smart fill).

### Task 4 — GIZ-03: Flatten face pick
- Hover face highlight + click-to-place: pick facet under cursor
  (SceneRaycaster exists), highlight it, on click rotate object so the picked
  normal faces down (upstream GLGizmoFlatten). Keep the auto-orient button.

### Task 5 — GIZ-04: Cut plane 3D drag/rotate
- Interactive plane manipulator in Cut/AdvancedCut modes: drag plane along
  axis, rotate plane normal (upstream GLGizmoCut3D grabbers). Bind to
  cutPosition/advCutPosition and cut axis rotation.

### Task 6 — GIZ-05: Measure 3D overlay
- Render distance/angle annotations in-scene (2D overlay projected from 3D
  anchor points is acceptable — upstream renders 3D lines + labels;
  AssemblePage overlay precedent exists RhiViewportRenderer.cpp:2080).

### Task 7 — GIZ-06: Emboss in-place edit + Simplify preview + SVG workbench
- Emboss: selecting an existing text volume opens the emboss panel with its
  TextConfiguration and re-generates in place (text2shapes pipeline exists).
- Simplify: async job + preview decimation + Apply/Cancel (upstream
  GLGizmoSimplify three-stage).
- SVG: drop-to-canvas places the SVG at drop point with move/rotate handles
  (upstream GLGizmoSVG workbench) — minimal: place at ray hit + existing
  transform panel.

## Verification

- Canonical build + all suites; interaction smoke per gizmo; notification
  stack visual check.
