# Phase 236 Plan — Dialog Reachability And Completion

**Requirements:** DLG-01..04
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` dialog section

## Plan 1 (single batch)

### Task 1 — DLG-01: entry points for orphan dialogs
- Emit `show*DialogRequested` from real triggers (BackendContext.h:446-454
  has the signals; find or add Q_INVOKABLE openers):
  - BedShapeDialog ← printer preset bed-shape edit button (LeftSidebar
    printer row gear / SettingsDialog printer tab).
  - EditGCodeDialog ← custom G-code fields' "edit" affordance (machine start/
    end G-code option rows) + CustomGcodeDialog template list.
  - AMS/Firmware/SpeedLimit/PluginManager/EnableLiteMode ← Preferences page
    entries (upstream Preferences hosts them) or honest gating.
  - CreatePresetsDialog ← Phase 235 Task 2 (SettingsDialog preset combo
    "create new" entry).
  - ExportPresetBundleDialog ← File > Export Preset Bundle menu item
    (BBLTopbar) — fixes the File-menu fake-green too.
### Task 2 — DLG-02: save paths
- EditGCodeDialog: onGcodeAccepted (main.qml:547-549 no-op) → forward edited
  text to ConfigViewModel::setValue for the source option key.
- WipeTowerDialog OK → persist flush matrix via new
  PresetServiceMock::saveFlushVolumes (calculateFlushMatrix exists :283-295).
### Task 3 — DLG-03: missing dialogs
- FileArchiveDialog (zip/3mf tree multi-select on import),
  RecenterDialog (out-of-bed objects prompt; EditorViewModel.cpp:6820 has the
  detection), ObjColorDialog (OBJ mtl colors → extruder mapping),
  SysInfoDialog (OpenGL/config dump), SingleChoiceDialog (shared), and 3MF
  load warnings Newer3mfVersion/MsgDataIncompatible/Legacy — small QML
  dialogs + service plumbing, each mapped to its upstream counterpart.
### Task 4 — DLG-04: AboutDialog license
- Replace hardcoded LGPL v3 (AboutDialog.qml:65-71) with upstream AGPL-3.0
  text + copyright years (AboutDialog.cpp:151-156).

## Verification

- Canonical build + all suites; QmlUiAudit test asserting every dialogs/*.qml
  is instantiated in main.qml/Preferences AND has ≥1 trigger (signal emitted
  somewhere or menu item).
