---
phase: 116-v4-5-verification-and-cross-workstream-regression
plan: 01
type: verification
wave: 1
depends_on:
  - phase: 103 through 115 (all v4.5 workstreams)
files_modified:
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [WTMESH-04, MEASURE-05]
---

# Phase 116 Plan: v4.5 Verification And Cross-Workstream Regression

## Objective
Close WTMESH-04 + MEASURE-05 + the cross-workstream verification gate. Lock the v4.5 contracts; run the canonical verifier + launch + regression ctest.

## Must Haves
<must_haves>
  <truth id="VV-01">A QmlUiAuditTests source-audit slot `v45CrossWorkstreamRegressionLocked` asserts: (a) WTMESH-04: Option A buildWipeTowerVertices preserved alongside Option B buildWipeTowerMeshVertices (the v4.4 Phase 99 Frozen Decision 2 baseline did not regress); (b) MEASURE-05: MeasureEngine (Phase 114) is wired, producing real measurements (the AABB stub is augmented or documented); (c) the cross-workstream anchors: filament-map 4-value enum + readback + popup, Option B mesh, CLI fixtures, D3D12 opt-in, per-volume ITS + raycaster + Measuring + snap UX. WTMESH-04 + MEASURE-05 + v4.5-named QVERIFY2 messages.</truth>
  <truth id="VV-02">Canonical build: `scripts/auto_verify_with_vcvars.ps1`. Production OWzxSlicer.exe links clean.</truth>
  <truth id="VV-03">Regression ctest: PartPlateTests, PrepareSceneDataTests, ViewModelSmokeTests, QmlUiAuditTests (incl. the new slot) all PASS. No regression from any v4.5 workstream.</truth>
  <truth id="VV-04">OWzxSlicer.exe launch liveness (5-second no-crash). Per STATE.md, runtime VISUAL evidence blocked.</truth>
  <truth id="VV-05">`git diff --check` exit 0; encoding guard clean.</truth>
</must_haves>

<threat_model>
Verification phase. No production code changes (a source-audit test only). Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="116-01-01" type="test">
    <title>Add v45CrossWorkstreamRegressionLocked source-audit slot</title>
    <read_first>
      - tests/QmlUiAuditTests.cpp (the pattern)
      - src/core/rendering/GizmoGeometry.h (buildWipeTowerVertices + buildWipeTowerMeshVertices — Option A + B coexistence)
      - src/core/rendering/MeasureEngine.h (the real measurement path)
      - src/core/rendering/AssemblyMeasureGeometry.h (the AABB stub)
    </read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add `v45CrossWorkstreamRegressionLocked` slot asserting: (a) Option A + B coexist (buildWipeTowerVertices + buildWipeTowerMeshVertices both present); (b) MeasureEngine wired; (c) the cross-workstream anchors (filament-map enum, Option B mesh, CLI fixtures, D3D12 opt-in, per-volume ITS, raycaster, Measuring, snap UX). VV-01.
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes (VV-01).</acceptance_criteria>
  </task>

  <task id="116-01-02" type="verification">
    <title>Canonical build + regression ctest + launch liveness + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md, scripts/auto_verify_with_vcvars.ps1, .planning/STATE.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest + 5-second launch liveness. Document the v4.5 verification result in SUMMARY.</action>
    <verify>SUMMARY records build + ctest + liveness.</verify>
    <acceptance_criteria>Production link clean, ctest pass, launch liveness (VV-02/03/04).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest.
2. Source audit: the new slot passes.
3. Encoding guard.

## Success Criteria
- WTMESH-04 + MEASURE-05 closed; every VV truth; v4.5 cross-workstream regression locked.

## Expected Non-Changes
- No new product behavior.
- No libslic3r changes.
- Runtime VISUAL evidence blocked per STATE.md.
