---
phase: 104-cli-fixture-recipes-and-multi-material-model
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 103-cli-fixture-readiness-gate
    provides: FIXTURE-02 — argv fixtures deterministically gated on objectCreated + frameSwapped (the gate Phase 104 recipes rely on).
files_modified:
  - tests/data/multi_material_fixture.3mf
  - tests/data/fixture_recipes.md
  - src/qml_gui/main_qml.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [FIXTURE-01, FIXTURE-03, FIXTURE-04]
---

# Phase 104 Plan: CLI Fixture Recipes And Multi-Material Model

## Objective

Close FIXTURE-01/03/04 — fill in the fixture content that Phase 103's readiness gate now deterministically applies. Adds a multi-material fixture model (so wipe-tower/filament-map/AMS features can be exercised), documents the canonical argv recipe combos that reach each major GUI state, and locks the anti-feature contract (these flags are OWzx-only test-evidence plumbing, NOT a user-facing deep-link product feature).

## Must Haves

<must_haves>
  <truth id="FR-01">A multi-material fixture model exists at `tests/data/multi_material_fixture.3mf` (or `.stl` if 3MF proves unsuitable). It is a real 2+ extruder fixture sourced from `third_party/OrcaSlicer/tests/data/` (preferred — source-truth-aligned) OR generated programmatically with explicit per-extruder geometry. A grep/existence check confirms the file is present and non-empty.</truth>
  <truth id="FR-02">A `tests/data/fixture_recipes.md` document lists canonical argv combos (`OWzxSlicer.exe <flags>`) covering the major GUI states: Prepare empty, Prepare with model, Preview, AssembleView, settings:printer (and other dialog routes from main_qml.cpp:137-160), calibration. Each recipe is a one-liner with the flags + a one-sentence description of what state it reaches.</truth>
  <truth id="FR-03">An anti-feature comment block is added in `main_qml.cpp` near the QCommandLineParser setup (around lines 170-205) explicitly documenting: these 4 flags (`--load-model`, `--open-page`, `--open-dialog`, `--skip-first-run`) are OWzx-only test-evidence plumbing; upstream OrcaSlicer has no equivalent (upstream argv is CLI-only `--load`/`--slice`/positional at `OrcaSlicer.cpp:7183`); they MUST NOT be promoted to a user-facing deep-link product feature (anti-feature per REQUIREMENTS.md).</truth>
  <truth id="FR-04">A `cliFixtureRecipesAndMultiMaterialModelPresent` (or similar) source-audit slot is added to `tests/QmlUiAuditTests.cpp` that: (a) asserts `tests/data/multi_material_fixture.3mf` (or `.stl`) exists; (b) asserts `tests/data/fixture_recipes.md` exists + contains each of the major GUI state names (Prepare/Preview/AssembleView/settings); (c) asserts `main_qml.cpp` contains the anti-feature comment anchor (e.g. `test-evidence plumbing` or `OWzx-only`). Mirrors the Phase 102/103 source-audit pattern (QFile + QT_TESTCASE_SOURCEDIR + QVERIFY2 + FIXTURE-named messages).</truth>
  <truth id="FR-05">The canonical build + regression ctest 4/4 passes (PartPlateTests, PrepareSceneDataTests, ViewModelSmokeTests, QmlUiAuditTests incl. the new slot). OWzxSlicer.exe compiles/links clean.</truth>
  <truth id="FR-06">`git diff --check` exits 0; encoding guard exits 0 on all modified files. All new comments English ASCII-only.</truth>
</must_haves>

<threat_model>
No new attack surface. The fixture model is test data consumed only by test code + the existing --load-model path (already input-validated). The recipes doc is markdown. The anti-feature comment is documentation. Zero new external input, zero network, zero auth. Threats open: 0.
</threat_model>

## Tasks

<tasks>
  <task id="104-01-01" type="implementation">
    <title>Add multi-material fixture model + recipes doc + anti-feature comment</title>
    <read_first>
      - .planning/phases/104-cli-fixture-recipes-and-multi-material-model/104-CONTEXT.md
      - src/qml_gui/main_qml.cpp:122-205 (existing page/dialog routes + QCommandLineParser setup)
      - tests/data/ (current fixtures)
      - third_party/OrcaSlicer/tests/data/ (potential multi-material source fixtures — look for 2+ extruder 3MF)
    </read_first>
    <files>
      - tests/data/multi_material_fixture.3mf
      - tests/data/fixture_recipes.md
      - src/qml_gui/main_qml.cpp
    </files>
    <action>
      **Step 1 — multi-material fixture (FIXTURE-01):** Locate a small 2+ extruder 3MF in `third_party/OrcaSlicer/tests/data/` (search for multi-material / multiextruder test files). If found, copy to `tests/data/multi_material_fixture.3mf`. If none suitable, generate a minimal 2-extruder fixture (two small cubes with distinct extruder assignments) using libslic3r or a hand-authored 3MF. Document the source/derivation in a one-line comment in `fixture_recipes.md`.

      **Step 2 — recipes doc (FIXTURE-03):** Write `tests/data/fixture_recipes.md` listing canonical argv combos. Cover at minimum: Prepare empty (`--open-page prepare`), Prepare with model (`--load-model tests/data/multi_material_fixture.3mf --open-page prepare`), Preview (`--open-page preview`), AssembleView (`--open-page prepare` then user toggles, OR a direct route if one exists), settings:printer (`--open-dialog settings:printer`), calibration (`--open-page calibration`). Each recipe: one-liner flags + one-sentence description.

      **Step 3 — anti-feature comment (FIXTURE-04):** Add a comment block in `main_qml.cpp` immediately above the `QCommandLineParser` setup (around line 170) documenting: these 4 flags are OWzx-only test-evidence plumbing; upstream has no equivalent; MUST NOT be promoted to a user-facing product feature. English ASCII-only.
    </action>
    <verify>
      `ls tests/data/multi_material_fixture.3mf` (exists, non-empty)
      `grep -c "Prepare\|Preview\|AssembleView\|settings" tests/data/fixture_recipes.md` (recipes cover major states)
      `grep -n "test-evidence plumbing\|OWzx-only" src/qml_gui/main_qml.cpp` (anti-feature comment present)
    </verify>
    <acceptance_criteria>
      - Multi-material fixture exists at tests/data/multi_material_fixture.3mf (or .stl) (FR-01).
      - fixture_recipes.md exists with recipes for each major GUI state (FR-02).
      - Anti-feature comment block present in main_qml.cpp near QCommandLineParser (FR-03).
    </acceptance_criteria>
  </task>

  <task id="104-01-02" type="test">
    <title>Add cliFixtureRecipesAndMultiMaterialModelPresent source-audit slot</title>
    <read_first>
      - tests/QmlUiAuditTests.cpp (existing source-audit pattern from Phase 102/103)
      - tests/data/fixture_recipes.md (the doc to assert)
      - src/qml_gui/main_qml.cpp (the anti-feature comment to assert)
    </read_first>
    <files>
      - tests/QmlUiAuditTests.cpp
    </files>
    <action>
      Add a `cliFixtureRecipesAndMultiMaterialModelPresent` slot to QmlUiAuditTests mirroring the Phase 102/103 pattern. Assert: (a) the multi-material fixture file exists (QFileInfo::exists); (b) fixture_recipes.md exists + contains the major GUI state names; (c) main_qml.cpp contains the anti-feature comment anchor. Each QVERIFY2 message names the FIXTURE-01/03/04 contract it locks.
    </action>
    <verify>
      `rg -n "cliFixtureRecipesAndMultiMaterialModelPresent" tests/QmlUiAuditTests.cpp`
      ctest QmlUiAuditTests passes incl. the new slot.
    </verify>
    <acceptance_criteria>
      - The slot exists and passes (FR-04). Deterministic (QFile + QFileInfo + QT_TESTCASE_SOURCEDIR).
    </acceptance_criteria>
  </task>

  <task id="104-01-03" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>
      - .codex/rules/build-rules.md
      - scripts/auto_verify_with_vcvars.ps1
    </read_first>
    <files></files>
    <action>
      Run canonical build + regression ctest 4/4. Document the result (production link OK, ctest 4/4 pass, optional launch liveness) in the SUMMARY. Fall back to targeted ninja if the wrapper times out.
    </action>
    <verify>
      SUMMARY records canonical build + ctest 4/4 pass.
    </verify>
    <acceptance_criteria>
      - Canonical build production-link clean (FR-05).
      - Regression ctest 4/4 pass (FR-05).
    </acceptance_criteria>
  </task>
</tasks>

## Verification

1. Canonical build: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
2. Regression ctest: `ctest --test-dir build -R "PartPlateTests|PrepareSceneDataTests|ViewModelSmokeTests|QmlUiAuditTests" --output-on-failure`
3. Source audit: `rg -n "test-evidence plumbing|OWzx-only" src/qml_gui/main_qml.cpp` + `ls tests/data/multi_material_fixture.3mf tests/data/fixture_recipes.md`
4. Encoding guard on modified files.
5. `git diff --check`

## Success Criteria

- FIXTURE-01/03/04 closed; every FR truth in must_haves; files_modified lists the fixture + recipes doc + main_qml.cpp + QmlUiAuditTests.cpp; threat_model zero-surface.

## Expected Non-Changes

- No change to the Phase 103 gate (FIXTURE-02).
- No new argv flags (the 4 existing flags are the full set).
- No user-facing deep-link product feature (anti-feature).
- No libslic3r changes.
