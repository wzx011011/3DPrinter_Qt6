---
phase: 51
plan: 03
status: pending
wave: 3
depends_on: ["51-01", "51-02"]
files_modified:
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
  - src/qml_gui/qml.qrc
  - docs/v3.6-ui-inventory.md
autonomous: true
requirements: [SHELL-02, SHELL-03, SHELL-04, SHELL-05]
---

# Plan 51-03: Shell State Tests, Notification Guard, Toolbar.qml Removal, Inventory Reconciliation

<objective>
Close out Phase 51 with the verification + cleanup layer. Four concerns: (1) Add automated tests proving the C++ shell gates emit correctly (canUndo/canRedo reflect the undo stack, canSave disables while slicing, the SHELL-02 round-trip preserves state) — extend the existing `tests/ViewModelSmokeTests.cpp` with a BackendContext shell-state slot and add a QmlUiAuditTests slot asserting the BBLTopbar Undo/Redo/Save/Slice bindings (the "QML route/resource registration" + "C++ viewmodel state" automated tests from CONTEXT). (2) Add a notification-placement guard test asserting all 3 notification surfaces (ErrorBanner/ErrorToast/NotificationCenter) keep their non-overlapping anchoring (SHELL-04). (3) Remove the stale unused `components/Toolbar.qml` + its `qml.qrc` entry (the ONLY shell-level legacy removal in Phase 51 per CONTEXT; SHELL-05). (4) Reconcile the inventory PREP-TOP `qt_target` cell to cite `BBLTopbar.qml` and add the Toolbar.qml removal to the §7 cleanup checklist. This plan depends on both prior plans (the C++ gates must exist to test them; the QML bindings must exist to audit them).

This plan does NOT remove the 4 orphaned pages (ModelMallPage, PreferencesPage, DeviceListPage, ConfigPage) — those are DEFERRED to Phase 57 per CONTEXT.
</objective>

<scope_fence>
TESTS + qrc + INVENTORY only. Files modified: `tests/ViewModelSmokeTests.cpp`, `tests/QmlUiAuditTests.cpp`, `src/qml_gui/qml.qrc`, `docs/v3.6-ui-inventory.md`. Plus deletion of `src/qml_gui/components/Toolbar.qml`.
- Do NOT modify `BackendContext.h/.cpp` (Plan 51-01) or `BBLTopbar.qml` (Plan 51-02) — only test against them.
- Do NOT remove any page QML file. The 4 orphaned pages (ModelMallPage, PreferencesPage, DeviceListPage, ConfigPage) stay — deferred to Phase 57. Only `components/Toolbar.qml` is removed (it is unused shell-level legacy that conflicts with the PREP-TOP reconciliation).
- Do NOT modify `main.qml` notification placement logic (it is already non-overlapping per CONTEXT — Phase 51's job is to verify + guard, not redesign). main.qml is only read, not written, by the notification guard test.
- Do NOT create a new test binary/target. Extend the two existing QtTest files (`ViewModelSmokeTests.cpp`, `QmlUiAuditTests.cpp`) — they are already wired into CMake/CTest.
- Do NOT add i18n `.ts` entries beyond active-workflow strings (Future item).
</scope_fence>

<tasks>

1.
<action>
In `tests/ViewModelSmokeTests.cpp`, add a new private slot declaration `void shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip();` in the `private slots:` block (near the other Phase 02-01 BackendContext tests around lines 123-137), and implement it after the existing `testTabSelectDrivesViewMode()` test. This test is the C++ viewmodel-state automated test for SHELL-02 + SHELL-03.

Implementation (construct BackendContext standalone like the existing `testRequestSelectTabSignal` does; it does not require libslic3r for the pure gate/state-preservation assertions, but it shares the `initTestCase` HAS_LIBSLIC3R gate — that is acceptable since the existing tab tests run the same way):
- Construct `BackendContext ctx;`.
- Assert the 8 gate properties are readable via the meta-object (proves Q_PROPERTY registered): for each of `canImport`, `canSlice`, `isSlicing`, `canExport`, `canSave`, `canUndo`, `canRedo`, `isBusy`, call `ctx.property("canUndo").toBool()` etc. and QVERIFY the meta-property exists (`ctx.metaObject()->indexOfProperty("canUndo") >= 0`).
- Assert `canUndo`/`canRedo` start false on a fresh BackendContext (empty undo stack): `QVERIFY(!ctx.property("canUndo").toBool())`, same for canRedo.
- SHELL-02 round-trip: set `ctx.setCurrentPage(tp3DEditor)` (1), then `ctx.requestSelectTab(tpPreview)` (2), then `ctx.requestSelectTab(tp3DEditor)` (1) again. Assert `ctx.currentPage()` returns to 1 and `ctx.currentViewMode()` returns to View3D (0) — proving the Prepare↔Preview round-trip preserves page/view state. Assert the gate properties are still readable and unchanged after the round-trip (no reset).
- Assert the 4 label properties are readable: `exportActionLabel`, `exportActionHint`, `saveActionLabel`, `saveActionHint` each have `indexOfProperty >= 0`.
- Assert `canSave` is true on a fresh idle context (not slicing): `QVERIFY(ctx.property("canSave").toBool())` — the slicing-disable is exercised conceptually; a full isSlicing=true assertion requires a running slice which is out of scope here (the gating logic is unit-covered by the canSave() body containing `!isSlicing()` which Plan 51-01 acceptance already enforced).

Use `QSignalSpy` on `&BackendContext::stateChanged` to assert that emitting EditorViewModel stateChanged (e.g. after `editorViewModel()->loadFile(...)` if libslic3r is available, else skip with `QSKIP` guarded by `hasLibslic3r()`) propagates to BackendContext::stateChanged — this proves the SHELL-02 forwarding wiring from Plan 51-01 task 4.
</action>
<read_first>
- tests/ViewModelSmokeTests.cpp lines 117-207 (the `ViewModelSmokeTests` class, `private slots:` block, existing BackendContext tests `testTabPositionEnumValues`/`testRequestSelectTabSignal`/`testTabSelectDrivesViewMode` — mirror their standalone `BackendContext ctx;` construction and QSignalSpy style)
- tests/ViewModelSmokeTests.cpp lines 1233-1422 (existing BackendContext test implementations — the exact pattern to follow for the new slot)
- src/qml_gui/BackendContext.h (the 8 gate + 4 label Q_PROPERTY names from Plan 51-01 to read via `ctx.property(...)`)
- src/qml_gui/BackendContext.cpp (the `stateChanged` forwarding wiring from Plan 51-01 task 4 — confirms the QSignalSpy assertion target)
- .planning/phases/51-shell-and-navigation-restoration/51-CONTEXT.md (SHELL-02: verify + add tests asserting state survives Prepare→Preview→Prepare round-trip; SHELL-03: gate properties emit correctly)
</read_first>
<acceptance_criteria>
- New slot declared: `grep -c "shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip" tests/ViewModelSmokeTests.cpp` returns >= 2 (declaration + implementation).
- The test asserts all 8 gate properties exist: the test body contains `indexOfProperty("canImport")`, `indexOfProperty("canSave")`, `indexOfProperty("isBusy")` (at minimum these three plus the others).
- The test asserts canUndo/canRedo start false: the test body contains `!ctx.property("canUndo").toBool()` and `canRedo`.
- The test performs the SHELL-02 round-trip: the test body contains `requestSelectTab` with tpPreview AND tp3DEditor and asserts the final `currentPage()`/`currentViewMode()`.
- The test uses QSignalSpy on `&BackendContext::stateChanged`: the test body contains `QSignalSpy` and `BackendContext::stateChanged`.
</acceptance_criteria>

2.
<action>
In `tests/QmlUiAuditTests.cpp`, add a new private slot `void shellActionsBindToBackendContextGates();` in the `private slots:` block (near the existing `prepareReadinessControlsBindBackendAvailability` slot around line 20) and implement it. This is the QML route/resource registration audit for SHELL-03 — it locks the BBLTopbar binding contract from Plan 51-02 against regression.

Implementation (mirror the existing `readSource(QStringLiteral("src/qml_gui/BBLTopbar.qml"))` + `QVERIFY2(...contains(...))` pattern used throughout this file):
- Read `src/qml_gui/BBLTopbar.qml`.
- Assert Undo/Redo toolbar buttons bind `enabled` to BOTH page-gate AND canUndo/canRedo: `QVERIFY2(topbar.contains("backend.currentPage === backend.tp3DEditor && backend.canUndo"), ...)` and the canRedo equivalent.
- Assert the page gate is preserved (defense-in-depth): `QVERIFY2(topbar.contains("backend.currentPage === backend.tp3DEditor"), "Undo/Redo must keep the page gate")`.
- Assert Save binds to canSave: `QVERIFY2(topbar.contains("enabled: backend.canSave"), ...)`.
- Assert Slice binds to canSlice: `QVERIFY2(topbar.contains("enabled: backend.canSlice"), ...)`.
- Assert canUndo appears >= 2 times (toolbar + menu) and canRedo >= 2 times: count occurrences and QVERIFY `>= 2`.

Also add a second new slot `void notificationSurfacesStayNonOverlapping();` for SHELL-04. Read `src/qml_gui/components/ErrorBanner.qml`, `src/qml_gui/components/ErrorToast.qml`, `src/qml_gui/components/NotificationCenter.qml`, and `src/qml_gui/main.qml`. Assert the placement contract is unchanged/non-overlapping:
- ErrorBanner uses inline push-down (not floating): `QVERIFY2(banner.contains("Layout.fillWidth: true"), "ErrorBanner must stay inline Layout.fillWidth (no overlap)")` and `QVERIFY2(!banner.contains("z: 200"), "ErrorBanner must not float at z=200 (that is ErrorToast)")`.
- ErrorToast is the floating centered overlay: `QVERIFY2(toast.contains("z: 200"), "ErrorToast must keep z=200 floating overlay")` and `QVERIFY2(toast.contains("anchors.bottom"), "ErrorToast anchors to bottom (not covering sidebar)")`.
- NotificationCenter is a top-right popup, not overlapping the slice/print dropdowns: `QVERIFY2(center.contains("anchors") || center.contains("Popup") || center.contains("x:"), "NotificationCenter keeps explicit placement")`.
- main.qml keeps all 3 surfaces mounted: `QVERIFY2(mainQml.contains("ErrorBanner { }"), ...)`, `ErrorToast { }`, `NotificationCenter {` (the three placements from main.qml lines 461-468, 564).

This is the SHELL-04 guard test — it asserts the existing non-overlapping placement is preserved (Phase 51 verifies, does not redesign, per CONTEXT).
</action>
<read_first>
- tests/QmlUiAuditTests.cpp lines 1-47 (the class, readSource helper, existing `private slots:` — mirror the slot declaration + readSource pattern)
- tests/QmlUiAuditTests.cpp lines 336-368 (`prepareReadinessControlsBindBackendAvailability` — the exact QVERIFY2 + readSource style to copy)
- src/qml_gui/BBLTopbar.qml (the bindings from Plan 51-02 to assert: canUndo/canRedo/canSave/canSlice)
- src/qml_gui/components/ErrorBanner.qml (Layout.fillWidth inline push-down — confirmed at lines 9-10)
- src/qml_gui/components/ErrorToast.qml (z:200 floating overlay, anchors.bottom — confirmed at lines 28-30, 38)
- src/qml_gui/components/NotificationCenter.qml (top-right Popup placement)
- src/qml_gui/main.qml lines 461-565 (the 3 notification surfaces mounted: NotificationCenter, ErrorBanner, ErrorToast)
- .planning/phases/51-shell-and-navigation-restoration/51-CONTEXT.md (SHELL-04: keep all 3 placements, add a guard test asserting non-overlap; SHELL-03 automated test = QML route/resource registration)
</read_first>
<acceptance_criteria>
- Two new slots declared: `grep -cE "void (shellActionsBindToBackendContextGates|notificationSurfacesStayNonOverlapping)\\(\\);" tests/QmlUiAuditTests.cpp` returns 2.
- The shell-actions test reads BBLTopbar.qml and asserts the canUndo/canRedo/canSave/canSlice bindings: the test body contains `readSource(QStringLiteral("src/qml_gui/BBLTopbar.qml"))` and `contains` assertions for each of the 4 properties.
- The notification test reads all 3 components + main.qml: the test body contains `readSource(QStringLiteral("src/qml_gui/components/ErrorBanner.qml"))`, `ErrorToast.qml`, `NotificationCenter.qml`, and `main.qml`.
- The notification test asserts z:200 belongs to ErrorToast (not ErrorBanner): the test body distinguishes the two.
</acceptance_criteria>

3.
<action>
Remove the stale unused `src/qml_gui/components/Toolbar.qml` and its `qml.qrc` entry — the ONLY shell-level legacy removal in Phase 51 (SHELL-05; CONTEXT: "Phase 51 removes ONLY shell-level legacy that conflicts with the restored workflow: delete the unused components/Toolbar.qml and its qml.qrc entry").

Steps:
- Delete the file `src/qml_gui/components/Toolbar.qml` (confirm first it is unused: `grep -rn "Toolbar" src/qml_gui --include=*.qml | grep -v "GLToolbars\|BBLTopbar"` should return no live import/reference to `components/Toolbar` — only `GLToolbars.qml` (different file) and `BBLTopbar.qml`'s comment references remain).
- In `src/qml_gui/qml.qrc`, remove the single line `<file>components/Toolbar.qml</file>` (currently at line 55). Do NOT remove the `components/GLToolbars.qml` line (line 71 — different file, still live).
- Confirm no C++ references `Toolbar.qml` by path string: `grep -rn "components/Toolbar" src/` returns nothing after removal.

This is the SHELL-05 deliverable for shell-level legacy removal. The 4 orphaned pages are NOT removed (deferred to Phase 57).
</action>
<read_first>
- src/qml_gui/components/Toolbar.qml (the file to delete — confirm it is the stale unused 1257-byte stub)
- src/qml_gui/qml.qrc lines 53-71 (the Components block — line 55 `<file>components/Toolbar.qml</file>` is the entry to remove; line 71 `<file>components/GLToolbars.qml</file>` MUST stay)
- run `grep -rn "Toolbar" src/qml_gui --include=*.qml` to confirm Toolbar.qml is not imported by any live QML (only GLToolbars/BBLTopbar comments reference the word)
- run `grep -rn "components/Toolbar" src/` to confirm no C++ loads it by path
- .planning/phases/51-shell-and-navigation-restoration/51-CONTEXT.md (SHELL-05: "delete the unused components/Toolbar.qml and its qml.qrc entry"; "Phase 51 removes ONLY shell-level legacy")
- docs/v3.6-ui-inventory.md §7 (cleanup checklist format — Toolbar.qml is NOT yet listed there; task 4 adds it)
</read_first>
<acceptance_criteria>
- `components/Toolbar.qml` no longer exists: `test ! -f src/qml_gui/components/Toolbar.qml` passes (the executor runs this and confirms exit 0).
- `qml.qrc` has no Toolbar.qml entry: `grep -c "components/Toolbar.qml" src/qml_gui/qml.qrc` returns 0.
- `GLToolbars.qml` entry preserved: `grep -c "components/GLToolbars.qml" src/qml_gui/qml.qrc` returns 1.
- No live import of Toolbar remains: `grep -rn "components/Toolbar" src/` returns nothing; `grep -rn "\"Toolbar\"" src/qml_gui --include=*.qml` returns nothing referencing the deleted component.
</acceptance_criteria>

4.
<action>
Reconcile the inventory PREP-TOP row in `docs/v3.6-ui-inventory.md` (§2 Prepare table, the PREP-TOP row currently at line ~140) — a factual correction per CONTEXT specifics ("This is a factual correction, not a re-decision — update the inventory cell to match the codebase").

In the PREP-TOP row, change the `qt_target` cell from `src/qml_gui/main.qml + src/qml_gui/components/Toolbar.qml` to `src/qml_gui/main.qml + src/qml_gui/BBLTopbar.qml` (Toolbar.qml is being removed in task 3; BBLTopbar.qml is the live PREP-TOP component). Keep all other cells (region_name, visible_controls, upstream_source `Plater.cpp MainFrame::* + GUI_App.cpp`, status `Hybrid`, verification `manual-visual`, modify_or_replace `modify`, cleanup `n/a`) unchanged.

Then in the §7 aggregate cleanup checklist code block (around lines 304-319), add a new `file:` line for the Toolbar.qml removal so it is grep-friendly and consumed by Phase 57/Phase 58. Add exactly one line in the `file:` group:
```
file:    src/qml_gui/components/Toolbar.qml
```
Place it after the existing Settings `file:` lines, with a short note line above it if needed: `# Phase 51 shell-level legacy removal (PREP-TOP reconciliation)`. Keep the six-tag machine-grep format (`file:` / `qrc:` / `route:` / `import:` / `test:` / `doc:`).

Do NOT change any other region row, status term, or the FROZEN column schema. This is a targeted qt_target cell correction + one cleanup-checklist addition.
</action>
<read_first>
- docs/v3.6-ui-inventory.md §2 PREP-TOP row (line ~140 — the row to correct; confirm current `qt_target` cell names Toolbar.qml)
- docs/v3.6-ui-inventory.md §6 PREP-TOP modify row (line ~254 — rationale mentions "main.qml + Toolbar.qml"; update the rationale text to cite BBLTopbar.qml too so §6 stays consistent with §2)
- docs/v3.6-ui-inventory.md §7 (lines 304-319 — the aggregate cleanup checklist; add the Toolbar.qml `file:` line in grep format)
- .planning/phases/51-shell-and-navigation-restoration/51-CONTEXT.md (specifics: "update the inventory's qt_target cell to cite BBLTopbar.qml"; "Mark Toolbar.qml for Phase 57 removal in the inventory §7 cleanup checklist")
- .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md (the frozen snapshot — do NOT edit it; only the canonical doc mutates per its own §0 note)
</read_first>
<acceptance_criteria>
- PREP-TOP qt_target now cites BBLTopbar: `grep -c "src/qml_gui/main.qml + src/qml_gui/BBLTopbar.qml" docs/v3.6-ui-inventory.md` returns >= 1.
- PREP-TOP qt_target no longer cites components/Toolbar.qml: the PREP-TOP row's qt_target cell does not contain `components/Toolbar.qml`. Verify with: extract the PREP-TOP row and confirm `BBLTopbar.qml` is present and `components/Toolbar.qml` is absent in that cell.
- §7 cleanup checklist has the Toolbar.qml file line: `grep -c "file:    src/qml_gui/components/Toolbar.qml" docs/v3.6-ui-inventory.md` returns 1.
- §6 PREP-TOP rationale updated to cite BBLTopbar.qml (consistency with §2): the §6 PREP-TOP row rationale mentions BBLTopbar.qml.
- Column schema unchanged: `grep -c "| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |" docs/v3.6-ui-inventory.md` still returns >= 4 (the 4 region tables unchanged).
- The frozen snapshot 50-INVENTORY.md is NOT edited: `git status --porcelain .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns nothing.
</acceptance_criteria>

5.
<action>
Build + test verification — the final Phase 51 gate. Run the canonical build command which compiles AND runs CTest (including the two extended test files). The build MUST exit 0 AND the new `shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip`, `shellActionsBindToBackendContextGates`, and `notificationSurfacesStayNonOverlapping` test slots MUST pass (the Toolbar.qml removal must not break the qrc compile; the inventory edit is doc-only).

Command: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`

If a test fails, fix the underlying code (Plans 51-01/51-02) or the test assertion before declaring Phase 51 complete — do not weaken a correct assertion to force a pass (source-truth rule: "Tests should lock the intended complete behavior"). Record the test result in the plan SUMMARY.
</action>
<read_first>
- scripts/auto_verify_with_vcvars.ps1 (ONLY build+test command per AGENTS.md; confirm it runs CTest)
- tests/ViewModelSmokeTests.cpp + tests/QmlUiAuditTests.cpp (the test files just extended — confirm they compile and the new slots are wired into the Q_OBJECT private slots)
- src/qml_gui/qml.qrc (confirm Toolbar.qml entry removed and the qrc still parses — a dangling entry would break RCC compile)
- .codex/rules/source-truth-migration.md (Required Verification: canonical build command; "When a test is built but not run, state that explicitly")
</read_first>
<acceptance_criteria>
- Build command exits 0: `scripts/auto_verify_with_vcvars.ps1` returns success.
- The 3 new test slots pass (executor reports CTest output showing `shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip`, `shellActionsBindToBackendContextGates`, `notificationSurfacesStayNonOverlapping` as PASS, or states explicitly if any is skipped due to HAS_LIBSLIC3R and why).
- qrc compiles (RCC) with Toolbar.qml removed — no "file does not exist" RCC error.
- `git status --porcelain` shows: `tests/ViewModelSmokeTests.cpp`, `tests/QmlUiAuditTests.cpp`, `src/qml_gui/qml.qrc`, `docs/v3.6-ui-inventory.md` modified, and `src/qml_gui/components/Toolbar.qml` deleted — and nothing else from this plan.
</acceptance_criteria>

</tasks>

<verification>
1. C++ state test added: `grep -c "shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip" tests/ViewModelSmokeTests.cpp` >= 2.
2. QML audit tests added: `grep -cE "void (shellActionsBindToBackendContextGates|notificationSurfacesStayNonOverlapping)\\(\\);" tests/QmlUiAuditTests.cpp` returns 2.
3. Toolbar.qml removed (SHELL-05): `test ! -f src/qml_gui/components/Toolbar.qml` && `grep -c "components/Toolbar.qml" src/qml_gui/qml.qrc` returns 0.
4. GLToolbars preserved: `grep -c "components/GLToolbars.qml" src/qml_gui/qml.qrc` returns 1.
5. Inventory reconciled: `grep -c "src/qml_gui/main.qml + src/qml_gui/BBLTopbar.qml" docs/v3.6-ui-inventory.md` >= 1 AND `grep -c "file:    src/qml_gui/components/Toolbar.qml" docs/v3.6-ui-inventory.md` returns 1.
6. Orphaned pages NOT removed (scope fence): `test -f src/qml_gui/pages/ModelMallPage.qml && test -f src/qml_gui/pages/PreferencesPage.qml && test -f src/qml_gui/pages/DeviceListPage.qml && test -f src/qml_gui/pages/ConfigPage.qml` — all 4 still exist (deferred to Phase 57).
7. Build + tests pass via ONLY command: `scripts/auto_verify_with_vcvars.ps1` exits 0, the 3 new slots pass (or skip is explicitly stated with reason).
8. Frozen snapshot untouched: `git status --porcelain .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns nothing.
</verification>

<must_haves>
- An automated C++ viewmodel-state test (`shellStateGatesForwardToEditorViewModelAndPreserveRoundTrip`) proves the 8 shell gates are Q_PROPERTY-registered, canUndo/canRedo reflect the empty undo stack, and the SHELL-02 Prepare↔Preview→Prepare round-trip preserves page/view state without reset (SHELL-02 + SHELL-03 automated coverage).
- An automated QML route/registration audit (`shellActionsBindToBackendContextGates`) locks the BBLTopbar Undo/Redo/Save/Slice bindings to the C++ gates from Plan 51-02, preventing regression of the undo-stack-empty fix (SHELL-03).
- A notification-placement guard test (`notificationSurfacesStayNonOverlapping`) asserts all 3 surfaces (ErrorBanner inline / ErrorToast z=200 floating / NotificationCenter top-right) keep their non-overlapping anchoring — SHELL-04 verified + guarded, not redesigned.
- The stale unused `components/Toolbar.qml` and its `qml.qrc` entry are removed (the ONLY shell-level legacy removal in Phase 51 — SHELL-05), with GLToolbars.qml preserved.
- The inventory PREP-TOP `qt_target` cell is corrected to cite `BBLTopbar.qml` and §7 cleanup checklist gains the Toolbar.qml `file:` line (factual reconciliation, INV-05 / CLEAN-01 forward-feed).
- The 4 orphaned pages (ModelMallPage, PreferencesPage, DeviceListPage, ConfigPage) are NOT removed — deferred to Phase 57 (scope fence respected).
- `scripts/auto_verify_with_vcvars.ps1` (ONLY build command) builds and the 3 new test slots pass.
</must_haves>
