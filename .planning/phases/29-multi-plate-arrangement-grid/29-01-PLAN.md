---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/model/PartPlateList.h
  - src/core/model/PartPlateList.cpp
  - src/core/model/PartPlate.h
autonomous: true
requirements: [ARRANGE-01]
must_haves:
  truths:
    - "PartPlateList owns the plate-grid geometry fields m_plate_cols, m_plate_width, m_plate_depth, m_plate_height and recomputes m_plate_cols from the plate count via update_plate_cols() after every structural change (createPlate/deletePlate/movePlate)."
    - "compute_colum_count reproduces upstream float-sqrt + round + strict-greater comparison exactly (PartPlate.hpp:38-50), yielding cols=1 for count=1, cols=3 for count=5, cols=6 for count=26 — NOT integer ceil."
    - "plate_stride_x()/plate_stride_y() equal size * (1 + LOGICAL_PART_PLATE_GAP) with LOGICAL_PART_PLATE_GAP = 1.0/5.0 (PartPlate.cpp:55); the file-scope constexpr is private to PartPlateList.cpp and not exported."
    - "compute_origin(i, cols) yields Vec3d {col*stride_x, -row*stride_y, 0} so plate 0 is (0,0,0), plate 1 is (+stride_x,0,0), plate cols is (0,-stride_y,0) — Y goes NEGATIVE for rows below the first."
    - "compute_plate_index(double translationX_mm, double translationY_mm) inverts the encode: col_value = translationX_mm / stride_x; row_value = (stride_y - translationY_mm) / stride_y; row=round(row_value); col=round(col_value); return row*m_plate_cols + col. This is the sign-flip decode (PartPlate.cpp:5365-5376) and takes mm doubles, NOT an ArrangePolygon (keeps PartPlateList a pure domain object free of libslic3r)."
    - "updatePlateOrigins() iterates plates and calls plate->setOrigin(compute_origin(i, m_plate_cols)) for each i; createPlate/deletePlate/movePlate call update_plate_cols() then updatePlateOrigins() after reindex (closes Phase 17 D-07 deferral)."
    - "PartPlate gains bool empty() const { return m_obj_to_instance_set.empty(); } mirroring upstream PartPlate.hpp:387; used by the recycling loop in Plan 03. (has_printable_instances is deferred — needs a model backref Qt6's PartPlate lacks.)"
    - "DATA LAYER ONLY: writing real origins is correct and necessary, but GLViewport does NOT read PartPlate::origin() (grep confirms origin()/setOrigin are defined but never called in src/). Multi-plate RENDERING is out of Phase 29 scope and will NOT follow automatically."
  artifacts:
    - path: "src/core/model/PartPlateList.h"
      provides: "Declarations of m_plate_cols/m_plate_width/m_plate_depth/m_plate_height, compute_colum_count (file-scope static), plate_stride_x/y, compute_shape_position, compute_origin, compute_plate_index, update_plate_cols, updatePlateOrigins, and a setPlateSize(int,int,int) test seam"
      min_lines: 120
    - path: "src/core/model/PartPlateList.cpp"
      provides: "Implementations of all geometry methods + LOGICAL_PART_PLATE_GAP constexpr + update_plate_cols/updatePlateOrigins calls wired into createPlate/deletePlate/movePlate"
      min_lines: 200
    - path: "src/core/model/PartPlate.h"
      provides: "bool empty() const accessor mirroring upstream PartPlate.hpp:387"
      min_lines: 245
  key_links:
    - from: "PartPlateList::compute_plate_index"
      to: "model instance world translations (consumed by Plan 03 rebuildPlatesAfterArrangement and Plan 04 arrangeObjects)"
      via: "double translationX_mm/translationY_mm parameters extracted from Slic3r::ModelInstance::get_offset() by ProjectServiceMock"
    - from: "PartPlateList::updatePlateOrigins"
      to: "PartPlate::setOrigin"
      via: "compute_origin(i, m_plate_cols) written per plate on every structural change (D-29-13/D-29-14)"
---

# Plan 29-01: PartPlateList Plate-Grid Geometry + PartPlate::empty()

<objective>
Land the ARRANGE-01 geometry foundation on PartPlateList: the plate-grid fields (m_plate_cols, m_plate_width, m_plate_depth, m_plate_height), the upstream compute_* function set (compute_colum_count, plate_stride_x/y, compute_shape_position, compute_origin, compute_plate_index), the maintenance helpers (update_plate_cols, updatePlateOrigins), and a setPlateSize test seam. Wire update_plate_cols() + updatePlateOrigins() into the existing createPlate/deletePlate/movePlate so plate origins are always consistent with plate position (closes Phase 17 D-07 deferral). Also add PartPlate::empty() for the recycling loop in Plan 03. This is a pure domain-object change with NO libslic3r dependency.
</objective>

<context>
Read first:
- `.planning/phases/29-multi-plate-arrangement-grid/29-CONTEXT.md` (decisions D-29-1 through D-29-4, D-29-13, D-29-14)
- `.planning/phases/29-multi-plate-arrangement-grid/29-RESEARCH.md` (sections 1.1-1.9, 4.1, 4.2, 8 gotchas 1/2/6/11/12)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.hpp:36-50` (MAX_PLATE_COUNT, compute_colum_count)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.hpp:387` (empty())
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:55` (LOGICAL_PART_PLATE_GAP)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:3905-3964` (compute_origin, compute_shape_position)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:4836-4870` (plate_stride_x/y, update_plate_cols)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:4872-4892` (update_all_plates_pos_and_size — the origin-update loop)
- `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:5365-5376` (compute_plate_index with sign flip)
- `src/core/model/PartPlateList.h` (current state — the file being modified)
- `src/core/model/PartPlateList.cpp` (current createPlate/deletePlate/movePlate — the wiring targets)
- `src/core/model/PartPlate.h` (origin()/setOrigin API, m_obj_to_instance_set)
- `CONVENTIONS.md` section in AGENTS.md (m_ prefix, 2-space indent, final on QObject — N/A here since PartPlateList is not QObject)
</context>

<tasks>
  <task id="29-01-T1" type="auto">
    <name>Add PartPlate::empty() accessor</name>
    <read_first>
- src/core/model/PartPlate.h (the file being modified — note m_obj_to_instance_set at line 223, clearInstances() at 156)
- third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.hpp:387 (upstream empty() = obj_to_instance_set.empty())
- .planning/phases/29-multi-plate-arrangement-grid/29-RESEARCH.md section 8 gotcha 6
    </read_first>
    <action>
Add a public const accessor to the OWzx::PartPlate class in src/core/model/PartPlate.h, immediately after clearInstances() (which is at line 156). Exact signature and body:

bool empty() const { return m_obj_to_instance_set.empty(); }

Place it adjacent to the membership section. Add a one-line comment citing upstream: `// Mirrors upstream PartPlate::empty() (PartPlate.hpp:387).` Do NOT add has_printable_instances() — it needs a model backref Qt6's PartPlate does not hold (RESEARCH gotcha 6 defers it).
    </action>
    <acceptance_criteria>
- src/core/model/PartPlate.h contains the exact text `bool empty() const { return m_obj_to_instance_set.empty(); }`
- The accessor is const and public (located in the public section near clearInstances)
- A grep for `has_printable_instances` in src/core/model/PartPlate.h returns zero matches (the printable-instance check is NOT added)
- The file still compiles standalone (no new includes needed)
    </acceptance_criteria>
  </task>

  <task id="29-01-T2" type="auto">
    <name>Add plate-grid geometry members and method declarations to PartPlateList.h</name>
    <read_first>
- src/core/model/PartPlateList.h (the file being modified — current 97 lines, m_plate_list at line 88, kMaxPlateCount at line 26)
- third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.hpp:38-50 (compute_colum_count — file-scope inline, NOT a member)
- third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.hpp:569-576 (m_plate_count/m_plate_cols/m_plate_width/m_plate_depth/m_plate_height field declarations)
- third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:3905-3964 (compute_shape_position, compute_origin)
- third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:5365-5376 (compute_plate_index signature + algorithm)
- .planning/phases/29-multi-plate-arrangement-grid/29-CONTEXT.md D-29-2 (full upstream function set), D-29-3 (plate size s
