---
phase: 29
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/PartPlateTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [ARRANGE-01]
must_haves:
  truths:
    - "tests/PartPlateTests.cpp exists as a QtTest target (it does NOT exist today — RESEARCH finding #1), registered in CMakeLists.txt following the PrepareSceneDataTests pattern at CMakeLists.txt:372-382."
    - "The computeColumCount parity test encodes ALL 36 rows from RESEARCH §1.2 as a data-driven test (QTest::addColumn<int> + 36 QTest::newRow entries), covering every perfect square (1,4,9,16,25,36) and every +1 transition point (2,5,10,17,26)."
    - "The computeOrigin grid-math test verifies the sign-flip: plate 0 = (0,0,0), plate 1 (cols=3) = (+stride,0,0), plate 3 (cols=3) = (0,-stride,0), plate 4 (cols=3) = (+stride,-stride,0)."
    - "The computePlateIndex round-trip test verifies the Y sign-flip decode including boundary cases x=239.9 / x=240.0 / x=240.1 and negative-Y translations, characterizing the std::round (round-half-away-from-zero) behavior."
    - "The updatePlateOrigins test verifies D-29-13: after createPlate×4 + updatePlateOrigins, plate(0)->origin()==(0,0,0), plate(1)->origin().x()>0, etc."
  artifacts:
    - path: "tests/PartPlateTests.cpp"
      provides: "QtTest class with private slots for computeColumCount_data/computeColumCount (36-row parity), computeOriginGridMath, computePlateIndexRoundTrip, updatePlateOriginsWritesToPlates"
      min_lines: 180
    - path: "CMakeLists.txt"
      provides: "PartPlateTests target registration: qt_add_executable + target_link_libraries + add_test + set_tests_properties ENVIRONMENT PATH"
      min_lines: 1
  key_links:
    - from: "PartPlateTests"
      to: "PartPlateList geometry (compute_colum_count, computeOrigin, computePlateIndex, updatePlateOrigins)"
      via: "setPlateSize test seam + direct geometry assertions"
---

# Plan 29-02: Create tests/PartPlateTests.cpp + CMake Registration (ARRANGE-01 unit tests)

**Objective:** Create `tests/PartPlateTests.cpp` (which does NOT exist — RESEARCH finding #1) and register it in CMakeLists.txt. Encode the full 36-row compute_colum_count parity table (RESEARCH §1.2), the computeOrigin sign-flip grid math, the computePlateIndex round-trip with Y sign-flip decode, and the updatePlateOrigins write-to-plates behavior (D-29-13). This is the deterministic ARRANGE-01 test coverage that the project's "deterministic tests replace transport confidence" pattern demands.

**Note on wave placement:** Plan 02 is declared wave 1 and `depends_on: []` because the test file can be authored in parallel with Plan 01 (the executor reads Plan 01's declarations). If executing strictly sequentially, run 01 first so the symbols compile. The acceptance criteria for the build gate still requires 01's code to exist.

## Tasks

### 29-02-T1 — Register PartPlateTests target in CMakeLists.txt

**read_first:** `CMakeLists.txt:327-384` (the test-registration block; PrepareSceneDataTests pattern at 372-382, ViewModelSmokeTests at 343-360 linking owzx_app_core); `tests/PrepareSceneDataTests.cpp:45-60` (final QObject class + private slots pattern).

**action:** Add a new target block in CMakeLists.txt inside the `if(BUILD_TESTING AND OWZX_QML_GUI)` block, immediately after the QmlUiAuditTests block (line 370) and before PrepareSceneDataTests (line 372). Use this exact shape mirroring ViewModelSmokeTests (which links owzx_app_core and includes PartPlate.h/PartPlateList.h):

```
qt_add_executable(PartPlateTests tests/PartPlateTests.cpp)
target_link_libraries(PartPlateTests PRIVATE owzx_app_core
    Qt6::Qml Qt6::Quick Qt6::QuickControls2 Qt6::OpenGL Qt6::Concurrent Qt6::Network Qt6::Test
)
target_compile_definitions(PartPlateTests PRIVATE
    QT_TESTCASE_SOURCEDIR="${CMAKE_SOURCE_DIR}"
)
target_link_options(PartPlateTests PRIVATE /FORCE:MULTIPLE)
add_test(NAME PartPlateTests COMMAND PartPlateTests)
set_tests_properties(PartPlateTests PROPERTIES
    ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}"
)
```

Use the `owzx_app_core` aggregation target (same as ViewModelSmokeTests) so PartPlate/PartPlateList sources are linked without listing each .cpp. The QT_TESTCASE_SOURCEDIR definition is needed if the tests reuse `hotend.stl` via the ViewModelSmokeTests.cpp:40-42 kStlPath pattern (Plan 05 uses it).

**acceptance_criteria:**
- CMakeLists.txt contains `qt_add_executable(PartPlateTests tests/PartPlateTests.cpp)`
- CMakeLists.txt contains `target_link_libraries(PartPlateTests PRIVATE owzx_app_core` and `Qt6::Test`
- CMakeLists.txt contains `add_test(NAME PartPlateTests COMMAND PartPlateTests)`
- CMakeLists.txt contains `set_tests_properties(PartPlateTests PROPERTIES` with `ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}"`
- The block is inside the `if(BUILD_TESTING AND OWZX_QML_GUI)` guard

### 29-02-T2 — Create tests/PartPlateTests.cpp with ARRANGE-01 geometry unit tests

**read_first:** `tests/PrepareSceneDataTests.cpp:1-60` (QtTest include + final QObject class + private slots + QVERIFY/QCOMPARE pattern); `tests/ViewModelSmokeTests.cpp:13-42` (includes PartPlate.h + PartPlateList.h, kStlPath QT_TESTCASE_SOURCEDIR pattern); `src/core/model/PartPlateList.h` (the API surface from Plan 01: setPlateSize, plateStrideX/Y, computeOrigin, computePlateIndex, updatePlateOrigins, createPlate, plate); 29-RESEARCH.md sections 1.2 (parity table), 7.1 (test design), 8 gotchas 1/2/3.

**action:** Create `tests/PartPlateTests.cpp` with:
1. Includes: `<QtTest>`, `<QPointF>` if needed, then `#include "core/model/PartPlate.h"` and `#include "core/model/PartPlateList.h"` (mirror ViewModelSmokeTests.cpp:18-19). Wrap Vec3d-consuming tests in `#ifdef HAS_LIBSLIC3R`.
2. Class declaration: `class PartPlateTests final : public QObject { Q_OBJECT private slots: ... };` (final per CONVENTIONS).
3. `void computeColumCount_data();` and `void computeColumCount();` — data-driven test. In `_data()` use `QTest::addColumn<int>("count"); QTest::addColumn<int>("expectedCols");` and add ALL 36 rows from RESEARCH §1.2 with `QTest::newRow("count_1") << 1 << 1;` ... `QTest::newRow("count_36") << 36 << 6;`. Critical rows to include verbatim (count → cols): 1→1, 2→2, 3→2, 4→2, 5→3, 6→3, 7→3, 8→3, 9→3, 10→4, 11→4, 12→4, 13→4, 14→4, 15→4, 16→4, 17→5, 18→5, 19→5, 20→5, 21→5, 22→5, 23→5, 24→5, 25→5, 26→6, 27→6, 28→6, 29→6, 30→6, 31→6, 32→6, 33→6, 34→6, 35→6, 36→6. In the test body: `QFETCH(int, count); QFETCH(int, expectedCols); QCOMPARE(OWzx::compute_colum_count(count), expectedCols);`.
4. `void computeOriginGridMath();` — under #ifdef HAS_LIBSLIC3R. Create a PartPlateList, call `setPlateSize(200, 200, 0)` (stride = 240), createPlate enough times to make cols=3 (4 creates gives plateCount=4, but compute_colum_count(4)=2; need plateCount≥5 for cols=3 — so createPlate 5 times for cols=3). Assertions: `computeOrigin(0, 3) == Vec3d(0,0,0)`; `computeOrigin(1, 3) == Vec3d(240,0,0)`; `computeOrigin(3, 3) == Vec3d(0,-240,0)` (sign flip — row 1); `computeOrigin(4, 3) == Vec3d(240,-240,0)`. Use QVERIFY on `.isApprox(Vec3d(...))` or QCOMPARE on .x()/.y()/.z() individually.
5. `void computePlateIndexRoundTrip();` — under #ifdef HAS_LIBSLIC3R. PartPlateList with setPlateSize(200,200,0), force cols=3 (5 plates). Assertions: `computePlateIndex(0.0, 0.0) == 0`; `computePlateIndex(240.0, 0.0) == 1` (col 1); `computePlateIndex(0.0, -240.0) == 6` (row_value=(240-(-240))/240=2.0 → round=2 → index 2*3+0=6). For (240,-240): col=1, row=2 → index 7. Boundary cases: `computePlateIndex(240.0, 0.0) == 1` (clean boundary); characterize `computePlateIndex(239.9, 0.0)` and `computePlateIndex(120.0, 0.0)` (col_value=0.5, std::round(0.5)=1 round-half-away-from-zero → col 1). Document the round behavior in a comment.
6. `void updatePlateOriginsWritesToPlates();` — under #ifdef HAS_LIBSLIC3R. PartPlateList; setPlateSize(200,200,0); createPlate 3 more times → plateCount=4, cols=2. The createPlate calls in Plan 01 already trigger updatePlateOrigins. Assertions: `plate(0)->origin()` is (0,0,0); `plate(1)->origin().x() == 240.0` (col 1, cols=2); `plate(2)->origin()` is (0,-240.0) (row 1, col 0); `plate(3)->origin().x() == 240.0 && .y() == -240.0`.
7. `QTEST_GUILESS_MAIN(PartPlateTests)` + `#include "PartPlateTests.moc"` (the cpp-internal Q_OBJECT moc include — standard for single-file QtTest).

**acceptance_criteria:**
- tests/PartPlateTests.cpp exists and contains `class PartPlateTests final : public QObject`
- computeColumCount_data contains `QTest::addColumn<int>("count");` and `QTest::addColumn<int>("expectedCols");`
- computeColumCount_data contains exactly 36 `QTest::newRow` calls covering counts 1..36 (perfect squares 1,4,9,16,25,36 and transitions 2,5,10,17,26 all present)
- computeOriginGridMath asserts `computeOrigin(3, 3)` has a NEGATIVE y (sign flip verified)
- computePlateIndexRoundTrip asserts `computePlateIndex(0.0, -240.0)` returns the row-2 index (verifying the `(stride_y - translation_y)/stride_y` decode, NOT `translation_y/stride_y`)
- computePlateIndexRoundTrip includes boundary cases 239.9 / 240.0 / 240.1 with a comment characterizing std::round behavior
- updatePlateOriginsWritesToPlates asserts plate(1)->origin().x() == 240.0 (origin actually written to plate)
- `ctest -R PartPlateTests --output-on-failure` exits 0 after canonical build

## verification

- `ctest --test-dir build -R PartPlateTests --output-on-failure` exits 0.
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` — cmake configure picks up PartPlateTests via AUTOMOC, ninja builds it, ctest runs it green.
- `rg -c "QTest::newRow" tests/PartPlateTests.cpp` — returns ≥ 36.
- AUTOMOC note (CMakeLists.txt:344-352): if a new slot is added later, the canonical verify script re-runs cmake configure which re-scans AUTOMOC; for incremental builds delete `build/PartPlateTests_autogen/timestamp`.

## threat_model

| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| compute_colum_count drifts from upstream after a refactor | Tampering | All 36 rows encoded as data-driven parity test — any drift fails loudly | mitigate |
| Y sign-flip in computePlateIndex inverted silently | Tampering | computePlateIndexRoundTrip asserts negative-Y translation maps to row≥1 via the (stride_y-translation_y) decode | mitigate |
| AUTOMOC misses the new cpp-internal Q_OBJECT | Denial of Service (test silently skipped) | Canonical verify re-runs cmake configure; document autogen timestamp delete for incremental builds | mitigate |
