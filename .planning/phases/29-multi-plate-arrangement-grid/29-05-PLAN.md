---
phase: 29
plan: 05
type: execute
wave: 4
depends_on: [29-02, 29-04]
files_modified:
  - tests/PartPlateTests.cpp
autonomous: true
requirements: [ARRANGE-02, ARRANGE-03]
must_haves:
  truths:
    - "ARRANGE-02 integration test: after arrangeObjects on N objects that overflow a small bed (e.g., 8 objects on a 100×100 bed with large spacing), plateCount() >= 2 AND at least one object's instance lands on plate >= 1 (not all on plate 0). Verified via plateObjectIndices(0) vs plateObjectIndices(1)."
    - "ARRANGE-03 locked-exclusion test: with plate 0 locked holding a known instance, after arrangeObjects, plate 0's objToInstanceSet is UNCHANGED (locked preserved) AND new objects land on plate >= 1."
    - "D-29-12 edge case: with ALL plates locked, arrangeObjects returns false and no membership changes occur (no crash)."
    - "Tests build models in-memory (no Phase 32 FIXTURE-01 dependency) — reuse the hotend.stl path from ViewModelSmokeTests.cpp:41 (kStlPath via QT_TESTCASE_SOURCEDIR) or addPrimitive."
  artifacts:
    - path: "tests/PartPlateTests.cpp"
      provides: "Integration test slots: arrangeDistributesAcrossPlates (ARRANGE-02), lockedPlateExclusion (ARRANGE-03), allLockedReturnsFalse (D-29-12)"
      min_lines: 260
  key_links:
    - from: "PartPlateTests"
      to: "ProjectServiceMock::arrangeObjects (Plan 04) → PartPlateList::rebuildPlatesAfterArrangement (Plan 03)"
      via: "end-to-end multi-plate distribution assertions"
---

# Plan 29-05: ARRANGE-02/03 integration tests through arrangeObjects

**Objective:** Add the ARRANGE-02 multi-plate distribution integration test, the ARRANGE-03 locked-exclusion test, and the D-29-12 all-locked edge case to `tests/PartPlateTests.cpp` (created in Plan 02). These tests exercise arrangeObjects end-to-end with in-memory models (no FIXTURE-01 dependency). They are the deterministic coverage the project's "deterministic tests replace transport confidence" pattern demands.

## Tasks

### 29-05-T1 — Add ARRANGE-02 distribution integration test

**read_first:** `tests/PartPlateTests.cpp` (the file created in Plan 02 — class shape, includes, the QT_TESTCASE_SOURCEDIR macro); `tests/ViewModelSmokeTests.cpp:13-42` (ProjectServiceMock + libslic3r include pattern, kStlPath hotend.stl path construction); `src/core/services/ProjectServiceMock.h:174` (arrangeObjects signature), `:114` (plateObjectIndices — the Q_INVOKABLE bridge for per-plate object indices), `:46-56` (Q_PROPERTY); `src/core/model/PartPlateList.h` (plateCount, plate); 29-RESEARCH.md section 7.2 (ARRANGE-02 test design — small bed forces overflow), 8 gotcha 9 (no fixture, use hotend.stl).

**action:** Add three private slots to the PartPlateTests class in tests/PartPlateTests.cpp. These tests are HAS_LIBSLIC3R-gated (they call arrangeObjects which is a no-op without libslic3r). Use `#ifdef HAS_LIBSLIC3R` around the test bodies and `QSKIP` in the #else branch.
1. `void arrangeDistributesAcrossPlates();` — under #ifdef HAS_LIBSLIC3R:
   - Construct a `ProjectServiceMock service;`
   - Load the hotend.stl fixture: `QVERIFY2(service.loadFile(kStlPath), "hotend.stl must load for arrange test");` where `kStlPath` is constructed identically to ViewModelSmokeTests.cpp:40-42: `const QString kStlPath = QDir::cleanPath(QStringLiteral(QT_TESTCASE_SOURCEDIR) + QStringLiteral("/third_party/OrcaSlicer/resources/profiles/hotend.stl"));` (add `<QDir>` include).
   - Duplicate the object N-1 times (N=8) to force overflow. Use the ProjectServiceMock duplicate API (find the exact Q_INVOKABLE name via grep — likely `duplicateSelectedObjects` or `duplicateObject(int)`). If unclear, use `service.addObject(...)` or whatever the existing ViewModelSmokeTests uses to add objects. Target: 8 distinct objects.
   - Capture initial plateCount: `const int beforeCount = service.plateCount();`
   - Call arrange with a small bed + large spacing to force multi-plate: `const bool ok = service.arrangeObjects(/*spacing=*/50.0f, /*allowRotation=*/false, /*alignY=*/false, QStringLiteral("0,0,100,0,100,100,0,100"));`
   - Assert distribution: `QVERIFY2(ok, "arrange should succeed on the small bed with tolerant vfn"); QVERIFY2(service.plateCount() >= 2, "arrange should distribute across >= 2 plates");` Then assert at least one object is off plate 0 using the confirmed Q_INVOKABLE bridge `plateObjectIndices(int)` (ProjectServiceMock.h:114) — `QVERIFY2(!service.plateObjectIndices(1).isEmpty(), "at least one object should land on plate >= 1");`
   - Determinism note comment: "Assertion is plateCount>=2 AND >=1 object off plate 0, NOT exact counts — libnest2d packing can vary. Fixed spacing + fixed bed + fixed object count keeps this reproducible."
2. `#else void PartPlateTests::arrangeDistributesAcrossPlates() { QSKIP("Requires HAS_LIBSLIC3R"); } #endif`

**acceptance_criteria:**
- tests/PartPlateTests.cpp contains `void arrangeDistributesAcrossPlates();` declaration and definition
- The test loads hotend.stl via kStlPath (QT_TESTCASE_SOURCEDIR pattern)
- The test calls `service.arrangeObjects(50.0f, false, false, QStringLiteral("0,0,100,0,100,100,0,100"))` (small bed + large spacing)
- The test asserts `service.plateCount() >= 2` AND at least one object on plate >= 1
- The #else branch QSKIPs without HAS_LIBSLIC3R
- `ctest -R PartPlateTests -R arrangeDistributes --output-on-failure` exits 0

### 29-05-T2 — Add ARRANGE-03 locked-exclusion + D-29-12 all-locked edge case tests

**read_first:** `tests/PartPlateTests.cpp` (after T1); `src/core/services/ProjectServiceMock.h:100-101` (isPlateLocked/setPlateLocked); `src/core/model/PartPlate.h:111` (isLocked), `:138` (objToInstanceSet); 29-RESEARCH.md section 7.3 (ARRANGE-03 test design), 3.3 (all-locked edge case); 29-CONTEXT.md D-29-11, D-29-12.

**action:** Add two more private slots to PartPlateTests:
1. `void lockedPlateExclusion();` — under #ifdef HAS_LIBSLIC3R:
   - Construct ProjectServiceMock, load hotend.stl, ensure ≥2 objects exist (duplicate if needed).
   - Manually place object 0 instance 0 on plate 0 (it should be there by default after load), then `QVERIFY(service.setPlateLocked(0, true));`
   - Capture plate 0's membership before: use the confirmed bridge `service.plateObjectIndices(0)` (ProjectServiceMock.h:114) as a snapshot.
   - Add 2-3 MORE objects (so there are movable objects to arrange).
   - Call `service.arrangeObjects(20.0f, false, false, QStringLiteral("0,0,100,0,100,100,0,100"));`
   - Assert plate 0 membership UNCHANGED: `QCOMPARE(service.plateObjectIndices(0), before);` Assert new objects off plate 0: at least one object index in the post-arrange state is on plate >= 1 that wasn't before.
2. `void allLockedReturnsFalse();` — under #ifdef HAS_LIBSLIC3R (D-29-12):
   - Construct ProjectServiceMock, load hotend.stl (1+ objects).
   - Lock plate 0: `service.setPlateLocked(0, true);`
   - Capture plate 0 membership before.
   - Call `const bool ok = service.arrangeObjects(20.0f, false, false, QStringLiteral("0,0,100,0,100,100,0,100"));`
   - Assert: `QVERIFY2(!ok, "all-locked arrange must return false (D-29-12)");` AND `QCOMPARE(service.plateObjectIndices(0), beforeMembers);` (membership unchanged — no crash, no movement).
3. Add #else QSKIP stubs for both.

**acceptance_criteria:**
- tests/PartPlateTests.cpp contains `void lockedPlateExclusion();` and `void allLockedReturnsFalse();`
- lockedPlateExclusion asserts plate 0's membership is unchanged after arrange AND at least one new object is on plate >= 1
- allLockedReturnsFalse asserts `!ok` (arrangeObjects returns false) AND plate 0 membership unchanged (D-29-12)
- Both have #else QSKIP stubs for non-HAS_LIBSLIC3R
- `ctest -R PartPlateTests --output-on-failure` exits 0 with all slots green (both under HAS_LIBSLIC3R build and the QSKIP path otherwise)

## verification

- `ctest --test-dir build -R PartPlateTests --output-on-failure` — all PartPlateTests slots green (computeColumCount 36 rows, computeOriginGridMath, computePlateIndexRoundTrip, updatePlateOriginsWritesToPlates from Plan 02; arrangeDistributesAcrossPlates, lockedPlateExclusion, allLockedReturnsFalse from Plan 05).
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` — full canonical suite green (all 6 test targets).
- `rg -n "void arrangeDistributesAcrossPlates|void lockedPlateExclusion|void allLockedReturnsFalse" tests/PartPlateTests.cpp` — all three present.

## threat_model

| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| Test depends on Phase 32 FIXTURE-01 (not yet committed) | Denial of Service | Reuses hotend.stl (already referenced by ViewModelSmokeTests.cpp:41); builds model in-memory | mitigate |
| Locked plate membership mutated by arrange | Tampering | lockedPlateExclusion captures before/after and QCOMPAREs | mitigate |
| All-locked crashes instead of returning false | Denial of Service | allLockedReturnsFalse asserts !ok + no membership change + no crash | mitigate |
