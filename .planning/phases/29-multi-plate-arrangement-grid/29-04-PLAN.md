---
phase: 29
plan: 04
type: execute
wave: 3
depends_on: [29-01, 29-03]
files_modified:
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
autonomous: true
requirements: [ARRANGE-02, ARRANGE-03]
must_haves:
  truths:
    - "ProjectServiceMock::arrangeObjects parses printableArea into the same Slic3r::BoundingBox bed_bb it already builds (ProjectServiceMock.cpp:2455), derives m_plate_width/depth from bed_bb.size() (μm→mm via /1000.0), calls setPlateSize on m_plateList (D-29-3 minimal-drift source), then arranges, then calls rebuildPlatesAfterArrangement(exceptLocked=true, recyclePlates=true) — but ONLY when arrange returns true (D-29-12: all-locked returns false, no rebuild)."
    - "Locked plates are excluded: rebuildPlatesAfterArrangement(exceptLocked=true) skips clearing locked membership so locked plates keep their instances untouched. Full upstream preprocess_arrange_polygon (PartPlate.cpp:5388-5396) deferred — minimal-determinism path (D-29-10/D-29-9)."
    - "arrangeObjects is the public Q_INVOKABLE entry (signature UNCHANGED — float, bool, bool, QString); EditorViewModel proxy (EditorViewModel.cpp:3855-3876) needs NO change and benefits transparently."
    - "Q_PROPERTY plateCols, plateStrideX, plateStrideY are exposed read-only on ProjectServiceMock (D-29-15), NOTIFY plateDataLoaded (cols derive from count, so the existing signal is the right trigger)."
    - "DATA LAYER ONLY — multi-plate rendering does NOT follow (GLViewport does not read origin()); Phase 29 delivers correct plate membership + origins, not GL rendering."
  artifacts:
    - path: "src/core/services/ProjectServiceMock.h"
      provides: "plateCols/plateStrideX/plateStrideY Q_PROPERTY + READ accessors"
      min_lines: 1
    - path: "src/core/services/ProjectServiceMock.cpp"
      provides: "arrangeObjects rewired: bed_bb→setPlateSize, arrange, rebuildPlatesAfterArrangement guarded by bool return, locked preprocessing comment"
      min_lines: 1
  key_links:
    - from: "EditorViewModel::arrangeAllObjects"
      to: "ProjectServiceMock::arrangeObjects"
      via: "unchanged Q_INVOKABLE signature; new distribution flows through syncTransformsFromModel + emit stateChanged"
    - from: "arrangeObjects"
      to: "PartPlateList::rebuildPlatesAfterArrangement (Plan 03)"
      via: "post-arrange membership reconstruction"
---

# Plan 29-04: Rewire arrangeObjects + Q_PROPERTY + locked exclusion (ARRANGE-02/03)

**Objective:** Rewire `ProjectServiceMock::arrangeObjects` (ProjectServiceMock.cpp:2416-2479) to distribute models across plates: (1) parse bed_bb → setPlateSize (D-29-3 minimal-drift source — the SAME bed_bb arrange uses), (2) arrange, (3) rebuildPlatesAfterArrangement guarded by the arrange bool return (D-29-12). Add locked exclusion via rebuildPlatesAfterArrangement(exceptLocked=true). Add the plateCols/plateStrideX/plateStrideY Q_PROPERTY (D-29-15). This is the integration layer that turns Plan 01+03's domain logic into observable multi-plate distribution.

## Tasks

### 29-04-T1 — Add plateCols/plateStrideX/plateStrideY Q_PROPERTY + accessors

**read_first:** `src/core/services/ProjectServiceMock.h:46-56` (existing Q_PROPERTY block, plateCount NOTIFY plateDataLoaded at line 51); `:324-326` (signals section, plateDataLoaded at 326); `src/core/model/PartPlateList.h` (plateCols/plateStrideX/plateStrideY accessors from Plan 01).

**action:** In ProjectServiceMock.h, after the `plateCount` Q_PROPERTY (line 51), add three read-only Q_PROPERTYs reusing the existing `plateDataLoaded` NOTIFY (cols/stride derive from count, so the same signal fires correctly):
```
Q_PROPERTY(int plateCols READ plateCols NOTIFY plateDataLoaded)
Q_PROPERTY(double plateStrideX READ plateStrideX NOTIFY plateDataLoaded)
Q_PROPERTY(double plateStrideY READ plateStrideY NOTIFY plateDataLoaded)
```
Add public accessors that delegate to m_plateList (with a null guard):
- `int plateCols() const { return m_plateList ? m_plateList->plateCols() : 0; }`
- `double plateStrideX() const { return m_plateList ? m_plateList->plateStrideX() : 0.0; }`
- `double plateStrideY() const { return m_plateList ? m_plateList->plateStrideY() : 0.0; }`
Place the inlines in the header (matches the pattern of simple read accessors). Do NOT add a new signal — plateDataLoaded is sufficient (D-29-15 chose this over overloading with plateGeometryChanged).

**acceptance_criteria:**
- ProjectServiceMock.h contains `Q_PROPERTY(int plateCols READ plateCols NOTIFY plateDataLoaded)`
- ProjectServiceMock.h contains `Q_PROPERTY(double plateStrideX READ plateStrideX NOTIFY plateDataLoaded)`
- ProjectServiceMock.h contains `Q_PROPERTY(double plateStrideY READ plateStrideY NOTIFY plateDataLoaded)`
- All three delegate to `m_plateList->plateCols()`/`plateStrideX()`/`plateStrideY()` with a null guard
- No new signal added (reuses plateDataLoaded)

### 29-04-T2 — Rewire arrangeObjects to set plate size + rebuild after arrange

**read_first:** `src/core/services/ProjectServiceMock.cpp:2416-2479` (the current arrangeObjects — parse bed_bb at 2444-2455, arrange call at 2458/2464); `:2438` (tolerantVfn); `src/core/services/ProjectServiceMock.h:364` (m_plateList unique_ptr); `:72` (rawModel); `third_party/OrcaSlicer/src/slic3r/GUI/PartPlate.cpp:5378-5422` (preprocess_arrange_polygon — minimal locked subset is lines 5388-5396); 29-CONTEXT.md D-29-3, D-29-5, D-29-9, D-29-10, D-29-12; 29-RESEARCH.md section 2.4 (current arrangeObjects + rewire), 3.1 (minimal locked subset), 3.3 (all-locked edge case), 5 (bed_bb source-of-truth recommendation), 8 gotcha 5 (tolerantVfn partial-apply).

**action:** Modify `ProjectServiceMock::arrangeObjects` (ProjectServiceMock.cpp:2416-2479). Keep the existing ArrangeParams + tolerantVfn setup (lines 2425-2438) UNCHANGED. Modify the bed-parse branch (2440-2460):
1. After `Slic3r::BoundingBox bed_bb(bed_shape);` (line 2455), BEFORE the arrange call, derive plate size from the SAME bed_bb (D-29-3 / RESEARCH section 5 — guarantees zero drift between grid decode and arrange coordinate space):
   ```
   if (m_plateList) {
     const double width_mm = bed_bb.size().x() / 1000.0;   // μm → mm
     const double depth_mm = bed_bb.size().y() / 1000.0;
     m_plateList->setModel(model_);  // backref for rebuildPlatesAfterArrangement (Plan 03)
     m_plateList->setPlateSize(static_cast<int>(width_mm), static_cast<int>(depth_mm), 0);
   }
   ```
2. Add locked-exclusion comment BEFORE arrange (D-29-10/D-29-9 minimal-determinism path). Per RESEARCH §2.4: locked plates are preserved by rebuildPlatesAfterArrangement(exceptLocked=true), which skips clearing locked membership. Add a comment: `// Locked exclusion (D-29-10/D-29-9): upstream pins ArrangePolygon.bed_idx to locked plate index before arrange (PartPlate.cpp:5388-5396). Qt6's arrange_objects overload operates on the whole model without exposing the ArrangePolygon list, so the minimal-determinism path preserves locked membership via rebuildPlatesAfterArrangement(exceptLocked=true) instead. Full preprocess_arrange_polygon (fixed-item/non-selected branches) deferred per CONTEXT.`
3. Capture the arrange result and call rebuild ONLY on success (D-29-12 — all-locked returns false, no rebuild):
   Replace `return Slic3r::arrange_objects(*model_, bed_bb, params, tolerantVfn);` (line 2458) with:
   ```
   const bool arranged = Slic3r::arrange_objects(*model_, bed_bb, params, tolerantVfn);
   if (arranged && m_plateList) {
     m_plateList->rebuildPlatesAfterArrangement(/*exceptLocked=*/true, /*recyclePlates=*/true);
     emit plateDataLoaded(m_plateList->plateCount());
   }
   return arranged;
   ```
   Do the SAME for the InfiniteBed fallback path (line 2463-2464): capture the bool; if true and m_plateList, call rebuildPlatesAfterArrangement(true, true) + emit plateDataLoaded. (InfiniteBed arrange won't overflow to multiple plates typically, but the rebuild keeps membership consistent and is idempotent.)
4. NOTE on partial-apply (RESEARCH gotcha 5): tolerantVfn swallows out-of-bed items, so arrange may return false even though some instances got world translations. The guard `if (arranged && ...)` means we do NOT rebuild on false. This mirrors upstream intent (false = don't commit distribution). If a future test needs the partial-apply path, revisit — but D-29-12 explicitly wants all-locked (and similar) to return false with no rebuild.

**acceptance_criteria:**
- ProjectServiceMock.cpp arrangeObjects contains `m_plateList->setModel(model_)` and `m_plateList->setPlateSize(static_cast<int>(width_mm), static_cast<int>(depth_mm), 0)` with `width_mm = bed_bb.size().x() / 1000.0` (the SAME bed_bb passed to arrange)
- arrangeObjects contains `const bool arranged = Slic3r::arrange_objects(*model_, bed_bb, params, tolerantVfn);`
- arrangeObjects contains `if (arranged && m_plateList) { m_plateList->rebuildPlatesAfterArrangement(/*exceptLocked=*/true, /*recyclePlates=*/true); emit plateDataLoaded(m_plateList->plateCount()); }`
- arrangeObjects returns `arranged` (the captured bool), NOT the raw arrange call
- The rebuild is guarded behind the bool return (D-29-12: false → no rebuild)
- The locked-exclusion comment cites PartPlate.cpp:5388-5396 and explains the minimal-determinism path
- The Q_INVOKABLE signature `bool arrangeObjects(float, bool, bool, const QString&)` is UNCHANGED

## verification

- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` — build green. (arrangeObjects is exercised end-to-end by Plan 05.)
- `rg -n "plateCols|plateStrideX|plateStrideY" src/core/services/ProjectServiceMock.h` — three Q_PROPERTYs + three accessors.
- `rg -n "setPlateSize|rebuildPlatesAfterArrangement|setModel" src/core/services/ProjectServiceMock.cpp` — rewired calls present.
- `rg -n "Q_INVOKABLE bool arrangeObjects" src/core/services/ProjectServiceMock.h` — signature unchanged.

## threat_model

| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| m_plate_width/depth drift from arrange bed → mis-assigned plates | Tampering | Derived from the SAME bed_bb arrange uses (RESEARCH section 5); setPlateSize immediately before arrange | mitigate |
| All-locked edge case rebuilds/moves locked instances (D-29-12) | Tampering | Rebuild guarded by `if (arranged && ...)`; all-locked returns false → no rebuild | mitigate |
| arrangeObjects signature change breaks EditorViewModel proxy | Denial of Service | Signature unchanged; EditorViewModel.cpp:3855-3876 needs no edit | mitigate |
