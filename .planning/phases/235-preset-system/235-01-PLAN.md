# Phase 235 Plan — Preset System Completion

**Requirements:** PSET2-01..08
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` Preset system section (all file:line)

## Plan 1 (single batch)

### Task 1 — PSET2-01: user preset persistence
- `createCustomPreset/savePresetValues` (PresetServiceMock.cpp:1619-1688) are
  memory-only. Persist to the user preset directory in the upstream JSON
  shape (Config.cpp:1390-1433 save_to_json: version/name/from/is_custom
  header + key/values). Load user presets at startup into the same category
  lists; delete/rename touch disk. Base dir: QStandardPaths AppDataLocation
  `/user/presets/{printer,filament,process}` mirroring upstream datadir.

### Task 2 — PSET2-02: CreatePresetsDialog scope + inheritance
- Fix inverted scope→category mapping (CreatePresetsDialog.qml:38-39/84-88 vs
  PresetServiceMock.h:18 Print=0/Filament=1/Printer=2) and honor the inherits
  selection: pass parent preset name into createCustomPreset and resolve
  inheritance at creation (existing resolveInheritance machinery,
  PresetServiceMock.cpp:682-789).

### Task 3 — PSET2-03: single dirty-guard modal with Transfer
- Triple-modal: 3 SettingsDialog instances each connect
  `pendingUnsavedChangesRequested` (SettingsDialog.qml:288-304 ×
  main.qml:584-601). Gate with a single active-dialog flag in ConfigViewModel
  (or route via BackendContext).
- Add Transfer button + per-item checkboxes to UnsavedChangesDialog
  (upstream UnsavedChangesDialog.cpp:1008-1011, 1859-2260): Transfer applies
  selected keys onto the target preset without saving the source.

### Task 4 — PSET2-04: bundle IO upstream-compatible
- Fix importBundleIni category swap (PresetServiceMock.cpp:1525-1528).
- Replace the custom owzx-preset-bundle envelope (exportBundle :1096-1145)
  and the fabricated `[preset]` ini format (:77-88) with upstream-compatible
  user-preset JSON files (one per preset, matching Task 1's shape). Export =
  zip of selected preset JSONs; import reads both zip and loose JSON/ini with
  the corrected category mapping.

### Task 5 — PSET2-05: combo sections + in-list graying
- PresetComboBoxes upstream (:1281-1317): section headers Project/User/
  System, incompatible items grayed with LABEL_ITEM_DISABLED. Requires
  compatibility list computed per current printer (existing
  update_compatible-ish logic :1304-1343) exposed per preset row in
  PresetListModel (role), rendered in CxComboBox delegatates or a custom
  popup in LeftSidebar + SettingsDialog combos.

### Task 6 — PSET2-06: per-extruder slot model
- Builds on Phase 232's per-slot selection: add `filament_presets` vector
  semantics — slot list persists (project + presets), extruder-count changes
  resize it, slot preset affects slicing (filament settings merged per slot
  into the slice config like upstream PresetBundle::apply).

### Task 7 — PSET2-07: rename/delete UI
- Wire existing `renamePreset/deletePreset/canDeletePreset`
  (PresetServiceMock.cpp:1631-1682; currently zero QML callers) into the
  preset combo row edit affordance (LeftSidebar) with in-use guard messaging.
  Fix ConfigViewModel::deletePreset dead branch (:685-711).

### Task 8 — PSET2-08: no empty tabs
- Printer "Material"/"Extruder" and Filament "Overrides"/"Multimaterial"/
  "Dependencies" tabs render zero rows (SettingsDialog.qml:99-115 vs
  ConfigOptionModel.cpp:863-992). Either map their keys (source-truth page
  assignment from Tab.cpp) or remove the tabs. Prefer mapping for tabs with
  real upstream keys (Dependencies = compatible_printers condition; Extruder
  pages need per-extruder model — defer those to the multi-nozzle scope and
  remove the tab).

## Verification

- Canonical build + all suites; new tests: preset persists across service
  restart (temp dir), CreatePreset honors scope+inherits, one modal only,
  bundle import of an upstream-exported bundle lands correct categories.
