---
phase: 25
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/qml_gui/Renderer/RhiViewport.h
  - src/qml_gui/Renderer/RhiViewport.cpp
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/Renderer/PrepareSceneData.h
  - src/qml_gui/Renderer/PrepareSceneData.cpp
  - src/qml_gui/Renderer/shaders/rhi_viewport.vert
  - src/qml_gui/Renderer/shaders/rhi_viewport.frag
  - tests/QmlUiAuditTests.cpp
  - tests/PrepareSceneDataTests.cpp
autonomous: true
requirements: [PREP-02, PREP-03, PREP-07]
must_haves:
  truths:
    - "QRhi model vertex buffers are uploaded only when mesh, plate, visibility, or GPU resource dirty state changes."
    - "Camera orbit, pan, zoom, fit, and preset changes update camera state and uniforms, not baked model vertices."
    - "Model colors are deterministic and selection/hover accent usage follows the Phase 25 UI-SPEC."
    - "The QRhi implementation maps user-visible behavior to upstream Prepare camera and GLCanvas3D concepts while staying Qt-native internally."
  artifacts:
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      provides: "QRhi model buffer ownership, draw calls, camera uniform uploads, and source-truth comments"
      min_lines: 320
    - path: "src/qml_gui/Renderer/RhiViewport.cpp"
      provides: "CameraController-driven input state and renderer synchronization"
      min_lines: 180
    - path: "src/qml_gui/Renderer/shaders/rhi_viewport.vert"
      provides: "3D position and MVP uniform shader path for bed and model rendering"
      min_lines: 1
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Static audits for model buffers, camera uniform path, and no per-frame mesh upload"
      min_lines: 1
  key_links:
    - from: "PrepareSceneData model batches"
      to: "RhiViewportRenderer model buffers"
      via: "dirty-gated upload and persistent QRhi buffer ownership"
    - from: "CameraController"
      to: "RhiViewportRenderer shader uniforms"
      via: "view/projection matrix copy in synchronize/render"
---

# Plan 25-02: QRhi Model Mesh Rendering And Camera Uniforms

<objective>
Render active-plate model meshes through QRhi using persistent model buffers and `CameraController` camera state. This plan converts the Phase 25 model batch contract into visible QRhi mesh drawing while proving camera interaction updates uniforms without full mesh buffer upload.
</objective>

<context>
Read first:
- `.planning/phases/25-prepare-model-mesh-rendering-and-camera-interaction/25-CONTEXT.md`
- `.planning/phases/25-prepare-model-mesh-rendering-and-camera-interaction/25-RESEARCH.md`
- `.planning/phases/25-prepare-model-mesh-rendering-and-camera-interaction/25-UI-SPEC.md`
- `.planning/phases/25-prepare-model-mesh-rendering-and-camera-interaction/25-01-PLAN.md`
- `src/qml_gui/Renderer/RhiViewportRenderer.h`
- `src/qml_gui/Renderer/RhiViewportRenderer.cpp`
- `src/qml_gui/Renderer/RhiViewport.h`
- `src/qml_gui/Renderer/RhiViewport.cpp`
- `src/qml_gui/Renderer/CameraController.h`
- `src/qml_gui/Renderer/CameraController.cpp`
- `src/qml_gui/Renderer/shaders/rhi_viewport.vert`
- `src/qml_gui/Renderer/shaders/rhi_viewport.frag`
- `tests/QmlUiAuditTests.cpp`
</context>

<tasks>
  <task id="25-02-T1" type="auto" tdd="true">
    <name>Add QRhi model buffer and camera audits first</name>
    <files>
tests/QmlUiAuditTests.cpp
tests/PrepareSceneDataTests.cpp
    </files>
    <behavior>
Audits must guard that QRhi has a model buffer path, a camera uniform path, `CameraController` integration, and separate mesh/camera dirty handling. Tests must continue proving model scene bounds are available for fit view.
    </behavior>
    <action>
Extend `QmlUiAuditTests` with checks for `m_modelVertexBuffer`, model upload helpers, `m_cameraUniformBuffer`, `CameraController`, MVP/uniform updates, and absence of camera-triggered full mesh upload patterns. Extend `PrepareSceneDataTests` if aggregate model bounds need additional assertions for fit view.
    </action>
    <verify>
      <automated>build/QmlUiAuditTests.exe</automated>
      <automated>build/PrepareSceneDataTests.exe</automated>
    </verify>
    <done>
Static and unit tests describe the required QRhi model/camera architecture before renderer code is complete.
    </done>
  </task>

  <task id="25-02-T2" type="auto" tdd="true">
    <name>Render model batches through persistent QRhi buffers</name>
    <files>
src/qml_gui/Renderer/RhiViewportRenderer.h
src/qml_gui/Renderer/RhiViewportRenderer.cpp
src/qml_gui/Renderer/PrepareSceneData.h
src/qml_gui/Renderer/PrepareSceneData.cpp
src/qml_gui/Renderer/shaders/rhi_viewport.vert
src/qml_gui/Renderer/shaders/rhi_viewport.frag
tests/QmlUiAuditTests.cpp
    </files>
    <behavior>
When model batches exist, QRhi draws active-plate model geometry with deterministic object colors. Model QRhi buffers are created or reuploaded only when mesh, plate, visibility, or GPU resource dirty state requires it. Bed/grid rendering from Phase 24 remains visible behind model geometry.
    </behavior>
    <action>
Extend the renderer vertex format to support 3D positions and colors for bed and model geometry. Add persistent QRhi buffers for model vertices and any required overlay metadata. Create or resize buffers only when the required byte size changes; otherwise upload model data only on mesh-relevant dirty flags. Draw each active model batch with deterministic muted colors aligned with `25-UI-SPEC.md`. Keep source-truth comments near the model draw path referencing `GLCanvas3D`, `PartPlate`, and `Selection`.
    </action>
    <verify>
      <automated>build/QmlUiAuditTests.exe</automated>
    </verify>
    <done>
The QRhi path renders active-plate model vertices through persistent buffers while preserving Phase 24 bed/plate rendering.
    </done>
  </task>

  <task id="25-02-T3" type="auto" tdd="true">
    <name>Wire CameraController interaction and uniform-only updates</name>
    <files>
src/qml_gui/Renderer/RhiViewport.h
src/qml_gui/Renderer/RhiViewport.cpp
src/qml_gui/Renderer/RhiViewportRenderer.h
src/qml_gui/Renderer/RhiViewportRenderer.cpp
src/qml_gui/Renderer/shaders/rhi_viewport.vert
tests/QmlUiAuditTests.cpp
    </files>
    <behavior>
Left drag on empty QRhi viewport orbits, middle drag pans, wheel zooms, fit view frames model bounds when present and bed bounds otherwise, and right-click context routing stays compatible with `PreparePage.qml`. Camera changes update MVP/uniform resources and call `update()`, but do not mark full model geometry dirty.
    </behavior>
    <action>
Add `CameraController` state to `RhiViewport` and copy render-ready camera data to `RhiViewportRenderer` during synchronization. Override mouse press/move/release/wheel handling for left orbit, middle pan, and wheel zoom. Implement `requestFitView(...)` so it fits model bounds from `PrepareSceneData` when available and preserves the existing toolbar call signature. Add a uniform buffer and shader resource binding for the MVP matrix. Ensure camera dirty state is separate from mesh dirty state and update audits accordingly.
    </action>
    <verify>
      <automated>build/QmlUiAuditTests.exe</automated>
    </verify>
    <done>
QRhi camera interaction uses the shared camera controller and updates uniforms without model vertex rebuild or full model buffer upload.
    </done>
  </task>
</tasks>

<verification>
- `build/QmlUiAuditTests.exe`
- `build/PrepareSceneDataTests.exe`
- `rg -n "CameraController|m_cameraUniformBuffer|m_modelVertexBuffer|uploadModel|DirtyCamera|requestFitView" src/qml_gui/Renderer tests`
- Canonical wave gate: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
</verification>

<threat_model>
| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| Malformed scene data reaches QRhi buffer allocation | Denial of Service | Consume only validated `PrepareSceneData` vertices and checked byte sizes | mitigate |
| Camera movement reuploads full mesh buffers | Denial of Service | Separate camera dirty/uniform updates from model dirty uploads and audit call sites | mitigate |
| QRhi model path hides fallback viewport failures | Reliability | Keep QRhi gated and retain Software/OpenGL property compatibility | mitigate |
</threat_model>

<success_criteria>
- Active-plate model geometry is drawn through QRhi buffers.
- Camera orbit, pan, zoom, and fit are handled in C++ with `CameraController`.
- Camera updates use uniforms and do not rebuild or reupload full model geometry.
- Renderer code includes source-truth mapping notes for user-visible Prepare behavior.
</success_criteria>
