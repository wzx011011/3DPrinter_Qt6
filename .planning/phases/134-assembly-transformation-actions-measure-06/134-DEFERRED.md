# Phase 134: Assembly Transformation Actions MEASURE-06 — DEFERRED

**Status:** Deferred to next milestone. MEASURE-06 (assembly-mode move/rotate/scale
per-volume) requires rendering-layer gizmo changes (RhiViewport assembly canvas
move/rotate/scale enablement) + ViewModel transform methods + AssemblePage QML
interaction — a substantial cross-layer feature. The v4.5/v4.6 feature-picking
foundation (AssemblyMeasureGeometry + SceneRaycaster) is shipped; the
transformation actions build on it but need focused rendering work.

**Carry-forward:** ASM-01 moves to the next milestone's requirements.
