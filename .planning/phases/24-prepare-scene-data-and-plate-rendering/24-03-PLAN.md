---
phase: 24
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/Renderer/PrepareSceneData.h
  - src/qml_gui/Renderer/PrepareSceneData.cpp
  - tests/PrepareSceneDataTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [RHI-04, PREP-01, PREP-05]
must_haves:
  truths:
    - "The QRhi Prepare path renders bed fill, border, grid, and origin cues from PrepareSceneData."
    - "Diagnostic triangle drawing is replaced for Prepare canvas when QRhi scene data is available."
    - "QRhi vertex buffers and pipelines are owned by RhiViewportRenderer, not QML."
    - "Buffer uploads are gated by scene dirty flags and viewport size changes; steady frames reuse GPU buffers."
  artifacts:
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.h"
      provides: "QRhi scene resource members and upload state"
      min_lines: 1
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      provides: "Bed/grid/origin QRhi rendering with dirty-gated uploads"
      min_lines: 180
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Static regression guards for QRhi scene rendering and upload gating"
      min_lines: 1
    - path: "tests/PrepareSceneDataTests.cpp"
      provides: "Scene dirty behavior coverage used by renderer upload logic"
      min_lines: 1
  key_links:
    - from: "RhiViewportRenderer::synchronize"
      to: "PrepareSceneData"
      via: "bed/plate property updates from RhiViewport"
    - from: "RhiViewportRenderer::render"
      to: "QRhiBuffer"
      via: "dirty-gated static buffer upload"
    - from: "QmlUiAuditTests"
      to: "RhiViewportRenderer"
      via: "source assertions for line/fill pipelines and dirty upload guard"
---

# Plan 24-03: QRhi Bed Grid And Plate Rendering

<objective>
Render the active Prepare bed/plate context through QRhi using the scene contract from Plans 01-02. This plan replaces the diagnostic Prepare draw path with bed fill, border, grid, and origin cues while proving GPU uploads are gated by explicit scene dirtiness.
</objective>

<context>
Read first:
- `.planning/phases/24-prepare-scene-data-and-plate-rendering/24-CONTEXT.md`
- `.planning/phases/24-prepare-scene-data-and-plate-rendering/24-RESEARCH.md`
- `.planning/phases/24-prepare-scene-data-and-plate-rendering/24-UI-SPEC.md`
- `.planning/phases/24-prepare-scene-data-and-plate-rendering/24-01-PLAN.md`
- `.planning/phases/24-prepare-scene-data-and-plate-rendering/24-02-PLAN.md`
- `src/qml_gui/Renderer/RhiViewportRenderer.h`
- `src/qml_gui/Renderer/RhiViewportRenderer.cpp`
- `src/qml_gui/Renderer/shaders/rhi_viewport.vert`
- `src/qml_gui/Renderer/shaders/rhi_viewport.frag`
- `tools/render_bench/main.cpp`
- `tests/QmlUiAuditTests.cpp`
</context>

<tasks>
  <task id="24-03-T1" type="auto" tdd="true">
    <name>Add static guards for QRhi scene resources and upload gating</name>
    <files>
tests/QmlUiAuditTests.cpp
src/qml_gui/Renderer/RhiViewportRenderer.cpp
src/qml_gui/Renderer/RhiViewportRenderer.h
    </files>
    <behavior>
Audits must fail if the renderer reverts to only a diagnostic primitive, if bed/grid rendering is not backed by `PrepareSceneData`, or if upload calls are not guarded by dirty state.
    </behavior>
    <action>
Add QML/source audit coverage such as `rhiViewportRendererUsesPrepareSceneDataAndDirtyUploads()`. Assert the renderer includes `PrepareSceneData`, has both triangle and line rendering paths or equivalent separate draw modes, creates QRhi vertex buffers for scene fill and line data, references dirty flags before uploading, and keeps QRhi resource ownership inside C++.
    </action>
    <verify>
      <automated>build/QmlUiAuditTests.exe rhiViewportRendererUsesPrepareSceneDataAndDirtyUploads</automated>
    </verify>
    <done>
The audit fails against the current diagnostic-only renderer and will pass only after QRhi scene rendering and dirty-gated uploads are implemented.
    </done>
  </task>

  <task id="24-03-T2" type="auto" tdd="true">
    <name>Implement QRhi bed fill and grid resources</name>
    <files>
src/qml_gui/Renderer/RhiViewportRenderer.h
src/qml_gui/Renderer/RhiViewportRenderer.cpp
src/qml_gui/Renderer/PrepareSceneData.h
src/qml_gui/Renderer/PrepareSceneData.cpp
tests/QmlUiAuditTests.cpp
    </files>
    <behavior>
For Prepare canvas, the renderer draws a bed fill and line overlay for border, grid, and origin cues. When scene data is unchanged, render uses existing QRhi buffers without re-uploading vertex data. When bed metrics or plate context changes, the renderer uploads only the affected scene buffers.
    </behavior>
    <action>
Extend `RhiViewportRenderer` with QRhi resources for scene fill and line geometry. Use the existing vertex format and shader resources when possible. Create a triangle pipeline for bed fill and a line pipeline for border/grid/origin lines, or an equivalent representation that preserves separate draw intent. In `synchronize()`, push bed and plate state into `PrepareSceneData`. In `render()`, call `takeDirtyFlags()` only when preparing upload batches, resize buffers when geometry byte size changes, and submit static buffer uploads through `QRhiResourceUpdateBatch`. Keep Preview canvas behavior unchanged except for shared clear/resource setup.
    </action>
    <verify>
      <automated>build/QmlUiAuditTests.exe rhiViewportRendererUsesPrepareSceneDataAndDirtyUploads</automated>
    </verify>
    <done>
QRhi renders bed fill and grid/origin lines for the Prepare canvas, and upload work is tied to dirty flags or resource size changes.
    </done>
  </task>

  <task id="24-03-T3" type="auto" tdd="true">
    <name>Prove plate switches mark renderer upload work</name>
    <files>
src/qml_gui/Renderer/PrepareSceneData.h
src/qml_gui/Renderer/PrepareSceneData.cpp
src/qml_gui/Renderer/RhiViewportRenderer.cpp
tests/PrepareSceneDataTests.cpp
tests/QmlUiAuditTests.cpp
    </files>
    <behavior>
Changing `currentPlateIndex` or active object indices marks plate and GPU dirty. Reapplying identical plate context does not force another upload. The renderer's upload guard consumes dirty state after scheduling the QRhi update.
    </behavior>
    <action>
Add or extend `PrepareSceneDataTests` for repeated plate context updates and dirty flag transitions. If helpful, add a small renderer-side upload generation counter only in C++ private state or log-free test-visible source assertions, not as UI state. Update the audit to check that `RhiViewportRenderer` reacts to plate dirty state rather than uploading every frame.
    </action>
    <verify>
      <automated>build/PrepareSceneDataTests.exe</automated>
    </verify>
    <done>
Plate switches produce exactly the dirty state needed for QRhi upload, and unchanged plate state remains steady-frame cheap.
    </done>
  </task>
</tasks>

<verification>
- `build/PrepareSceneDataTests.exe`
- `build/QmlUiAuditTests.exe rhiViewportRendererUsesPrepareSceneDataAndDirtyUploads`
- `rg -n "PrepareSceneData|DirtyPlate|DirtyGpu|QRhiBuffer|QRhiGraphicsPipeline::Lines|QRhiGraphicsPipeline::Triangles|uploadStaticBuffer" src/qml_gui/Renderer tests`
- Canonical full command at wave gate: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
</verification>

<threat_model>
| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| Renderer uploads full geometry every frame | Denial of Service | Dirty flag guard and static audit require upload calls behind state checks | mitigate |
| QRhi resource lifetime crosses into QML or viewmodel | Tampering | Resource members remain private to `RhiViewportRenderer` | mitigate |
| Prepare canvas becomes blank under QRhi | Denial of Service | Bed/grid draw path replaces diagnostic-only rendering and canonical build validates resources | mitigate |
</threat_model>

<success_criteria>
- PREP-01 covered visually in QRhi scope: bed fill, border, grid, and origin cues are rendered.
- PREP-05 covered in QRhi scope: plate context changes dirty renderer state without inactive object leakage.
- RHI-04 covered for Prepare scene/cache: steady frames reuse buffers and dirty changes schedule uploads.
- Preview G-code segment rendering remains assigned to later Preview phases and is not claimed here.
</success_criteria>
