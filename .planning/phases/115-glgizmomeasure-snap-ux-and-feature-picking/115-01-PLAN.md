---
phase: 115-glgizmomeasure-snap-ux-and-feature-picking
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 113-scene-and-mesh-raycaster-port
    provides: SceneRaycaster (two-stage pick, hit result).
  - phase: 114-measure-engine-instantiation-and-feature-readouts
    provides: MeasureEngine (per-volume Measuring, get_feature, scrubbed QtFeature).
files_modified:
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewport.cpp
  - src/qml_gui/Renderer/RhiViewport.h
  - src/core/viewmodels/EditorViewModel.cpp
  - src/core/viewmodels/EditorViewModel.h
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [MEASURE-04]
---

# Phase 115 Plan: GLGizmoMeasure Snap UX And Feature Picking

## Objective
Close MEASURE-04: wire the GLGizmoMeasure snap UX (mouse-move → raycast → feature pick; Shift toggle; two-point measurement) through Phase 113 raycaster + Phase 114 MeasureEngine. GizmoMeasure=3 enum slot exists.

## Must Haves
<must_haves>
  <truth id="MS-01">When the measure gizmo is active (GizmoMeasure=3) and the mouse moves over the Prepare/Assembly view, the two-stage pick (Phase 113 SceneRaycaster) runs → hit result (facetIdx + point + normal) → MeasureEngine::getFeature → SurfaceFeature type (Point/Edge/Circle/Plane) + snapped position. The pick uses the cached per-volume raycaster (pitfall 7 — no all-volumes loop).</truth>
  <truth id="MS-02">Shift toggle: default = FeatureSelection (snap to nearest feature — edge midpoint, plane centroid, etc.); Shift held = PointSelection (exact mouse point, no feature snap). Read from keyboard modifiers (Qt::ShiftModifier). Mirrors upstream GLGizmoMeasure.cpp:409-442.</truth>
  <truth id="MS-03">Visual feedback: the picked feature is highlighted in the gizmo overlay — a point marker for Point, a line for Edge, a circle for Circle, a plane for Plane. The Phase 114 EditorViewModel measurement readouts update live on mouse-move.</truth>
  <truth id="MS-04">Two-point measurement: clicking sets point A (the current snapped feature); moving + clicking sets point B; the readout shows the A→B measurement (angle/distance/etc.). A second click or Escape clears/resets. Mirrors upstream GLGizmoMeasure two-click flow.</truth>
  <truth id="MS-05">Performance: mouse-move pick is responsive (the two-stage pick + cached Measuring keep it O(hit volume) not O(all volumes)). If the pick is too slow on large meshes, document a throttle (e.g. pick every Nth mouse-move) — but prefer the cache makes it fast enough.</truth>
  <truth id="MS-06">A QmlUiAuditTests source-audit slot `glGizmoMeasureSnapUxWired` asserts: (a) the GizmoMeasure=3 path runs the raycast + getFeature; (b) the Shift toggle is read; (c) the 4 SurfaceFeatureTypes are handled. MEASURE-04-named QVERIFY2 messages. (UI-interaction testing is hard to unit-test deterministically; the source-audit + the Phase 114 readout test are the verification bar — runtime visual evidence remains blocked per STATE.md.)</truth>
  <truth id="MS-07">Canonical build + regression ctest passes; OWzxSlicer.exe links clean.</truth>
  <truth id="MS-08">`git diff --check` exit 0; encoding guard clean.</truth>
</must_haves>

<threat_model>
UI interaction layer. Reads mouse/keyboard input (trusted local input). The raycast + Measuring consume trusted libslic3r state (Phase 112-114 boundary already scrubbed). No network, no external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="115-01-01" type="implementation">
    <title>Wire mouse-move → raycast → getFeature → Shift toggle → visual feedback</title>
    <read_first>
      - .planning/phases/115-glgizmomeasure-snap-ux-and-feature-picking/115-CONTEXT.md
      - src/qml_gui/Renderer/RhiViewport.h:97 (GizmoMeasure=3)
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp (mouse handling + gizmo overlay rendering)
      - src/core/rendering/SceneRaycaster.h (Phase 113 pick)
      - src/core/rendering/MeasureEngine.h (Phase 114 getFeature)
      - src/core/viewmodels/EditorViewModel.h (Phase 114 readout Q_PROPERTYs)
      - third_party/OrcaSlicer/src/slic3r/GUI/GLGizmoMeasure.cpp:409-442 (upstream Shift toggle)
      - third_party/OrcaSlicer/src/libslic3r/Measure.hpp:16-22 (SurfaceFeatureType)
    </read_first>
    <files>
      - src/qml_gui/Renderer/RhiViewportRenderer.cpp
      - src/qml_gui/Renderer/RhiViewportRenderer.h
      - src/qml_gui/Renderer/RhiViewport.cpp
      - src/qml_gui/Renderer/RhiViewport.h
      - src/core/viewmodels/EditorViewModel.cpp
      - src/core/viewmodels/EditorViewModel.h
    </files>
    <action>
      Wire mouse-move (when GizmoMeasure active) → SceneRaycaster::hitTest → MeasureEngine::getFeature → update EditorViewModel readouts. Shift toggle (FeatureSelection default; PointSelection on Shift). Visual feedback in the gizmo overlay (point/edge/circle/plane highlight). Two-click measurement (A then B). MS-01/02/03/04/05.
    </action>
    <verify>
      `rg -n "GizmoMeasure\|hitTest\|getFeature\|ShiftModifier\|SurfaceFeatureType" src/qml_gui/Renderer/ src/core/viewmodels/`
      Build clean.
    </verify>
    <acceptance_criteria">
      - Mouse-move pick wired (MS-01). Shift toggle (MS-02). Visual feedback (MS-03). Two-click measurement (MS-04).
    </acceptance_criteria>
  </task>

  <task id="115-01-02" type="test">
    <title>Add glGizmoMeasureSnapUxWired source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp, src/qml_gui/Renderer/RhiViewportRenderer.cpp</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add `glGizmoMeasureSnapUxWired` source-audit asserting: GizmoMeasure path runs raycast+getFeature; Shift toggle read; 4 SurfaceFeatureTypes handled. MS-06. (Deterministic source-audit; runtime interaction not unit-tested per STATE.md evidence bar.)
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes (MS-06).</acceptance_criteria>
  </task>

  <task id="115-01-03" type="verification">
    <title>Build + regression ctest + launch liveness + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_full>
    <files></files>
    <action>Canonical build + regression ctest + 5-second launch liveness. Document the snap UX wiring + the runtime-visual-evidence-blocked caveat (the gate MS-06 source-audit + Phase 114 readout test are the verification bar) in SUMMARY.</action>
    <verify>SUMMARY records build + ctest + liveness.</verify>
    <acceptance_criteria>Production link clean, ctest pass, launch liveness (MS-07).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest.
2. Source audit: `rg -n "GizmoMeasure.*hitTest\|ShiftModifier\|SurfaceFeatureType::Point\|::Edge\|::Circle\|::Plane" src/`
3. Encoding guard.

## Success Criteria
- MEASURE-04 closed; every MS truth; snap UX wired; Shift toggle; 4 feature types.

## Expected Non-Changes
- No MEASURE-05 regression lock (Phase 116).
- No MEASURE-06 Assembly actions (LOCKED future).
- No libslic3r changes.
- Runtime VISUAL evidence remains blocked per STATE.md — the source-audit + Phase 114 readout test are the bar.
