---
phase: 119-tick-type-coverage-and-drag-relocation
plan: 01
type: implementation
wave: 1
depends_on: [118]
files_modified:
  - src/core/viewmodels/PreviewViewModel.h
  - src/core/viewmodels/PreviewViewModel.cpp
  - src/qml_gui/components/PreviewLayerRail.qml
  - src/qml_gui/components/CustomGcodeDialog.qml
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [TICK-04, TICK-05]
---

# Phase 119 Plan: Tick Type Coverage And Drag Relocation

## Objective
Close TICK-04 (all 5 upstream tick types round-trip) + TICK-05 (drag-to-relocate). Phase 118 wired write-back + re-slice; Phase 119 adds the missing ColorChange/Template add paths + moveTick drag.

## Must Haves
<must_haves>
  <truth id="TC-01">PreviewViewModel gains `Q_INVOKABLE void addColorChangeAtLayer(int layer, int extruder, const QString& color)` (type=ColorChange, stores extruder + color). And `Q_INVOKABLE void addTemplateAtLayer(int layer)` (type=Template). Both: dedup guard + sort + emit tickMarksChanged + writeTicksToModel (Phase 118 re-slice path).</truth>
  <truth id="TC-02">PreviewViewModel gains `Q_INVOKABLE void moveTick(int fromLayer, int toLayer)`: find tick at fromLayer, guard if toLayer already occupied (dedup), change tick to toLayer, re-sort, emit tickMarksChanged + writeTicksToModel. Returns nothing (QML checks via tickAtLayer) or a bool success.</truth>
  <truth id="TC-03">PreviewLayerRail.qml tick delegate gains a drag handler: dragging a tick recomputes the target layer from the new vertical position and calls `previewVm.moveTick(tickLayer, targetLayer)` on release. The drag is vertical-only (rail orientation). The tick follows the cursor visually during drag (y binding).</truth>
  <truth id="TC-04">The PreviewLayerRail Add menu gains "Add Color Change" (opens a color/extruder picker — reuse CustomGcodeDialog extended, or a minimal inline picker) and "Add Template" entries, so all 5 types are reachable from the UI. CustomGcodeDialog may be extended with an extruder+color row for ColorChange/ToolChange add (the existing dialog is CustomGcode-text-only).</truth>
  <truth id="TC-05">A `tickTypeCoverageAndDragRelocation` source-audit slot asserts: (a) addColorChangeAtLayer + addTemplateAtLayer + moveTick are declared/defined in PreviewViewModel; (b) PreviewLayerRail.qml references moveTick (drag wired); (c) the Add menu has color-change + template entries (grep for the menu items).</truth>
  <truth id="TC-06">Canonical build (j6) + regression ctest passes; encoding guard clean.</truth>
</must_haves>

<threat_model>
No new attack surface. Tick add/drag mutates user's own project state; write-back goes through the hardened Phase 118 path (libslic3r plates_custom_gcodes + startSlice). User-authored color/extruder values are validated by the QML input controls (IntValidator/ComboBox bounds). No network/auth. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="119-01-01" type="implementation">
    <title>Add addColorChangeAtLayer + addTemplateAtLayer + moveTick to PreviewViewModel</title>
    <read_first>
      - .planning/phases/119-tick-type-coverage-and-drag-relocation/119-CONTEXT.md
      - src/core/viewmodels/PreviewViewModel.h:222-232 (Q_INVOKABLE tick CRUD decls — add new ones here)
      - src/core/viewmodels/PreviewViewModel.cpp:1778-1945 (existing CRUD impl + Phase 118 writeTicksToModel pattern)
      - src/core/rendering/TickCodeTypes.h:7-24 (TickType enum)
    </read_first>
    <files>
      - src/core/viewmodels/PreviewViewModel.h
      - src/core/viewmodels/PreviewViewModel.cpp
    </files>
    <action>
      **Step 1 — addColorChangeAtLayer(int layer, int extruder, const QString& color).** Dedup guard (skip if layer occupied). Build TickCode{tick=layer, type=ColorChange, extruder, color}. Append + sort + emit tickMarksChanged + writeTicksToModel().
      **Step 2 — addTemplateAtLayer(int layer).** Dedup. TickCode{tick=layer, type=Template}. Append + sort + emit + writeTicksToModel().
      **Step 3 — moveTick(int fromLayer, int toLayer) -> bool.** Find tick at fromLayer (return false if not found). If a tick exists at toLayer, return false (dedup). Change its tick to toLayer, re-sort, emit tickMarksChanged + writeTicksToModel(). Return true.
      **Step 4 — comments.** English ASCII-only. Cite upstream IMSlider tick drag + the 5-type coverage rationale.
    </action>
    <verify>`rg -n "addColorChangeAtLayer|addTemplateAtLayer|moveTick" src/core/viewmodels/PreviewViewModel.{h,cpp}`</verify>
    <acceptance_criteria>TC-01, TC-02 implemented.</acceptance_criteria>
  </task>

  <task id="119-01-02" type="implementation">
    <title>Wire drag-to-relocate + Add menu color-change/template into PreviewLayerRail.qml</title>
    <read_first>
      - src/qml_gui/components/PreviewLayerRail.qml (Phase 117 — tick Repeater delegate + sliderAddMenu)
      - src/qml_gui/components/CustomGcodeDialog.qml (extend for color/extruder picker if needed)
    </read_first>
    <files>
      - src/qml_gui/components/PreviewLayerRail.qml
      - src/qml_gui/components/CustomGcodeDialog.qml
    </files>
    <action>
      **Step 1 — tick drag.** In the tick delegate (Repeater), add a `DragHandler` or `MouseArea` drag that tracks vertical movement. During drag, bind the delegate's y to the cursor (visual follow). On release, compute target layer from final y: `targetLayer = Math.round((y - trackMargin) / trackHeight * lastLayerIndex)`, clamp, call `previewVm.moveTick(tickLayer, targetLayer)`. If moveTick returns false (occupied), snap back.
      **Step 2 — Add menu color-change/template.** Add "Add Color Change" and "Add Template" items to sliderAddMenu. Color Change opens a small picker (extruder ComboBox + color TextField, or reuse an extended CustomGcodeDialog mode). Template calls addTemplateAtLayer directly (no extra input).
      **Step 3 — CustomGcodeDialog extension (if needed).** Add an optional extruder+color row (visible when adding ColorChange/ToolChange). Keep the CustomGcode-text field for Custom type.
      **Step 4 — comments.** English ASCII-only. Cite upstream IMSlider drag.
    </action>
    <verify>Build clean (j6); PreviewLayerRail references moveTick + the 2 new menu items.</verify>
    <acceptance_criteria>TC-03, TC-04 implemented.</acceptance_criteria>
  </task>

  <task id="119-01-03" type="test">
    <title>Add tickTypeCoverageAndDragRelocation source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (Phase 117/118 slot pattern + readSource)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add slot asserting: (a) PreviewViewModel.h/cpp have addColorChangeAtLayer + addTemplateAtLayer + moveTick; (b) PreviewLayerRail.qml references moveTick; (c) PreviewLayerRail.qml Add menu has color-change + template items (grep the menu text or method calls). TICK-04/TICK-05-named QVERIFY2.</action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>TC-05.</acceptance_criteria>
  </task>

  <task id="119-01-04" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1 (j6)</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records build + ctest results.</verify>
    <acceptance_criteria>TC-06.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- TICK-04 + TICK-05 closed; every TC truth satisfied.

## Expected Non-Changes
- No Phase 118 write-back changes (reused as-is).
- No new tick types beyond the 5 upstream.
- Full template "save state" UI is future (data round-trip in-scope).
