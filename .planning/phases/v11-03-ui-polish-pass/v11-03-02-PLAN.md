---
phase: v11-03-ui-polish-pass
plan: 02
type: execute
wave: 2
depends_on: ["v11-03-01"]
files_modified:
  - src/qml_gui/panels/PrintSettings.qml
  - src/qml_gui/panels/Sidebar.qml
autonomous: true
requirements:
  - UI-01
  - UI-03

must_haves:
  truths:
    - "PrintSettings panel uses Theme tokens for all non-semantic, non-brand colors"
    - "Sidebar panel uses Theme tokens for all non-semantic colors"
    - "Both panels build without QML warnings"
  artifacts:
    - path: "src/qml_gui/panels/PrintSettings.qml"
      provides: "Tokenized print settings panel"
      min_lines: 1400
    - path: "src/qml_gui/panels/Sidebar.qml"
      provides: "Tokenized sidebar panel"
      min_lines: 400
  key_links:
    - from: "src/qml_gui/panels/PrintSettings.qml"
      to: "Theme.qml"
      via: "Theme.bgPanel, Theme.textTertiary, Theme.borderSubtle, etc."
      pattern: "Theme\\."
    - from: "src/qml_gui/panels/Sidebar.qml"
      to: "Theme.qml"
      via: "Theme.bgFloating, Theme.bgInset, Theme.borderInput, etc."
      pattern: "Theme\\."
---

<objective>
Tokenize PrintSettings.qml (98 hardcoded colors) and Sidebar.qml (20 hardcoded colors).

Purpose: PrintSettings is the right-panel parameter editor -- the most interactive panel during the configure step of the slicing workflow. Sidebar is the collapsible right-side panel shell. Both are visible on every Prepare page interaction. This wave depends on Plan 01 because it uses the new Theme tokens (bgTooltip, textMuted, borderInput) added there.

Output: Two files with hardcoded hex colors replaced by Theme tokens, except semantic colors (filament slot brand colors, value chain level colors, modified-option highlight colors).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/v11-03-ui-polish-pass/03-RESEARCH.md
@src/qml_gui/Theme.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize PrintSettings.qml</name>
  <files>src/qml_gui/panels/PrintSettings.qml</files>
  <action>
Replace hardcoded hex colors in PrintSettings.qml (98 occurrences) with Theme tokens. The file is ~1723 lines. Apply the following systematic mapping:

**Section header backgrounds** (appears ~8 times):
- `#151a22`, `#1a2030`, `#161c28`, `#161c27` -> `Theme.bgPanel` (#1a1e28, close match)
- `#0f1520` (value chain popup darker bg) -> `Theme.bgBase` (#0d0f12)

**Text colors** (appears ~25 times):
- `#8b949e`, `#8a96a8` (muted/label text) -> `Theme.textMuted` (new token from Plan 01, #8b949e)
- `#9daaba`, `#a0abbe` (secondary text) -> `Theme.textSecondary` (#a0abbe)
- `#c8d4e0`, `#dde4ef`, `#e2e8f1` (primary text) -> `Theme.textPrimary` (#e8edf6)
- `#566070` (disabled text) -> `Theme.textDisabled` (#566070)
- `#7a8fa3` (tertiary text) -> `Theme.textTertiary` (#7f90a6)

**Border colors** (appears ~12 times):
- `#2e3540`, `#2e3848`, `#2a3040` (subtle borders) -> `Theme.borderSubtle` (#2a3040) or `Theme.borderInput` (#2e3848) for input field borders specifically
- `#363d4e`, `#454d5e` (default borders) -> `Theme.borderDefault` (#363d4e)
- `#4d5566` (stronger borders on buttons) -> `Theme.borderStrong` (#454d5e)

**Background fills** (appears ~15 times):
- `#1e2229` (panel bg) -> `Theme.bgPanel`
- `#252b38` (elevated bg) -> `Theme.bgElevated`
- `#0d1117`, `#0f1318` (inset/input bg) -> `Theme.bgInset` (#10141c)
- `#2a2a2a` (dim bg for unsliced plate) -> `Theme.bgElevated` (#252b38)

**KEEP AS-IS (intentional semantic/brand colors -- do NOT replace):**
- Filament slot colors: `#b97914`, `#214bc2`, `#d63a21`, `#b9b9b9` (upstream-defined brand colors)
- Value chain level indicator colors: `#506070`, `#6ed4a0`, `#d4a06e`, `#6ea8d4` (semantic: printer/filament/process level)
- Modified option highlight: `#e8864a` (orange for "dirty" option key)
- Accent colors that already match: `#18c75e` -> use `Theme.accent` where it appears

**Font size normalization** where pixelSize matches a Theme token:
- `font.pixelSize: 11` -> `Theme.fontSizeSM`
- `font.pixelSize: 12` -> `Theme.fontSizeMD`
- `font.pixelSize: 14` -> `Theme.fontSizeLG`
- `font.pixelSize: 10` -> `Theme.fontSizeXS`
- Leave font sizes 8, 9, 13 as-is (no matching token)

Work through the file section by section. Use grep to find each occurrence, replace with the mapped token, and move to the next.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && echo "=== PrintSettings remaining hex ===" && grep -n '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/panels/PrintSettings.qml | grep -v '#b97914\|#214bc2\|#d63a21\|#b9b9b9\|#506070\|#6ed4a0\|#d4a06e\|#6ea8d4\|#e8864a' | wc -l && echo "=== Theme refs ===" && grep -c 'Theme\.' src/qml_gui/panels/PrintSettings.qml</automated>
  </verify>
  <done>PrintSettings.qml has at most 12 remaining hardcoded hex colors (all intentional semantic colors: filament slot brand colors, value chain level indicators, modified-option highlight). All panel backgrounds, text colors, and borders use Theme tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize Sidebar.qml</name>
  <files>src/qml_gui/panels/Sidebar.qml</files>
  <action>
Replace hardcoded hex colors in Sidebar.qml (20 occurrences) with Theme tokens. Apply the following mapping:

**Panel backgrounds:**
- `#1a202bd9` (floating panel bg with alpha) -> `Theme.bgFloating` (#1a202bd9, exact match)
- `#1e2229` -> `Theme.bgPanel`
- `#252b38` -> `Theme.bgElevated`

**Input field backgrounds:**
- `#0d1117` -> `Theme.bgInset` (#10141c, close match)

**Input borders:**
- `#2e3848` -> `Theme.borderInput` (new token from Plan 01)
- `#2e3540` -> `Theme.borderSubtle`

**Text colors:**
- `#8b949e` -> `Theme.textMuted` (new token from Plan 01)
- `#9daaba` -> `Theme.textSecondary`
- `#c8d4e0`, `#e2e8f1` -> `Theme.textPrimary`
- `#566070` -> `Theme.textDisabled`

**Other:**
- `#2d3443` (border) -> `Theme.borderSubtle` (#2a3040, close)
- `#363d4e` -> `Theme.borderDefault`
- `#1e2535` (hover bg) -> `Theme.bgHover` (#2e3444, close match)
- `#4a0b1018` (shadow/gradient) -- KEEP AS-IS if used in gradient/visual effect

Font size normalization: same rules as PrintSettings -- replace only values that exactly match a Theme token.
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && echo "=== Sidebar remaining hex ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/panels/Sidebar.qml && echo "=== Theme refs ===" && grep -c 'Theme\.' src/qml_gui/panels/Sidebar.qml</automated>
  </verify>
  <done>Sidebar.qml has at most 2 remaining hardcoded hex colors (gradient/shadow effects that have no Theme token equivalent). All panel backgrounds, input fields, borders, and text colors use Theme tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure QML visual property replacement. No trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v11-03-02 | N/A | N/A | accept | No security-relevant changes |
</threat_model>

<verification>
1. Build passes: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
2. PrintSettings.qml: non-semantic hardcoded hex count <= 12 (down from 98)
3. Sidebar.qml: non-semantic hardcoded hex count <= 2 (down from 20)
4. Both files have increased Theme.* reference count
</verification>

<success_criteria>
1. PrintSettings.qml: all panel bg, text, and border hardcoded colors replaced with Theme tokens. Remaining <= 12 hardcoded colors are intentional semantic/brand colors.
2. Sidebar.qml: all panel bg, input bg, border, and text hardcoded colors replaced with Theme tokens. Remaining <= 2 hardcoded colors are gradient/shadow effects.
3. Build passes with zero errors and zero QML warnings
</success_criteria>

<output>
After completion, create `.planning/phases/v11-03-ui-polish-pass/v11-03-02-SUMMARY.md`
</output>
