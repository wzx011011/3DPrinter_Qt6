---
phase: v11-03-ui-polish-pass
plan: 03
type: execute
wave: 3
depends_on: ["v11-03-01"]
files_modified:
  - src/qml_gui/pages/PreparePage.qml
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "PreparePage uses Theme tokens for all gizmo panel backgrounds, borders, and label text"
    - "Semantic axis colors (red/green/blue for X/Y/Z) remain unchanged"
    - "Gizmo-specific accent colors (cut-mode blue, connector amber) remain unchanged"
    - "Build passes with zero QML warnings"
  artifacts:
    - path: "src/qml_gui/pages/PreparePage.qml"
      provides: "Tokenized prepare page with 3000+ lines"
      min_lines: 2800
  key_links:
    - from: "src/qml_gui/pages/PreparePage.qml"
      to: "Theme.qml"
      via: "Theme.bgFloating, Theme.borderSubtle, Theme.textMuted, Theme.textPrimary, etc."
      pattern: "Theme\\."
---

<objective>
Tokenize PreparePage.qml -- the largest single file (195 hardcoded hex colors, 3000+ lines).

Purpose: PreparePage is the primary workspace page. It contains the most gizmo overlay panels (Measure, Cut, Support Paint, Seam Paint, Emboss, Boolean, Drill, Simplify, etc.), each with their own floating panel UI. The replacement pattern is highly repetitive: gizmo panels all use `#161c27de`/`#161c27e0` for background and `#2d3443` for borders. This is the highest-volume mechanical task in the phase.

Output: PreparePage.qml with all gizmo panel backgrounds, borders, and text labels using Theme tokens. Semantic colors preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/v11-03-ui-polish-pass/03-RESEARCH.md
@src/qml_gui/Theme.qml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize PreparePage.qml gizmo panels and general UI (195 hardcoded colors)</name>
  <files>src/qml_gui/pages/PreparePage.qml</files>
  <action>
This is a large file (~3600 lines) with 195 hardcoded hex colors. Work systematically through the file. The dominant patterns are:

**Gizmo floating panel backgrounds (appears ~10 panels, each with same pattern):**
- `#161c27de` / `#161c27e0` (panel bg with alpha) -> `Theme.bgFloating` (#1a202bd9, functionally identical in dark theme)
- `#2d3443` (panel border) -> `Theme.borderSubtle` (#2a3040)

These two values alone account for ~40 of the 195 occurrences (each gizmo panel has both).

**Gizmo panel title text:**
- `#e8edf6`, `#e2e8f5`, `#e2e8f1` (bright title text) -> `Theme.textPrimary` (#e8edf6)

**Gizmo panel label text:**
- `#8b949e`, `#8a96a8` (muted labels like "X:", "Y:", "Z:", "类型:", "尺寸:") -> `Theme.textMuted` (new token #8b949e from Plan 01)

**Gizmo panel value text:**
- `#c8d4e0` (value readouts like measurement dimensions) -> `Theme.textPrimary` (#e8edf6)

**Gizmo mode selector buttons (active/inactive states):**
- `#1a1e28` (inactive button bg) -> `Theme.bgPanel` (#1a1e28, exact match)
- `#1c2a3e` (active button bg) -> `Theme.accentSubtle` (#0e6636) -- but `#1c2a3e` is blue-tinted while accentSubtle is green-tinted. For cut gizmo, the active bg is blue-tinted intentionally. KEEP `#1c2a3e` AS-IS for cut/connector gizmos where the accent color is blue/amber (not green).
- For measure gizmo where active uses `#569cd6` (blue accent) -- KEEP AS-IS (semantic Z-axis label color)
- `#2e3444` (inactive button border) -> `Theme.borderSubtle` (#2a3040)

**Semantic colors to KEEP AS-IS (do NOT replace):**
- Axis colors: `#e066a0` (X-axis pink), `#4ec9b0` (Y-axis teal), `#569cd6` (Z-axis blue)
- Cut mode accent: `#5b8def` (blue for cut mode)
- Connector colors: `#e8a838` (amber for connector type/style/shape)
- Extruder brand colors: `#FF4444`, `#44AA44`, `#4444FF`, `#FF8800`
- Object status colors: `#27ae60` (healthy), `#e67e22` (warning), `#c0392b` (error)
- Warning bg colors: `#4a1c1c`, `#3a3420` -> already have `Theme.bgErrorSubtle` and `Theme.bgWarningSubtle` from Plan 01 -- USE THESE TOKENS
- Warning border colors: `#ef4444` -> `Theme.statusError`, `#f59e0b` -> `Theme.statusWarning`
- Brand purple/blue: `#7c3aed`, `#6d28d9`, `#2563eb`, `#1d4ed8` (preview/export buttons)
- Gradients: `#4a0b1018`, `#1a213018`, `#09101840` (shadow/fade gradients) -- KEEP AS-IS
- White: `"#fff"`, `"#ffffff"` -- KEEP AS-IS (text on colored buttons)

**General UI elements:**
- `#1e2229` (panel bg) -> `Theme.bgPanel`
- `#252b38` -> `Theme.bgElevated`
- `#0f1318`, `#141920` -> `Theme.bgInset` (#10141c)
- `#2e3540`, `#2a3040` (borders) -> `Theme.borderSubtle`
- `#363d4e`, `#4d5566` (stronger borders) -> `Theme.borderDefault` / `Theme.borderStrong`
- `#18c75e` (accent green) -> `Theme.accent`
- `#0e3325` (success subtle bg) -> `Theme.accentSubtle` (#0e6636, different but same semantic role)
- `#2e2510` (warning subtle bg) -> `Theme.bgWarningSubtle` (#3a3420, close)
- `#1e2535` (hover bg) -> `Theme.bgHover` (#2e3444)
- `#9daaba` -> `Theme.textSecondary`
- `#566070` -> `Theme.textDisabled`
- `#6b7d94` -> `Theme.textTertiary` (#7f90a6, close)
- `#e06666` (delete red) -> `Theme.statusError` (#e04040, close)

**Font size normalization** where pixelSize matches a Theme token:
- `font.pixelSize: 11` -> `Theme.fontSizeSM`
- `font.pixelSize: 12` -> `Theme.fontSizeMD`
- `font.pixelSize: 10` -> `Theme.fontSizeXS`
- `font.pixelSize: 9` -- KEEP AS-IS (no token)
- `font.pixelSize: 14` -> `Theme.fontSizeLG`
  </action>
  <verify>
    <automated>cd E:/ai/3DPrinter_Qt6 && echo "=== PreparePage remaining hex ===" && grep -c '"#[0-9a-fA-F]\{3,8\}"' src/qml_gui/pages/PreparePage.qml && echo "=== Theme refs ===" && grep -c 'Theme\.' src/qml_gui/pages/PreparePage.qml</automated>
  </verify>
  <done>PreparePage.qml has at most 40 remaining hardcoded hex colors (all intentional: axis colors, cut-mode blue, connector amber, extruder brand colors, object status colors, button brand purple/blue, gradient shadows, white text on colored buttons, and micro font size 9 labels). All gizmo panel backgrounds (`#161c27de`/`#161c27e0`), borders (`#2d3443`), and label text colors replaced with Theme tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure QML visual property replacement. No trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v11-03-03 | N/A | N/A | accept | No security-relevant changes |
</threat_model>

<verification>
1. Build passes: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
2. PreparePage.qml: total hardcoded hex count <= 40 (down from 195)
3. PreparePage.qml: Theme.* reference count significantly increased
4. Specifically verify gizmo panel pattern: `grep '#161c27' src/qml_gui/pages/PreparePage.qml` should return 0 results (all replaced with Theme.bgFloating)
5. Verify gizmo border pattern: `grep '#2d3443' src/qml_gui/pages/PreparePage.qml` should return 0 results
</verification>

<success_criteria>
1. PreparePage.qml: all gizmo panel backgrounds use Theme.bgFloating instead of hardcoded `#161c27de`/`#161c27e0`
2. PreparePage.qml: all gizmo panel borders use Theme.borderSubtle instead of hardcoded `#2d3443`
3. PreparePage.qml: all gizmo label text uses Theme.textMuted or Theme.textPrimary instead of hardcoded `#8b949e`/`#c8d4e0`
4. PreparePage.qml: remaining hardcoded hex colors are only semantic colors (axis, brand, status, gradient)
5. Build passes with zero errors and zero QML warnings
6. Total hardcoded hex count across all 3 plans' files reduced from ~344 to <= 54
</success_criteria>

<output>
After completion, create `.planning/phases/v11-03-ui-polish-pass/v11-03-03-SUMMARY.md`
</output>
