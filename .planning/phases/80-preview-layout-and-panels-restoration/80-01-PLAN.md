# Phase 80 Plan: Restore Preview Layout And Panels

## Requirements

- `PVLAYOUT-01`: screenshot-aligned Preview layout without overlap.
- `PVLAYOUT-02`: readable statistics, metadata, estimates, legend, and G-code
  source panel.
- `PVLAYOUT-03`: no visible Preview placeholders, raw internal labels, or dead
  controls.

## Plan

1. Add a failing QML source audit to `QmlUiAuditTests` that locks Phase 80
   layout anchors, right-panel bindings, and absence of Preview mojibake.
2. Update `PreviewPage.qml`:
   - fixed target left/right widths,
   - dense right analysis stack,
   - readable Preview header metrics,
   - stable G-code source panel,
   - preserve existing renderer/ViewModel bindings.
3. Update `StatsPanel.qml`, `VisibilityFilter.qml`, and `Legend.qml`:
   - replace mojibake with readable localized strings,
   - keep toggle methods and role/legend bindings,
   - reduce panel row height and nested visual weight.
4. Run focused verification:
   - `QmlUiAuditTests.exe previewLayoutRestoresScreenshotRegionsAndGcodePanel`,
   - `git diff --check`,
   - encoding guard.
5. Complete Phase 80 state and summary only after the audit passes.

## Expected Non-Changes

- No C++ backend behavior changes.
- No QRhi backend policy change.
- No renderer wire-format or parser change.
