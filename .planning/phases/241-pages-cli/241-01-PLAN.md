# Phase 241 Plan — Page Honesty And CLI Surface

**Requirements:** PAGE-01..04, CLI-01..02
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` pages + CLI sections

## Plan 1 (single batch)

### Task 1 — PAGE-01: HomePage honesty
- Recent projects: real list from ProjectViewModel (persistence from Phase
  233's recent-file work) or honest removal; clickable cards → open.
- Quick actions: wire open/new/calibration to real handlers; remove/replace
  the dead ModelMall entry. DailyTips: rotate BackendContext hint database
  (hints.json already loaded) with image/link support where available.

### Task 2 — PAGE-02: ProjectPage honesty
- Remove the console.log New Project button → backend.topbarNewProject;
  file tree from the real loaded project (source file + plate thumbnails)
  or honest empty state; details panel (Size/Modified) via QFileInfo on the
  real path. Long-term shape (3MF thumbnail/aux viewer) noted as follow-up.

### Task 3 — PAGE-03: calibration completeness
- PA_Pattern/PA_Tower modes (upstream Plater.cpp:9406-9413; pattern
  generation in GCode.cpp calib paths + bundled
  pressure_advance_test.stl), FlowRate pass1/pass2 two-stage flow (bundled
  flowrate-test-pass1/pass2.3mf assets exist, zero refs), "save to preset"
  writes the measured value into the filament preset (PA → pressure_advance
  / flow ratio), history persisted to disk (QSettings/JSON in app data).

### Task 4 — PAGE-04: preferences take effect
- defaultPage: startup maps to the saved page (main_qml.cpp:126 mapping).
- use_inches: unit conversion in size inputs (object list dims, manipulator).
- autoSave/backup: real periodic project backup to app-data backup dir
  (upstream backup_switch semantics — seconds-level crash backup; reconcile
  the semantic drift or rename the setting honestly).
- Remaining decorative settings (userRole, region, compactMode, reducedMotion,
  autoUpload, dev-page knobs): wire where a consumer is reasonable in scope,
  else honest "not yet effective" hint. Remove settings with no upstream
  mapping that stay dead.

### Task 5 — CLI-01/02: transforms, exports, key overrides
- CliRunner: --arrange/--orient/--cut x y z/--split/--assemble/--repair/
  --scale-to-fit before slicing; --export-stl/--export-3mf/--export-slicedata
  post-load; arbitrary `--key value` PrintConfig overrides layered onto the
  merged preset config (validate against known option keys via
  DynamicPrintConfig).

## Verification

- Canonical build + all suites; CliTests extended for transforms + key
  override; page smoke (no dead controls per QmlUiAudit pattern).
