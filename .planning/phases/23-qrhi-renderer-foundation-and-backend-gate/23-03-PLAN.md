---
phase: 23
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tools/render_bench/main.cpp
  - scripts/auto_verify_with_vcvars.ps1
  - tests/QmlUiAuditTests.cpp
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-VERIFICATION.md
  - .planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-01-SUMMARY.md
autonomous: true
requirements: [RHI-06, PERF-05, RHI-01, RHI-03, RHI-05]
must_haves:
  truths:
    - "Optional render benchmark still reports D3D12/D3D11 metrics and Vulkan-disabled status."
    - "Canonical verification still leaves QRhi disabled by default and benchmark optional."
    - "Phase 23 requirements are updated with implementation evidence after verification."
    - "Fallback, gate, shader, and benchmark behavior are all covered by automated checks or explicit manual note."
  artifacts:
    - path: "tools/render_bench/main.cpp"
      provides: "Benchmark JSON evidence and backend naming consistency"
      min_lines: 1
    - path: "scripts/auto_verify_with_vcvars.ps1"
      provides: "Optional benchmark invocation remains env-gated"
      min_lines: 1
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Final regression guard for gate/default/benchmark contract"
      min_lines: 1
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 23 requirement status/evidence"
      min_lines: 1
    - path: ".planning/ROADMAP.md"
      provides: "Phase 23 plan/progress handoff update"
      min_lines: 1
    - path: ".planning/STATE.md"
      provides: "Phase status update"
      min_lines: 1
    - path: ".planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-VERIFICATION.md"
      provides: "Canonical and optional benchmark verification evidence"
      min_lines: 20
    - path: ".planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-01-SUMMARY.md"
      provides: "Phase 23 completion summary and handoff notes"
      min_lines: 20
  key_links:
    - from: "scripts/auto_verify_with_vcvars.ps1"
      to: "owzx-render-bench"
      via: "OWZX_RENDER_BENCH env gate only"
    - from: "QmlUiAuditTests"
      to: "scripts/auto_verify_with_vcvars.ps1"
      via: "assert canonical script does not enable QRhi renderer by default"
    - from: "REQUIREMENTS.md"
      to: "Phase 23 implementation"
      via: "evidence rows for RHI-01, RHI-02, RHI-03, RHI-05, RHI-06, PERF-05"
---

# Plan 23-03: Benchmark Evidence, Verification, And Requirement Traceability

<objective>
Close Phase 23 by validating benchmark evidence, guarding the optional benchmark/default startup contract, running canonical verification, and updating requirement traceability. This plan covers RHI-06 and PERF-05 while proving RHI-01/RHI-03/RHI-05 did not regress during implementation.
</objective>

<context>
Read first:
- `.planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-CONTEXT.md`
- `.planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-RESEARCH.md`
- `.planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-VALIDATION.md`
- `tools/render_bench/main.cpp`
- `scripts/auto_verify_with_vcvars.ps1`
- `tests/QmlUiAuditTests.cpp`
- `.planning/REQUIREMENTS.md`
- `.codex/rules/build-rules.md`
</context>

<tasks>
  <task id="23-03-T1" type="auto">
    <name>Align benchmark diagnostics with app backend policy</name>
    <files>
tools/render_bench/main.cpp
tests/QmlUiAuditTests.cpp
    </files>
    <action>
Review `tools/render_bench/main.cpp` after Plans 01-02 and adjust only if needed so its JSON keys and backend names match app diagnostics: requested backend, attempted backend failures, selected backend, D3D12/D3D11 results, and Vulkan-disabled status. Do not make Vulkan a default app backend. Add or extend QmlUiAuditTests assertions that benchmark `auto` uses D3D12/D3D11 while `all` reports Vulkan separately.
    </action>
    <verify>
      <automated>build/owzx-render-bench.exe --segments 1000 --frames 5 --backend all</automated>
    </verify>
    <done>
Benchmark emits JSON with D3D12 and D3D11 successful results when available and `failures.vulkan` explaining current QtGui Vulkan-disabled status.
    </done>
  </task>

  <task id="23-03-T2" type="auto">
    <name>Run canonical and optional benchmark verification</name>
    <files>
scripts/auto_verify_with_vcvars.ps1
tests/QmlUiAuditTests.cpp
.planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-VERIFICATION.md
    </files>
    <action>
Run the canonical command exactly: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`. Then run the optional benchmark through the canonical script path by setting `OWZX_RENDER_BENCH=1`, with reduced `OWZX_RENDER_BENCH_SEGMENTS=1000`, `OWZX_RENDER_BENCH_FRAMES=5`, and `OWZX_RENDER_BENCH_BACKEND=all` if needed for quick evidence. Record results in `23-VERIFICATION.md` with `status: passed` only if the canonical command exits 0 and optional benchmark reports expected backend JSON. If full canonical verification cannot be run, record `status: human_needed` or the actual failure reason instead of claiming pass.
    </action>
    <verify>
      <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
    </verify>
    <done>
`23-VERIFICATION.md` exists and contains the exact commands, exit status, QmlUiAudit result, and benchmark JSON summary. Canonical verification is not bypassed.
    </done>
  </task>

  <task id="23-03-T3" type="auto">
    <name>Update Phase 23 traceability and handoff state</name>
    <files>
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/STATE.md
.planning/phases/23-qrhi-renderer-foundation-and-backend-gate/23-01-SUMMARY.md
    </files>
    <action>
After verification, update Phase 23 planning docs only with evidence that actually exists. Mark RHI-01, RHI-02, RHI-03, RHI-05, RHI-06, and PERF-05 complete or partially complete with file/test evidence. Update ROADMAP Phase 23 plan list/progress, STATE current position to Phase 24 next, and write `23-01-SUMMARY.md` with commits, deviations, and self-check. Keep Phase 24/25 real plate/model rendering explicitly pending; do not claim them from Phase 23.
    </action>
    <verify>
      <automated>rg -n "RHI-01|RHI-02|RHI-03|RHI-05|RHI-06|PERF-05|Phase 24|23-VERIFICATION" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/23-qrhi-renderer-foundation-and-backend-gate</automated>
    </verify>
    <done>
Requirement traceability cites concrete Phase 23 files/tests; STATE points to Phase 24; SUMMARY/VERIFICATION exist with honest status.
    </done>
  </task>
</tasks>

<verification>
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
- With optional env: `OWZX_RENDER_BENCH=1`, `OWZX_RENDER_BENCH_SEGMENTS=1000`, `OWZX_RENDER_BENCH_FRAMES=5`, `OWZX_RENDER_BENCH_BACKEND=all`.
- `build/owzx-render-bench.exe --segments 1000 --frames 5 --backend all` may be used for direct benchmark confirmation after target build.
</verification>

<threat_model>
| Threat | STRIDE | Mitigation | Disposition |
|--------|--------|------------|-------------|
| Benchmark env leaks into normal startup | Denial of Service | Script gate remains `OWZX_RENDER_BENCH`; QmlUiAudit checks no QRhi renderer env is set by default | mitigate |
| Requirements marked complete without evidence | Repudiation | VERIFICATION/SUMMARY must cite exact commands and files before status update | mitigate |
</threat_model>

<success_criteria>
- RHI-06 covered: benchmark runs and reports structured backend JSON.
- PERF-05 covered: backend/timing evidence recorded.
- RHI-01 remains covered: canonical verification does not enable QRhi by default.
- Phase 23 docs honestly distinguish foundation completion from deferred real plate/model/preview rendering.
</success_criteria>
