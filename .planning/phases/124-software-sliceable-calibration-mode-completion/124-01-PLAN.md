---
phase: 124-software-sliceable-calibration-mode-completion
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/core/services/CalibrationServiceMock.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [CALIB-01]
---

# Phase 124 Plan: Software-Sliceable Calibration Mode Completion

## Objective
Close CALIB-01: add the 3 libslic3r tower modes Qt6 doesn't dispatch (Vol_speed=7, VFA=8, Retraction=9). SliceService/Print/GCode need NO changes (transparent passthrough). This is a CalibrationServiceMock.cpp data change + a regression slot.

## Must Haves
<must_haves>
  <truth id="CM-01">CalibrationServiceMock::buildMockData adds 3 new CalibrationType entries: MaxVolumetricSpeed (calibMode=7), VFA/VolumetricRate (calibMode=8), RetractionTune (calibMode=9). Each: implemented=true, startable=true, non-empty start/end/step, source-truth-aligned id/name/desc, no unavailableReason (or honest "uses current-plate model"). The existing max_volumetric_speed placeholder (:160-179) is upgraded (calibMode 0→7, remove "Pending" reason, startable false→true) rather than duplicated.</truth>
  <truth id="CM-02">No SliceService / Print / GCode changes — the transparent passthrough (static_cast<CalibMode>(int) at SliceService.cpp:572) handles the new modes. libslic3r GCode branches (GCode.cpp:4612-4630) already exist for Vol_speed/VFA/Retraction.</truth>
  <truth id="CM-03">The "Pending: outside Phase 12" marker on max_volumetric_speed is removed (replaced with real dispatch). Hardware modes (bed_leveling/vibration) KEEP their honest "requires hardware" unavailableReason (out of scope).</truth>
  <truth id="CM-04">A `calibrationTowerModesDispatchToLibslic3r` source-audit slot asserts: (a) CalibrationServiceMock.cpp has calibMode 7/8/9 (the 3 new tower modes); (b) the max_volumetric_speed entry no longer says "Pending"; (c) bed_leveling/vibration still have their hardware unavailableReason (not accidentally enabled). CALIB-01-named QVERIFY2.</truth>
  <truth id="CM-05">Canonical build (j6) + regression ctest passes; the existing calibrationUnsupportedModesAreExplicitlyUnavailable smoke test may need updating if it asserted max_volumetric_speed was unavailable (it's now available) — update the assertion to assert only hardware modes stay unavailable.</truth>
</must_haves>

<threat_model>
Low risk. Calibration modes produce G-code via the hardened SliceService worker path (capture-by-value, same as all slices). The tower geometry is the user's current-plate model (documented tech-debt vs upstream's dedicated .drc). No new external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="124-01-01" type="implementation">
    <title>Add 3 tower CalibrationType entries in CalibrationServiceMock.cpp</title>
    <read_first>
      - .planning/phases/124-software-sliceable-calibration-mode-completion/124-CONTEXT.md
      - src/core/services/CalibrationServiceMock.cpp:23-186 (buildMockData — the 6 existing entries; :160-179 max_volumetric_speed to upgrade)
      - third_party/OrcaSlicer/src/libslic3r/calib.hpp:16-30 (CalibMode enum: Vol_speed_Tower=7, VFA_Tower=8, Retraction_tower=9)
    </read_first>
    <files>src/core/services/CalibrationServiceMock.cpp</files>
    <action>
      **Step 1 — upgrade max_volumetric_speed** (:160-179): set calibMode=7 (Calib_Vol_speed_Tower), implemented=true, startable=true, sensible start/end/step (e.g. 5.0/30.0/0.5 mm³/s per upstream), remove the "Pending: outside Phase 12" unavailableReason.
      **Step 2 — add VFA entry**: new CalibrationType{id="vfa_tower", calibMode=8, name/desc source-truth-aligned, start/end/step (e.g. speed 10-100 step 5), implemented=true, startable=true.
      **Step 3 — add Retraction entry**: new CalibrationType{id="retraction_tune", calibMode=9, start/end/step (e.g. 0-2mm step 0.1), implemented=true, startable=true.
      **Step 4 — keep hardware modes unavailable**: bed_leveling/vibration unchanged (honest "requires hardware").
      **Step 5 — comments**: English ASCII-only. Cite calib.hpp GCode branches + the documented geometry tech-debt (current-plate model, not upstream .drc).
    </action>
    <verify>`rg -n "calibMode = 7|calibMode = 8|calibMode = 9|Pending: outside Phase" src/core/services/CalibrationServiceMock.cpp`</verify>
    <acceptance_criteria>CM-01, CM-02, CM-03.</acceptance_criteria>
  </task>

  <task id="124-01-02" type="test">
    <title>Add calibrationTowerModesDispatchToLibslic3r slot + fix calibrationUnsupported smoke if needed</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern), tests/ViewModelSmokeTests.cpp (calibrationUnsupportedModesAreExplicitlyUnavailable — may assert max_volumetric_speed unavailable)</read_first>
    <files>tests/QmlUiAuditTests.cpp, tests/ViewModelSmokeTests.cpp</files>
    <action>
      Add calibrationTowerModesDispatchToLibslic3r source-audit slot (read CalibrationServiceMock.cpp; assert calibMode 7/8/9 present; assert "Pending: outside Phase" gone; assert bed_leveling/vibration unavailableReason still present). If ViewModelSmokeTests::calibrationUnsupportedModesAreExplicitlyUnavailable asserted max_volumetric_speed was unavailable, update it to assert ONLY the 2 hardware modes are unavailable (max_volumetric_speed is now available).
    </action>
    <verify>ctest passes incl. new/updated slots.</verify>
    <acceptance_criteria>CM-04, CM-05.</acceptance_criteria>
  </task>

  <task id="124-01-03" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>CM-05 build clean.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- CALIB-01 closed; every CM truth satisfied; 6/9 software modes now dispatch (PA/FlowRate/TempTower + Vol_speed/VFA/Retraction).

## Expected Non-Changes
- No SliceService/Print/GCode changes.
- No dedicated .drc model loading (future tech-debt).
- No hardware modes enabled.
