---
phase: 110-filament-map-popup-ui-and-mode-surfacing
plan: 01
type: implementation
wave: 1
depends_on:
  - phase: 107-filament-map-mode-enum-widening-and-3mf-migration
    provides: 4-value FilamentMapMode enum + 3MF migration.
  - phase: 108-filament-map-auto-recommendation-readback
    provides: EditorViewModel Q_PROPERTYs (hasAutoFilamentMap, autoFilamentMapMode, autoFilamentMaps) — the popup binding targets.
files_modified:
  - src/qml_gui/dialogs/FilamentGroupPopup.qml
  - src/qml_gui/panels/BBLTopbar.qml
  - src/qml_gui/qml.qrc
  - src/core/model/PartPlate.h
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [FMAP-03]
---

# Phase 110 Plan: Filament-Map Popup UI And Mode Surfacing

## Objective
Close FMAP-03: a FilamentGroupPopup UI surfaces the 3 selectable filament-map modes + auto-recommended map preview. ALSO close Phase 107 REVIEW R-02 (enum range validation at the Q_INVOKABLE boundary).

## Must Haves
<must_haves>
  <truth id="FP-01">A new `FilamentGroupPopup.qml` exists in `src/qml_gui/dialogs/` (or `components/`), based on `CxPopup`. It surfaces 3 selectable mode radio buttons: AutoForFlush (labeled "Filament-Saving" or similar), AutoForMatch ("Convenience"), Manual ("Custom"). It binds the auto-recommended map preview to `editorVm.autoFilamentMaps` (Phase 108 Q_PROPERTY) and the current mode to `editorVm.autoFilamentMapMode`. `fmmDefault` is NOT exposed as a 4th radio button (anti-feature).</truth>
  <truth id="FP-02">The popup is wired into the existing topbar filament indicator (BBLTopbar.qml — find the placeholder/indicator) so the user can open it. If no placeholder exists, add a minimal trigger (the popup is reachable from the Prepare topbar).</truth>
  <truth id="FP-03">A Q_INVOKABLE write path exists so the popup can set the selected mode back: `setPlateFilamentMapMode(int plateIndex, int mode)` on ProjectServiceMock (or EditorViewModel) that calls `PartPlate::setFilamentMapMode`. If `setPlateFilamentMap` (ProjectServiceMock.h:152) already covers this, document the reuse.</truth>
  <truth id="FP-04">R-02 FIX: `setFilamentMapMode(int)` at PartPlate.h:232 validates the int is in [0,3] (the 4 enum values). Out-of-range clamps to fmmDefault (the safe per-plate sentinel) OR asserts in debug. A grep-assertable guard is present (e.g. `if (mode < 0 || mode > 3) mode = fmmDefault;` or equivalent). This prevents a QML/Q_INVOKABLE caller passing mode=5 from silently storing an invalid enum.</truth>
  <truth id="FP-05">A QmlUiAuditTests source-audit slot `filamentGroupPopupSurfacesThreeModesNotFour` asserts: (a) FilamentGroupPopup.qml exists; (b) the 3 mode names appear (AutoForFlush/AutoForMatch/Manual or their labels); (c) fmmDefault is NOT a selectable radio (anti-feature); (d) the R-02 range validation is present in PartPlate.h. FMAP-03 + R-02-named QVERIFY2 messages.</truth>
  <truth id="FP-06">The popup is registered in qml.qrc (or the QML type registration path) so it's loadable. CxPopup-based; follows the existing dialog import conventions.</truth>
  <truth id="FP-07">Canonical build + regression ctest 4+/4-equivalent passes; OWzxSlicer.exe links clean.</truth>
  <truth id="FP-08">`git diff --check` exit 0; encoding guard clean; English ASCII comments (note: QML user-facing strings use qsTr() per project convention).</truth>
</must_haves>

<threat_model>
No new attack surface. The popup is a UI surface binding to existing EditorViewModel Q_PROPERTYs. The write path uses the existing ProjectServiceMock API. R-02 hardens the enum boundary. No network, no external input. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="110-01-01" type="implementation">
    <title>R-02 fix: enum range validation in setFilamentMapMode(int)</title>
    <read_first>
      - src/core/model/PartPlate.h:96-101, 231-232 (enum + the int overload)
      - .planning/phases/107-filament-map-mode-enum-widening-and-3mf-migration/107-REVIEW.md (R-02 finding)
    </read_first>
    <files>
      - src/core/model/PartPlate.h
    </files>
    <action>
      Add range validation to `setFilamentMapMode(int mode)` at PartPlate.h:232: clamp out-of-range to fmmDefault. Pattern: `if (mode < 0 || mode > 3) mode = static_cast<int>(FilamentMapMode::fmmDefault); m_filament_map_mode = FilamentMapMode(mode);`. The clamp is grep-assertable for FP-05.
    </action>
    <verify>
      `rg -n "fmmDefault\|mode < 0\|mode > 3" src/core/model/PartPlate.h`
      Build clean.
    </verify>
    <acceptance_criteria">
      - Range validation present (FP-04). Out-of-range clamps to fmmDefault.
    </acceptance_criteria>
  </task>

  <task id="110-01-02" type="implementation">
    <title>Add FilamentGroupPopup.qml + BBLTopbar wiring + qml.qrc registration</title>
    <read_first>
      - src/qml_gui/controls/CxPopup.qml (popup base)
      - src/qml_gui/dialogs/ (existing dialog patterns — pick the closest, e.g. AMSSettingsDialog or a simpler one)
      - src/qml_gui/panels/BBLTopbar.qml (find the filament indicator / placeholder)
      - src/core/viewmodels/EditorViewModel.h:671-679 (Phase 108 Q_PROPERTYs — binding targets)
      - src/qml_gui/qml.qrc (registration pattern)
    </read_first>
    <files>
      - src/qml_gui/dialogs/FilamentGroupPopup.qml
      - src/qml_gui/panels/BBLTopbar.qml
      - src/qml_gui/qml.qrc
    </files>
    <action>
      Create FilamentGroupPopup.qml: CxPopup-based, 3 radio mode buttons (AutoForFlush/AutoForMatch/Manual with user-facing qsTr labels), auto-map preview bound to editorVm.autoFilamentMaps, current mode bound to editorVm.autoFilamentMapMode. Wire into BBLTopbar (find the filament indicator; add an onClicked that opens the popup). Register in qml.qrc. FP-01/FP-02/FP-06.
    </action>
    <verify>
      `ls src/qml_gui/dialogs/FilamentGroupPopup.qml`
      `rg -n "FilamentGroupPopup" src/qml_gui/qml.qrc src/qml_gui/panels/BBLTopbar.qml`
      Build clean.
    </verify>
    <acceptance_criteria>
      - Popup exists, CxPopup-based, 3 modes, auto-map preview (FP-01). Wired into BBLTopbar (FP-02). Registered in qml.qrc (FP-06).
    </acceptance_criteria>
  </task>

  <task id="110-01-03" type="implementation">
    <title>Add Q_INVOKABLE write path (setPlateFilamentMapMode) if not already present</title>
    <read_first>
      - src/core/services/ProjectServiceMock.h:149-152 (setPlateFilamentMap — check if a mode-only variant is needed)
      - src/core/viewmodels/EditorViewModel.h (Q_INVOKABLE patterns)
    </read_first>
    <files>
      - src/core/services/ProjectServiceMock.h
      - src/core/services/ProjectServiceMock.cpp
      - src/core/viewmodels/EditorViewModel.h
      - src/core/viewmodels/EditorViewModel.cpp
    </files>
    <action>
      If setPlateFilamentMap (ProjectServiceMock.h:152) already covers mode-only writes (it takes mode + maps), document the reuse. If a mode-only Q_INVOKABLE is cleaner for the popup, add `Q_INVOKABLE bool setPlateFilamentMapMode(int plateIndex, int mode)` to ProjectServiceMock + EditorViewModel delegate. The popup calls it when the user picks a mode. FP-03.
    </action>
    <verify>
      `rg -n "setPlateFilamentMapMode\|setPlateFilamentMap" src/core/services/ src/core/viewmodels/`
      Build clean.
    </verify>
    <acceptance_criteria">
      - Write path exists (reused or new) (FP-03).
    </acceptance_criteria>
  </task>

  <task id="110-01-04" type="test">
    <title>Add filamentGroupPopupSurfacesThreeModesNotFour source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp, src/qml_gui/dialogs/FilamentGroupPopup.qml, src/core/model/PartPlate.h</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add `filamentGroupPopupSurfacesThreeModesNotFour` source-audit slot asserting: (a) FilamentGroupPopup.qml exists; (b) the 3 mode names/labels appear; (c) fmmDefault NOT a selectable radio (anti-feature — assert the popup file does NOT have a 4th mode radio); (d) R-02 range validation in PartPlate.h. FMAP-03 + R-02-named QVERIFY2 messages. FP-05.
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes (FP-05).</acceptance_criteria>
  </task>

  <task id="110-01-05" type="verification">
    <title>Build + regression ctest + launch liveness + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest 4+/4-equivalent + 5-second launch liveness. Document the popup + R-02 fix in SUMMARY.</action>
    <verify>SUMMARY records build + ctest pass + liveness.</verify>
    <acceptance_criteria>Production link clean, ctest pass, launch liveness (FP-07).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest 4+/4-equivalent.
2. Source audit: `rg -n "FilamentGroupPopup\|fmmDefault.*clamp\|mode > 3" src/`
3. Encoding guard.

## Success Criteria
- FMAP-03 closed + R-02 fixed; every FP truth; 3 modes not 4 (anti-feature honored).

## Expected Non-Changes
- No FMAP-04 full round-trip (Phase 111).
- No fmmDefault as 4th radio (anti-feature).
- No change to the Phase 107/108 enum/readback.
- No libslic3r changes.
