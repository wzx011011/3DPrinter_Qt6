---
phase: 02-9-page-notebook-bbltopbar
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/qml_gui/BBLTopbar.qml
  - src/qml_gui/main.qml
  - src/qml_gui/qml.qrc
  - i18n/zh_CN.ts
autonomous: true
requirements: [ARCH-01, ARCH-04, TOPBAR-01, TOPBAR-02, TOPBAR-03, TOPBAR-04, TOPBAR-05, TOPBAR-06, TOPBAR-07]
user_setup: []

must_haves:
  truths:
    - "main.qml top-level lays out as a single BBLTopbar strip above a 9-page StackLayout"
    - "BBLTopbar contains Logo + [File ▾] + [▾] + Save/Undo/Redo/Calibration + placeholder[Account/ModelStore/Publish] + 9-tab TabBar + side_tools(Slice/Print/FilamentGroupPopup placeholder) + CenteredTitle + Bell + Min/Max/Close"
    - "Clicking any of the 9 tabs triggers backend.requestSelectTab(TabPosition value); StackLayout currentIndex follows backend.currentPage"
    - "Placeholder tabs (TabPosition 7, 8) render disabled with tooltip 'v2.1 实现' and do NOT switch page when clicked"
    - "[File ▾] menu contains in exact order: New / Open / Recent (submenu) / Save / Save As / Import (3MF/STL/OBJ/STEP/AMF submenu) / Export (G-code/3MF/Model submenu) / Quit"
    - "[▾] menu contains in exact order: Edit submenu / View submenu / Preferences / Calibration submenu (9 disabled entries placeholder) / Help submenu"
    - "CenteredTitle shows backend.displayProjectTitle centered horizontally with elide mode ElideRight"
    - "Window control buttons (Min/Max/Close) call showMinimized/showNormal-or-showMaximized/Qt.quit"
    - "No hardcoded integer page indices (1, 2, 7, 9) remain in main.qml — all references use backend.TabPosition.tpX"
    - "build/startup_diagnostics.log shows no QML errors after launch"
  artifacts:
    - path: "src/qml_gui/BBLTopbar.qml"
      provides: "Top strip component: file menu, dropdown, tool buttons, 9-tab TabBar, side_tools, centered title, window controls"
      min_lines: 250
      contains: "TabBar"
    - path: "src/qml_gui/main.qml"
      provides: "Slim ApplicationWindow hosting BBLTopbar + 9-page StackLayout + ErrorBanner + StatusBar + ErrorToast + dialogs"
      contains: "BBLTopbar"
    - path: "src/qml_gui/qml.qrc"
      provides: "Registers BBLTopbar.qml as qml resource"
      contains: "BBLTopbar.qml"
    - path: "i18n/zh_CN.ts"
      provides: "Updated translation keys for new menu items"
      contains: "导出 G-code"
  key_links:
    - from: "src/qml_gui/BBLTopbar.qml::TabBar delegate onClicked"
      to: "backend.requestSelectTab"
      via: "Q_INVOKABLE call with TabPosition enum"
      pattern: "backend\\.requestSelectTab\\(backend\\.TabPosition\\."
    - from: "src/qml_gui/main.qml::StackLayout.currentIndex"
      to: "backend.currentPage"
      via: "Q_PROPERTY binding"
      pattern: "currentIndex:\\s*backend\\.currentPage"
    - from: "src/qml_gui/BBLTopbar.qml::projectTitleLabel.text"
      to: "backend.displayProjectTitle"
      via: "Q_PROPERTY read"
      pattern: "text:\\s*backend\\.displayProjectTitle"
    - from: "src/qml_gui/BBLTopbar.qml::sliceButton.onClick"
      to: "sliceTopMenu.popup()"
      via: "CxMenu popup"
      pattern: "sliceTopMenu\\.popup"
---

<objective>
Rewrite `main.qml` top-level layout from the current hand-built 4-tab `titleBar` + 12-page StackLayout into a new `BBLTopbar.qml` component (title+menu+toolbuttons+9-tab TabBar+side_tools+centeredTitle+window controls in one strip) above a slimmed 9-page StackLayout. Complete the [File ▾] and [▾] menus to upstream OrcaSlicer coverage.

Purpose: ARCH-01 (9-page TabBar+StackLayout), ARCH-04 (side_tools region), TOPBAR-01 through TOPBAR-07 (full BBLTopbar). This plan consumes the `backend.TabPosition.tpX` enum and `backend.requestSelectTab(int)` added in Plan 02-01 — every page reference in QML migrates from hardcoded integers (`pagePrepare=1`, `pageDevice=7`, `pageOnline=9`) to `backend.TabPosition.tp3DEditor` / `tpDevice` / `tpProject`, eliminating Pitfall 1 (value desync) and Pitfall 6 (off-by-one after page count change) from 02-RESEARCH.md.

Output: New `src/qml_gui/BBLTopbar.qml` (~300 lines), rewritten `src/qml_gui/main.qml` (slimmed to ~400 lines, all topbar logic delegated), updated `qml.qrc`, regenerated `i18n/zh_CN.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-9-page-notebook-bbltopbar/02-CONTEXT.md
@.planning/phases/02-9-page-notebook-bbltopbar/02-RESEARCH.md

@src/qml_gui/main.qml
@src/qml_gui/controls/CxMenu.qml
@src/qml_gui/controls/CxMenuItem.qml
@src/qml_gui/controls/CxIconButton.qml
@src/qml_gui/Theme.qml
@src/qml_gui/BackendContext.h

<interfaces>
<!-- Existing QML primitives the executor reuses (no re-exploration). -->

From src/qml_gui/main.qml — PRESERVE these elements as-is, MOVE them into BBLTopbar.qml or keep in main.qml depending on plan task:
- CxMenu `fileMenu` (lines 115-157) — partial [File ▾], needs Import/Export submenus added
- CxMenu `topMenu` (lines 160-282) — already has Edit/View/Preferences/Calibration/Help — extend Calibration to 9 entries placeholder
- CxMenu `sliceTopMenu` (lines 285-301) — keep as-is
- CxMenu `printTopMenu` (lines 304-314) — keep as-is
- `projectTitleLabel` (lines 762-774) — moves into BBLTopbar.qml as CenteredTitle
- Window control CxIconButtons (lines 820-844) — move into BBLTopbar.qml
- 12-page StackLayout (lines 859-941) — collapse to 9 pages

From src/qml_gui/controls/CxIconButton.qml — accepted `cxStyle: CxIconButton.Style.Chrome` and `CxIconButton.Style.ChromeDanger`, props: buttonSize, iconSize, iconSource, toolTipText, onClicked.

From src/qml_gui/controls/CxMenu.qml — Qt Quick Controls `Menu` subclass, supports nested `CxMenu { title: "..."; ... }` submenus (verified at main.qml:133-154 and main.qml:217-227 — already used). Use `MenuSeparator {}` between groups.

From src/qml_gui/Theme.qml — tokens available: Theme.topbarHover, Theme.topbarPressed, Theme.topbarText, Theme.accent, Theme.bgHover, Theme.bgPressed, Theme.textPrimary, Theme.textSecondary, Theme.textDisabled, Theme.fontSizeMD, Theme.spacingLG, Theme.spacingMD, Theme.borderSubtle.

From Plan 02-01 (already merged) — BackendContext exposes:
- `backend.TabPosition.tpHome` (0) through `backend.TabPosition.tpPlaceholder2` (8) — Q_ENUM accessible from QML
- `backend.requestSelectTab(int position)` — Q_INVOKABLE
- signal `backend.tabSelectRequested(int position)` (optional to subscribe; main path is currentPage binding)
- Existing `backend.currentPage` Q_PROPERTY (NOTIFY currentPageChanged) — already wired to StackLayout.currentIndex
- Existing `backend.displayProjectTitle` Q_PROPERTY — bind CenteredTitle to this

From upstream BBLTopbar.cpp:29-41 — CUSTOM_ID enum order for tool buttons (informational, not a contract for QML): ID_LOGO, ID_TOP_FILE_MENU, ID_TOP_DROPDOWN_MENU, ID_TITLE, ID_MODEL_STORE, ID_PUBLISH, ID_CALIB. Current main.qml tool button order matches: Logo / File ▾ / ▾ / Save / Undo / Redo (Calibration button missing — add per TOPBAR-04).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BBLTopbar.qml — full top strip component</name>
  <files>src/qml_gui/BBLTopbar.qml, src/qml_gui/qml.qrc</files>
  <read_first>
    - src/qml_gui/main.qml (FULL — extract titleBar block lines 583-854, menu definitions 115-314, projectTitleLabel 762-774, window controls 819-844, buildWorkflowTabs function 50-58)
    - src/qml_gui/controls/CxIconButton.qml (cxStyle enum, prop names, Chrome vs ChromeDanger)
    - src/qml_gui/controls/CxMenu.qml + CxMenuItem.qml (Menu subclass, nested submenu pattern)
    - src/qml_gui/Theme.qml (available tokens)
    - third_party/OrcaSlicer/src/slic3r/GUI/BBLTopbar.cpp (lines 29-41 — CUSTOM_ID enum; lines 43-94 — CenteredTitle pattern; lines 285-395 — tool button layout)
    - .planning/phases/02-9-page-notebook-bbltopbar/02-CONTEXT.md (locked decisions for [File ▾] / [▾] menu structure, tool button list, side_tools content, window controls)
  </read_first>
  <action>
    Create `src/qml_gui/BBLTopbar.qml` as an `Item` (NOT ApplicationWindow — main.qml owns the window). Root properties:
      - `required property var backend` — bind to root `backend` from main.qml
      - `signal requestSlice()`, `signal requestPrintDialog()`, `signal requestExportDialog()` — BBLTopbar emits these, main.qml wires to sliceTopMenu/printTopMenu popup or EditorViewModel
      - `signal undoRequested()`, `signal redoRequested()` — main.qml wires to preparePage.undoFromTopbar/redoFromTopbar
      - `property var preparePageRef: null` — for undo/redo shortcut dispatch (set from main.qml)

    Root layout: `Rectangle { height: 40; color: "#181818" }` containing a `RowLayout { anchors.fill: parent; spacing: 0 }`.

    **LEFT GROUP (logo, file, dropdown, save, undo, redo, calibration, placeholders)** — port from main.qml:611-713. Order:
      1. Logo button (40x40) — onClicked `backend.requestSelectTab(backend.TabPosition.tpHome)` (NOT setCurrentPage(0) literal — use enum)
      2. TitleBarDivider
      3. File button "文件 ▾" — onClicked `fileMenu.popup()`
      4. Dropdown button "▾" — onClicked `topMenu.popup()`
      5. TitleBarDivider
      6. Save CxIconButton (iconSource device-floppy.svg) — onClicked: `if (!backend.topbarSaveProject()) saveAsRequested()` (emit `signal saveAsRequested()` — main.qml opens saveProjectAsDialog)
      7. Undo CxIconButton (iconSource arrow-back-up.svg) — enabled `backend.currentPage === backend.TabPosition.tp3DEditor`, onClicked `undoRequested()`
      8. Redo CxIconButton (iconSource arrow-forward-up.svg) — same enabled binding, onClicked `redoRequested()`
      9. Calibration CxIconButton (iconSource settings.svg) — NEW per TOPBAR-04, onClicked emit `signal calibrationRequested()` (main.qml wires to backend.setCurrentPage(tpCalibration) — Phase 7 will replace this)
      10. Three placeholder CxIconButtons for Account / ModelStore / Publish — `enabled: false; opacity: 0.4; toolTipText: qsTr("v2.1 实现")` (visible-disabled per CONTEXT.md "占位" decision). Icons: reuse `printer.svg` (Account), `layout-grid.svg` (ModelStore), `send-2.svg` (Publish) — already in qrc.

    **CENTER GROUP (TabBar)** — REPLACE the existing hand-built `Repeater` (main.qml:723-755) with a Qt Quick Controls `TabBar` per CONTEXT.md locked decision D-ARCH-01:
      - `id: navTabBar`
      - `currentIndex: backend.currentPage`
      - `onCurrentIndexChanged: { if (currentIndex !== backend.currentPage && currentIndex >= 0 && currentIndex <= 8) backend.requestSelectTab(currentIndex) }` — guard prevents feedback loop (Pitfall 2 in 02-RESEARCH.md)
      - `Repeater { model: 9-element array; delegate: TabButton { ... } }` with model:
        ```
        [
          { label: qsTr("首页"), pos: backend.TabPosition.tpHome },
          { label: qsTr("准备"), pos: backend.TabPosition.tp3DEditor },
          { label: qsTr("预览"), pos: backend.TabPosition.tpPreview },
          { label: qsTr("设备"), pos: backend.TabPosition.tpDevice },
          { label: qsTr("多设备"), pos: backend.TabPosition.tpMultiDevice },
          { label: qsTr("项目"), pos: backend.TabPosition.tpProject },
          { label: qsTr("校准"), pos: backend.TabPosition.tpCalibration },
          { label: qsTr("占位"), pos: backend.TabPosition.tpPlaceholder1, enabled: false, tooltip: qsTr("v2.1 实现") },
          { label: qsTr("占位"), pos: backend.TabPosition.tpPlaceholder2, enabled: false, tooltip: qsTr("v2.1 实现") }
        ]
        ```
      - TabButton delegate: `text: modelData.label; enabled: !modelData.enabled === false; ToolTip.text: modelData.tooltip || ""; onClicked: { if (backend.currentPage === modelData.pos) return; var token = backend.beginLatency("tab-switch", modelData.label); backend.requestSelectTab(modelData.pos) }` — beginLatency token ends via Connections onCurrentPageChanged (handled in main.qml).
      - Provide custom `contentItem` and `background` for TabButton using Theme tokens (topbarHover, accent, topbarText) to match existing dark theme (Pitfall 7). Active tab: `background.color: Theme.accent; contentItem.color: "#ffffff"; font.bold: true`. Hover: `Theme.topbarHover`. Inactive: transparent background, `Theme.topbarText` text color.

    **RIGHT GROUP (side_tools, title, bell, window controls)** — port from main.qml:759-844 plus new side_tools:
      1. `RowLayout { id: sideTools }` — NEW per ARCH-04. Contains:
         - Slice dropdown CxIconButton (iconSource layers.svg or send-2.svg) — `onClicked: sliceRequested()` (main.qml wires to sliceTopMenu.popup())
         - Print dropdown CxIconButton (iconSource printer.svg) — `onClicked: printRequested()` (main.qml wires to printTopMenu.popup())
         - FilamentGroupPopup placeholder CxIconButton (iconSource box.svg) — `enabled: false; opacity: 0.4; toolTipText: qsTr("多耗材分组切片 (v2.1 实现)")` per CONTEXT.md "占位 only" rule. Add `// TODO(v2.1): implement FilamentGroupPopup` comment. NO Q_INVOKABLE calls, NO state (Pitfall 5).
      2. Stretch spacer `Item { Layout.fillWidth: true }`
      3. CenteredTitle `Text { id: projectTitleLabel; text: backend.displayProjectTitle; horizontalAlignment: Text.AlignHCenter; elide: Text.ElideRight; Layout.maximumWidth: Math.max(160, (parent.width / 2) - 500); Layout.minimumWidth: 160 }` (port from main.qml:762-774)
      4. TitleBarDivider
      5. Bell button (notification center) — port from main.qml:779-817 unchanged, but the NotificationCenter Popup stays in main.qml (BBLTopbar emits `signal bellClicked()` and main.qml owns the popup). Reason: NotificationCenter is a complex component, keep its Popup lifecycle in main.qml.
      6. Minimize CxIconButton — `onClicked: windowMinimizeRequested()` (main.qml wires to showMinimized)
      7. Maximize/Restore CxIconButton — dynamic iconSource based on `windowVisibility === Window.Maximized`, `onClicked: windowMaximizeRequested()`
      8. Close CxIconButton (cxStyle ChromeDanger) — `onClicked: windowCloseRequested()`

    **MenuBar** for TOPBAR-07 (macOS conditional): at top of BBLTopbar.qml root, add a Qt Quick Controls `MenuBar` wrapped in a Loader whose `active: Qt.platform.os === "osx"`. Populate with `Menu` items mirroring [File ▾] and [▾] structure (can reuse same CxMenu definitions via `Menu { title: "File"; ... }`). On Windows/Linux the Loader stays inactive (no system menubar rendered). Add `// TODO(cross-platform): full macOS MenuBar content validated when cross-platform build supported` comment.

    Move the CxMenu definitions (fileMenu, topMenu, sliceTopMenu, printTopMenu) INTO BBLTopbar.qml as child items so the popups are local. Update [File ▾] menu structure per CONTEXT.md locked order to match upstream exactly:
      - New (新建项目) → emit `newProjectRequested()`
      - Open (打开项目...) → emit `openProjectRequested()`
      - Recent submenu — port Instantiator pattern from main.qml:133-154
      - Save (保存项目) → `if (!backend.topbarSaveProject()) saveAsRequested()`
      - Save As (项目另存为...) → emit `saveAsRequested()`
      - Import submenu (NEW per TOPBAR-02): Import 3MF / Import STL / Import OBJ / Import STEP / Import AMF — each emits `importModelRequested(filter)` (main.qml opens openModelDialog with matching filter, or directly calls backend.topbarImportModel after FileDialog accept)
      - Export submenu (NEW per TOPBAR-02): Export G-code → emit `exportGcodeRequested()`; Export 3MF → emit `exportProjectRequested()`; Export Model → emit `exportModelRequested()`
      - Quit (退出) → `Qt.quit()`

    Update [▾] menu Calibration submenu per CONTEXT.md: extend to 9 entries matching upstream CALIB-01 (Temperature / Max flowrate / Pressure advance / Flow ratio / Retraction / Cornering / Input Shaping Freq / Input Shaping Damp / VFA) — each `enabled: false; onTriggered: {}` with comment `// Phase 7: wire to actual calibration dialog`. Plus a final "Calibration Guide" entry `enabled: false`. All have tooltip "Phase 7 实现".

    Use `qsTr()` for EVERY user-visible string. All menu item labels must be qsTr-wrapped.

    **qml.qrc**: add `<file>BBLTopbar.qml</file>` after the `<file>main.qml</file>` line (line 4 area).
  </action>
  <verify>
    <automated>grep -c "BBLTopbar.qml" src/qml_gui/qml.qrc && grep -cE "backend\.TabPosition\.tp(Home|3DEditor|Preview|Device|MultiDevice|Project|Calibration|Placeholder1|Placeholder2)" src/qml_gui/BBLTopbar.qml</automated>
  </verify>
  <acceptance_criteria>
    - File `src/qml_gui/BBLTopbar.qml` exists
    - `wc -l src/qml_gui/BBLTopbar.qml` returns >= 250
    - `grep -c "BBLTopbar.qml" src/qml_gui/qml.qrc` returns 1
    - `grep -c "TabBar" src/qml_gui/BBLTopbar.qml` returns at least 1
    - `grep -c "backend.TabPosition.tpHome\|backend.TabPosition.tp3DEditor\|backend.TabPosition.tpPreview\|backend.TabPosition.tpDevice\|backend.TabPosition.tpMultiDevice\|backend.TabPosition.tpProject\|backend.TabPosition.tpCalibration\|backend.TabPosition.tpPlaceholder1\|backend.TabPosition.tpPlaceholder2" src/qml_gui/BBLTopbar.qml` returns >= 9 (each enum value referenced at least once)
    - `grep -c "backend.requestSelectTab" src/qml_gui/BBLTopbar.qml` returns >= 1
    - `grep -c "v2.1 实现" src/qml_gui/BBLTopbar.qml` returns >= 4 (2 placeholder tabs + Account/ModelStore/Publish buttons + FilamentGroupPopup placeholder = at least 6; allow >= 4)
    - `grep -c "Export G-code\|Export 3MF\|Export Model" src/qml_gui/BBLTopbar.qml` returns 3
    - `grep -c "Import 3MF\|Import STL\|Import OBJ\|Import STEP\|Import AMF" src/qml_gui/BBLTopbar.qml` returns 5
    - `grep -c "backend.displayProjectTitle" src/qml_gui/BBLTopbar.qml` returns >= 1
    - `grep -c "Qt.platform.os === \"osx\"" src/qml_gui/BBLTopbar.qml` returns 1 (TOPBAR-07 conditional branch)
    - `grep -cE "setCurrentPage\\([0-9]+\\)" src/qml_gui/BBLTopbar.qml` returns 0 (no hardcoded integer page indices — Pitfall 1 guard)
  </acceptance_criteria>
  <done>BBLTopbar.qml exists with full top strip layout; qml.qrc registers it; all 9 TabPosition enum values referenced by name (no integer literals); [File ▾] complete with Import/Export submenus; [▾] Calibration submenu extended to 9+1 entries placeholder; side_tools region present with 3 buttons (2 functional + 1 FilamentGroupPopup disabled placeholder); window controls emit signals for main.qml to wire; macOS MenuBar Loader reserved but inactive on Windows.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite main.qml to host BBLTopbar + 9-page StackLayout</name>
  <files>src/qml_gui/main.qml</files>
  <read_first>
    - src/qml_gui/main.qml (FULL — to know what to delete/move/keep)
    - src/qml_gui/BBLTopbar.qml (from Task 1 — to know the signal contract)
    - src/qml_gui/BackendContext.h (currentPage default is 1; TabPosition.tpX values)
    - .planning/phases/02-9-page-notebook-bbltopbar/02-CONTEXT.md (9-page order lock; latency tracking migration rule)
  </read_first>
  <action>
    Replace main.qml body to delegate the title bar to BBLTopbar.qml and collapse StackLayout from 12 to 9 pages.

    **Keep unchanged** in main.qml:
    - `ApplicationWindow` root (id: root, width/height/title/color/flags/minimumWidth/minimumHeight)
    - Frame properties: `resizeMargin`, `frameMargin`, `frameRadius` (lines 22-24)
    - `accentColor` / `topbarHover` / `topbarPressed` / `topbarText` color properties (lines 30-33) — keep as fallback, but BBLTopbar uses Theme tokens
    - All `FileDialog` blocks (openModelDialog, openProjectDialog, saveProjectAsDialog — lines 80-113)
    - All `Dialog` blocks (shortcutDialog, aboutDialog, newProjectDialog — lines 317-493)
    - All `Shortcut` blocks (lines 495-551)
    - `compareReferenceSource` property (lines 553-559) — but update the index literals: `backend.currentPage === 1` → `backend.currentPage === backend.TabPosition.tp3DEditor`, `=== 2` → `=== tpPreview`, `=== 3` → `=== tpDevice`. The reference PNG mapping itself stays.
    - `shell` Rectangle (lines 561-577)
    - `ErrorBanner`, `StatusBar`, `ErrorToast` (lines 857, 944-947, 951) — keep as-is
    - All `Connections { target: backend ... }` blocks for dialog requests (lines 963-979)
    - All bottom dialog instantiations (ConfigWizardDialog, BedShapeDialog, EditGCodeDialog, AMSSettingsDialog, FirmwareDialog, SpeedLimitDialog, WipeTowerDialog, PrintHostDialog, PluginManagerDialog, EnableLiteModeDialog — lines 955-1028)
    - Resize MouseArea border handlers (lines 1035-1145)
    - `Component.onCompleted` first-run ConfigWizard logic (lines 1029-1033)

    **DELETE** from main.qml:
    - `pagePrepare`/`pagePreview`/`pageDevice`/`pageOnline` integer constants (lines 26-29) — replaced by `backend.TabPosition.tpX`
    - `workflowTabs` property + `buildWorkflowTabs()` function + `hiddenTabs` (lines 35-58) — BBLTopbar owns the tab model now
    - `pendingSwitchToken` / `pendingSwitchTargetPage` properties (lines 37-38) — replaced by Connections onCurrentPageChanged
    - `toggleTabVisibility()` function (lines 40-48)
    - `TitleBarDivider` inline component (lines 60-65) — move into BBLTopbar.qml
    - The entire `titleBar` Rectangle block (lines 583-854) — BBLTopbar.qml owns this now
    - All CxMenu definitions in main.qml (fileMenu, topMenu, sliceTopMenu, printTopMenu — lines 115-314) — moved into BBLTopbar.qml
    - `onFrameSwapped` latency-end hook (lines 72-78) — replace with Connections onCurrentPageChanged

    **REPLACE titleBar** with:
    ```
    BBLTopbar {
        id: bblTopbar
        backend: backend
        preparePageRef: preparePage
        Layout.fillWidth: true
        Layout.preferredHeight: 40
        windowVisibility: root.visibility
        onNewProjectRequested: newProjectDialog.open()
        onOpenProjectRequested: openProjectDialog.open()
        onSaveAsRequested: saveProjectAsDialog.open()
        onImportModelRequested: function(filter) {
            openModelDialog.nameFilters = [filter, qsTr("所有文件 (*)")]
            openModelDialog.open()
        }
        onExportGcodeRequested: preparePage.openExportDialog()
        onExportProjectRequested: { /* TODO: backend.topbarExport3MF */ }
        onExportModelRequested: { /* TODO: backend.topbarExportModel */ }
        onUndoRequested: if (backend.currentPage === backend.TabPosition.tp3DEditor) preparePage.undoFromTopbar()
        onRedoRequested: if (backend.currentPage === backend.TabPosition.tp3DEditor) preparePage.redoFromTopbar()
        onCalibrationRequested: backend.requestSelectTab(backend.TabPosition.tpCalibration)
        onSliceRequested: sliceTopMenuExternal.popup()  // keep slice/print menus as separate top-level items
        onPrintRequested: printTopMenuExternal.popup()
        onBellClicked: notificationCenterPopup.open()
        onWindowMinimizeRequested: root.showMinimized()
        onWindowMaximizeRequested: root.visibility === Window.Maximized ? root.showNormal() : root.showMaximized()
        onWindowCloseRequested: Qt.quit()
        onTitleBarDragStarted: if (root.visibility !== Window.Maximized) root.startSystemMove()
        onTitleBarDoubleClicked: root.visibility === Window.Maximized ? root.showNormal() : root.showMaximized()
    }
    ```
    If Task 1 placed slice/print menus inside BBLTopbar.qml, the onSlice/onPrint handlers can instead emit the popup directly from BBLTopbar. Verify with Task 1's final API.

    For the startSystemMove / double-click resize on title bar: BBLTopbar must emit `titleBarDragStarted` and `titleBarDoubleClicked` signals (add to BBLTopbar.qml if not present).

    **REPLACE StackLayout** from 12 pages to 9 pages per TabPosition enum (0..8):
      - Index 0 (tpHome): HomePage (keep Loader pattern)
      - Index 1 (tp3DEditor): PreparePage { id: preparePage; ... } — keep as eager (not Loader) for undo/redo access
      - Index 2 (tpPreview): PreviewPage { ... } — eager
      - Index 3 (tpDevice): MonitorPage (was page index 3 already — keep MonitorPage component, NOT DeviceListPage which was at old index 7)
      - Index 4 (tpMultiDevice): MultiMachinePage (use Loader, lazy) — was old index 10
      - Index 5 (tpProject): ProjectPage (Loader)
      - Index 6 (tpCalibration): CalibrationPage (Loader)
      - Index 7 (tpPlaceholder1): a placeholder `Item { visible: false }` or simple "v2.1 实现" label Rectangle
      - Index 8 (tpPlaceholder2): same placeholder treatment
    - DELETE the old Pages 7 (DeviceListPage), 8 (PreferencesPage), 9 (ModelMallPage), 11 (SettingsPage) — Preferences/Settings/ModelMall/DeviceList move out of StackLayout; they will be opened via Dialog or in Phase 3+. For v2.0 keep ModelMallPage, PreferencesPage, SettingsPage, DeviceListPage components on disk but unreferenced from StackLayout (do not delete the .qml files — they're still used by future phases).
    - StackLayout `currentIndex: backend.currentPage` (unchanged binding).

    **LATENCY TRACKING** migration (Pitfall 3): replace `onFrameSwapped` hook with:
    ```
    Connections {
        target: backend
        function onCurrentPageChanged() {
            // End any in-flight tab-switch latency token here.
            // BackendContext tracks the pending token internally if introduced later;
            // for v2.0 we re-derive from BBLTopbar's beginLatency return value via property.
        }
    }
    ```
    Add a `property int activeTabSwitchToken: -1` on root, set it in BBLTopbar's TabButton onClicked (beginLatency return), and end it in onCurrentPageChanged: `if (root.activeTabSwitchToken >= 0) { backend.endLatency(root.activeTabSwitchToken); root.activeTabSwitchToken = -1 }`. Since BBLTopbar internally calls beginLatency, expose the token via `bblTopbar.lastTabSwitchToken` read property.

    **NotificationCenter Popup**: keep in main.qml (BBLTopbar emits `bellClicked`, main.qml opens the popup). Add the Popup + NotificationCenter block (ported from current main.qml:802-816) as a top-level child of root.

    **StatusBar text** (line 946): change `"页面 " + (backend.currentPage + 1)` to `"页面 " + (backend.currentPage + 1) + " / 9"` to reflect the new 9-page count.

    Use `qsTr()` for all new strings.
  </action>
  <verify>
    <automated>grep -cE "setCurrentPage\\([0-9]+\\)" src/qml_gui/main.qml && grep -cE "currentPage === [0-9]+" src/qml_gui/main.qml && grep -c "BBLTopbar" src/qml_gui/main.qml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BBLTopbar" src/qml_gui/main.qml` returns >= 1
    - `grep -cE "setCurrentPage\\([0-9]+\\)" src/qml_gui/main.qml` returns 0 (no hardcoded integer page switches)
    - `grep -cE "currentPage === [0-9]+" src/qml_gui/main.qml` returns 0 (no hardcoded page index comparisons)
    - `grep -c "backend.TabPosition.tp3DEditor\|backend.TabPosition.tpDevice\|backend.TabPosition.tpPreview" src/qml_gui/main.qml` returns >= 3
    - `grep -c "buildWorkflowTabs\|workflowTabs\|pendingSwitchToken\|pendingSwitchTargetPage\|toggleTabVisibility\|hiddenTabs" src/qml_gui/main.qml` returns 0 (all removed)
    - `grep -cE "pagePrepare:|pagePreview:|pageDevice:|pageOnline:" src/qml_gui/main.qml` returns 0 (legacy integer constants removed)
    - StackLayout has exactly 9 children (verify by counting top-level Loader/Page children inside the StackLayout block)
    - `wc -l src/qml_gui/main.qml` returns <= 600 (down from 1147)
    - `grep -c "Layout.preferredHeight: 40" src/qml_gui/main.qml` in BBLTopbar invocation returns 1 (top bar height)
    - `cmake --build build --config Release --target OWzxSlicer` exits 0
    - Launching build/OWzxSlicer.exe produces a startup_diagnostics.log with NO lines containing "QML" + "Error" or "TypeError"
  </acceptance_criteria>
  <done>main.qml delegates top strip to BBLTopbar.qml; StackLayout has 9 pages matching TabPosition 0..8; all hardcoded page index literals replaced with backend.TabPosition.tpX; legacy workflowTabs/pendingSwitch* removed; latency tracking migrated to Connections onCurrentPageChanged; app launches without QML errors.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate i18n + run full smoke verification</name>
  <files>i18n/zh_CN.ts, i18n/en.ts</files>
  <read_first>
    - i18n/zh_CN.ts (current translation key structure — note context `<name>` tags and `<message>` blocks)
    - CMakeLists.txt (locate the `qt6_add_translation` or `lupdate` invocation — confirm where .ts files are listed)
    - .planning/phases/02-9-page-notebook-bbltopbar/02-CONTEXT.md (国际化 decision: keys like menu.file.new, menu.edit.undo)
  </read_first>
  <action>
    Run `lupdate` to extract new qsTr() strings from BBLTopbar.qml and updated main.qml into i18n/zh_CN.ts (and en.ts). Exact command:
    ```
    lupdate src/qml_gui -recursive -ts i18n/zh_CN.ts i18n/en.ts
    ```
    (Adjust path if Qt6 lupdate is invoked differently — check CMakeLists.txt for the existing pattern; if the project uses `qt6_add_translation(TS_FILES ...)` then the CMake configure step regenerates automatically and you only need to verify the new keys landed in the .ts file.)

    After regeneration, open i18n/zh_CN.ts and verify the new keys are present (with `type="unfinished"` markers expected for new strings). Required new keys (translated to Chinese for zh_CN):
    - "多设备" (Multi-device tab label)
    - "占位" (Placeholder tab labels)
    - "v2.1 实现" (tooltip for placeholder tabs + FilamentGroupPopup + Account/ModelStore/Publish)
    - "Export G-code" / "Export 3MF" / "Export Model"
    - "Import 3MF" / "Import STL" / "Import OBJ" / "Import STEP" / "Import AMF"
    - 9 Calibration submenu entries (Temperature / Max flowrate / Pressure advance / Flow ratio / Retraction / Cornering / Input Shaping Freq / Input Shaping Damp / VFA / Calibration Guide)
    - "Phase 7 实现" tooltip for Calibration entries

    Fill in zh_CN translations for these new keys (provide Chinese translations directly in the .ts file as `<translation>` element content). en.ts can keep English source as translation (or leave unfinished — English is the source language).

    Run the full verification chain (the canonical build script per build-rules.md):
    ```
    powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1
    ```
    This runs cmake configure + ninja build + smoke test (which exercises app launch, QML load, basic page navigation). Confirm:
    - Build exit code 0
    - OWzxSlicer.exe launches (process starts, no immediate crash)
    - startup_diagnostics.log contains no QML errors
    - 9 tabs are visible and clickable in the launched app (verify via screenshot if smoke_test.ps1 captures one)
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "多设备" i18n/zh_CN.ts` returns >= 1
    - `grep -c "Export G-code" i18n/zh_CN.ts` returns >= 1
    - `grep -c "Pressure advance" i18n/zh_CN.ts` returns >= 1 (one of the 9 calibration entries)
    - `grep -c "v2.1 实现" i18n/zh_CN.ts` returns >= 1
    - `cmake --build build --config Release --target OWzxSlicer` exits 0
    - `cmake --build build --config Release --target ViewModelSmokeTests` exits 0
    - `ctest --test-dir build -R "testTabPositionEnumValues|testRequestSelectTabSignal|testRequestSelectTabOutOfRange" -V` shows 3/3 PASS (Plan 02-01 tests still pass)
    - `scripts/auto_verify_with_vcvars.ps1` exits 0 (canonical full verification)
    - `build/startup_diagnostics.log` (after running OWzxSlicer.exe) contains ZERO lines matching `qrc:.*Error|TypeError|ReferenceError`
  </acceptance_criteria>
  <done>i18n .ts files contain all new qsTr keys with Chinese translations for zh_CN; full smoke verification passes via the canonical build script; smoke test green; no QML errors in startup_diagnostics.log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML → C++ (menu action dispatch) | Menu items call backend.topbar* Q_INVOKABLEs — these already validate file paths |
| FileDialog → backend.topbarImportModel | User-selected file path crosses trust boundary; existing topbarImportModel sanitizes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | FilamentGroupPopup placeholder | mitigate | Render as disabled CxIconButton with NO Q_INVOKABLE call and `// TODO(v2.1)` comment — prevents partial logic leaking into Phase 5 (Pitfall 5 in 02-RESEARCH.md) |
| T-02-04 | Information Disclosure | CenteredTitle project path display | accept | displayProjectTitle already extracts basename from projectPath; no full-path disclosure |
| T-02-05 | Tampering | Import submenu filter injection | accept | Each Import X submenu item uses a static nameFilter; no user-controlled filter input |
| T-02-06 | Denial of Service | TabBar currentIndex feedback loop | mitigate | Guard `if (currentIndex !== backend.currentPage && currentIndex >= 0 && currentIndex <= 8)` before requestSelectTab emit (Pitfall 2) |
| T-02-07 | Elevation of Privilege | macOS MenuBar Loader | accept | Loader active only when `Qt.platform.os === "osx"`; on Windows build the branch is inactive |
</threat_model>

<verification>
- `scripts/auto_verify_with_vcvars.ps1` exits 0 (canonical full verification per build-rules.md)
- `cmake --build build --config Release --target ViewModelSmokeTests && ctest --test-dir build -R "testTabPositionEnumValues|testRequestSelectTabSignal|testRequestSelectTabOutOfRange"` shows 3/3 PASS
- `build/startup_diagnostics.log` has zero QML errors after launch
- Visual smoke test: 9 tabs visible, [File ▾] expands with Import/Export submenus, [▾] expands with 9 Calibration entries, placeholder tabs disabled, side_tools visible with 3 buttons
- All 9 TabPosition enum values referenced by name (no integer literals) — verify via `grep -E "setCurrentPage\([0-9]+\)" src/qml_gui/` returns empty
</verification>

<success_criteria>
- main.qml top-level hosts BBLTopbar.qml + 9-page StackLayout matching TabPosition 0..8
- [File ▾] menu has New / Open / Recent / Save / Save As / Import(3MF/STL/OBJ/STEP/AMF) / Export(G-code/3MF/Model) / Quit in upstream order
- [▾] menu has Edit / View / Preferences / Calibration(9 disabled entries + Guide) / Help
- Tool buttons Save / Undo / Redo / Calibration + 3 disabled placeholders (Account/ModelStore/Publish)
- side_tools region: Slice ▾ + Print ▾ + FilamentGroupPopup disabled placeholder
- CenteredTitle centered with elide ElideRight
- Window controls Min/Max/Close using CxIconButton Chrome/ChromeDanger
- macOS MenuBar Loader reserved (inactive on Windows)
- Zero hardcoded page index literals in QML
- Full smoke verification green; no QML errors
- ARCH-01, ARCH-04, TOPBAR-01..07 all observable from running app
</success_criteria>

<output>
Create `.planning/phases/02-9-page-notebook-bbltopbar/02-02-SUMMARY.md` when done. Cover: BBLTopbar.qml signal contract (so Phase 3 Plater work can subscribe to tabSelectRequested), the 9-page StackLayout index → TabPosition mapping, latency tracking migration, i18n key inventory, smoke test results.
</output>
