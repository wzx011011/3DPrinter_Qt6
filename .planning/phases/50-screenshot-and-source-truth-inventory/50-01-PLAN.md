---
phase: 50
plan: 01
status: pending
wave: 1
depends_on: []
files_modified:
  - docs/v3.6-ui-inventory.md
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05]
---

# Plan 50-01: Canonical Screenshot & Source-Truth UI Inventory

<objective>
Create the single long-lived canonical inventory `docs/v3.6-ui-inventory.md` that maps every screenshot-visible region across the four supplied screenshots to the frozen 9-column schema (region_id, region_name, visible_controls, qt_target, upstream_source, status, verification, modify_or_replace, cleanup), with per-cluster upstream coverage assertions and a greppable aggregate cleanup checklist. This doc is the single source of truth consumed by every downstream v3.6 phase (51-57) and verified by Phase 58 (VERIFY-01). No code, route, resource, registration, or test file is touched.
</objective>

<scope_fence>
DOCUMENTATION ONLY. The only file created is `docs/v3.6-ui-inventory.md`.
- Do NOT modify any `.qml`, `.cpp`, `.h`, `CMakeLists.txt`, `qml.qrc`, `qmldir`, route, type-registration, or test file.
- Do NOT create `50-INVENTORY.md` here (that is Plan 50-02, which depends on this one).
- Do NOT modify seed docs (`docs/源码真值功能矩阵.md` etc.); read them for seeding only.
</scope_fence>

<tasks>

1.
<action>
Create `docs/v3.6-ui-inventory.md` with the exact front/header sections from 50-RESEARCH §4, in this order:
- Title: `# v3.6 UI Inventory — Screenshot & Source-Truth Contract`
- A living-document banner line: `> Living document. Statuses updated by Phases 51–57. Region IDs are immutable.`
- `## 0. How to read this document` — list the FROZEN 7-term status vocabulary (`Real` | `Hybrid` | `Mock` | `Blocked` | `Placeholder` | `Superseded` | `Missing`), the FROZEN 6-term verification vocabulary (`automated-test` | `deterministic-harness` | `manual-visual` | `manual-uat-checklist` | `build-only` | `upstream-parity-audit`), the exact 9-column schema in fixed order, the region-ID convention (ASCII-only, page-prefixed, stable, format `^[A-Z]+-[A-Z0-9]+$`), and the upstream reference convention (file-level + symbol, NO line numbers; blank upstream must be `none-mapped (Owzx-only)`).
- `## 1. Upstream & Qt source baselines` — cite upstream `third_party/OrcaSlicer @ v7.0.1` lock, Qt targets `src/qml_gui/**` + `src/core/**`, screenshot visual truth `shotScreen/*.png`. List the four screenshot filenames verbatim.

Use English-ASCII comments/headers only. Chinese is permitted ONLY inside region display names (in parentheses) and the four screenshot filenames.
</action>
<read_first>
- shotScreen/准备页.png (read pixels/layout via mcp__4_5v_mcp__analyze_image; if the large image is rejected by the vision pipeline, fall back to structural analysis of PreparePage.qml/LeftSidebar.qml as triangulation per 50-RESEARCH §0)
- shotScreen/预览页.png (same vision-or-structural fallback; PreviewPage.qml is the high-confidence 1:1 region reference)
- shotScreen/打印机参数设置页.png (read via mcp__4_5v_mcp__analyze_image — settings dialogs were analyzed successfully in research)
- shotScreen/材料参数设置页.png (read via mcp__4_5v_mcp__analyze_image)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md (FROZEN schema, vocab, scope, Claude's-discretion clauses)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md (proposed region IDs §3, doc structure §4, seed-data shortcuts §10, ambiguities §9)
- .planning/REQUIREMENTS.md (status terms §"Status Terms", INV-01..INV-05 definitions)
- src/qml_gui/pages/PreparePage.qml (confirm region layout / shell + 3-pane + gizmo panels)
- src/qml_gui/pages/PreviewPage.qml (the 1:1 region layout reference for PREV-* rows)
</read_first>
<acceptance_criteria>
- docs/v3.6-ui-inventory.md exists and is non-empty.
- `grep -c "^## 0. How to read this document" docs/v3.6-ui-inventory.md` returns 1.
- `grep -c "^## 1. Upstream & Qt source baselines" docs/v3.6-ui-inventory.md` returns 1.
- The file contains all four screenshot filenames verbatim: `grep -c "准备页.png\|预览页.png\|打印机参数设置页.png\|材料参数设置页.png" docs/v3.6-ui-inventory.md` returns 4.
- The living-document banner line `> Living document. Statuses updated by Phases 51–57. Region IDs are immutable.` is present exactly once.
- All seven status tokens appear in §0: `grep -c "Missing" docs/v3.6-ui-inventory.md` returns >= 1.
</acceptance_criteria>

2.
<action>
Write section `## 2. Prepare page — 准备页.png` with a GitHub-flavored markdown table using the FIXED 9-column header (identical in every region table):
`| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |`

Produce the PREP-* region rows. Use the region IDs proposed in 50-RESEARCH §3.1 (`PREP-TOP`, `PREP-SIDEBAR`, `PREP-OBJLIST`, `PREP-VIEWPORT`, `PREP-VTOOLBAR`, `PREP-GIZMOFLOAT`, `PREP-PLATEBAR`, `PREP-SLICESTATUS`, `PREP-VIEWOPTS`). Target 6–12 rows (the 9 listed IDs are within band; you MAY merge/split within band but keep IDs ASCII and stable). NOTE: the single-dash form `PREP-GIZMOFLOAT` is mandated because the region-ID regex `^[A-Z]+-[A-Z0-9]+$` allows only ONE dash segment after the prefix; do NOT use `PREP-GIZMO-FLOAT`.

CORRECTION to apply: 50-RESEARCH proposed `PREP-ARRANGE/VIEW-OPTS` with a `/` — this violates the required `^[A-Z]+-[A-Z0-9]+$` pattern. Use `PREP-VIEWOPTS` instead (no slash, ASCII + digits only). Flag `PREP-CTXMENU` as an optional region: include it ONLY if a context menu is actually open in the screenshot; otherwise omit (keeps count within band).

Each row's `upstream_source` cell MUST cite at least one of the INV-02 cluster files (`Plater.*`, `GLCanvas3D.*`, `GUI_ObjectList.*`, `GUI_ObjectSettings.*`, `Gizmos/*`) as file-level + optional symbol. Fill `qt_target` from §1.2 of 50-RESEARCH and `docs/项目结构.md`. Every row must have all 9 cells populated — no empty cells. Use honest `status` values (e.g. `PREP-GIZMOFLOAT` likely `Placeholder` or `Hybrid`; gizmo entry points depending on OpenVDB are `Blocked`).

End the section with a greppable coverage anchor comment on its own line:
`<!-- INV-02 coverage: Plater.* GLCanvas3D.* GUI_ObjectList.* GUI_ObjectSettings.* Gizmos/* -->`
listing all 5 cluster globs (one check each, marked present once all 5 are referenced across the section's rows).
</action>
<read_first>
- shotScreen/准备页.png (vision or structural fallback)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §3.1, §10 (region table + upstream seed values)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md (status/verification vocab, modify-vs-replace rules)
- docs/顶部工具栏验收清单.md (seeds PREP-TOP row)
- docs/源码真值功能矩阵.md (seeds upstream_source — but REWRITE CrealityPrint paths to OrcaSlicer per research §0)
- docs/项目结构.md (seeds qt_target)
- src/qml_gui/pages/PreparePage.qml (region boundaries + gizmo floating panels)
- src/qml_gui/panels/LeftSidebar.qml (PREP-SIDEBAR structure: printer/filament/process)
- src/qml_gui/panels/ObjectList.qml (PREP-OBJLIST)
- src/qml_gui/Renderer/GLViewport.h (PREP-VIEWPORT qt_target)
- src/qml_gui/components/GLToolbars.qml (PREP-VTOOLBAR)
- third_party/OrcaSlicer/src/slic3r/GUI/Plater.hpp (Sidebar::*, plate, menu symbols)
- third_party/OrcaSlicer/src/slic3r/GUI/GLCanvas3D.hpp (viewport + view toolbar)
- third_party/OrcaSlicer/src/slic3r/GUI/GUI_ObjectList.hpp (object list/obj settings)
- third_party/OrcaSlicer/src/slic3r/GUI/Gizmos/ (directory listing — gizmo entry points)
</read_first>
<acceptance_criteria>
- `grep -c "^## 2. Prepare page" docs/v3.6-ui-inventory.md` returns 1.
- Prepare rows count between 6 and 12: rows under §2 matching `^| PREP-` number between 6 and 12.
- Every PREP- region_id matches `^[A-Z]+-[A-Z0-9]+$` (ASCII, single dash segment only, no slash): run `grep -oE "^\| PREP-[A-Z0-9-]+" docs/v3.6-ui-inventory.md | sed 's/^| //' | grep -vE "^PREP-[A-Z0-9]+$"` — it MUST return nothing (any surviving line is an invalid multi-dash or non-conforming ID). This catches both slash IDs and multi-dash IDs like the forbidden `PREP-GIZMO-FLOAT`.
- No forbidden slash ID: `grep -c "PREP-ARRANGE/VIEW-OPTS" docs/v3.6-ui-inventory.md` returns 0.
- No forbidden multi-dash gizmo ID: `grep -c "PREP-GIZMO-FLOAT" docs/v3.6-ui-inventory.md` returns 0.
- INV-02 coverage anchor present: `grep -c "<!-- INV-02 coverage" docs/v3.6-ui-inventory.md` returns >= 1.
- The coverage anchor line contains all five cluster globs: `Plater.`, `GLCanvas3D.`, `GUI_ObjectList.`, `GUI_ObjectSettings.`, `Gizmos/`.
- No empty cells: `grep -nE "^\| PREP-" docs/v3.6-ui-inventory.md | grep -E '\|\|'` returns nothing (no adjacent empty cells).
- Every PREP- row `upstream_source` cell references at least one INV-02 cluster file.
</acceptance_criteria>

3.
<action>
Write section `## 3. Preview page — 预览页.png` with the same fixed 9-column header, then PREV-* rows using IDs from 50-RESEARCH §3.2 (`PREV-TOP`, `PREV-LEFT`, `PREV-VIEWPORT`, `PREV-VSLIDER`, `PREV-MSLIDER`, `PREV-RIGHT`, `PREV-LEGEND`, `PREV-STATS`, `PREV-TOOLTIP`). 9 rows — within band.

Each `upstream_source` MUST cite at least one INV-03 cluster file (`GUI_Preview.*`, `GCodeViewer.*`, `GLCanvas3D.*`, `libslic3r/GCode/*`). Fill `qt_target` from `PreviewPage.qml` confirmed 1:1 region structure (LayerSlider.qml, MoveSlider.qml, Legend.qml, StatsPanel.qml, ToolPositionTooltip.qml, Renderer/GLViewport). Classify `PREV-LEFT` plate-thumbnail sub-area honestly (`Placeholder` or `Missing` if no Qt target yet per research §3.2 note). All 9 cells populated per row.

End with coverage anchor:
`<!-- INV-03 coverage: GUI_Preview.* GCodeViewer.* GLCanvas3D.* libslic3r/GCode/* -->`
</action>
<read_first>
- shotScreen/预览页.png (vision or structural fallback)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §3.2, §10
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md
- src/qml_gui/pages/PreviewPage.qml (THE 1:1 region layout reference — high confidence)
- src/qml_gui/components/LayerSlider.qml (PREV-VSLIDER)
- src/qml_gui/components/MoveSlider.qml (PREV-MSLIDER)
- src/qml_gui/components/Legend.qml (PREV-LEGEND)
- src/qml_gui/components/StatsPanel.qml (PREV-STATS)
- src/qml_gui/components/ToolPositionTooltip.qml (PREV-TOOLTIP)
- src/qml_gui/Renderer/GCodeRenderer.h (PREV-VIEWPORT G-code render)
- third_party/OrcaSlicer/src/slic3r/GUI/GUI_Preview.hpp (Preview layout + IMSlider)
- third_party/OrcaSlicer/src/slic3r/GUI/GCodeViewer.hpp (legend, stats, marker)
- third_party/OrcaSlicer/src/libslic3r/GCode/ (directory listing — GCodeProcessor etc.)
</read_first>
<acceptance_criteria>
- `grep -c "^## 3. Preview page" docs/v3.6-ui-inventory.md` returns 1.
- Preview rows count between 6 and 12: rows under §3 matching `^| PREV-` number between 6 and 12.
- Every PREV- region_id matches `^[A-Z]+-[A-Z0-9]+$`.
- INV-03 coverage anchor present and contains all four cluster globs: `GUI_Preview.`, `GCodeViewer.`, `GLCanvas3D.`, `libslic3r/GCode/`.
- At least one row uses status `Placeholder` or `Missing` (honest classification of the plate-thumbnail gap): `grep -cE "^\| PREV-LEFT .*(Placeholder|Missing)" docs/v3.6-ui-inventory.md` returns >= 1, OR the executor documents why plate-thumbnail is fully present.
- Every PREV- row `upstream_source` references at least one INV-03 cluster file.
</acceptance_criteria>

4.
<action>
Write section `## 4. Printer settings — 打印机参数设置页.png` with a one-line "Settings dialog shell (shared with §5)" note, then the fixed 9-column header, then SETPRINT-* rows from 50-RESEARCH §3.3 (`SETPRINT-SHELL`, `SETPRINT-PRESETBAR`, `SETPRINT-TABS`, `SETPRINT-GROUPNAV`, `SETPRINT-OPTIONS`, `SETPRINT-SEARCH`, `SETPRINT-FOOTER`, `SETPRINT-DIRTY`). 8 rows — within band.

Per research §3.4 shared-shell note: SETPRINT-* and SETMAT-* intentionally share qt_target/upstream_source/cleanup strings — do NOT deduplicate the rows (INV-01 wants every visible region per screenshot), but add a one-line note that the duplication is intentional. Fill `qt_target` honestly: the main independent settings dialog shell is likely `Missing` (current `SettingsPage.qml`/`ConfigPage.qml` is an embedded page, not the dialog form) — use status `Missing` where no Qt target exists; `status` values must reflect current truth. Each `upstream_source` MUST cite at least one INV-04 cluster file (`Tab.*`, `PresetComboBoxes.*`, `ConfigManipulation.*`, `UnsavedChangesDialog.*`, `CreatePresetsDialog.*`, `PrintConfig.*`, `Preset.*`, `PresetBundle.*`).

End with coverage anchor:
`<!-- INV-04 coverage: Tab.* PresetComboBoxes.* ConfigManipulation.* UnsavedChangesDialog.* CreatePresetsDialog.* PrintConfig.* Preset.* PresetBundle.* -->`
</action>
<read_first>
- shotScreen/打印机参数设置页.png (read via mcp__4_5v_mcp__analyze_image — successfully analyzed in research)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §3.3, §3.4, §7, §10
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md
- .planning/REQUIREMENTS.md (INV-04, SETTINGS-01 rejects off-design Project/Settings embedding)
- src/qml_gui/pages/SettingsPage.qml (current embedded settings page — confirm it is NOT an independent dialog → status Missing for shell)
- src/qml_gui/pages/ConfigPage.qml (current config embedding)
- src/qml_gui/components/ParamsPage.qml (option editing area)
- src/qml_gui/dialogs/UnsavedChangesDialog.qml (exists — seeds SETPRINT-DIRTY/FOOTER upstream link)
- src/qml_gui/dialogs/SavePresetDialog.qml (exists — seeds PRESETBAR save/save-as)
- third_party/OrcaSlicer/src/slic3r/GUI/Tab.hpp (dialog shell, tabs, dirty markers)
- third_party/OrcaSlicer/src/slic3r/GUI/PresetComboBoxes.hpp (preset bar)
- third_party/OrcaSlicer/src/slic3r/GUI/ConfigManipulation.hpp (option editing)
- third_party/OrcaSlicer/src/libslic3r/PrintConfig.hpp (option keys)
- third_party/OrcaSlicer/src/libslic3r/Preset.hpp (preset dirty/compat)
- third_party/OrcaSlicer/src/libslic3r/PresetBundle.hpp (bundle/preset bar)
</read_first>
<acceptance_criteria>
- `grep -c "^## 4. Printer settings" docs/v3.6-ui-inventory.md` returns 1.
- Printer rows count between 6 and 12: rows under §4 matching `^| SETPRINT-` number between 6 and 12.
- Every SETPRINT- region_id matches `^[A-Z]+-[A-Z0-9]+$`.
- At least one row uses status `Missing` (independent dialog shell): `grep -cE "^\| SETPRINT-.*\| Missing \|" docs/v3.6-ui-inventory.md` returns >= 1, OR the executor documents why an independent dialog target already exists.
- INV-04 coverage anchor present and contains all eight cluster globs.
- Every SETPRINT- row `upstream_source` references at least one INV-04 cluster file.
</acceptance_criteria>

5.
<action>
Write section `## 5. Material settings — 材料参数设置页.png` with the fixed 9-column header, then SETMAT-* rows from 50-RESEARCH §3.4 (`SETMAT-SHELL`, `SETMAT-PRESETBAR`, `SETMAT-TABS`, `SETMAT-GROUPNAV`, `SETMAT-OPTIONS`, `SETMAT-SEARCH`, `SETMAT-FOOTER`, `SETMAT-DIRTY`). 8 rows — within band.

Apply the shared-shell dedup decision from research §3.4: keep all 8 rows (option (a), recommended for INV-01 compliance) but note the qt_target/upstream_source/cleanup strings mirror SETPRINT-*. Read the actual screenshot to set the tab names exactly as shown (Filament / Temperature / Cooling / etc.) and the option fields (temperature °C, retraction mm, flow %). Each `upstream_source` MUST cite at least one INV-04 cluster file. Same `Missing`-shell classification as §4.

End with coverage anchor:
`<!-- INV-04 coverage: Tab.* PresetComboBoxes.* ConfigManipulation.* UnsavedChangesDialog.* CreatePresetsDialog.* PrintConfig.* Preset.* PresetBundle.* -->`
</action>
<read_first>
- shotScreen/材料参数设置页.png (read via mcp__4_5v_mcp__analyze_image)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §3.4, §9.3, §10
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md
- src/qml_gui/pages/SettingsPage.qml (shared shell target)
- src/qml_gui/components/ParamsPage.qml (option editing)
- src/core/viewmodels/ConfigViewModel.h (config state surface)
- src/core/viewmodels/Models/ConfigOptionModel.h (typed option model)
- third_party/OrcaSlicer/src/slic3r/GUI/Tab.hpp (shell/tabs/dirty)
- third_party/OrcaSlicer/src/libslic3r/PrintConfig.hpp (filament option keys)
</read_first>
<acceptance_criteria>
- `grep -c "^## 5. Material settings" docs/v3.6-ui-inventory.md` returns 1.
- Material rows count between 6 and 12: rows under §5 matching `^| SETMAT-` number between 6 and 12.
- Every SETMAT- region_id matches `^[A-Z]+-[A-Z0-9]+$`.
- INV-04 coverage anchor appears (>= 1) and contains all eight cluster globs.
- Every SETMAT- row `upstream_source` references at least one INV-04 cluster file.
</acceptance_criteria>

6.
<action>
Write section `## 6. Cross-cutting modify-vs-replace summary` — a 3-column table `| region_id | modify_or_replace | rationale |` with ONE row per region_id across all four screenshots (PREP-*, PREV-*, SETPRINT-*, SETMAT-*). Apply the decision rules from 50-RESEARCH §7: `modify` when structurally aligned (cleanup `n/a`); `replace` when materially off-design per REQUIREMENTS Scope Contract (cleanup must list old files/routes/qrc/registrations/imports/tests); `missing` paired with status `Missing` when no Qt target exists (cleanup `n/a (no prior Qt target)`). Pre-flag the known candidates: SettingsPage/ConfigPage embedding = `replace` (SETTINGS-01), Printer/Material independent dialogs = `missing`, Top shell BBLTopbar = `modify`, Prepare left sidebar = `modify`, Preview page = `modify`. Below the table add aggregate counts: `# modify`, `# replace`, `# missing-target`.
</action>
<read_first>
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §7 (decision rules + known replace/missing candidates table)
- .planning/REQUIREMENTS.md (Scope Contract: "If an existing Qt page is materially off-design... replace it")
- .planning/STATE.md (v3.6 scope, cleanup rule)
- .codex/rules/source-truth-migration.md (Completeness Over Legacy Compatibility — replace + remove rule)
- docs/项目结构.md (confirm which Qt files exist as replace/modify targets)
</read_first>
<acceptance_criteria>
- `grep -c "^## 6. Cross-cutting modify-vs-replace summary" docs/v3.6-ui-inventory.md` returns 1.
- The summary table has one row per region_id (count matches total region rows across §2-§5, expected ~33-35).
- `modify_or_replace` column uses only `modify`, `replace`, or `missing`.
- At least one `replace` row exists for the Settings embedding: the summary table references SettingsPage/ConfigPage or an equivalent settings-embedding Qt target with `replace`.
- At least one `missing` row exists for independent settings dialogs.
- Aggregate count lines present: `grep -cE "# (modify|replace|missing-target)" docs/v3.6-ui-inventory.md` returns >= 3.
</acceptance_criteria>

7.
<action>
Write section `## 7. Aggregate cleanup checklist` — concatenated, deduplicated cleanup items across ALL `replace` decisions, grouped by removal category, one item per line, machine-grep format with verbatim paths:
`file:    src/qml_gui/...`, `qrc:     ...`, `route:   ...`, `import:  ...`, `test:    ...`, `doc:     ...`
(EXACT tag prefixes `file:`, `qrc:`, `route:`, `import:`, `test:`, `doc:`). Use 4-space alignment after the tag as in 50-RESEARCH §6.2. Only include items that are genuine removal targets for the replace decisions (e.g. old embedded SettingsPage routes/qrc entries/imports/tests; doc path rewrites `third_party/CrealityPrint/` → `third_party/OrcaSlicer/`). Every `file:` path MUST be a verbatim path that exists on disk today (do not invent paths). Add a note that this checklist is consumed directly by Phase 57 (Deprecated UI Removal) and Phase 58 (VERIFY-01) and must stay grep-friendly.

Then write section `## 8. Open behavior gaps` — a table `| region_id | gap | classification | owning phase | note |` where `classification` is one of the 7 status terms and `owning phase` is one of 51-57. Enumerate the known gaps (gizmos needing OpenVDB → Blocked; plate-thumbnail in Preview → Placeholder/Missing; settings dialog shell → Missing; any Mock services).
</action>
<read_first>
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §6.2 (cleanup tag format), §7 (replace candidates), §8 (gap classifications)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md (cleanup must be machine-grep-friendly)
- src/qml_gui/qml.qrc (confirm qrc entries that would be removed for replaced pages)
- src/qml_gui/qmldir (confirm registrations to remove)
- docs/源码真值功能矩阵.md (doc path rewrite target: CrealityPrint → OrcaSlicer)
- src/core/services/PresetServiceMock.h, src/core/services/ProjectServiceMock.h (confirm Mock services for gap table)
</read_first>
<acceptance_criteria>
- `grep -c "^## 7. Aggregate cleanup checklist" docs/v3.6-ui-inventory.md` returns 1.
- `grep -c "^## 8. Open behavior gaps" docs/v3.6-ui-inventory.md` returns 1.
- Cleanup items use only the six allowed tags: `grep -nE "^(file|qrc|route|import|test|doc):" docs/v3.6-ui-inventory.md` returns >= 1 line per category that has items, and `grep -nE "^[a-z]+:" docs/v3.6-ui-inventory.md | grep -vE "^(file|qrc|route|import|test|doc):"` returns nothing (no stray tags).
- Every `file:` cleanup entry references a path that exists on disk: for each `file: <path>` line, `test -f <path>` succeeds (executor verifies; this is a Phase 58 test target).
- Gap table `classification` column uses only the 7 status terms.
- Gap table `owning phase` column uses only values in 51-57.
</acceptance_criteria>

8.
<action>
Write section `## 9. Traceability` mapping each INV requirement to where it is satisfied in this doc:
- INV-01 → §2–§5 completeness (every visible region across all 4 screenshots cataloged)
- INV-02 → §2 coverage assertion (Prepare mapped against Plater.*/GLCanvas3D.*/GUI_ObjectList.*/GUI_ObjectSettings.*/Gizmos/*)
- INV-03 → §3 coverage assertion (Preview mapped against GUI_Preview.*/GCodeViewer.*/GLCanvas3D.*/libslic3r/GCode/*)
- INV-04 → §4–§5 coverage assertion (Settings mapped against Tab.*/PresetComboBoxes.*/ConfigManipulation.*/UnsavedChangesDialog.*/CreatePresetsDialog.*/PrintConfig.*/Preset.*/PresetBundle.*)
- INV-05 → §6 (modify-vs-replace per module) + §7 (cleanup checklist per replacement)

Each traceability line must name the requirement ID and the section(s). No prose-only traceability — use a table or explicit `INV-0x → §N` lines.

Finally, run a self-check pass over the whole file: confirm the fixed 9-column header is byte-identical in every region table (§2, §3, §4, §5), confirm every region row has exactly 9 pipe-delimited cells, confirm no `upstream_source` cell is empty (blank → must read `none-mapped (Owzx-only)`), confirm all status/verification tokens are from the frozen vocabularies. Fix any deviations found.
</action>
<read_first>
- docs/v3.6-ui-inventory.md (the file just written — re-read to self-check all tables)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §6.1 (fixed header string), §8 (test assertions to self-check against)
- .planning/REQUIREMENTS.md (INV-01..INV-05 exact definitions)
</read_first>
<acceptance_criteria>
- `grep -c "^## 9. Traceability" docs/v3.6-ui-inventory.md` returns 1.
- All five requirement IDs appear in §9: `grep -cE "INV-0[1-5]" docs/v3.6-ui-inventory.md` returns >= 5.
- The fixed 9-column header appears identically in every region table: `grep -c "| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |" docs/v3.6-ui-inventory.md` returns >= 4 (one per screenshot section).
- No empty upstream cells: `grep -nE "\|\s*\|\s*(status|verification)" docs/v3.6-ui-inventory.md` finds no row with an empty upstream_source-adjacent cell pattern indicating a blank upstream (executor confirms every row's upstream_source is non-empty or explicitly `none-mapped (Owzx-only)`).
- Total region rows across §2-§5 is between 30 and 40 (33-35 expected; 4 screenshots × 6-12 band).
</acceptance_criteria>

</tasks>

<verification>

Phase 50 is documentation-only; per `.codex/rules/source-truth-migration.md` "Required Verification", lightweight text checks are acceptable for documentation-only phases. Run these deterministic greps against `docs/v3.6-ui-inventory.md`:

1. File exists and non-empty:
   `test -s docs/v3.6-ui-inventory.md && echo PASS || echo FAIL`
2. All 9 top-level sections present (§0–§9):
   `grep -cE "^## [0-9]\. " docs/v3.6-ui-inventory.md` returns >= 9 (§0–§9 minus §1 counting nuance; assert >= 9).
3. All 4 screenshot filenames referenced:
   `grep -cE "准备页.png|预览页.png|打印机参数设置页.png|材料参数设置页.png" docs/v3.6-ui-inventory.md` returns >= 4.
4. Fixed 9-column header present in every region table (>= 4 occurrences):
   `grep -c "| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |" docs/v3.6-ui-inventory.md`
5. Per-cluster coverage anchors present:
   `grep -c "<!-- INV-02 coverage" docs/v3.6-ui-inventory.md` >= 1
   `grep -c "<!-- INV-03 coverage" docs/v3.6-ui-inventory.md` >= 1
   `grep -c "<!-- INV-04 coverage" docs/v3.6-ui-inventory.md` >= 2 (printer + material)
6. Region IDs ASCII-only and pattern-valid (no slashes, no unicode in IDs):
   `grep -nE "^\| (PREP|PREV|SETPRINT|SETMAT)-" docs/v3.6-ui-inventory.md` — every matched ID matches `^[A-Z]+-[A-Z0-9]+$`.
7. No `git diff --check` whitespace errors and no unintended source files changed:
   `git status --porcelain` shows ONLY `docs/v3.6-ui-inventory.md` as the new untracked/modified file for this plan (no .qml/.cpp/.h/CMake/qrc/qmldir/test touched).

</verification>

<must_haves>
- `docs/v3.6-ui-inventory.md` exists and is the single source of truth for every screenshot-visible region across all four screenshots (INV-01).
- Every Prepare region row references at least one INV-02 upstream cluster file, with an INV-02 coverage anchor (INV-02).
- Every Preview region row references at least one INV-03 upstream cluster file, with an INV-03 coverage anchor (INV-03).
- Every Settings region row (printer + material) references at least one INV-04 upstream cluster file, with INV-04 coverage anchors (INV-04).
- The doc records a modify-vs-replace decision per region (§6) and a greppable per-replacement cleanup checklist (§7) (INV-05).
- Region IDs are ASCII-only, stable, page-prefixed, and pattern-valid so Phase 58 VERIFY-01 can deterministically assert counts.
- The 9-column header is byte-identical in all four region tables.
</must_haves>
