---
phase: 50
plan: 02
status: pending
wave: 2
depends_on: ["50-01"]
files_modified:
  - .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05]
---

# Plan 50-02: Frozen Phase Contract & Deterministic Inventory Sign-Off

<objective>
Produce the frozen phase-contract artifact `.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` from the canonical `docs/v3.6-ui-inventory.md` written in Plan 50-01. The phase contract embeds/excerpts the same greppable region tables, adds an explicit "Verification & Sign-Off" section restating exactly what the Phase 50 deterministic harness checked, and adds a full INV-01..INV-05 traceability table with evidence row counts and per-cluster coverage pass/fail. This is the snapshot that gates Phase 50 close and that Phase 58 (VERIFY-01) consumes. No code, route, resource, registration, or test file is touched.
</objective>

<scope_fence>
DOCUMENTATION ONLY. The only file created is `50-INVENTORY.md`.
- Do NOT modify any `.qml`, `.cpp`, `.h`, `CMakeLists.txt`, `qml.qrc`, `qmldir`, route, type-registration, or test file.
- Do NOT modify `docs/v3.6-ui-inventory.md` (owned by Plan 50-01; read it as the source of truth and copy/excerpt — do not edit).
- Do NOT write a real `tests/InventoryAuditTests.cpp` here; Phase 58 owns the compiled test. This plan captures the deterministic grep checks that the future test will encode, by running them as a documentation verification pass and recording results.
</scope_fence>

<tasks>

1.
<action>
Create `.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` with header `# Phase 50 Inventory Contract — Screenshot & Source-Truth Sign-Off`, a `**Frozen:**` date line (today's date), and a one-line role statement: this is the frozen Phase 50 snapshot of `docs/v3.6-ui-inventory.md` (the canonical doc is the live copy mutated by Phases 51-57; this file is the verified snapshot at Phase 50 close).

Add section `## 1. Inventory excerpts` and under it reproduce (copy) the four region tables from `docs/v3.6-ui-inventory.md` §2-§5 (Prepare, Preview, Printer, Material) verbatim, each under a sub-heading (`### 1.1 Prepare page (准备页.png)`, `### 1.2 Preview page (预览页.png)`, `### 1.3 Printer settings (打印机参数设置页.png)`, `### 1.4 Material settings (材料参数设置页.png)`). Copy the per-cluster coverage anchor comments into each sub-section so this file is independently greppable. Also copy the `## 6` modify-vs-replace summary table and the `## 7` aggregate cleanup checklist verbatim as `### 1.5 Modify-vs-replace summary` and `### 1.6 Aggregate cleanup checklist`.

The copy MUST be byte-identical in the 9-column header rows and the cleanup tag lines so the same greps pass against this file as against the canonical doc.
</action>
<read_first>
- docs/v3.6-ui-inventory.md (the canonical source of truth just written in Plan 50-01 — copy/excerpt from it)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §5 (50-INVENTORY.md vs canonical doc role table; §5 recommendation: embed/excerpt same tables + add Verification & Sign-Off + traceability)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md (both artifacts produced; phase contract = sign-off gate)
- .planning/REQUIREMENTS.md (INV-01..INV-05 definitions for traceability)
</read_first>
<acceptance_criteria>
- `test -f .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` passes and file is non-empty.
- `grep -c "^# Phase 50 Inventory Contract" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 1.
- `grep -c "^## 1. Inventory excerpts" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 1.
- All four excerpt sub-headings present: `grep -cE "^### 1\.[1-4] " .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 4.
- The fixed 9-column header appears in each excerpted table: `grep -c "| region_id | region_name | visible_controls | qt_target | upstream_source | status | verification | modify_or_replace | cleanup |" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns >= 4.
- INV-02/03/04 coverage anchors copied into this file: `grep -c "<!-- INV-02 coverage" 50-INVENTORY.md` >= 1, `grep -c "<!-- INV-03 coverage" 50-INVENTORY.md` >= 1, `grep -c "<!-- INV-04 coverage" 50-INVENTORY.md` >= 2.
</acceptance_criteria>

2.
<action>
Add section `## 2. Verification & Sign-Off`. This section re-states exactly what the Phase 50 deterministic harness checked against the just-produced files. Run each of the following checks (via Bash/grep) against `docs/v3.6-ui-inventory.md` AND `50-INVENTORY.md` and record each as a `PASS`/`FAIL` line with the literal command and the numeric result. If any check FAILS, do NOT fabricate PASS — instead stop, report the failure, and fix the underlying content (this may require returning to Plan 50-01 tasks; record the deviation honestly).

Checks to run and record (from 50-RESEARCH §8):
1. Presence: both files exist and non-empty.
2. Per-screenshot region count: for each of Prepare/Preview/Printer/Material, count rows under its section matching `^| (PREP|PREV|SETPRINT|SETMAT)-`; assert each is between 6 and 12 and total across all four is between 30 and 40.
3. Column schema: every region row has exactly 9 pipe-delimited cells and the header matches the fixed 9-column string.
4. Status enum: every `status` cell matches one of `Real|Hybrid|Mock|Blocked|Placeholder|Superseded|Missing`.
5. Verification enum: every `verification` cell matches one of `automated-test|deterministic-harness|manual-visual|manual-uat-checklist|build-only|upstream-parity-audit`.
6. Region ID format: every `region_id` matches `^[A-Z]+-[A-Z0-9]+$`, ASCII-only, unique across the doc.
7. Upstream coverage (INV-02/03/04): each coverage anchor present and lists all required cluster globs.
8. Cleanup format: every `cleanup` cell in a `replace` row is either `n/a` or contains at least one of `file:/qrc:/route:/import:/test:/doc:`; AND every `file:` cleanup entry exists on disk (`test -f`).
9. No blank upstream: no `upstream_source` cell is empty — blank must be explicitly `none-mapped (Owzx-only)`.

End the section with a sign-off summary line: `**Sign-Off:** PASS` only if all checks pass, with the total region count, the per-screenshot counts, and the date. If any check is FAIL, the sign-off line reads `**Sign-Off:** FAIL — see <check#>` and the executor must resolve before Phase 50 can close.
</action>
<read_first>
- docs/v3.6-ui-inventory.md (subject of the verification pass)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-RESEARCH.md §8 (the exact test assertions a future tests/InventoryAuditTests.cpp will make — the harness records these)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-CONTEXT.md (phase contract = verified contract + sign-off gate)
- tests/QmlUiAuditTests.cpp (existing audit-test pattern the future InventoryAuditTests.cpp will mirror — read for grep-assertion style)
</read_first>
<acceptance_criteria>
- `grep -c "^## 2. Verification & Sign-Off" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 1.
- The section records all 9 checks by number: `grep -cE "^[0-9]\." .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` reflects the 9 checks listed (>= 9 numbered check lines in the verification section).
- A sign-off line exists: `grep -c "Sign-Off:" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns >= 1.
- The sign-off is honest: if any recorded check line contains `FAIL`, the `Sign-Off:` line contains `FAIL` (no `Sign-Off: PASS` while a FAIL check is recorded).
- The sign-off summary includes a total region count number.
</acceptance_criteria>

3.
<action>
Add section `## 3. INV-01..INV-05 Traceability` — a table `| Requirement | Where satisfied | Evidence |` with one row per requirement:
- INV-01 → `§1.1–§1.4 excerpts` → evidence: `<N>` total region rows across all 4 screenshots (record the exact count, e.g. 34), every visible region cataloged with region name, visible controls, Qt target, upstream source, behavior status, verification method.
- INV-02 → `§1.1 Prepare excerpt + INV-02 coverage anchor` → evidence: all 5 Prepare cluster globs (Plater.*, GLCanvas3D.*, GUI_ObjectList.*, GUI_ObjectSettings.*, Gizmos/*) referenced; `<n>` Prepare rows.
- INV-03 → `§1.2 Preview excerpt + INV-03 coverage anchor` → evidence: all 4 Preview cluster globs referenced; `<n>` Preview rows.
- INV-04 → `§1.3 + §1.4 Settings excerpts + INV-04 coverage anchors` → evidence: all 8 Settings cluster globs referenced across printer + material; `<n>` printer + `<n>` material rows.
- INV-05 → `§1.5 modify-vs-replace summary + §1.6 cleanup checklist` → evidence: `<n>` modify / `<n>` replace / `<n>` missing decisions; cleanup checklist uses the six grep tags and every `file:` entry exists on disk.

Fill the `<N>`/`<n>` placeholders with the actual counts measured by the verification pass in Task 2. Do not leave placeholders.

Add a final `## 4. Phase 50 → Phase 51 handoff` note (2-4 lines): the canonical `docs/v3.6-ui-inventory.md` is the live copy Phases 51-57 will update statuses in; region IDs are immutable; Phase 58 VERIFY-01 will encode these deterministic checks as `tests/InventoryAuditTests.cpp`.
</action>
<read_first>
- docs/v3.6-ui-inventory.md (to read the actual counts for the traceability evidence cells)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md §1-§2 (the excerpts + verification results just written — pull counts from there)
- .planning/REQUIREMENTS.md (INV-01..INV-05 exact definitions and the Traceability table)
- .planning/ROADMAP.md (Phase 51 is the downstream consumer; Phase 58 VERIFY-01 owns the compiled test)
</read_first>
<acceptance_criteria>
- `grep -c "^## 3. INV-01..INV-05 Traceability" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 1.
- All five requirement IDs appear in the traceability table: `grep -cE "INV-0[1-5]" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns >= 5.
- No unfilled placeholders remain: `grep -cE "<[Nn]>" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 0.
- Evidence cells contain actual integer counts (e.g. `34 total region rows`, not a placeholder).
- `grep -c "^## 4. Phase 50 → Phase 51 handoff" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 1.
</acceptance_criteria>

4.
<action>
Final cross-file consistency check. Re-read both `docs/v3.6-ui-inventory.md` and `50-INVENTORY.md` and confirm: (a) the total region row count is identical between the two files; (b) the four coverage anchors are present in both; (c) the cleanup checklist tag lines are byte-identical between the two; (d) `git status --porcelain` shows ONLY the two documentation files created this phase (`docs/v3.6-ui-inventory.md` new, `50-INVENTORY.md` new) — no `.qml/.cpp/.h/CMakeLists.txt/qml.qrc/qmldir` and no test file changed. If any drift is found, fix the `50-INVENTORY.md` copy to match the canonical doc (the canonical doc is authoritative per 50-CONTEXT).
</action>
<read_first>
- docs/v3.6-ui-inventory.md (canonical — authoritative)
- .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md (the contract just written — must match canonical)
</read_first>
<acceptance_criteria>
- Region row counts match between the two files (executor asserts the count of `^| (PREP|PREV|SETPRINT|SETMAT)-` is equal in both files).
- `git status --porcelain` shows no source file modified: `git status --porcelain | grep -E "\.(qml|cpp|h|cmake|qrc|qmldir)$"` returns nothing, and no file under `tests/` is modified or added.
- The only planning/docs files touched this phase are `docs/v3.6-ui-inventory.md` and `.planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` (plus the two PLAN.md files already committed).
- Coverage anchors and cleanup tag lines are byte-identical between the two files (executor confirms via diff of the relevant sections).
</acceptance_criteria>

</tasks>

<verification>

Documentation-only phase; deterministic grep/text checks per `.codex/rules/source-truth-migration.md`. Run against `50-INVENTORY.md`:

1. File exists and non-empty:
   `test -s .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md && echo PASS || echo FAIL`
2. All 4 sections present (§1 excerpts, §2 verification, §3 traceability, §4 handoff):
   `grep -cE "^## [1-4]\. " .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 4.
3. Sign-off recorded:
   `grep -c "Sign-Off:" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns >= 1.
4. INV-01..05 all present in traceability:
   `grep -cE "INV-0[1-5]" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns >= 5.
5. No placeholders left:
   `grep -cE "<[Nn]>" .planning/phases/50-screenshot-and-source-truth-inventory/50-INVENTORY.md` returns 0.
6. No source/test files changed this phase (scope-fence integrity):
   `git status --porcelain | grep -E "\.(qml|cpp|h|cmake|qrc|qmldir)$"` returns nothing; no `tests/` path changed.
7. Cross-file region-count parity:
   count of `^| (PREP|PREV|SETPRINT|SETMAT)-` is equal in `docs/v3.6-ui-inventory.md` and `50-INVENTORY.md`.

</verification>

<must_haves>
- `50-INVENTORY.md` exists as the frozen Phase 50 contract embedding/excerpting the same greppable region tables, coverage anchors, modify-vs-replace summary, and cleanup checklist as the canonical doc (INV-01..05 carried forward as snapshot).
- A "Verification & Sign-Off" section records the 9 deterministic checks with honest PASS/FAIL and a sign-off line; sign-off is PASS only if every check passes (this is the Phase 50 close gate).
- A full INV-01..INV-05 traceability table maps each requirement to where it is satisfied with real evidence counts (no placeholders).
- No source, route, resource, registration, or test file is modified — only the two documentation files (this plan + Plan 50-01) are created in Phase 50.
- Cross-file consistency: 50-INVENTORY.md matches the canonical doc on region counts, coverage anchors, and cleanup tags.
</must_haves>
