---
phase: 107-filament-map-mode-enum-widening-and-3mf-migration
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/core/model/PartPlate.h
  - src/core/model/PartPlate.cpp
  - src/core/services/ProjectServiceMock.cpp
  - src/core/services/ProjectServiceMock.h
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - tests/PartPlateTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [FMAP-02]
---

# Phase 107 Plan: Filament-Map Mode Enum Widening And 3MF Migration

## Objective
Close FMAP-02: widen Qt6 filament-map mode 2-value → 4-value (upstream `fmmAutoForFlush/fmmAutoForMatch/fmmManual/fmmDefault`) with 3MF read-side migration so pre-v4.5 "Manual"=1 does not silently reload as "AutoForMatch"=1.

## Must Haves
<must_haves>
  <truth id="FM-01">PartPlate.h declares the 4-value `FilamentMapMode` enum (either `enum class FilamentMapMode : int { fmmAutoForFlush=0, fmmAutoForMatch=1, fmmManual=2, fmmDefault=3 }` OR a documented `int` with the 4 named constants) matching upstream PrintConfig.hpp:424-429 exactly (names + values). The comment cites the upstream anchor.</truth>
  <truth id="FM-02">The 3MF WRITE side at ProjectServiceMock.cpp:4997 writes the mode as the upstream enum-name STRING ("fmmAutoForFlush"/"fmmAutoForMatch"/"fmmManual"/"fmmDefault") via the config option, NOT raw int. This is forward-compatible with upstream's ConfigOptionEnum<FilamentMapMode> serialization (PrintConfig.cpp:580-583). Note: upstream deliberately has NO "Default" string per FEATURES.md — `fmmDefault` is a per-plate inherit sentinel resolved before persistence.</truth>
  <truth id="FM-03">The 3MF READ side at ProjectServiceMock.cpp:5413 has migration logic: (a) if the config value reads as a string, map it to the enum; (b) if it reads as a raw int (legacy pre-v4.5 file), map 0→fmmAutoForFlush, 1→fmmManual (PRESERVING pre-v4.5 "Manual"=1 intent — NOT the new fmmAutoForMatch=1), and any other int → fmmDefault (safe fallback). This is the Pitfall 2 migration fix.</truth>
  <truth id="FM-04">All callers of `setFilamentMapMode` / `filamentMapMode` updated: ProjectServiceMock::setPlateFilamentMap (:1169-1187), EditorViewModel Q_PROPERTY/Q_INVOKABLE, PartPlateTests::filamentMapManualAssignment (the existing test uses mode=1 meaning Manual — update to use the new `fmmManual` constant). QML callers updated if any reference the raw int.</truth>
  <truth id="FM-05">A regression test slot `filamentMapModeEnumWidenedAnd3MFMigrates` (or similar) is added to PartPlateTests (or ViewModelSmokeTests) asserting: (a) the 4 enum values exist + map to the correct ints; (b) a legacy raw-int-1 3MF reload maps to fmmManual (the migration); (c) the new string round-trips correctly. Mirrors the Phase 97 thumbnail round-trip pattern.</truth>
  <truth id="FM-06">A QmlUiAuditTests source-audit slot `filamentMapModeEnumWidenedToUpstream4Value` asserts the 4 enum names appear in PartPlate.h + the migration comment/logic appears in ProjectServiceMock.cpp. Locks FMAP-02 against regression.</truth>
  <truth id="FM-07">The `fmmDefault` semantics are documented in a PartPlate.h comment: per-plate "inherit from global" sentinel resolved by a `get_real_filament_map_mode`-equivalent (or document that the Qt6 layer treats fmmDefault as a stored value that the UI resolves). The UI does NOT expose fmmDefault as a 4th radio button (anti-feature per FEATURES.md).</truth>
  <truth id="FM-08">Canonical build + regression ctest 4/4 passes; OWzxSlicer.exe links clean.</truth>
  <truth id="FM-09">`git diff --check` exit 0; encoding guard clean; English ASCII comments.</truth>
</must_haves>

<threat_model>
No new attack surface. The enum widening is internal; the 3MF read-side migration parses an enum string/int from the project file (already trusted input from libslic3r's 3MF reader). No network, no auth, no external input beyond the existing 3MF load path. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="107-01-01" type="implementation">
    <title>Widen filament-map mode enum to 4-value + migrate 3MF read/write</title>
    <read_first>
      - .planning/phases/107-filament-map-mode-enum-widening-and-3mf-migration/107-CONTEXT.md
      - .planning/research/PITFALLS.md (pitfall 2: 3MF migration hazard)
      - .planning/research/FEATURES.md (WS1: 4-value enum truth + fmmDefault semantics)
      - src/core/model/PartPlate.h:202-203, 268-273 (current enum + member)
      - src/core/services/ProjectServiceMock.cpp:4973-4997 (write site) + :5409-5413 (read site) + :1169-1187 (setPlateFilamentMap) + :5620 (apply on load)
      - src/core/viewmodels/EditorViewModel.h/.cpp (filament-map Q_PROPERTY/Q_INVOKABLE — grep filamentMap)
      - third_party/OrcaSlicer/src/libslic3r/PrintConfig.hpp:424-429 (upstream enum)
      - third_party/OrcaSlicer/src/libslic3r/PrintConfig.cpp:580-583 (upstream string serialization)
    </read_first>
    <files>
      - src/core/model/PartPlate.h
      - src/core/model/PartPlate.cpp
      - src/core/services/ProjectServiceMock.cpp
      - src/core/services/ProjectServiceMock.h
      - src/core/viewmodels/EditorViewModel.h
      - src/core/viewmodels/EditorViewModel.cpp
    </files>
    <action>
      **Step 1 — widen the enum (PartPlate.h):** Replace `int m_filament_map_mode = 0; // 0=Auto, 1=Manual` with the 4-value enum matching upstream PrintConfig.hpp:424-429. Update the accessor return type + setter param type. Add a `getRealFilamentMapMode()` helper if `fmmDefault` needs runtime resolution (or document that the Qt6 layer stores fmmDefault as-is and the UI resolves it).

      **Step 2 — 3MF write side (ProjectServiceMock.cpp:4997):** Write the enum-name STRING instead of raw int. Use the config option's enum-string serialization (if ConfigOptionEnum supports it) OR `opt->setString(...)` / equivalent. The 4 strings: "fmmAutoForFlush"/"fmmAutoForMatch"/"fmmManual"/"fmmDefault" (note: upstream has no "Default" string — `fmmDefault` is resolved before persistence; document this).

      **Step 3 — 3MF read side migration (ProjectServiceMock.cpp:5413):** Add migration logic. If the config reads as a string → map to enum. If it reads as a raw int (legacy): 0→fmmAutoForFlush, 1→fmmManual (PRESERVING pre-v4.5 intent), other→fmmDefault. Document the migration in a comment.

      **Step 4 — caller updates:** Update all callers (setPlateFilamentMap, EditorViewModel Q_PROPERTY/Q_INVOKABLE, the load-apply site at :5620). The existing test `filamentMapManualAssignment` uses mode=1 meaning Manual — update to use the new `fmmManual` constant (which is now value 2, not 1).

      **Step 5 — fmmDefault documentation (PartPlate.h):** Document that fmmDefault is a per-plate "inherit from global" sentinel; the UI does NOT expose it as a 4th radio button.
    </action>
    <verify>
      `rg -n "fmmAutoForFlush\|fmmAutoForMatch\|fmmManual\|fmmDefault" src/core/model/PartPlate.h` (4 enum names present)
      `rg -n "filament_map_mode" src/core/services/ProjectServiceMock.cpp` (write + read + migration)
      Build clean.
    </verify>
    <acceptance_criteria>
      - 4-value enum declared matching upstream (FM-01).
      - Write side uses enum-name string (FM-02).
      - Read side migrates legacy raw-int-1 → fmmManual (FM-03).
      - All callers updated (FM-04).
      - fmmDefault documented (FM-07).
    </acceptance_criteria>
  </task>

  <task id="107-01-02" type="test">
    <title>Add filamentMapModeEnumWidenedAnd3MFMigrates regression test</title>
    <read_first>
      - tests/PartPlateTests.cpp (existing filamentMapManualAssignment slot — update + add new slot)
      - .planning/milestones/v4.3-phases/97-thumbnail-save-reload-round-trip/97-01-SUMMARY.md (round-trip test pattern)
    </read_first>
    <files>
      - tests/PartPlateTests.cpp
    </files>
    <action>
      Update the existing `filamentMapManualAssignment` slot to use the new `fmmManual` constant (was raw int 1, now enum value 2). Add a new `filamentMapModeEnumWidenedAnd3MFMigrates` slot asserting: (a) the 4 enum values exist + map to correct ints (0/1/2/3); (b) a legacy raw-int-1 reload maps to fmmManual (migration); (c) the new string round-trips. Use the Phase 97 synthesized-QImage round-trip pattern adapted for filament-map mode.
    </action>
    <verify>
      `rg -n "filamentMapModeEnumWidenedAnd3MFMigrates\|fmmManual" tests/PartPlateTests.cpp`
      ctest PartPlateTests passes.
    </verify>
    <acceptance_criteria>
      - Existing test updated + new slot exists (FM-05). Both pass.
    </acceptance_criteria>
  </task>

  <task id="107-01-03" type="test">
    <title>Add filamentMapModeEnumWidenedToUpstream4Value source-audit</title>
    <read_first>tests/QmlUiAuditTests.cpp, src/core/model/PartPlate.h, src/core/services/ProjectServiceMock.cpp</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>
      Add a `filamentMapModeEnumWidenedToUpstream4Value` source-audit slot asserting the 4 enum names in PartPlate.h + the migration logic in ProjectServiceMock.cpp. FMAP-02-named QVERIFY2 messages. Mirrors Phase 102/103/104/105/106 pattern.
    </action>
    <verify>ctest QmlUiAuditTests passes incl. new slot.</verify>
    <acceptance_criteria>Slot exists, deterministic, passes (FM-06).</acceptance_criteria>
  </task>

  <task id="107-01-04" type="verification">
    <title>Build + regression ctest + document in SUMMARY</title>
    <read_first>.codex/rules/build-rules.md</read_first>
    <files></files>
    <action>Canonical build + regression ctest 4/4. Document the migration in SUMMARY (especially the legacy-int-1 → fmmManual preservation, which is the user-visible behavior change).</action>
    <verify>SUMMARY records build + ctest 4/4 + migration behavior.</verify>
    <acceptance_criteria>Production link clean, ctest 4/4 pass (FM-08).</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build + regression ctest 4/4.
2. Source audit: `rg -n "fmmAutoForFlush\|fmmAutoForMatch\|fmmManual\|fmmDefault" src/core/`
3. Encoding guard on modified files.

## Success Criteria
- FMAP-02 closed; every FM truth; the legacy-int-1 → fmmManual migration is the critical user-visible behavior.

## Expected Non-Changes
- No FMAP-01 readback (Phase 108).
- No FMAP-03 UI (Phase 110).
- No fmmDefault as 4th radio button (anti-feature).
- No libslic3r changes.
