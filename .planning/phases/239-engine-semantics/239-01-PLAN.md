# Phase 239 Plan — Slicing Engine Semantics

**Requirements:** ENGN-01..03 (ENGN-04 done in Phase 233)
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` engine section

## Plan 1 (single batch)

### Task 1 — ENGN-01: auto-reslice on Preview switch
- Upstream do_reslice (Plater.cpp:6165-6260): switching to preview with stale
  results re-slices automatically. EditorViewModel::switchToPreview
  (:7431-7440) only signals when canPreview(); add stale→auto-reslice flow:
  kick requestSlice(current plate) with a "then switch to preview" pending
  flag (existing queue machinery), keep manual option when slicing disabled.
### Task 2 — ENGN-02: G-code reuse path
- `loadGCodeFromPrevious()` (SliceService.cpp:896) has no UI entry. Wire:
  when entering preview and the plate result is valid (not stale), reuse the
  previous G-code instead of forcing a reslice (upstream
  export_gcode_from_previous_file semantics :199-221). Ensure the reuse is
  invalidated by the same invalidation set as slicing (Phase 231's undo
  invalidation included).
### Task 3 — ENGN-03: non-blocking export + warnings
- Move exportSourceToPath (:1196-1304) chunked copy to a worker
  (QtConcurrent) with progress; validation warnings (Print::validate
  warning string) surfaced as notification (postValidateWarning exists, zero
  callers — wire it), distinct from errors.

## Verification

- Canonical build + all suites; E2E: transform → switch to preview →
  auto-reslice → preview shows new state; export big gcode without UI freeze
  (smoke), warning notification on e.g. objects-too-close validate warning.
