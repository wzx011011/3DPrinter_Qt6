---
phase: 226-source-mapped-process-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/Models/ConfigOptionModel.h
  - src/qml_gui/Models/ConfigOptionModel.cpp
  - src/qml_gui/dialogs/SettingsDialog.qml
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements:
  - HIER-01
  - HIER-02
must_haves:
  truths:
    - "The Process dialog exposes exactly Quality, Strength, Speed, Support, Multimaterial, and Others in locked TabPrint order."
    - "Every loaded supported Process option is reached through its exact upstream page-qualified group and key order, never through a category, alphabetical, or Others fallback."
    - "The Process-only dialog path renders the C++ source order with static group headings while Printer and Material settings retain their existing hierarchy behavior."
  artifacts:
    - path: "src/qml_gui/Models/ConfigOptionModel.cpp"
      provides: "One ordered TabPrint page/group/key manifest, derived lookup, fail-closed Process assignment, and ordered QML projection methods."
      contains: "processPageNames"
    - path: "src/qml_gui/dialogs/SettingsDialog.qml"
      provides: "Process-only six-tab navigation and C++-ordered static group rendering."
      contains: "orderedProcessIndicesForGroup"
    - path: "tests/ViewModelSmokeTests.cpp"
      provides: "Independent C++ regression contract for all six Process pages, 36 page-qualified groups, membership, and order."
      contains: "sourceMappedProcessHierarchyMatchesTabPrint"
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "Process-path audit preventing legacy tabs, QML grouping/fallback, sidebar revival, and decorative Process disclosure affordances."
      contains: "processSettingsConsumesSourceMappedHierarchy"
  key_links:
    - from: "ConfigOptionModel::loadFromUpstreamSchema"
      to: "ordered TabPrint manifest"
      via: "manifest-derived schema key union and page/group assignment"
      pattern: "loadFromUpstreamSchema.*processPageNames"
    - from: "SettingsDialog.qml"
      to: "ConfigOptionModel"
      via: "processPageNames, processGroupsForPage, and orderedProcessIndicesForGroup invokables"
      pattern: "processPageNames|processGroupsForPage|orderedProcessIndicesForGroup"
    - from: "SettingsDialog.qml Process branch"
      to: "OptionRow.qml"
      via: "existing value delegate with Process header disabled because the parent renders the C++-derived static heading"
      pattern: "showGroupHeader: false"
---

<objective>
Make the FFF Process Settings dialog consume one complete, ordered mapping of
locked OrcaSlicer TabPrint pages, groups, and option keys.

Purpose: replace the divergent flat/category-derived Process hierarchy without
altering the established edit, preset, or non-Process settings routes.
Output: an audited C++ source manifest, Process-only ordered dialog projection,
and focused C++/QML regression coverage for HIER-01 and HIER-02.
</objective>

<execution_context>
@C:/Users/67376/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/67376/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/226-source-mapped-process-hierarchy/226-CONTEXT.md
@.planning/phases/226-source-mapped-process-hierarchy/226-RESEARCH.md
@.planning/phases/226-source-mapped-process-hierarchy/226-UI-SPEC.md
@.planning/phases/226-source-mapped-process-hierarchy/226-VALIDATION.md
@.planning/research/v5.11-PROCESS-SETTINGS-SUMMARY.md
@.planning/phases/46-config-editing-dirty-state-and-reset-semantics/46-01-SUMMARY.md
@.codex/rules/source-truth-migration.md
@.codex/rules/qml-boundaries.md
@.codex/rules/build-rules.md
@third_party/OrcaSlicer/src/slic3r/GUI/Tab.cpp:2005-2376
@src/qml_gui/Models/ConfigOptionModel.h
@src/qml_gui/Models/ConfigOptionModel.cpp
@src/core/viewmodels/ConfigViewModel.h
@src/core/viewmodels/ConfigViewModel.cpp
@src/qml_gui/dialogs/SettingsDialog.qml
@src/qml_gui/components/OptionRow.qml
@tests/ViewModelSmokeTests.cpp
@tests/QmlUiAuditTests.cpp

<interfaces>
Existing contracts the implementation must retain:

- `ConfigViewModel::filterOptionIndices(const QString &amp;tier, const QString &amp;searchText, bool advancedMode)` remains the C++ filtering authority and returns candidate row indices.
- `ConfigOptionModel::optKey(int)`, `optPage(int)`, and `optGroup(int)` expose schema metadata; `setValue()` and `optionValueChanged` remain on the existing edit route.
- `SettingsDialog.qml` owns local `activeTab`, invokes `ConfigViewModel`, and presents `OptionRow`; it must not encode upstream page/group/key membership.
- `OptionRow.qml` is retained for existing typed value rendering. The Process parent supplies its static heading and passes `showGroupHeader: false`; do not change OptionRow or non-Process callers in this phase.

Create these Process-only `ConfigOptionModel` invokables as the interface for the
dialog: `processPageNames()`, `processGroupsForPage(const QString &amp;page)`, and
`orderedProcessIndicesForGroup(const QList<int> &amp;candidateIndices, const QString &amp;page, const QString &amp;group)`.
</interfaces>
</context>

<execution_preflight>
Before changing any Phase 226 file, execute the protected-hunk preflight in
`226-VALIDATION.md`. It snapshots the starting worktree versions and unstaged
diffs of `tests/ViewModelSmokeTests.cpp` and `tests/QmlUiAuditTests.cpp`, along
with the starting index. Those two files already contain unrelated user hunks;
they are baseline state, not Phase 226 input. Add the new test slots at the
phase-owned anchors specified there so the generated zero-context patches do
not overlap the captured hunks.

At commit time, follow the validation document's temporary-index procedure.
It generates one patch per Phase-owned file relative to the preflight snapshot,
checks and applies only those patches to an alternate index rooted at `HEAD`,
verifies that the captured user patches are absent from that index, then creates
the commit and restores any previously staged user patch. Do not run `git add`
on either test file, do not use `git add -p`, and do not commit a whole path from
the working tree. If a patch cannot apply or a preserved-user-hunk assertion
fails, leave `HEAD`, the real index, and the user worktree unchanged; stop and
report the overlap rather than staging the user's work.
</execution_preflight>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the complete TabPrint hierarchy in a fail-closed C++ manifest</name>
  <files>src/qml_gui/Models/ConfigOptionModel.h, src/qml_gui/Models/ConfigOptionModel.cpp, tests/ViewModelSmokeTests.cpp</files>
  <behavior>
    - `processPageNames()` returns exactly `Quality`, `Strength`, `Speed`, `Support`, `Multimaterial`, and `Others`, in that order; it rejects the old Process pages `Base`, `Cooling`, `Retraction`, and singular `Other`.
    - `processGroupsForPage()` returns the exact source order for all 36 page-qualified groups, including four independent `Advanced` groups on Strength, Speed, Support, and Multimaterial.
    - For candidates returned by `ConfigViewModel::filterOptionIndices("print", ...)`, `orderedProcessIndicesForGroup()` returns only the matching page/group's manifest keys in upstream option order. Each loaded supported Process key has one manifest location and no Process row reaches `pageForCategory()`, alphabetical append, or an Others fallback.
    - Existing Printer and Material `pageNames()`, mapping, and filtering behavior remains unchanged.
  </behavior>
  <action>
Run the protected-hunk preflight in `226-VALIDATION.md` before implementation. Add the failing `sourceMappedProcessHierarchyMatchesTabPrint` QtTest slot and its declaration at the validation document's Phase-owned anchors, not inside a captured user hunk. Give the test an independent expected manifest transcribed from active `TabPrint::build()` calls at `third_party/OrcaSlicer/src/slic3r/GUI/Tab.cpp:2005-2376`; never derive expected values by reading the production manifest. Assert the six pages and every group in this exact page-qualified order: Quality: Layer height, Line width, Seam, Precision, Ironing, Wall generator, Walls and surfaces, Bridging, Overhangs; Strength: Walls, Top/bottom shells, Infill, Advanced; Speed: Initial layer speed, Other layers speed, Overhang speed, Travel speed, Acceleration, Jerk(XY), Advanced; Support: Support, Raft, Support filament, Advanced, Tree supports; Multimaterial: Prime tower, Filament for Features, Ooze prevention, Flush options, Advanced; Others: Skirt, Brim, Special mode, G-code output, Post-processing Scripts, Notes. For every group, assert the ordered option-key sequence from the active source, including `Option` wrapper calls, then assert every loaded Process row belongs exactly once and that the Process projection preserves the expected order.

In `ConfigOptionModel`, replace `kPrintPageGroupMap()` as the Process authority with nested ordered page/group/key definitions whose single source comment cites `Tab.cpp:2005-2376` in English ASCII. Derive a lookup only from that ordered manifest; do not repair selected `QHash` entries. Derive the print schema key union from the same manifest before loading, then assign page/group metadata from the derived lookup. A print key that the schema loader would expose but which has no manifest entry is a mapping failure: do not call category mapping or put it in `Others`; make the mismatch observable to the test/audit path and keep it out of the Process projection. Retain generic category fallback and generic `pageNames()` only for non-Process consumers.

Declare and implement the three Process-only invokables named in `<interfaces>`. They must expose manifest order without copying values or preset state, accept C++-filtered candidates, reject invalid page/group combinations, and never sort by labels or keys. Keep `ConfigViewModel::filterOptionIndices`, all edit/reset/preset/slice routes, persistence, and `OptionRow` untouched. This implements the CONTEXT discretion using the source-derived C++ model boundary, not QML rules.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command '
&amp; powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\build\ViewModelSmokeTests.exe sourceMappedProcessHierarchyMatchesTabPrint
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
'</automated>
  </verify>
  <done>The Process model has one complete source-derived manifest covering all six pages and 36 page-qualified groups; all loaded supported Process keys map once in source order; the new independent QtTest passes; no legacy/category/fallback path can expose an unmapped Process option.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render only the Process dialog from the ordered C++ hierarchy</name>
  <files>src/qml_gui/dialogs/SettingsDialog.qml, tests/QmlUiAuditTests.cpp</files>
  <behavior>
    - Opening the print dialog selects Quality and presents six equal-width, one-line source-derived tabs; pointer and standard tab keyboard activation select exactly one page.
    - The active Process page renders only non-empty groups supplied in upstream order, with a localized static title/divider before its already ordered OptionRow delegates; identical labels on different pages remain distinct.
    - The Process branch has no sidebar, `+`, chevron, group focus/toggle/count/reset state, or fallback content. Printer and Material continue to use their existing tab, header, and option-list route.
  </behavior>
  <action>
First add the failing `processSettingsConsumesSourceMappedHierarchy` QML audit slot and declaration at the validation document's Phase-owned anchors, not inside a captured user hunk. Scope assertions to the print-specific block rather than searching the entire file, because `Cooling` remains valid in the Material tier. Replace the obsolete audit that requires `key: "Other"` with assertions that the Process branch calls `processPageNames`, `processGroupsForPage`, and `orderedProcessIndicesForGroup`; has the exact six page keys through the C++ contract; does not contain Process `Base`, `Cooling`, `Retraction`, or singular `Other`; does not derive group changes from adjacent `filteredIndices`; passes `showGroupHeader: false` to its Process OptionRow delegates; retains `GroupNavSidebar.qml` outside `qml.qrc`; and leaves explicit Printer/Material branch anchors intact.

In `SettingsDialog.qml`, keep the existing non-modal 736x593 shell, preset bar, `ConfigViewModel::filterOptionIndices` candidate filter, edit route, and Printer/Material branches. Add a Process-only tab/list presentation branch. Bind its six `TabButton`/`TabBar` cells to `optionModel.processPageNames()` and localize displayed labels with `qsTr()`. Use standard tab semantics, including pointer, Enter, Space, Left/Right, Home, and End activation, visible focus indication, accessible localized name, selected state, and set position. Initialize the Process branch to its first source page, preserve a single active tab, use six equal-width 38px cells, `Text.ElideRight`, Theme tokens, and no new colors, fonts, dependencies, or registry entries.

For Process content, iterate `optionModel.processGroupsForPage(activeTab)` and ask `orderedProcessIndicesForGroup(filteredIndices, activeTab, group)` for each group's rows. Render a localized static title and `Theme.borderSubtle` divider only when that ordered result is non-empty, then render the existing `OptionRow` values in that returned order with `showGroupHeader: false`. This keeps hierarchy/order in C++ while QML only lays out and forwards tab selection. Do not add collapse state, counts, reset actions, search redesign, typed-row changes, QSettings/preset/project persistence, per-extruder UI, `GroupNavSidebar`, or new fallback pages. Leave the generic non-Process OptionRow decorative header alone because changing it would widen this phase.
  </action>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command '
&amp; powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
&amp; .\build\QmlUiAuditTests.exe processSettingsConsumesSourceMappedHierarchy
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
'</automated>
  </verify>
  <done>The print-only dialog is a keyboard-operable six-tab C++-backed hierarchy with static source-order headings and no decorative disclosure/fallback; the focused QML audit passes while Printer and Material behavior remains on the existing path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locked upstream source to local C++ manifest | A transcription or future schema change could silently place a Process option in the wrong semantic page/group. |
| C++ hierarchy projection to QML dialog | QML could recreate a category or adjacent-row grouping rule that diverges from upstream metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01 | Tampering | `ConfigOptionModel` Process manifest | mitigate | Use one ordered `TabPrint::build()` transcription, derive all lookups and the schema key union from it, fail closed for unmapped Process keys, and assert the full independent six-page/36-group/key contract. |
| T-226-02 | Tampering | `SettingsDialog.qml` Process branch | mitigate | Bind only Process invokables and static C++-ordered rows; audit absence of legacy tabs, adjacent-row group derivation, sidebar registration, and Process fallback/disclosure tokens. |
| T-226-03 | Elevation | QML-to-config boundary | accept | No privilege or external input surface is added; existing C++ edit/preset routes are preserved and this phase adds no mutation command. |
| T-226-SC | Tampering | Package manager installs | accept | No package, registry, CMake dependency, or install operation is in scope. |
</threat_model>

<verification>
Complete every required command and inspection in
`226-VALIDATION.md`. Each automated command invokes the canonical build script,
checks `$LASTEXITCODE` before its focused test, and checks the focused test
before continuing. The phase gate also runs the encoding guard against the
alternate index that contains only Phase 226 changes.

The manual Process-dialog inspection is required after the automated gate: at
736x593 and the established higher UI scale, Quality opens first; all six tabs
remain one line; pointer and keyboard choose one active page; headings and rows
do not clip; no group sidebar, Process `+`, or chevron appears; and
Printer/Material dialogs remain unchanged. This is a presentation check only;
disclosure, counts, typed editing, preset lifecycle, and final E2E/visual
evidence belong to Phases 227-230.
</verification>

<success_criteria>
- HIER-01: The Process dialog displays exactly Quality, Strength, Speed, Support, Multimaterial, and Others, in locked upstream order.
- HIER-02: Each supported Process option is loaded, validated, and rendered once through its `TabPrint::build()` page-qualified group/key position; no category, alphabetical, or Others fallback exists on the Process route.
- The two focused tests, alternate-index encoding guard, canonical verification command, and required Process dialog inspections pass, with `GroupNavSidebar.qml` still absent from resources.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 226 | Exact upstream Process page/group navigation instead of divergent flat/fallback list | 226-01 Task 1-2 | COVERED | C++ manifest is the authority; QML consumes it only.
REQ | HIER-01 | Six upstream Process pages in exact order | 226-01 Task 1-2 | COVERED | Independent manifest assertion plus Process-only tab binding.
REQ | HIER-02 | Reach every supported option through upstream page/group | 226-01 Task 1-2 | COVERED | Full key membership/order test and ordered page-qualified projection.
RESEARCH | R-01 | One ordered manifest replaces `kPrintPageGroupMap` authority | 226-01 Task 1 | COVERED | Derived lookup is an implementation detail, never the ordering source.
RESEARCH | R-02 | Fail closed; no category/alphabetical/Others fallback for Process | 226-01 Task 1 | COVERED | Process load/projection rejects unmapped keys and test locks it.
RESEARCH | R-03 | Preserve ConfigViewModel filtering and existing value/preset route | 226-01 Task 1-2 | COVERED | Candidate filtering is reused; no edit/preset service changes.
RESEARCH | R-04 | Keep GroupNavSidebar retired and defer Phase 227 interaction | 226-01 Task 2 | COVERED | Static headings only; QML audit retains the resource exclusion.
CONTEXT | discretion | Use locked source, roadmap, and QML boundary rules; no locked D-XX decisions exist | 226-01 Task 1-2 | COVERED | C++ owns mapping; QML owns presentation/selection only.
</source_audit>

<output>
Create `.planning/phases/226-source-mapped-process-hierarchy/226-01-SUMMARY.md` when done.
</output>
