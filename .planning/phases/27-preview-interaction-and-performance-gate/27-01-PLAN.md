---
plan_id: 27-01
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
  - tools/render_bench/main.cpp
requirements: [PREV-05, PERF-01, PERF-02]
autonomous: true
---

# Plan 27-01: Preview Interaction + Performance Gate

<objective>
Validate Phase 26's Preview pipeline: (1) add frame/upload timing to RhiViewportRenderer (PERF-01), (2) run render_bench 1M/5M D3D11 benchmarks (PERF-02), (3) audit playback/scrub no-re-upload path (PREV-05). Mostly instrumentation + benchmark runs; minimal new code.
</objective>

<must_haves>
  <truth>RhiViewportRenderer captures Preview frame time + upload time via QElapsedTimer</truth>
  <truth>render_bench stableAuto order aligned to D3D11-first (match app)</truth>
  <truth>render_bench 1M D3D11 benchmark captured in structured output</truth>
  <truth>render_bench 5M D3D11 benchmark captured or documented as driver-limited</truth>
  <truth>PREV-05 audited: layer/playback changes do NOT trigger buffer re-upload (code-level proof)</truth>
  <truth>canonical verify exits 0</truth>
</must_haves>

<tasks>

  <task id="27-01-T1">
    <action>
Add Preview timing instrumentation to RhiViewportRenderer. In the header, add members: `QElapsedTimer m_previewFrameTimer;`, `qint64 m_previewLastUploadMs = -1;`, `qint64 m_previewLastFrameMs = -1;`, `qint64 m_previewFirstFrameMs = -1;`, `bool m_previewFirstFrameDone = false;`.

In render() Preview branch: start `m_previewFrameTimer.restart()` at the beginning of the CanvasPreview draw section (after the segment upload check, before `cb->beginPass`). After `cb->endPass()`, capture `m_previewLastFrameMs = m_previewFrameTimer.elapsed()`. If first frame, store `m_previewFirstFrameMs`. Log via `qInfo("[RHI-PERF] preview frame: %lldms upload: %lldms segments: %u backend: preview", ...)`. Include `#include <QElapsedTimer>` if not present.

In uploadPreviewSegmentBuffer: wrap the `uploadStaticBuffer` call with `QElapsedTimer t; t.start();` ... after upload `m_previewLastUploadMs = t.elapsed();`.
    </action>
    <read_first>
- `src/qml_gui/Renderer/RhiViewportRenderer.cpp:155-179` (Preview render branch — timing insertion points)
- `src/qml_gui/Renderer/RhiViewportRenderer.cpp:618-630` (uploadPreviewSegmentBuffer — upload timing)
    </read_first>
    <acceptance_criteria>
- Header has QElapsedTimer + timing members
- render() logs [RHI-PERF] frame time for Preview
- uploadPreviewSegmentBuffer logs upload time
- File compiles (T4)
    </acceptance_criteria>
  </task>

  <task id="27-01-T2">
    <action>
Align render_bench `stableAuto` order to D3D11-first (matching the app's crash workaround). In `tools/render_bench/main.cpp` at the `stableAuto` candidate list (~line 156-159), change from `{d3d12, d3d11}` to `{d3d11, d3d12}`. Add a comment explaining: "D3D11-first to match the app's RhiBackendSelector (D3D12 crashes in app Prepare render; D3D11 is the safe default)."
    </action>
    <read_first>
- `tools/render_bench/main.cpp:156-159` (stableAuto order)
- `src/qml_gui/Renderer/RhiBackendSelector.cpp:37-46` (app's D3D11-first order — the reference to match)
    </read_first>
    <acceptance_criteria>
- `grep "d3d11.*d3d12\|d3d11.*Direct3D11" tools/render_bench/main.cpp` shows d3d11 before d3d12
- Comment explains the alignment
    </acceptance_criteria>
  </task>

  <task id="27-01-T3">
    <action>
Build render_bench and run D3D11 benchmarks. Build: `ninja -C build owzx-render-bench`. Run:
- `build/owzx-render-bench.exe --backend d3d11 --segments 1000000` (1M)
- `build/owzx-render-bench.exe --backend d3d11 --segments 5000000` (5M)
Capture JSON output. If 5M fails (driver/memory), document the failure reason.

Also audit PREV-05: read `RhiViewportRenderer.cpp:76-86` (synchronize) and confirm that layerMin/Max/moveEnd changes do NOT invalidate `m_previewSegmentBufferUploaded` — only `m_previewData` change triggers re-parse. Document this code-level proof in the VERIFICATION.
    </action>
    <read_first>
- `tools/render_bench/main.cpp` (CLI args + output format)
- `src/qml_gui/Renderer/RhiViewportRenderer.cpp:76-86` (synchronize — PREV-05 proof)
    </read_first>
    <acceptance_criteria>
- 1M D3D11 benchmark JSON captured (uploadMs, medianFrameMs, p95)
- 5M D3D11 benchmark JSON captured or documented as limited
- PREV-05 audit documented (synchronize only re-parses on data change)
    </acceptance_criteria>
  </task>

  <task id="27-01-T4">
    <action>
Run canonical verify to confirm timing instrumentation compiles and no regression.
    </action>
    <read_first>
- `.codex/rules/build-rules.md`
    </read_first>
    <acceptance_criteria>
- `auto_verify_with_vcvars.ps1` compiles clean (0 errors)
- Existing tests don't regress (build-linked)
    </acceptance_criteria>
  </task>

</tasks>

<verification>
- Frame/upload timing logs in RhiViewportRenderer Preview path
- render_bench D3D11-first order aligned with app
- 1M/5M D3D11 benchmark JSON captured
- PREV-05 audited (no re-upload on layer/playback change)
- Build compiles clean
</verification>
