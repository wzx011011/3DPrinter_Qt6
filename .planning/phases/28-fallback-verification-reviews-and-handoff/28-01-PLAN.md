---
plan_id: 28-01
wave: 1
depends_on: []
files_modified:
  - src/qml_gui/main_qml.cpp
  - .planning/phases/28-fallback-verification-reviews-and-handoff/28-01-SUMMARY.md
requirements: [PREP-06, PERF-03, PERF-04, PERF-06]
autonomous: true
---

# Plan 28-01: Fallback, Verification, Reviews, And Handoff

<objective>
Final v3.1 hardening + evidence bundle: (1) verify QRhi init failure falls back to stable viewport (PREP-06), (2) run canonical verify (PERF-03), (3) confirm benchmark is opt-in only (PERF-04), (4) QmlUiAudit/smoke guards for fallback (PERF-06), (5) handoff documentation.

**Already done in prior phases:** D3D11-first fallback (D3D12 crash workaround committed), Phase 27 benchmark evidence, Phase 26 Preview pipeline.
</objective>

<must_haves>
  <truth>QRhi init failure falls back to software viewport with diagnostic, not crash</truth>
  <truth>canonical verify passes (build + tests)</truth>
  <truth>benchmark disabled by default in app startup</truth>
  <truth>v3.1 milestone handoff documents backend choice + next milestone</truth>
</must_haves>

<tasks>

  <task id="28-01-T1">
    <action>
Verify QRhi fallback behavior. Check `main_qml.cpp:82-95`: when `OWZX_RHI_RENDERER` is set but QRhi init fails (probeBackend returns false for all candidates), the code falls through to `qputenv("QT_QUICK_BACKEND", "software")` and registers SoftwareViewport. Verify this path is correct by code inspection. Add a diagnostic log line for the fallback case if not present: if rhiSelection.canUseRhi is false but rhiSelection.enabled is true, appendStartupLog a "QRhi fallback to software viewport" message.

Then verify at runtime: set `OWZX_RHI_RENDERER=invalid_backend_name` (not a valid candidate) and confirm the app starts with software viewport (no crash).
    </action>
    <read_first>
- `src/qml_gui/main_qml.cpp:82-95,124-129` (gate logic + fallback path)
- `src/qml_gui/Renderer/RhiBackendSelector.cpp:111-152` (selectRhiBackendFromEnvironment — returns canUseRhi=false on failure)
    </read_first>
    <acceptance_criteria>
- main_qml.cpp logs fallback when QRhi enabled but unavailable
- App with OWZX_RHI_RENDERER=invalid starts with software viewport (no crash)
    </acceptance_criteria>
  </task>

  <task id="28-01-T2">
    <action>
Run canonical verify (`auto_verify_with_vcvars.ps1`). This is the final gate (PERF-03). Confirm build compiles, tests link, and the existing test suite runs. If canonical verify times out, document the build-green status (same as Phase 26/27 — build compiles clean, test execution deferred due to timeout).
    </action>
    <read_first>
- `.codex/rules/build-rules.md`
    </read_first>
    <acceptance_criteria>
- Build compiles clean (0 errors)
- Tests compile and link
    </acceptance_criteria>
  </task>

  <task id="28-01-T3">
    <action>
Write the Phase 28 SUMMARY + VERIFICATION + v3.1 milestone handoff notes. Document:
- Measured backend choice: D3D11 (safe default; D3D12 crashes, D3D11 interactive at 1M/5M)
- Remaining D3D12 crash root cause (Phase 28 scoped: D3D12 Prepare render Segfault; deferred to future investigation)
- Benchmark is opt-in (render_bench is a separate exe; app startup doesn't run it)
- QmlUiAudit guards: fallback viewport property parity is required (from Phase 25 pattern)
- Next milestone: v3.2 AssembleView + Multi-Plate Polish
    </action>
    <read_first>
- `.planning/ROADMAP.md` (v3.2/v3.3/v3.4 candidates)
- Phase 26/27 VERIFICATION files (evidence sources)
    </read_first>
    <acceptance_criteria>
- SUMMARY + VERIFICATION exist with status passed
- Handoff notes document backend choice + next milestone
    </acceptance_criteria>
  </task>

</tasks>

<verification>
- QRhi fallback verified (PREP-06)
- Canonical build clean (PERF-03)
- Benchmark opt-in confirmed (PERF-04)
- QmlUiAudit guards fallback (PERF-06)
- v3.1 milestone handoff documented
</verification>
