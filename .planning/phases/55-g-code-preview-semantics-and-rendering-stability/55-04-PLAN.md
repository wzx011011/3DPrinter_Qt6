---
phase: 55-g-code-preview-semantics-and-rendering-stability
plan: 04
type: execute
wave: 2
depends_on: [55-02]
files_modified:
  - tests/ViewModelSmokeTests.cpp
  - tests/QmlUiAuditTests.cpp
  - tests/E2EWorkflowTests.cpp
autonomous: true
requirements: [GCODE-01, GCODE-03, GCODE-04, GCODE-05]
user_setup: []

must_haves:
  truths:
    - "After a live slice, gcodePreviewData is a non-empty GCV1 blob with layerCount > 0, moveCount > 0, and no segment carries placeholder/demo/sample marker bytes (GCODE-01 RED test now GREEN)"
    - "sliceFailed, sliceResultCleared, and plate-switch-to-invalid each trigger resetPreviewState and clear gcodePreviewData_ (one test per trigger)"
    - "toggleRoleVisibility does NOT mutate gcodePreviewData_ (byte-for-byte equality before/after a toggle)"
    - "Legend min/max labels are unchanged across a layer-range or move drag (global-scope legend invariant)"
    - "setCurrentMove updates currentGcodeLine and the gcodeLines window atomically (single stateChanged emission)"
    - "PreviewPage.qml contains zero occurrences of 'SoftwareViewport' (GCODE-04 source-audit guard)"
    - "main_qml.cpp registers RhiViewport as the default GLViewport with SoftwareViewport only behind an explicit gate (GCODE-04 startup-policy guard)"
    - "computePreviewDrawRange contains the role-skip block (GCODE-02 source-audit supporting evidence)"
    - "gcodePreviewData survives layer/move/camera/toggle interaction setters (GCV1 payload-survives-interaction invariant)"
    - "Reslice clears and rebuilds gcodePreviewData with different bytes and recomputed layer/move counts"
    - "Export while Preview is visible leaves gcodePreviewData intact"
    - "Page switch Prepare<->Preview preserves gcodePreviewData"
  artifacts:
    - path: "tests/ViewModelSmokeTests.cpp"
      provides: "Role-toggle no-repack test, legend-coherence test, currentMove atomicity test"
      contains: "toggleRoleVisibility"
    - path: "tests/QmlUiAuditTests.cpp"
      provides: "PreviewPage SoftwareViewport guard, computePreviewDrawRange role-skip assertion, PackedSegment sizeof guard"
      contains: "SoftwareViewport"
    - path: "tests/E2EWorkflowTests.cpp"
      provides: "No-placeholder RED test, reslice invalidation test, export-stability test, page-switch preservation test"
      contains: "resetPreviewState"
  key_links:
    - from: "tests/ViewModelSmokeTests.cpp"
      to: "src/core/viewmodels/PreviewViewModel"
      via: "PreviewViewModel API (toggleRoleVisibility, gcodePreviewData, legendItems, currentGcodeLine)"
      pattern: "gcodePreviewData"
    - from: "tests/QmlUiAuditTests.cpp"
      to: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      via: "source-audit grep over computePreviewDrawRange"
      pattern: "computePreviewDrawRange"
    - from: "tests/E2EWorkflowTests.cpp"
      to: "src/core/services/SliceService"
      via: "sliceFinished / sliceFailed / sliceResultCleared signals + live slice fixture path"
      pattern: "sliceFinished"
---

<objective>
Land the regression-test coverage for GCODE-01 (no-placeholder real-data path), GCODE-03 (legend + G-code-text coherence), GCODE-04 (D3D11/SoftwareViewport guards), and GCODE-05 (payload-survives-interaction, reslice invalidation, export stability, page-switch preservation). These are deterministic, headless tests — visual UAT for stability is deferred to Phase 58 per 55-VALIDATION.md.

Purpose: Plan 02 closed the data semantics; Plan 03 closed the UI. Plan 04 locks both behind audit and integration tests so the disappearing-preview class of bug cannot regress. This is the heart of "rendering stability" (GCODE-04, GCODE-05) and the no-placeholder guarantee (GCODE-01).

Output: Extended ViewModelSmokeTests.cpp, QmlUiAuditTests.cpp, E2EWorkflowTests.cpp with new test methods covering every requirement-level row in 55-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-CONTEXT.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-02-SUMMARY.md

@src/core/viewmodels/PreviewViewModel.h
@src/qml_gui/Renderer/RhiViewportRenderer.cpp
@tests/ViewModelSmokeTests.cpp
@tests/QmlUiAuditTests.cpp
@tests/E2EWorkflowTests.cpp

<interfaces>
<!-- Plan 02 ViewModel/Renderer API these tests assert against. All added by Plan 02. -->
PreviewViewModel Q_INVOKABLE: bool isRoleVisible(int roleIndex) const;
PreviewViewModel Q_INVOKABLE: void toggleRoleVisibility(int roleIndex);
PreviewViewModel Q_PROPERTY: QVariantList roleVisibilities (NOTIFY stateChanged)
PreviewViewModel Q_PROPERTY: QByteArray gcodePreviewData (NOTIFY stateChanged) — already exists.
PreviewViewModel Q_INVOKABLE: bool loadGCodeForPreview(const QString &filePath) — already exists; used by tests to load tests/fixtures/orca_sample.gcode.
PreviewViewModel: void resetPreviewState() — already private; tests verify observable effect (gcodePreviewData empty, layerCount==0).

Renderer source-audit targets (QmlUiAuditTests reads these as text):
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp must contain a `computePreviewDrawRange` definition with a role-skip block matching `m_roleVisibility`.
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp must contain `static_assert(sizeof(GcvPackedSegment) == 76`.
  - src/qml_gui/pages/PreviewPage.qml must NOT contain the literal "SoftwareViewport".

SliceService signals (E2EWorkflowTests already wires these):
  sliceFinished(), sliceFailed(QString), sliceResultCleared(), resultChanged() — already present.

E2EWorkflowTests helper (already exists at lines 18-52 of E2EWorkflowTests.cpp):
  int gcv1SegmentCount(const QByteArray &payload) — returns -1 if payload doesn't start with "GCV1".
  const QString kStlPath = .../test_3mf/Prusa.stl — live-slice fixture path.

<!-- New test method slots to add (one per VALIDATION.md row). -->
ViewModelSmokeTests new slots:
  - roleVisibilityToggleDoesNotRepackGcodePreviewData()
  - legendGradientBoundsStableAcrossLayerMoveDrag()
  - currentMoveUpdatesGcodeLineWindowAtomically()
  - viewModesExposeUpstreamSeventeenModes() // belt-and-suspenders vs PreviewParserTests
QmlUiAuditTests new slots:
  - previewPageNeverReferencesSoftwareViewport()
  - rhiViewportRendererComputePreviewDrawRangeAppliesRoleFilter()
  - rhiViewportRendererHasGcvPackedSegmentRoleGuard()
E2EWorkflowTests new slots:
  - previewReceivesRealGcodeAfterLiveSliceNoPlaceholder()  // GCODE-01 RED
  - sliceFailedClearsPreviewState()
  - sliceResultClearedClearsPreviewState()
  - plateSwitchToInvalidClearsPreviewState()
  - resliceRebuildsGcodePreviewDataWithDifferentBytes()
  - exportWhilePreviewVisibleLeavesGcodePreviewDataIntact()
  - pageSwitchPreparePreviewPreservesGcodePreviewData()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ViewModelSmokeTests for role-toggle no-repack, legend coherence, current-line atomicity</name>
  <files>tests/ViewModelSmokeTests.cpp</files>
  <read_first>
    - tests/ViewModelSmokeTests.cpp (full file — locate the ViewModelSmokeTests class declaration around line 117, the private slots block, the PreviewViewModel construction pattern around lines 1890-1910, and QSignalSpy usage around lines 248/1902)
    - src/core/viewmodels/PreviewViewModel.h (full file — gcodePreviewData(), layerCount(), currentLayerMin/Max(), moveCount(), currentMove(), currentGcodeLine(), gcodeLines(), legendItems_, roleVisibilities(), isRoleVisible(), toggleRoleVisibility(), setLayerRange(), setCurrentMove())
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (Requirement-Level Verification Map rows for GCODE-02/03 — exact behaviors to assert)
    - tests/fixtures/orca_sample.gcode (the Plan 01 fixture to load via loadGCodeForPreview)
  </read_first>
  <behavior>
    - roleVisibilityToggleDoesNotRepackGcodePreviewData: construct PreviewViewModel, loadGCodeForPreview(tests/fixtures/orca_sample.gcode), capture QByteArray before = vm.gcodePreviewData(); vm.toggleRoleVisibility(1 /*Perimeter*/); capture QByteArray after = vm.gcodePreviewData(); QVERIFY(before == after); QVERIFY(before.size() > 8); QVERIFY(gcv1SegmentCount(before) > 0). Use the existing gcv1SegmentCount helper (lines 18-52 region).
    - legendGradientBoundsStableAcrossLayerMoveDrag: load fixture, setViewModeIndex to a gradient mode (e.g. the new VT_FanSpeed index per Plan 02 enum — use the named index, not a hardcoded number), capture legendGradientMinLabel/MaxLabel; vm.setLayerRange(0, 1); vm.setCurrentMove(2); labels must be unchanged. (Asserts global-scope legend per CONTEXT.md.)
    - currentMoveUpdatesGcodeLineWindowAtomically: load fixture, QSignalSpy spy(&vm, &PreviewViewModel::stateChanged); vm.setCurrentMove(N); QVERIFY(spy.count() == 1); QVERIFY(vm.currentGcodeLine() != 0); QVERIFY(!vm.gcodeLines().isEmpty()). The single stateChanged emission proves atomicity.
    - viewModesExposeUpstreamSeventeenModes: QVERIFY(vm.viewModes().size() == 17); QVERIFY(vm.viewModes().contains("Line Type")); QVERIFY(vm.viewModes().contains("Summary")); QVERIFY(vm.viewModes().contains("Tool")). (Belt-and-suspenders alongside PreviewParserTests — same upstream contract.)
  </behavior>
  <action>
    Add four private slots to the ViewModelSmokeTests class declaration (near the existing slots block around line 121). Implement each per <behavior>. Use QSignalSpy for stateChanged emissions. Use QDir(QT_TESTCASE_SOURCEDIR).filePath("tests/fixtures/orca_sample.gcode") for the fixture path (same pattern as QmlUiAuditTests::readSource).

    For legendGradientBoundsStableAcrossLayerMoveDrag: choose a gradient mode by name, not number — call vm.setViewModeIndex(vm.viewModes().indexOf("Fan Speed")) so the test survives the Plan 02 renumbering. Capture labels BEFORE any layer/move change, then change layer range + currentMove, capture labels AFTER, QVERIFY equal.

    For roleVisibilityToggleDoesNotRepackGcodePreviewData: QVERIFY2(before == after, "toggleRoleVisibility must not mutate gcodePreviewData_ (render-side filter only)"). This is the GCODE-02 + Phase 41 invariant — the single most important regression guard for the disappearing-preview bug.

    Encoding: ASCII/UTF-8 without BOM. English ASCII comments. Project comment convention: `// Phase 55 (GCODE-03): <why this assertion exists>` before each new test method.

    AUTOMOC caveat: ViewModelSmokeTests already has many Q_OBJECT slots; adding 4 more is fine but the executor must delete build/ViewModelSmokeTests_autogen/timestamp if a new slot fails to moc in incremental builds (note as a top-of-file comment per 55-PATTERNS.md AUTOMOC note).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "roleVisibilityToggleDoesNotRepackGcodePreviewData\|legendGradientBoundsStableAcrossLayerMoveDrag\|currentMoveUpdatesGcodeLineWindowAtomically\|viewModesExposeUpstreamSeventeenModes" tests/ViewModelSmokeTests.cpp returns >= 4 (2x for decl + impl).
    - `ctest --output-on-failure -R ViewModelSmoke` passes with all four new test methods green.
    - The no-repack assertion is a hard QVERIFY2 (not QEXPECT_FAIL).
  </acceptance_criteria>
  <done>Four new ViewModelSmokeTests methods green; role-toggle no-repack is the central guard; legend/global-scope + currentMove atomicity locked.</done>
</task>

<task type="auto">
  <name>Task 2: Extend QmlUiAuditTests for SoftwareViewport guard, role-skip guard, sizeof guard</name>
  <files>tests/QmlUiAuditTests.cpp</files>
  <read_first>
    - tests/QmlUiAuditTests.cpp (full file — class declaration lines 8-48, readSource helper lines 50-56, existing tests mainRegistersRhiViewportByDefaultWithSoftwareFallback / mainRegistersRhiViewportOnlyBehindExplicitGate / previewRhiRendererBindsPreviewStateAndUsesExactDrawSpans / previewRhiInteractionSettersPreservePreviewPayload for the assertion-style pattern)
    - src/qml_gui/pages/PreviewPage.qml (confirm no SoftwareViewport string)
    - src/qml_gui/Renderer/RhiViewportRenderer.cpp (confirm computePreviewDrawRange contains the m_roleVisibility skip block and the static_assert(sizeof(GcvPackedSegment) == 76) line — both added by Plan 02)
    - src/qml_gui/main_qml.cpp (the RhiViewport registration block guarded by an env var — already covered by existing tests, do not duplicate)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (GCODE-04 rows)
  </read_first>
  <action>
    Add three private slots near the existing previewRhi* slots (around lines 25-32):

    1. previewPageNeverReferencesSoftwareViewport(): use readSource("src/qml_gui/pages/PreviewPage.qml"); QVERIFY2(!src.contains("SoftwareViewport"), "PreviewPage.qml must not reference SoftwareViewport (GCODE-04 no-regression)");

    2. rhiViewportRendererComputePreviewDrawRangeAppliesRoleFilter(): readSource("src/qml_gui/Renderer/RhiViewportRenderer.cpp"); QVERIFY2(src.contains("computePreviewDrawRange"), ...); QVERIFY2(src.contains("m_roleVisibility"), "computePreviewDrawRange must consult m_roleVisibility for render-side role filtering (Plan 02 contract)"); Optionally extract the function body and verify it contains a `!m_roleVisibility` skip pattern.

    3. rhiViewportRendererHasGcvPackedSegmentRoleGuard(): readSource("src/qml_gui/Renderer/RhiViewportRenderer.cpp"); QVERIFY2(src.contains("static_assert(sizeof(GcvPackedSegment) == 76"), "GcvPackedSegment must have a compile-time sizeof==76 guard (Plan 02 wire-format lockstep)");

    Each test uses the existing readSource helper. Use QVERIFY2 with descriptive failure messages. Add a Phase 55 (GCODE-04/05) comment header to each.

    Encoding: ASCII/UTF-8 without BOM. AUTOMOC caveat applies — note in top-of-file comment.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "previewPageNeverReferencesSoftwareViewport\|rhiViewportRendererComputePreviewDrawRangeAppliesRoleFilter\|rhiViewportRendererHasGcvPackedSegmentRoleGuard" tests/QmlUiAuditTests.cpp returns >= 6 (3x for decl + impl).
    - `ctest --output-on-failure -R QmlUiAudit` passes with all three new tests green.
    - The previewPageNeverReferencesSoftwareViewport test FAILS the build if anyone later adds SoftwareViewport to PreviewPage.qml (this is the GCODE-04 regression gate).
  </acceptance_criteria>
  <done>Three source-audit tests green; SoftwareViewport never appears in PreviewPage.qml; computePreviewDrawRange role-skip and GcvPackedSegment sizeof guards are enforced at test time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend E2EWorkflowTests for no-placeholder, reslice, export, page-switch invariants</name>
  <files>tests/E2EWorkflowTests.cpp</files>
  <read_first>
    - tests/E2EWorkflowTests.cpp (full file — locate the namespace helpers gcv1SegmentCount + kStlPath at lines 18-52; the existing preview parser test around lines 540-580; the existing previewRhiInteractionSettersPreservePreviewPayload test if present; the sliceFinished/sliceFailed QSignalSpy pattern around lines 1900-1910)
    - src/core/services/SliceService.h (sliceFinished, sliceFailed, sliceResultCleared, resultChanged signals; outputPath() getter)
    - src/core/viewmodels/PreviewViewModel.h (syncPreviewWithActiveResult is private; tests verify observable behavior: gcodePreviewData non-empty/empty, layerCount, moveCount)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md (all GCODE-01 + GCODE-05 rows)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-CONTEXT.md (Section "Real-data-path & no-placeholder verification" + "Interaction regression & renderer stability tests")
  </read_first>
  <behavior>
    - previewReceivesRealGcodeAfterLiveSliceNoPlaceholder: run a deterministic slice over kStlPath (the existing pattern), wait for sliceFinished via QSignalSpy, then QVERIFY(vm.gcodePreviewData().size() > 8); QVERIFY(gcv1SegmentCount(vm.gcodePreviewData()) > 0); QVERIFY(vm.layerCount() > 0); QVERIFY(vm.moveCount() > 0); QVERIFY2(!vm.gcodePreviewData().contains("demo") && !vm.gcodePreviewData().contains("sample") && !vm.gcodePreviewData().contains("placeholder"), "no placeholder/demo/sample marker bytes in live-slice GCV1 payload"). The slice output path must be SliceService::outputPath() (real local file), not a fixture.
    - sliceFailedClearsPreviewState: trigger a slice, immediately cancel/fail it (or use a deliberately invalid input that emits sliceFailed), QVERIFY(sliceFailed spy fires); QVERIFY(vm.gcodePreviewData().isEmpty()); QVERIFY(vm.layerCount() == 0).
    - sliceResultClearedClearsPreviewState: emit SliceService::sliceResultCleared (or call the existing clear path); assert gcodePreviewData empty + layerCount 0.
    - plateSwitchToInvalidClearsPreviewState: simulate plate switch to an invalid/empty plate; assert gcodePreviewData empty.
    - resliceRebuildsGcodePreviewDataWithDifferentBytes: slice once, capture payload bytes + counts; change a setting that invalidates; slice again; QVERIFY(newPayload != oldPayload); QVERIFY(vm.layerCount() > 0); QVERIFY(vm.moveCount() > 0). (Defends GCODE-05 reslice invariant.)
    - exportWhilePreviewVisibleLeavesGcodePreviewDataIntact: slice, capture payload; invoke the export path (Phase 42 export wiring — invoke via the existing export entry point used by E2E tests); QVERIFY(vm.gcodePreviewData() == capturedPayload). (Defends GCODE-05 export invariant.)
    - pageSwitchPreparePreviewPreservesGcodePreviewData: slice, capture payload; simulate Prepare->Preview->Prepare page switch via the existing BackendContext view-mode entry point; QVERIFY(vm.gcodePreviewData() == capturedPayload). (Defends GCODE-05 page-switch invariant — the original disappearing-preview bug class.)
  </behavior>
  <action>
    Add seven private slots to the E2EWorkflowTests class. Reuse the existing QSignalSpy + gcv1SegmentCount + kStlPath helpers. For SliceService lifecycle signals, follow the sliceFinished/sliceFailed spy pattern at lines 1902-1903.

    For tests that need to drive the slice to completion deterministically (previewReceivesRealGcodeAfterLiveSliceNoPlaceholder, resliceRebuildsGcodePreviewDataWithDifferentBytes): use the same slice-trigger pattern the existing tests use (likely SliceService::requestSlice or BackendContext bridge); use QSignalSpy::wait() with a generous timeout (e.g. 30000ms) since slicing is async via Qt Concurrent.

    For plateSwitchToInvalidClearsPreviewState and pageSwitchPreparePreviewPreservesGcodePreviewData: use the existing BackendContext or EditorViewModel entry point that the existing E2E tests use for plate/page transitions. If no such entry point is wired in the test, document the gap in a comment and use QSKIP with a clear message rather than skipping silently (do NOT mark as passing without verification — source-truth honesty rule).

    The no-placeholder assertion is the GCODE-01 RED test from CONTEXT.md — it MUST be a hard QVERIFY2, not QEXPECT_FAIL.

    Encoding: ASCII/UTF-8 without BOM. AUTOMOC caveat applies — note in top-of-file comment.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "previewReceivesRealGcodeAfterLiveSliceNoPlaceholder\|sliceFailedClearsPreviewState\|sliceResultClearedClearsPreviewState\|plateSwitchToInvalidClearsPreviewState\|resliceRebuildsGcodePreviewDataWithDifferentBytes\|exportWhilePreviewVisibleLeavesGcodePreviewDataIntact\|pageSwitchPreparePreviewPreservesGcodePreviewData" tests/E2EWorkflowTests.cpp returns >= 14 (2x for decl + impl of 7 methods).
    - `ctest --output-on-failure -R E2EWorkflow` passes (or any QSKIP is documented with a Phase 58 follow-up owner per source-truth rules — never a silent pass).
    - The no-placeholder test asserts slice output comes from SliceService::outputPath() (real local file), NOT from tests/fixtures/.
    - previewReceivesRealGcodeAfterLiveSliceNoPlaceholder asserts gcodePreviewData does not contain "demo", "sample", or "placeholder" substrings.
  </acceptance_criteria>
  <done>Seven new E2EWorkflowTests methods covering GCODE-01 no-placeholder and GCODE-05 reslice/export/page-switch/sliceFailed/sliceResultCleared/plate-switch-invalid invariants. Any method that cannot be fully driven is QSKIP'd with a Phase 58 owner note, not silently passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local test fixture -> test process | Test reads committed fixture and local slice output; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04-SC | Tampering | none — no package installs | accept | All test code is project-local C++ linking Qt6::Test and owzx_app_core. |
| T-55-04-01 | Repudiation | test coverage gaps | mitigate | Any invariant that cannot be driven deterministically MUST be QSKIP'd with a Phase 58 owner note (source-truth honesty). Silent passes are forbidden — the no-placeholder assertion is a hard QVERIFY2. |

No high-severity threats. No network, auth, or persistence surface.
</threat_model>

<verification>
- Canonical build script passes (all three test targets compile and link after extensions).
- `ctest --output-on-failure` (full suite) — all new test methods green or explicitly QSKIP'd with documented owner.
- The no-placeholder RED test (previewReceivesRealGcodeAfterLiveSliceNoPlaceholder) is GREEN and asserts all four conditions: non-empty GCV1, layerCount>0, moveCount>0, no demo/sample/placeholder marker bytes.
- The role-toggle no-repack guard (roleVisibilityToggleDoesNotRepackGcodePreviewData) is GREEN and is a hard QVERIFY2.
</verification>

<success_criteria>
- GCODE-01 closed: no-placeholder real-data path is regression-tested end-to-end.
- GCODE-03 closed: legend/global-scope + currentMove/G-code-text atomicity are locked.
- GCODE-04 closed: PreviewPage.qml SoftwareViewport absence is guarded at test time (Plan 05 adds the final D3D11 startup-policy guard).
- GCODE-05 closed: reslice/export/page-switch/slice-failed invariants are all under test.
- VALIDATION.md "Requirement-Level Verification Map" — every GCODE-01..05 row has a green automated command.
</success_criteria>

<output>
Create `.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-04-SUMMARY.md` when done.
</output>
