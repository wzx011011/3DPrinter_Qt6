---
phase: 55-g-code-preview-semantics-and-rendering-stability
plan: 03
type: execute
wave: 2
depends_on: [55-02]
files_modified:
  - src/qml_gui/components/VisibilityFilter.qml
  - src/qml_gui/pages/PreviewPage.qml
  - src/qml_gui/qml.qrc
autonomous: true
requirements: [GCODE-02]
user_setup: []

must_haves:
  truths:
    - "A VisibilityFilter.qml component exists, is registered in qml.qrc, and renders a collapsible 'Visible Line Types' group inside the right panel"
    - "Each role row binds a CxCheckBox to previewVm.isRoleVisible(index) and calls previewVm.toggleRoleVisibility(index) on change"
    - "The GLViewport in PreviewPage.qml is bound to previewVm.roleVisibilities via the roleVisibility property (render-side filtering fires)"
    - "No raw CheckBox/Switch/ScrollView in VisibilityFilter.qml — only Cx* controls and Theme tokens (project CLAUDE.md rule)"
    - "All user-visible strings use qsTr() with the English/Chinese copywriting contract from 55-UI-SPEC.md"
  artifacts:
    - path: "src/qml_gui/components/VisibilityFilter.qml"
      provides: "Collapsible per-role line-type visibility checkbox group"
      contains: "CxCheckBox"
    - path: "src/qml_gui/pages/PreviewPage.qml"
      provides: "VisibilityFilter insertion between StatsPanel and Legend + roleVisibility binding to GLViewport"
      contains: "Components.VisibilityFilter"
    - path: "src/qml_gui/qml.qrc"
      provides: "components/VisibilityFilter.qml resource registration"
      contains: "<file>components/VisibilityFilter.qml</file>"
  key_links:
    - from: "src/qml_gui/components/VisibilityFilter.qml"
      to: "src/core/viewmodels/PreviewViewModel"
      via: "previewVm.isRoleVisible / previewVm.toggleRoleVisibility"
      pattern: "toggleRoleVisibility"
    - from: "src/qml_gui/pages/PreviewPage.qml"
      to: "src/qml_gui/Renderer/RhiViewport"
      via: "GLViewport.roleVisibility binding"
      pattern: "roleVisibility"
---

<objective>
Bind the render-side per-role visibility API from Plan 02 to user-visible UI: a collapsible "Visible Line Types" group in the Preview right panel, with one CxCheckBox per upstream extrusion role, and a QML binding that pushes previewVm.roleVisibilities into GLViewport.roleVisibility so render-side filtering fires on every toggle.

Purpose: GCODE-02 requires the user-visible filter UI to match upstream GCodeViewer's right-side visibility list and the screenshot. CONTEXT.md locks the placement (right panel, between StatsPanel and Legend) and the mechanism (render-side, no repack — already implemented by Plan 02). This plan delivers only the presentation + wiring; all business logic stays in PreviewViewModel/RhiViewport per QML boundary rules.

Output: New VisibilityFilter.qml, modified PreviewPage.qml (insertion + roleVisibility binding), qml.qrc registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-CONTEXT.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-UI-SPEC.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-02-SUMMARY.md

@src/qml_gui/components/StatsPanel.qml
@src/qml_gui/components/Legend.qml
@src/qml_gui/components/CollapsibleSection.qml
@src/qml_gui/controls/CxCheckBox.qml
@src/qml_gui/pages/PreviewPage.qml
@src/qml_gui/qml.qrc
@src/qml_gui/Theme.qml

<interfaces>
<!-- Plan 02 ViewModel API the UI binds to. Source: Plan 02 Task 1 <behavior>. -->

Q_PROPERTY (PreviewViewModel.h, added by Plan 02):
  Q_PROPERTY(QVariantList roleVisibilities READ roleVisibilities NOTIFY stateChanged)

roleVisibilities() returns QVariantList of QVariantMap rows. Each row (per 55-UI-SPEC "ViewModel API Additions"):
  {
    "roleIndex": int,     // 1..19 upstream EGCodeExtrusionRole index (excludes 0 None; 18 Custom hidden)
    "label": QString,     // English upstream name e.g. "Inner wall"
    "color": QString,     // "#RRGGBB" role color from DEFAULT_EXTRUSION_ROLES_COLORS
    "visible": bool       // current visibility from m_roleVisibility[roleIndex]
  }

Q_INVOKABLE (PreviewViewModel.h, added by Plan 02):
  bool isRoleVisible(int roleIndex) const;
  void toggleRoleVisibility(int roleIndex);

RhiViewport property (added by Plan 02):
  Q_PROPERTY(QVariantList roleVisibility READ roleVisibility WRITE setRoleVisibility NOTIFY roleVisibilityChanged)
  // Expects a flat QVariantList of bool (length 20). Plan 02 initializes to all-true.

<!-- Existing UI patterns to mirror. Source: 55-PATTERNS.md + listed component files. -->
StatsPanel.qml structure: Item root + required property var previewVm + implicitHeight binding.
Legend.qml Repeater: model: root.previewVm ? root.previewVm.legendItems : []; delegate: RowLayout { Rectangle{width:10;height:10;radius:2;color:modelData.color} Label{text:modelData.label} }.
StatsPanel.qml toggle row: ToggleRow { label: qsTr("显示空驶"); checked: previewVm.showTravelMoves; onChanged: previewVm.setShowTravelMoves(checked) }.
PreviewPage.qml insertion point (around lines 362-372): the right-panel ColumnLayout containing Components.StatsPanel and Components.Legend.
qml.qrc qresource block contains existing <file>components/StatsPanel.qml</file> and <file>components/Legend.qml</file> around lines 60-66.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VisibilityFilter.qml + register in qml.qrc</name>
  <files>src/qml_gui/components/VisibilityFilter.qml, src/qml_gui/qml.qrc</files>
  <read_first>
    - src/qml_gui/components/StatsPanel.qml (full file — Item root, required property var previewVm, ColumnLayout with anchors.margins: 9, spacing: 6)
    - src/qml_gui/components/Legend.qml (full file — Repeater + colored Rectangle + Label pattern to mirror for role rows)
    - src/qml_gui/components/CollapsibleSection.qml (full file — section header with collapse/expand; the right-panel card wrapper)
    - src/qml_gui/controls/CxCheckBox.qml (full file — CxCheckBox API: checked, onChanged/onCheckedChanged signal)
    - src/qml_gui/Theme.qml (token names: bgPanel, bgElevated, borderSubtle, textPrimary, textSecondary, fontSizeSM, fontSizeXS, spacingSM, spacingMD)
    - src/qml_gui/qml.qrc (full file — locate the <qresource prefix="/"> block with components/StatsPanel.qml and components/Legend.qml around lines 60-66)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-UI-SPEC.md (Section "VisibilityFilter Component Specification" + "Copywriting Contract" — exact qsTr strings and the 18-row role list with default-true visibility)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md (Section "src/qml_gui/components/VisibilityFilter.qml (NEW component, request-response)" — full pattern catalog)
  </read_first>
  <action>
    Create src/qml_gui/components/VisibilityFilter.qml mirroring StatsPanel.qml's structure (Item root + `required property var previewVm` + implicitHeight binding). Inside, wrap everything in a CollapsibleSection with title `qsTr("可见线条类型")` (English source: "Visible Line Types") per 55-UI-SPEC copywriting contract. Default expanded.

    Body content:
    - A ColumnLayout (anchors fill the section content, spacing: Theme.spacingSM = 6) containing a Repeater.
    - Repeater model: `root.previewVm ? root.previewVm.roleVisibilities : []` (each row is the QVariantMap from <interfaces>).
    - Delegate: RowLayout with Layout.fillWidth: true, spacing: Theme.spacingXS (4). Contents in this exact left-to-right order:
        (a) Rectangle (width: 10, height: 10, radius: 2, color: modelData.color) — the role's FeatureType color swatch.
        (b) Label (Layout.fillWidth: true, text: qsTr(modelData.label), color: Theme.textPrimary, font.pixelSize: Theme.fontSizeSM, elide: Text.ElideRight). Use qsTr() with the English source string from 55-UI-SPEC "Copywriting Contract" (Inner wall / Outer wall / Overhang wall / Sparse infill / Internal solid infill / Top surface / Ironing / Bridge / Gap infill / Skirt / Support / Support interface / Prime tower / Bottom surface / Internal bridge / Brim / Support transition / Mixed).
        (c) CxCheckBox bound to `checked: modelData.visible` with `onChanged: if (root.previewVm) root.previewVm.toggleRoleVisibility(modelData.roleIndex)`. Use CxCheckBox, NOT a raw Controls CheckBox (project CLAUDE.md control rule).
    - Row height: 22 (matching existing G-code text row height in PreviewPage.qml per 55-UI-SPEC).
    - Row hover feedback: Rectangle background with color: Theme.bgHover when MouseArea contains mouse (use a HoverHandler on the row, project debugging.md notes HoverHandler works on Items).

    All visual values MUST come from Theme tokens. No ad-hoc colors, font sizes, or spacings.

    Encoding: ASCII/UTF-8 without BOM. English ASCII comments only (project CLEAN-04 rule).

    qml.qrc registration: add `<file>components/VisibilityFilter.qml</file>` to the existing `<qresource prefix="/">` block, immediately after `<file>components/Legend.qml</file>` (around line 60-66). Do not create a new qresource block.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - src/qml_gui/components/VisibilityFilter.qml exists, ASCII/UTF-8 without BOM.
    - grep -c "CxCheckBox" src/qml_gui/components/VisibilityFilter.qml returns >= 1; grep -c "Controls.CheckBox\|CheckBox {" returns 0 (no raw CheckBox — only the Cx prefix control).
    - grep -c "qsTr" src/qml_gui/components/VisibilityFilter.qml returns >= 1 (the section header at minimum; the role-label Repeater uses qsTr(modelData.label) which the 18-row role contract guarantees via the QVariantMap).
    - grep -c "Theme\." src/qml_gui/components/VisibilityFilter.qml returns >= 4 (Theme tokens for color/font/spacing — no ad-hoc values).
    - grep -c "toggleRoleVisibility" src/qml_gui/components/VisibilityFilter.qml returns >= 1 (the CxCheckBox onChanged handler).
    - grep -c "<file>components/VisibilityFilter.qml</file>" src/qml_gui/qml.qrc returns 1.
    - `ctest --output-on-failure -R QmlUiAudit` passes (no new raw-control violations, no qsTr regressions).
    - Canonical build script succeeds (QML compiles via qmlcachegen without warnings).
  </acceptance_criteria>
  <done>VisibilityFilter.qml component committed, registered in qml.qrc, uses only Cx* controls + Theme tokens, qsTr applied to user-visible strings, mirrors StatsPanel/Legend patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Wire VisibilityFilter into PreviewPage.qml + bind roleVisibility to GLViewport</name>
  <files>src/qml_gui/pages/PreviewPage.qml</files>
  <read_first>
    - src/qml_gui/pages/PreviewPage.qml (full file — locate the right-panel ScrollView ColumnLayout containing Components.StatsPanel and Components.Legend around lines 362-372; locate the GLViewport instance binding previewData/layerMin/layerMax/moveEnd/showTravelMoves around the canvas section; locate the `import "../components" as Components` line)
    - src/qml_gui/components/StatsPanel.qml (how it consumes previewVm)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-UI-SPEC.md (Section "Location" — insertion between StatsPanel and Legend; "Behavior" — VisibilityFilter is always visible regardless of view-mode)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md (Section "src/qml_gui/pages/PreviewPage.qml (MODIFY page, request-response)" — insertion snippet)
  </read_first>
  <action>
    Two edits in src/qml_gui/pages/PreviewPage.qml:

    Edit A — insert VisibilityFilter between StatsPanel and Legend in the right-panel ColumnLayout. The exact insertion target is the line range where `Components.StatsPanel { ... }` is followed by `Components.Legend { ... }`. Insert `Components.VisibilityFilter { Layout.fillWidth: true; previewVm: root.previewVm }` between them. Do not change StatsPanel or Legend blocks. The `import "../components" as Components` line is already present (line 7 per 55-PATTERNS) — do not duplicate it.

    Edit B — add a `roleVisibility: root.previewVm ? root.previewVm.roleVisibilities : []` binding to the GLViewport instance that already binds `previewData`, `layerMin`, `layerMax`, `moveEnd`, `showTravelMoves`. The GLViewport is the RhiViewport registered as the `GLViewport` QML type. Plan 02 added the `Q_PROPERTY(QVariantList roleVisibility WRITE setRoleVisibility)` so this binding triggers the no-repack render-side filter.

    Implementation rules:
    - Do NOT introduce any new business logic in PreviewPage.qml (QML boundary rule — filtering lives in ViewModel/Renderer).
    - Do NOT add a second CxScrollView; VisibilityFilter lives inside the existing right-panel ScrollView.
    - All bindings must null-guard `root.previewVm` to avoid QML warnings during early init.
    - Keep ASCII/UTF-8 without BOM; no Chinese in code comments (qsTr strings are fine).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Components.VisibilityFilter" src/qml_gui/pages/PreviewPage.qml returns >= 1.
    - grep -c "roleVisibility:" src/qml_gui/pages/PreviewPage.qml returns >= 1 (the GLViewport binding).
    - grep -c "SoftwareViewport" src/qml_gui/pages/PreviewPage.qml returns 0 (GCODE-04 invariant preserved — already guarded by existing QmlUiAudit test).
    - `ctest --output-on-failure -R QmlUiAudit` passes (existing PreviewPage audit tests including previewLayoutRestoresScreenshotRegionsAndGcodePanel and previewStatsPanelCallsOnlyQmlInvokableSetters still pass).
    - Canonical build script succeeds; QML loads without "roleVisibility is undefined" or "VisibilityFilter is not a type" warnings.
  </acceptance_criteria>
  <done>VisibilityFilter renders between StatsPanel and Legend; GLViewport.roleVisibility bound to previewVm.roleVisibilities; no new QML warnings; no business logic added to PreviewPage.qml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure QML presentation layer; no untrusted input, no network, no persistence. All state is owned by PreviewViewModel (Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03-SC | Tampering | none — no package installs | accept | No third-party packages; VisibilityFilter uses only project-local Cx* controls and Theme tokens. |
| T-55-03-01 | Elevation / Tampering | QML toggle handler | accept | toggleRoleVisibility is invoked through the existing Q_INVOKABLE path; no new privileged surface. Render-side filter operates on already-uploaded GPU buffers; no shader/asset download. |

No high-severity threats. No network, auth, or persistence surface.
</threat_model>

<verification>
- Canonical build passes (qmlcachegen compiles VisibilityFilter.qml + PreviewPage.qml edits without warnings).
- `ctest --output-on-failure -R QmlUiAudit` — existing PreviewPage layout/wiring audits remain green.
- Manual-only verification deferred to Phase 58 UAT per 55-VALIDATION.md (VisibilityFilter visual parity + interaction).
</verification>

<success_criteria>
- GCODE-02 UI complete: user can toggle per-role line-type visibility from the Preview right panel; toggling fires render-side filtering via the GLViewport.roleVisibility binding; no repack occurs (already enforced by Plan 02 + Plan 04 audit tests).
- UI matches 55-UI-SPEC: collapsible section header "可见线条类型", 18 role rows in upstream order, each with color swatch + qsTr label + CxCheckBox, default expanded.
- QML boundary rule respected: no business logic in PreviewPage.qml or VisibilityFilter.qml.
</success_criteria>

<output>
Create `.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-03-SUMMARY.md` when done.
</output>
