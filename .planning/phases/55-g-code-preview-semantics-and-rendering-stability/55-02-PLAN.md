---
phase: 55-g-code-preview-semantics-and-rendering-stability
plan: 02
type: execute
wave: 1
depends_on: [55-01]
files_modified:
  - src/core/viewmodels/PreviewViewModel.h
  - src/core/viewmodels/PreviewViewModel.cpp
  - src/qml_gui/Renderer/RhiViewport.h
  - src/qml_gui/Renderer/RhiViewport.cpp
  - src/qml_gui/Renderer/RhiViewportRenderer.h
  - src/qml_gui/Renderer/RhiViewportRenderer.cpp
autonomous: true
requirements: [GCODE-01, GCODE-02]
user_setup: []

must_haves:
  truths:
    - "PreviewViewModel parses ;TYPE: comments into 20 EGCodeExtrusionRole indices (0..19) using the exact upstream strings from ExtrusionEntity::role_to_string"
    - "StoredSegment, PackedSegment, and GcvPackedSegment each carry an int role field in identical layout (76 bytes packed)"
    - "PreviewDrawSpan carries int role; computePreviewDrawRange skips spans whose role is masked off (render-side filter, no repack)"
    - "viewModes() returns the 17 upstream EViewType names in upstream order; PreviewParserTests::test_view_modes_match_upstream_seventeen is GREEN"
    - "toggleRoleVisibility(i) emits stateChanged() only; it does NOT call recolorAndPackSegments() and does NOT mutate gcodePreviewData_"
    - "setShowTravelMoves() continues to repack (travel visibility is a different mechanism from role visibility)"
    - "PreviewParserTests::test_role_string_mapping_covers_upstream_enum is GREEN"
  artifacts:
    - path: "src/core/viewmodels/PreviewViewModel.h"
      provides: "StoredSegment.role, roleVisibility state, roleVisibilities/isRoleVisible/toggleRoleVisibility API"
      contains: "Q_INVOKABLE void toggleRoleVisibility"
    - path: "src/core/viewmodels/PreviewViewModel.cpp"
      provides: "fine-grained ;TYPE: parser, 17-mode viewModes, role-aware PackedSegment, render-side toggle"
      contains: "Inner wall"
    - path: "src/qml_gui/Renderer/RhiViewport.h"
      provides: "roleVisibility Q_PROPERTY on RhiViewport"
      contains: "Q_PROPERTY(QVariantList roleVisibility"
    - path: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      provides: "GcvPackedSegment.role, PreviewDrawSpan.role, computePreviewDrawRange role skip"
      contains: "static_assert(sizeof(GcvPackedSegment)"
  key_links:
    - from: "src/core/viewmodels/PreviewViewModel.cpp"
      to: "src/qml_gui/Renderer/RhiViewportRenderer.cpp"
      via: "GCV1 wire format (PackedSegment bytes -> GcvPackedSegment bytes)"
      pattern: "int role;"
    - from: "src/qml_gui/pages/PreviewPage.qml"
      to: "src/qml_gui/Renderer/RhiViewport"
      via: "roleVisibility Q_PROPERTY binding (added in Plan 03)"
      pattern: "roleVisibility"
---

<objective>
Close GCODE-01 (real-data path stays real, no placeholder) and GCODE-02 (full upstream 20-role parsing + 17 view modes + render-side per-role visibility) at the data + renderer contract level. UI binding to the new toggles is Plan 03; the audit-test extensions for GCODE-01/03/04/05 are Plan 04.

Purpose: The 55-RESEARCH.md identifies three concrete gaps in the current implementation: (1) `styleFor()` collapses `;TYPE:` into 5 coarse categories instead of upstream's 20 EGCodeExtrusionRole values; (2) StoredSegment/PackedSegment/GcvPackedSegment carry no role field, blocking per-role visibility filtering; (3) viewModes() returns 13 entries vs upstream's 17 EViewType values, with index 9 mislabeled "Flow" vs upstream "Filament". This plan closes all three by extending the GCV1 wire format in lockstep across viewmodel + renderer and replacing the coarse parser with the upstream-aligned fine-grained mapping. Render-side role filtering (no repack) preserves the Phase 41 interaction-stability guarantee that CONTEXT.md locks as a hard constraint.

Output: Modified PreviewViewModel (.h/.cpp), RhiViewport (.h/.cpp), RhiViewportRenderer (.h/.cpp). PreviewParserTests RED slots from Plan 01 flip to GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-CONTEXT.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-01-SUMMARY.md

@src/core/viewmodels/PreviewViewModel.h
@src/core/viewmodels/PreviewViewModel.cpp
@src/qml_gui/Renderer/RhiViewport.h
@src/qml_gui/Renderer/RhiViewportRenderer.h
@src/qml_gui/Renderer/RhiViewportRenderer.cpp
@tests/PreviewParserTests.cpp

<interfaces>
<!-- Existing GCV1 wire-format contract that this plan extends in lockstep. -->

From src/core/viewmodels/PreviewViewModel.cpp:46-66 (anonymous namespace, current 72-byte layout):
```cpp
struct PackedSegment {
  float x1, y1, z1, x2, y2, z2;
  float r, g, b;
  float feedrate, fan_speed, temperature, width, layer_time, acceleration;
  int extruder_id, layer, move;
};  // 72 bytes
```
Target: append `int role;` -> 76 bytes. Role index range 0..19 per upstream EGCodeExtrusionRole.

From src/qml_gui/Renderer/RhiViewportRenderer.cpp:571-577 (must mirror PackedSegment exactly):
```cpp
struct GcvPackedSegment {
  float x1, y1, z1, x2, y2, z2;
  float r, g, b;
  float feedrate, fan_speed, temperature, width, layer_time, acceleration;
  int extruder_id, layer, move;
};
```
Target: append `int role;` -> 76 bytes. Add `static_assert(sizeof(GcvPackedSegment) == 76, "...")` after the struct (55-RESEARCH Pitfall 1, 55-PATTERNS "PackedSegment / GcvPackedSegment Lock-Step").

From src/qml_gui/Renderer/RhiViewportRenderer.h:105-110 (current span):
```cpp
struct PreviewDrawSpan {
  int layer;
  int move;
  quint32 vertexOffset;
  quint32 vertexCount;
};
```
Target: add `int role;` field.

From src/core/viewmodels/PreviewViewModel.h:257-273 (StoredSegment):
```cpp
struct StoredSegment {
  float x1, y1, z1, x2, y2, z2;
  float baseR, baseG, baseB;
  float feedrate, fan_speed, temperature, width, height, layer_time, acceleration, volumetric_rate;
  int extruder_id, layer, move;
  bool isTravel;
};
```
Target: add `int role = 0;` (default None).

Upstream role-string -> index map to implement in PreviewViewModel.cpp (source: third_party/OrcaSlicer/src/libslic3r/ExtrusionEntity.cpp:583-608 and 55-RESEARCH.md Section "Pattern 2"):
  "Inner wall"=1, "Outer wall"=2, "Overhang wall"=3, "Sparse infill"=4,
  "Internal solid infill"=5, "Top surface"=6, "Bottom surface"=7, "Ironing"=8,
  "Bridge"=9, "Internal Bridge"=10, "Gap infill"=11, "Skirt"=12, "Brim"=13,
  "Support"=14, "Support interface"=15, "Support transition"=16,
  "Prime tower"=17, "Custom"=18, "Multiple"=19; default 0 (None) for travel/unrecognized.

Upstream EViewType 17-mode list (source: 55-RESEARCH.md "Corrected view-mode list"; order from upstream update_by_mode):
  0=Summary, 1=Line Type, 2=Filament, 3=Speed, 4=Actual Speed, 5=Acceleration,
  6=Jerk, 7=Layer Height, 8=Line Width, 9=Flow, 10=Actual Flow, 11=Layer Time,
  12=Layer Time (log), 13=Fan Speed, 14=Temperature, 15=Pressure Advance, 16=Tool.

Upstream DEFAULT_EXTRUSION_ROLES_COLORS (source: 55-RESEARCH.md "EGCodeExtrusionRole (20 values)" table — third column). Use these normalized to [0,1] floats for FeatureType-mode base color assignment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GCV1 wire format with int role (viewmodel + renderer lockstep)</name>
  <files>src/core/viewmodels/PreviewViewModel.h, src/core/viewmodels/PreviewViewModel.cpp, src/qml_gui/Renderer/RhiViewport.h, src/qml_gui/Renderer/RhiViewport.cpp, src/qml_gui/Renderer/RhiViewportRenderer.h, src/qml_gui/Renderer/RhiViewportRenderer.cpp</files>
  <read_first>
    - src/core/viewmodels/PreviewViewModel.h (full file — StoredSegment at lines 257-273, members at 207-299)
    - src/core/viewmodels/PreviewViewModel.cpp (lines 46-66 PackedSegment; lines 989-1013 segment construction; lines 1233-1358 recolorAndPackSegments incl. the `seg[i]` packing loop)
    - src/qml_gui/Renderer/RhiViewport.h (full file — Q_PROPERTY block lines 20-55, member block 217-266)
    - src/qml_gui/Renderer/RhiViewport.cpp (lines 72-102 interaction setter pattern)
    - src/qml_gui/Renderer/RhiViewportRenderer.h (lines 97-128 — preview pipeline state, PreviewDrawSpan 105-110)
    - src/qml_gui/Renderer/RhiViewportRenderer.cpp (lines 60-91 synchronize block; lines 570-629 GcvPackedSegment + parsePreviewSegments incl. line 625 span append; lines 648-713 computePreviewDrawRange)
    - third_party/OrcaSlicer/src/libslic3r/ExtrusionEntity.cpp:583-639 (upstream role strings)
    - third_party/OrcaSlicer/src/libvgcode/include/Types.hpp:131-157 (upstream EGCodeExtrusionRole enum)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md (Sections "Render-Side Filtering Integration Points", "Common Pitfalls" 1+5, "Code Examples" — extended struct snippets)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md (Sections on each renderer file; "PackedSegment / GcvPackedSegment Lock-Step"; "Render-Side Skip Pattern")
  </read_first>
  <behavior>
    - PackedSegment and GcvPackedSegment both gain `int role;` as the LAST field; both are exactly 76 bytes; static_assert(sizeof(GcvPackedSegment) == 76) in RhiViewportRenderer.cpp anonymous namespace passes.
    - StoredSegment gains `int role = 0;` member.
    - PreviewDrawSpan gains `int role;` field; parsePreviewSegments populates it from seg[i].role.
    - computePreviewDrawRange skips a span when `span.role >= 0 && span.role < m_roleVisibility.size() && !m_roleVisibility[span.role]` (inserted after the move check at line 695, before the startOffset/endOffset logic).
    - RhiViewport gains Q_PROPERTY(QVariantList roleVisibility READ roleVisibility WRITE setRoleVisibility NOTIFY roleVisibilityChanged) and member QVariantList m_roleVisibility. setRoleVisibility stores and calls update() only (does NOT mutate m_previewData — Phase 41 invariant).
    - RhiViewportRenderer::synchronize reads viewport->m_roleVisibility into a new m_roleVisibility QVector<bool> member alongside the existing m_showTravelMoves read (line 89 region).
    - PreviewViewModel exposes Q_PROPERTY(QVariantList roleVisibilities NOTIFY stateChanged), Q_INVOKABLE bool isRoleVisible(int), Q_INVOKABLE void toggleRoleVisibility(int), and a private std::array<bool, 20> m_roleVisibility (all true by default — matches upstream extrusion_roles_visibility defaults in 55-RESEARCH "EGCodeExtrusionRole" table).
    - toggleRoleVisibility flips m_roleVisibility[i] and emits stateChanged() only. It MUST NOT call recolorAndPackSegments() and MUST NOT mutate gcodePreviewData_.
    - roleVisibilities() returns a QVariantList of {label,color,visible} objects for Plan 03's Repeater (18 entries: roles 1..19 except None(0); "Custom"(18) hidden per UI-SPEC copywriting table).
  </behavior>
  <action>
    Wire-format extension (do this first; everything else depends on it):

    1. PreviewViewModel.h StoredSegment: add `int role = 0;` after `bool isTravel;`. Add to the public API block (near lines 59-63): `Q_PROPERTY(QVariantList roleVisibilities READ roleVisibilities NOTIFY stateChanged)`. Add Q_INVOKABLE declarations near lines 149-155: `Q_INVOKABLE bool isRoleVisible(int roleIndex) const;` and `Q_INVOKABLE void toggleRoleVisibility(int roleIndex);`. Add public getter `QVariantList roleVisibilities() const;` and private member `std::array<bool, 20> m_roleVisibility;` near line 251. Include <array>.
    2. PreviewViewModel.cpp PackedSegment: append `int role;` after `int move;` (lines 46-66). In the recolorAndPackSegments() pack loop (around the existing `seg[i].layer`/`seg[i].move` writes), add `out.role = s.role;`. In the constructor, initialize `m_roleVisibility.fill(true);` before any signal emission.
    3. RhiViewportRenderer.h PreviewDrawSpan: add `int role;`. Add private member `QVector<bool> m_roleVisibility;` near m_showTravelMoves (line 102 region).
    4. RhiViewportRenderer.cpp GcvPackedSegment: append `int role;` after `int move;`. Add `static_assert(sizeof(GcvPackedSegment) == 76, "GcvPackedSegment must be 76 bytes after adding role");` immediately after the struct. In parsePreviewSegments line 625 span append, change `{seg[i].layer, seg[i].move, vertexOffset, 2}` to `{seg[i].layer, seg[i].move, vertexOffset, 2, seg[i].role}`. In synchronize (line 89 region), add `m_roleVisibility` conversion: read viewport->m_roleVisibility (QVariantList of bools) and store as QVector<bool>. In computePreviewDrawRange after the `if (span.move >= m_moveEnd) continue;` line, insert the role-skip block from <behavior>.
    5. RhiViewport.h: add Q_PROPERTY(QVariantList roleVisibility READ roleVisibility WRITE setRoleVisibility NOTIFY roleVisibilityChanged) near line 26. Add `QVariantList roleVisibility() const { return m_roleVisibility; }` `void setRoleVisibility(const QVariantList &value);` near lines 118-119. Add `void roleVisibilityChanged();` to the signals block (line 193-200). Add private member `QVariantList m_roleVisibility;` near line 223.
    6. RhiViewport.cpp: implement setRoleVisibility following the line 72-102 setter pattern — guard against equality, store, call update() only. Do NOT touch m_previewData. Initialize m_roleVisibility to a QVariantList of 20 `true` values in the constructor (so renderer-side filtering is a no-op until QML binds Plan 03's UI).

    Implementation rules (per project CLAUDE.md + 55-RESEARCH):
    - The role index 0 (None) and 18 (Custom) are not shown in roleVisibilities() (UI-SPEC copywriting). They remain in m_roleVisibility array for safe indexing but roleVisibilities() filters them out for the QML Repeater.
    - render-side filtering applies ONLY to extrusion roles. Travel/wipe visibility continues to use the existing repack mechanism (setShowTravelMoves -> recolorAndPackSegments). Do NOT change setShowTravelMoves. (55-RESEARCH Pitfall 5.)
    - Do NOT add `;JERK:` or `;PA:` parsing in this plan (out of scope; fixture has none). Jerk and PressureAdvance view modes added in Task 2 will return a uniform/empty gradient and the ViewModel logs `[Preview] Jerk/PA data unavailable in fixture-driven path` once at first access — this matches 55-RESEARCH Assumptions Log A2.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds via canonical script.
    - grep -n "int role;" src/core/viewmodels/PreviewViewModel.cpp returns >= 1 (PackedSegment). grep -n "static_assert(sizeof(GcvPackedSegment)" src/qml_gui/Renderer/RhiViewportRenderer.cpp returns 1 line asserting == 76.
    - grep -n "m_roleVisibility" src/qml_gui/Renderer/RhiViewportRenderer.cpp returns matches in BOTH the synchronize read AND the computePreviewDrawRange skip block.
    - grep -n "Q_PROPERTY(QVariantList roleVisibility" src/qml_gui/Renderer/RhiViewport.h returns 1.
    - grep -n "Q_INVOKABLE void toggleRoleVisibility" src/core/viewmodels/PreviewViewModel.h returns 1; grep "recolorAndPackSegments" inside toggleRoleVisibility in PreviewViewModel.cpp returns 0 (the toggle must NOT repack — guard against accidental copy of setShowTravelMoves pattern).
    - ViewModelSmokeTests (existing) still passes after the wire format change (the GCV1 blob size grows by 4 bytes per segment; existing payload tests assert `payload.size() >= 8 + count * sizeof(PackedSegment)` patterns via gcv1SegmentCount helper which only reads the count — verify this still holds; if any test hardcodes 72-byte stride, fix it to use sizeof).
    - ctest --output-on-failure passes for the existing test targets QmlUiAudit, ViewModelSmoke, E2EWorkflow (Plan 04 adds the new assertions; this plan must not regress existing ones).
  </acceptance_criteria>
  <done>Wire format extended in lockstep (PackedSegment/GcvPackedSegment 76 bytes, PreviewDrawSpan.role, StoredSegment.role); render-side role skip live in computePreviewDrawRange; PreviewViewModel exposes roleVisibilities/isRoleVisible/toggleRoleVisibility with no-repack guarantee; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace styleFor() with 20-role parser and align viewModes() to 17 upstream modes</name>
  <files>src/core/viewmodels/PreviewViewModel.cpp, tests/PreviewParserTests.cpp</files>
  <read_first>
    - src/core/viewmodels/PreviewViewModel.cpp (lines 105-210 — FeatureStyle/styleFor/parseTaggedValue; lines 228-330 — G-code line parser that calls styleFor and constructs StoredSegment; lines 345-362 viewModes(); lines 1241-1358 recolorAndPackSegments switch on `mode`; lines 1419-1430 legend label switch on `mode`)
    - third_party/OrcaSlicer/src/libslic3r/ExtrusionEntity.cpp:583-639 (string_to_role + role_to_string)
    - third_party/OrcaSlicer/src/libvgcode/src/ViewerImpl.cpp:283-305 (DEFAULT_EXTRUSION_ROLES_COLORS palette — 20 colors)
    - third_party/OrcaSlicer/src/slic3r/GUI/GCodeViewer.cpp:66-103 (get_view_type_string for the 17 EViewType display names) and :1070-1114 (update_by_mode ordering)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md (Sections "Pattern 2: Upstream ;TYPE: Comment Parsing", "Upstream Enum Reference (Definitive)" incl. the corrected 17-mode list, "Anti-Patterns to Avoid")
    - tests/PreviewParserTests.cpp (the RED slots from Plan 01 Task 2 to flip green)
  </read_first>
  <behavior>
    - roleForType("Inner wall") == 1; roleForType("Outer wall") == 2; ... roleForType("Prime tower") == 17; roleForType("Custom") == 18; roleForType("Multiple") == 19; roleForType("") or any unrecognized/travel -> 0 (None).
    - PreviewParserTests::test_role_string_mapping_covers_upstream_enum PASSES (no longer QEXPECT_FAIL).
    - viewModes().size() == 17 and the list equals exactly: ["Summary", "Line Type", "Filament", "Speed", "Actual Speed", "Acceleration", "Jerk", "Layer Height", "Line Width", "Flow", "Actual Flow", "Layer Time", "Layer Time (log)", "Fan Speed", "Temperature", "Pressure Advance", "Tool"].
    - PreviewParserTests::test_view_modes_match_upstream_seventeen PASSES (no longer QEXPECT_FAIL).
    - PreviewParserTests::test_summary_mode_has_no_gradient_legend PASSES: when viewModeIndex_ == 0 (Summary), legendType() returns a value that Legend.qml treats as "no gradient" (use the existing m_legendType=0 discrete convention, or document a sentinel — see action).
    - recolorAndPackSegments() mode-switch no longer indexes by the old 0..12 numbers; it uses a named EViewType-indexed lookup OR the corrected 0..16 indices from 55-RESEARCH "Corrected view-mode list". Mode 6 (Jerk) and mode 15 (Pressure Advance) and mode 4 (Actual Speed) and mode 10 (Actual Flow) emit a uniform/empty gradient with a one-time log when the underlying StoredSegment field is unavailable.
    - FeatureType-mode (Line Type, index 1) recolor uses the upstream DEFAULT_EXTRUSION_ROLES_COLORS palette indexed by role (replaces the current 5-category styleFor palette).
  </behavior>
  <action>
    Parser replacement:

    1. In PreviewViewModel.cpp anonymous namespace, replace the 5-category `FeatureStyle styleFor(const QString &)` function with a fine-grained `int roleForType(const QString &type)` returning the upstream EGCodeExtrusionRole index. Implement as a sorted-or-unsorted `static const struct { const char *name; int role; } kRoleMap[]` table using the exact strings from <interfaces>. Case-sensitive matching on the original QString (do NOT call toUpper() — upstream G-code emits mixed-case "Inner wall", and lower-casing risks future role-name collisions). Add `int roleForType(const QString &)` overload that takes no arg and returns 0 (None) for travel moves.
    2. Add a `static const float kRoleColors[20][3]` palette using the normalized [0,1] values from the 55-RESEARCH "EGCodeExtrusionRole" table's "Default Color" column.
    3. In the G-code parsing loop (around lines 989-1013), set `seg.role = extruding ? roleForType(currentType) : 0;` and replace the styleFor-based color assignment for FeatureType mode with `seg.baseR/G/B = kRoleColors[seg.role][0/1/2]`. (FeatureType colors are now baked per-role; non-FeatureType recoloring continues to overwrite these in recolorAndPackSegments.)
    4. Remove the now-unused FeatureStyle struct and styleFor function (do not leave dead code — project CLEAN-03 rule).
    5. Replace viewModes() body with the exact 17-entry list from <behavior>.
    6. Refactor recolorAndPackSegments() mode switch. The current switch uses raw indices `case 1: v = s.height; break;` etc. After the index renumber these are all wrong. Convert to a named-enum or named-constant approach: declare `enum EViewType { VT_Summary=0, VT_LineType=1, VT_Filament=2, ... VT_Tool=16 };` in the anonymous namespace and rewrite every case label using the enum. Map each case to the StoredSegment field per the 55-RESEARCH "Corrected view-mode list" notes (e.g. VT_Height -> s.height, VT_Flow -> s.volumetric_rate, VT_ActualSpeed -> computed s.feedrate-derived value or uniform, VT_Jerk/VT_PressureAdvance -> uniform + log).
    7. For VT_Summary: skip gradient legend computation; set m_legendType to 0 (discrete) and clear legendItems_ OR set a new sentinel — choose option A (m_legendType stays 0, legendItems_ cleared) to avoid introducing a new public sentinel; update test_summary_mode_has_no_gradient_legend in PreviewParserTests.cpp to assert `legendItems_.empty() && legendType() == 0`.
    8. Refactor the legend label switch (around lines 1419-1430) the same way (enum-based case labels).
    9. Flip the three RED tests in PreviewParserTests.cpp: remove the QEXPECT_FAIL wrappers and make the assertions real. Adjust the summary-mode test to assert legendItems_.isEmpty() && legendType()==0 per step 7.

    Implementation rules:
    - Do NOT introduce new StoredSegment fields for jerk/pressure_advance/actual_flow in this plan. Those modes log "data unavailable" once and use a uniform gradient (or skip legend). Adding fields is deferred until upstream G-code emits the corresponding tags and is out of Phase 55 scope per the fixture (Plan 01 deliberately omits ;JERK:/;PA:).
    - The viewModeIndex_ setter `setViewModeIndex(int)` must still clamp to `viewModes().size() - 1` (now 16, not 12). Any persisted settings index (QSettings) greater than 12 must be re-mapped or clamped; check if `setViewModeIndex` reads a persisted index and clamp at load time.
    - roleForType must be exposed to PreviewParserTests. Since PreviewViewModel is a QObject, add `Q_INVOKABLE int roleForType(const QString &) const;` so the test can call it directly on the viewmodel instance without parsing a file.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "FeatureStyle styleFor" src/core/viewmodels/PreviewViewModel.cpp returns 0 (old function removed).
    - grep -c "roleForType" src/core/viewmodels/PreviewViewModel.cpp returns >= 2 (definition + usage in parser loop).
    - grep "Inner wall" src/core/viewmodels/PreviewViewModel.cpp returns >= 1 (the kRoleMap entry).
    - grep -c "QStringLiteral(\"Summary\")" src/core/viewmodels/PreviewViewModel.cpp returns >= 1 (viewModes list head).
    - grep -c "VT_Summary\|VT_LineType\|VT_Filament\|VT_Tool" src/core/viewmodels/PreviewViewModel.cpp returns >= 4 (enum-based case labels in recolor switch).
    - `ctest --output-on-failure -R PreviewParser` PASSES with zero QEXPECT_FAIL / QSKIP remaining for the three target tests. test_fixture_has_expected_role_coverage still passes.
    - `ctest --output-on-failure -R QmlUiAudit` passes (no source-audit regression).
    - `ctest --output-on-failure -R ViewModelSmoke` passes (existing PreviewViewModel smoke tests tolerate the new 17-mode + roleVisibilities property).
    - `ctest --output-on-failure -R E2EWorkflow` passes (parser regression suite still green; the GCV1 blob size change from Task 1 is absorbed).
  </acceptance_criteria>
  <done>Fine-grained 20-role ;TYPE: parser replaces styleFor; viewModes() returns 17 upstream EViewType names; recolorAndPackSegments uses enum-based case labels; the three PreviewParserTests RED slots are now GREEN; all existing tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local .gcode file -> PreviewViewModel parser | G-code file (from local slice output or local file open) is parsed line-by-line. Already untrusted-input handling territory; parser already validates via regex helpers. |
| GCV1 blob (in-process) -> RhiViewportRenderer | In-process QByteArray; not a trust boundary (same process, same privilege). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02-SC | Tampering | none — no package installs | accept | No npm/pip/cargo installs; all changes are in-repo C++/QML. |
| T-55-02-01 | Tampering | ;TYPE: comment parser | mitigate | roleForType returns 0 (None) for any unrecognized string and never indexes kRoleColors out of bounds (role clamped via `if (role < 0 \|\| role >= 20) role = 0;` before array access). Static array sizes are fixed at compile time. |
| T-55-02-02 | Denial of Service | computePreviewDrawRange role skip | accept | Per-frame span iteration is O(spans); role check is O(1). No new allocation per toggle. Existing frame budget unchanged. |
| T-55-02-03 | Information Disclosure | parser logs (jerk/PA "data unavailable") | accept | Logs contain no user data; standard qInfo pattern. |

No high-severity threats. No network/auth surface; parser robustness already covered by role-index clamping.
</threat_model>

<verification>
- Canonical build (`scripts/auto_verify_with_vcvars.ps1`) passes.
- `ctest --output-on-failure -R PreviewParser` — three previously-RED tests now GREEN, fixture test still GREEN.
- `ctest --output-on-failure` (full suite) — no regression in QmlUiAudit / ViewModelSmoke / E2EWorkflow.
- Static_assert(sizeof(GcvPackedSegment) == 76) holds at compile time (build failure is the signal).
- roleForType boundary check prevents any out-of-bounds role index from reaching kRoleColors or m_roleVisibility.
</verification>

<success_criteria>
- GCODE-01 supported: no-placeholder real-data path is unchanged; the parser still consumes real slice output via rebuildFromGCode(SliceService::outputPath()). The live no-placeholder RED test lands in Plan 04.
- GCODE-02 core supported: 20-role parsing, 17-mode audit, and render-side per-role visibility (no repack) all implemented at the viewmodel+renderer contract level. Plan 03 binds the UI.
- Phase 41 interaction-stability invariant preserved: toggleRoleVisibility calls update() only; it never mutates gcodePreviewData_.
- Wire-format drift risk eliminated: static_assert guards sizeof(GcvPackedSegment) at compile time.
</success_criteria>

<output>
Create `.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-02-SUMMARY.md` when done.
</output>
