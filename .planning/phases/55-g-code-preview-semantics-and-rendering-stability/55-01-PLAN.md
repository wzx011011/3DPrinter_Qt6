---
phase: 55-g-code-preview-semantics-and-rendering-stability
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/orca_sample.gcode
  - tests/PreviewParserTests.cpp
  - CMakeLists.txt
autonomous: true
requirements: [GCODE-01, GCODE-02]
user_setup: []

must_haves:
  truths:
    - "A committed OrcaSlicer-style .gcode fixture exists with at least 8 distinct ;TYPE: roles, travel moves, and tagged comments (;HEIGHT:, ;WIDTH:, ;FEEDRATE:, ;FAN:, ;TEMP:, ;ACCEL:)"
    - "PreviewParserTests target is registered in CMakeLists.txt and runnable via ctest --output-on-failure -R PreviewParser"
    - "PreviewParserTests scaffold has failing (RED) test slots for the 20-role parse and 17-mode coverage that Plan 02 will turn green"
  artifacts:
    - path: "tests/fixtures/orca_sample.gcode"
      provides: "Deterministic OrcaSlicer-style G-code fixture for parser/view-mode/role tests"
      contains: ";TYPE:Outer wall"
    - path: "tests/PreviewParserTests.cpp"
      provides: "QtTest unit-test target for ;TYPE: string-to-role mapping and view-mode coverage"
      contains: "PreviewParserTests"
    - path: "CMakeLists.txt"
      provides: "PreviewParserTests target registration"
      contains: "qt_add_executable(PreviewParserTests"
  key_links:
    - from: "tests/PreviewParserTests.cpp"
      to: "tests/fixtures/orca_sample.gcode"
      via: "QT_TESTCASE_SOURCEDIR-relative file load"
      pattern: "orca_sample\\.gcode"
---

<objective>
Wave 0 foundation: commit a deterministic OrcaSlicer-style G-code fixture and a registered (RED) PreviewParserTests target so that Plan 02's parser/role/mode work has a place to land assertions.

Purpose: The RESEARCH and VALIDATION docs both flag PreviewParserTests + a committed .gcode fixture as Wave 0 gaps. Plan 02 (wire format, 20-role parser, 17-mode audit) and Plan 04 (no-placeholder + regression tests) both depend on these assets existing. Landing them first prevents the "MISSING test scaffold" Nyquist violation in later plans.

Output: One committed fixture file, one new QtTest target (skeleton + RED tests), CMakeLists.txt registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md
@.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-VALIDATION.md

@src/core/viewmodels/PreviewViewModel.cpp
@tests/QmlUiAuditTests.cpp

<interfaces>
<!-- Upstream role strings PreviewParserTests will assert against. Source: 55-RESEARCH.md Section "Upstream Enum Reference". -->
<!-- These exact English strings must appear in ;TYPE: comments in the fixture and be parsed by Plan 02's role mapper. -->
Upstream EGCodeExtrusionRole display names (ExtrusionEntity::role_to_string, 55-RESEARCH.md):
  "Inner wall", "Outer wall", "Overhang wall", "Sparse infill", "Internal solid infill",
  "Top surface", "Bottom surface", "Ironing", "Bridge", "Internal Bridge", "Gap infill",
  "Skirt", "Brim", "Support", "Support interface", "Support transition", "Prime tower",
  "Custom", "Multiple"

Upstream EViewType display names (get_view_type_string, 17 modes, 55-RESEARCH.md):
  "Summary", "Line Type", "Filament", "Speed", "Actual Speed", "Height", "Width",
  "Flow", "Actual Flow", "Layer Time", "Layer Time (log)", "Fan Speed", "Temperature",
  "Pressure Advance", "Acceleration", "Jerk", "Tool"

CMake test registration analog (CMakeLists.txt:388-396):
  qt_add_executable(QmlUiAuditTests tests/QmlUiAuditTests.cpp)
  target_link_libraries(QmlUiAuditTests PRIVATE Qt6::Test)
  target_compile_definitions(QmlUiAuditTests PRIVATE QT_TESTCASE_SOURCEDIR="${CMAKE_SOURCE_DIR}")
  add_test(NAME QmlUiAuditTests COMMAND QmlUiAuditTests)
  set_tests_properties(QmlUiAuditTests PROPERTIES ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}")

Integration-test link analog (CMakeLists.txt:369-386) for tests that need PreviewViewModel:
  qt_add_executable(ViewModelSmokeTests tests/ViewModelSmokeTests.cpp)
  target_link_libraries(ViewModelSmokeTests PRIVATE owzx_app_core Qt6::Qml Qt6::Quick Qt6::QuickControls2 Qt6::OpenGL Qt6::Concurrent Qt6::Network Qt6::Test)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit OrcaSlicer-style G-code fixture</name>
  <files>tests/fixtures/orca_sample.gcode</files>
  <read_first>
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-RESEARCH.md (Section "Upstream Enum Reference" — exact ;TYPE: strings; Section "Pattern 2: Upstream ;TYPE: Comment Parsing")
    - src/core/viewmodels/PreviewViewModel.cpp (lines ~228-330 — current parser: parseTaggedValue, parseAxis, parseFValue, parseSValue, normalizeFanSpeed to mirror tagged-comment format)
    - third_party/OrcaSlicer/src/libslic3r/ExtrusionEntity.cpp:583-639 (upstream role_to_string / string_to_role — the exact strings the fixture must emit)
  </read_first>
  <action>
    Create a minimal but realistic OrcaSlicer-style G-code fixture at tests/fixtures/orca_sample.gcode. The fixture must be parseable by the existing PreviewViewModel parser (parseAxis/parseFValue/parseTaggedValue handle `G1 X.. Y.. Z.. E.. F..`, `;TAG:VALUE` lines) AND carry the upstream role strings so Plan 02's fine-grained role mapper has data to exercise.

    Content requirements (all MUST be present):
    - G-code header: a comment line `;Generated by OrcaSlicer fixture (OWzx test)` so the GCODE-01 no-placeholder test in Plan 04 can assert the fixture is NOT mistaken for the live slice output (the live slice path writes to SliceService::outputPath(), never to tests/fixtures/).
    - At least 12 distinct `;TYPE:` blocks covering these exact upstream strings (per 55-RESEARCH.md role reference): `Inner wall`, `Outer wall`, `Overhang wall`, `Sparse infill`, `Internal solid infill`, `Top surface`, `Bridge`, `Gap infill`, `Skirt`, `Support`, `Support interface`, `Prime tower`. Each `;TYPE:` block followed by 2-4 `G1 X.. Y.. E.. F..` extrusion moves.
    - At least 4 travel move blocks: `G0 X.. Y.. F..` (no E) interleaved between type blocks (travel segments for the Plan 04 no-travel-repack test).
    - Tagged comments at least once each: `;HEIGHT:0.2`, `;WIDTH:0.45`, `;FEEDRATE:60`, `;FAN:128`, `;TEMP:210`, `;ACCEL:500` (matches existing parseTaggedValue tag set in PreviewViewModel.cpp).
    - Two layers: a `;LAYER:0` block and a `;LAYER:1` block (so layer-range filtering tests in Plan 04 have >1 layer).
    - A `T0` / `T1` tool change line between two segments (for the GCODE-05 reslice/tool-change assertion in Plan 04).

    Keep the file under ~120 lines. ASCII only (project CLAUDE.md rule: UTF-8 without BOM, English ASCII comments). Do NOT include `;JERK:` or `;PA:` tags — the RESEARCH Assumptions Log (A2) marks jerk/PA data availability as MEDIUM risk and out of scope for the fixture; Plan 02 will document those modes as "data unavailable" gracefully.
  </action>
  <verify>
    <automated>grep -v -c -E "^;" tests/fixtures/orca_sample.gcode | awk "{exit !($1 >= 30)}" || (echo "fixture must have at least 30 non-comment G-code lines" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File tests/fixtures/orca_sample.gcode exists and is ASCII-only (git diff shows no BOM marker; `file tests/fixtures/orca_sample.gcode` reports ASCII text).
    - grep -c ";TYPE:" tests/fixtures/orca_sample.gcode returns >= 12.
    - grep -E ";TYPE:(Inner wall|Outer wall|Overhang wall|Sparse infill|Internal solid infill|Top surface|Bridge|Gap infill|Skirt|Support|Support interface|Prime tower)" tests/fixtures/orca_sample.gcode returns at least one match for each of the 12 listed strings.
    - grep -c ";HEIGHT:" returns >= 1; same for ;WIDTH:, ;FEEDRATE:, ;FAN:, ;TEMP:, ;ACCEL:.
    - grep -c ";LAYER:" returns >= 2.
    - grep -c "^G0 " returns >= 4 (travel moves).
    - grep -c "T[01]" returns >= 1 tool-change line.
  </acceptance_criteria>
  <done>Fixture committed with the required role/type/tag/layer/travel/tool-change coverage; ASCII-only; under 120 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register PreviewParserTests target with RED scaffold</name>
  <files>tests/PreviewParserTests.cpp, CMakeLists.txt</files>
  <read_first>
    - tests/QmlUiAuditTests.cpp (lines 1-56 — QtTest class structure, readSource helper using QT_TESTCASE_SOURCEDIR)
    - tests/ViewModelSmokeTests.cpp (lines 117-130 — class declaration + private slots pattern; lines 1890-1910 — SliceService/PreviewViewModel construction if needed)
    - CMakeLists.txt (lines 359-396 — existing qt_add_executable + target_link_libraries + add_test + set_tests_properties pattern; lines 411-425 — PartPlateTests owzx_app_core link pattern)
    - .planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-PATTERNS.md (Section "tests/PreviewParserTests.cpp (NEW test, unit)" — full CMake registration template; AUTOMOC single-file caveat)
  </read_first>
  <behavior>
    - Test `test_fixture_has_expected_role_coverage`: loads tests/fixtures/orca_sample.gcode via QT_TESTCASE_SOURCEDIR; asserts file contains "Inner wall", "Outer wall", "Sparse infill", "Bridge", "Support", "Skirt", "Prime tower" (presence check on the fixture, so Plan 02 cannot accidentally shrink the fixture). MUST pass at end of Plan 01 (fixture already exists).
    - Test `test_role_string_mapping_covers_upstream_enum` (RED): asserts a `PreviewViewModel::roleForType(QString)` invokable returns the correct upstream index for the 12 fixture strings. Plan 02 implements roleForType; until then this test fails to compile or fails at runtime. Use QEXPECT_FAIL or a TODO comment so the target still builds and the rest of the scaffold passes.
    - Test `test_view_modes_match_upstream_seventeen` (RED): asserts PreviewViewModel::viewModes().size() == 17 and that the list contains "Line Type", "Filament", "Flow", "Actual Speed", "Acceleration", "Jerk", "Pressure Advance", "Tool", "Summary". Plan 02 makes this pass; currently 13 modes -> RED.
    - Test `test_summary_mode_has_no_gradient_legend` (RED): asserts that when viewModeIndex corresponds to "Summary", legendType() returns a sentinel meaning "no legend" (Plan 02 defines the sentinel; until then RED).
  </behavior>
  <action>
    Create tests/PreviewParserTests.cpp as a single-file QtTest class `PreviewParserTests` with Q_OBJECT and the four private slots listed in <behavior>. For RED tests that depend on PreviewViewModel API not yet present (roleForType, the 17-mode list, summary legend sentinel), do one of:
      (a) Compile-guard the bodies with `#if 0` / `#endif` plus a `QSKIP("Plan 02 implements roleForType / 17-mode list", SkipSingle);` inside an `#else` slot that still compiles, OR
      (b) Construct PreviewViewModel and assert the CURRENT behavior (e.g. viewModes().size() == 13 today) wrapped in `QEXPECT_FAIL("", "Plan 02 raises this to 17", Continue);` so the test is RED-by-expectation until Plan 02.
    Choose (b) for test_role_string_mapping_covers_upstream_enum and test_view_modes_match_upstream_seventeen so they auto-flip to passing when Plan 02 lands. Choose (a) for test_summary_mode_has_no_gradient_legend (new sentinel, no current equivalent).

    Construct PreviewViewModel the same way ViewModelSmokeTests does (SliceService + QObject parent). Load the fixture via QDir(QT_TESTCASE_SOURCEDIR).filePath("tests/fixtures/orca_sample.gcode") and call loadGCodeForPreview() — do NOT invent a new parser entry point.

    Register in CMakeLists.txt following the ViewModelSmokeTests/PartPlateTests pattern (links owzx_app_core + Qt6 modules) since PreviewParserTests needs the real PreviewViewModel:
      qt_add_executable(PreviewParserTests tests/PreviewParserTests.cpp)
      target_link_libraries(PreviewParserTests PRIVATE owzx_app_core Qt6::Qml Qt6::Quick Qt6::QuickControls2 Qt6::OpenGL Qt6::Concurrent Qt6::Network Qt6::Test)
      target_compile_definitions(PreviewParserTests PRIVATE QT_TESTCASE_SOURCEDIR="${CMAKE_SOURCE_DIR}")
      target_link_options(PreviewParserTests PRIVATE /FORCE:MULTIPLE)  # match ViewModelSmokeTests if present
      add_test(NAME PreviewParserTests COMMAND PreviewParserTests)
      set_tests_properties(PreviewParserTests PROPERTIES ENVIRONMENT "PATH=${_qt6_bin_dir};$ENV{PATH}")
    Place the registration immediately after the PartPlateTests block (around CMakeLists.txt:425).

    IMPORTANT (AUTOMOC caveat from 55-PATTERNS.md): single-file QtTest with Q_OBJECT has weak moc tracking. After creating the file, the executor must delete build/PreviewParserTests_autogen/timestamp in any incremental build if a new slot is added later. Note this as a comment at the top of the test file.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1</automated>
  </verify>
  <acceptance_criteria>
    - tests/PreviewParserTests.cpp exists, is ASCII/UTF-8 without BOM, contains `class PreviewParserTests final : public QObject` and `Q_OBJECT`.
    - CMakeLists.txt contains a `qt_add_executable(PreviewParserTests tests/PreviewParserTests.cpp)` line and a corresponding `add_test(NAME PreviewParserTests` line.
    - Canonical build script `scripts/auto_verify_with_vcvars.ps1` succeeds (the build is the gate; the new test target compiles and links).
    - `ctest --output-on-failure -R PreviewParser` runs the new target. `test_fixture_has_expected_role_coverage` PASSES. The two QEXPECT_FAIL / QSKIP RED tests are reported as expected failures/skips, NOT as hard failures (target exits 0).
    - No raw `QObject`/`QTest` headers missing; no `using namespace` violations (project convention).
  </acceptance_criteria>
  <done>PreviewParserTests target builds via the canonical script, runs under ctest -R PreviewParser, the fixture-presence test is green, and the three Plan-02-dependents are explicitly RED (QEXPECT_FAIL or QSKIP) so Plan 02 flips them to green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/fixtures/ -> test process | Test reads a local committed fixture file; no untrusted input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01-SC | Tampering | none — no package installs in this plan | accept | No npm/pip/cargo install tasks; fixture and test are project-local files. No third-party package legitimacy gate needed. |
| T-55-01-01 | Information Disclosure | tests/fixtures/orca_sample.gcode | accept | Fixture is synthetic test data with no PII/secrets; committed alongside existing test data under tests/. |

No high-severity threats. No network, auth, or persistence surface in this plan.
</threat_model>

<verification>
- `ctest --output-on-failure -R PreviewParser` runs and reports the expected RED/GREEN split described in Task 2.
- Full canonical build (`scripts/auto_verify_with_vcvars.ps1`) passes.
- Fixture grep checks (Task 1 acceptance) all pass.
</verification>

<success_criteria>
- Foundation assets committed: tests/fixtures/orca_sample.gcode, tests/PreviewParserTests.cpp, CMakeLists.txt registration.
- Plan 02 has a deterministic place to land role-for-type, 17-mode list, and summary-mode assertions.
- Plan 04 has the fixture path it needs for the no-placeholder live-slice RED test.
- VALIDATION.md Wave 0 gaps for "G-code fixture" and "PreviewParserTests" are closed.
</success_criteria>

<output>
Create `.planning/phases/55-g-code-preview-semantics-and-rendering-stability/55-01-SUMMARY.md` when done.
</output>
