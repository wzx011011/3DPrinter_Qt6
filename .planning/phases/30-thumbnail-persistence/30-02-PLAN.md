---
phase: 30
plan: 02
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
autonomous: true
requirements: [THUMB-01]
must_haves:
  truths:
    - "Q_INVOKABLE QString generatePlateThumbnailVariant(int plateIndex, int size, int variant) generates 2 variants: variant=0 (main flat-color, current generatePlateThumbnail), variant=1 (NEW top-down 2D footprint via QPainter — objects as bounding-box rectangles colored by palette)."
    - "Existing generatePlateThumbnail stays as-is (variant=0 backward-compat); generatePlateThumbnailVariant(plateIndex, size, 0) delegates to it."
---

# Plan 30-02: generatePlateThumbnailVariant — 2 thumbnail variants (THUMB-01)

**Objective:** Add the `generatePlateThumbnailVariant` Q_INVOKABLE with 2 variants: main (existing flat-color) + NEW top-down 2D footprint. THUMB-01.

## Tasks

### 30-02-T1 — Add generatePlateThumbnailVariant declaration + main variant delegation

**read_first:** `src/core/services/ProjectServiceMock.h` (generatePlateThumbnail at ~line 200); `src/core/services/ProjectServiceMock.cpp:4119+` (current generatePlateThumbnail).

**action:**
1. In ProjectServiceMock.h, add `Q_INVOKABLE QString generatePlateThumbnailVariant(int plateIndex, int size, int variant);` near generatePlateThumbnail.
2. In ProjectServiceMock.cpp, implement: `if (variant == 0) return generatePlateThumbnail(plateIndex, size);` then `if (variant == 1) return generateTopDownThumbnail(plateIndex, size);` then `return generatePlateThumbnail(plateIndex, size);` (default to main).

**acceptance_criteria:**
- ProjectServiceMock.h contains `Q_INVOKABLE QString generatePlateThumbnailVariant(int plateIndex, int size, int variant);`
- ProjectServiceMock.cpp generatePlateThumbnailVariant delegates variant=0 to generatePlateThumbnail
- compiles green

### 30-02-T2 — Implement top-down 2D footprint thumbnail (variant=1)

**read_first:** `src/core/services/ProjectServiceMock.cpp:4119+` (generatePlateThumbnail — the flat-color rendering pattern + palette color logic); `src/core/services/ProjectServiceMock.h` (modelCount, objectIndicesOnPlate, plateWidth/Depth/bed dimensions).

**action:** Add a private helper `QString generateTopDownThumbnail(int plateIndex, int size)`:
1. Create `QImage image(size, size, QImage::Format_RGBA8888); image.fill(Qt::transparent);`
2. `QPainter painter(&image); painter.setRenderHint(QPainter::Antialiasing);`
3. Draw the plate as a rectangle (the plate bed footprint) — e.g. `painter.fillRect(0, 0, size, size, QColor(40,40,40))` for the bed background.
4. For each object index on the plate (via objectIndicesOnPlate(plateIndex)), get its bounding box (via the object transform API — find the existing accessor for object bbox/position/scale). Compute its footprint rectangle in plate coordinates, scale to the thumbnail size, draw as a filled rectangle colored by palette (reuse the color logic from generatePlateThumbnail).
5. Convert to base64 PNG: `QByteArray ba; QBuffer buf(&ba); buf.open(QIODevice::WriteOnly); image.save(&buf, "PNG"); return "data:image/png;base64," + ba.toBase64();`
6. Add `#include <QPainter>`, `<QBuffer>` if not present.

**acceptance_criteria:**
- ProjectServiceMock.cpp contains `QString ProjectServiceMock::generateTopDownThumbnail(int plateIndex, int size)`
- Uses QPainter, draws plate bed + object rectangles
- Returns `data:image/png;base64,` + base64 PNG
- generatePlateThumbnailVariant(plateIndex, size, 1) calls generateTopDownThumbnail
- compiles green

## verification
- `owzx_app_core` compiles green
- `rg "generatePlateThumbnailVariant" src/core/services/ProjectServiceMock.{h,cpp}` → declared + implemented
- `rg "generateTopDownThumbnail" src/core/services/ProjectServiceMock.cpp` → present
