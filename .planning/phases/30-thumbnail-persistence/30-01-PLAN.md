---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/model/PartPlate.h
  - src/core/model/PartPlate.cpp
autonomous: true
requirements: [THUMB-01, THUMB-02]
must_haves:
  truths:
    - "PartPlate stores QImage m_thumbnail (Qt-native, QML-friendly) with setThumbnail(QImage)/thumbnail() accessors."
    - "Cache invalidation: addInstance/removeInstance/clearInstances/setPlateLocked reset m_thumbnail = QImage() so the next access regenerates."
---

# Plan 30-01: PartPlate thumbnail cache field + invalidation

**Objective:** Add the `QImage m_thumbnail` field to `PartPlate` with accessors + cache invalidation on every content change. This is the domain foundation for THUMB-01/02 (Phase 30).

## Tasks

### 30-01-T1 — Add QImage m_thumbnail + accessors to PartPlate

**read_first:** `src/core/model/PartPlate.h` (current state, private members at line ~200+); `src/core/model/PartPlate.cpp` (constructor).

**action:**
1. Add `#include <QImage>` to PartPlate.h.
2. Public accessors: `QImage thumbnail() const { return m_thumbnail; }`, `void setThumbnail(const QImage& img) { m_thumbnail = img; }`, `bool hasThumbnail() const { return !m_thumbnail.isNull(); }`.
3. Private member: `QImage m_thumbnail;` (null by default).
4. Comment: `// v3.2 Phase 30: cached plate thumbnail (Qt-native). Invalidated on content change (D-30-10).`

**acceptance_criteria:**
- PartPlate.h contains `#include <QImage>`
- PartPlate.h contains `QImage m_thumbnail;` private member
- PartPlate.h contains `QImage thumbnail() const`, `void setThumbnail(const QImage&)`, `bool hasThumbnail() const`
- `owzx_app_core` compiles green

### 30-01-T2 — Invalidate cache on content change

**read_first:** `src/core/model/PartPlate.h` (addInstance at 143, removeInstance at 148, clearInstances at 156, setLocked); `src/core/model/PartPlate.cpp`.

**action:** Add `m_thumbnail = QImage();` (invalidation) at the start of:
1. `addInstance(int, int)` — clear cache before adding.
2. `removeInstance(int, int)`.
3. `clearInstances()`.
4. `setLocked(bool)` (locked state may affect rendering).
Also add to any setter that changes plate appearance (origin/size if they exist — check and invalidate defensively).

**acceptance_criteria:**
- addInstance, removeInstance, clearInstances, setLocked each contain `m_thumbnail = QImage();`
- `rg "m_thumbnail = QImage\(\)" src/core/model/PartPlate.cpp` → ≥ 4 matches
- `owzx_app_core` compiles green

## verification
- `owzx_app_core` compiles green
- `rg "QImage m_thumbnail" src/core/model/PartPlate.h` → present
- `rg -c "m_thumbnail = QImage\(\)" src/core/model/PartPlate.cpp` → ≥ 4
