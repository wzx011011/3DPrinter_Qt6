---
phase: 123-mmu-segmentation-paint-end-to-end
plan: 01
type: implementation
wave: 1
depends_on: [122]
files_modified:
  - src/core/viewmodels/EditorViewModel.h
  - src/core/viewmodels/EditorViewModel.cpp
  - src/qml_gui/pages/PreparePage.qml
  - tests/QmlUiAuditTests.cpp
autonomous: true
requirements: [PAINT-05]
---

# Phase 123 Plan: MMU Segmentation Paint End-To-End

## Objective
Close PAINT-05: MMU painted regions (Extruder1..16) feed the multi-material slice. Reuses Phase 122's writePaintToModelVolume(PaintKind::Mmu).

## Must Haves
<must_haves>
  <truth id="MM-01">EditorViewModel::paintAtFacet writes back via writePaintToModelVolume(obj,vol,PaintKind::Mmu,selector) when gizmoMode==GizmoMmuSegmentation. The selector carries Extruder1..16 states (set by the paint interaction for the selected extruder).</truth>
  <truth id="MM-02">clearMmuSegmentation TODO stub (EditorViewModel.cpp:1234) implemented: PaintEngine.clearObject + clearPaintOnModelVolume(Mmu) + invalidate + return true. No longer return false.</truth>
  <truth id="MM-03">MMU gizmo honest gating: when the project has a single extruder (mmuExtruderCount<=1), the MMU paint gizmo surfaces an honest "requires multi-material" reason (disabled or banner), no silent no-op. The slice only applies MMU segmentation when multi-material is active.</truth>
  <truth id="MM-04">MMU segmentation survives save→reload (3MF round-trip automatic via bbs_3mf paint_color attribute, since FacetsAnnotation is populated).</truth>
  <truth id="MM-05">A `mmuSegmentationPaintFeedsSlice` source-audit slot asserts: (a) EditorViewModel paintAtFacet references PaintKind::Mmu or the mmu write path; (b) clearMmuSegmentation no longer returns false/TODO; (c) mmu_segmentation_facets referenced. PAINT-05-named QVERIFY2.</truth>
  <truth id="MM-06">Canonical build (j6) + regression ctest passes; encoding guard clean.</truth>
</must_haves>

<threat_model>
No attack surface. Multi-material segmentation flows user's mesh edits into the local slice engine. No network/auth. Threats open: 0.
</threat_model>

## Tasks
<tasks>
  <task id="123-01-01" type="implementation">
    <title>Wire MMU paint path to PaintKind::Mmu + implement clearMmuSegmentation + honest gating</title>
    <read_first>
      - .planning/phases/123-mmu-segmentation-paint-end-to-end/123-CONTEXT.md
      - src/core/viewmodels/EditorViewModel.cpp:2519-2631 (paintAtFacet — add gizmoMode==10 → Mmu), :1234-1239 (clearMmuSegmentation TODO)
      - src/core/services/ProjectServiceMock.h (writePaintToModelVolume from Phase 122 + PaintKind::Mmu)
      - src/qml_gui/pages/PreparePage.qml:2840-2927 (MMU panel — honest gating)
      - src/core/viewmodels/EditorViewModel.h:560-561 (mmuSelectedExtruder/mmuExtruderCount)
    </read_first>
    <files>src/core/viewmodels/EditorViewModel.h, src/core/viewmodels/EditorViewModel.cpp, src/qml_gui/pages/PreparePage.qml</files>
    <action>
      **Step 1 — MMU write-back.** paintAtFacet: when gizmoMode==GizmoMmuSegmentation, paintKindForCurrentGizmo() returns Mmu; writePaintToModelVolume(...,PaintKind::Mmu,...) writes mmu_segmentation_facets.
      **Step 2 — clearMmuSegmentation.** Implement: clearObject + clearPaintOnModelVolume(Mmu) + invalidate + return true.
      **Step 3 — honest gating.** In PreparePage MMU panel or EditorViewModel activability, surface "requires multi-material" when mmuExtruderCount<=1 (disabled state or a banner text). No silent no-op.
      **Step 4 — comments.** English ASCII-only. Cite MultiMaterialSegmentation.cpp + the multi-material-active requirement.
    </action>
    <verify>`rg -n "PaintKind::Mmu|mmu_segmentation_facets|clearMmuSegmentation" src/core/`</verify>
    <acceptance_criteria>MM-01, MM-02, MM-03.</acceptance_criteria>
  </task>
  <task id="123-01-02" type="test">
    <title>Add mmuSegmentationPaintFeedsSlice source-audit slot</title>
    <read_first>tests/QmlUiAuditTests.cpp (slot pattern)</read_first>
    <files>tests/QmlUiAuditTests.cpp</files>
    <action>Add slot asserting MM-05 (a/b/c).</action>
    <verify>ctest passes.</verify>
    <acceptance_criteria>MM-05.</acceptance_criteria>
  </task>
  <task id="123-01-03" type="verification">
    <title>Canonical build (j6) + regression ctest</title>
    <read_first>scripts/auto_verify_with_vcvars.ps1</read_first>
    <files></files>
    <action>Run canonical build + regression ctest. Document in SUMMARY.</action>
    <verify>SUMMARY records results.</verify>
    <acceptance_criteria>MM-06.</acceptance_criteria>
  </task>
</tasks>

## Verification
1. Canonical build (j6); 2. Regression ctest; 3. Source audit grep; 4. Encoding guard.

## Success Criteria
- PAINT-05 closed; every MM truth satisfied; WS2 complete (120-123).

## Expected Non-Changes
- No ProjectServiceMock bridge changes (Phase 122 added writePaintToModelVolume).
- No 3MF changes (automatic).
- No slice engine changes.
