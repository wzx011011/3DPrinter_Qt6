---
plan_id: 16-02
wave: 2
depends_on: [16-01]
files_modified:
  - src/core/services/ProjectServiceMock.h
  - src/core/services/ProjectServiceMock.cpp
  - src/core/viewmodels/EditorViewModel.cpp
requirements: [PLATE-06]
autonomous: true
---

# Plan 16-02: Migrate ProjectServiceMock Plate Storage to PartPlateList (Big-Bang)

<objective>
Perform the big-bang migration (D-05): delete all 9 parallel `QList` plate vectors and `plateCount_` from `ProjectServiceMock`, replace them with a single `PartPlateList` member (from plan 16-01), and rewrite the ~30 plate Q_PROPERTY/Q_INVOKABLE methods + the 3MF load-path reconstruction to read directly from the new model. Existing user-visible plate behavior (add/delete/rename/lock/select + current plate) MUST be preserved (PLATE-06) — no feature changes, only the storage backing changes.

This is the highest-risk plan in v3.0: single large change surface, ~30 methods rewritten, load path reworked. Run canonical verification after each logical sub-step, not just at the end.
</objective>

<must_haves>
  <truth>ProjectServiceMock.h no longer declares plateCount_, plateNames_, plateLockedStates_, plateBedTypes_, platePrintSequences_, plateSpiralModes_, plateFirstLayerSeqChoices_, plateFirstLayerSeqOrders_, plateOtherLayersSeqChoices_, plateOtherLayersSeqEntries_, m_mockPlateOverrides</truth>
  <truth>ProjectServiceMock.h declares a PartPlateList member (e.g. std::unique_ptr<PartPlateList> or PartPlateList member) as the single plate storage</truth>
  <truth>plateCount() reads from the PartPlateList</truth>
  <truth>addPlate/deletePlate/renamePlate/setPlateLocked/setCurrentPlateIndex delegate to PartPlateList</truth>
  <truth>plateScopedOptionValue/setPlateScopedOptionValue read/write the PartPlate's DynamicPrintConfig (no longer return false stub)</truth>
  <truth>3MF load path (read_from_archive reconstruction) populates PartPlateList from PlateData</truth>
  <truth>Existing ViewModelSmokeTests plate tests still pass unchanged (behavior preserved — PLATE-06)</truth>
  <truth>canonical verify exits 0</truth>
</must_haves>

<context_source>
Decisions D-04 (DynamicPrintConfig config), D-05 (big-bang, no bridge) from `.planning/phases/16-partplate-data-model-foundation/16-CONTEXT.md`. Risk posture: test-first already satisfied by 16-01; this plan runs canonical verify after each sub-step.
</context_source>

<tasks>

  <task id="16-02-T1">
    <action>
Replace the plate storage in `src/core/services/ProjectServiceMock.h`. Delete these member declarations (currently at lines 328, 336, 337, 344-361): `int plateCount_`, `int currentPlateIndex_`, `QStringList plateNames_`, `QList<QList<int>> plateObjectIndices_`, `QList<bool> plateLockedStates_`, `QList<int> plateBedTypes_`, `QList<int> platePrintSequences_`, `QList<int> plateSpiralModes_`, `QList<int> plateFirstLayerSeqChoices_`, `QList<QList<int>> plateFirstLayerSeqOrders_`, `QList<int> plateOtherLayersSeqChoices_`, `QList<QList<MockLayerSeqEntry>> plateOtherLayersSeqEntries_`, and `QHash<int, QHash<QString,QVariant>> m_mockPlateOverrides`.

Add in their place: `std::unique_ptr<PartPlateList> m_plateList;` (include `core/model/PartPlateList.h`). Initialize it in the constructor (ensure at least the default first plate exists — PartPlateList constructor creates 1 plate, matching the old `plateCount_=0` then addPlate-on-first-load behavior; verify the existing initial-state expectations in the constructor).

Keep the `MockLayerSeqEntry` struct definition but relocate it (move into PartPlate or a small helper header `src/core/model/LayerSequence.h` if it's reused) — it is part of plate state and should live with the model. The per-plate bed-type/print-sequence/spiral/layer-sequence state moves INTO the PartPlate (add the corresponding fields to PartPlate.h if not already present — they were scoped out of 16-01 T1's explicit list but are needed for D-05 completeness; add them now as part of the model: `int m_bed_type, m_print_sequence, m_spiral_mode, m_first_layer_seq_choice, m_other_layers_seq_choice; std::vector<int> m_first_layer_seq_order; std::vector<LayerSeqEntry> m_other_layers_seq_entries;` with getters/setters).

Do NOT change any public method signatures (Q_PROPERTY/Q_INVOKABLE signatures stay identical — only their implementations change in T2).
    </action>
    <read_first>
- `src/core/services/ProjectServiceMock.h:320-374` (the member declarations to delete + constructor area)
- `src/core/model/PartPlate.h` and `src/core/model/PartPlateList.h` (from plan 16-01 — the new storage)
- `src/core/services/ProjectServiceMock.cpp` constructor (find where plateCount_ etc. are initialized — understand initial state to preserve)
- `.planning/phases/16-partplate-data-model-foundation/16-CONTEXT.md` (D-05 big-bang, the 9 vectors listed explicitly)
    </read_first>
    <acceptance_criteria>
- `grep -c "plateCount_\|plateNames_\|plateLockedStates_\|plateBedTypes_\|platePrintSequences_\|plateSpiralModes_\|plateFirstLayerSeqChoices_\|plateFirstLayerSeqOrders_\|plateOtherLayersSeqChoices_\|plateOtherLayersSeqEntries_\|m_mockPlateOverrides" src/core/services/ProjectServiceMock.h` returns 0
- `grep "PartPlateList" src/core/services/ProjectServiceMock.h` returns a match
- `grep "core/model/PartPlateList.h" src/core/services/ProjectServiceMock.h` returns a match
- Public Q_PROPERTY/Q_INVOKABLE declarations in ProjectServiceMock.h are unchanged in signature (same names, same parameter types)
- The file does not yet compile (T2 implements the methods) — this task only changes storage declarations
    </acceptance_criteria>
  </task>

  <task id="16-02-T2">
    <action>
Rewrite the plate method implementations in `src/core/services/ProjectServiceMock.cpp` to delegate to `m_plateList` (D-05: direct read, no bridge/cache). Every method that previously read/wrote the parallel vectors must now go through PartPlateList/PartPlate. Concretely:

- `plateCount()` → `return m_plateList ? m_plateList->plateCount() : 0;`
- `currentPlateIndex()` → `return m_plateList ? m_plateList->currentPlateIndex() : -1;`
- `setCurrentPlateIndex(int)` → `m_plateList->setCurrentPlateIndex(index); emit ...;` (preserve the existing NOTIFY signal emission — check current impl for which signals fire)
- `addPlate()` → `return m_plateList->createPlate() != nullptr;` + emit plate signals
- `deletePlate(int)` → `return m_plateList->deletePlate(plateIndex);` + emit plate signals
- `renamePlate(int, QString)` → `m_plateList->renamePlate(plateIndex, newName.toStdString());` + emit
- `isPlateLocked(int)` → `PartPlate* p = m_plateList->plate(plateIndex); return p ? p->isLocked() : false;` (add isLocked getter to PartPlate if missing)
- `setPlateLocked(int, bool)` → `m_plateList->setPlateLocked(plateIndex, locked);` + emit
- `plateNames()` → build QStringList from `m_plateList->plate(i)->name()` for all i (returns ALL plate names — matches existing semantics)
- `plateObjectIndices(int)` → `return m_plateList->objectIndicesOnPlate(plateIndex);` (the instance→object bridge query from 16-01 T2)
- `currentPlateObjectIndices()` → `return plateObjectIndices(currentPlateIndex());`
- `plateObjectCount(int)` → `return m_plateList->objectIndicesOnPlate(index).size();`
- `plateIndexForObject(int)` → `return m_plateList->plateIndexForObject(objIdx);`
- `setObjectPlateForIndex(int objIdx, int plateIdx)` → remove the object's instances from whatever plate currently holds them, then add (objIdx, 0) [or all instances] to the target plate's PartPlate instance set. Preserves the existing "move object to plate" behavior. Emit plate signals.
- `plateScopedOptionValue(int, QString, QVariant)` → under HAS_LIBSLIC3R: read the plate's `DynamicPrintConfig` for the key; return as QVariant (convert the config option value to QVariant — use the existing config-key-to-QVariant conversion pattern if one exists, else build a minimal string/double/int conversion). Non-HAS_LIBSLIC3R: return fallbackValue (mock path). FIXES the `return false`/fallback stub at ProjectServiceMock.cpp:1336.
- `setPlateScopedOptionValue(int, QString, QVariant)` → under HAS_LIBSLIC3R: write into the plate's `DynamicPrintConfig` via its set/optptr API; return true. Non-HAS_LIBSLIC3R: return false (no-op mock). This replaces the stubbed `return false`.
- `plateScopedOverrideCount` / `plateScopedOverriddenKey` / `resetPlateScopedOptionValue` → operate on the DynamicPrintConfig's key set.

Preserve EVERY existing NOTIFY signal emission (plateDataLoaded, plateSelectionChanged, stateChanged — whatever the current methods emit). The QML bindings depend on these. If unsure whether a signal fired before, check the current implementation before rewriting — do not silently drop signals.
    </action>
    <read_first>
- `src/core/services/ProjectServiceMock.cpp` (read ALL plate method implementations — find each of plateCount, addPlate, deletePlate, renamePlate, setPlateLocked, plateNames, plateObjectIndices, setObjectPlateForIndex, plateScopedOptionValue, setPlateScopedOptionValue, etc. — note which signals each currently emits)
- `src/core/model/PartPlate.h` / `src/core/model/PartPlateList.h` (from 16-01 — the API to delegate to; note objectIndicesOnPlate, plateIndexForObject, plate(i)->config())
- `src/core/services/ProjectServiceMock.cpp:1312-1342` (the stubbed plateScoped methods being fixed)
- `src/core/services/ProjectServiceMock.h:49-128` (the full plate Q_PROPERTY/Q_INVOKABLE surface — every signature to preserve)
- `src/core/viewmodels/EditorViewModel.cpp` (find plate-related reads — confirm what EditorViewModel depends on so the migration doesn't break it)
    </read_first>
    <acceptance_criteria>
- `grep -c "plateCount_\|plateNames_\|plateLockedStates_\|m_mockPlateOverrides" src/core/services/ProjectServiceMock.cpp` returns 0
- `grep "m_plateList->" src/core/services/ProjectServiceMock.cpp` returns matches (the delegation calls)
- `grep "setPlateScopedOptionValue" src/core/services/ProjectServiceMock.cpp` — the method body no longer contains `return false;` as the sole HAS_LIBSLIC3R behavior (it now writes DynamicPrintConfig and returns true under HAS_LIBSLIC3R)
- All existing Q_INVOKABLE plate method signatures in ProjectServiceMock.h are unchanged
- The build succeeds (T4 verification) — confirms the rewrite is complete and consistent
    </acceptance_criteria>
  </task>

  <task id="16-02-T3">
    <action>
Update the 3MF load path in `src/core/services/ProjectServiceMock.cpp` (`loadProject`, currently around lines 4656-4803) to populate `m_plateList` from the `plateDataList` returned by `Slic3r::Model::read_from_archive`. Today the reconstruction at lines 4776-4799 reads `plate->plate_name` + `plate->objects_and_instances` into the parallel vectors (loadedPlateNames, loadedPlateObjectIndices) — rewrite it to build PartPlate objects in m_plateList.

For each `PlateData` entry from plateDataList:
1. `PartPlate* p = m_plateList->createPlate()` (or reuse the initial plate for index 0)
2. `p->setName(plate->plate_name)` (convert std::string ↔ QString as needed)
3. For each (objIdx, instIdx) in `plate->objects_and_instances`: `p->addInstance(objIdx, instIdx)` — this populates the instance-level membership (D-03). The current code only tracks object indices; upgrade it to parse the instance dimension from objects_and_instances (which is a vector of pairs per upstream bbs_3mf.hpp:71-121).
4. Copy `plate->locked`, `plate->config` (DynamicPrintConfig — direct assign), `plate->bed_type`, filament_maps, layer sequences if present in PlateData, into the PartPlate's corresponding fields.

This makes the load path instance-aware and DynamicPrintConfig-aware (Phase 18 will add the matching write path; Phase 16 just needs load to not break and to populate the new model correctly).

Ensure `m_plateList` is reset/rebuilt cleanly at the start of loadProject (clear old plates, start fresh) so reloading doesn't accumulate stale plates. The constructor-initialized first plate may need explicit handling (delete the auto-created empty plate before loading, or reuse it for index 0).
    </action>
    <read_first>
- `src/core/services/ProjectServiceMock.cpp:4656-4803` (loadProject — the full load path including read_from_archive + the reconstruction at 4776-4799)
- `third_party/OrcaSlicer/src/libslic3r/Format/bbs_3mf.hpp:71-121` (PlateData structure — plate_name, locked, config, objects_and_instances, filament_maps; the fields available to populate PartPlate)
- `src/core/model/PartPlate.h` (from 16-01 — the setters to call: setName, addInstance, config accessors, setLocked, bed-type setters)
- `src/core/model/PartPlateList.h` (from 16-01 — createPlate, plate(i) for population)
- `.planning/phases/16-partplate-data-model-foundation/16-CONTEXT.md` (integration point: load path must adapt to instance-level + DynamicPrintConfig per D-03/D-04)
    </read_first>
    <acceptance_criteria>
- `grep -c "loadedPlateNames\|loadedPlateObjectIndices" src/core/services/ProjectServiceMock.cpp` returns 0 (old reconstruction variables removed) OR the remaining occurrences are only in non-active/dead code paths
- The load path contains calls to `createPlate()` and `addInstance(` on the new model (populating m_plateList from PlateData)
- The load path reads `plate->objects_and_instances` at the instance level (object+instance pairs), not just object indices
- A loaded multi-plate 3MF project produces the correct plateCount() and plate names (verified in T4 by the existing plate-load smoke test, if any, or a manual load assertion)
    </acceptance_criteria>
  </task>

  <task id="16-02-T4">
    <action>
Run the canonical verification command. This is the gate confirming the big-bang migration preserved all existing behavior (PLATE-06) while moving storage to PartPlateList. Run it NOW — do not batch further changes. If it fails, fix the specific failures (missing signal, broken EditorViewModel read, load-path crash) before proceeding.
    </action>
    <read_first>
- `.codex/rules/build-rules.md`
    </read_first>
    <acceptance_criteria>
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` exits 0
- `build/ViewModelSmokeTests.exe` reports 37 passed, 0 failed (the 5 new from 16-01 + the existing 32 — none regress)
- `build/QmlUiAuditTests.exe` reports 7 passed, 0 failed (no QML changed)
- `build/E2EWorkflowTests` passes (exercises load → plate ops → slice end-to-end — confirms the migration didn't break real workflows)
- No existing plate behavior regresses: add/delete/rename/lock/select all still work (covered by the smoke + E2E tests)
    </acceptance_criteria>
  </task>

  <task id="16-02-T5">
    <action>
Add a regression test to `tests/ViewModelSmokeTests.cpp` proving the storage migration preserved behavior end-to-end through ProjectServiceMock: `projectServicePlateOpsBackedByPartPlateList()`. Construct/use the existing ProjectServiceMock instance (match how other tests obtain it), call addPlate() → assert plateCount() increased; renamePlate(last, "Test") → assert plateNames().last()=="Test"; setPlateLocked(0, true) → assert isPlateLocked(0)==true; deletePlate(last) → assert plateCount() decreased; assert setCurrentPlateIndex(0) works and currentPlateIndex()==0. This locks in PLATE-06 (existing behavior preserved after the big-bang migration).

If ProjectServiceMock construction in tests requires specific setup (model_ initialization, HAS_LIBSLIC3R), match the pattern of an existing ProjectServiceMock-touching test in the file. Do not remove/modify existing tests.
    </action>
    <read_first>
- `tests/ViewModelSmokeTests.cpp:160-270` (existing test patterns + how ProjectServiceMock is accessed in tests)
- `src/core/services/ProjectServiceMock.h:61-128` (the plate API the test exercises)
- `src/core/services/ProjectServiceMock.cpp` (from T2 — the migrated implementations under test)
    </read_first>
    <acceptance_criteria>
- `grep "projectServicePlateOpsBackedByPartPlateList" tests/ViewModelSmokeTests.cpp` returns a match
- `build/ViewModelSmokeTests.exe` reports 38 passed, 0 failed (37 from 16-01/T4 + this 1 new)
- canonical verify still exits 0
    </acceptance_criteria>
  </task>

</tasks>

<verification>
- Canonical build passes: `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` exits 0
- 38 unit tests pass in ViewModelSmokeTests (5 from 16-01 + 1 new from this plan + 32 baseline preserved)
- No parallel-vector plate storage remains in ProjectServiceMock (all 9 vectors + plateCount_ + m_mockPlateOverrides deleted)
- PartPlateList is the single source of truth; plateScopedOptionValue/setPlateScopedOptionValue operate on DynamicPrintConfig (stub fixed)
- Load path populates PartPlateList from PlateData at instance granularity
- Existing plate behavior (add/delete/rename/lock/select) preserved end-to-end (PLATE-06) — proven by the new regression test + unchanged smoke/E2E tests
</verification>

<notes>
This is the destructive half of Phase 16. Run canonical verification at T4 (after the migration) and again at T5 end — not just once at the very end. If EditorViewModel or any QML binding breaks, the cause is almost certainly a dropped NOTIFY signal or a changed return-value semantics in a rewritten method — grep the old implementation for `emit` before finalizing each rewrite.
</notes>
