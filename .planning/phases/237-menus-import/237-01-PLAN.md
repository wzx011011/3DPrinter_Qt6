# Phase 237 Plan — Menus, Shortcuts, Drag-Drop, And Import

**Requirements:** VIEW-01..06
**Evidence:** `.planning/research/v5.16-GAP-BASELINE.md` menus/import sections

## Plan 1 (single batch)

### Task 1 — VIEW-01: View menu
- Replace the disabled stub (BBLTopbar.qml:866-889) with a working View menu:
  6 camera presets Ctrl+0..6 (route to existing requestViewPreset on the
  active viewport), perspective/orthographic toggle (camera controller),
  Show G-code window toggle (PreviewPage panel), Show overhang / outline
  (viewport render flags; overhang exists as concept in upstream
  GLCanvas3D). Items without a real consumer stay honestly gated.
### Task 2 — VIEW-02: Edit/File completion + shortcuts
- Delete All Ctrl+D (clearWorkspace exists, zero callers; add confirm),
  Import Zip Archive (delegates to FileArchiveDialog from Phase 236),
  Import Configs (load config ini/json → preset/apply path), bind missing
  shortcuts Ctrl+N/Ctrl+Shift+S/Ctrl+A/Esc/Ctrl+P/Ctrl+0..6/Ctrl+E/C
  (fix KBShortcutsDialog Ctrl+D label while at it).
### Task 3 — VIEW-03: file drag & drop
- Window-level DropArea (main.qml): accept 3mf/stl/obj/step/amf/svg/zip;
  multi-file → addFilesToPlate(current), single .svg → SVG gizmo point-drop
  flow. Activate window + switch to Prepare (upstream PlaterDropTarget).
### Task 4 — VIEW-04: import unit inference + notices
- looks_like_saved_in_meters/imperial (upstream Plater.cpp:4234-4253) on the
  loaded mesh bbox → conversion confirm dialog; zero-volume object notice
  (:4231-4287).
### Task 5 — VIEW-05: project load restores embedded config
- topbarOpenProject → loadFile uses LoadStrategy::LoadModel only
  (ProjectServiceMock.cpp:678-686). Route .3mf project opens through
  loadProject (LoadModel|LoadConfig) so embedded config/presets apply
  (projectConfigLoaded → applyProjectConfig wiring exists,
  BackendContext.cpp:177-179).
### Task 6 — VIEW-06: .gcode.3mf export + scale-to-fit
- Export plate sliced file: package current plate's gcode + plate 3mf block
  into .gcode.3mf (upstream export_gcode_3mf, Plater.cpp:11499) via zip
  writer (QZipWriter/miniz available).
- scale_selection_to_fit_print_volume: new EditorViewModel invokable scaling
  selection to bed bbox (upstream Plater.cpp scale_selection_to_fit).

## Verification

- Canonical build + all suites; QML audit test asserting View menu items are
  enabled and shortcut list parity; manual drag-drop smoke.
