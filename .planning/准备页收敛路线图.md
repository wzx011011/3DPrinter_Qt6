# 准备页 → OrcaSlicer 对齐：收敛路线图

> 建立日期：2026-07-02
> **2026-07-02 修订**：补交互级审计（可见 + 可点 + 后端非 no-op + 非 stub），基线表降级 + 17 处假绿清单 + 修复优先级。审计 quick 任务：`.planning/quick/260702-1bn-prepare-roadmap-audit-downgrade/`
> 范围：准备页（Prepare 工作区）相对上游 OrcaSlicer v7.0.1 的差距收敛
> 依据：截图视觉对比 + `src/` 代码现状核查 + `third_party/OrcaSlicer/src/slic3r/GUI/` 上游真值 + `docs/源码对照迁移任务追踪.md`
> 关联 plan：`~/.claude/plans/orcaslicer-imperative-kernighan.md`

---

## 1. 背景：截图版本与代码现状的偏差

用户提供的对比截图（`shotScreen/准备页.png`）经核实为 **Phase 50/51 重构之前**的旧构建：
- 左侧栏仍是 `PrintSettings.qml` 详细参数面板（层高/接缝/精度/墙壁…）
- 顶部仅 4 个导航标签

当前 `main` 分支已经过：
- **Phase 52**（左侧栏与 Preset 控件，PREPSB-01..05，VERIFICATION **PASS**）
- **Phase 53**（对象/平板/视口工作流，PREPWF-01..06，VERIFICATION **PASS**）

重构后左侧栏 = `LeftSidebar.qml`（preset 选择器 + 对象列表 + 全局/对象/盘作用域 + 搜索），顶部 = `BBLTopbar.qml`（8 标签）。证据：`panels/DockableSidebar.qml:99` 装载 `LeftSidebar`；`BBLTopbar.qml:280-336` 全套 8 标签。

**结论**：截图给人的"差距很大"是错觉。最新代码骨架层已基本对齐上游，真正的核心缺口集中在两处：
1. 左侧栏参数面板 page_view 7-Tab 占位（`LeftSidebar.qml:586-656`，标注 Phase 56）
2. OpenVDB 阻塞的涂刷/挖空 GL 层（SupportPaint/SeamPaint/Hollow/MMU）

2026-07-02 验证：`scripts/auto_verify_with_vcvars.ps1` 全链路通过（configure + 254/254 编译 + ViewModel/QML-UI/E2E 测试全绿，exit 0）。

### 1.1 2026-07-02 交互级审计修订（重要）

**初版基线表的"已对齐"判定过度乐观**——当时用的是视觉级门槛（看截图大结构在 + QML 有对应控件就给 ✅），没有逐入口验证可点性、后端是否 no-op、是否 stub。本次按新标准（可见 + 可点 + 后端非 no-op + 非 stub）对 10 行基线**逐入口核查到 `.cpp` 实现层**，发现 **4 行含误导性假绿，共 17 处**"控件在但点击被吞 / 后端空 / 只 log / 假数据"的入口。详见第 2-4 节。判定标准记于 `memory/alignment-verification-standard.md`。

---

## 2. 已对齐基线（2026-07-02 交互级审计后）

**判定枚举**：✅ 真绿（可见 + 可点 + 后端真通）｜⚠️ 降级（含误导性假绿，见第 3 节）｜ℹ️ INFO（基础设施，非直接入口）

| # | 模块 | 初版 | 审计判定 | 关键证据（file:line） |
|---|---|---|---|---|
| 1 | 顶部 8 页导航 + 完整文件菜单 | ✅ | ✅ **真绿** | `BBLTopbar.qml:280-336/478-592`；49 个入口全通、0 假绿；占位项 `enabled/visible:false` 诚实门控 |
| 2 | 左侧栏 preset 三选择器 + dirty dot + 兼容性灰化 + 值来源链 | ✅ | ⚠️ **降级（4 假绿）** | 齿轮⚙/↻ DEAD（`CollapsibleSection.qml:84-88` 吞点击）、工艺 ☰ STUB、颜色 picker STUB（`FilamentSlot.qml:68`）、耗材缺编辑/添加按钮 |
| 3 | 作用域切换（全局/对象/盘） | ✅ | ✅ **真绿** | `LeftSidebar.qml:262-302`；`ConfigViewModel.cpp:895-927` 真改 `settingsScope_` + 真过滤可写键 + 真叠加 plate/object 覆盖 |
| 4 | 搜索栏 + Ctrl+F SearchDialog | ✅ | ✅ **真绿** | `PrintSettings.qml:1521-1538` SearchDialog 全实现；左侧栏搜索过滤真做但 UI 无消费（Phase 56） |
| 5 | 对象列表（树形 + volume 子级 + 分组 + 拖拽重排序 + 跨盘拖拽 + 完整右键菜单） | ✅ | ⚠️ **降级（6 假绿 + 标注不实）** | "在参数表中编辑"4 处 DEAD、简化/导出 STL/修复/重载 STUB、**分组功能实际缺失**（初版误标） |
| 6 | 热床 + 多盘 plate tabs（最多 36 盘、缩略图、盘级耗材顺序） | ✅ | ✅ **真绿** | `PreparePage.qml:2973-3136`；增删/复制/锁定/打印序列/耗材顺序全通 |
| 7 | 切片引擎（真实 libslic3r 路径） | ✅ | ✅ **真绿** | `SliceService.cpp:325-765` 真跑 `Print::apply/process/export_gcode`（`#ifdef HAS_LIBSLIC3R` 内真路径） |
| 8 | EditorViewModel（200+ Q_PROPERTY/Q_INVOKABLE） | ✅ | ℹ️ **INFO** | 基础设施，非直接入口；少量 Q_INVOKABLE 是 stub 菜单死路径（`simplifyMeshSelected`/`meshBooleanSelected`，见第 3 节 #6） |
| 9 | Move/Rotate/Scale/Flatten/Cut/AdvancedCut/Measure/MeshBoolean/Emboss/SVG/Simplify 控制面板 | ✅ | ⚠️ **降级（4 假绿）** | Move/Rotate/Scale/Cut/AdvancedCut/Emboss/SVG/Simplify 面板真通；**Flatten/Measure/MeshBoolean/Drill STUB** |
| 10 | 值来源可视化、修改选项摘要栏、视口告警覆盖层 | ✅ | ✅ **基本真** | 值来源链/重置/修改计数全实现（`ConfigViewModel.cpp:1470-1538`）；告警覆盖层未单独深核 |

**小结**：10 行 → 4 真绿（#1/3/6/7）+ 1 基本真（#10）+ 1 INFO（#8）+ **4 降级（#2/5/9 + #4 小瑕疵）**。真正"用户会踩坑"的假绿集中在 #2 / #5 / #9 三块，详见第 3 节。

---

## 3. 误导性假绿清单（17 处，按根因分类）

> **"误导性"** = 控件可见（甚至可点）但点击被吞 / 后端空 / 只 log / 假数据。**不含**诚实门控的 HIDDEN 项（见 3.5）。
> 每条均经审计员读 `.cpp` 实现层确认（不只读 `.h` 声明）。

### 根因 A — `CollapsibleSection` MouseArea 吞点击（1 处代码，3 个症状）

`CollapsibleSection.qml:84-88` 标题栏 `MouseArea { anchors.fill: parent }` 声明在 `actionsRow`（`:69-72`）之后、同 z 值 → 处于顶层，吞掉所有注入 `rightActions` 的按钮点击。

| # | 入口 | 判定 | 证据 file:line | 说明 |
|---|---|---|---|---|
| 1 | 打印机齿轮⚙ → ConfigWizard | DEAD | `LeftSidebar.qml:50-56`；`CollapsibleSection.qml:84-88` | 后端真通（`BackendContext.cpp:564`→`main.qml:578`），但点齿轮只折叠面板，弹不出 ConfigWizard |
| 2 | 耗材 ↻自动匹配 → autoMatchFilament | DEAD | `LeftSidebar.qml:175-182`；`ConfigViewModel.cpp:740-755` | 后端真做兼容匹配，但 ↻ 被吞点击——**点 ↻ 只折叠面板，永不触发** |
| 3 | 耗材不兼容红点 ToolTip | DEAD(hover) | `LeftSidebar.qml:166-174` | 后端真算（`ConfigViewModel.cpp:757-764`），但 hover 被 titleBar MouseArea 盖住 |

**修复**：给 `actionsRow` 设 `z:1`，或 titleBar MouseArea 改只覆盖标题文字区，或移到 RowLayout 之前声明。**一改修 3 个 DEAD**。

### 根因 B — CGAL 5.4 < 5.6+（版本阻塞）

| # | 入口 | 判定 | 证据 file:line | 说明 |
|---|---|---|---|---|
| 4 | MeshBoolean 面板"执行" | STUB | `ProjectServiceMock.cpp:2980` | 硬编码 `return false`，`:2982` 后全是死代码；面板可点、undo 命令齐全、CGAL 前置代码齐全。CGAL 版本不足 |
| 5 | Drill 面板"执行" | STUB | `ProjectServiceMock.cpp:3167` | 构建完整 cylinder 工具 mesh + feature 矩阵，末尾 `return false` |
| 6 | volume 右键"简化模型" | STUB | `EditorViewModel.cpp:2810-2815` | `qWarning + return false`，注释 "needs simplify dialog"。注：Simplify **面板**（`simplifySelected`）真通，死的是右键菜单这条路径 |

### 根因 C — 死信号 / 死路径（emit 无人接 / 自赋值 no-op）

| # | 入口 | 判定 | 证据 file:line | 说明 |
|---|---|---|---|---|
| 7 | "在参数表中编辑"（4 入口：工具栏"参数" + 对象右键 + volume 右键 + 全选路径） | DEAD | `ObjectList.qml:144/481/970`；`EditorViewModel.cpp:3062` | `requestSelectionSettings()` 仅 `emit selectionSettingsRequested()`，该信号**全仓库 0 连接**——点击完全无反应 |
| 8 | 对象右键"导出为 STL…" | STUB | `EditorViewModel.cpp:3046` | 只设 `statusText="请使用主菜单 文件>导出 STL"`，无任何导出 |
| 9 | 对象/volume 右键"修复网格" | STUB | `ProjectServiceMock.cpp:3569/3591` | 核心是 `m = vol->mesh(); vol->set_mesh(std::move(m))`——**自赋值 no-op**，注释自承 TriangleMesh 无 public repair() |
| 10 | 对象/volume 右键"从磁盘重新加载" | STUB | `ProjectServiceMock.cpp:3629` | 注释自承 "Reload is deferred"，实际走 fixMesh 同款自赋值，**未重读源文件** |
| 11 | 工艺 ☰setting → forwardSettingsRequest("process") | STUB | `LeftSidebar.qml:332-341`；`BackendContext.cpp:569-577` | 显式 no-op log（"pending Phase 56"），且 emit 的 `settingsRequested` 信号 `main.qml:576-592` **无 handler** |

### 根因 D — 上游功能未迁移（假数据 / 假行为 / 缺失）

| # | 入口 | 判定 | 证据 file:line | 说明 |
|---|---|---|---|---|
| 12 | Measure gizmo | STUB | `EditorViewModel.cpp:90` | 尺寸直接取整对象 bbox（`QVector4D(dx,dy,dz,dx*dy*dz)`），无点/边/面拾取、无射线检测。**最隐蔽假绿**——用户以为测局部，实际只看整体包围盒 |
| 13 | Flatten"平放"按钮 | STUB | `EditorViewModel.cpp:1117-1149` | HAS 路径是 `TODO: orientation::orient()` 空块；唯一生效是 mock 强制旋转 (0,0,0) + 伪造"候选面:6"。无凸包面计算 |
| 14 | 分组功能 | MISSING | `ObjectList.qml`（全文） | 初版基线表误标"分组"。实际只有只读 groupHeader（按 module_name），**无分组/拆分分组任何入口**；合并只在 `PreparePage.qml:405` 空白右键 multiContextMenu |
| 15 | plate 菜单"添加图元 > 文字" | DEAD | `EditorViewModel.cpp:1036` | `addTextObject` 要求 `selectedSourceObjectIndex≥0` 且 `m_textContent` 非空，菜单直调未预置 → 必 early-return false |
| 16 | plate 菜单"添加图元 > SVG" | DEAD | `EditorViewModel.cpp:1070`；`PreparePage.qml:2918` | `importSVG` 要求 `m_svgFilePath` 非空，而 SVG 路径输入框 `enabled:false` 无法填入 → 必 early-return false |
| 17 | ObjectList "+ 导入模型"按钮 | DEAD | `ObjectList.qml:1232` | importMA 内 `enabled:false`——按钮可见但点击被吞 |

### 3.5 诚实门控项（HIDDEN，非误导，无需降级，仅记录）

GLToolbars Hollow(mode 8)/MMU(10)/FaceDetector(15)/SLA(18)：`EditorViewModel.cpp:4243-4247` `canActivateGizmo` return false → mask 不含 → 按钮 opacity 0.35 不可点 + status 文案说明阻塞原因（OpenVDB 未链接 / TriangleSelector 未迁移）。**这是正确的 gate 处理**——#4/#5 MeshBoolean/Drill 应学习此模式（当前它们是可点假绿，应改为门控禁用，见第 4 节 P1）。

---

## 4. 修复优先级（横向，跨 phase）

### P0 — 小改、高价值（建议立即，独立于 Phase 56）
- **修 `CollapsibleSection.qml:84-88` MouseArea**（给 `actionsRow` `z:1` 或限制 MouseArea 范围）→ 一改修第 3 节 #1/#2/#3 三个 DEAD
- **"在参数表中编辑" 4 入口（#7）**：接通到 Settings 页，或诚实隐藏（避免 4 个死入口）
- **耗材补编辑/添加 preset 按钮**：后端 `createCustomPreset`/`renamePreset`/`deletePreset` 已通（`PresetServiceMock`），QML 无入口

### P1 — 诚实化（把假绿降为明确禁用 + 原因提示）
- **#4/#5 MeshBoolean/Drill 面板按钮**：加 `enabled:false` + tooltip"需 CGAL 5.6+"，别做成可点假绿（学习 3.5 的 Hollow 门控模式）
- **#6 volume 右键"简化模型"**：指向 Simplify 面板（`simplifySelected`，已通），或隐藏菜单项
- **#8/#9/#10 导出 STL / 修复网格 / 重载**：接主菜单导出，或禁用 + 提示"导入时已修复 / 请用文件菜单"

### P2 — 上游功能迁移（工作量大，专项）
- **#12 Measure**：迁移 GLGizmoMeasure 点/边/面拾取 + 射线检测
- **#13 Flatten**：迁移凸包面选择（`orientation::orient` 真路径）
- **#14 分组/合并**：承接上游 `GUI_ObjectList` group/ungroup 行为，对象列表右键接入
- **Layer 编辑**（`GLToolbars.qml:102` `enabled:false`）：变量层高编辑器（后端 `layerRangeCount` 已通，QML 未渲染）

### P3 — 已诚实门控（无需动）
- Hollow/MMU/FaceDetector/SLA（OpenVDB 阻塞）
- Account/ModelStore/Publish 占位（`BBLTopbar.qml:226-257` `visible:false`）

---

## 5. 缺口分级与分阶段路线图

> 注：第 4 节 P0 的三项（CollapsibleSection 修复 / 耗材按钮 / "在参数表中编辑"）是独立小改，**建议在 Phase 56 之前先行落地**，因它们不依赖参数面板 page_view 工作。

### Phase 56 — 左侧栏参数面板 page_view 落地【P0，最大缺口】

- **缺口**：`LeftSidebar.qml:586-656` ParamsPanel 的 7 个子 Tab 按钮（Print/盘/对象/部件/层/耗材/打印机）已存在，但参数列表区是占位文本 `"参数列表暂不可用"`（`:641-648`），`paramsTabBar.currentTab` 切换未真正过滤参数。真实参数编辑目前只在独立视图 `PrintSettings.qml`。
- **目标**：把 `PrintSettings.qml:1143-1199` 已有的 `activeCategories` + `pageFilteredIndices(category, page)` 双层过滤 Repeater 接到 `LeftSidebar` ParamsPanel，与全局/对象/盘作用域联动。
- **复用**（均已存在）：`ConfigViewModel::filterIndicesByCategory / filterIndicesByPage / setValue`、值来源链 `valueSourceForKey/valueChainForKey`、修改计数 `optIsDirty`、滚动到高亮 `scrollToHighlight`。
- **同时收敛**：Simple/Advanced/Expert 三档可见性（`LeftSidebar.qml:304-326`，标注 Phase 56）；频变参数 og_freq（层高/填充/支撑聚合控件，对齐上游 `Sidebar::og_freq_chng_params`）。
- **验证**：PREPSB-04（basic/advanced + search/filter）；7-Tab 切换真实过滤参数；频变区与详细参数双向同步。

### Phase 56b — Gizmo 入口补齐 + stub 清理【P2】

- **缺口**：Hollow/MMU/Drill/FaceDetector/Text/SlaSupports 在 `GLToolbars.qml` 无独立按钮；`EditorViewModel.h:384-387` 的 `simplifyMeshSelected`/`meshBooleanSelected` 注释为 stub。
- **目标**：按 `availableGizmoMask` 补齐 GLToolbars 按钮（VDB 依赖项保持 gate，不得"看起来能用"），清理 stub 注释。
- **验证**：每个 gizmo 入口可达且按 mask 门控；VDB 依赖项明确不可点并给出原因提示。

### Phase 57 — 涂刷/挖空 GL 层【P1，受 OpenVDB 阻塞】

- **缺口**：SupportPaint/SeamPaint/Hollow/MMU 的 GL 渲染 + TriangleSelector 数据层缺失（对齐上游 `GLGizmoPainterBase`）。
- **阻塞**：OpenVDB 未链接（Windows 构建依赖缺失）。
- **前置子阶段 57.0**：评估 OpenVDB 接入可行性；若不可接入，确定长期 gate 策略（保持 Phase 53 隐藏入口）。
- **若可接入**：迁移 TriangleSelectorPatch + per-triangle paint state + 笔刷渲染（半径/Ctrl+滚轮、bucket fill、smart fill、笔触 undo/redo）。
- **验证**：涂刷可见、可保存、随对象 3MF 持久化、撤销重做。

### Phase 58 — Plate 真实架构【P1，根基阻塞 P0.5.5】

- **缺口**：上游 `PartPlateList` 依赖 wxWidgets/OpenGL，Qt6 侧当前为功能性 Mock（`ProjectServiceMock` plate 部分）。
- **目标**：在 libslic3r 之上重建 Qt6 侧 PartPlateList 适配层（不引 wxWidgets）。
- **影响**：多盘深度行为（盘级打印序列 ByDefault/ByObject/ByLayer、per-plate 切片/导出、盘锁定）。
- **验证**：M0.5 里程碑闸门；plate 增删/复制/导出与上游行为对齐。

### Phase 59 — 视觉像素对齐 + 收尾【P3】

- **缺口**：底部参数 hint 提示栏（hover 说明）；AssembleView（`Plater.qml:99-112` 占位）；Shell 渲染（P2.8.2）；像素级配色/间距/滚动条；账号/模型库/Publish 占位（`BBLTopbar.qml:226-257`）。
- **目标**：与 OrcaSlicer 截图像素级对齐；完成 `docs/v3.6-ui-inventory.md` 的 PREP-* 9 区域手动 UAT。
- **验证**：截图对比 PASS + 手动 UAT 签字；更新 `docs/源码对照迁移任务追踪.md` 状态符号。

---

## 6. 依赖与阻塞关系

```
P0 三项（CollapsibleSection/耗材按钮/在参数表中编辑）── 独立小改，建议最先
Phase 56  (参数面板 page_view)      ── 独立，最大缺口
Phase 56b (Gizmo 入口/stub)         ── 独立
Phase 57  (涂刷/挖空 GL)            ── 被 OpenVDB 阻塞，需先做 57.0 评估
Phase 58  (Plate 真实架构)          ── 独立但工作量大（架构级）
Phase 59  (视觉像素对齐收尾)        ── 依赖 56/56b；Shell 项被 libslic3r 集成阻塞
```

建议执行顺序：**P0 三项 → 56 → 56b →（57.0 评估 → 57 或长期 gate）→ 58 → 59**。

---

## 7. 关键文件锚点

| 阶段 / 优先级 | 关键文件 |
|---|---|
| P0（第 4 节） | `src/qml_gui/components/CollapsibleSection.qml`（MouseArea :84-88）、`src/qml_gui/panels/LeftSidebar.qml`（耗材 section :158-216）、`src/qml_gui/panels/ObjectList.qml`（"在参数表中编辑" :144/481/970）、`src/core/viewmodels/EditorViewModel.cpp`（`requestSelectionSettings` :3062） |
| 56 | `src/qml_gui/panels/LeftSidebar.qml`（ParamsPanel 7-Tab，586-656）、`src/qml_gui/panels/PrintSettings.qml`（复用参数渲染，1143-1199）、`src/core/viewmodels/ConfigViewModel.h`（filter/setValue API） |
| 56b / P1 | `src/qml_gui/components/GLToolbars.qml`、`src/core/viewmodels/EditorViewModel.h/.cpp`（stub，384-387 / 2810-2822）、`src/core/services/ProjectServiceMock.cpp`（meshBoolean :2980 / drillObject :3167 硬编码 return false）、`src/qml_gui/Renderer/GLViewport.h`（GizmoMode/availableGizmoMask） |
| 57 | `src/core/rendering/SupportPaintTypes.h`、`GLViewportRenderer`（笔刷渲染）、新增 TriangleSelector 适配 |
| 58 | `src/core/services/ProjectServiceMock.h/.cpp`（plate 部分）、新增 PartPlateList 适配层 |
| 59 | `src/qml_gui/pages/PreparePage.qml`、`BBLTopbar.qml`、`Plater.qml`（AssembleView，99-112） |

---

## 8. 验证方式（端到端）

- **构建**：`scripts/auto_verify_with_vcvars.ps1`（唯一权威路径，唯一构建目录 `build/`）
- **自动化**：smoke test（ViewModel + QML UI audit + E2E）+ `tests/ViewModelSmokeTests.cpp`（QSignalSpy）
- **视觉**：`shotScreen/` 最新截图 vs OrcaSlicer 上游截图，按 `docs/v3.6-ui-inventory.md` PREP-* 9 区域对照（PREP-TOP/SIDEBAR/OBJLIST/VIEWPORT/VTOOLBAR/GIZMOFLOAT/PLATEBAR/SLICESTATUS/VIEWOPTS）
- **交互级审计**（本次新增标准）：判定"对齐"前逐入口核查 **可见 + 可点 + 后端非 no-op + 非 stub** 四条，读 `.cpp` 实现层而非 `.h` 声明；所有 `visible:false`/`active:false`/`TODO(Phase X)`/只 `emit` 未接的 signal 必须显式列入缺口，不得藏在"已对齐"行。详见 `memory/alignment-verification-standard.md`
- **文档**：每个 phase 完成后更新 `docs/源码对照迁移任务追踪.md` 状态符号（`[x]`/`[-]`/`[ ]`）+ `.planning/STATE.md`，遵循 `.claude/rules/source-truth-migration.md`"不得仅因页面存在就标完成"规则
