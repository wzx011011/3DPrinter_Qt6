---
status: complete
date: 2026-07-08
---

# Quick Task 260708-e60 Summary

## Completed

Added extensible GUI startup deep-link arguments for `OWzxSlicer.exe`.

## Changes

- `src/qml_gui/main_qml.cpp`
  - Added `--open-page <page>`.
  - Added repeatable `--open-dialog <dialog>`.
  - Added `--skip-first-run` as a non-persistent launch-only wizard suppressor.
  - Added centralized page and dialog route registries.

- `src/qml_gui/main.qml`
  - Uses `startupSkipFirstRun` to avoid opening the first-run wizard only for the current launch.

- `tests/QmlUiAuditTests.cpp`
  - Added `guiStartupDeepLinkArgumentsAreExtensible` to lock CLI parser, route registry, settings-dialog routes, and non-persistent first-run suppression.

## Verification

- Targeted QML audit:
  `.\build\QmlUiAuditTests.exe guiStartupDeepLinkArgumentsAreExtensible`
  - 3 passed, 0 failed.

- Canonical verifier:
  `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
  - Build succeeded.
  - PrepareSceneData, PartPlate, ViewModelSmoke, QML UI audit, PreviewParser, and E2E tests passed.

- Runtime deep-link launch:
  `.\build\OWzxSlicer.exe --skip-first-run --open-page prepare --open-dialog settings:process`
  - App process launched and responded.
  - `startup_diagnostics.log` recorded:
    - `Startup open-page handled: prepare`
    - `Startup open-dialog handled: settings:process`

## Notes

- The previous canonical verifier failure at `PartPlateTests.cpp.obj` did not reproduce; a full rerun completed successfully.
- `owzx-cli` remains a headless slicing CLI. QML page/dialog opening is intentionally implemented on the GUI executable.
