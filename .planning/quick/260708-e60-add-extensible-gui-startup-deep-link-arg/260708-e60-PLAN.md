---
status: in_progress
created: 2026-07-08
---

# Quick Task 260708-e60: Add Extensible GUI Startup Deep-Link Arguments

## Goal

Add repeatable GUI startup arguments so visual verification can open pages and dialogs without simulated mouse clicks.

## Scope

- Add `OWzxSlicer.exe` GUI arguments:
  - `--open-page <page>`
  - `--open-dialog <dialog>`
  - `--skip-first-run`
- Keep `owzx-cli` as the headless slicing CLI; do not mix QML window routing into it.
- Route startup actions through centralized page/dialog registries so future dialogs can be added by one table entry.
- Add source/QML audit coverage for the startup deep-link contract.

## Verification

- RED: new QML UI audit fails before implementation.
- GREEN: targeted QML UI audit passes.
- Encoding guard passes for touched files.
- `git diff --check` exits 0.
- Canonical verifier passes:
  `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1`
- Launch app with:
  `.\build\OWzxSlicer.exe --skip-first-run --open-page prepare --open-dialog settings:process`
