# Quick Task 260705-e8x: Fix RHI gizmo depth overlay and viewport drag review findings

## Scope

Fix the three review findings from the v3.8 RHI viewport pass:

- RHI gizmo pipelines must render as an overlay and not fail against model depth.
- Left-button drag that starts on a model surface must still orbit the camera after it becomes a drag, unless an active gizmo drag has started.
- Rotate gizmo geometry must reserve vertex count, not byte count.

## Plan

1. Add regression tests/audit guards for the three findings.
2. Change the RHI gizmo pipeline depth state to match the overlay behavior.
3. Change RhiViewport mouse move handling so click-to-select still works, while dragged movement over a model orbits.
4. Fix rotate gizmo reserve sizing.
5. Run focused tests, the canonical verifier, and launch `build/OWzxSlicer.exe`.
