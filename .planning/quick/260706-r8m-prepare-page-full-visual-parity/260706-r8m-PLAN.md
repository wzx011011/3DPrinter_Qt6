---
quick_id: 260706-r8m
slug: prepare-page-full-visual-parity
status: complete
date: 2026-07-06
design: docs/superpowers/specs/2026-07-06-prepare-page-visual-parity-design.md
---

# Quick Task 260706-r8m: Prepare page full visual parity

## Goal

Fix the remaining Prepare page visual differences against `shotScreen/准备页.png` after the left sidebar pixel restoration.

## Approved Scope

- Central viewport visual state and bed framing.
- Top viewport toolbar geometry and disabled/icon treatment.
- Right floating viewport tool strip geometry and opacity.
- Bottom status/action strip placement and color.
- Deterministic visual-compare state only where needed to separate layout differences from runtime data differences.

## Acceptance

- Current 2560x1400 Prepare screenshot aligns with target major region boundaries.
- Left sidebar stays aligned with the previous `400px` target boundary tolerance.
- Structural tests fail before implementation and pass after restoration.
- Pixel evidence is saved under `build/pixel-audit/`.
- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` passes.

## Plan

1. Capture current and target screenshots and compute region-level visual deltas.
2. Add failing structural tests for top toolbar, right floating strip, bottom status strip, and visual-compare state gates.
3. Implement scoped QML/renderer/default-state fixes.
4. Re-capture and compare screenshots at 2560x1400.
5. Run encoding guard, focused tests, and the canonical verifier.
6. Commit code and GSD records.

## Result

- Completed in code commit `b62e224`.
- Restored the Prepare page shell chrome to a two-row workflow layout with Prepare-specific slice/export controls.
- Repositioned the viewport top action toolbar, right gizmo strip, and bottom view controls to the screenshot-driven anchors.
- Moved the bottom plate/status strip to the target viewport inset and adjusted Prepare viewport clear color toward the screenshot gray.
- Added `prepareFullVisualParityContract()` coverage in `tests/QmlUiAuditTests.cpp`.

## Verification

- `powershell -ExecutionPolicy Bypass -File scripts/auto_verify_with_vcvars.ps1` passed on 2026-07-06 after the implementation.
- `python %USERPROFILE%\.coding-encoding-guard\encoding_guard.py ...` passed for changed source/test files.
- `git diff --check` passed.
- Final visual evidence:
  - Current: `build/pixel-audit/prepare-full-visual-runtime-final-current.png`
  - Side by side: `build/pixel-audit/prepare-full-visual-final-current-side-by-side.png`
  - Diff: `build/pixel-audit/prepare-full-visual-final-current-diff.png`
  - Metrics: `build/pixel-audit/prepare-full-visual-final-current.json`

## Residual Visual Differences

The remaining visible differences are not QML layout differences: the reference screenshot includes a loaded Benchy and Creality bed-frame skin, while the normal current startup is an empty scene with the QRhi bed grid. Those belong to a renderer/default fixture and bed asset parity follow-up.
