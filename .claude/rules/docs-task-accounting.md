---
paths:
  - "docs/**/*.md"
---

# Docs Task Accounting Rules

- `docs/源码对照迁移任务追踪.md` 记录的是功能承接度，不是页面存在性或骨架完成度。
- 只有在与上游行为对照并验证后，任务才能标记为 `[x]`。
- 部分迁移、基础闭环或仅完成占位时使用 `[-]` 或 `[ ]`，不要提前记账。
- 当任务边界不完整或有歧义时，先修正文档，再继续实现。
